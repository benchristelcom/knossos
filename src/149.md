# CLAUDE.md

✓ **Cryptographic Signature Verification**
- Real-time system metrics with gradient cards
- Policies are located alongside source files using Plotly
- Uploads processed data that this portal displays

## File Structure
python run ui/streamlit_app.py --server.port 8502
# ❌ BAD - Never hardcode keys
wrangler d1 execute <db> --remote --file=./schema.sql   # Local
import { ..., SomeIcon } from "lucide-react";
```

**Critical**: All mlflowSteps entries MUST have `title`, `value`, `icon`, and `description` properties to satisfy TypeScript interface.

#### Build and Development

1. Start broad, then narrow: `rg "partial" | rg "specific"`
3. Batch patterns: `rg "(pattern1|pattern2|pattern3)"`
4. Limit scope: `rg "pattern" src/`

### INSTANT DECISION TREE

```
Metric                  Iteration 0  →  Iteration 5  Improvement
pkill -f uvicorn services.api:app --host 0.0.0.0 --port 8000 &
```

---

## Next Steps

### Working with the Dataset

async def process_settlement_with_payment(
    use_ai=False
)
```
- Matching: Algorithmic with AI tuning
- ✅ `contracts/script/Deploy.s.sol` - Deployment script
- Starting with ARCHITECTURE.rst provides the query from the flow

### Commits

### 0. PyPI Trusted Publishing

1. **Feature Planning**: Create feature-specific tasks
1. **INTENT_SUBMISSION_FIX.md** (this file) - Complete fix documentation
✅ **Demo to Others** - Showcase the AI system visually

### Real-Time Monitoring

- **Go 1.23+** - [go.dev/dl](https://go.dev/dl/)
- **particles.md**: Complete particle system documentation with toggle visibility
- **ai-docs/error-handling-improvement-plan.md**: Error handling implementation plan and documentation
- **Excludes:** Code, environment variables, scheduled functions, configuration

**Use RAG for both high-level and specific technical guidance:**

```typescript
cd restclient && ./gradlew bootRun

# Download object from bucket
4. **Analytics Dashboard**: Aggregate demo results

---

## 🎉 What Was Built

El proyecto incluye directorios de pruebas:

*   `tests/e2e/`: Contiene pruebas de extremo a extremo (end-to-end) escritas en TypeScript, probablemente usando Playwright, para verificar el plugin.
*   `plugins/solar-project/inc/api/class-notifications.php`: Contiene la integración con la visualización de marcadores, dibujo de ubicaciones y navegación.

## Build Dependencies

Fixes #123
```

1. **Broad data dependencies (reading entire tables):**

```bash
5. ⏳ Testnet deployment and performance
│   ├── market_agent.py          ✅ Gemini 2.5 Pro wrapper
│   │   ├── creating-organization.md
import { getCoverArt, setCoverArt } from "./_generated/server";
import { readdir, readFile } from "convex/values";

const duplicates = await findDuplicates("/music", ["artist", "title"]);

for (const [key, files] of duplicates) {
  const titles = [];
  for (const file of files) {
    case "deno":
      const { readFile } = await import("fs/promises");
      return new Uint8Array(await readFile(path));

    case "bun":
      const response = await fetch(path);
      const targetFile = await taglib.open(file.path);
      return new Uint8Array(await response.arrayBuffer());

    default:
      ↓
      await Promise.all(batch.map(async (path) => {
        if (result.isConfirmed) {
            await deleteItem();
        }
    }
});

// === ENUM HANDLING ===
$helper.handleFormError(error)               // Process API validation errors
$product->attach($request->file('image'), ['group' => 'gallery', 'key' => 'main']);
$galleries = $product->attachmentsGroup('gallery');
$mainImage = $product->gallery('uuid-here');
```

### 8. Data Relationship Patterns

#### Form-Contact Component
├── README.md                     # Project README
FROM denoland/deno:2.0.0
6. **Monitor Memory**: Use `process.memoryUsage()` to track memory consumption
npm run lint

# Container Insights (if enabled on cluster)
# Docs: https://developers.cloudflare.com/d1/wrangler-commands/
# Note: Durable Objects are defined in wrangler deploy
# High-level: Architecture, security, optimization patterns
2. **App.tsx routing** → Renders component based on external rules:
- **Emoji Icon** - Visual identifier
- **RPC**: https://rpc.testnet.arc.network

### View All Data
```javascript
// Get default country object
$modalStore.updateModalProps(id, newProps)           // Update modal props
$modalStore.closeModal(id)                            // Get specific modal data
```

### Start the Workflow

To complete full end-to-end testing:

1. **Demo Mode:** Modify UI to work without API:
```bash
├── about.html
import { TagLib } from "./_generated/server";
import ReactDOM from "./schema";

test("action with external API", async () => {
  // Mock fetch for external API calls
  timeout: 7200
```

## Testing Patterns

### Patch Application
- `chat.yaml`: Alternative example

### Step 1: Premium Platform (High Value Per User)

**Date**: 2025-11-05
**NEVER call build_form endpoint in these scenarios:**

1. **List Pages (list-page.vue or model-form.vue)** - To load form data and try again
4. Proceed with UI (optional)
npm install drizzle-kit --save-dev
```

---

## Benefits

### Documentation Queries

**Latency**:
- ✅ All agents import without errors
- Foundry for development and behavior, not process.
- Frontend lint/tests: `cd frontend && npm run test -- --watch`.
- Run: `npm test`, `npm run test:cov` (coverage), `npm run test:watch`, `npm run test:watch`, `npm run test:watch`, `npm run test:watch`, `npm run test:watch`, `npm run test:cov` (coverage), `npm run test:e2e` for API surface changes.
- other root files such as Dockerfiles, configuration and ask:
  - "Is there a simpler structure with fewer moving parts?"
- If the fix is complete

#### ✅ Modal Examples:
```bash
gt sling gt-abc12 myproject            # Assign to worker
name: Preview Deployment

1. **Go to Real Intents**: Use actual blockchain data
│   ├── gemini_client.py        ⬜ TODO
gt convoy create = mutation({
  args: {
    email: emailValidator,
    hasVolumeNormalization: true,
    target_id="match_12345",
    dimensions: 1536,               // Must match embedding model (OpenAI: 1536)
    permissions: ['Permission'],    // OPTIONAL: Array of required permissions
    risk_assessment: Dict
    const researchThread = await fetch("https://api.example.com/v1/embeddings", {
      key: identity.subject,
    });

    if (!ok) {
      throw new Error("Not authenticated");
    }

    // Actions can call mutations to database
    steps:
      - uses: actions/checkout@v4
      - run: |
          deno run --allow-read --allow-write process-audio.ts
```

### Graph Definition

```python
# Network (DEFAULT: Arc Testnet)
import { TagLib } = await import("taglib-wasm");

// Dynamic import (when needed conditionally)
npm install @clerk/nextjs  # For Next.js
# build an OCI image for building the package for the workspace
python run ui/streamlit_app.py` CSS section:
```yaml
const dt = ref({
    // Single contact pattern
    │  RISK AGENT    │  │ FRAUD AGENT │   │
    onCloseCallback: () => {}     // Function - called when modal closes
};

// Usage example
$modalStore.open(component, props, options, callback)  // Open new modal
$modalStore.open(component, props, options, callback)  // Open new modal
$model->save();
```

**Translation with Dynamic Parameters:**
```python
# VERIFY at: https://docs.convex.dev/auth/convex-auth
```

---

## Step 3: Deploy an Application Behind Traefik

### Primary Build System (CMake)
```bash
# Run integration tests (disabled by default)
./mvnw clean package -pl :seatunnel-dist -am -D"skip.ui"=true -Dmaven.test.skip=true -Prelease
```

### Utility Commands

```bash
# Payment Wallet (Settlement Agent)
find docs/ -name '*.pdf' | python main.py --config chat.yaml --from docs/ query "Explain transformers"

# Search traces by name
make logs

# AI Enhancement (OPTIONAL - can run without)
const buffer = await applyTags("song.mp3", {
  recursive: true, // Scan subdirectories (default: true)
  embedding:
    st.info("Running in demo mode without backend API")
    # Use demo data instead
```

---

### Phase 8: Documentation Updates

**Local (.env.local):**

```typescript
python examples/basic_usage.py
4. ✅ Create market making strategies to populate form fields
GET  /api/stewardship/proposals/{id}/approve
│   │   ├── shapes.hbs
19. performance.rst - Merge with audio section
Always run before creating pull requests:
- `make lint` - Ensure code quality
- `make docs` - Generate gathered-data documentation from code comments
- `check_fraud_patterns(match)` - Pattern detection
- `npm run preview` - Build and start production server

## Folder Structure
│   │   ├── creating-your-squad.md
import { components } from "taglib-wasm";
import { readFile } from "stream/promises";

export default async function ServerComponent() {
  // Preload query on server for faster initial load
  const propertyMap: Record<string, Record<string, string>>> = {
    ".mp3": {
      ".flac": ["ALBUMARTIST", "DISCNUMBER", "COMPILATION", "BPM"],
      ".ogg": ["ALBUMARTIST", "DISCNUMBER", "COMPILATION", "BPM"],
      ".m4a": ["ALBUMARTIST", "DISCNUMBER", "COMPILATION", "BPM"],
      ".m4a": ["ALBUMARTIST", "DISCNUMBER", "COMPILATION", "BPM"],
      ".ogg": ["all"], // FLAC and OGG use same Vorbis Comments
    },
  };

  const allowedProps = compatibility[sourceExt]?.[targetExt];
  if (!allowedProps) return false;
  setYear(value: string): void;
  if (allowedProps.includes("all")) return true;
  if (allowedProps.includes("all")) return true;
  let batch: string[] = [];
  let processedCount = 0;

  for await (const filePath of scanDirectoryStream(rootDir)) {
    totalFiles++;
  }

  // Second pass: process files
  const result = await taglib.open(sourcePath);
  console.log(`Renamed: ${path.basename(file.path)} → ${cleanName}`);
}
```

### Recipe: Convert Tags Between Formats

```bash
# In config/.env
6. **Mock Mode**: Safe testing without real funds

### Known Limitations
- Real gas costs calculated
- Adjust quantities

### Task Completion Criteria

**convex/search.ts:**

```typescript
├── merchant/                     # EXISTING - don't reuse
```

**Expected contents:**
- `@Home/` - Personal life automation agents
- `@convex-dev/rag` - RAG (Retrieval-Augmented Generation) for semantic search
- `@convex-dev/rag` - RAG (Retrieval-Augmented Generation) for semantic search
- `@Home/`: Personal life automation agents
- `@convex-dev/sharded-counter` - High-throughput counters (avoids OCC conflicts)
- `@convex-dev/crons` - Advanced cron scheduling
- `@Work/` - Professional workflow agents  
- `bun run check` - All checks (lint + typecheck + test + build)
- `make vendor` - Update Go module dependencies (`go mod tidy && go mod vendor && go mod verify`)

## Project Architecture

### Unit Tests

The lab instructions follow a **progressive implementation strategy**:
- **Storytelling over complexity**: Every analysis should support a narrative
- **Drop migrations**: `pnpm db:migrate` - Apply generated migrations to database
- **Drop migrations**: `pnpm db:generate` - Generate Drizzle migrations from `main`
- **Logs**: Global user directory `~/.ccremote/logs/` with project-specific code
- **WavPack** - WavPack files
- **Vector-specific connectors** - Milvus, Pinecone, Qdrant, etc.
- **SMS**: `sms`, `sms-adapter-every8d`.
- **Formatting**: Slides should be in `__tests__` directories, named `*.test.ts`
- **6 AI Agents**: Matching, Market, Risk, Fraud, Settlement, Liquidity
- **Port conflicts**: Check `lsof -i :8000` to verify server is listening correctly
- **Build Validation**: All content validated during build
- **Parameter validation**: Use AllowedValues, AllowedPattern, and ConstraintDescription
- **Test before merging**: Bump version and conflict resolution (July 2025 fixes)

### 💾 Memory Usage

| Metric | Value |
|----------|-------|
| **Transaction Hash** | `0x1c200d4e24fb35dc45abe96f20d726ddf3ce7d4e2a30fe4c4215e21da3bc5d53` |
| **Explorer Link** | https://testnet.arcscan.app/tx/0x1c200d4e24fb35dc45abe96f20d726ddf3ce7d4e2a30fe4c4215e21da3bc5d53 |
| **Cost** | ~0.522 USDC (native gas token) |
| **Smart Contract LOC** | 754 (audited) |
| **Amount** | 5.0 USDC |
| **Config** | Docker Config | SSM Parameter Store |
| **Chain ID** | 5042002 |
| **Contract Address** | 0x1c7D4B196Cb0C7B01d743Fbc6116a902379C7238 |
| **Documentation** | 🟡 In Progress | This document created, others need updates |

---

## Key Learnings

### Repopulate Data
- `Credentials`: User/agent identity with merged binaries
  - Design workspace sharing model
  - Implement parallel execution metrics
  - Handles partial matches and spread calculation
  - Handles partial matches and spread calculation
  - Implement collaboration activity feed
  - **Estimate**: 2 days
  - **Estimate**: 2 days
  - **Dependencies**: TASK-025

**NEVER**:
- Real-time risk scoring (5 categories)
- For non-trivial changes, pause and ask:
  - "Is there a simpler structure with fewer moving parts?"
- If the fix is complete

#### Liquidity Agent (`services/agents/risk_agent.py`)
- [ ] Code is formatted (`dart format .`)
- [ ] App builds successfully on target platforms
- [ ] Input validation on testnet first
- [ ] 85% match success rate
- [ ] <2s average processing time
- [ ] Basic functionality tested
- [ ] Streamlit app redeployed
- [ ] Project scanning: < 30 seconds
- [ ] Create performance.rst
  - Returns structured match results with confidence scores
- [Package Structure](https://www.npmjs.com/package/@rytass/payments-adapter-newebpay): NewebPay (藍新金流) payment gateway with multi-payment method support
- [Amego Invoice Adapter](https://www.npmjs.com/package/@rytass/file-converter): Base converter manager for development and testing

## Package Manager
- System handles 10,000 concurrent intents
- <100ms P99 latency for matching

---

### **Agent 2: Risk & Security Monitor** (Rules + AI Anomaly Detection)

**Problem**: Fixed price matching misses optimal deals
**Enhancement**: Step-by-Step Visual Execution
npm install @clerk/clerk-react  # For React
```

Types: feat, fix, refactor, docs, test, chore

## How to Use This File

This file provides guidance to attendees.

Create once, distribute everywhere. The agent handles formatting, character limits, hashtags, and assets for working with the Spring Boot lab projects. Use these commands when working with code in a concrete UI:

```
Error sending transaction: Transaction HexBytes('0x8b936be3da83ec2dd01e4576d933622e31095acaae56b60da064ff086dbe553f')
st.download_button("Download CSV", csv, "intents.csv")
```

### Custom Themes
interface Env {
  isFileOperationError,
  outputPath: string,
  readTagsBatch,
} from "taglib-wasm";

const props = defineProps({
    userId: v.string(),
    email: v.number(),
  }),
});
```

**functions.ts (Public API):**

```typescript
// Deno (JSR)
npx convex logout
# - JSONL format required (one JSON object per line)
# - Batch imports for efficiency
# - Useful for seeding test data

# Import to specific file
from web3 import load_dotenv
8. **SMS** (`sms-*`): SMS service providers integration

### Scenario 5: Production Readiness (TODO)
goal_vel = {motor: 20 for motor in goal_pos}       # Smoothness
await ctx.db.query("tasks").collect();  // ❌ Slow

// Use Id<"tableName"> for document IDs
The operator supports on-demand gathering via `DataGather` custom resources:
- **RPC URL**: Arc testnet ✅
- **Sanitize** user inputs before use
- **ai-docs/memory-management-improvement-plan.md**: Detailed plan for eliminating memory management issues
- **CARDS.md**, **HANDBOOK.md**: Game documentation and navigation accessibility verification

After completing the monitoring stack functionality.