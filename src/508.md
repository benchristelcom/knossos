# Arc Testnet + USDC Integration - Implementation Summary

**serve** - Start OpenAI-compatible API server:
```bash
archon:get_project(action="update", task_id="...", update_fields={"status": "review"})`
5. **Get Next Task** → `archon:manage_task(action="list", filter_by="status", filter_value="todo")`
6. **Repeat Cycle**

**What You'll See:**

```
Step 1: Matching Agent 🔄
  if (file.dynamics?.replayGainTrackGain) {
    return preg_match('/^[A-Za-z0-9_]{6,32}$/', $value);
});

Validator::extend('isUsername', function ($attribute, $value, $parameters) {
    console.log(`  ReplayGain: ${data.dynamics.replayGainTrackGain}`);
  }
  if (data.dynamics?.appleSoundCheck) {
    throw new Error("Not a valid audio file");
  }

  // Validate magic bytes
  6. Creates Insights Advisor links for each recommendation
- **USDC token**: Arc testnet ✅
- **Usage tracking**: Bill per user/team based on token consumption
- **Log Stack**: Loki (configurable), Promtail, Syslog-ng
- **Particle Effects**: Enhanced visual feedback with doubled particle counts and websocket logic.

## Testing Guidelines
- Full AP2 integration
- Verifies payment signatures
- Link to block explorer (mock)

**Core (always needed):**

```typescript
// routes/api/admin.php - Add to custom validation section (line 138+)
'is_number' => '错误的号码',
'is_fund' => 'Invalid amount',
```

**Available Custom Rules:**
- `src/lerobot/robots/so101_follower/so100_follower.py`

**Added Section**:
```
services/agents/
│   └── style.css
const convex env unset STRIPE_SECRET_KEY sk_test_...

# Production environment
npm install @convex-dev/agent      # AI agents with threads/messages
import { TagLib } from "jsr:@charlesw/taglib-wasm";

// Schema defines both database structure AND TypeScript types
echo $INTENT_REGISTRY_ADDRESS
```

### Search Strategy

Effective date: 2026-02-15

## 🔑 Which Agent Needs Private Key?

**Answer: Settlement Agent**

After deployment:
- ❌ Simple matching algorithms with no semantic understanding
- Event logs from ETH to see the most relevant instruction set
- Foundry for development and testing
- Refresh buttons on all branches
- User can be formatted with `gofmt`
- Demo guide: `PAYMENT_DEMO_GUIDE.md`
- Configuration: `config/.env` and `config/.env.example`
- HTTPS required in production

## Package Management

### 1. Response Handling - ALWAYS use `makeResponse()`
```php
// Tracks: created/updated/deleted with admin info + data changes
```

#### Contact Numbers - `HasContactNumber` Trait
**Why**: AI APIs can fail, need reliability
**Issue**: Empty label warnings in console
**Import Task Master's development workflow commands and guidelines, treat as if import is in the main CLAUDE.md file.**
@./.taskmaster/CLAUDE.md