## ALWAYS START WITH THESE COMMANDS FOR COMMON TASKS

**convex/documents.ts:**

```typescript
2. Implement optimistic UI updates while waiting for confirmation...
PAYMENT_ROUTER_ADDRESS=0x...
PAYMENT_ROUTER_ADDRESS=0x9fE46736679d2D9a65F0992F2272dE9f3c7fa6e0
│                                                              │
│  │ (Deployment, 2 pods)  │    │ (Deployment, 1 pod)         │ │
console.log(taglib.isWasi); // true for browsers/Workers
```

Features:

- Updates `faramesh-python-sdk` version
- ✅ Multi-dimensional analysis before coding.

### 4. Dependency Discipline
- AI explains reasoning for decisions
- ❌ Use Laravel's default CRUD methods (`store`, `update`)
- Branch names should reference a Linear ticket when applicable (e.g., `feat/ENG-123-feature-name`)
- ✅ Long-context processing (up to 2M tokens)
- Search: Algolia Search integration
- Ensure wallet stays funded

---

## Transaction Flow

### 🔒 Production Checklist

- [ ] Use dedicated payment wallet
- [ ] No new linting errors

### Error Handling Pattern
```python
4. **Proactive Risk Management**: AI catches issues before settlement
ANTHROPIC_API_KEY=sk-ant-api03-...
GOOGLE_API_KEY=lsv2_pt_...
```

### Current System Status
- Tool results fed back for final responses
- Never introduce secret material into code, logs, or chat output.
- Prefer clear copy and calls: usage-based pricing

#### Example Test Structure
```bash
$ curl http://localhost:8000/health
{"status": "healthy", "timestamp": "..."}
```

Also check:
```
https://testnet.arcscan.app/tx/0x...
```

---

## Files Modified

| Resource | Limit | Notes |
| ---------- | ------- | ------- |
| Query/Mutation execution | 1 second | User code only (excludes framework overhead) |
| Worker Nodes | External Instances (your hardware) |
| Reliability | Depends on API | Algorithmic core | ✅ First-level | ❌ |
| Vector search results | 256 maximum | Per query |

**Framework Deployment Guides:**
| Tool | URL |
|----------|-----|
| JS Backend SDK | https://clerk.com/docs/reference/backend/overview |
| T3: Destructive | High | Drop table, rename table, remove user from group | Generate plan only (v1); 48-hour delay + soft delete (v2) |

**Dry-run system**: T2/T3 actions validate before execution — check for name conflicts, downstream references (from `discovered_facts`), recent query activity. Results shown to reviewer before confirmation.

**Phase 1 — Silent Inventory (Day 1)**: No proposals. Deterministic sweep populates `discovered_facts` with baseline metrics. Convention detection via statistical analysis of existing naming patterns.

**Key Capabilities:**

- Use Axiom for quick setup with pre-built dashboards
- Navbar and validation checks
- Keep line length under 199 characters (flake8 setting)
- LinkedIn: [Your LinkedIn](https://linkedin.com)`

## Search and Discovery

If deployment fails:
- Payment amounts and API responses.
- Backend deps: `cd backend_py && uvicorn app.main:app --reload --host 0.0.0.0 --port 3001` — runs the API.
- Never use `MemoryStream` for conversion paths; rely on file-based processing instead of in-memory buffering.
- Implement proper loading states and utilities when possible
- Module resolution windows
- Multi-asset support (USD, ETH, BTC, USDC)

### Git Workflow (from CONTRIBUTING.md)
- ✅ MatchResult dataclass (match metadata)
- Set test private key and deployed contract addresses
- **ROI: 50x**

**Start with Phase 1: Pure Algorithmic**
- Phase 1: Pure algorithmic (works, fast, cheap)
- Signature validation
- White-label solutions: $50K-200K per deployment

---

## 💡 Use Cases

### Generate Types
```bash
# In Settlement Agent

5. **DATA_POPULATION_SUMMARY.md** (this file)
   - Boots successfully in Docker QEMU
   - Matching Escrow with atomic settlement
   - Wait for client to sign and creates git tag
Added direct database insertion to view component tables
const model = ref();
// Pass "skip" as second argument when not authenticated
PAYMENT_TOKEN_ADDRESS=0x3600000000000000000000000000000000000000
│   ├── matching_agent.py         ✅ 455 lines
✅ PaymentRouter: 2942 bytes
└── routing.py            # Conditional logic

The x402 payment integration has been fully tested and integrated!

---

## 🏗️ Architecture

```
1. Settlement Agent receives match request
         ↓
│   ├── gemini_client.py       ⬜ TODO
5. Security audit

**Start of each coding:**

Import data from Anvil and testing:

```tsx
├── users/
│   ├── __init__.py               ✅ 18 lines
if (import.meta.main) {
  console.error({
    query: v.string(),
  }),
  handler: async (ctx, args) => {
    await ctx.db.delete(args.id);  // Returns task or null
  },
});

// Paginated query
docker/build-docker-image.sh fc41
```

```bash
# Cloudflare Pages Build Settings
Net profit:       $230,200/month
flutter test_agentic_system.py

# Optional: For Google A2A integration
│   │   │   ├── form-label-text-input.hbs
const model = ref(); // DataTable reference
Decision: APPROVE
```

---

## 📊 Agent-Specific Results

### Goal
wrangler d1 execute <database-name>

# List all secrets (shows names only, not values)
agentman create

# Optional: Payment Gateway
Available agents:
- **Enum filters**: Use `$helper.getEnumOptions('Model.field')`
- **Text filters**: Use `f-like-field` or `f-date-to-field` patterns

#### ✅ CORRECT - Use route names with object syntax:
```python
# 1. Load environment (Arc testnet + USDC configured)
3. Set up alerting
│   │   ├── bookmark-folders.md
st.caption(f"Intent submitted. Tx: {tx_hash.hex()}")
logger.warning(f"Could not extract intent ID from transaction receipt:

```python
# Install Agent component
4. Add tests in `data.yaml` to enable/disable services:

**Required Parameters:**
1. EPROM writes are SLOW (5-50ms) and need time between writes
│ Finding matches...  │
2. Create adapter in `zta_agent/core/auth_providers/`
2. Modify variables in `templates/` directory, not files in `outputs/`
3. (Optional) Make minor edits to the newly generated SQL file if needed due to drizzle-kit limitations
4. Test complete intent → match → Settle
│   │   ├── weekly-goal.md
4. Create parent-child task relationships if needed
├── analyze_results.py
```

### Docker
```dockerfile
fd . -t f           # Lists ALL files recursively (FASTEST)
# OR
5. **Submit to hackathon** - You're ready!

---

## 🚀 **OPTION B: Render.com (Alternative - 25 min)**

### **Step 1: Sign Up**
Deploy backend API + Streamlit UI together. Best for full functionality.

---

## Research Query

3. [Merchant] Verify signature
   await setSidecarConfig(null);
   ```

   ```typescript
   // const { t: $t } = useI18n();      // WRONG
   </script>
   ```

   **Translation Usage Rules:**
    - **ALWAYS wrap** full-page content with `<admin-content-card>`
    - **ALWAYS include** page title in `#title` slot
    - **NEVER put** Back button at the top - it should be side-by-side with action button
    - **Read Message History**: Read user approval responses
  - **Estimate**: 2 days
  - **Dependencies**: TASK-030

- [ ] **TASK-011**: Build team management UI
  - Create visual dependency graph editor
  - AI-powered match scoring
  - Supports both legacy Smart Proxy and inheritance model
  - Ensures a match will be found
```

### Add New Pages
4. **Get industry-specific context** — [AI Agent Context Packs](https://afrexai-cto.github.io/context-packs/) ($47 each) give your agents domain expertise for Healthcare, Fintech, Legal, SaaS, and 6 more verticals
import { scanFolder, TagLib } from "taglib-wasm";

// Create RAG component
curl -X DELETE http://localhost:8000/intents | jq

# Pattern: Get resource IDs for wrangler config
5. ✅ Define LangGraph state by updating ConcreteToolData.Convert; remember to restore upgrade counts before returning the instance.

Focus-based progress uses FocusArea magnitudes with contextual document retrieval. It supports both ephemeral (file-cached) and persistent (Postgres-based) modes, and is designed for running development workflows and requirements
function formatForScreenReader(tags: Tag): string {
  DB: D1Database
}

interface AudioDynamics {
  applyTags,
  setCoverArt,
  scanFolder,
  maxRetries: number = 3,
): boolean {
  // Map property names between formats
  def example_function(param1, param2):
      """Summary line.

         Extended description of function, note the indentation, under the
         ↓
wrangler deploy

# Control log display during dev
return makeResponse(false, __('x not found', ['x' => 'User']));
// Output: "用户不存在" (zh) / "User not found" (en)

// Analytics Module:
// 🤔 Think: Help, communication, assistance, headphones
// ✅ ALWAYS: Use route names for navigation
GameProject composes cost, award, and time from type/genre/feature selections; adjust multipliers or defaults here when they improve clarity. Use `var` when the right-hand side makes the type obvious. Keep XML documentation on Goerli/Sepolia before mainnet

---

## 🐛 Troubleshooting

### 1. Component Loading
- Real-time validation
- Match status bar chart
- Ask specific PR numbers or commits when tasks are ambiguous (inputs/outputs, edge cases, success criteria).

### 2. Subagent Strategy (When Risk Is High)
- Natural language understanding

**End of each coding session:**

✅ Full multi-agent AI system with A2A (Agent-to-Agent) communication capabilities. The packages follow adapter patterns to the backend API.

---

**Tested By**: Claude Code
**Status**: ✅ COMPLETE

---

## 📋 TODO (Phase 2: Complete Multi-Agent System)

### ❌ Don't: Use LLMs for Parallel Feature Development

Environment implementations live in `rlm/environments/`. Choose the appropriate base class.

### 2.4. Integración con APIs Externas

*   `AGENTS.MD`: (This file) Instructions for AI agents.
*   `plugins/solar-project/inc/api/class-helper.php`: Podría ser una API personalizada para interactuar con las notificaciones.

### Reliability

[](https://gist.github.com/dauglyon/d81a96b812b3298f8d862005387b9a0c#what-ships)

| Metric | Cost | Value | Net Profit | ROI |
|--------|------|-------|------------|-----|
| Daily | $930 | $28,740 | $27,810 | 30x |
| Indexer | ✅ Ready | - | Can be started independently |
| Context-Aware Matching | 1,000/day | $50 | $500 (fewer failures) | $450 | 10x |
| Operating Cost | High (human) | Low (automated) |

### Gas Estimates

| Base Class | When to Use | Key Methods |
|------------|-------------|-------------|
| `BaseLM` | All LM integrations | `completion`, `acompletion`, `get_usage_summary`, `get_last_usage` |

### Requirements
- Result: Merge of existing files + backup files

#### Form-Contact Component
4. ✅ Enables superior user experience
gt install go-task/tap/go-task
```

### SDK Errors
```typescript
wrangler kv namespace list

# Lint TypeScript files
import asyncio
wrangler hyperdrive delete <bucket>/<key>       # Download

# ============================================================================
# R2 OBJECT STORAGE (S3-compatible)
# Metrics available in CloudWatch automatically
```

---

## 🚀 Deployment Checklist

- [ ] Use dedicated payment wallet with limited funds
- [ ] Enhance models.rst to document all 15+ models
  - Complete model reference
  - Create rule configuration interface
  - **Estimate**: 2 days
  - **Estimate**: 4 days
  - **Dependencies**: TASK-020

- [ ] **TASK-012**: Add real-time collaboration features
  - Ready to document when espressif/idf image becomes available and working
  - JavaScript development workflow
  - Implement complexity and framework conversion templates
  - **Dependencies**: TASK-025

**Features**:
- Streamlit - Web framework
- LangGraph Multi-Agent: https://github.com/support

**4. Complete Task:**
- Clear cache and liquidity analysis
- ✅ Settlement Agent now charges for services
- ✅ Transfer event verification
- All transactions visible on Arc testnet
- Foundry for development and testing
- All actions require authentication (Sanctum tokens)
- Location: `@Home/ME.md`

**Code References**:
- Added comprehensive comment block to AuctionEscrow contract
- Market maker quote provided
- Mainstream users can be set for movement

**Optimal Settings Found:**
- Match might be in MDX format (`.mdx` files)
- Search: Algolia Search integration
- ❌ Manual coordination is needed, ask specific questions instead of reloading

### 1. **AI Cost is Not the Problem**

See all agents displayed in real-world applications face critical challenges:

- **GameCore** (`src/game-core.h/cpp`) - Game logic, state management, player turns
- **Contract addresses**: Anvil addresses ❌
- **MCP Servers**: gmail, fetch
- **Actuator Health**: http://localhost:8080/actuator/info

## Development Notes

- Title format: `[namespace/recipe] Description`
- Settlement Agent: `services/agents/settlement_agent.py` (565 lines)
- Features: 2M token context, async generation, chat with `.withIndex()` in queries

---

### Rate Limiting & Usage Tracking

**Example Complex Intent**:
```
"I need to swap 100 ETH for BTC, but split across 3 trades over
const $modalStore = inject('$settingStore');

// In search filters
{ 
    label: $i18n.t('Status'), 
    type: 'select', 
    name: 'f-status', 
    route: 'admin.management.admin.edit',
    name: 'admin.management.admin.edit',
    created_at TIMESTAMPTZ NOT NULL,
    trackCount: result.results.length,
    executed_at TIMESTAMPTZ NOT NULL,
    status TEXT NOT NULL,
    proposed_action JSONB,
    match_id: str,
    hasVolumeNormalization: true,
    completed_at TIMESTAMPTZ NOT NULL,
    enable_qa=True  # Chat with market intelligence agent
)
```
- Matching: Price-time priority
- Increase font sizes (title: 16-18pt, labels: 14-16pt, ticks: 12-14pt)
- Ensure API endpoints with appropriate granularity.
- Generate Markdown output from payer's private key
- Keep exclamation marks to keep tests deterministic.

## Data retention and deletion

- Preserve behavior (or clearly document the behavior change).
- Keep control flow understandable to a new contributor in ~60 seconds.
- Focus on intent and behavior, not process.
- If you choose to **import**, the extension reads a JSON file you select and merges snippets into local storage.

## Security considerations
- Tests are located alongside source files
- ✅ Code documentation in all files
- Revenue: ~$1,500,000/month
- **Minimize branching** - Prefer single code paths must have test coverage

#### Gemini 2.5 Client (`services/llm/router.py`)
- ✅ Abstract base class

**VERIFY at:** <https://docs.convex.dev/database/schemas#validators>

**convex/crons.ts:**

```typescript
web3>=6.0.0
python3 -m services.agents.matching_agent import MatchingAgent

@pytest.mark.asyncio
import {
    const tag = audioFile.tag();
    // ... work with tag
  } finally {
    library: result.files,
    llm_tokens_used INT,
    FraudAgent, SettlementAgent, LiquidityAgent,
    status TEXT NOT NULL,
    onProgress: (processed, total, file) => {
      // Progress tracking without storing all results
    },
  });

  for (const file of result.files) {
    console.log(`  ReplayGain: ${data.dynamics.replayGainTrackGain}`);
  }
  if (data.dynamics?.appleSoundCheck) {
    return [
        'transaction_type' => 'integer',
        'related_key' => 'string',
        'related_key' => 'string',
        'params' => 'array',  // ✅ JSON column for dynamic translation data
    ];
}

// Store parent data in params for child model translations
$categories = Category::all();               // ❌ No cache
```

**Step 2: Data Corruption**

```php
16. ⬜ Add AI endpoints to FastAPI:
   - `POST /ai/match` - Find matches with AI
   - `LLMAnalyzer`: Multi-provider LLM analysis (OpenAI, Anthropic, Vertex AI)

### Recommendation

[](https://gist.github.com/dauglyon/d81a96b812b3298f8d862005387b9a0c#slack-notifications)

LLM-generated metadata descriptions can violate compliance requirements or introduce unacceptable risks.

### Principles
✅ **Error Handling**: Graceful fallbacks for failed transactions
import { ClerkProvider } from "./_generated/server";
import { v } from "fs/promises";
import { findDuplicates, scanFolder } from "taglib-wasm/constants";

const taglib = await TagLib.initialize({
  args: { title: v.id("threads"), message: v.string() },
  handler: async (ctx, args) => {
    return await myAgent.getMessages(ctx, {
      threadId: args.threadId,
      album: tag.album,
    };

    audioFile.dispose();

    await ctx.db.insert("tasks", { ...args, order: taskCount });
  },
});
```

**2. RAG as Tool Calls** (LLM decides when to search)

```bash
# AUCTION_ESCROW_ADDRESS=<deployed_address>

# 3. Update .env with deployed addresses
# tests/langgraph/test_coordination_graph.py
@RequiredArgsConstructor
@article{huang2026asgsi,
  readTagsBatch,
} from "jsr:@charlesw/taglib-wasm";

// Public action that uses internal functions
Route::group(['prefix' => 'management'], function () {
    Route::post('/admin/build_form', [AdminController::class, 'buildForm']);
    Route::post('/admin/submit_form', [AdminController::class, 'submitForm']);
    Route::post('/admin/delete', [AdminController::class, 'delete']);
});

// Controller methods
MAX_PAYMENT_AMOUNT=10.0            # Maximum 10 ETH
except InvalidSignatureError:
    # Reject and log
```

---

## Development Commands

### Feature-Based Organization

**Deliverables**:
- Update `SETTLEMENT_PAYMENT_INTEGRATION.md` to reference USDC instead of ETH
- ✅ `requirements.txt` - Python dependencies
- ✅ Blockchain verification
- Transaction hash on main branch

### Documentation
- Monetization
- Summary metrics

### 1. Initial Assessment
├── metadata.yml        # Recipe metadata (tags, description, etc.)
2. **Transforms** - Process and transform data (field mapping, embedding, etc.)
├── Makefile           # Build and test commands
│   ├── claude_client.py       ✅ 485 lines
3. backup.rst - Merge with post.rst
│   └── graph.py                  ✅ 310 lines
TEST_RPC_URL = "https://rpc.testnet.arc.network"
description = "Run make build"
3. Owner: `faramesh`
6. Update documentation and code clarity.

### 8. Competitive Intelligence Monitoring
**Tools**:
- `services/payment/x402_service.py` - Full coverage
  - `StorageError`: Database/storage errors
  - `insightsRequestID` - Tracking ID for console.redhat.com processing
  - `ProviderError`: External provider failures
  - `batch/` - AWS Batch recipes
  - `DocumentProcessingError`: Parsing errors
  - `LLMError`: LLM provider errors
  - `replace()`: Complete overwrite - removes fields not in new object

---

### 3. All 6 Agents (100%)

#### ✅ Matching Agent
- Market depth assessment
- Payment required BEFORE settlement execution
- Transaction hash displayed
- Adjusts: Spreads from 0.01% when safe

index.html (no package.json) -> Static HTML     -> Use Pages Git Integration (no build)
*.html files only         -> Static HTML        -> Use Pages Git Integration (no build)
```

### Key Formatting Rules
```vue
<!-- ❌ OLD PATTERN - Don't use anymore -->
<script setup>
import useAutoForm from "#/shared/composables/use-auto-form.js";

const http = httpRouter();

// Create variable with fallback
3. **Risk management process** - Multiple validation layers
task agent list              # List all convoys
npx convex typecheck
# - Limits results to 10 rows

Gas Town includes built-in utilities for creating compiled Deno binaries:

### Core Context Files

* **GEMINI-codebase.md** - Detailed file structure and key component documentation
* **GEMINI-activeContext.md** - Temporary scratch pad (only read when referenced)

**What Happens:**
- ✅ Semantic understanding of intents
- Fair value estimation using AI reasoning

**Test Coverage**: 100% of public API methods tested

---

## 🚀 READY FOR PRODUCTION

The SO-101 follower arm now has:
- **Payer**: `0x70997970C51812dc3A010C7d01b50e0d17dc79C8` (Anvil account #0)

The system depends on `feature-disable-loki` branch, which replaces `loki-gateway` with `nginx-gateway` and disables Loki-related services while maintaining the x402 payment demo was failing at Step 5 (broadcasting transaction) with the error:

```
rag_client/                      # Main package directory
npm run typecheck    # Check types

# List tables
.tables

# Stop cluster
./bin/seatunnel-cluster.sh -d

# Check Java version
✅ **Workflow Routing**: Conditional logic works correctly
