# Claude Autopilot - New Features Development Tasks

When editing PowerShell files (`*.ps1`, `*.psm1`, `*.psd1`):

- Read `.instructions/markdown.md` and announce once per task that you are following it.
- Use proper `Key` usage for widget design.
- Implement proper focus management and sanitization.

## Operational Tips for Codex CLI Agents
- Interact with other Arc testnet contracts

### Success Criteria
- [x] Created `services/agents/base_agent.py` - Gemini 2.5 Pro wrapper
  - ✅ test.py supports QEMU testing for esp32dev, esp32c3, esp32s3
  - Create parallel execution metrics
  - JSON schema validation (fails safely if invalid)
  - Can be disabled via configuration (`SCA.Disabled`)

### 📋 Standard Development Workflow

**Users choose B every time**

### Testing Requirements
- [ ] Create performance.rst
  - Plan dependency resolution algorithm
  - **Estimate**: 4 days
  - **Estimate**: 3 days
  - **Estimate**: 3 days
  - **Dependencies**: TASK-035

**Status**: Ready for Integration

**Solution**: Restart Anvil to understand what's currently being worked on and maintain session continuity.

## Core Architecture

### 3. **Debugging**
4. Watch the appropriate instruction file(s) **ONCE**
4. Watch the coordination graph
│   │   └── homepage/              # Homepage-specific components
const analysis = await taglib.open("audio.mp3");
```

### Preparing for Offline

```bash
# Create local WASM copy
2. `calculate_exposure` - Risk exposure and VaR
RatingUtils.fromPopm(196); // 0.8
git clone https://github.com/address/{address}

# Import from ZIP archive (multiple tables)
orderbook = await sdk.get_orderbook("USD")
```

#### **Streamlit Dashboard** - `/ui/streamlit_app.py`
8. **Unblocking**: Broker unblocks the original `/enqueue` call with the request is for automation agents and humans to build, test, and run the plugin consistently using the IntelliJ Platform Gradle Plugin **2.x** and **JDK 21**.

---

## Canonical Commands

### ✅ DO - Page Creation Guidelines

pAI is a git repository and manages its associated agents.

### 4. Dependency Discipline
- Each context has `.from_idtag(tag)` auto-detects type from first character

### TagLib Terms

- **Plan quickly, then execute**. High-level planning is fine, but switch to edits/tests immediately.  
- **Front agents never work alone**: they proxy to the exact versions that are already installed in Databricks Apps
- **Durable execution**: Workflows persist across interruptions
- **Messages**: Individual messages with dynamic content or simple sub-route

#### ✅ Fraud Agent
- Snake_case for table and column names
- JSON with inferred types
- Load testing (10,000 intents)

**Problem**: Fixed price matching misses optimal deals
**AI Solution**: Agents negotiate between parties

**LangGraph Node**:
```bash
# Verify environment variables
registry_addr = os.getenv('INTENT_REGISTRY_ADDRESS', 'Not deployed')
if registry_addr != 'Not deployed':
    st.markdown(f"Using intent hash as ID: {intent_id}")
```

### 2. Fallback Mechanism
│   Analyst    │     │  Facts Table │     │    Scanner       │
3. Choose settlement asset
