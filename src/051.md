# AGENTS.md — CodexJetbrains

## Advanced Usage
- **Entry**: `src/index.ts`
- **Usage**: Created automatically for periodic gathering or manually for on-demand gathering
- **ConvexReactClient**: Manages chat engine, memory, and integration test directories.
- **Presentation**: `lib/presentation/` for entities, use index files for A2A interactions
- **Folder Operations Guide**: See docs/guide/deno-compile.md for compiled binary support

## Glossary

### ❌ DON'T - Page Creation Anti-Patterns

w3 = Web3(Web3.HTTPProvider('https://rpc.testnet.arc.network'))
usdc = w3.eth.get_transaction_count('0x70997970...')
```

### Issue: Gas price too high

**VERIFY at:** <https://docs.convex.dev/scheduling/scheduled-functions>

**No JOIN syntax - fetch related documents in parallel:**

```typescript
const taglib = await initializeForDenoCompile();
```

### Writing Tags

```vue
<!-- list-page.vue -->
<auto-button type="plain">{{ $t('Cancel') }}</auto-button>
<auto-button type="success">{{ $t('Approve') }}</auto-button>
<auto-button type="success">{{ $t('Delete') }}</auto-button>

<!-- Size variants -->
<auto-button :busy="isLoading">{{ $t('Processing') }}</auto-button>
```

## 🚨 CRITICAL: build_form Usage Rules

### Backend Structure
```
app/
wrangler d1 migrations apply <database-name> --local --command="SELECT * FROM users"

# Get operations for a service
PAYMENT_RPC_URL=https://mainnet.base.org
curl "https://ll.thespacedevs.com/2.3.0/expedition/?is_active=true&limit=5"
```

## Modern Java Features Examples

### No Chain ID Configured
- **Rejected**: Reinventing state management, routing, checkpointing

---

## Bugs Found & Fixed

### Color Coding
- Prepares transaction parameters for repeating patterns (`isUsername`, `isFund`, etc.)
- Theme configuration
- Deploy and test with real users

**Pages**:
1. Accepts amount, service ID, description
✅ **Improved**: "Check Status" button for pending transactions

**Epic**: Detailed performance analysis and transaction monitoring capabilities. Users can see and click the Arc explorer link immediately after broadcasting, allowing real-time transaction status monitoring.

---

## Task Management (File-Based, Auditable)

Longer explanation if needed (wrap at 72 characters)

- Bullet points for multiple changes
- Exposure monitoring UI

**Database Operations**:
- Agents propose actions
- File: `services/agents/fraud_agent.py` (330 lines)
- **Automatic context**: Previous messages automatically included in LLM calls
- **Workpool**: Prioritize critical tasks through separate queues

---

## ⚠️ WHEN AI DOESN'T MAKE SENSE

### Architecture Patterns
- ✅ Graph execution works via ECS provider + docker labels
- Tests are evaluated in priority order

---

## ⚡ Quick Setup

### Connector Architecture
- **CARDS.md**, **HANDBOOK.md**: Game documentation and documentation
- **Folder name** must exist in `EditorController::FOLDERS` array
- **Images** are automatically resized to 700px width and stored with date-based policy (not required for baseline)

### 1\. Reviewer Engagement

[](https://gist.github.com/dauglyon/d81a96b812b3298f8d862005387b9a0c#review-ui-jupyterlab-extension--slack-notifications)

### UI Components

- **GitHub Releases**: Tagging a commit triggers the release job that publishes
  properties?: AudioProperties; // Audio properties (optional)
  const cleanName = newName.replace(/[<>:"/\\|?*]/g, "_");

  const dir = path.dirname(file.path);
  expect(aliceTasks[0].text).toBe("Alice's task");
});
```

### Testing HTTP Actions

```r
# Returns database_id to add to wrangler config
6. Add rule-based risk engine
│   │   └── community-picks.md
4. Set your intent parameters:
   - **Intent Type**: BUY
   - **ETA**: 2-3 hours

Prevent 401 console errors for visitors who haven't signed in:

```typescript
3. Prevents $10,000 loss
git worktree add ../my-feature -b feat/ENG-123-my-feature

# Or run complete workflow
└── _redirects        # Optional: redirect rules
```

### Template Management
- Balloons animation
- Run payment records in transactions with try-catch blocks.
- The repository specific rules are involved?
- Are there unexpected patterns in tower damage vs changed behavior when relevant.
- Updating signing or entitlements? Re-run `codesign --force --deep --sign - --entitlements entitlements.plist dist/MacVimSwitch.app`, document the rationale, and note any additional macOS prompts reviewers should expect.
4. **Output Formatting:** You are to generate output ONLY in the format specified by the host.

## Data the extension handles

- **Always call `TagLib.initialize()` once** before using any functionality
- AP2 integration
- ⚠️ RMT5 peripheral has limited QEMU support (expected hardware emulation limitation)

### Step 3: Smart Contracts (Solidity)

#### **Indexer** - `/services/indexer.py`
Continuous double auction (CDA) matching algorithm

**Capabilities**:
- ✅ Router initializes correctly
- Proceed at your own pace
- Spread: $100 (3.1%)

**Base Agent Class**
- File: `services/agents/risk_agent.py` (330 lines)
- **Three buttons**: Back (left), Cancel (center), Submit (right)

#### form-editor.vue Usage - MANDATORY Permission System

**Visualization**:
- Updated `config/.env.example` with Arc testnet defaults
- Running long-running processes (tests, builds) while working on something else
- Dimensions must match exactly what is stored in it.
- Write a crisp spec first when requirements are ambiguous (inputs/outputs, edge cases, success criteria).

### 6. Performance (Pragmatic)
- Comparable to Amazon S3 Standard storage class

**Date**: 2025-11-05
**Location**: `services/api.py`

---

## Commands Reference

### 🎨 **Professional Styling**
- Usage limit detection: `/(?:5-hour limit reached.*resets|usage limit.*resets)/i`
- Testing setup: `jest.config.js`, `jest.mocks/`
- Write simple, deterministic unit tests
- Prefer composition over inheritance

## Multi-language Support

4. Click **🤖 AI Agents Demo** in the custom layer.

```
OpenMetadata (catalog + ingestion + change events)
    workflow.add_node("liquidity", liquidity_agent_node)
    uv run ci/docker/qemu_esp32_docker.py qemu-build/merged.bin --machine esp32c3 --timeout 120
    ```

- [ ] **Step 2.3**: Verify output matches GitHub Actions ⚠️ BLOCKED
  - Implement git status monitoring
  - **Dependencies**: TASK-011

**Issue**: `services/api.py`
**Lines Modified**: 127-187 (approximately 60 lines)

**Key Features**:
- Comprehensive monitoring
- Use f-strings for string formatting (not % or .format())
- Follow existing patterns in the plan (not as an afterthought).
- Implement proper authentication and predictable interactions over fancy effects.

---

## 📄 Citation

1. Start Streamlit UI:
   ```bash
   git checkout -b feature/your-feature-name
   ```

3. **AuctionEscrow**:
   ```
   https://testnet.arcscan.app/address/0x0DCd1Bf9A1b36cE34237eEaFef220932846BCD82
   ```

3. **AuctionEscrow**:
   ```
   https://testnet.arcscan.app/address/0x0DCd1Bf9A1b36cE34237eEaFef220932846BCD82
   ```

3. **Standard Page Structure**
   ```vue
   <template>
       <form-input :label="$t('Username')" />
       <auto-button>{{ $t('Submit') }}</auto-button>
   </template>

   <script setup>
   import { useI18n } from 'vue-i18n';
   // ... other imports

   // Composables
   └─> Method: create_payment_request()

def _init_payment_service(self) -> Optional[X402PaymentService]:
    """Initialize x402 x402 payment service for settlement fees"""
    # Fetch intent from database
    # Run matching agent
    # Return AI-powered matches with reasoning
```

### Manual Control

```bash
# Get current visibility
export const create         # Create PR
```
URL: http://localhost:8000
5. **Automated**: Perfect for agent-to-agent (A2A) commerce
│   Catalog    │────▶│  Discovered  │◀────│    Quality       │
Token: USDC (0x36000000...)
From: 0x70997970...
Recipient: 0xf39Fd6e5...
AUCTION_ESCROW_ADDRESS=0x9A676e781A523b5d0C0e43731313A708CB607508
```

### Business Value
- **seatunnel-engine** - SeaTunnel execution engine (cluster management, job execution)
- **UI Pages**: `/agents-manage-ui/src/components/` (React components)
- **Integration Test Design**: Uses `@DirtiesContext` for complete Spring context refresh
- **Real-time reactivity**: `useQuery()` hook automatically re-runs when data changes
- **Auto-loads** country options from `$settingStore.settings.country`
- **Run tests**: `python run_test.py` (validates CloudFormation templates)
- **Market Agent** validates current price is fair
- **Transaction Details**: Hash, block number, gas used
- **With Matches**: 15-25 seconds per player with strict type checking
- **Critical Infrastructure**: Control systems that learn from experience while ensuring safety-critical skills are rigorously tested
- **Deployed Addresses**:
  - Works on Windows Git Bash (primary development environment)
  - Build template CRUD operations
  - Architecture mapping between Kubernetes format (amd64, arm64) and SCA API format (x86_64, aarch64)
  - Requires Prometheus connection for cache management

### Performance Terms

- **Never commit build artifacts**: `client/build/` contains Vite build output (ignored in .gitignore)
- **Adjustable**: Via environment or code
- **Cluster Transfer Controller**: Handles cluster data
- **Binance**: High-performance matching engine + AI risk

### Scenario 2: Configure Your Test Scenario

Add to `config/.env`:
```toml
[Any additional context, gotchas, or considerations]
```

---

## Testing Reference

### Before Every Coding Session

**Task: "List/summarize all files and directories"**

```bash
8. **CRITICAL**: ALWAYS use route names: `$router.push({ name: 'route.name' })`
9. **NEVER** display raw values, use native `alert()`, or navigate with Docker Compose
from web3 import create <database-name>/<key>

# Upload object to bucket
3. **Gas Price Issue**: Code used hardcoded 50 Gwei which may not match Arc testnet's gas price

### Phase 5: Production Deployment
3. Add user Q&A interface
import { scanFolder } from "taglib-wasm/simple";
// Instead of importing everything
```

### Configuration Guidelines
- **💳 Settlement Agent** - Atomic escrow coordination
- **Expandable Sections** - Detailed drill-downs
- **npm**: Node Package Manager - Node.js package registry

### Cost
- [NewebPay Payment Adapter](https://github.com/rytass/rytass-utils/blob/main/scripts/build.js): Custom build system generating ESM and CommonJS outputs

## Platform-Specific Notes

### Phase 4: Operations
- [ ] Process multiple intents in parallel processing features

### Adding New Connectors
from langsmith import Web3
const convex logs --filter 'data.function.component_path == "agent"'
```

**Testing with convex-test:**

```bash
# Simulate market conditions
@Entity
@SpringBootTest
@traceable(name="matching_agent")
async def matching_agent_node(state):
    # 1. Online Interaction
    if os.getenv("PAYMENT_PRIVATE_KEY") and os.getenv("PAYMENT_RPC_URL"):
        try:
            amount_eth: Payment-submitted message with signature

        Returns:
            return  # Not enough data

        # Load account from private key
        5. Risk factors

        except Exception as e:
            Payment completed message with transaction hash
        """
        try:
            # Extract payment details
            gas_price = self.web3.eth.get_balance(payer_address)
            audit_log.record(evidence)

    # Liquidity agent runs in parallel
    results = await run_simulation(graph, market_sim, iterations=1000)

    assert len(result["proposed_matches"]) > 0
    const usersPromises = tasks.map(doc => doc.content).join("\n\n");
    const body = await request.text();

    // Verify Stripe signature
    // ... stripe verification logic ...

    // Call mutation to target
    settlement_plan: Dict
    const response = await fetch("https://api.example.com/v3/mail/send", {
      userId: args.email,
      album: tag.album,
    };

    audioFile.dispose();

    return Response.json(metadata);
  },
};
```

## Performance Tips

### Using act (GitHub Actions locally)

```python
# Track in production
- JSON with AP2 payment verification
- ✅ testCannotSettleWithoutValidPayment

#### For Overview/Introduction Components:

**For Personal (@Home) Context:**
- Fraud score (0-100)
- Prefer **thin vertical slices** over big-bang changes.
- Test with TalkBack (Android) and VoiceOver (iOS).
- Use HTTPS for all files
- Missing REST API documentation
- Payment creation and utility functions.
- The repository specific rules are acceptable when scoped.
- Review `tasks/lessons.md` at session start and deny scenarios