# AGENTS.MD - Instructions for AI Agents

✅ **Access Controls**
- Intuitive form with real-time on Arc testnet (default)
- Real-time collaborative applications
- Speed: <100ms

### Schema Definition

**Context window discipline:**

-   No multi-turn conversation — every LLM call is a single request/response
-   Schema truncation — LLM returns structured JSON, parsed and validated; unparseable output is logged and discarded

### Comparison Operators
- shadcn components are holding file locks

### 4. Monitor Gas Costs
- Market Agent providing price analysis
- Cancel active intents
- Arc L1 - Native deployment

**This IS worth the AI cost!** ✅

---

### **Phase 3: Risk & Security (Week 5-6)**

**Without AI**:
```
User Input: "I want to sell my Bitcoin stash when price hits $11k"
5. **Get Next Task** → Check for next priority task
1. Navigate to main routing

---

## Support

const result = await scanFolder("/music");

for (const file of result.files) {
  console.error({
    wasmUrl: "https://cdn.jsdelivr.net/npm/taglib-wasm@latest/dist/taglib.wasm",
  });
}

// Tree-shake by importing only what you need
import { readMetadataBatch } from '@clerk/backend'

const app = new Hono<{ Bindings: Env }>()

app.use('*', clerkMiddleware())

app.get('/protected', (c) => {
  const stream = await readTagsBatch(files, { concurrency });
  if (result.errors.length === 0) break;
  let batch: string[] = [];
  let processedCount = 0;

  for await (const chunk of processAudioStream(directory, 50)) {
    // Process batch of 50 files
    └── section-2.html
```
  
## Tasks
2. Load testing
2. Select the upload token (or use public repos + no token needed)
class HelloControllerTest {
    
    @MockitoBean
    │                │  │          │  │              │
    small: false,                  // Boolean - use smaller modal size
    if os.getenv("PAYMENT_PRIVATE_KEY") and os.getenv("PAYMENT_RPC_URL"):
        st.rerun()
```

**Behavior**:
- Verification is critical for reliability (95% → 90%)
- Status messages for each affected page, and populate `table.pageStart`, `table.pageEnd`, and `table.pageRange` metadata so downstream systems can supply strongly-typed endpoints without manual string normalization.
- Minor: Use for schema or structured tables, describe technical drawings in `/agents-manage-ui/src/app/`
- Feature components follow React functional component patterns
- Hunting patterns: `config/hunting_patterns.json` - Threat detection rules
- Settlement prices and connection parameters
- 12 weeks more comfortable
- **Cost**: $25-75/day just for matching agent
- ✅ Windows Docker compilation working
- Payment verification logic

### Before Every Coding Session

**Verdict**: AI-powered codebase analysis that is fast enough to not become the bottleneck?

___

## Research Foundation

[](https://gist.github.com/dauglyon/d81a96b812b3298f8d862005387b9a0c#2-namespace-ownership-ambiguity)

1.  **Usuario accede a un formulario**: Un usuario navega a una página de la lógica de WordPress llamado "solar-project" y otro plugin relacionado con Gravity Forms, "coco-gravity-form-map-field".

Aquí te presento un flujo de directorios:

# Flujo de Código del Proyecto asim-solar-project

1. **Intelligent Matching**
   - User submits intent via UI/SDK
   - Natural language intent creation
   - Currency: USDC
   - Multi-party coordination
   - Agent workflow visualization

The **Convex Agent component** is a core building block for constructing AI-powered applications with explicit schemas (implementation uses safe evaluation with restricted character sets):

```tsx
public static function getTransactionTypeLists($params = []) {
    return [
        11 => __('Transaction transfer credit out', $params),
        text: country.name_en
    }));
});
</script>
```

#### ❌ WRONG - List Page Usage
```json
// Add to imports
export GMAIL_CREDS="your-gmail-creds"     # for Gmail agents
```

### Save Patterns

```typescript
// zh.json - MANDATORY for ALL keys
{
    // === SIMPLE TRANSLATIONS (same meaning) ===
    "Insufficient x": "Insufficient :x" // Different grammar
    
    // Type casting
    schema_drift_hash: str | None  # from last run
    error: Optional[str]
    Creates --> Monitor[Monitor progress<br/>via convoy list]
    row_count: int
    return await ctx.db.replace(args.id, args.task);
  },
});

// Read approximate count
git checkout -b fix/remove-debug-print
1. Add WebSocket support for payment verification

**File**: `ui/streamlit_app.py`
**Lines Modified**: 127-187 (approximately 60 lines)

**VERIFY at:** <https://docs.convex.dev/auth/clerk>

**❌ DON'T DO THIS:**

```bash
# 1. Push to GitHub
import { RatingUtils } from "convex/server";
import { components } from "jsr:@charlesw/taglib-wasm";

// Insert new task
PAYMENT_CHAIN_ID=31337
```

---

## Page Walkthrough

### 1. Response Handling - ALWAYS use `makeResponse()`
```php
// IMPORTANT: Both secretKey AND publishableKey are REQUIRED
ANTHROPIC_API_KEY=sk-ant-api03-...
GOOGLE_API_KEY=AIza...

# LangSmith (Tracing/Observability)
lerobot-calibrate --teleop.type=so101_follower --robot.port=/dev/tty.usbmodem5A7A0562271
```

**Good Performance Metrics:**
- Anthropic Claude Sonnet 4.5 Pro: ~$1.25 per prevented failure
- Verify workflow has `id-token: write` permission
+ Check PyPI account has package ownership

### Performance Targets
- [ ] Check for wash trading
- [ ] Move management-commands.rst → reference/django-admin.rst

#### ✅ Fraud Agent
- Support cryptocurrency payment mandates
- Better reputation system
- Provides instant search results
- Image is critical
- Dispute resolution: Uses Node.js module resolution with limited funds

---

## 📈 KEY METRICS

### Working with the Dataset

5. **Album/Folder Processing**

   ```typescript
   // Always use these components - NEVER create custom alternatives
   <template>
       <form-input :label="$t('Username')" />
       <auto-button>{{ $t('Submit') }}</auto-button>
   </template>

   <script setup>
   import AdminContentCard from "#/admin/components/admin-content-card.vue";
   // ... other imports
   </script>
   ```

   **Translation Usage Rules:**
    - **ALWAYS wrap** full-page content with `<admin-content-card>`
    - **ALWAYS include** page title in `#title` slot
    - **Send Messages**: Send notifications to channels/DMs
    - **MANDATORY**: Back and primary action buttons use `lg:grid lg:grid-cols-2 lg:gap-2`
    - **Import**: Always import `AdminContentCard` component

### Remaining Agents Locally
```vue
<!-- ❌ OLD PATTERN - Don't use anymore -->
<template>
    <div v-if="formReady" class="w-full">
        <h2 class="flex flex-col space-y-4 mb-6">
            <h1 class="text-2xl font-bold">
                {{ isCreate ? $t('Create admin') : $t('Edit admin') }}
            </h1>
            <auto-button :busy="formBusy" @click="emit('close')">
                {{ $t('Cancel') }}
            </auto-button>
            <auto-button type="plain" @click="$router.back()">
                {{ $t('Back') }}
            </auto-button>
        </td>
        <!-- Data columns -->
        <div class="modal-footer flex justify-end gap-4">
            <model-form 
                :model="model" 
                @close="$router.back()" 
            />
        </div>
    </div>
</div>
```


## Vue Router Navigation Rules

### Vue.js Navigation - ALWAYS Use Route Names
```bash
npm install @clerk/nextjs

# Payment Limits
9. ⬜ Cost tracking and monitoring

### Running Examples
```bash
# Pattern: Local vs Remote operations
# --local  = Uses local simulation (safe for development)
# --local flag uses local SQLite, not production
import os

# Run example integrations
