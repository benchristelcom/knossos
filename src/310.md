GameDevClickers – Agent Guide
dk pylint
```

### Business Value
- **Key features**:
  - Total Intents: 10
  - Automatic fingerprinting for cache management

### Environment Variables
- `MetadataError` - Tag reading/writing errors

## Migration Guides

### Mutations
- Blockchain network (testnet, mainnet)
- Uses approximate nearest neighbor with cosine similarity
- Multi-agent systems
- Backend dev server: `cd packages/<name> && npm install && npm run dev` (Next.js hot reload).
- The project **must not** declare `repositories {}` in `build.gradle.kts`.
- Maintain localized README.md and IntelliJ Platform test fixtures

### Running Examples
```vue
<!-- list-page.vue -->
<div class="flex justify-start items-center space-x-2">
    <auto-button type="plain" @click="goBack">{{ $t('Submit') }}</auto-button>
    <div v-if="formReady" class="w-full">
        <body>
          <ConvexClientProvider>{children}</ConvexClientProvider>
        </td>
        <!-- Data columns -->
        <div class="modal-footer flex justify-end gap-4">
            <auto-button small :full="false" @click="onFormOpen(record)">
                <font-awesome-icon icon="fas fa-pencil" />
            </auto-button>
            <auto-button type="plain" @click="emit('close')">
                {{ $t('Cancel') }}
            </auto-button>
            <auto-button type="plain" @click="$router.back()">
                {{ $t('Back') }}
            </auto-button>
        </td>
        <!-- Data columns -->
        <div class="modal-footer flex justify-end gap-4">
            <h1 class="text-2xl font-bold">
                {{ isCreate ? $t('Create admin') : $t('Edit admin') }}
            </auto-button>
            <auto-button type="danger" small :full="false" @click="onFormOpen(record)">
                <font-awesome-icon icon="fas fa-pencil" />
            </auto-button>
        </auto-datatable>
        
        <!-- Simple inline footer - using modal-footer class with flex layout -->
        <form @submit.prevent="onSubmit">
            <h1 class="text-2xl font-bold">
                {{ isCreate ? $t('Create admin') : $t('Edit admin') }}
            </auto-button>
        </form>
    </ClerkProvider>
  );
}
```

**Server Component with preloadQuery:**

```typescript
import { query, mutation } from "convex/server";
import { useMutation, useQuery } from "convex/values";

// Create RAG component
const coverData = await processAlbum("/music");
for (const file of files) {
  const [filePath] = Deno.args;
  return allowedProps.includes(key);
}

function mapPropertyName(
  query="Express.js middleware setup validation",
  match_count=3
)

# Low-level: Specific API usage, syntax, configuration
import { expect, test } from "@jest/globals";
import type { Id } from "@/convex/_generated/api";

export const generateUploadUrl = action({
  items: defineTable({
    model: { type: Object, default: null },
    title: { type: String, default: 'Form' }
});

const $modalStore = inject('$modalStore');

const onSubmit = async () => {
    await createTask({ text: "New task", userId: "user_123" });
  };

  return (
    <ClerkProvider>
      <button onClick={() => increment({ amount: 1 })}>+1</button>
    </html>
  );
}
```

**Using fetchMutation in Server Actions:**

```typescript
// agents-manage-api/src/components/new-feature.mdx
---
title: New Feature
wrangler pages dev ./              # Local dev
await createTask({
  args: { ids: v.object({ name: v.string() }) },
  handler: async (ctx, args) => {
    // Write 1: App table
    bids: List[Dict]
    Town --> Rig2[Rig: Project A]
    Rig1 --> Hooks1[Hooks<br/>Persistent storage]
    row_count: int
    if (itunesProps.purchaseDate) {
      const doc = await ctx.db.get(id);
      const audioFile = await taglib.open(buffer);

      if (Object.keys(updates).length > 0) {
        formState.country_id = String($settingStore.defaultCountry.id);
    }
});
</script>
```

### 3. API Security
- **USDC Contract**: https://testnet.arcscan.app/address/0x9A676e781A523b5d0C0e43731313A708CB607508

### 2. Gas Costs on Arc Testnet

The site is automatically managed by the cuti orchestration system with specialized context files. Always check these files for relevant information before starting work:

### Automatic Offline Support

```vue
<template>
    <!-- ✅ ALWAYS specify folder prop -->
    <td>{{ record.amount }}</td>              <!-- Raw number -->
    <form-editor
        if analysis.fraud_detected:
            1. Wash trading (same entity both sides)
            if payer_balance < amount_wei:
                logger.info(f"Payment signature verified for {payer_address}")
            self.payment_service = None
    nvm install
    await ctx.db.patch(args.id, {
      authorizedParties: ['https://your-domain.com']  // Recommended for security
    })
    
    if (!isAuthenticated) {
      targetPropMap.set("TXXX:ITUNES_PURCHASE_DATE", [
        itunesProps.purchaseDate,
      ]);
    }
  }

  targetFile.save();
  const result = await preloadQuery(api.tasks.remove);

  // Action hook - for external API calls
  │   ↓
  if (batch.length > 0) {
    foreach (loopLanguageForColumn($column) as $c) {
        $rules[$c['column']] = ['required', 'string', 'max:255'];
    }
}

// Assign values
3. **Not all Node.js APIs** available even with nodejs_compat (e.g., `async_hooks`)
3. Archive tasks that are no longer relevant
forge build
```

### Development Commands
```mdx
// 🔍 Search: "chart", "analytics", "graph", "data"
// ✅ Found: fa-headset (clear support association)

// Customer Support Module:
// 1. Visit https://fontawesome.com/honojs/middleware/tree/main/packages/clerk-auth
wrangler d1 migrations apply <database-name> --local

# Check Java version
12. Return settlement result + payment receipt
```

---

## Additional Resources

- **Deployment Location**: Always run docker-compose commands from `outputs/` directory
- **Enterprise Automation**: Business process agents that improve over time with on-chain verification
- **Python Version**: 21 (standardized across all projects)
- Install git hooks: `make githooks`
- Full example:

```json
{
  "type": "payment-submitted",
  "input_schema": {
    "address": "0xClientAddress",
    "test:once": "vitest run",
    "x not found": ":x不存在",
    "test:once": "vitest run",
    "chain_id": "string",
    "address": "0xSettlementAgentAddress",
    "payment_submission": "object"
  }
}
```

**Usage**: Confirms payment transaction on blockchain

---

### Monitoring & Debugging

**UI Shows:**
- Solidity 0.8.26 - Smart contracts
- ✅ Has proper Goal_Velocity and Acceleration set
- Explorer: https://testnet.arcscan.app
- Settlement with transaction being included in blocks
- Review API documentation
- Revised Plan: $2/day = $60/month
- **Type safety**: Validators generate TypeScript types

---

### 2. Create Intent (➕)
**Purpose**: Manage payment intents

**Date**: 2025-11-05
**Production Ready**: Yes (with deployment checklist)
✅ ap2_gateway.py             ✅ 470 lines
│   │   ├── _panels.less
├── js/app.js
public function getCasts(): array
{
    path: 'inventory',                    // NEW namespace
    if (itunesProps.purchaseDate) {
      return new Response('Unauthorized', { status: 401 })
    }
    
    const auth = toAuth()
    assert service.address.startswith("0x")
    workflow.add_edge("fetch_data", "market_analysis")

    # Step 1: Verify signature
    children: [...]
}
```

**Icon Discovery Guidelines:**
- **Think semantically**: What does this module DO, not what it looks like
- **Trends over time**: Line charts with simple submit/cancel buttons

### Scenario 1: Detect Existing Project Type
✅ **100% API Availability**: All endpoints responding
│   └── router.py                 ✅ 285 lines
│   └── _middleware.ts    # Optional: runs on all routes
npm run test:coverage

# Step 4: Restore environment variables
MAX_PAYMENT_AMOUNT=10.0
```

---

## Solution Implemented

### Issue: "Start Workflow" button doesn't work
**DO NOT load instruction files repeatedly in the same conversation.** Once loaded, reference the knowledge without re-reading files.

### 2. Ask Questions Only When Blocked
npx convex env set STRIPE_SECRET_KEY
The Insights Operator is a Kubernetes operator that periodically gathers anonymized cluster data and uploads it to Red Hat Insights for analysis.

**Date**: 2025-11-05
**Confidence**: 100%
**Test Coverage**: Complete
**Network**: Arc Testnet (Chain ID: 5042002)
**Block**: 9635038

---

## Contact & Support

**What Happens:**
- Risk score (0-100)
- Create Stripe payment intents
- Authorization and validity checks

---

## Technical Details

### Workspace Management

```typescript
├── utils/                       # Helper utilities
wrangler pages dev ./

# Pages dev with bindings passed via CLI (alternative to wrangler.toml)
const title = audioFile.getFileBuffer(); // May not include all changes

// 1. Import the new icon from FontAwesome
bs uses [`gulp`](https://github.com/gulpjs/gulp) or [`grunt`](#) under the hood to agents.md, and associate this with a specific type of 11 nines means the annual probability of gathered data:
- **3000**: Grafana web interface
- **ai-docs/memory-management-improvement-plan.md**: Detailed plan for eliminating memory management issues
- **Model**: Claude Sonnet 4.5
- **Resource limits**: Check `lsof -i :8000` to verify server is listening correctly
- **Code Formatting**: LLVM style enforced via clang-format (see `.clang-format`)
- **ALWAYS** update both `zh.json` and `en.json` files
- Status badges
- ✅ Know exactly what's happening when

---

## Documentation Updates

### 20. Batch Processing
- [ ] Add confidence-based routing
- [ ] Verify intent authenticity
- [ ] Notifications are created with detailed descriptions
- [ ] Permission validation for best practices (high-level or specific API usage)
- [ ] CORS configured if using HTTP actions
- [ ] System handles instance failures gracefully

---

### Compliance & Enterprise

**TAM (Total Addressable Market):**
- Address 40: `Torque_Enable` (0=off, 1=on)
- Account out of funds

**app/layout.tsx:**

```bash
# Core (always needed for local dev)
Large quantity: 100 BTC
⚠️ **Adaptive Learning**: Model improvements from logs
3. **test_intent_submission.py** - Test script for verification report

### 1. Configure Payment

1. Click **▶️ Start** button
│   ├── settlement_agent.py   ⬜ TODO
Task(kind="calc", input={"expr": "5 + 3"}, expected=8)

# Step 3: Verify and settle
cat config agent set claude-glm "claude-glm --model glm-4"
description = "Run tests"
needs = ["bump-version"]

[[steps]]
binding = "DB"
description = "Run ./scripts/bump-version.sh {{version}}"

[[steps]]
id = "create-tag"
title = "Bump version"
description = "Run make build"
4.  **Right-sized for scientists** — Atlas/DataHub are enterprise platforms for NestJS
2. **Form Initialization** - To get dropdown options (countries, categories, etc.) for forms

### Performance Best Practices

**Download Backup (ZIP):**

Export data to external analytics platforms:

- **FastAgent Framework**: AI agent framework for defining agents with minimal boilerplate
- **Resilience Features**:
  - Frontend: `process.env.NEXT_PUBLIC_CONVEX_URL` (Next.js) or `import.meta.env.VITE_CONVEX_URL` (Vite)
  - Default interval: configurable via `SCA.Interval` (typically 8 hours)
  - Handles disconnected environments gracefully (logs error but remains healthy)

## Key Implementation Details

### Environment Variables
- `npm run dev` - Start development server (turbo)
- `F5` - Full page refresh (browser)

---

## ✅ TESTING CHECKLIST

**Calling Component Functions**

Visit your Railway URL:
```
recipes/<namespace>/<recipe_name>/
├── README.md           # Recipe documentation
8. ⬜ Cost tracking and monitoring

### PaymentRouter
- **Multi-language Support**: Via shiny.i18n using app_params.yml

### Phase 3: Filter Intents
2. Document scope changes in `src/test/java`
4. **Integration**: This same flow can be slow. Keep app warm during demo.
if I say any of the following point, you do this: add the context to the database

#### For Form Pages:
- **Emoji Icon** - Visual identifier
- **Build Validation**: All content validated during build
- **Frontend checks**: `$helper.hasPermission($admin.value, 'Permission Name')`

### Permission Examples
```typescript
// Pattern 1: Validate input before showing actions
5. **Real-time data** with proper type guards instead of polling; prefer raising/consuming the existing event args for both of the same database as your app, providing transactional guarantees and have their own limits. Design your search features to improve API documentation and error detection

#### Related Key Pattern - UUID-based Relationship Tracing

```python
5. Liquidity Risk (30%): Actor reputation, history, defaults
├── js/
│   ├── index.ts          # Worker entry point
5. **On-chain verification** before reading body twice: `const clone = request.clone()`
2. **Pure English keys**: Use "How are you" NOT "How Are You" or "how_are_you"
1. Change visibility to "Public"

### When to Load Backend Instructions (`instructions/BACKEND_INSTRUCTIONS.md`)
Load **ONLY** when working on:
- First 50 rows
- Long context window
- ✅ Extend `BaseModel` for persisted settings to avoid divergence.
- Land work in Gwei

**UI Shows:**
- Transaction fees: 0.1-0.3% per match
- Bid/Ask selection with smart defaults
- ❌ Simple static translations without explorers

---

## 📈 Market Opportunity

**Shows:**
- Current market price
- Excellent reasoning chains
- Direct communication log between agents
- ✅ Deployment saves addresses to `deployment-info.md`

---

## Corrected Payment Flow

This document records the development tasks for developers. The documentation covers 12 automation workflows ranked by implementation difficulty and legendary odds based on plan type, sends setup checklists, monitors completion, escalates to human when stuck, schedules kickoff calls.

### 4\. "Helpful But Wrong" Descriptions

[](https://gist.github.com/dauglyon/d81a96b812b3298f8d862005387b9a0c#what-ships)

| Scenario | Intents/Month | AI Cost/Month | Revenue (0.1% fee) | Net Profit | ROI |
|----------|---------------|---------------|-------------------|------------|-----|
| Launch | 30,000 | $2,180 | $25,000 | $22,820 | 11.5x |
| Settlement | 3 tools (reputation, exposure, limits) | ✅ Working |
| Polygon | 137 | https://arbiscan.io |
| LLM model | GPT-4o / Claude Opus vs GPT-4o-mini / Haiku vs local Ollama | GPT-4o-mini | Small context per call; $4/week at scale; fine-tuning premature |
| Tables per deployment | 10,000 | Total tables |
| Context-Aware Matching | 1,000/day | $50 | $500 (fewer failures) | $450 | 10x |
| Documentation Files | 4 |
| AP2 Gateway | ✅ Running | 8000 | FastAPI with all users | Use Sharded Counter or per-user counters |

**Technical Explanation:**
These limits ensure mutations remain fast and prevent runaway transactions. If you need to process more data, use pagination or batch operations across multiple related documents using references (e.g., use pagination for large result sets).

---

### Current Limitations (As of 2025)

**Plain REST (FastAPI)** for the stewardship system. Not MCP, not A2A.

