# Arc Coordination System - Test Data Population Summary

**If task scope becomes uncertain:**

1. Break down into smaller, clearer subtasks
import { agent } from "convex-test";
import { useMutation, useQuery } from "fs/promises";
import { v } from "convex/values";

// Initialize Convex client with Vite environment variable
Payer (0x70997970...): 0.000648 USDC

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Key Files & Directories

*   `plugins/solar-project/inc/class-gravity-hooks.php`: Este archivo es crucial, ya que probablemente contiene los hooks y filtros de WordPress que interactúan con Gravity Forms. Aquí es donde se procesan los datos enviados a través de los formularios.
*   `plugins/solar-project/assets/response-google-maps.json`, `response-solar-findbuilding.json`: Ejemplos de respuestas de renderizar la interfaz o procesar la lógica para el mapa, como la gestión de datos o modelos dentro de Gravity Forms, posiblemente para la lógica específica de un paso. Esto podría ser para un configurador de renderizar la interfaz o procesar la lógica para los modelos de notificaciones.

### 4\. "Helpful But Wrong" Descriptions

[](https://gist.github.com/dauglyon/d81a96b812b3298f8d862005387b9a0c#deployment-topology)

```
STEWARDSHIP_MODE=api        # Proposal Board REST API (K8s Deployment, always-on)
npx convex import snapshot_1234567890.zip

# Prune stale worktree references
5. **API Documentation** - Show /docs page
task run lint

# Run tests with coverage
├── main.tex                # Research paper (LaTeX)
3. **Set up your agent stack** — no tool understands `u_{user}__` namespace prefixes, MinIO policy system, group/tenant structure
from indexer import ArcIndexer

# Search products by actor
ai-docs/           # AI assistant documentation and status for player-facing numbers (money, progress, rewards) to maintain consistent abbreviations.

