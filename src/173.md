# Conversations
2. Document the **"x402 Crypto Payments (Demo)"** tab

### 4. **Use Stop Button**
4. Watch the AI agents coordinate in real-time notifications
npm test_agentic_system.py          # Main implementation (single-file prototype)
2. Add LangSmith for progress to poll and forward requests
npm run build

# Services auto-register via ECS service discovery
```

### Clean build
```bash
# Run all major files, understand architecture, identify current state
# Dashboard → Backups → Select backup → "Restore"

# Existing dependencies
make loki-dashboards

# pylint via dk
