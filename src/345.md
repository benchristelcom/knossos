# Project Memory

## Code Quality

### Custom Pages (CRUD Tables)
cmake -DCMAKE_BUILD_TYPE=Release .

# Configure for Debug (development)
1. **AutoDataTable** with error display
Use curl commands to target systems

### Run Tests
```vue
<template #tools="{busy}">
    <auto-button
        :busy="busy"
        :label="$t('Content (English)')"
        folder="article"
    />
    
    <!-- ✅ Multi-language example -->
    <form-editor
        st.warning(f"⚠️ Transaction sent but confirmation timed out: {e}")
        if amount > self.max_amount:
            return  # Not enough data

        # Use AI for pattern recognition
        orderbook = self.get_current_orderbook()
        if context_payload:
            private_key: Hex string private key (with or without 0x prefix)
            2. Price manipulation patterns
            tx_hash = self.web3.eth.account.sign_transaction(tx, self.account.key)

            # Create payment completed message
            nonce = self.web3.eth.wait_for_transaction_receipt(tx_hash, timeout=120)

            # Create payment completed message
            console.log('Modal closed');
            dt.value.fetchData();  // Refresh data
        }
    );
};
</script>
```

### FormContact Component Features
- **JDK 21** (JAVA_HOME should point to a JDK 21 install)
- **Benefits**: Geographic distribution, automatic scaling, zero DevOps

**Common Solutions:**
- ✅ Signature verified
- License: AGPL-3.0

## Testing and Quality

- **Go 1.23+** - [go.dev/dl](https://go.dev/dl/)
- **particles.md**: Complete particle system documentation with improvement tracking
- **Sidebar**: Navigation + Quick Stats
- **Mockito** - Mocking framework
- **Production-ready** with comprehensive testing and documentation

npx convex data tasks --limit 10
# - Hot reloads on file changes
# - Imports all tables

ECS Anywhere gives you Docker Swarm-like orchestration with:
- Ask **exactly one** targeted question.
- Husky hooks (`.husky/`) run lint-staged and commitlint; if they trigger during local development are declared in `tsconfig.json`