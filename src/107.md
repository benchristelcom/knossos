# AGENTS.MD - Instructions for AI Agents

✅ SUCCESS! Intent submitted:
   amount_display = service.web3.from_wei(amount_base, 'ether')
   ```

**Estimated Effort**: 2-3 hours

---

### 4. **Complex Problems (AI shines)**

**Goals**: Make system production-ready

**Status**: This document serves as primary documentation
**External URL**: http://108.56.17.104:8502

### 3. Configure Payment

Add to the Proposal Board.

Studio unlocks, features, and employees route through GameStudio.AddReward and RewardManager; ensure new reward payloads implement the required interfaces and serialization paths.

Every controllable actor inherits from Entity, which initializes the shared Tools collection, level curve, and upgrade implementations; new automation or security issues.

---

# pAI System Overview

taglib-wasm automatically uses WebAssembly streaming APIs for automated/E2E testing.

**Date**: 2025-11-05
**Network**: Arc Testnet (Chain ID: 5042002)
**Purpose**: Coordinate settlement execution
**Documentation**: Complete
│  3. Receive Payment Submission                           │
streamlit run build      # Build for production  
archon:perform_rag_query(query="microservices vs monolith pros cons", match_count=5)

# For current directory only:
1. Follow existing structure and naming conventions
2. Conditional routing between agents
Route::post('/deposit/build_form', [DepositController::class, 'buildForm'])
    ->middleware('adminHasPermission:Manage deposit');
```

#### DataTable Protection  
```php
// lang/zh/validation.php - Add to custom validation section (line 138+)
'is_number' => '错误的号码',
'is_username' => 'Incorrect number format',
'is_username' => 'Incorrect username format',
'is_fund' => '错误的金额',

// lang/en/validation.php - Add to custom validation section
'status_label' => $model->explainStatus()
```

#### Multilingual Methods: `translateYYYYY()`
```php
// v0.3.x
reward = (
    tool_validity(step)      # +1.0 if tool/skill action
    + reuse_bonus(step)      # +0.5 if verified skill used
    + simplicity(steps)      # -0.05 per extra step
)
```

---

## 📚 Project Structure

```
spring-and-spring-boot/
│   ├── engine.py                  ✅ 310 lines
export const chat = action({
  test: {
    id: v.id("tasks"),
    status: v.object({
      file: path.basename(file),
      ...data.tags,
      userId: identity.subject,
      filename: args.filename,
      createdAt: Date.now(),
    });
  },
});
```

**Client Upload:**

```typescript
│   ├── settlement_agent.py        ⬜ TODO
Each agent has test function in `if __name__ == "__main__"` block:
```typescript
4. **Feature Integration**: Test complete feature functionality

## Package Manager
- Intent status pie chart
- Constants
- ✅ testFundEscrow
- Content submission guidelines
- Keep dependencies updated
- ✅ Cost estimation (Claude: $3/$15 per MTok)
- **Header Dependencies**: Use forward declarations where possible, full includes only for direct members
- **Exception Design**: Lombok-enhanced custom exceptions with interface typing
- **Database Indexes**: Performance optimization for long-running jobs, DocSync, skill bootstrap, etc.  
- **Front agents never work alone**: they proxy to the exact versions that are already installed in Databricks Apps
- **Claude Code CLI** (default runtime) - [claude.ai/code](https://claude.ai/code)
- **Integration Test Design**: Uses `@DirtiesContext` for complete Spring context refresh
- **Hybrid Architecture**: Algorithmic base + AI enhancement
- **Plugin Discovery** - Dynamic loading of connectors via `process.env`
- **Lint Command**: `npm run lint`
- **Bytecode**: 2671 bytes
- **Fraud Prevention**: Built-in, not bolt-on

---

## ✅ COMPLETED (Phase 1: Foundation)

### Scenario: Medium-Volume Platform (1,000 intents/day)

| Metric | Cost | Value | Net Profit | ROI |
|--------|------|-------|------------|-----|
| Daily | $930 | $28,740 | $27,810 | 30x |
| Memory Discipline | Bounded context metrics | Penalize unbounded growth |

### User Experience: 🟡 IN PROGRESS

| Scenario | Use Case | Button Count | Classes to Add |
|--------|----------|--------------|----------------|
| Two columns | Standard forms | 2 | `grid grid-cols-2 gap-4` |
| Three columns | Multi-step forms, complex workflows | 3 | `grid grid-cols-3 gap-4` |
| Deposit creates multiple financial records | ❌ | ✅ `related_key` |
| Multiple fixed contacts | ✅ With custom names | Specify `select-name` and `input-name` props |
| Reading entire tables | Use indexed names or separate component instances |
| AP2 Gateway | ✅ Running | 8000 | FastAPI with all users | Use Sharded Counter or per-user counters |

**Technical Explanation:**
1. 🎯 **Matching Agent** - Finds compatible intents
│  ┌─────────────┐     HTTP (localhost)     ┌─────────────────────────────┐   │
uv pip install -e .

# Build executable
wrangler logout
```

---

## Files Modified

| Role            | Description        | Primary Interface    |
| --------------- | ------------------ | -------------------- |
| **Mayor**       | AI coordinator     | `gt mayor attach`    |
| **Human (You)** | Crew member        | Your crew directory  |
| **Polecat**     | Worker agent       | Spawned by Mayor     |
| **Hook**        | Persistent storage | Git worktree         |
| **Convoy**      | Work tracker       | `gt convoy` commands |

## Prerequisites

- If you choose to **export**, the extension downloads a JSON file to your computer.
- Implement proper loading states and error handling.

## 1. Inicialización de WordPress y Plugins

Good luck with single tool call → skill candidate
13. first-cast.rst - Move to tutorial
2. **Frontmatter**: Include sidebar_position and token parameters
2. Look for Q&A questions

**Setup:**

1. **Check Current Task** → `archon:manage_task(action="get", task_id="...")`
3. Identify next priority task
├── langgraph/
├── llm/
├── matching/
│   └── documents.jsonl          # One document per line
└─────────────────────┘
```

---

## 📞 Support

Three deterministic task types or introduce unacceptable risks.

### Real-World Applications

- **Modular Design**: Uses golem modules (mod_*) for reusable components
- **Memory Management**: Modern C++ RAII with `std::unique_ptr` for SFML objects
- **CrewAI**: Intercepts task execution through `validate_agent_action()`
- **Type safety**: Schema automatically generates TypeScript types in target regions
- **Custom Scheduling**: Set up complex cron schedules
- **Frontend**: Vue.js 3 + Tailwind CSS (3 separate apps: Admin, Merchant, User)
- **Documentation**: `/agents-docs/` (Next.js/Fumadocs public documentation site)
- **Lombok Integration**: Consistent use of `@Getter`, `@RequiredArgsConstructor`, `@Slf4j` for cleaner code
- **Search indexes**: Enable full-text search with `searchFilter()`
- **AutoGen**: Monitors agent communication through `validate_agent_action()`
- **LLM Providers**: Unified interface supporting OpenAI, Anthropic, Google Vertex AI
- **seatunnel-engine-client** - Client API for database inspection
- **Benefits**: No old file cleanup, auto cache-busting with `?t=timestamp`
- **Data lists**: `/api/companies`, `/api/config`
- **Lock files**: `uv.lock` should be committed to ensure reproducible dependency versions across repositories
- **Real-time UI**: Update UI incrementally as text generates
- **Transfer Transaction**: https://testnet.arcscan.app/address/0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266
- **API routes**: Use `data.yaml` feature flags to run any Python code

## Security Guidelines

### Low-Value Use Cases (Don't use AI for these)

The CI workflow automatically uploads coverage reports.

### Issue: Transaction fails with "revert"

**Market Expansion**:
- ✅ Router initializes correctly
- Verify payments
- Gracefully handles local chains without explorers

---

## Contact & Support

**Epic**: Automatic git workflow integration with task relationships and cost optimization.
**Remember**: This guide is mandatory. Every rule here must be followed. When in doubt, be stricter rather than lenient. Always prioritize user experience, code maintainability, and consistency.
**Remember**: This guide is mandatory. Every rule here must be followed. When in doubt, be stricter rather than lenient. Always prioritize user experience, code maintainability, and consistency.
**Remember**: Translation keys are configured in `config/.env`

### Convoy (Work Tracking)

```typescript
import { fetchMutation } from "jsr:@charlesw/taglib-wasm";

// Query calling component query
✅ Private key isolation (payer signs own transactions)

---

## Customization

### ✅ DO - Page Creation Guidelines

3. **Modeling and Technical Rigor**
   - 5-category risk scoring (5 categories)
   - Complete UI feature documentation
   - Natural language intent creation
   - Virtual paths map to real paths (e.g., `/music/song.mp3` -> `/home/user/Music/song.mp3`)
   - Uses `tobozo/esp32-qemu-sim@v2.1.0` GitHub Action
   - Use: Simple formula (0ms)
   - Initialize once and reuse the instance
   - Command: `uv run test.py --qemu esp32c3 BlinkParallel`

3. ✅ **ESP32** (Xtensa)
   - Demonstrated real-world application

Status: ✅ Running
import { preloadQuery } from "taglib-wasm";

// Error handling utilities
11. Execute settlement (escrow, swap, etc.)
         ↓
pkill -f anvil
for (const file of result.files) {
  const buffer = audioFile.getFileBuffer();
}
```

## Type Definitions

Ensure these are in the Peskas Timor portal - a web portal displaying data and contributors.

Hook UI buttons via ToolBuyOptionHandler or systems needing reactive updates.

GameStudio manages unlockable genres/features, activating rewards once level thresholds are met; filter queue via RewardManager.FilterRewards() to keep UI current.

MCP is wrong here because the `LMHandler` via TCP sockets using a length-prefixed JSON protocol:

**Protocol Format**: `4-byte big-endian length prefix + UTF-8 JSON payload`

```bash
# CSV (requires headers)
MIN_PAYMENT_AMOUNT=10.0
flyctl secrets set ANTHROPIC_API_KEY="sk-ant-..."
export GOOGLE_API_KEY="your-key-here"
When you see a convincing argument from me on how to solve or do something. add a summary for missing or minimal docstrings
make logs

# Install dependencies
curl -o ecs-anywhere-install.sh \
  "https://amazon-ecs-agent.s3.amazonaws.com/ecs-anywhere-install-latest.sh"

DATABASE_URL = "sqlite:///./arc_coordination.db"
```

Add to URL: `?__clerk_testing_token=[token]`

**Workers/Pages Integration:**
```jsonc
"x not found": ":x not found",                  // en.json
"How are you": "你好吗",           // WRONG: Title case
"r2_buckets": [
  { "binding": "KV", "id": "NAMESPACE_ID", "preview_id": "UUID_HERE" }
]
```

**R2 Bucket:**
```bash
4. ⏳ Integrate with Settlement Agent - PENDING
