# AGENTS.md — CodexJetbrains

## Project Feature Integration

### 3. Interactive Step-by-Step
- Target: $25M year 1 transaction volume

**Breaking Changes**: None
**System Version**: 1.0.0
**Manual cost:** $1,400/month (15 hrs/week)  
**Automated cost:** $90/month  
**Annual savings:** $24,840  
**Annual savings:** $10,080  
**Annual savings:** $36,600  
**Annual savings:** $10,080  
**Annual savings:** $26,400  
**Difficulty:** Low  

What it does: Monitors aging invoices, sends escalating follow-ups at 30/60/90 days, flags high-risk accounts, generates collection priority scores.

Never push your changes unless explicitly asked to do so. Let a human review them and push.

## MLflow Reference

- Snippets are stored locally using Chrome’s `chrome.storage.local`.

## Domain-specific Package Families (examples)
- **Builder Patterns**: `/configuration/src/builder/` (agent.ts, graph.ts)
- **Use for**: Dashboard-style pages, critical single actions, main business functions
- **Automatic updates**: Types regenerate when schema or functions change
- **Tools**: prepare_settlement, estimate_gas, verify_collateral
- **Color Border** - Unique color per agent

### Content Style
- **sound.md**: Sound effects system and audio feedback implementation

## Reference Links

### Before Starting
- ✅ Explainable AI with confidence scores
- Monitor activity

---

### Test 5: Transaction Status Query ✅

**The Fixes:**

**Files Modified:**
- `logging_feature_flag: false` - Syslog-ng metrics exporter
- `llm_query(prompt, model=None)`: For sub-LM calls
- `send_lm_request()` / `send_lm_request_batched()`: Helper functions for socket communication

### 3. Context-Aware Loading
```
✅ Test 1: Register AP2 Mandate - PASS
Use BigRational.Format() and NumberFormatter for on-demand Insights data gathering (TechPreview):
- **20: Smooth, gentle (recommended for 30 Hz control)** ✨
- ✅ Intelligent matching beyond competitors
- Stores data in SQLite for metadata

### Vector Database Specific
- Before editing:
  - reproduce → isolate root cause → fix → then expand.
- Use golden tests tied to your users
- Payer balance over time
```

### Option 2: Google Gemini 2.5 Pro

**Error Message**:
```
START
  await saveFile({ storageId, filename: file.name });
};
```

---

## Full Text Search

**Index Optimization:**

```typescript
4. Click verify
3. Configure your app → Settings → Secrets
6. **Standards-Based**: Follows x402 protocol specification

---

## 🧪 Testing

### Commits

### 22. Advanced NLP
- [ ] 30x ROI on AI costs
- [ ] Analyze price trends and error handling
- [ ] False positive rate < 5%
- [ ] Admin dashboard operational

### Performance Optimization

All contracts successfully deployed to Arc testnet
│                     LANGGRAPH SUPERVISOR                        │
const generateUploadUrl = useMutation(api.files.saveFile);

console.log(`\nNormalization stats:`);
console.log(`- With cover art: ${withCoverArt.length} files`);

// Find duplicates
7. **Enable logging** - Configure proper log aggregation
│   ├── risk_agent.py       # Market intelligence reports
├── risk_agent.py    ⬜ TODO
├── drizzle.config.ts     # Drizzle config (if using)
npx convex env set VARIABLE_NAME value --prod
```

**Best Practice:**

- AI chatbots with memory
- Market sentiment (Bullish/Bearish/Neutral)

**UI Shows:**
- Load instruction files multiple times
- Implement **proper watchers** with deep/immediate options
- **Image uploads** are processed at `/editor_upload_image` endpoint
- **Memory**: Efficient batch processing
- **Root Cause**: CSS class used `-mb-4` which created gap between footer and PaymentRouter
  - Design workspace sharing model
  - Default interval: configurable via `ClusterTransfer.Interval` (typically 12 hours)
  - Preserves heading and property metadata
  - ✅ Fixed: test.py now uses Docker compilation on Windows (test.py:133-134)
  - **Gas Cost**: ~0.000021 ETH

**Key Insight:** Acceleration must match your control loop frequency!
- Summary of all 7 steps
- 💳 Settlement Agent planning... ✅
- 🔮 First protocol partnerships

**Date**: 2025-11-05
**For models requiring admin operation tracking**

```bash
npx convex docs
# - Generates types from schema and functions
# - Orders by creation time descending
```

### Integration Tests

```bash
# Docs: https://developers.cloudflare.com/d1/reference/kv-commands/
# ============================================================================

# Create new project interactively
def _request_payment(self, tool_input: Dict[str, Any]) -> Dict[str, Any]:
    """Request x402 payment service on-chain"""
    service = X402PaymentService(
        payment_submission: Dict[str, Any]
    ) -> bool:
        """
        Verify payment signature from client (x402 payment-required)

        if self.is_first_time_trader(match.bidder):
            risk.add_flag("amount_exceeded", severity=HIGH)

        Args:
            private_key: Hex string private key (with or without 0x prefix)
            2. Price manipulation patterns
            dt.value.fetchData();
        }
    );
};
</script>
```

**Payer Account**:
```
services/agents/
│   ├── __init__.py               ✅ 14 lines
const taglib = await readTagsBatch(files, { concurrency: 8 });

for (const { file, data } of metadata.results) {
  throw new Error(`File too small: ${buffer.byteLength} bytes`);
}

// Pattern 2: Track memory usage
├── css/style.css
6. **Skill Promotion**: Add to graph with verification

### HTTP Broker Pattern (Isolated Environments)

1. **Plan First**
   - Multi-factor risk scoring (5 categories)
   - Merged binary generation working
   - Depth of data limitations or biases
   - Example: `api_key_command: "pass my-api-key | head -1"`

3. **YAML Anchor References**:
   - Anthropic: https://console.anthropic.com/
   - Google AI: https://makersuite.google.com/app/apikey

public function submitForm(Request $request)
{
    $data = [];
    
    // Load existing model for editing
    const imageType = detectImageType(coverData);

    // 3. Combine results
    children: [
        {
            path: 'product/list',
            component: () => import('./pages/main/inventory/product/list-page.vue'),
            name: 'admin.inventory.product.form',
        },
        {
            path: 'product/form/:id?',
            component: () => import('./pages/main/inventory/product/list-page.vue'),
            name: 'admin.inventory.product.list', 
            permissions: ['Manage product']
        },
        {
            name: 'Manage category', 
            permissions: ['Inventory report']
        },
    ]
},
```

**Sub-Level Guidelines:**
- **ALWAYS include** `icon` property for access control
- **Key spec fields**:
  - `conditions` - Status of gathering phases (DataUploaded, DataRecorded, DataProcessed, RemoteConfiguration*)
  - `gatherers` - Individual gatherer statuses with execution time
  - `IndexingError`: Document indexing failures
  - `pcluster/` - AWS ParallelCluster recipes
  - `ConfigurationError`: Invalid configuration
  - `dir/` - Directory services recipes
  - `InsightsRecommendationActive` - Fires when SCA certificates are unavailable
  - `prepare_payment_transaction()` ✓
  - `get_transaction_received()` ✓
  - `InsightsRecommendationActive` - Fires for each active Insights recommendation
- **Conditions**: Use path aliases (`~/` for src/), group imports (React, libraries, internal)
- **CMake 3.16+**
- **Optional**: [Ollama](https://ollama.ai/) for model-based policy (not required for baseline)

### Metadata Terms

- **Total Files Created**: 20+
- **Smart Contracts**: 3 (with full test coverage)
- **Browsers/Workers**: Uses Emscripten (web compatibility)

```bash
# Show block explorer link (Arc testnet + USDC configured)
3. `get_market_depth` - Order book depth and adversarial inputs

---

## 📦 Deliverables

### 5. **Refresh for New Run**
