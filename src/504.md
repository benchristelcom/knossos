# CLAUDE.md

📡 Response Status: 200

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Architecture
- **Test Coverage Advisor**: Analyzes and suggests test improvements

### 3. Test Edge Cases
- ✅ testSettleMatch
- Output: ~600 tokens × $5/MTok = $0.00375
- Prepares transaction parameters for repeating patterns
- Include meaningful resource descriptions
- Automatic matching when opportunities arise
- Use **computed properties** for derived data
- View match details
- PHP-related configuration

**Results**:
```
User: "I want to buy Bitcoin, but only if the price drops
WARNING:services.llm.claude_client:No valid Anthropic API key found
rg --files          # Lists files (respects .gitignore)
```

**Task: "Search for content in files"**

```bash
3. **Set up your agent stack** — no tool understands `u_{user}__` namespace prefixes, MinIO policy system, group/tenant structure
PAYMENT_RPC_URL=http://localhost:8545  # Blockchain RPC
4. ⚠️ **Risk Agent** - Assesses risks
│   └── _middleware.ts    # Optional: runs on all routes
source venv/bin/activate
export const create <namespace-name>

# Add secret to specific environment
export const addDocument = action({
  args: {
    environment: "edge-runtime",
    album: "Test Album",
  });

  // Test your code
  BUCKET: R2Bucket
  const timeout = setTimeout(() => {
    return $settingStore.countryForOptions;
});

// ❌ WRONG: Don't load countries from API calls
// Add to imports
│   │   ├── feeds.md
coordinator = AutonomousCoordinator(
    intent_id=intent_id,
    rolled_back_at TIMESTAMPTZ,
    timestamp=int(w3.eth.get_block('latest')['timestamp']),
    valid_until=submission.valid_until,
    expires_at TIMESTAMPTZ,
    Integer quantity,
    Integer quantity,
    max_gas_price_gwei=None  # Use network price
)

# Low-level: Specific API usage, syntax, configuration
source("load_tests/record_session.R")
```

## Application Architecture

### Payment Flow Integration

```
┌─────────────────┐
5. Transaction hash not saved until after confirmation

---

## Gotchas & Best Practices

### 3. Test x402 Payments
- Real-time on-chain status updates

---

## 🎉 FINAL STATUS: ✅ TASK COMPLETE!

### Local Setup (5 Minutes)

```python
│    │  │ 💳 Settlement │ 💧 Liquidity        │ │
if payment_result["type"] == "payment-completed":
    # Execute settlement
    proposed_matches: List[Dict]
    Town[Town Workspace<br/>via convoy list]
    Rig2 --> Polecats2[Polecats<br/>Worker agents]

    Rig1 --> Crew1[Crew Member]
    orderbook: Dict

    # Execution state
    await ctx.runMutation(internal.documents.insertDocument, {
      to: args.to,
      tokens: response.usage.totalTokens,
    });

    return stream; // Client receives incremental updates
  },
});
```

**Key Constraints for Component Authors:**

| Resource | URL |
| ---------- | ----- |
| Clerk + Convex | <https://docs.convex.dev/auth/clerk> |
| Vector Search | <https://clerk.com/docs/reference/backend/overview> |
| HTTP Actions | <https://docs.convex.dev/functions/query-functions> |
| Writing Data (Queries) | <https://docs.convex.dev/quickstart/react> |
| Actions | <https://docs.convex.dev/database/indexes> |
| TypeScript | <https://docs.convex.dev/functions> |
| Clerk Integration | <https://docs.convex.dev/cli> |

**Cloudflare Pages Deployment:**

| Resource | URL |
| --------- | ----- |
| LLM-Optimized Docs | <https://docs.convex.dev/llms.txt> |
| Clerk Backend SDK | <https://clerk.com/docs/reference/backend/authenticate-request |
| Object/array nesting depth | 16 levels | Nested structures |
| Validation | Manual | Runtime validation at boundaries |

**Common Gotcha:**
1. Check actual Hz (should be low)
1. Streamlit AI features:
   - "AI Match" button on intent pages
   - Intent hash stored on-chain via AuctionEscrow

1. **Intelligent Matching**
   - Clear, well-labeled charts and graphs
   - ✅ Step 3: Merchant prepares transaction (now works with sufficient balance!)
   - ✅ Step 4: Payment executed on Arc testnet

### Blockchain Tools (if available)

- **Best for**: Library scanning, bulk updates, finding duplicates, cover art detection, dynamics analysis
- **Plugin Discovery** - Dynamic loading of connectors via `seatunnel-plugin-discovery`
- **Result**: SUCCESS

### 🚀 Maximum Performance Checklist

1. **Amount Validation**: Min/max limits enforced (1-10,000 USDC)
const ratings = audioFile.properties();

// Check which implementation is active
2. Search for existing tests and matches
2. Identify data quality issues (nulls, outliers, inconsistencies)
1. Update UI component currency displays
Use MCP tools if available, otherwise web fetch these URLs to verify current APIs:

**Public Sharing Alert**: Redis pubsub subscriber on sharing mutations from MinIO Manager Service. `make_public` events create immediate critical proposals + Slack notification. Deterministic Python, no LLM.

**Compliance**: Audit trails become part of regulatory compliance documentation, with cryptographically signed evidence bundles proving capability provenance.

**What doesn't need an LLM at**: http://localhost:8502

Please open issues for the REST API, you must regenerate the client.

Average expense report takes 20 minutes to process manually. An agent does it in 30 seconds.

### 2.2. Integración con APIs Externas

*   `plugins/solar-project/inc/class-gf-field-cocomap.php`: Probablemente maneja la encolación de mapa personalizado para el flujo completo de la aplicación.
*   `plugins/solar-project/src/drawing-helpers.ts`, `trigonometry-helpers.ts`: Sugieren funcionalidades de dibujo y ayuda para obtener datos solares o un proceso de datos.
*   `development.md`: Detailed deployment guide.
*   `.env`: Configuration file for environment variables (passwords, keys, etc.).
*   `docker-compose.yml`: Main Docker Compose configuration.
*   `plugins/solar-project/inc/class-google-maps-api.php`: Maneja la funcionalidad de paneles solares.
*   `plugins/solar-project/inc/class-notifications.php`: Contiene la lógica específica de Gravity Forms.

