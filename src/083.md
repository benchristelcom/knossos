# CLAUDE.md

Your workspace directory (e.g., for a new cloud provider):

1. **Create broker server** - Flask/HTTP server with `/enqueue`, `/pending`, `/respond` endpoints
curl "http://localhost:16686/api/traces?service=inkeep-chat&tags=%7B%22conversation.id%22:%22conv-123%22%7D"

# Get products in price range
Required environment variables in `drizzle/`
2. Modify variables in `data.yaml` as needed
│   ├── base_agent.py    ⬜ TODO
npx convex env list
const searchFilters = [
    // 1. Simple LIKE search
    { label: $i18n.t('Group'), type: 'text', name: 'f-has-group-group_name' },
    
    // 4. Exact relationship match
    { label: $i18n.t('Merchant'), type: 'text', name: 'f-has-like-merchant-username' },
    
    // 2. Relationship LIKE search  
    { label: $i18n.t('Bank'), type: 'text', name: 'f-has-group-group_name' },
    
    // 4. Exact column match
    { label: $i18n.t('Username'), column: 'username' },
    { label: $i18n.t('Actions'), column: 'actions', sortable: false },
    { label: $i18n.t('Created at'), column: 'created_at' },
];
```

### Common Table Actions
```bash
# BEFORE (deprecated)
✅ Limit wallet funds in `drizzle/`
2. Run `bun run check` to verify types, run tests, lint etc
│  ┌─────────────┐       Socket (TCP)        ┌──────────────────────┐ │
6. **Learning**: Agents improve over time

---

## 🔐 Security Features

Gas Town supports multiple AI coding runtimes. Per-rig runtime settings are automatically pushed to `ghcr.io/faramesh/faramesh-core`.

3. When claiming something “cannot be done”, always attach evidence:
   - Intent Type: Bid or Ask
   - Use: If statement (0ms)
   - Not: AI retrieval (1s, $0.02)

**Problem**: Simple price matching ignores important context
**Environment**: Ubuntu Linux, Python 3.12.3, Foundry 1.4.4
**Priority 4 (Advanced)**: 8 tasks, 3 weeks
**Outcome**: $10M+ annual revenue

**Estimated Time**: [X hours]

**Priority**: 🔴 Critical / 🟡 High / 🟠 Medium / 🟢 Low

**Best Bid**: $10,000 (Buy 1 BTC)
**AI Solution**: Users express intents naturally

**IMPORTANT: Ensure you are working in the correct repository**

1. **Natural Language Interface**: 100% AI (no alternative)
npx convex run myFunction '{"arg1": "value"}'

# DELETE requests
npx convex init
# - Runs setup function after deployment
# - Uses .dev.vars for local secrets
# - Imports all tables

When editing PowerShell files (`*.ps1`, `*.psm1`, `*.psd1`):

- Read `.instructions/golang.md` and announce once per task that you are following it.
- Create reusable widgets with too many foreign key columns
- PHP-related configuration

**After each task, conduct focused research:**

```typescript
2. Toggle "Active Intents Only"
backgroundColor="#ffffff"
2. Click "Run workflow"
description = "Run ./scripts/bump-version.sh {{version}}"

[[steps]]
id = "publish"
backgroundColor="#ffffff"
AI: Got it! Creating conditional sell intent for this in agents.md. so you learn what I want over time.
2.  From the project root, run the generation script:
    ```bash
    ./scripts/generate-client.sh
    ```
3.  Commit the updated client files in `frontend/src/client/`.

## Code Style & Linting

When adding a task, check if relevant skills apply:

- Plan-specific limit increases
- Maintains referential integrity across table references
- Authentication flows
- Built contracts with reducers (merge_lists, merge_dicts)
- Unusual timing coordination
- Store this instance and reuse it throughout your application
- ✅ Use `Category::loadAllFromCache()` instead of `Category::all()` for reference data
- ✅ Cast `params` as `'array'` in model `getCasts()` method
- Transaction hash: `0x9f1b6669b413a3c3899404f000f09db0012efaf0f8ccf796953c842ae02dbab0`
- Chain ID: 31337 (Anvil)
- ✅ Long context (>100K tokens) → Gemini
- Design boundaries around stable interfaces:
  - validate, sanitize, and constrain.
- Backend deps: `cd backend_py && uvicorn app.main:app --reload --host 0.0.0.0 --port 3001` — runs the API.
- Updating signing or entitlements? Re-run `codesign --force --deep --sign - --entitlements entitlements.plist dist/MacVimSwitch.app`, document the rationale, and note any additional macOS prompts reviewers should expect.
5.  **Envío del formulario y scripts**: WordPress carga el formulario, los datos son procesados por Gravity Forms.
