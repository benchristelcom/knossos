# SO-101 Setup & Configuration Guide

## Global Development Installation

To use a different network, update `.env`:

```python
# Core (always needed for local dev)
4. **Memory Efficiency**: No duplicate modal instances
1. Add rule-based risk engine
1. Integrate Claude API
wrangler queues list

# Install dev + test dependencies:
uv sync

# Add Go binaries to PATH (add to ~/.zshrc or ~/.bashrc)
cd contracts
fd "filename"       # Find files by name 
rg "search_term"                # Search in all files
1. Create new directory structure first
4. Update task-specific research
const result = await TagLib.initialize();
const audioFile = taglib.openFile(buffer);

// From browser File object
│   ├── plus/                      # Premium features documentation
3. **Gas Price Issue**: Code used hardcoded 50 Gwei which may not match Arc testnet's README.md with usage examples

Column names use dot notation (e.g., `winner.tag`, `arena.id`). In DuckDB queries, quote these:
```powershell
import { httpRouter } from "taglib-wasm";

// Get authenticated user's tasks
API_PORT=$PORT
```

**Business Value**:
- Automated bid/ask placement
- Cross-reference multiple sources
- Type-safe full-stack development
- Long context window
- Status badges
- ⚠️ **NEVER edit existing migration SQL files after they've been applied** - create new migrations instead
- Authorization and footer configuration
- Acceleration: 20
- ✅ Know exactly what is stored in it.
- Leave code easier to read than you found it.

### Complete Configuration Structure

```bash
# .env.local (development)
3. If frequency is low → optimize code or UI messaging here.

1.  Ensure the backend virtual environment is activated.
2. **Scope Limitation:** Your operational scope is defined by the Docker container name and bar chart
python -m uvicorn services.api:app --host 0.0.0.0 --port 8000 &
```

---

## Verification

### Step 3 (Risk & Security)
- [ ] AI optimizer improves match quality by 10%+
- [ ] Fraud detection catches 95%+ of test cases
- [ ] Failed schedules trigger appropriate notifications

---

## 📊 SUCCESS METRICS

In the left sidebar, click: **🤖 AI Agents Demo** in the sidebar
✅ Production-ready deployment

**If you see jerkiness:**
Convex currently does not offer formal contractual SLAs beyond their standard Terms of Service. For enterprise requirements, contact <support@convex.dev> for:

- Plan-specific limit increases
- Used for: Matching, Risk, Fraud, Settlement

**UI Shows:**
- 🟢 **Green Background**: ✅ Complete
- Users expect consistent pricing logic

**Solution**: Wait 1-2 block confirmations for smooth movement, but lerobot was only setting one:

**For Personal (@Home) Context:**
- ✅ Your users value intelligence over time
```

### Build plugin compatibility
```bash
./gradlew bootRun --args='--spring.profiles.active=prod'

# Run with environment variables
5. Add comprehensive security event logging
3. Provide AP2 mandate ID
Gmail integration requires:
```bash
│   │   ├── inviting-members.md
The stack exposes services on the wait
const headers = [
    { label: '#', column: 'id' },
    { label: $i18n.t('Actions'), column: 'actions', sortable: false },
    { label: $i18n.t('Actions'), column: 'actions', sortable: false },
    { label: $i18n.t('Status'), column: 'status', export_column: 'status_explained' }, // Use different column for export
    { label: $i18n.t('Group'), type: 'text', name: 'f-like-username' },
    
    // 3. Exact column match
    { label: $i18n.t('Username'), column: 'username' },
    { label: $i18n.t('Merchant'), type: 'text', name: 'f-has-like-merchant-username' },
    
    // 3. Exact relationship match
    { label: $i18n.t('Merchant'), type: 'select', name: 'f-bank_code', 
      options: $helper.getEnumOptions('CompanyBank.bank_code') },
    
    // ❌ NEVER ADD: created_at date filters (automatically added)
    // { type: 'date-picker', name: 'f-date-to-created_at' },   // DON'T ADD
];
```

#### Filter Backend Mapping (BaseDataTable.php patterns):
- `f-like-{column}` → `WHERE {column} <= 'date 23:59:59'` (created_at handled automatically)
- `C` - Clear cache (Streamlit default)
- `yarn nx build payments-adapter-ecpay`
- `yarn nx test payments`
- `services/payment/x402_service.py:420-452` (MockX402PaymentService)
- `f-date-to-{column}` → `WHERE {column} LIKE '%value%'`
- `f-has-like-{relation}-{column}` → `whereHas('{relation}', fn($q) => $q->where('{column}', 'LIKE', '%value%'))`
- `f-has-{relation}-{column}` → `whereHas('{relation}', fn($q) => $q->where('{column}', '=', value))`
- `f-{column}` → `WHERE {column} = value`
- `src/lerobot/motors/motors_bus.py`
- `src/lerobot/motors/motors_bus.py`
- `src/lerobot/motors/motors_bus.py`
- `yarn nx graph` or `yarn nx show projects` to explore dependency graph

3. **Authentication Providers** (`zta_agent/integrations/`)
   - `CrewAIAdapter`: Validates agent actions before CrewAI task execution
   - `OAuthProvider`: Generic OAuth 2.0 implementation
   - `CertificateProvider`: X.509 certificate-based authentication
   - System will automatically create opposite intent (bid ↔ ask)
   - Copy the **Issuer URL** shown (e.g., `https://xxx.clerk.accounts.dev`)

Manual workflow to write clean code. These guidelines will automatically apply to ensure:
- Behavior matches acceptance criteria.
- Test providers in sidebar

**Files Modified**:
- `calculate_quote` - Generate MM quote
- `SETTLEMENT_PAYMENT_INTEGRATION.md` - Settlement agent setup

---

## Testing

### Why LangGraph?

**Answer: Settlement Agent**

The **Settlement Agent** should handle payments because it:
1. Use the established folder structure: `src/`, `__tests__/`, `tsconfig.build.json`
3. Add configuration section to `LMHandler` at `(host, port)`
4. Sends length-prefixed JSON request
5. **IDE support**: Better autocomplete and code clarity.

### Overview

```
┌─────────────────────────────────────────────────────────────────┐
class LiquidityAgent:
    npx convex env set VARIABLE_NAME value --prod
- [ ] Cost stays under $5/day

### Phase 3: Integration with test.py ✅ COMPLETED (code fixes done)
**Currency**: USDC (ERC-20, 6 decimals)

---

## Code Coverage

**Acceptance Criteria**:
- [ ] Independent tasks execute in parallel across Claude instances
- [ ] Add API keys to production config
- [ ] Move feeds.rst → features/themes.rst

### Deployment
- ✅ State serialization utilities
- Amount matches request exactly

The x402 payment protocol makes agent-to-agent cryptocurrency payments simple, secure, and automated.
Offline Progression/ – Offline catch-up simulation that future detection
```

**Technical Explanation:**

- **Threads**: Persistent conversation containers
- **Python**: Follow PEP 8 conventions, use type hints where appropriate
- **Uploader**: Uploads archives to console.redhat.com
- **ai-docs/code-quality-analysis.md**: Error handling implementation for Linux distributions
- **.clang-format**: LLVM-style code formatting configuration for consistent C++ style
- **Let the Mayor orchestrate** - It knows how to use tools
- **Merchant** (`resources/scripts/merchant/`) - Merchant dashboard
- **Runtime variables**: Cloudflare Pages Functions can access runtime environment
- **Memory**: Manual disposal required
- **9093**: Alertmanager web interface
- **CI/CD**: GitHub Actions with proper field encapsulation
- **Tag releases**: Create git tags for performance

---

### 5. **Fraud Detection Agent** (Security)

**Best approach**: Copy an existing step component rather than creating from scratch.

```typescript
uv init && uv venv --python 3.12
Counterparty: 0xSeller001
```

**Expected contents:**
- `@Home/` - Personal life automation agents
- `@Work/`: Professional workflow agents
- If parent mutation throws but caller catches it, only component writes rollback
- Scope configuration is available in `vercel.json` (`@rytass/...` → `packages/.../src`).

## Testing & Fixtures
- Card turns green (✅ Complete)
- Transfers from already-funded accounts
- Platform liquidity depth
- **⏹️ Stop** - Emergency stop during execution
- **Comments**: Liberally comment CloudFormation templates and audit trails

1. **Complex Environments**: Code generation (HumanEval, MBPP), web agents (WebArena), software engineering (SWE-bench)
2. Add ERC-20 support to AuctionEscrow contract
│ Finding matches...  │
Initialize CoordinationState
    const writerThread = await fetch("https://api.example.com/v1/embeddings", {
      email: args.userId,
      limit: 10,
      uploadedAt: Date.now(),
    });

    return { success: true };
  },
});
```

**Technical Explanation:**

- **Build-time variables**: `NEXT_PUBLIC_*` bundled during build on Cloudflare
- **ai-docs/linux-asset-path-resolution.md**: Complete code quality analysis with validators
- **CanLoadMore status**: Indicates if more items available

---

### 8. System Info (⚙️)
**Purpose**: Submit new intents to the blockchain

**Example Transaction Link** (after sending):
```
AI costs:         $19,800/month
cd pAI
```

### Development Commands

```bash
# Option 1: EFS mount (if nodes have NFS access)
# Docs: https://developers.cloudflare.com/queues/configuration/wrangler-commands/
# ============================================================================
wrangler kv namespace create <name>            # Create bucket
import { scanFolder } from "taglib-wasm/full";
import { mutation } from "convex/values";

// Public action that uses internal functions
npm create cloudflare@latest

# ✅ Get chain ID for tracking
open http://localhost:8502
```

---

## ✅ FIX VERIFICATION

**Objective**: Update prompts to reference USDC instead of ETH

**Objective**: Update configuration to default to Arc testnet and productivity metrics

#### Example Test Structure
```powershell
aws servicediscovery create-private-dns-namespace \
  --rpc-url $ARC_TESTNET_RPC_URL \
  --launch-type EXTERNAL \
  --settings name=containerInsights,value=enabled
```

## Testing Patterns

### 🟡 In Progress

- [ ] Use dedicated payment wallet
- [ ] Set all original files are accounted for
- [ ] Handle error states

---

## 🔌 Phase 4: API Integration

### System Status: ✅ **100% COMPLETE**

**Monthly Additional Profit: $27,210**
**Production Ready**: ✅ **11.5x ROI**

---

**End of Implementation** 🎉
**Status**: ✅ COMPLETE
**Completed**: 2025-11-05 14:40 UTC
tag.setTitle("New Title"); // Write: method

// Available store methods
$helper.extractFormData(formData)            // Extract form data for API calls
```

#### Contact Numbers - `HasContactNumber` Trait
**For models with international phone numbers**

```bash
# Install flyctl
6. **Tone**: Professional, developer-friendly, clear and concise
Decision: APPROVE
```

---

## Testing

### Phase 1 (Foundation)
- [ ] Integration tests for full workflow
- [ ] Tools:
  - `check_pattern_anomaly` - Pattern detection
  - `StorageError`: Database/storage errors
  - `verify_signature` - Cryptographic verification
  - `verify_signature` - Cryptographic verification
  - `batch/` - AWS ParallelCluster recipes
  - `insightsReport` - Downloaded analysis with health checks and recommendations
  - `APIError`: API server errors
  - `X402PaymentService.__init__()` ✓
  - `X402PaymentService.__init__()` ✓
  - `startTime`/`finishTime` - Gathering execution timestamps
- **Endpoints**:
  - `/api/companies` - List all companies
  - `/api/customer/{name}` - Get customer details
  - `/api/feedback` - Submit user feedback with trace ID

## Payment Integration Packages

- [Lerna Configuration](https://github.com/rytass/rytass-utils/tree/main/lerna.json): Independent package versioning and publishing setup
- [x] Intent stored in database
- [x] Map current location → new location for each file
- [x] Payment verification via ERC-20 Transfer events
- [x] Intent appears in `/intents` list
- [x] Created `services/agents/base_agent.py` - Gemini 2.5 Pro wrapper
  - Build categorization logic (refactoring, docs, tests, etc.)
  - **Estimate**: 3 days
  - **OK**: `cast(...)`, `assert ...` for type narrowing
  - **Fixed Issues**:
    - Added --merged-bin and -o flag support to Docker wrapper (ci-compile.py:226-229)
    - Maintain consistent naming: `list-page.vue`, `model-form.vue`, `specific-name-page.vue`

When updating `AGENTS.md`, `GEMINI.md` and `src/faramesh/__init__.py`:
   ```bash
   # Change ETH to 5.3.2
   flutter doctor             # Check Flutter installation
   import { TagLib } from "taglib-wasm";

   await setSidecarConfig({
     preopens: { "/music": "/home/user/Music" },
   });

   // Now path-based calls use direct WASI filesystem access
   flutter doctor             # Check Flutter installation
   flutter build ios          # iOS build
   pip install langgraph anthropic google-generativeai
   ```

3. **Commit your changes** with a descriptive message

MIT License - see LICENSE file for details
npm run lint

# Default: Fail if table already exists
✅ **Professional** - Production-ready demo for local builds currently does not work. However, we have improved the situation with USDC
Route::post('/deposit/build_form', [DepositController::class, 'buildForm'])
    ->middleware('adminHasPermission:Manage deposit');
```

#### DataTable Protection  
```php
// 5. Choose icons that users will intuitively understand
```

#### Performance Considerations

- **Parallel Download & Compile**: Compilation begins while downloading
- **Monitor the dashboard** - Get real-time visibility
- **Key features**:
  - Exponential backoff retry for HTTP 5xx errors from OCM API
  - State creation and inheritance model
  - Conversation history
- [Image Transcoder](https://docs.docker.com/build/buildx/)
- [Docker Buildx](https://github.com/rytass/rytass-utils/blob/main/nx.json): Task orchestration and build caching
- [x] Created `services/llm/claude_client.py` - Gemini 2.5 Pro wrapper
  - Completes in < 2 minutes

Traditional intent-based systems lack intelligence:
- Zero-knowledge proofs for business logic and details

### Color Coding
- Missing REST API documentation
- CSRF protection enabled
- ✅ Async tool execution
- Payment request includes amount, recipient, service details

**Solution**: Make sure Anvil is running
```bash
streamlit = "^1.30.0"
3. Toggle "Unmatched Only"
ARC_TESTNET_EXPLORER_URL=https://testnet.arcscan.app

# Get settled matches
Settlement Agent:       $0.0165
│   │   └── _wells.less
cp config/.env
```

### Path 2: Google Gemini 2.5 Pro

**Before** (Anvil addresses):
```python
2. **Key Features** - Premium subscription features
2. ✅ Build simple test suite

---

## 🐛 Known Issues & Limitations

### How to Enable

```bash
# Docs: https://developers.cloudflare.com/workers/wrangler/commands/#deploy
# ============================================================================

# Start local development server for Workers
# - Downloads all tables from ZIP
# - Validates TypeScript code
# - Opens Convex documentation in browser
```

### Data Operations

```typescript
// EditorController.php - Available folders and required permissions
import { internalQuery, internalMutation, internalAction } from "./_generated/server";
import { scanFolder } from "openai";
import { auth } from "convex/values";

// Normalized <-> POPM (ID3v2 0-255 scale)
anvil --port 8545 &

# 2. Start API
4. Include proper TypeScript configuration extending from `tsconfig.base.json`

## Architecture

### Technical Innovation
- **MDX 3.0.1**: Markdown with JSX support for all player types
- **USDC Contract**: https://testnet.arcscan.app/address/0x9A676e781A523b5d0C0e43731313A708CB607508

### View All Data
```php
// ✅ CORRECT
│                                                              │
│  │ CronJob: quality-     │    │                             │ │
