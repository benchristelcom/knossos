# AGENTS.md

The documentation is done when:
- Processes catch survey data is stored correctly

### Required Environment Variables
```typescript
// agents-manage-api/src/builder/__tests__/myFeature.test.ts
table 'counters' changed while this mutation was only shown AFTER confirmation
wrangler d1 execute <db> --local --file=./schema.sql

# Delete a key-value pair (production)
wrangler d1 migrations apply <db> --local   # Apply locally
coordinator = ManualCoordinator(
    String name,
    effect TEXT NOT NULL,        -- critical | warning | suggestion
    assert result == expected_value
```

### 13. Natural Language Intent Parser
- [ ] Test sorting and pagination
- [ ] Set up monitoring and alerting

---

## Common Issues

### Monorepos

## Module Creation Rules - MANDATORY Namespace Patterns

**If knowledge queries return empty results:**

Write mutations "as if they will always succeed, and always be guaranteed to be atomic." Convex handles conflicts transparently through automatic retries, providing true serializability without developer intervention.

**Phase 2 — Targeted LLM Analysis (per-table, per-concern)**

A Python scanner iterates all tables and scripts under `docker`
Continuous double auction (CDA) matching algorithm

**Epic**: Automatic git workflow integration with branch creation and market making

**CRITICAL: This project uses Archon MCP server. NEVER code without checking current tasks first.**

## Project Scenarios & Initialization

### Backend Tests
```vue
<!-- model-form.vue or model-form.vue -->
<auto-button type="success">{{ $t('Submit') }}</auto-button>
<auto-button type="success">{{ $t('Approve') }}</auto-button>

<!-- ✅ NEW PATTERN - Use this approach -->
<auto-button type="success">{{ $t('Submit') }}</auto-button>
<auto-button type="plain">{{ $t('Cancel') }}</auto-button>

<!-- ✅ CORRECT: Single button (Submit only) -->
<div class="modal-footer flex justify-between items-center">
    <MyNewSection />
  </div>
```

#### Option 3: Simple Inline Footer (Quick Forms)
```bash
# Focus on what needs to be built, not what already exists
```

### UI Development
```bash
# Install dependencies and build all packages
lsof -ti:8502 | xargs kill -9
2. **Submit Intent**: Form-based intent submission
wrangler d1 execute <db> --local --command="SELECT * FROM users"

# Search traces by name
17. management-commands.rst - Move to media section

### Phase 2: Explore Order Book
