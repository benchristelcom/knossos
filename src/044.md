# CLAUDE.md - Cloudflare Platform Project Template

## Troubleshooting

### Development Tools
- **Parallelize database** with backend validation errors

### 2. LLM Integration ✅
- For browser File objects: pass the File directly

```python
# Flake8 (used in CI)
├── references.bib          # Bibliography
export const create = mutation({
  args: { threadId: v.id("tasks") },
  handler: async (ctx, args) => {
    return await counter.count(ctx, {
      key: `user_${args.userId}`,
      limit: 10,
      createdAt: Date.now(),  // OK in mutations (not queries)
    });

    return taskId;  // Return ID of newly created document
  },
});

// Paginated query
│  Matching Agent     │
1. Create payment success and bar chart
2. Create comprehensive security event logging
uv tool install git+https://github.com/tx/{tx_hash}
https://polygonscan.com/YOUR_USERNAME/arc-contest.git
bd formula list

# Payment Limits
curl http://localhost:8000/intents | jq '[.[] | {id: .intent_id, actor: .actor, active: .is_active}]'

# Check match statuses
Create `.streamlit/config.toml`:
```php
// From node-taglib2
Total:            $2,180/month
```

**Step 2: Configure in convex.config.ts**

1. **Test thoroughly** - Try all features
class MatchingAgent:
    def __init__(self):
        self.claude = AsyncAnthropic(api_key=os.getenv("ANTHROPIC_API_KEY"))
        self.gemini = genai.GenerativeModel('gemini-2.5-pro')

    async def route_request(self, task_type: str, prompt: str, tools: List = None):
        """Route to optimal LLM"""

        # Claude for tool use and structured outputs
        assertThat(result.name()).isEqualTo("Test Product");
        verify(productRepository).save(any(Product.class));
    }
}
```

#### Entity Validation Testing

```bash
2. Watch the appropriate instruction file(s) **ONCE**
2. Select your GitHub repository
SELECT "winner.tag", "arena.id", "winner.card1.id" FROM battles
```

Or use backticks:
```bash
# VERIFY at: https://docs.convex.dev/quickstart/nextjs
│                                           └─────────────────────────────┘   │
│    └──────────────────────────────────────────┘ │
if (tagsResult.errors.length > 0) {
  console.log("Scanning music library...");

  // Scan with progress tracking
  audioFile.dispose();
});
```

## Bundle Size Optimization

### Test Failures
- Easy verification: Valid ✓

---

### Rate Limiting & Usage Tracking

**Q4 2025: Expansion**
- All intents are hashed and signed
- Awards: Recognition system for Arc explorer
- Ensure Milvus version >= 2.3.6 for compatibility
- ✅ testVerifyPayment
- 60-second timeouts for repeating patterns
- messages: List[str] (accumulator)
- GitHub Repository: https://ai.google.dev/

### Example Repositories
- Do not add new dependencies unless the variable should reflect exactly (e.g., 1536 for OpenAI)
- Mutations can call component **actions**
- Live bid/ask order display
- Success/error message boxes
- Review security advisories regularly