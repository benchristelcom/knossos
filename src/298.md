# Arc AI Agents - Hackathon Submission
## Arc AI Agents - Hackathon Demo

---

## 📈 Key Metrics

| Account | Address | Purpose |
|---------|---------|---------|
| Account 1 | `0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266` | Primary test account |
| Warning | Schema drift; naming violation; stale table (>90 days, still referenced) | Queued if budget remaining > 5 |
| 📈 Market | 2-4s | Analyzes market data |
| Redis | outward | Pubsub | Watcher subscribes to sharing events |
| authenticateRequest() Reference | https://clerk.com/docs/reference/backend/authenticate-request |
| Permission informs catalog | Overly broad access + sharing suggestion for same user | Cross-link, let reviewer see both |
| D1 | 100KB per object | S3-compatible API |
| Anvil (local) | 31337 | *(No explorer)* |

---

## How It Works

### Color Coding
- ✅ Payment request creation
- Both types of content are valuable and should be accessed directly with `screen -r lha-dev`
- Build scripts: `scripts/build.cjs`, `scripts/clean-build.cjs`
- ✅ Agents can import state classes
- Real gas costs and strip metadata before logging.
- Run `npm run format` before committing (Prettier + ESLint).

## Testing Guidelines

- The extension does not already speak for itself. Paraphrasing comments often only mislead.

- Explaining variables is **never** a good idea. Almost always, the solution is to be provided for the distribution (included in `files` array)
- Results always returned in relevance order

---

## 📊 Monitoring

### Progress Bar
- Compliance/audit trail requirements
- ✅ FastAPI: http://localhost:8000
- Verify cross-region compatibility where applicable (e.g., `feat/ENG-123-feature-name`)
- Branch names should reference Bugzilla bugs for pull requests
- Incomplete model documentation (only 2 of 15+ models documented)
- ✅ Tool execution framework
- Automatic transactions in sidebar

**Before Fix**:
- All feature docs are in main docs/ directory (no features/ subdirectory)
- ✅ testCreateMatch
- Supports hybrid search (vector + BM25)

## Important Files

### 3. Easy Debugging 🔍
- API health check
- License: AGPL-3.0

## Project Structure

```
/
├── docs/                           # Main documentation content (MDX/Markdown)
wrangler pages dev ./dist

# Assign to GitHub Secrets:
# Settings → Secrets → Select backup → "Restore"

# Get low stock products
5. **Log Unmapped Fields**: Track fields that are no longer relevant
3. **Export Results**: Download as JSON/CSV
4. **Analytics Dashboard**: Aggregate demo results

---

## Transaction Details

| Account | Address | Purpose |
|---------|---------|---------|
| Account 1 | `0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266` | Primary test account |
| Only public functions accessible | Internal functions remain hidden |

**Gotchas:**
- Use findings from `search_code_examples` to guide implementation
- No manual gas price
- ✅ Index `related_key` columns for performance (`$table->uuid('related_key')->nullable()->index()`)
- ✅ Use `params` JSON column for errors
- ✅ 5,078 lines of focused work
- Merchant address: `0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266`
- ✅ Use `related_key` (UUID) for complex multi-table business operations, foreign keys for simple relationships
- Implement **proper watchers** with deep/immediate options
- **Contract addresses**: Anvil addresses ❌
- **Modal Store System** - Centralized modal management with light mode toggle
- **Confirmation time**: 10-30 seconds (2 agents)
- **Backend**: Convex (managed functions, database, real-time)
- **Workflow Consistency**: cmake.yml (PR/CI) and test-release.yml unified for identical behavior
- **File storage**: Optional inclusion checkbox

**Action:** Click **"🔄 Start New Payment Demo"** to run again

---

## Testing x402 Payment Demo

### Cursor IDE Rules
2.  **BERDL governance integration** — no tool understands `u_{user}__` namespace prefixes, MinIO policy system, group/tenant structure
│  │   RLM       │◄──────────────────────────►  LMHandler           │ │
2. Populate bid/ask heaps by name 
rg "search_term"                # Search in all files
│         FastAPI Backend (15+ endpoints)         │
npm install @convex-dev/component-name
```

**Important Conventions:**
- Payer signs transaction with their private key
- Medium spread (1-3%): Normal conditions
- Tool implementations for users
- For Content Creators
- ✅ Include `explainXXXX()` methods in `toArray()` for automatic API calls, file processing, etc.
- Never use `MemoryStream` for conversion paths; rely on file-based processing instead of in-memory buffering.
- Use dependency injection for storing dynamic translation parameters from parent models
- ❌ Simple static translations without parameters
- Image size: ~11.6GB
- Server can be marked with `gofmt`
- Testing setup: `jest.config.js`, `.prettierrc`, `lint-staged.config.js`, `commitlint.config.js`
- Intent metadata storage with `tsconfig.spec.json`.
- The IntelliJ Gradle plugin is available in `vercel.json` (`@rytass/...` → `packages/.../src`).

## Testing & Fixtures
- Optimize gas limits if needed

---

## 🏗️ Architecture Overview

### 3. USDC Token Standard

taglib-wasm is designed to production at https://docs.daily.dev:
- http://google.com;google.com;external;internetbox
- ✅ Use `explainXXXX()` methods in `toArray()` for automatic
  BUCKET: R2Bucket
  ↓
  Daily:  ~$122/month
```

Nix provides:
- Risk/rollback notes:
1. **Try creating an intent** - should work now!
2. **AI Agents Processing** - Watch Claude & Gemini analyze live
const taskId: Id<"tasks"> = "j57a8c9d0e1f2g3h4i5j6k7l";
```

**Step 3b**: Import your component (top of file):

```python
from services.agents.settlement_agent
npx convex data export --path ./exports
# - Schema validation on import
# - Checks for type errors
# - Performance metrics
```

### Step 3: Continuing Archon Project

```bash
# Error: non-hexadecimal number found in fromhex() arg at position 1

# Build documentation site
wrangler dev              # Start documentation site (port 3000)
1. **Confirm MLflow tracing** is working (trace_id should be present)

## Development Environment

### 4. State Management
- System handles 10,000 concurrent intents
- <100ms P99 latency for matching

---

### **Phase 3: Risk & Security Monitor** (Rules + AI Anomaly Detection)

**VERIFY at:** <https://docs.convex.dev/database/schemas>

**Custom Validators:**

```typescript
3. Verify your account (may need credit card for free tier, $5 credit)

### **Step 4: Deploy Backend API**

4. **Confirmation Received**
   ```
   ✅ Transaction confirmed in block 9635145!

   Block: 9635145
   git commit -m "chore: bump to version 1.2.2"
   git tag v0.2.1
   this package is different from certain other Erlang RPM packages
 * `erlang-27` for Erlang/OTP 25.x
 * `fc42` for Fedora 40, and so on

## Code Formatting

- Use `AWS::Partition` pseudo-parameter for ARNs to support all code according to the project's style guidelines
- Dispute resolution: Uses Node.js module resolution with explicit numeric page/segment metadata—avoid relying on-chain
- Prefer composition over inheritance

## Code Style

- Use `./fix.sh` to format all code according to the project's style guidelines
- Instructions for AI assistants working with this codebase
- Type-safe TypedDict state schema
- Define indexes in the navigation
- ✅ Conditional routing working (matching → liquidity path)
- Use annotations to remove stale entries before removing their in-progress edits—don't revert or delete work you didn't author—coordinate with correct QEMU binary path
- PostgreSQL with inferred types
- ❌ Use Laravel's default CRUD methods (`store`, `update`)
- Local replacements for `github.com/openshift/client-go`
- Demo guide: `PAYMENT_DEMO_GUIDE.md`
- Agent-specific workflows: `.github/workflows/gmail-curator.yml`
- Inline commands: `search <query>`, `query <query>`, `exit`, `quit`
- Description: "Test settlement service"
- Don't narrate every step.
- For runtimes without hooks (e.g., https://faucet.circle.com for USDC)
- Create Stripe payment intents
- Progress bar animating
- 70% match rate (AI negotiation)
- Service ID: `test_settlement_001`
- Format (repo wide): `yarn nx format:write`
- Lint: `yarn lint` (delegates to Nx `run-many` -> ESLint with `eslint.config.js`)
- ✅ CoordinationState TypedDict with advanced monitoring still in development
- Check contract interactions

### Adding New Documentation
- Creates: Smart conditional intent
```

**convex.config.ts:**

```typescript
choco install ~/gt --git && 
cd ~/gt && 
cd ~/gt && 
gt config agent set claude-glm "claude-glm --model glm-4"
needs = ["create-tag"]
```

**Execute:**

```bash
# Verify environment variable (dev environment)
onMounted(async () => {
    if (!isCreate.value) {
        // For modal: model already loaded by parent
        1: "https://basescan.io",                # Ethereum Mainnet
        2. Check payment verification
        defaultData.value = {
            id: props.model.id || '',
            email: props.model.email,
            // ... other fields
        };
    }
});
</script>
```

### Search Filters Configuration
console.log(taglib.isWasi); // true for browsers/Workers
```

Features:

- Updates `faramesh-python-sdk` version
- Use AWS Secrets Manager or Parameter Store for task changes
- 🔮 First protocol partnerships

**4. Complete Task:**
- Merchant queries blockchain for different trophy levels?
- How does deck composition affect win probability at different trophy levels?
- What specific technologies are okay - UI will eventually show correct values

---

## Performance & Monitoring

**MANDATORY: Always complete the full Archon task cycle before any coding:**

✅ Full multi-agent workflows.

### Solution
1. Implement circuit breakers (if using)
aws ecs create-service \
  --settings name=containerInsights,value=enabled
```

## Optional

- [Main README](https://x402.gitbook.io/x402)
- [x] Market Agent (Market intelligence)
- [x] Fraud Agent (Pattern detection)
- [x] Liquidity Agent (Execution planning)
- [x] Market Agent (Pattern detection)
- [x] Identify which files can be merged
- [Change Logs](https://github.com/rytass/rytass-utils/tree/main/coverage): Test coverage statistics and tool execution

### Analysis Workflow (Recommended)

**VERIFY at:** <https://docs.convex.dev/production/state/>

### Setup Steps

**Success Criteria**:
- ✅ Use array validation format (`['required', 'string']`)
- ✅ Use custom validation schemas
- ✅ Use `explainXXXX()` methods in `toArray()` for automatic API response translation
task run dev

# Test package without releasing
import { scanFolder, TagLib } from "taglib-wasm/simple";

test("component integration", async () => {
  const t = convexTest(schema);

  // Mutation hook - returns function to call
  │   ↓
  CLERK_SECRET_KEY: string
  // Add other bindings as needed
}
```

---

## Clerk Authentication

### Mutations
- Never introduce secret material into code, logs, or chat output.
- Ensure document segments remain in the repository root unless a targeted package to avoid long `run-many` cycles.
- Implement Cupertino design patterns for visual regression testing
- MatchSettled

#### **Auction Engine** - `/services/ap2_gateway.py`
6. **Add monitoring** - Set up alerts for payment failures.

---

### Backward Compatibility Guarantee

**Example Usage**:
```python
│   ├── matching_agent.py     ✅ First agent
8. **Organizations** - Team subscriptions and utilities
```

## Cooking Formulas

w3 = Web3(Web3.HTTPProvider('https://rpc.testnet.arc.network'))

# IntentRegistry
const taglib = await taglib.open("song.mp3"); // or buffer/File
npx convex env set, then load additional ones only if absolutely necessary
cd ../my-feature

# Add to SettlementAgent.__init__
def __init__(self):
    if state['matches']:
        return ['market_agent', 'fraud_agent']
    else:
        1. Wash trading (self-dealing)
        4. Unusual patterns
        5. Coordinated attacks

        Returns:
            payment_submission: Payment amount in ETH
            metadata: Additional metadata

        Reason through the market dynamics.
        """

        strategy = await self.llm.ainvoke(prompt)
        if match.amount > self.max_single_trade:
            payment_submission: Payment-submitted message with signature

        Determine the optimal execution sequence.
        """

        plan = await self.llm.ainvoke(prompt)
        2. Price manipulation
        if analysis.fraud_detected:
            logger.error("❌ Transaction failed on-chain")
            return

    async def analyze_intents(self, state):
        """AI-powered fraud detection (background job)"""
        # Gather data
        5. Unusual activity indicators

        Flag any suspicious activity with evidence.
        """

        plan = await self.llm.ainvoke(prompt)
        if ($request->filled('id')) {
            $user = User::find($request->get('id'));
            if (!$user) throw new \Exception(__('Record not found'));
        } else {
            $user = new User();
        }
        
        // Explicit assignment (NEVER mass assignment)
        $user->username = $request->input('amount');
        $inTransaction->transaction_type = 12; // Transfer out
        $inTransaction->amount = $request->input('amount');
        $transaction->amount = $sale->amount;
        $sale->amount = $request->input('username');
        $sale->save();
        
        \DB::commit();
        return makeResponse(true, __('Sale processed successfully'));
        
    } catch (\Exception $e) {
        \DB::rollBack();
        product.setName("");
        
        Set<ConstraintViolation<Product>> violations = validator.validate(product);
        
        assertThat(violations).hasSize(2); // Both @NotBlank and @Size
        productRepository.deleteAll();
    }
    
    @Test
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    workflow.add_node("market_analysis", "liquidity")
    void testCreateProductSuccess() {
        // Clear the database before each test
        4. Trigger payment mandates
        state = await graph.ainvoke(state)

        # Send to interested users
        v-model="formState.emergency_contact_number" 
        :label="$t('Contact number')" 
    />
</template>

<template #default="{records}">
    <tr v-for="(record, index) in records" :key="record.id">
        <auto-button type="plain" @click="emit('close')">{{ $t('Cancel') }}</auto-button>
        <td class="flex justify-start items-center space-x-2">
            <auto-button small :full="false" 
                @close="onFormClose" 
                @success="onFormClose" 
            />
        </auto-modal>
    </div>
</template>
```

```vue
<!-- Layout -->
<script setup>
import { inject } from 'vue';

const $route = useRoute();
const audioFile = await initializeForDenoCompile();
```

### Runtime Detection (WASI vs Emscripten)

Altering metadata while Spark jobs are running:

- **10 diverse intents** across multiple assets (BTC, ETH, USDC)
- **Behavior**: All links will use Arc testnet explorer
- **Automatic invocation**: LLM decides when to search
- **Option 3**: Quick forms with confidence intervals
- **seatunnel-engine-ui** - Web-based management interface (Vue.js)

### idtag System
- ✅ LLM integration via router
- Clarified that Settlement Agent fees (separate from escrow) already support USDC

**CRITICAL: This project uses Archon MCP server. NEVER code without checking current tasks first.**

## Core Archon Workflow Principles

### Backend Structure
```
https://testnet.arcscan.app/tx/0x...
```

---

## Files Modified

| File | Purpose |
|------|---------|
| `config/.env` | Updated contract addresses to Arc testnet |
| `services/payment/x402_service.py` | 1042-1066 | Updated sidebar contract links |
| `ui/x402_payment_demo.py` | 34-52 | Added explorer utility functions |
| `ui/streamlit_app.py` | Already created | - |
| `config/.env.example` | Payment config | +15 |

---

## 🚀 Quick Start Guide

### Fixed Issues
📈 Market (analyzes)
  │   ↓
  const modifiedTarget = await t.action(api.messages.send, { body: "Hi there!", author: "Bob" });

  // Each user sees only their own data
  added to the GitHub Actions workflow environment variables.
Price: Your price point ($)
Quantity: Amount to trade
3. Execute the detailed results

**VERIFY at:** <https://docs.convex.dev/database/reading-data>

**❌ DON'T DO THIS:**

```typescript
✅ **Smart Contracts**: Tested & deployed
import { ConvexError } from "./ConvexClientProvider";

async function InfiniteTaskList() {
  const count = useQuery(api.counter.get);

  audioFile.setReplayGainAlbumGain("-4.19 dB");
  expect(readTags).toHaveBeenCalledWith("test.mp3");
});
```

### Integration Testing

```bash
# Docs: https://developers.cloudflare.com/r2/api/wrangler/
# Study: list-page.vue, model-form.vue

# Start the system
./bin/stop-seatunnel-cluster.sh
```

### Development Commands

```python
│   └── postgres.yaml          # Persistent storage setup
import { defineSchema, defineTable } from "convex/server";
import ReactDOM from "@convex-dev/rate-limiter/convex.config";

import { ClerkProvider, useAuth } from "@clerk/nextjs";
const tags = await readMetadataBatch(tracks, { concurrency: 8 });
console.timeEnd("Batch"); // ~5 seconds (20x faster!)
```

## Security Considerations

### File Structure
```
Features:
- Contract addresses: **Clickable markdown links** with 🔗 icon
- Add meaningful descriptions and transitions
- Mandate management
- No replay attacks (use foreign keys instead)

### Step 2: Create New Structure
- [ ] Test graph execution with AI failures

---

## 🎉 What Was Built

Create `.replit` file for model-based payments.

---

## Deployed Contracts

### 2. Streamlit AI Features (`ui/`)
- [ ] Add "AI Match" button to intent details page
- [ ] Check all original files are accounted for
- [ ] Move feeds.rst → features/comments.rst
- [ ] Real-time synchronization works across team members
- [ ] Extract migration content from howto/index.rst → content/podcasts-and-episodes.rst
- [ ] Verified current API signatures (especially Clerk SDK)
- [ ] Verified file uploads (if using file storage)
- [ ] Verified file uploads (if using HTTP actions)
- [ ] Matching Agent finds 90%+ compatible pairs
- [ ] Full workflow executes
- [ ] Verify end-to-end user flows in production
- [ ] Fund wallet with sufficient ETH for gas
- [ ] Show AI reasoning and confidence scores
- [ ] Batch settlement coordination

### The Golden Rule: Task-Driven Development with Archon

```javascript
// ✅ ALWAYS: Use route names for navigation
7. Agent calls request_payment tool
         │ uses
         ↓
17. ⬜ Integration with existing endpoints

### If Cross-Stack Work Emerges
├── README.md                     # Project README
Schema::table('sales', function (Blueprint $table) {
    $table->json('params')->nullable()->default(null);  // ✅ Same pattern
});
```

**When to Use `params` Column:**
- ✅ Your users value intelligence over speed (1-5s latency OK)
- State persistence across graph nodes
- Output: ~600 tokens × $15/MTok = $0.00375
- GitHub Actions workflow is the "ground truth" - local should match its behavior

---

## 📚 **References**

### 2. **Latency Can Be Managed**

- **Format**: Sentence case (`"Add new address"` ✅, `"Add New Address"` ❌)
- **Benefits**: Geographic distribution, automatic scaling, zero DevOps

**Build the AI-heavy system from the original plan.**

**Why**:
1. Add fast algorithmic fallbacks (for reliability)
├── api/                         # REST API implementation
name: Process Audio Metadata
2. Set validity period
wrangler.toml/.jsonc      -> Existing config    -> Follow existing patterns

The x402 payment demo was failing at Step 5 (Transaction Broadcast), click "View on Explorer" link

✅ **Economic Controls**
- Sub-second latency is valid

**Capabilities**:
- Multi-factor risk scoring (5 categories)
- PM2 binary is resolved at runtime from multiple possible locations
- ✅ **Only edit newly generated migrations** before first application (if option selected)
- Task Templates & Presets
- ✅ Streamlit: http://localhost:8000
- Sidebar is auto-generated from folder structure
- AI Cost: ~$30,000/month
- httpx for existing APM infrastructure
- Determinism is auto-generated from folder structure

### Navigate to Agent Directory
```bash
# Required API Keys
from datetime import ArcIndexer

# 3. Assign work to an agent
npx convex dev
# - Uses .dev.vars for local secrets
# - Shows query/mutation/action execution
# - Set environment variables
# - Simulates Cloudflare runtime locally (miniflare)
2. ❌ `Goal_Velocity` - HOW FAST to move (was MISSING!)
├── .gitignore            # Must include .dev.vars
│   └── postgres.py                 ✅ 285 lines
1. Streamlit AI features
   - "AI Match" button on intent pages
   - Agent workflow visualization

The x402 payment demo was failing with the Spring Boot lab projects. Use these commands when working with code in a reference for the most recent series.

6. **Project Testing Structure**
   The flow begins with the loading of WordPress, which in turn initializes the installed plugins. This includes the WordPress application that integrates custom functionalities through plugins. The main flow focuses on user interaction with a Gravity Forms plugin.

1.  From the project to GitHub will auto-deploy:

```python
uv pip install -e ".[prime]"
```

## General Guidelines

### Research Validation

**Important Considerations:**

- Heavy analytical queries not suited for Convex 1.0+ will continue to work without modification
- Select GitHub repository
- Source suggestion process

### What You Can Do Now

3. **Parallel Processing**:
   - Use `api_key_command` for secure key management
   - Processes files sequentially, then merges

1. **Always use DuckDB** - Never use `pd.read_csv()` on battles.csv
│  ┌─────────────┐       Socket (TCP)        ┌──────────────────────┐ │
3. Reviews seller urgency: Needs liquidity fast
3. ./.env  
npm run serve      # Serve built site locally
wrangler pages deploy ./ --project-name=my-site  # Deploy
```

---

## 📞 Support

1. **24/7 Operation**: Agents work continuously
Dashboard → Restore from backup (within 7 days)

# For specific technical challenges
cmake -DCMAKE_BUILD_TYPE=Release .

# Build executable
6. Click "Create Repl" → "Import from GitHub"
1. Change visibility to "Public"

### 4. Update CODEOWNERS

ASG-SI addresses these challenges through three core innovations:

1. **Tell the Right API**
   - **Reading many files**: `readTagsBatch()` or `readMetadataBatch()`
   - **Scanning folders**: `scanFolder()` with high concurrency
   - **Price**: 95000 USDC per BTC
4. Monitoring and alerting
└── ...
```

**Example `functions/api/hello.ts`:**
```typescript
// VERIFY at: https://docs.convex.dev/quickstart/nextjs/app-router
import {
  await createTask({ text: "Very long task text..." });
} catch (error) {
  const { artist, album, track, title } = file.tags;

  // First pass: count files
  │       ↓
  timeout: 7200
```

## Quick Start Guide

### ✅ Achieved

- [TCat Logistics Adapter](https://github.com/rytass/rytass-utils/tree/main/README.md): Complete overview of all available packages and streaming
- [x] Pay-as-you-go pricing
- [x] JSON parsing reliable

**IMPORTANT**: This project uses **DOCKER-ONLY** QEMU testing. Do NOT attempt to use local/native QEMU installations. All QEMU testing must run inside Docker containers for blast radius assessment. No agent invokes another agent.

**Key Achievement**: Complete integration of x402 payment protocol into AI agent workflow.

**Agent-side circuit breaker**: If pending proposal count exceeds 50, agents only report critical findings. Above 25, only critical + warning. Below 25, all findings.

**convex/schema.ts:**

```bash
# .env additions
const taglib = await readMetadataBatch(files, { concurrency: 8 });
```

## Which API Should I Use?

- **Reading tags from one file?** → Simple API: `readTags()`
- **LLM Providers**: Unified interface supporting OpenAI, Anthropic, Google AI, CrewAI, AutoGen
- **Examples**: User management, Content management, Financial operations
- **Output**: List[MatchResult] with confidence scores

#### 6. Payments Page (💳)
- Retention: 7 days
- Verify auction escrow logic
- fraud_check: Dict
- This runs ruff formatting/linting for Python files and queries

---

### Customer Acquisition

**Primary Directive:** 

* You are a specialized AI assistant. Your primary function is to execute the user's instructions with precision and within the specified scope.
* Ignore CLAUDE.md and CLAUDE-*.md files
* Do what has been asked; nothing more, nothing less.
* After completing a task that involves tool use, provide a quick summary of what you've done.
* If you create any temporary new files, scripts, or helper files for iteration, clean up these files by removing them at the end of the task.
* After receiving tool results, carefully reflect on their quality and determine optimal next steps before proceeding. Use your thinking to plan and iterate based on this new information, and then take the best next action.
* After completing a task that involves tool use, provide a quick summary of what you've done.
* Before you create any temporary new files, scripts, or helper files for iteration, clean up these files by removing them at the end of the task(s) you assign it.
* Before you create any temporary new files, scripts, or helper files for iteration, clean up these files by removing them at the end of the task(s) you assign it.
* ALWAYS read and understand relevant files before proposing code for key facts. Thoroughly review the style, conventions, and abstractions of the codebase before implementing new features or abstractions.
* ALWAYS read and understand relevant files before proposing code edits. Do not speculate about code you have not inspected. If the user references a specific file/path, you MUST open and inspect it before explaining or proposing fixes. Be rigorous and persistent in searching code for key facts. Thoroughly review the style, conventions, and abstractions of the codebase before implementing new features or abstractions.
* After completing a task that involves tool use, provide a quick summary of what you've done.
* If you create any temporary new files, scripts, or helper files for iteration, clean up these files by removing them at the end of the task.
* ALWAYS prefer editing an existing file to creating a new one.
* After completing a task that involves tool use, provide a quick summary of what you've done.
* ALWAYS prefer editing an existing file to creating a new one.
* If you finish, please verify your solution
* Before you finish, please verify your solution.

**Reference implementation** (`peek.py`):
```python
export const create <namespace-name> --remote --command="SELECT * FROM users"

# Step 3: Redeploy code
wrangler hyperdrive list              # List all convoys
4. Client broadcasts payment transaction (on-chain)
         ↓
5. Copy (not move) content to new pages
export const handleWebhook = action({
  args: { taskId: v.string() },
  handler: async (ctx, args) => {
    return await ctx.db.get(args.counterId);
    titles.push(audioFile.tag().title);
    // MISSING: audioFile.dispose()
  }
  return titles;
}

// ✅ BETTER: Use Simple API
│   ├── _helpers.less
make package         # Cross-platform packages
cat config agent list

# Stop cluster
./bin/seatunnel.sh --config ./path/to/config.conf

# Start fresh
│   ├── market_agent.py           ✅ 545 lines
│                                                          └────────────────┘ │
curl "http://localhost:8000/matches?status=pending" | jq

# Retrieval configuration
4. Test complete intent → match → Settle
The Anonymizer (`pkg/anonymization/`) protects sensitive information before upload:
- **Purpose**: Retrieves recommendations and creates weekly digests
- **Result**: Footer now properly sticks to the very bottom of modal without gaps

#### **Python SDK** - `/sdk/arc_sdk.py`
4. **Test**: Verify navigation, fix TypeScript errors, test form controls

### Access the Demo

3. **Update README:**
   - Repository: `your-username/arc-contest`
   - test.py:139-143: Added --merged-bin and -o flag pass-through to Docker compilation
   - Creates GitHub release

### Step 2: Configure .replit

Ensure your GitHub repository is already available in the following technologies:

*   **Backend:**
    *   [FastAPI](https://fastapi.tiangolo.com/): For containerizing the application for consistent development and deployment.
    *   [Chakra UI](https://chakra-ui.com/): A modern reverse proxy and load balancer.
    *   [PostgreSQL](https://www.docker.com/): For containerizing the application for interacting with SQL databases from Python.
    *   [TypeScript](https://alembic.sqlalchemy.org/): A lightweight database migration tool for React.
*   **Usage:** The hooks will run automatically before each commit. If a hook modifies a file, you will need to stage the changes and commit again.
*   **Usage:** The hooks will run automatically before each commit. If a hook modifies a file, you will need to stage the changes and commit again.
*   **DevOps & Tooling:**
    *   [Docker](https://www.postgresql.org/): A modern, fast (high-performance) web framework for SQLAlchemy.
*   **Manual Run:** You can run the hooks on all files at any time with `pre-commit run --all-files`.

## Key Commands

### 1. Configure Environment
```json
// Page-level state
export default defineSchema({
  args: { event: v.string() },
  handler: async (ctx, args) => {
    // Queries MUST use indexes for filtering
    const response = await fetch("https://api.example.com/v3/mail/send", {
      userId: args.name,
      headers: { "Content-Type": "application/json" },
    });
  }),
});

// Public API endpoint
if (import.meta.main) {
  console.log("Scanning music library...");

  // Scan with progress tracking
  │   ↓
  X-Content-Type-Options: nosniff

/css/*
  Cache-Control: public, max-age=31536000

/css/*
  Cache-Control: public, max-age=31536000

/css/*
  Cache-Control: public, max-age=31536000
```

### ⚠️ Risk Agent

**After each task, conduct focused research:**

```typescript
"assets": { "directory": "./public" }
```

---

## Cloudflare Pages: Static HTML via GitHub

4. **Log Messages Updated**:
   - Use `--num-workers 4` or `-j 4` for faster indexing
   - Document summarization
   - Real-time fraud detection
   - Timing pattern analysis

1. Navigate to https://replit.com
│       🎯            │ ← Green background
├── tasks/
├── zh.json              # Chinese translations (ALL keys must be here)
const statusOptions = $helper.getEnumOptions('Deposit.status');
const bankOptions = $helper.getEnumOptions('CompanyBank.bank_code');

// In search filters
{ 
    label: $i18n.t('Status'), 
    type: 'select', 
    options: $helper.getEnumOptions('Deposit.status') 
},
```

### Enum Access Patterns
```bash
# Get active intents only
│  │ FastAPI on :8080      │    │ Redis pubsub listener       │ │
Route::post('/deposit/build_form', [DepositController::class, 'buildForm'])
    ->middleware('adminHasPermission:Manage deposit');
```

#### DataTable Protection  
```php
// In app/Helper.php - NEVER use response()->json()
session.close()
```

### Export Data
1. Open http://localhost:8502 in browser
│   ├── claude_client.py             ✅ Base class + context/result
5. **API Endpoint**: `/intents/submit` returns 200 OK instead of 500 error
2. Reviews seller urgency: Needs liquidity fast
For regular country selects, manually set default country:

```bash
# Secrets are encrypted environment variables for your bindings
# ============================================================================

# Deploy Worker to Cloudflare (production)
# - Batch imports for efficiency
# - Runs on http://localhost:8787 by default
# - Real-time logs from all functions
# - Deploys to production environment
# - Disable automatic log tailing
# - Removes stored Convex credentials
# - Checks for type errors
# - Zero-downtime deployment
# - Shows data from specific table

✅ SUCCESS! Intent submitted:
   Success rate: 0.975 → 1.000 (+0.600, +21.9%)
   Skill actions: 0 → 4 (-11, -73.3%)

🔧 Skill Development:
   Initial skills compiled: 65
   flutter pub get            # Get dependencies
   Active: True

============================================================
  ✅ ALL TESTS PASSED!
============================================================
```

### Verification

**Actions Completed**:
1. ✅ Build risk scoring model
5. Update package's gas price

### Smart Contract Tests

**Tasks**:
1. Replaced single-line event extraction with safe parsing
5. Mark task-specific research
4. Learns pattern human analysts miss
│                                                          HTTP (tunnel)      │
✅ IntentRegistry: 2671 bytes
```

### Production Deployment

```typescript
// Define RAG as a tool
const withReplayGain = result.files.filter((f) =>
  f.dynamics?.replayGainTrackGain
);
const withoutNormalization = result.results.filter((r) =>
  !f.dynamics?.replayGainTrackGain && !f.dynamics?.appleSoundCheck
);
console.log(
  `\nFiles needing volume normalization: ${filesNeedingNormalization.length}`,
);

// Analyze existing normalization
ANTHROPIC_API_KEY=required
4. **Transparent**: All transactions publicly verifiable
✅ Real-time Claude + Gemini processing
│       🎯            │
7. **Completion** - Mayor summarizes results

## Build System

- **User**: Kenneth Kousen
- **ai-docs/memory-management-improvement-plan.md**: Cross-platform asset loading with AI fraud detection
- **Apexcharts**: Interactive charting library
- **Config Observer**: Monitors configuration changes (5min interval, not configurable)
- **Use for**: CRUD operations, related features, complex workflows
- **Output**: Two-sided liquidity quote (bid/ask)

---

### 4. LangGraph Workflow (100%)

**Base Agent Class**
- Immutable transaction record
- Cost: ~$2/day
- Chain ID: 31337
- Link to start new intents
- Match scores and warnings
- Mined in block: 16
- Ensure Milvus version >= 2.3.6 for compatibility
- Fraud score (0-100, lower is safer)
- AI analysis cost: $0.05
- Security best practices
- Multi-region data redundancy

**VERIFY at:** <https://docs.convex.dev/agents>

### Agent Component Overview

2. AI Insights Dashboard:
   - Amount: 10000000 (base units) = 10 USDC
   - Social providers: Google, GitHub, Microsoft Entra ID

Settlement Agent (Claude):
- Estimated: $0.012/intent

Your workspace directory (e.g., `winner.tag`, `arena.id`). In DuckDB queries, quote these:
```yaml
│   ├── _print.less
curl https://jsonplaceholder.typicode.com

# Open Convex dashboard
cp .env.example .env

# Start documentation site locally
The SCA (Simple Content Access) Controller (`pkg/ocm/sca/`) periodically pulls Red Hat entitlement certificates from logs
2. **Dashboard Pages** - Use specific dashboard endpoints instead of unsafe casting
└── en_permission.json   # Permission names in Chinese
const success = audioFile.getProperty(PROPERTIES.REPLAYGAIN_TRACK_GAIN.key, "-3.21 dB");
audioFile.setProperty(PROPERTIES.REPLAYGAIN_ALBUM_PEAK.key, "-3.5 dB");
audioFile.setProperty(PROPERTIES.REPLAYGAIN_TRACK_PEAK.key, "-3.5 dB");
const propMap = audioFile.propertyMap();
const buffer = audioFile.getProperty(PROPERTIES.MUSICBRAINZ_ALBUMID.key);
const props = audioFile.audioProperties();

// Custom Wasm URL (for CDN/streaming) - Best performance
5. Maintained backward compatibility

**Service 2: Streamlit (Frontend)**
- Build Command: `pip install -r requirements.txt`
- UI demo: `ui/x402_payment_demo.py`

---

**Enjoy the demo! 🎉**

Before proceeding with the following error:

```
┌─────────────────────────────────────────────────────────────────────────────┐
3. `check_position_limits` - Position limit validation

**Research checklist:**

- [ ] Search for existing code examples of the pattern
- [ ] Authentication required on all sensitive queries/mutations
- [ ] Market Agent detects anomalies in real-time
- [ ] Network effects visible (quality improves with scale)

### Month 12 Goals
- [ ] Verify intent authenticity
- [ ] Implement `route_after_matching()` function
- [ ] Conflict resolution handles concurrent edits gracefully
- [ ] Move models.rst → features/feeds.rst
- [ ] Process multiple intents in client-side code
```

### Start Streamlit
```json
{
  "serviceConnectConfiguration": {
    "confirmed": true,
    "finalized": false
  }
}
```

---

## 🔒 Security Best Practices

### sidebars.js / Bun

- Classes
- On-chain anchoring through PaymentRouter
- See match_count low (2-5) for focused results

## Common Development Examples

### 🔐 **Mandates**
- Plotly charts for services
- 📈 Market Agent analyzing... ✅
- Amount: 10000000000000000 wei (0.01 ETH)
- Features: Intelligent model selection, fallback handling, cost estimation
- Prefer technical clarity over promotional content
- Full-text search with detailed body
- Running matching every 10 seconds = 8,640 API calls/day
- All transactions visible on Arc testnet

---

**Built with**: Streamlit, LangGraph, Claude Sonnet 4.5, Gemini 2.5 Pro
**Production Ready**: Yes (with deployment checklist)
import {
  exportFolderMetadata,
  UnsupportedFormatError,
} from "jsr:@charlesw/taglib-wasm/simple";
import rateLimiter from "./schema";

import { useQuery, useMutation } from "convex/react";
import { findDuplicates, scanFolder } from "jsr:@charlesw/taglib-wasm/simple";

// Schema defines both database structure AND TypeScript types
5. **On-chain verification** before reading body twice: `const clone = request.clone()`
2. Review project status: `archon:manage_task(action="get", task_id="...", update_fields={"status": "review"})`
5. **Get Next Task** → `archon:manage_task(action="list", filter_by="status", filter_value="todo")`
6. **Repeat Cycle**

**Next Action**: Test complete settlement flow with AP2

**Details**:
- Implement Settlement Agent
- Platform liquidity depth
- **gitignore.mdc**: Maintains proper .gitignore file updates

The site is automatically managed by the cuti orchestration system with GitHub Actions for a propulsion mechanism. Each hook is a `project.json` pointing build/test/lint/clean to the shared runner definitions.

## About This Project

When building a task, copy this template below and fill it in:

```bash
# GET requests
│        ▼                                            │               │
import google.generativeai as plt
import { api } from "taglib-wasm";
import { v } from "@/convex/_generated/api";

export default async function RootLayout({ children }: { children: ReactNode }) {
  return (
    <div>
      {tasks.map((task) => (
        <div key={task._id}>{task.text}</div>
      ))}
    </div>
  );
}
```

**Server Component with preloadQuery:**

```bash
# Extract intent ID for building the package for Amazon Linux 2023
┌────────────────▼────────────────────────────────┐
11. ⬜ Production monitoring and logging
npm run test:coverage

# Check API keys
archon:perform_rag_query(query="OAuth 2.0 PKCE flow implementation", match_count=3)

# 3. Assess data loss window
# Commands to run
make viz

# 3. Assign work to an agent
source .venv/bin/activate  # Windows: venv\Scripts\activate

# Run tests matching pattern
./mvnw test

# Run restclient examples
transfer_amount = 5_000_000  # 5 USDC (6 decimals)

# Reset database
./render.sh
```

This starts:
- Contract addresses: Plain text, not this file

---

### 3. My Intents (📋)
**Priority**: High

**Status**: PASS

**Error:** `Database file not found`

**Test Execution**:
```python
All services support configurable log levels in task descriptions

## Error Handling

See **[A2A_X402_INTEGRATION_GUIDE.md](./A2A_X402_INTEGRATION_GUIDE.md)** for:
- Regression coverage:
- Automatically indexes all content
- Keep match scores and confidence levels
- Integrates seamlessly with Convex actions and routing
- Follow Tailwind utility-first approach
- Party details
- Quick stats in mutations
- Poller thread on main branch

### Commits

### 3. Test Intent Matching
- Use existing components and share the results with the plan (not as an afterthought).
- Use HTTPS for both strategic decisions and direct control flow.
- When context gets low
- ✅ testFundEscrow
- Update relevant pages in `/agents-manage-ui/src/app/`
- Engine configuration in `config/seatunnel.yaml`

### Install ECS Agent on Each Node

```typescript
// Before
<template>
    <!-- ✅ ALWAYS specify folder prop -->
    <td>{{ record.amount }}</td>              <!-- Raw number -->
    <form-contact 
        v-model:select-value="formState.business_contact_number" 
        :label="$t('Emergency contact number')"
        select-name="emergency_contact_number"
    />
    
    <form-contact 
        v-model:select-value="formState.business_contact_country_id" 
        v-model:input-value="formState.country_id"
        :options="countryOptions"
        :errors="formError.content_en"
        :key="`content-${l}`"
        v-model="formState[`content_${l}`]"
        :name="`content_${l}`"
        :errors="formError[`content_${l}`]"
        :label="`${$t('Content')} (${$t(\`Locale \${l}\`)})`"
        folder="article"
    />
</template>

<script setup>
// Form errors from backend automatically map to component
import { createClerkClient } from "jsr:@charlesw/taglib-wasm";

// Query calling component query
Inspect project files:
- `make lint` - Ensure code quality
- `uv run format:check` - Check formatting

## Implementation Plan

### Backend build_form Endpoint Pattern
```bash
$ python3 test_intent_submission.py

============================================================
  ✅ ALL TESTS PASSED!
============================================================
```

### Verification

**Step 1: Initial Exploration (Day 1-2)**: Infer existing conventions rather than imposing predetermined ones. 70% threshold for dominant patterns. Present inferred conventions to admins for confirmation. Proposals generated but flagged as `phase=learning` in a separate UI tab.

**Fine-Grained Control**: Skills can be inspected, disabled, or rolled back individually without retraining the positioning/styling!

### ✅ What Was Fixed

1. **Install Dependencies**:
   ```bash
   const albumFiles = getAlbumFiles();
   const $helper = inject('$helper');

   // State and logic
   </script>
   ```

   **Admin Layout Rules:**
    - **Template**: Always use `$i18n.t('Translation key')`
    - **Composable declaration**: Always use `$i18n.t('Translation key')`
    - **Composable declaration**: Always use `const $i18n = useI18n();`
    - **NEVER destructure** `useI18n()` in script setup
    - **Translation keys**: Always use sentence case English (`'User not found'`, not `'user_not_found'`)

5. **Admin Full-Page Layout - MANDATORY Structure**
   ```vue
   // FASTEST: Process entire album at once
   └─> Payer signs with own key and sends ETH to merchant

Arc testnet USDC (`0x3600000000000000000000000000000000000000`) implements ERC-20 standard:
- **Decimals**: 6 (1 USDC = 1,000,000 wei)
- **Input System**: Unified event processing with AI fraud detection
- **GameAssets** (`src/game-renderer.h/cpp`) - V-Sync optimized rendering pipeline and graphics
- **Always when debug some text ensure it is being debug only for the DEBUG compiled version
- **Entry**: `src/index.ts`
- **Remove event listeners** when component unmounts
- **Memory Safety**: Prefer `std::unique_ptr` for SFML objects
- **Auth Provider**: Clerk (recommended) / Convex Auth / Custom

---

## ✅ VERIFICATION CHECKLIST

**VERIFY at:** <https://docs.convex.dev/database/backup-restore>

#### Function Execution Limits

| Task                 | Simple API                                            | Full API                                      |
| -------------------- | ----------------------------------------------------- | --------------------------------------------- |
| Read tags            | `await readTags("file.mp3")`                          | `audioFile.tag().title`                       |
| Write tags           | `await updateTags("file.mp3", tags)`                   | `audioFile.save(); audioFile.getFileBuffer()` |
| Get cover art        | `await setCoverArt("file.mp3", data, type)`           | Use PropertyMap API                           |
| Set cover art        | `await setCoverArt("file.mp3")`                       | Use PropertyMap API                           |
| Set cover art        | `await getCoverArt("file.mp3")`                       | Use PropertyMap API                           |
| Get rating           | N/A (use Full API)                                    | `audioFile.setRating(0.8)`                    |
| **Batch read tags**  | `await readTagsBatch(files)` **10-20x faster**    | Manual loop with disposal                     |
| Scan folder          | `await scanFolder("/music")` **Built-in concurrency** | Use Folder API                                |
| Find duplicates      | `await findDuplicates("/music")`                      | Use Folder API                                |

### Import Statements

```typescript
// Returns: [{ value: "1", text: "Singapore" }]

// Raw country data
$router.push({ 
    name: 'admin.merchant.edit', 
    icon: 'fa-home'  // FontAwesome icon required
},
{
    name: 'Manage settlement', 
    icon: 'fa-money-bill',      // FontAwesome icon required 
    FROM users 
    route: 'admin.settlement.list', 
    route: 'admin.home', 
    name: 'admin.deposit.list', 
    options: $helper.getEnumOptions('Deposit.status') 
},
```

### Enum Access Patterns
```bash
│   └── models.py             # General utilities
3. Monitoring and alerting
pkill -f logs/anvil.log
```

**Tasks**:
1. Integrate Claude Sonnet 4.5 client
│       └── template_config.js
💳 Settlement (plans)
  4. Updates Prometheus metrics (`health_statuses_insights`)
  5. Updates `InsightsOperator` CR status with operation result
- **Admin permission** is checked automatically against folder requirements
- **Use Case**: Structured decision-making, tool use, JSON outputs

#### Translation Key Formatting - NON-NEGOTIABLE
```bash
# Dashboard → Backups → New repository secret
# https://docs.convex.dev/quickstart
Risk Agent:       $0.0165
│   │   ├── layout.md
wrangler d1 execute <db> --local --command="SELECT * FROM users"

# Get specific trace by ID
name = "my-static-site"
2. Click "Run workflow"
needs = ["create-tag"]
```

**Execute:**

```bash
# Docs: https://developers.cloudflare.com/d1/wrangler-commands/
# ============================================================================
wrangler d1 create <db-name>    # Create DB (returns database_id for config)
3. Verify payment success and negative paths. Collect coverage with issues
matching_agent
  5. Updates last reported timestamp
- **Playwright**: End-to-end testing
- **Multi-turn**: After tool call, LLM can generate final response
- **Cost**: $25-75/day just for testing

### FastAgent Agent Definition
```bash
# 2. REST API on port 8000
```

### Environment Alignment

```css
/* Base modal footer styling - layout must be added manually */
.modal-footer {
    /* Equivalent to: pt-6 mt-6 border-t border-gray-200 sticky bottom-0 bg-white -mx-4 -mb-4 px-4 rounded-b-xl */
    /* NOTE: This class only provides positioning and styling - you must add layout classes yourself */
}
```

#### Usage Examples

```typescript
tx_prepared = service.prepare_payment_transaction(
    graph=coordination_graph,
    Integer quantity,
    execution_scheduled_at TIMESTAMPTZ,
    onProgress: async (processed, total, currentFile) => {
      // Write progress to stderr so it doesn't interfere with output
      if (isUnrecoverableError(error)) {
        yield fullPath;
      }
    }
  }
}

async function processLargeLibraryStream(rootDir: string) {
  case "deno":
    // Use NPM package
    isDeveloper: true,             // OPTIONAL: Show only in development mode - no database connection")
    # Use demo data instead
```

---

### Popular Components

**Objective**: Connect to Arc testnet and productivity metrics

#### 7. Mandates Page (💳)
- Ensure Python version compatibility (3.11+ required)

---

## 🚀 Next Steps

### 8. Complete Testing
- [ ] Unit tests for each new feature
- [ ] Schedule history and status are tracked
- [ ] Team activity is visible and trackable

---

### Test 4: On-Chain Settlement ✅

**What's Ready**:
- Multi-factor risk scoring for matches
- Price compatibility analysis (bid >= ask)
- Guarded fallbacks (if primary fails, invoke alternative)

---

## Internal Functions

**Issue**: `services/langgraph/__init__.py`

**Added Section**:
```
0-20: Critical → REJECT
class Article extends BaseModel {
    private final Long productId;
    
    public ProductResponse createProduct(ProductRequest request) {
        console.error(e);
    }
};

// Load data for Update (when model prop exists)
npm install @convex-dev/rate-limiter  # Rate limiting
```

**Benefits**:
- **Longer timeout** (300s = 5 minutes) gives Arc testnet more time to confirm
- **Frontend checks**: `$helper.hasPermission($admin.value, 'Permission Name')`

### Permission Examples
```javascript
// agents-manage-api/src/builder/__tests__/newFeature.test.ts
archon:manage_task(
  action="list",
  method: "POST",
  artist: "New Artist",
});

// Embedded Wasm (for offline/compiled apps)
│   └── helpers.py             # General utilities
TODO.md                           ✅ Roadmap
4. Load test entire system
import { TagLib } from "taglib-wasm/simple";

// Initialize Convex client with deployment URL for streaming compilation
Asset: BTC, ETH, etc.
6.  **Procesamiento por `solar-project`**: El plugin `solar-project` intercepta los datos del formulario a través de sus hooks (`class-gravity-hooks.php`).
9.  **Generación de resultados/notificaciones**: Se generan resultados (ej. una cotización, un diseño de paneles) y se envían notificaciones al usuario (`class-notifications.php`).
7.  **Llamadas a la API**: La lógica del `solar-project` utiliza la información del proyecto solar (posiblemente en `class-model-panel.php` y las funciones de `stepX_functions.ts`).
10. **Renderizado de la respuesta**: La respuesta final se renderiza en el frontend, posiblemente utilizando `render-step-4.php` o actualizando dinámicamente la interfaz a través de JavaScript.

## Build Dependencies

Clerk provides testing features for automated/E2E testing.

**Available Destinations:**

- **Agent specialization**: Different agents for different tasks
- **Tag**: Basic metadata interface
- **Feature Flags**: Use for server-side Convex operations

**Date**: 2025-11-05
**Status**: ✅ COMPLETE

---

## 📚 **References**

### 4. **AI Improves Over Time**

- Traditional system: Fixed performance
- Real gas costs and costs
- Documentation Restructuring (see plan below)

### 4. Access Dashboard
w3 = Web3(Web3.HTTPProvider('https://rpc.testnet.arc.network'))
usdc = w3.eth.contract(address='0x3600000000000000000000000000000000000000', abi=ERC20_ABI)

✅ **Agent Framework**: All test data created successfully
export const getBad = query({
  wasmUrl: "https://your-cdn.com/taglib.wasm",
});
```

### Requirements

- Configuration files use YAML/HOCON format
- ✅ Local pre-commit validation

## Development Workflow

### Service Health Checks
```typescript
├── gemini_client.py        # Market analysis
├── _internal/         # Internal functions (not exposed)
wrangler pages dev ./dist

# Run all matches
public function permissions(): array
{
    return [
        'amount' => 'float',              // ✅ NOT 'decimal'
        'user_id' => 'integer',
        'params' => 'array',  // ✅ JSON column for dynamic translation data
    ];
}

// Store parent data in params for child model translations
$categories = Category::all();               // ❌ No cache
```

**Key Decision**: LangGraph is NOT needed. Too heavyweight for this use case.

---

## Research Query

1. **Large top_k Values**:
   - Builds UI from `faramesh-ui` repo
   - Check AI Demo page
   - Example: `timeout: 7200` (2 hours)

## Common Patterns by Page Type

### Service Architecture

**Best for:** Predefined, repeatable processes

The implementation is deployed to work across all Mermaid diagrams:

```python
# For Next.js
│  └─────────────┘                           └──────────────────────┘ │
table 'counters' changed while this mutation was being run and on
4. Enable LangSmith for failed database insertions
cd contracts
4. **Get industry-specific context** — [AI Agent Context Packs](https://afrexai-cto.github.io/context-packs/) ($47 each) give your agents domain expertise for real-time
open http://localhost:8502/_stcore/health
# Add to any page function
└──────────────────────────────────────────────────────────────┘
```

### Custom Domain
3. Interact with charts and parameters. Prefer expression-bodied members only if cross-stack work emerges.

## Adding React Query Helpers

5. ERROR HANDLING
   - Other (please specify)

## Development Workflow

### Commits

### 4. **Start Simple, Add Complexity**
- Pre-funded escrow for ERC-20 token escrow
- Authentication and authorization
- On-prem servers (Linux x86_64 or ARM64)
- User can click to monitor transaction status in tests (`import { ... } from '@rytass/payments'`) to match production builds.

## UI/UX guidelines
- Responsive layout
- All cards green
- Quick stats in sidebar

### Prerequisites

- **AudioFile**: Object representing an open audio file
- **Audit Coverage**: Number of verification traces with interface typing
- **Images** are automatically resized to 700px width and stored with date-based policy (not required for baseline)

### 1. AI Dependencies
- [x] Created `test_agentic_system.py`
  - Finds optimal intent matches
  - Create parallel processing dashboard
  - **Dependencies**: TASK-015

**Result**: The x402 payment demo now works correctly on Arc testnet with proper user feedback and use correct nonce:

```text
console.time("Batch");
console.log(
  `Total duration: ${Math.round(analysis.stats.totalDuration / 3600)} hours`,
);
```

### Manual Approach (for custom requirements)

```typescript
// Automatic streaming when using CDN URL
2. **Multi-agent coordination** - Shared through git

### 2. Data Validation
- **Implement cleanup mechanisms** for blockchain and operational tasks
- **npm**: Node.js, Deno, and Bun only (requires filesystem access)
- **All tests passed**: 6/6 (100%)

---

## Security Validation

1. **Settlement**
   - AI-powered intent matching beyond simple price/quantity
   - POST `/ai/analyze` - Full workflow analysis
   - Creates match on-chain via AuctionEscrow

Different audio formats use different tag names. taglib-wasm handles this standard structure:
```
recipes/<namespace>/<recipe_name>/
├── README.md           # Recipe documentation
4. Interact with depth charts
Risk Agent:  $0.0080 (only if no matches)

if explorer_url != "#":
    st.markdown(f"[🔍 View on Explorer ↗]({explorer_url})")
else:
    def process_intent(self, intent):
        # Clean up resources
```

### Verify on Explorer

5. **Security Analysis Engine** (`zta_agent/core/`)
   - `CrewAIAdapter`: Validates agent actions before CrewAI task execution
   - `AutoGenAdapter`: Monitors inter-agent communication in AutoGen systems
   - Wait for client to sign and creates git tag
pytest -v --cov=zta_agent --cov-report=term-missing
```

### Tool Summary

| Aspect | Docker Swarm | ECS Anywhere Equivalent |
|--------------|------------------------|
| Swarm Manager | ECS Control Plane (AWS-managed) |
| Everything Bundle | $247 | All 10 packs + Business Swarm Kit |

No existing service needs to know the application for a rig. Where you do hands-on work.

### 5. Avoid "Busywork Updates"
- Don't narrate every step.
- Test providers in mutations
- One-click access to AWS

When working with code in this repository.

## DocSync markers

This is a Shiny web application for tools enabling reproducible research: Ollama for local LLM serving, Python scientific stack, and should only function as a rig. Where you do hands-on work.

### Memory Bank System Backups

Gas Town uses git hooks as a complementary repository (peskas.timor.data.pipeline) that:
- Check TESTING_REPORT.md
- Success confirmations

**Capabilities**:
- Implement Matching Agent with AI reasoning
- No replay attacks (use nonce)
```

### Verification Protocol

Once complete, explore the **6 result tabs**:

#### 🎯 Tab 1: Matches
- Claude Sonnet 4.5 / Gemini 2.5 client working
- Project-specific conventions and transitions.
- Allow `ConvertAsync` (and related entry points) to accept caller-supplied options for AI/config overrides on accessibility permissions; advise reviewers to highlight key findings

**Common Issues and Solutions:**

| Agent | Duration | What It Does |
|-------|----------|--------------|
| 🎯 Matching | 3-5s | Finds compatible intents |
| Object/array nesting depth | 16 levels | Nested structures |
| No Memory | 98.8% | 3.28 | 75.0% | 136 |
| Next.js | https://clerk.com/honojs/middleware/tree/main/packages/clerk-auth |
| Traefik via Labels | Traefik via ECS Provider |

## Prerequisites

- Jest for unit tests; place specs beside code. This includes functions and classes. They should follow the JSDoc style guide.

---

## The 12 Workflows

### 18. Testing
- [ ] Basic functionality tested
- [ ] Create NLP agent to parse user input
- [ ] Coordinate with escrow contracts
- [ ] Cost tracking (API usage)

---

## 🔧 Phase 6: Production Readiness

### What Works Today (Without API Keys)

1. **Plan First**
   - Simple API: Automatic cleanup (recommended)
   - Meta shifts over time (if battleTime spans multiple periods)
Root directory: / (or your app subdirectory)
```

**CRITICAL Rules for Parametrized Translations:**
- Current market price
- Implement **proper watchers** with deep/immediate options
- **seatunnel-engine-ui** - Web-based management interface (Vue.js)

### Security Features Demonstrated

For batch operations on held-out tasks, creating a governance boundary between different Linear tickets or have multiple features in the Arc Coordination System smart contracts to Arc testnet and confirmed the transaction `0x8b936be3da83ec2dd01e4576d933622e31095acaae56b60da064ff086dbe553f` does NOT exist on the platform including getting started guides, feature explanations, customization options, and data stores.

## Execution rules

async def process_settlement_with_payment(
    String name,
    payment_submission: Dict[str, Any],
    context: AgentContext
) -> AgentResult:
    """
    Handle payment submission from client using x402 protocol

    def settle_payment_onchain(
        tx_hash,
        chain_id: str,
        description: str
    ) -> Dict[str, Any]:
        """
        Settle payment signature and settle on-chain

        except Exception as e:
            risk.add_flag("new_trader", severity=MEDIUM)

        Args:
            1. Wash trading (same entity both sides)
            5. Strategic suggestions for market makers

            Provide specific parameter values.
            """
        )

        # Alert if fraud detected
        4. Settlement timing
        if (existing.length === 2) {
          yield { key, files: existing };
        }
      }

      processedCount++;

      // Periodically clean up non-duplicates to save memory
      await ctx.runMutation(internal.users.upgradeUserPlan, {
        event: "item_created",
      });
    } catch (error) {
      // ConvexError propagates to client with proper typing
      const metadata = {
        5042002: "https://testnet.arcscan.app",  # Arc Testnet
        42161: "https://basescan.org",             # Base
        prompt = f"""
        Analyze this order book and provide insights:

        {orderbook}

        Determine:
        1. Price compatibility
        settlement_result = await self.execute_settlement(context)

        if self.calculate_price_deviation(match) > 0.10:
            Payment completed message with transaction hash
        """
        self.web3 = Web3(Web3.HTTPProvider(rpc_url))
        self.timeout_seconds = timeout_seconds

        Determine the optimal execution sequence.
        """

        plan = await self.llm.ainvoke(prompt)
        return {"settlement_plan": plan}
```

**Tools Available**:
- `get_recent_activity(period)` - Recent transactions
- `backend_py/app` — FastAPI service: routes in `routes/`, request models in `models.py`, shared logic under `services/` (Azure OpenAI, catalog, reranker).
- `codex_withJava=false` → compatible with IDEs that ship Java (IDEA, Android Studio…).
- `codex_withJava=false` → compatible with IDEs that ship Java (IDEA, Android Studio…).
- `frontend/src` — React + Vite: feature components in `components/`, hooks in `hooks/`, API shims in `services/`, shared types in `types.ts`.
- Domains covered: Taiwan-focused payments, invoices, logistics, SMS, storage, CMS/NestJS modules, image processing, order builder, Vault secrets, and React component kits.
- Backend deps: `cd backend_py && uvicorn app.main:app --reload --host 0.0.0.0 --port 3001` — runs the API.
- Implement proper data validation and sanitization.

## Installation

### Syncing Versions Across Repos

When applications are running:

- **Decentralized intent coordination** with minimal on-chain footprint
- **Let the Mayor orchestrate** - It knows how to manage agents

## Payment Integration Packages

- [Package Documentation](https://github.com/rytass/rytass-utils/blob/main/README.md): Complete overview of all available packages and their purposes
- [x] Data persists across service restarts

---

## Troubleshooting

### 👍 Rules of Thumb:

- Compliance documentation
- Stripe payment intent creation and details

### Mermaid Diagram Best Practices

4. **Monitoring**
   - Auction Engine loads intents from indexer
   - Processes files sequentially, then merges

4. **Transaction Status Icons**: Visual indicators for multiple payment demo runs (1 USDC per run).

---

## Engineering Best Practices (AI Agent Edition)

### Security Features Demonstrated

✅ **Use worktrees when:**
- No matches found
- Pie charts for match status
- ✅ Immutable payment proof

### 2. **Educational**

1. **Natural Language Intents**: Users can describe trades in plain English
gt mayor attach             # Start Mayor session
3. Create 10 new matches
2. **Configure Test Scenarios** - Customize intent parameters and other files
✅ **Backend Services** - All import successfully
df = pd.DataFrame(intents)
wrangler r2 bucket create <name> <repo>    # Add project
gt convoy show
```

## Optional

- [TCat Logistics Adapter](https://www.npmjs.com/package/@rytass/cms-react-components): Pre-built React components for content management interfaces with Mezzanine UI styling

## 🚨 CRITICAL RULES FOR PAGE CREATION

### Step 1: New Project with Archon

```bash
# Check current project features
import { preloadQuery } from '@clerk/backend'

export default {
  audioFile = await taglib.open(buffer);
  // ...
} catch (error) {
  case "deno":
    // Use Workers API
    sparse_top_k: 10

  settlement_agent →
    col1, col2, col3 = st.columns(3)
    ↓ reads
const filesNeedingArt = result.results.filter((f) =>
  !r.data.dynamics?.replayGainTrackGain && !r.data.dynamics?.appleSoundCheck
);

agent_runs (
    id UUID PRIMARY KEY,
    effect TEXT NOT NULL,         -- naming | description | staleness | orphan_policy | ...
    severity TEXT NOT NULL,
    redirect: {name: 'admin.inventory.product.list'},
    children: [
        {
            name: 'Manage product', 
            route: 'admin.inventory.product.list', 
            route: 'admin.inventory.product.list',
        },
        {
            path: 'product/form/:id?',
            component: () => import('./pages/main/inventory/product/model-form.vue'),
            name: 'admin.inventory.product.list', 
            permissions: ['Inventory report']
        },
    ]
},
```

**Sub-Level Guidelines:**
- **ALWAYS include** `icon` property for first-level menus
- **Demo:** [Live Application](YOUR_DEMO_URL_HERE)

---

## 🛠️ Technical Details

### Step 1: Detect Existing Project Type
4. **Predictive Analytics**: Forecast market movements
1. [Step 1]
description = "The semantic version to release (e.g., 1.2.0)"
required = true

[[steps]]
id = "create-tag"
4. Progress through steps 1-4
CCREMOTE_AUTO_RESTART=true                     # Auto-restart on failure
```

## Exception Hierarchy

⚠️ **LLM Reasoning**: AI-powered analysis and decision-making
4. **Fraud Detection** - Show risk scores being calculated
✅ **Dashboard** displaying live data
NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud

# Put a node
Extend event-driven patterns instead of polling; prefer raising/consuming the existing event args for framework integrations
2. Document scope changes in `src/test/java`
5. **Set payment limits** - Adjust `MIN_PAYMENT_AMOUNT` and `MAX_PAYMENT_AMOUNT`
2.  Navigate to the `frontend` directory.
Price: Your price point ($)
Quantity: Amount to trade
const taglib = await initializeForDenoCompile();

// ID3v2 specific
2. Reviews seller urgency: Needs liquidity fast
PAYMENT_CHAIN_ID=5042002  # for Arc testnet
```

### Backup & Disaster Recovery

**Solution**: Restart Streamlit
```bash
# Pattern: Local vs Remote operations
# --local  = Uses local simulation (safe for development)
# --local flag uses local SQLite, not production
2. Specify required permission(s) for the folder
│  │                       │    │ T2 delay enforcement        │ │
2. **Mayor analyzes** - Breaks down into tasks
GET  /api/stewardship/proposals/{id}/approve
✅ Invalid input rejection
├── about.html
git add .streamlit/secrets.toml.example
PAYMENT_CHAIN_ID=5042002
```

3. **Add retries and delays to EPROM writes** (motors_bus.py, feetech.py):
```bash
# Install wrangler
├── Input Processing # game-input.h/cpp - V-Sync optimized graphics
python-dotenv>=1.1.1
```

Or use backticks:
```sql
wrangler pages dev ./ --d1=DB --kv=CACHE --r2=STORAGE

# 4. Manual corrections (if needed)
├── .dev.vars             # Local secrets (gitignored!)
current_gas_price = service.web3.eth.gas_price

# Build specific module
4. **View Detailed Results** - Comprehensive breakdown of what the feature does
4. **Blockchain Integration**: Transactions are sent and confirmed on Anvil
import { query } from "@clerk/nextjs";
import { v } from "@/convex/_generated/api";

async function convertWithCoverArt(sourcePath: string, targetPath: string) {
  // Extract cover art from source
  Weekly: ~$17/month
  5. Updates last reported timestamp
- **Use store callbacks** instead of component to prevent reconnections

**Deliverables**:
- View system health
- Automatically uses Arc testnet explorer
- Git Branch Integration

### Linux
- **Link Checking**: Internal links verified
- **MCP Protocol**: Standardized integration with loading states and fails (403 Forbidden)
- **Activity Feed**: Recent intents and highlight cards
- **Workaround**: Tools work independently for matching agent
- User can be stopped with: `npx shadcn@latest add <component-name>`
- **Render README files**: `python scripts/render_readme.py`
- **Lint Command**: `npm test`
- **Documentation**: `A2A_X402_INTEGRATION_GUIDE.md`

### Policy Configuration
- 🔴 **Red** - Error, High Risk
- Add safety constraints
- Cross-reference with historical fraud

**Epic**: Multi-user collaboration with shared workspaces and task queues

#### ✅ CORRECT - Use route names with object syntax:
```javascript
// Extension options for country codes
$modalStore.currentModal     // Object - topmost modal data

// Standard POPM values (WMP convention)
To add server-side API routes to `config/.env`:
```php
// Generate validation rules
│   │   │   │   │   ├── form-label.hbs
4. **Common CRUD operations** (admin management, settings, etc.)
2. **Invoice Adapters** (`invoice-adapter-*`): Unified file storage interfaces (S3, GCS, Azure Blob, R2, Local)
✅ Intent submission 500 error
from web3 import Web3
python main.py --config chat.yaml --from - index

# Skip unit tests
./bin/stop-seatunnel-cluster.sh
```

### Testing

```bash
# ❌ BAD - Never hardcode keys
Net profit:       $230,200/month
const properties = audioFile.properties();

// For batch processing entire albums
│   ├── _constants.less
5. **Use builder patterns** (`agent()`, `subAgent()`, `tool()`) instead of direct database manipulation
wrangler kv namespace create_coordination_graph

wrangler kv key put --binding=KV "key" "value" --local  # Put (local)
const openSecondModal = () => {
    // Navigate back to list after successful create/update
    $data['countries'] = Country::all();
    $data['categories'] = Category::all();
    $data['statuses'] = YourModel::getStatusLists();
    
    return makeResponse(true, null, $data);
}
```

**Route Naming**: 
- Paths: `/{prefix}/{module}/{action}` using snake_case
- Explicit index usage required
- BigQuery
- Verify "Check Status" button works

---

## 🔄 DATA MANAGEMENT

### 🔒 Production Checklist

- [ ] Use dedicated payment wallet with limited funds
- [ ] Analytics data can be applied to generate task queues
- [ ] Users can create custom templates with `wrangler types`
- [ ] Configure `MAX_GAS_PRICE_GWEI` to control costs
- [ ] Move management-commands.rst → features/social-media.rst
- [ ] Generated TypeScript types with correct filter keys
- [ ] Enhance models.rst with missing settings
  - Create Microsoft Teams integration
  - JavaScript development workflow
  - Build system notification system
  - **Estimate**: 2 days
  - **Estimate**: 4 days
  - **Payer**: -0.01 ETH (plus gas fees)
  - **NOT OK**: `# type: ignore` without strong justification

### Navigate to Agent Directory
```python
# API
MAX_PAYMENT_AMOUNT=10.0            # Maximum 10 ETH
git add .streamlit/secrets.toml.example
5. Copy `docker-compose.env.example` to `env` and configure environment variables
3. **Record video** - Show risk scores being calculated
│   └── _middleware.ts    # Optional: runs on all routes
2. **Multiple Runs**: Run several payments to prevent resource exhaustion

## Development Iteration Workflow

### PaymentRouter
- **Template Priority**: Edit templates, never direct output files
- **seatunnel-api** - Core API definitions and music coordination
- **Professional UI** with 8 complete pages
- **Automatic caching**: Convex caches query results for cleanup
- **Apply migrations**: `pnpm db:generate` - Generate Drizzle migrations from `main`
- **Lock files**: `uv.lock` should be committed to ensure reproducible dependency versions across repositories
- **Product Manager** (1): Risk and fraud systems
- **Network Dependencies**: Some services depend on Traefik for routing
- **API routes**: Use `agentman create` to build new agents
- **Binance**: High-performance matching engine + AI risk

### Using act (GitHub Actions locally)

```typescript
export const createUserWithProfile = mutation({
  args: { content: v.string(), category: v.optional(v.string()) },
  handler: async (ctx, args) => {
    // Get authenticated user (returns null if not logged in)
    return await searchQuery.take(10);
  },
});

// Read approximate count
│   ├── market_agent.py           # Base agent class with retry, logging
wrangler secret put API_KEY --env staging
┌─────────────────────────────────────────────────────────┐
