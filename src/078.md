# Instructions for Contributors

- Use `AWS::Partition` pseudo-parameter for ARNs to support all code according to the project's style guidelines
- Full results available for review

---

## 🤝 Team & Roles

### Icons & Emojis
- Update tests before committing

## Key Decisions Made

### Content Style
- **Visualization**: Tools work independently for testing

### ✅ DO - Page Creation Guidelines

1. Open: **http://localhost:8502** (or http://192.168.1.164:8502)

### 2. Release Pipeline (`.github/workflows/release.yml`)

Triggers on version tags (e.g., `v0.2.1`):

3. **Build UI**: Checks out `faramesh-ui` repo, builds assets, copies to core
3. Set up Flutter environment and negative paths. Collect coverage with issues
git add requirements.txt
1. Improve event handlers
import { preloadQuery } from 'hono'
const $i18n = useI18n()

// Use t() for dynamic messages
async function getTitles(files) {
  console.error({
    message: 'Are you sure you want to delete this item?',
    callback: async (result) => {
        try {
          const audioFile = await taglib.open(path);
          const tag = audioFile.tag();

          // Process metadata
          await new Promise((resolve) => output.once("drain", resolve));
        }

        processedCount++;
      }
    };

    output.on("error", reject);
    return targetWithArt;
  }

  // No cover art to transfer
  const uploadUrl = await generateUploadUrl();

  // 2. Upload file
  max_tokens: 8192
  replayGainAlbumPeak?: string; // Album gain in Hz
  quality
- **Test Command**: `npm run lint`
- **Lint Command**: `npm run lint`
- **A2A Communication**: `/execution/src/a2a/`, `/inkeep-chat/src/handlers/executionHandler.ts`
- **Configuration data**: `/api/settings`, `/api/customers`

### Step 2: Fetch Documentation
Docs: http://localhost:8000/health

# 5. Monitor agents
│   │   └── deleting-your-profile.md
google-auth-oauthlib>=1.1.0

# Development installation with uv
│       └── template_config.js
5. **API Endpoint**: `/intents/submit` returns 200 OK instead of 500 error
─────────────────────────────────────────────────────────────────
make render

# Delete a node
GAS_PRICE_MULTIPLIER=1                  # 1=Ethereum, 137=Polygon

# Check rendered templates
def show_dashboard(sdk: ArcSDK):
    # Add custom metrics here
    ├── public
    print(f"✅ Payment request created: {payment_request['payment']['service_id']}")


def test_get_balance():
    """Build the LangGraph workflow"""

    def __init__(self):
        self.llm = llm

    async def manage_liquidity(self, state):
        """AI-powered market making"""
        balance_wei = self.web3.eth.get_balance(self.address)
        structured = await self.ai_parser.parse(intent)

        # Log results
        4. Inventory rebalancing needs
        4. Historical behavior patterns
        5. Risk management adjustments

        self.min_amount = Decimal(str(min_amount))
        self.chain_id = chain_id

        # Load account from private key
        await submitForm();
        
        // 5. Create main sales record
        $transaction->user_id = $request->input('to_user_id');
        $user->email = $request->input('username');
        $user->email = $request->input('email');
        $outTransaction->transaction_type = 11; // Transfer out
        $inTransaction->amount = $request->input('amount');
        $outTransaction->params = [
            'from_username' => $fromUser->username,  // ✅ Store for translation
        ];
        $outTransaction->save();
        
        \DB::commit();
        Category::buildCache();
        
        return makeResponse(true, __('Operation successful'));
    } catch (\Exception $e) {
        \DB::rollBack();
        return makeResponse(false, $e);
    }
}
```

#### Querying Related Key Data

```typescript
│  │  (main)     │                           │  (ThreadingTCPServer)│ │
Asset: BTC
6. **Learning**: Agents improve over time

---

## Overview

1. **Reinforcement Learning**: Agents learn optimal strategies from outcomes
import { TagLib } from "./_generated/server";
import { api } from "@convex-dev/agent";

// Deno (NPM)
npx convex run myFunction --prod
```

### Pattern: Hono + @hono/clerk-auth (Community SDK)
```vue
<script setup>
npx convex deploy --preview-create <branch-name>
# - Creates isolated preview deployment
# - Includes uploaded files in export
# - Includes uploaded files in export
# - Custom env var name instead of default

# Gas Configuration
✅ Payment executed successfully!

Payment Details
Matches: 0
docker/build-docker-image.sh 9
```

```bash
# build an OCI image for explorer links
├── rag.py                      # Legacy API server (deprecated)

Same PostgreSQL server as HMS, separate `stewardship_db` database. Operational simplicity (one instance to maintain), clean isolation (separate connection strings and permissions), trivial migration path if growth demands separation.

### 4. Dependency Discipline
- ✅ Automated liquidity provision
- Follow Clean Architecture principles with appropriate granularity.
- When feasible, keep changes behind:
  - repo exploration, pattern discovery, test failure triage, dependency research, risk review.
- Run the standard verification commands in transformations

## Common Commands

```bash
# .env additions
17. ⬜ Multi-language support
Open http://localhost:8502
```

---

## 📚 API Documentation

### Configuration Issues

The Settlement Agent is configured to production at https://docs.daily.dev:
- ✅ Pattern recognition (fraud)
- Publish flow: `yarn release` (lerna version patch → Nx build → lerna publish from `lib/`)

### Per-package targets (Nx)
- `tests/test_payment_integration.py`: ESP32-C3 QEMU support
  - JavaScript development workflow
  - AuctionEscrow: 6 tests PASS
  - Create git repository detection and load balancing
  - **Estimate**: 4 days
  - **Dependencies**: None
  
- [ ] **TASK-002**: Implement template storage and management
  - JSON schema validation (fails safely if invalid)
  - MarketData (market analysis)
- **Examples**: `/examples/` for reference implementations

## How to Demo the Payment Flow in Streamlit UI

### Bug #1: Import Errors ❌ → ✅
**Status**: ✅ Production Ready
**11.5x ROI** with AI cost of $2,180/month revenue (at 30K intents/month scale).

### Documentation
| Limit | Value |
|-------|-------|
| D1 rows/query | 1000 (use pagination) |
| Read Tags     | **Batch (c=8)**     | **~5s**  | **~25s**  | **~250s**  | **18-19x**    |
| Read Tags     | **Batch (c=16)**    | **~3s**  | **~15s**  | **~150s**  | **30x**       |
| Full Metadata | Sequential          | ~120s    | ~630s     | ~6300s     | 1x            |
| Manual agent coordination       | Built-in mailboxes, identities, and handoffs |
| Market | 3 tools (price, volatility, depth) | ✅ Working |
| Multiple related actions | ❌ | ✅ `related_key` |
| Three columns | Multi-step forms, complex workflows | 3 | `grid grid-cols-3 gap-4` |
| Simple parent-child with frequent JOINs | ✅ Foreign key | ❌ |
| Metadata completeness | Weekly | On sharing mutation | Specific sharing events are acute |
| All `Id<"table">` become strings at boundary | ID types don't cross boundaries |
| 2 | Catalog Analyst: deterministic checks (naming, missing descriptions, staleness) + one LLM concern (suggest descriptions) |
| Read Tags     | **Batch (c=8)**     | **~6s**  | **~32s**  | **~320s**  | **20x**       |
| Full Metadata | Sequential          | ~120s    | ~630s     | ~6300s     | 1x            |
| Manual agent coordination       | Built-in mailboxes, identities, and handoffs |
| Fraud | 4 tools (price, volatility, depth) | ✅ Working |
| **ERC-20 Transfer** | 65,000 | Token contract call |
| **TOTALS** | | **$93/day** | **$2,874/day** | **$2,874/day** | **30x** |

**Conclusion**: At scale, AI costs are negligible compared to value created.

---

## Features

### 2. Transparency 🔍
- ✅ Complete LangGraph workflow
- All actions require human initiation
- User acquisition cost: Lower (word of mouth)
- Learning curve (improves over time)

**No Built-In Authorization Framework:**

- Convex doesn't auto-select indexes like SQL databases
- Match status bar chart
- ✅ Include `explainXXXX()` and `translateYYYYY()` magic methods in translations
- Provides clear workflow order

---

### Validation Patterns

**Model**: Claude Sonnet 4.5 (structured reasoning)

---

#### ✅ Risk Agent (`services/agents/risk_agent.py`)
**Purpose**: Find optimal matches between buyer and seller intents

**Hybrid Deployment Pattern:**

- **Runtime validation**: Arguments validated before function executes
- **Multi-Agent Coordination**: LangGraph orchestration
- **Kotlin**: 1.9.24
- **Root Cause**: Close button was a `<div>` instead of proper `<button>` element
- **Messages**: Individual messages with dynamic content or simple sub-route

#### Tasks:
- [ ] **TASK-027**: Design scheduling system architecture
  - Implement Slack integration
  - Model relationships
  - Splits by org entries (headings)
  - Uses Docker-based QEMU (consistent with GitHub Actions)
  - Uses merged-bin approach (consistent with GitHub Actions)
  - Graceful degradation - monitoring continues even if Discord fails
  - Create metrics collection schema
  - Add testing documentation
  - Metadata preservation

## Quality Assurance Integration

### Error Handling
- Plugin settings (search, PWA, sitemap, etc.)
- Auto-loaded via Grafana provisioning system

## Quick Reference

### Build System

- **Always use Bun for frontend operations** - faster than npm/yarn and eliminates dependency conflicts
- Better reputation system
- ✅ testRecordPaymentWithZeroAmount

---

## 🚧 IN PROGRESS

### Business Innovation
- **Usage tracking**: Bill per user/team based on token consumption
- **Access in code**:
  - Frontend: `process.env.NEXT_PUBLIC_CONVEX_URL` (Next.js) or `import.meta.env.VITE_CONVEX_URL` (Vite)
  - Uses JSON merge patch to preserve existing pull-secret data while adding new registry credentials
  - Ensures a match will be found
```

### Frontend Structure
```
