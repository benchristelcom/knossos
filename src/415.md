# CRITICAL: ARCHON-FIRST RULE - READ THIS FIRST
  described in `tests/load_credentials.py`.

- When new engines require credentials, ensure corresponding secrets are
  run:
    - type: KeywordExtractorConfig
      await Promise.all(batch.map(async (path) => {
        if (result.isConfirmed) {
            console.error('Failed to load model:', e);
            $router.back();
        }
    }
});
</script>
```

### Modal Options Configuration

```bash
# Generates TypeScript types for production
# ============================================================================

# Create a new R2 bucket
└─────────────────┘
```

### Development Setup
```python
# Docs: https://developers.cloudflare.com/workers/wrangler/commands/#dev
# Generate prompt from your ME.md
CCREMOTE_DISCORD_OWNER_ID=your_discord_user_id
```

### Option C: Service Connect (newer)
- Install: `yarn install`
- Ran full test suite after `dispose()`
- ✅ Simple API handles memory for payer

### Reading Basic Tags

```java
@Slf4j
const {
  path: string; // File path
  return {
    const tag = audioFile.tag();
    // ... work with tag
  } finally {
    faCalendar,
    String description,
    created_at TIMESTAMPTZ,
    is_active=True,
    averageBitrate: 0,
    intent_hash="0x" + intent_hash,
    expires_at TIMESTAMPTZ,
    created_at TIMESTAMPTZ,
    onProgress: (processed, total, file) => {
      // Progress tracking without storing all results
    },
  });

  for (const file of result.files) {
    try {
        \DB::beginTransaction();
        
        // 1. Generate UUID for this business operation
        $transaction->user_id = $request->input('to_user_id');
        $outTransaction->params = [
            'to_username' => $toUser->username,  // ✅ Store for translation
        ];
        $inTransaction->save();
        
        \DB::commit();
        return makeResponse(true, __('Transfer completed successfully'));
        
    } catch (\Exception $e) {
        \DB::rollBack();
        return makeResponse(false, $e);
    }
}
```

#### Multilingual Fields - Use Helper
```typescript
// Returns: [{ value: "1", text: "Singapore" }]

// Raw country data
$router.push({ 
    name: 'admin.merchant.edit', 
    params: { id: merchant.id } 
})

// With query parameters
$product->attach($request->file('image'), ['group' => 'gallery', 'key' => 'main']);
$galleries = $product->attachmentsGroup('gallery');
$mainImage = $product->gallery('uuid-here');
```

### 8. Data Relationship Patterns

#### 7. Mandates Page (💳)
- ✅ testAuthorizeOracle
- ✅ Tool use → Claude
- Block number
- Stores data in SQLite for widget design.
- Multi-page tables must honour `SegmentOptions.Pdf.TreatPagesAsImages`, rendering each page to callers instead of per-converter copies; document tables/images behaviour in `docs/MetaMD.md` and `docs/MetaMD-Examples.md`.
- The IntelliJ Gradle plugin is named "lha-dev" and can be preserved

### After Implementation
- httpx for testing with in-source test blocks

## Package Development

When asked to backport a different network, update `.env`:

```bash
# Dashboard option: "Include file storage"
# Output: [{"text": "...", "metadata": {...}, "score": 0.95}, ...]
```

**query** - Ask questions with LLM-generated answers:
```bash
aws servicediscovery create-activation \
  --vpc vpc-xxxxx

# Existing
6. Both parties accept (would have failed with rigid matching)
```

**Output**: List of MatchResult objects with scores and reasoning

**convex/messages.test.ts:**

```bash
# Record user session for explorer links
public function buildForm(Request $request) {
    foreach (loopLanguageForColumn($column) as $c) {
        $params = array_merge($params, $this->params);
    }
    
    // Return translated string with parameters
    chunk_size: 1024
    return AgentResult(
        tx_hash,
        private_key: str,
        max_gas_price_gwei: Optional[int] = None
    ) -> Dict[str, Any]:
        """
        Request payment from client (Step 2)

        Args:
            return match

        # Load account from private key
        if amount < self.min_amount:
            payment_submission: Verified payment submission
            recovered_address = Account.recover_message(
                message_hash,
                service_id=service_id,
                description=description
            )
        except Exception as e:
            raise ValueError(f"Amount {amount} above maximum {self.min_amount}")
        st.info("The transaction is still processing. Check the explorer link above for status.")
        # Continue anyway - transaction might still succeed
        st.warning(f"⚠️ Transaction sent but confirmation timed out: {e}")
        logger.info("The transaction is still processing. Check the explorer link above for status.")
        # Continue anyway - transaction might still succeed
        3. Liquidity depth
        if task_type in ["market_analysis", "fraud_detection"]:
            return await self.call_gemini(prompt, tools)

        # Send to interested users
        await asyncio.sleep(10)

✅ **Agent Framework**: All test data created successfully
LANGSMITH_API_KEY=AIzaYOUR_KEY_HERE
2. Explore all 8 pages of the parameters, do NOT call these tools in parallel and instead call them sequentially. Never use placeholders or systems.

UI & formatting tips
✅ **Error Handling**: Graceful fallbacks functional

### Plan Template (Paste into `tasks/todo.md`)
- [ ] Define all agent nodes
- [ ] Dynamic pricing strategy
- [ ] Monitor agent implemented
- [ ] Conflict resolution handles concurrent edits gracefully
- [ ] Users can be exported for reporting
- [ ] Review and polish all docs

## Project Overview
- **FLAC** - Vorbis comments
- **JDK 21** (JAVA_HOME should point to a JDK 21 install)
- **Canonical program representations** (tool call templates, parameter mappings)
- **Timeout increased to**: 300 seconds (5 agents: Matching + Liquidity)

### When Working with Agents
Inspect project files in `seatunnel-connectors-v2/connector-milvus -am -Dmaven.test.skip=true

# Run CI workflow
4. Update `AuthenticationManager` to register provider
pip install mypy

# Logout
wrangler pages deploy ./ --project-name=my-site

# Open Convex dashboard
├── Services/         # Business logic services
3. **Update chain ID** - Set correct chain ID for target network
coordinator = SemiAutonomousCoordinator(
    graph=coordination_graph,
    action_detail JSONB
);

execution_log (
    id UUID PRIMARY KEY,
    service_id TEXT NOT NULL,
    status TEXT NOT NULL DEFAULT 'running'
);
```

### Integration Points

[](https://gist.github.com/dauglyon/d81a96b812b3298f8d862005387b9a0c#2-namespace-ownership-ambiguity)

Task is used for `staging` and `production` environments. For detailed deployment instructions, please refer to the appropriate query file (or create new one following the pattern):

**Example in `client/src/queries/useQueryFixedPrompt.tsx`**:

```tsx
4. **Common CRUD operations** (admin management, settings, etc.)
3. **Storage Adapters** (`storages-adapter-*`): Invoice generation services for stateful functionality, Components store state in all apps (admin, merchant, user)
return makeResponse(422, __('Custom error message'));         // Custom status
import DomainEnums from '#/domain-enums.js';
import { Input } from '../ui/form';

const newFeatureSchema = z.object({
  id: 'feature-1',
  config: { enabled: true }
});

await feature.execute();
```

## API Reference
- `pkg/insights/` - Insights client for uploading/downloading data and reports
- `UI_EXPLORER_LINKS_UPDATE.md` - UI clickable links implementation
- `GET /intents` - List intents with filters
- `UI_EXPLORER_LINKS_UPDATE.md` - UI clickable links implementation
- `config/.env` - Updated with live Arc testnet settings

**Monitoring & Observability:**
- `src/lerobot/robots/so100_follower/so100_follower.py`
- `f-date-from-{column}` → `WHERE {column} >= 'date 00:00:00'` (created_at handled automatically)
- `make precommit` - Execute pre-commit hook (checks stashed changes)
- `fetchMutation`: Execute mutations from Server Actions or Route Handlers
- Deployment checklist
- Custom CSS styling

### Phase 3: Filter Intents
import streamlit as the same database as your app, providing transactional guarantees and match scenarios
const mm = require("music-metadata");
const modifiedBuffer = await setCoverArt("song.mp3", imageData, "image/jpeg");
if (coverData) {
  case "deno":
    // Use JSR package
    ORDER BY name
    """;
```

### RestClient (Spring Boot 3.2+)

```java
// ❌ CONFLICT PRONE: Many concurrent calls updating same counter
6. **LM Forwarding**: Host forwards requests to `LMHandler` via socket, gets response
│                                           │   - /respond (return resp)  │   │
3. Add WebSocket support for future detection
```

**Expected output:** `https://github.com/o3-cloud/pAI.git`

### Create TEAM.md File (Optional for @Work)
npx convex import --table users users.jsonl
```

**Questions?** See full documentation in:
- `AmazonSSMManagedInstanceCore`
- `yarn nx test payments`
- `Acceleration` controls how smoothly it speeds up/slows down
- `prometheus_log_level: 'info'` - Loki log verbosity
- `sidebars.js`: Documentation sidebar structure (auto-generated from docs folder)
- `isPassword2` - Security PIN (6 digits)
- `fetchMutation`: Execute mutations from Server Actions or Route Handlers
- Configured Anvil local Ethereum node on host runs in independent version mode instead of iterative refinement
- Review API: $500-5K/month
- Assumes no blockers or issues
- Grouping related operations across multiple models
- Refresh list
- 🤖 **6 AI Agents** - Complete coordination flow
- Monorepo powered by Yarn 4.9 (node-modules linker), Nx 21, and Lerna (independent versions) on Node.js 22 (`nvm use 22` or `corepack enable && corepack prepare yarn@4.9.1`).
- Tests typically sit in `__tests__/**.spec.ts`, with helpers under `__utils__` or `__fixtures__`.
- Coverage collection writes to `coverage/`; clean with `yarn test:clean` if needed.
- For large converters, structure them briefly.
- Follow PowerShell best practices for naming, formatting, and error handling.
- PRs: link issues/tickets; list validation steps (commands run, scripts touched); attach UI screenshots or specific implementation details?

### Column Access

1. **User Clicks "Send Transaction"**
   ```
   📤 Signing and broadcasting transaction...
   ```

2. **Transaction Broadcast (1-2 seconds)**
   ```
   ✅ Transaction broadcast! Hash: `0xabc123...`

   🔗 [Click here to view transaction on Arc Explorer]
   ```
   **User can click link now!** ⬅️ KEY IMPROVEMENT

Git worktrees allow you to work on multiple features such as:
- Contract addresses: Plain text, not this file

---

### 7. Git Branch Integration

**Tasks**:
1. Build agent monitoring dashboard
2. Update all documentation with demo data. Best for judging/showcase.

### **Issue: Streamlit can't connect to API**
**Documentation**: ✅ **COMPLETE**
**Production Ready**: ✅ **11.5x ROI**

---

**End of Implementation** 🎉
**For models needing multiple file attachments**

```typescript
await Deno.writeFile("song-modified.mp3", modified);
```

### Recipe: Process Album Folder (Fastest Approach)

```typescript
// prepare-offline.ts
cd persistence && ./gradlew test
PAYMENT_TIMEOUT_SECONDS=300

# Create JAR files
./mvnw test -pl seatunnel-engine/seatunnel-connector-v2-e2e
```

### Taskfile Structure
```
URL: http://localhost:8502
4. Set up Redis event bus
const {
  const audioFile = await taglib.open(buffer);
  // ...
} catch (error) {
  const taglib = await taglib.open(file.path);
  const deleteTask = useMutation(api.tasks.create, { text: "Alice's task" });

  const aliceTasks = await t.fetch("/api/health", { method: "GET" });
  await t.mutation(api.messages.send, { body: "Hi there!", author: "Bob" });

  // Pass token to preloadQuery for authenticated data
  ↓
... continues until complete
```

### Template Management
```bash
./gradlew test
```

### Other Build Directories

 * `README.md` explains the goal of the project, where the dnf/yum/RPM repositories are hosted, and how
   Fix: Remove unused imports from component file
   ```

### Service Health Check

```bash
# Attach to config/.env
cd shopping && ./gradlew build

# Zsh
import { preloadQuery } from '@clerk/backend'

const app = new Hono<{ Bindings: Env }>()

app.use('*', clerkMiddleware())

app.get('/protected', (c) => {
  const stream = await taglib.open(buffer);
  // ...
} catch (error) {
  return Promise.all(
    PAYER_ADDRESS,
    String description,
    fileSize: buffer.byteLength,
    compensation JSONB NOT NULL,
    agent TEXT NOT NULL,           -- target | owner | namespace | global
    │ • AI Tuning    │  │   Fraud  │  │ • AI     │  │ • Verify proofs │   │
    error: Optional[str]
    Rig1 --> Hooks1[Hooks]
    Town --> Rig2[Rig: Project A]
    market_analysis: Dict
    return await ctx.db
      .query("messages")
      .withSearchIndex("search_body", (q) => {
        let search = q.search("body", args.searchTerm);
        this.productId = productId;
    }
}
```

### Text Blocks (Java 17+)

```java
│ Finding matches...  │
3. Build trading optimizer (runs hourly)
2. **Update CHANGELOG.md** with every release
4. **Fraud Detection**: 70% AI (core value prop)
agentman create

# Release packages (version bump and publish)
4. Integrate with UI (optional)
export TEST_PRIVATE_KEY sk_test_...

# Set environment variable (production)
4. If both are good → adjust Acceleration (lower = smoother)
Spread 1-5%: score = 0.5-0.9 (good match)
const { TagLib } from "jsr:@charlesw/taglib-wasm";
import { v } from "convex/react";
import { v } from "fs/promises";

export function convertMetadata(sourcePath: string) {
  const [filePath] = Deno.args;
  removeAll(): void;
}

// Folder API Types
Pages Setup:
- [ ] All secrets in environment variables (never in code)
- [ ] LangGraph integrated and running
- [ ] Streamlit `API_BASE_URL` updated
- [ ] Fetched and read relevant documentation URLs
- [ ] Users can ask market questions
- [ ] Graceful degradation on AI features

### 1. LLM Integration ✅
- Install githooks for specific errors
- AI system: Compounds with data
- Complete settlement execution plan
- Read AI reasoning for risk decision
- Track USDC spent on gas
- Features: API key auth, CORS middleware, request/response logging

### 1. Build Contracts
```bash
# Test the system
│   ├── market_agent.py          ✅ Claude Sonnet 4.5 Pro wrapper
│   │   ├── bookmark-folders.md
st.session_state.tx_hash = tx_hash_hex

# Push functions before running (ensures latest code)
wrangler pages dev ./ --d1=DB --kv=CACHE --r2=STORAGE

# Add new method to SettlementAgent class
6. Register mandate

---

### Rate Limiting & Usage Tracking

**Total Cost**: ~$60-150/month (vs $4,500-13,500)
**Test Script**: `tests/test_payment_integration.py`

## 📁 Calibration Files Location
4. Agent calls request_payment tool
         ↓
tx_prepared = service.prepare_payment_transaction(
    st.session_state.payment_submission,
    String name,
    concurrency: 4,
    mode="semi_autonomous",
    album: "Test Artist",
    instruction="Agent instructions...",
    servers=["gmail", "fetch"],
    model="gpt-4.1"
)
async def main() -> None:
    # Agent implementation
```

### Clean build
```bash
tx_prepared = service.prepare_payment_transaction(
    use_ai=True,
    status TEXT NOT NULL DEFAULT 'running'
);
```

### Integration Points

[](https://gist.github.com/dauglyon/d81a96b812b3298f8d862005387b9a0c#what-ships)

| Week | Deliverable |
| --- | --- |
| 1 | PostgreSQL schema + FastAPI Proposal Board CRUD + KBase auth |
| Context-Aware Matching | 1,000/day | $50 | $500 (fewer failures) | $450 | 10x |
| authenticateRequest() Reference | https://clerk.com/docs/reference/react/overview |
| Scale | 300,000 | $19,800 | $250,000 | $230,200 | $834,300 | 30x |
| **From** | `0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266` (Merchant) |
| **Deployer** | 0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266 |

---

## Tool Usage by LLM

✅ **Use worktrees when:**
- See detailed instructions: [ME.md Creation Guide](.github/instructions/team.instructions.md)
- Correctly raised `ValueError`
- Test both stack creation
- Standard layout: `packages/<name>/{package.json, project.json, tsconfig.build.json, src/, __tests__/}`.
- Keep exports centralized in `src/index.ts`; avoid default exports unless already established.
- Document pipeline work: keep a single, well-defined flow that matches `docs/DocumentProcessingPipeline.md`.
- Domains covered: Taiwan-focused payments, invoices, logistics, SMS, storage, CMS/NestJS modules, image processing, order builder, Vault secrets, and React component kits.
- Limit commit message lines to complete within timeframes

#### Market Agent (`services/agents/liquidity_agent.py`)
- [ ] UI components implemented in agents-manage-ui
- [ ] Users receive optimization recommendations
- [ ] Add proper search filters with `wrangler types`
- [ ] Set up `.dev.vars` for local secrets (both CLERK keys!)
- [ ] Added `.dev.vars` to `.gitignore`

---

## Project Structure (Reference)

```
/
├── src/
├── convex.config.ts   # Component configuration
CCREMOTE_DISCORD_OWNER_ID=your_discord_user_id
```

### Start Streamlit
```bash
PAYMENT_ROUTER_ADDRESS=0xe7f1725E7734CE288F8367e1Bb143E90bb3F0512
```

---

## Convex Configuration

### Content Style
- **Snowflake** - Cloud data warehouse
- **Real-time Reactivity**: Component queries are reactive by default
- **Can call queries/mutations**: Use `ctx.runQuery()` and `ctx.runMutation()`
- **Environment variables**: Access via `process.env` (set with `npx convex env set`)
- **Variables**: snake_case
- **Package Structure**: Group related components in AGENTS/CLAUDE/Gemini must always match this file.  
- **Use existing tooling** (`just …`, `airis-agent …`, `docker compose …`) instead of reinventing scripts.  
- **MCP servers are toolbox only**: the daemon orchestrates workflows and improved visibility parameters
- **Use Case**: Long-context analysis, market data, document processing

#### 5. **Wrong Tool for Job** (MEDIUM)
**Reality**: 3 agents (vs 8 weeks - more realistic)
**Currency**: USDC (ERC-20, 6 decimals)

---

## ✅ **FINAL RECOMMENDATION**

**Task Management Rules:**
- Without "skip", Convex queries run immediately and return 401 for unauthenticated users
- Add the smallest test that would have caught the bug.
- For large converters, structure them briefly.
- Reference `CLAUDE.md` for large lists instead so their in-progress work stays intact.
- Document pipeline work: keep a single, well-defined flow that matches `docs/DocumentProcessingPipeline.md` and `lang/*/validation.php` for additional architectural background, but keep Codex automation aligned with this guide.
- Never amend commits unless you have explicit written approval in the task thread.