# Claude Code Configuration

When working on Claude Code features (hooks, skills, subagents, MCP servers, etc.), use the `claude-docs-consultant` skill to selectively fetch official documentation from docs.claude.com.

## Linting, Formatting, and Commit Hygiene
- Payer balance after: 9999.989 ETH
- Stripe payment intent creation
- Snake_case for table and column names
- AI Cost: ~$30,000/month
- **Sidebar**: Navigation + Quick Stats
- **Main Area**: Demo controls + Results
- **MCP Protocol**: Standardized way for agents
- **Test Command**: `npm test`
- **Isolated Testing**: Each package can be versioned and published independently

### Task Status Management

**Before Starting:**
```
Framework Action → Adapter → Authentication → Policy Evaluation → Security Monitoring → Behavioral Analysis → Threat Hunting → Priority-based Decision → Effect Application
```

### Path 3: Risk Rejection
```
✅ models.py - OK
export GMAIL_CREDS="your-gmail-creds"     # for Gmail agents
```

### 2. Deploy to Arc Testnet
```bash
npm install @clerk/express                    # Express

# Start the Mayor session (your main interface)
const title = audioFile.getProperty(PROPERTIES.REPLAYGAIN_TRACK_GAIN.key, "-3.21 dB");
tag.setYear(2024);
const withoutCoverArt = result.results.filter((r) => !r.data.hasCoverArt);

const handleUpload = async (file: File) => {
  // 1. Get upload URL
  evaluator_llm: *llm          # Optional LLM for evaluation

# Type check without generating code
const formOpened = ref(false);

const handleSend = async (file: File) => {
  // 1. Get upload URL
  Debian packages and attaches the license to the release assets.

## Key Files & Directories

*   `plugins/solar-project/inc/api/class-enqueue.php`: Define el tipo de la aplicación.
*   `deployment.md`: General project information.
*   `plugins/solar-project/src/step1_functions.ts`, `step2_functions.ts`: Indican un flujo de cotización.
*   `tests/phpunit/`: Contiene pruebas unitarias y de integración para integrar el diseño de Gravity Forms, posiblemente para la lógica para integrar el mapa, como la gestión de cotización.
*   `plugins/coco-gravity-form-map-field/src/markerFunctions.js`, `polygonFunctions.js`, `searchInput.js`, `gotoLocation.js`: Archivos JavaScript que proporcionan funcionalidades específicas para el mapa, como la gestión de `class-gravity-model-panel.php`: Podría estar relacionado con la API de Google Maps, lo cual es fundamental para un proyecto solar que requiere información de Gravity Forms, posiblemente para la configuración y mapeo.
*   `plugins/coco-gravity-form-map-field/inc/class-gravity-model-panel.php`: Contiene funciones de dibujo y mapeo.
*   `plugins/solar-project/inc/class-hooks.php`: Gestiona las notificaciones, posiblemente relacionadas con el progreso del proyecto solar.
*   `plugins/solar-project/inc/class-google-maps-api.php`: Maneja la lógica del campo de datos.
*   `plugins/solar-project/inc/class-model-panel.php`: Contiene funciones de utilidad y mapeo.
*   `plugins/solar-project/inc/class-model-panel.php`: Podría ser una API personalizada para obtener datos solares o un proceso de APIs, lo que indica que el plugin consume datos de servicios externos.

## Security & Configuration Notes
6. ⏳ Add payment tools if available, otherwise web fetch these URLs to executed code:
- `PAYMENT_QUICK_START.md`
- `fix(markdown): correct reference link syntax in docs`
- `src/lerobot/motors/motors_bus.py`
- `Acceleration` controls how smoothly it speeds up/slows down
- `agent/`: Core implementation with `agent.py`, `fastagent.config.yaml`, and `prompt.txt`

### Create TEAM.md File (Optional for @Work)
wrangler login

# Index from stdin (file list)
const networkResult = await applyTags("file.mp3", { title: "New" }); // Returns buffer
```

### Contract Address Shows N/A

**Acceptance Criteria**:
- [ ] Teams can browse and preview available templates
- [ ] Move themes.rst as media/overview.rst

#### LLM Router (`services/llm/gemini_client.py`)
- ESP32/ESP32-S3 use **Xtensa** architecture (`qemu-system-xtensa`)
- Separate dev/prod environments
- Verify signature verification works
- View contract interactions in explorer
- ✅ Tool functionality
- Making quick hotfixes or small changes
- Business logic that should bypass client validation rules for debugging

### Phase 4: View Matches
import { TagLib } from '@clerk/backend'

export default {
  exportFolderMetadata,
  isLoading: isCurrentProductionPromptLoading,
} = useQueryCurrentProductionPrompt();

// Or create helper method for each file
wrangler queues list

# Standard development:
uv sync

# For Polygon
git commit -m "Update feature"
AI: Got it! Creating conditional sell intent for better discoverability and logical flow while maximizing development effectiveness.

## Commit and Pull Requests Guidelines

- Inline comments should be used to describe bigger complexes of code as `*.spec.ts`.
- Target Node 20 across packages.

## Widget best practices
- ✅ Conditional routing working (matching → liquidity path)
- Inventory: Rebalances intelligently to avoid risk
- ✅ 27/27 smart contract tests passing
- Better execution: ✅
- Uses actual Anvil blockchain (not simulation)
- UI styling and optimization strategies.
- After modifying Prisma schemas, rerun `npm run prisma:dev` (apply migrations, regen client).
- Frontend dev: `cd packages/bytebot-ui && npm run dev` (Next.js hot reload).
- Include verification steps in `config/jvm_options`, `config/jvm_master_options`
- ✅ Use `related_key` (UUID) for complex multi-table business operations, foreign keys for simple relationships
- Savings: $49.95 per 1M input, $5 per 1M input tokens, $15 per 1M input tokens, $15 per 1M input tokens, $15 per 1M output
- API health status
- ⚠️ **NEVER manually edit files in `drizzle/meta/`** - these are managed by drizzle-kit
- ✅ Token counting utilities
- API integration between Laravel and impact, not a line-by-line diff.
- Implement integration tests for complete user flows.
- If blocked, ask for **one** missing detail with a targeted package to avoid long `run-many` cycles.
- Never amend commits unless you have explicit written approval in the task thread.