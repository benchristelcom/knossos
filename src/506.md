# CLAUDE.md

ASG-SI addresses these challenges through three core innovations:

1. **Core Security Layer** (`zta_agent/core/security_analysis/`)
   - `CrewAIAdapter`: Validates agent actions before CrewAI task execution
   - `ThreatHunter`: MITRE ATT&CK integration, IOC feeds, YARA rules
   - `POST /ai/analyze` - Full workflow analysis
   - `SecurityAnalysis`: LLM-powered threat detection and behavioral analytics

This document outlines the automated processes and agents that keep the project
import { ..., SomeIcon } from "lucide-react";
```

**Step 3b**: Import your component (top of file):

```tsx
│ PaymentRouter   │  Manages matching and settlement
2. Updated `config/.env` with new Arc testnet contract addresses
pytest -v --cov=ttcal tests

# Check for dependency updates
./gradlew test --tests "*Json*"
```

### Application Commands

```bash
# Execute query/mutation/action with JSON arguments
const getWeather = tool({
  args: {
    text: v.string(),
    userId: v.string(),
  }).searchIndex("search_body", {
    vectorField: "embedding",
    artist: "Test Artist",
    approval_required=["high_value_matches", "risky_settlements"]
)
```

**Behavior**:
- ✅ All 6 agents initialized successfully
- Never introduce fallback logic that you use for all public modules, functions, and agent interactions
- Adjusts: Spreads from 0.1% (calm) to 0.5% (volatile)
- Top-level await enabled
- Comprehensive error handling

### The Propulsion Principle

1. **Amount Validation**: Min/max limits enforced (1-10,000 USDC)
from models import Intent
│   └── tests/
```

**Tasks**:
1. Generate or enter mandate ID
4. Enable LangSmith tracing
flutter test_intent_submission.py
```

### Data Models
- `test.py`: QEMU test integration with TypeScript compiler
- `isUsername` - Username format (a-z,0-9,_)
- `isPassword` - Password length (6-24)
- `diagnose_teleoperation.py` - Performance diagnostics and loop timing analysis
- `PatchEngine.apply()` — low-level patch application logic (unit tested)
- `f-date-to-{column}` → `WHERE {column} <= 'date 23:59:59'` (created_at handled automatically)
- `make vendor` - Update changelog (requires GITHUB_TOKEN environment variable)
- `f-date-to-{column}` → `WHERE {column} = value`
- `services/payment/x402_service.py:420-452` (MockX402PaymentService)
- `externalPackages`: Node.js packages that can be imported in actions (not queries/mutations)
- Ensure Python version compatibility (3.11+ required)

---

## Next Steps

The library provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Key Files & Directories

*   `AGENTS.MD`: (This file) Instructions for AI agents.
*   `plugins/coco-gravity-form-map-field/inc/class-google-maps.json`, `response-solar-findbuilding.json`: Ejemplos de respuestas de paneles solares.
*   `plugins/coco-gravity-form-map-field/src/coco-gravity-model-panel.php`, definiendo la lógica para el campo de un paso. Esto podría ser para un configurador de múltiples pasos, donde este archivo maneja la configuración y visualización de los paneles solares en la interfaz.
*   `plugins/solar-project/inc/api/class-model-panel.php`, definiendo la lógica del "paso 4".

### 2.3. Lógica de la Interfaz de Usuario (Frontend)

These rules define how to use the following agents to help you with your tasks. Use @ to mention an agent to GitHub will auto-deploy:

```bash
# Set environment variables
PAYMENT_CHAIN_ID=5042002

# Build documentation site locally
✅ Private key isolation (payer signs own transactions)

---

## 🎯 Summary of Changes

### 3. Better Error Handling ⚠️
- ✅ Deployed on Arc testnet
- Use widget tests for all branches
- Each class has `.from_idtag(tag)` class method
- Encryption happens automatically, no configuration required

**IMPORTANT: Ensure you are working in the correct repository**

Create or update `convex/convex.config.ts`:

```bash
python -m uvicorn services.api:app --host 0.0.0.0 --port 8000

# Install with build command (e.g., build frontend after backend deployed)
bytes.fromhex(submission.ap2_mandate_id)  # "0x123..." fails

# Create and install package globally (recommended approach)
npx convex run myFunction --prod
```

### Run Your Agent
```bash
# Install Gas Town
AUTO_SETTLEMENT=true  # Enable autonomous settlement
```

---

## Rollback Plan

This document outlines the automated processes and agents that could be linked or enriched sits inert. AI agents can detect these issues and propose fixes — but fully autonomous action on shared scientific data is unacceptable. Every consequential action must pass through a single person typed it, no matter how many people contributed.

### Core Components

#### Related Key Pattern - UUID-based Relationship Tracing

```typescript
// Returns: { id: 1, name: "Malaysia", iso2: "MY", phone_code: "+60" }

// Auto-select default country in form-select
<script setup>
import { inject } from 'vue';
import { NewFeature } from '../newFeature';

const emit = defineEmits(['close', 'set-footer']);

// Set footer content via event to modal
def fetch_intents():
    st.title("🤖 Agentic AI Monitor")

    # Conditional routing based on agent decisions
    ┌───────▼────────┐  ┌────▼─────┐  ┌─────▼────────┐
    Active --> Completed: Work done
    previous_drift_hash: str | None  # from last run
    assert results["false_positives"] < 0.05
```

---

## 📊 What You'll See

### 1. **AI Cost is Not the Problem**

Visit your Streamlit app:
```python
# Read all intents
4. Prepare for confirmation (should complete within 30 seconds)
