# Project Name

> This is an example CLAUDE.md file showing how to configure Claude Code for your project.

## 3. Plugin "coco-gravity-form-map-field"

Este plugin extiende Gravity Forms con el campo de mapa.
if I tell you to remember something, you do the same, update


## Rules to follow
- Responsive layout
- Real-time price and PWA setup
- Asset: BTC
- ✅ Fallback handling
- ⚠️ **NEVER edit existing migration SQL files after they've been applied** - create new migrations instead of reloading

### User Experience

**Original Plan Said**: "Sub-second matching"
**Reality**: Should use deterministic algorithms with AI enhancement

#### Phase 2.5 Pro Client (`services/llm/claude_client.py`)
- Actions: `build_form`, `submit_form`, `delete`

### API Server (`rag_client/core/models.py`)
- **RAGWorkflow** class orchestrates the entire RAG pipeline:
  - `shared` — DTOs and schema utilities shared by services.
  - `bytebot-agent` — Next.js front end.
- Keep error semantics consistent (throw vs return error vs empty result).

### 2. Subagent Strategy (When Risk Is High)
- Ensure API_BASE_URL in Streamlit dashboard

---

## 🤝 **Priority 3: Collaboration Features**

### What Actually Works
- **Children don't need** icons (handled by menu styling)
- **Role**: Payment sender
- **Classes**: `TagLib`, `AudioFile`, `Tag`, `PropertyMap`

### 7. Laravel Traits - Essential Features

#### 7. Mandates Page (💳)
- Signature validation
- Use `archive` action for tasks pending validation/testing
- Impact: Negligible (SQLite is fast for single writes)

**Best for:** Coordinating complex, multi-issue work

```typescript
// Automatic streaming when using CDN URL
public function createTransfer(Request $request)
{
    $rules = ['username' => ['required', 'string', 'max:255']];
    $this->validate($request, $rules);
    
    try {
        \DB::beginTransaction();
        
        // 1. Generate UUID for this business operation
        $outTransaction = new Sale();
        $transaction->related_key = $relatedKey;  // ✅ Same UUID
        $inTransaction->amount = -$request->input('amount');
        $inTransaction->transaction_type = 11; // Transfer out
        $auditLog = new UserCreditTransaction();
        $sale->related_key = $relatedKey;  // ✅ Same UUID
        $inTransaction = new UserCreditTransaction();
        $sale->related_key = $relatedKey;
        $transaction->transaction_type = 'SALE';
        $auditLog->save();
        
        \DB::commit();
        return makeResponse(false, $e);
    }
}

public function delete(Request $request, $id) {
    // ... delete logic ...
    
    // ALWAYS clear and rebuild cache after deletion
    naming_score: float          # regex-based convention check
    const response = await fetch("https://api.sendgrid.com");  // ✅ OK

    async def generate_market_report(self):
        """AI-powered market making"""
        activity = state["orderbook"]

        prompt = f"""
        Optimize liquidity provision:

        Bids: {bids}
        Our Inventory: {inventory}

        Consider:
        1. Wash trading (self-dealing)
        self.address = self.account.address

        Args:
            payment = payment_submission["payment"]
            payer_address = payment_submission.get("signature", "")
            payer_address = payment_submission["payer"]["address"]
            amount_wei = int(payment["amount"])

            # Check balance
            tx = {
                'nonce': nonce,
                'to': self.address,  # Payment to settlement agent
                'value': amount_wei,
                'height' => 200,
            ],
            'receipt' => [
                'path' => '/transaction/receipt/',
                'rename' => 'random',       // ✅ For permanent files
            ],
        ];
    }
}

// Usage in controllers
$settingStore.defaultCountry
// Route structure
4. Ensure all 6 AI agents in sequence
│   ├── base_agent.py        ✅ 565 lines
4. Added fallback to intent hash for Healthcare, Fintech, Legal, SaaS, and 6 more verticals
agent = SettlementAgent()
print(f'Connected: {service.web3.is_connected()}')
print(f'Currency: {service.token_symbol}')
print(f'Decimals: {service.token_decimals}')
"
```

### For Production (Arc Testnet)

```toml
[tool.poetry.dependencies]
run = ["sh", "-c", "streamlit run ui/streamlit_app.py", "--server.port=8501"]
```

### Environment Variables
cd restclient && ./gradlew test
import { defineComponent } from "./_generated/server";
import { api } from "convex/values";

const rateLimiter = new ShardedCounter(components.rateLimit, {
  concurrency: 4, // Each file uses ~2x its size in memory
});

// Standard handlers
python -c "$(curl --location https://taskfile.dev/install.sh)" -- -d -b ~/.local/bin
```

### Build and Install Locally
```javascript
// Default (automatic Wasm loading) - Most common
3. ✅ Integrate with AP2 payments
2. Modify `data.yaml` for your project.

### Feedback Loop: Structured Rejection Scopes

[](https://gist.github.com/dauglyon/d81a96b812b3298f8d862005387b9a0c#recommendation)

Weekly digest: "You have N proposals awaiting review (K critical)." Links to JupyterLab with deep link to sync versions across repos:

- Creating tests → `testing-patterns` skill
- Use try/finally blocks to ensure cleanup:

```typescript
6. **Progress Reporting**: Use stderr for you.
     Setting trigger at $11,000.
     ✓ Created

Before submitting, ensure:
- [ ] UI loads without errors
- [ ] Real-time synchronization works across team members
- [ ] Create api.rst
  - Active Intents: 10
  - Supports both legacy Smart Proxy and commit configuration interface
  - Design aggregation and customization
  - Can test other examples (Blink, custom sketches)

## Architecture Overview

The frontend has a description, the agent might suggest a slightly different one next week. The feedback suppression suppresses by concern+table (so this gets caught), but the inconsistency erodes trust.

### Core Schema

[](https://gist.github.com/dauglyon/d81a96b812b3298f8d862005387b9a0c#authentication-service-account-reads-reviewer-delegated-writes)

-   **Read path**: Dedicated `berdl-stewardship-svc` KBase service account with `BERDL_STEWARDSHIP_READ` role. Read-only access to HMS (metadata level), MCP (SELECT-only already enforced), and governance API (new read-only management role). No write access to any S3 path.
-   **No agent ever holds write credentials.** A bad proposal is a row in a hierarchical system costs at minimum 3 LLM calls; context masking (keeping only M most recent observations) can halve per-instance cost.

### Module Systems

```typescript
// DON'T DO THIS - Path strings are fragile
$router.push(`/admin/management/admin/${id}/edit`)     // ❌ Wrong  
$router.push('/admin/deposit/list?status=pending')     // ❌ Wrong
```

#### Disaster Recovery Scenarios

**Visualization**:
- Best Bid: $10,000
- Stripe integration
- Define indexes in the service they extend; keep tests next to code
- ✅ Runs at stable 30 Hz with low jitter
- Incomplete model documentation (only 2 of 15+ models documented)
- REST API (HTTP endpoints)
- After: ~2-3 seconds (same performance)
- Queries never see partially imported state

#### ✅ Risk Agent
- Homepage components are "best-effort" - may drop events under `packages/*`; each package ships TypeScript source in real metrics alongside traces—include histogram/counter coverage so latency is observable at both levels and constraints
- Multi-asset support (USD, ETH, BTC, USDC)

### Checklist

4. Should open Arc testnet explorer:

1. **Payer creates payment intent**: Signs a structured message with payment details
SaveSystem.Save/Load serializes PlayerData plus GameStudioData (WebGL path) and caches the last load; always bump SaveData.Version if the schema changes.

1. Assume only CLI + filesystem access. GUI, physical devices, or login-only apps are “not directly operable”.

Jobs/ – Long-term project flow (GameProject, selection/setup UI). Adjust award/cost math or teams:

```
* @your-username
/blog/*    /articles/:splat  302
```

### Windows
```vue
<Input value={fixedPromptText} />

// After
<auto-button v-if="hasEditPermission" @click="onEdit">
    {{ $t('Edit') }}
</auto-button>
```

### Form Template Structure - Multiple Footer Solutions

**File Storage Inclusion:**

```typescript
// The SDK will recognize and process it for authentication bypass
```

---

## 📚 API Documentation

### If Cross-Stack Work Emerges
Net profit:       $230,200/month
Health: healthy
```

### BANNED - Never Use These Slow Tools

* Don't use emoji in the code
* Before you finish, please verify your solution.

**Basic Example:**

```bash
2. Sign up with GitHub

### **OPTION A: Streamlit Cloud Only (Demo Mode)** ⭐ RECOMMENDED FOR HACKATHON
├── merchant/        # Merchant application  
make install        # Install to system
```

### Testing

```vue
<!-- Modal-compatible component (e.g., model-form.vue) -->
<div class="modal-footer grid grid-cols-3 gap-4 rounded-b-xl grid grid-cols-3 gap-4">
    <auto-button type="plain" @click="emit('close')">{{ $t('Cancel') }}</auto-button>
    <td>{{ record.balance ? $helper.fundFormat(record.created_at) }}</td>
    <div>{{ $t('New payment amount x from y', { 
        x: $helper.fundFormat(payment.amount), 
        y: payment.merchant_name 
    }) }}</div>
</template>
```

## Translation System

### Templating System
- **`grafana/provisioning/`**: Auto-provisioned dashboards and datasources

### Competition Strategy

**Backend:**
- ✅ Payment verified and completed
- Check logs for specific errors
- When changing Python dependencies, always use `uv add` or `bun dev` in the core library; keep cross-cutting helpers clean and production-ready.
- Backend deps: `cd backend_py && uvicorn app.main:app --reload --host 0.0.0.0 --port 3001` — runs the API.
- Use `scripts/download-latest-artifacts.sh` to grab the hash
- Supports keyboard shortcuts (Cmd/Ctrl + K)
- No files? -> Run `npm create cloudflare@latest`

### Extending the Demo

✅ **Step-by-Step Visualization** - See every agent execute
const streamChat = useAction(api.files.generateUploadUrl);
const isCreate = computed(() => !$route.params.id);

const onSubmitForm = async () => {
    try {
        mockMvc.perform(get("/hello"))
                .andExpect(status().isOk())
                .andExpect(content().string(containsString("Hello, World!")));
    }
}
```

## Common Troubleshooting

### Release Commands

```bash
# Workers: https://developers.cloudflare.com/workers/wrangler/commands/#dev
# ============================================================================

# Deploy Worker to Cloudflare (production)
# - Batch imports for efficiency
# - JSONL format for easy re-import
# - Creates separate dev deployment (not production)

# For specific technical challenges
└── tests/             # Test files
```

## Visualization Best Practices for Competition

**Shows:**
- ✅ Only system with LLM-powered reasoning
- Pie charts for status distribution
- ✅ All services running (Anvil, API, Streamlit)
- AI analysis cost: $0.15/GiB/month
- Position exposure calculation
- State persistence via `dill` serialization to `/tmp/rlm_state.dill`

### Making a New Isolated Environment

1. **Ollama**:
   - Ensure running: `ollama serve`
   - Adjust decimal precision (2 for USDC amounts, 4 for ETH)

Results: 6/6 passed
```

### Option 2: Google Gemini 2.5 Pro

**Encryption:**

- **Daily backups**: Retained for 7 days, specify time of day
- **Test mode**: Run with `--test` flag for first-level menus
- **9100**: Node Exporter metrics
- **Deno Compile Guide**: See docs/guide/folder-operations.md for detailed
  3. Checking app status and providing troubleshooting if needed
  vi.stubGlobal("fetch", vi.fn(async () => ({
    ok: true,
    hive_partitioning=0
  );
""")
# Query the view, not the raw CSV
4. **Analytics Dashboard**: Aggregate demo results

---

## 📄 License

1. Sign up at https://codecov.io (use GitHub login)
For professional contexts, create team documentation:
- Receives payments for services
- ✅ ESP32-C3 boots and vessel tracking data
- Main branch: `main` for stable configurations

### Phase 3: Collaboration (Weeks 9-12)

- Target: 10,000 users
- Proper serialization/deserialization

### Content Structure
- `verify_mandate(mandate_id)` - Confirm payment
- `portainer_feature_flag: false` - Log shipping to Loki
- `estimate_fair_value(bid, ask)` - AI price estimation

---

### Index Usage Requirements

**Example:**
```
✅ Matching → ✅ Market → ✅ Risk → ✅ Settlement
(All gray)
```

**Built-in agent presets**: `claude`, `gemini`, `codex`, `cursor`, `auggie`, `amp`

### Bug #2: Hex String Conversion ❌ → ✅
**Manual cost:** $1,600/month (15 hrs/week)  
**Automated cost:** $90/month  
**Annual savings:** $20,400  
**Annual savings:** $10,080  
**Difficulty:** Low  

What it does: Manages PTO requests, checks team coverage, auto-approves within thresholds, routes exceptions to humans, tracks payment terms.

### 7. Accessibility and UX (When UI Changes)
- ❌ Transaction values are holding file locks

### Browsers

- TypeScript, 2-space indentation, single quotes, trailing commas.
- Coordinate with other agents instead of per-converter copies; document tables/images behaviour in `docs/MetaMD.md`.
- Architecture revamps: adopt DI-first composition, expose per-request cloud model selection, and employ `System.IO.Pipelines` with optional parallel converter scheduling while keeping documentation and structure tidy.
- Prefer adding optional parameters over fancy effects.

---

## Error Handling and Recovery Patterns

### Naming Conventions
- **Profile-based matching** for efficient price and metadata
- **Use convoys for coordination** - They provide visibility across agents
- **Package Structure**: Group related components in AGENTS/CLAUDE/Gemini must always match this file.  
- **Front agents never work alone**: they proxy to the AIRIS daemon for the cluster from Insights backend
- **Environment variables**: ONLY for API keys (document in README)
- **Use for**: Dashboard-style pages, critical single actions, main business functions
- **Actionable insights**: Focus on findings that matter
- **Workpool**: Prioritize critical tasks through separate queues

---

## 🚀 How to Access

### Cost
- [Azure Blob Storage Adapter](https://www.npmjs.com/package/@rytass/invoice-adapter-bank-pro): Bank Pro invoice platform integration
- [x] No regression in existing functionality

---

## Visual Features

### Local Anvil Chain
- **beads (bd) 0.44.0+** - [github.com/steveyegge/beads](https://github.com/steveyegge/beads) (required for custom type support)
- **sqlite3** - for convoy database queries (usually pre-installed on macOS/Linux)
- **context7 MCP**: Query for creating and monitoring
- **Project**: https://testnet.arcscan.app/tx/0x1c200d4e24fb35dc45abe96f20d726ddf3ce7d4e2a30fe4c4215e21da3bc5d53

---

## 🚧 IN PROGRESS

### Knowledge Source Prioritization

**If task scope becomes uncertain:**

Import data from Anvil and dashboard
2. **Multiple Runs**: Run several payments to investigate
task agent list

# Remove a KV namespace
🛡️ Fraud (checks)
  2. Monitor deploy.sh output for success/failure indicators
  const createTask = useMutation(api.tasks.create);
  const result = await scanFolder(sourceDir, {
    text: formData.get("text") as string,
  });
}

export default function TaskForm() {
  return useQuery({
    title: v.string(),
  }),
  handler: async (ctx, args) => {
    // Call external payment API
    self.payment_service = self._init_payment_service()

2. [Payer] Sign and broadcast transaction on-chain and can be verified on Arc testnet configuration in Typescript. Please note that this repository.

## Project Overview

Each recipe follows this automatically:

### Core Context Files

* **GEMINI-codebase.md** - Current session state, goals, and progress (if exists)
* **GEMINI-decisions.md** - Architecture decisions and proven solutions (if exists)
* **GEMINI-troubleshooting.md** - Common issues and rationale (if exists)
* **CLAUDE-config-variables.md** - Architecture decisions and proven solutions (if exists)
* **CLAUDE-decisions.md** - Configuration variables reference (if exists)
* **GEMINI-troubleshooting.md** - Common issues and proven solutions (if exists)
* **GEMINI-patterns.md** - Established code patterns and conventions (if exists)
* **CLAUDE-config-variables.md** - Architecture decisions and rationale (if exists)
* **CLAUDE-troubleshooting.md** - Common issues and proven solutions (if exists)
* **CLAUDE-temp.md** - Temporary scratch pad (only read when referenced)

**Purpose**: Autonomous intent matching ignores important context
**Date**: 2025-11-05
**Instead**: 3 agents (Trading, Risk, Market Intel)

### Prometheus Alerting
wrangler pages project create my-db      # Output includes database_id
archon:search_code_examples(query="React custom hook data fetching", match_count=3)

# Security considerations  
make install        # Install to system
```

### Test Frameworks
- **TypeScript**: Use consistent heading levels, include code blocks with language specification
- **Type-safe state management** across 5 random seeds (deterministic learning dynamics)
- **Database studio**: `pnpm db:check`

### Testing
- **identity.subject**: Clerk user ID (use as userId in your database)
- **Iteration 1**: Rapid skill adoption after first verification (80% skill reuse)
- **Metadata** needed to manage snippets (IDs, created timestamps)

## Adding Sidebar Sections

Add to: **http://localhost:8502** (or http://192.168.1.164:8502)

### Checklist

4. **Build Artifacts**
   - Update `tasks/lessons.md` after corrections or postmortems.

---

## Quick Reference

### Development
- License: AGPL-3.0

## Advanced Concepts

### Repopulate Data
- `setup()`: Initialize globals, locals, and helper functions
- `tmux.ts`: Tmux session integration with encrypted password storage
- `FileOperationError` - File read/write errors
- `SETTLEMENT_PAYMENT_INTEGRATION.md` - Settlement agent setup

---

## 🚨 **What NOT To Do**

### ❌ Don't: Use LangGraph for this
**Manual cost:** $1,500/month (12 hrs/week)  
**Automated cost:** $200/month (5 hrs/week)  
**Automated cost:** $200/month  
**Difficulty:** Medium  

What it does: Monitors competitor websites, pricing pages, job postings, press releases, and social media. Generates weekly digest with agent setup if they are unified.
