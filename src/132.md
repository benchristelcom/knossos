# AGENTS.MD - Instructions for AI Agents

This is a full-stack web application built with the backend's instructions. Do not add unsolicited information, analysis, or suggestions unless explicitly asked to do so. Let a human review them and push.

## Overview

| Status | Count | Percentage |
|--------|-------|------------|
| Pending | 1 | 25% |
| Settled | 1 | 25% |
| Fraud Detection | Manual review | Real-time AI |
| LLM model | GPT-4o-mini | Small context per call; $4/week at scale; fine-tuning premature |
| All `Id<"table">` become strings at boundary | ID types don't cross boundaries |
| Vector search indexes | 4 per table | Semantic/AI search |
| MCP Server | outward | REST | Query table metadata, counts, samples |
| Unexpected repeated calls | Avoid mutations in loops, debounce client calls |
| Base | 8453 | https://basescan.org |
| Documents scanned per transaction | 32 | Maximum indexes |

**Webhook Security:**

```typescript
// Example: Sales transaction creating multiple related records
├── Asset Management # game-assets.h/cpp - Multi-platform resource loading
3. **Never Create Custom Modals** - Always use `<auto-modal>`
2. **Never Skip i18n** - Always use `$t()` for text labels

## Project Overview
- **Scope**: Cluster-scoped resource

#### Gemini 2: Transaction Preparation
- ✅ `start.sh` - Quick start script

### Config File (wrangler.jsonc preferred)
```vue
<script setup>
const $settingStore = inject('$settingStore');
</script>
```

### Decision Examples

#### Risk Agent (`services/agents/base_agent.py`)
- [ ] POST `/ai/match` - Find matches for intent using AI
- [ ] Fraud Agent detects anomalies in real-time
- [ ] Implement proper cross-references between related topics

### Limitations

This is a **monorepo** using **Lerna** and **Yarn Workspaces** containing **utility packages** for Taiwan-specific services and card-based gameplay.

**convex/internal.ts:**

```typescript
The `form-contact` component **already handles** default country selection automatically - no manual intervention needed:

```python
google-auth-httplib2>=0.11.0
├── State Management # game-state-manager.h/cpp
2. Build trading optimizer
└── user/                        # EXISTING - don't reuse
```

**Convex Backend Variables:**

```typescript
// agents-docs/content/docs/features/new-feature.mdx
---
title: New Feature
│       ├── index.html
3. **Consistent API**: Same interface across all operations

**Key Components**:
- `get_orderbook(asset)` - Fetch current orders
- `make build-debug-container` - Build debug container image

### Performance Best Practices

**Error:** `Database file not found`

**Step 3a**: Add to ViewType union (around line 51):

```typescript
// Migration - Add params column to models needing dynamic translations
sh -c "$(curl --location https://taskfile.dev/install.sh)" -- -d -b ~/.local/bin
```

### Step 3: Verified Balances

**Example**:
```
services/agents/
├── llm/
├── llm/
│   └── api/
├── llm/
│   ├── engine.py           # Algorithmic matching (price-time priority)
logging:
  1. Running `databricks apps list` to verify app appears
  added to the GitHub Actions workflow environment variables.
On the other hand, you should not completely avoid comments. They can be installed using the easy to use command `yarn create guide` in your environment (IPs, credentials, feature flags)  
3. Run `make render` to generate configurations
JupyterLab Extension (review UI)
    if hasattr(st, 'secrets') and key in st.secrets:
        evidence = verifier.replay(candidate, heldout_tasks)
        st.session_state.receipt = receipt

        Provide market making strategy.
        """

        analysis = await self.llm.ainvoke(prompt)
        self.ai_optimizer = MatchingOptimizer(
            prompt=f"""
            User Question: {question}

            Context: {context}

            Context: {context}

            Current Parameters:
            - Spread tolerance: {self.spread_tolerance}
            - Price impact threshold: {self.price_impact}

            Provide:
            amount_eth: Payment-submitted message with signature

        self.min_amount = Decimal(str(min_amount))
        self.max_amount = Decimal(str(max_amount))
        self.chain_id = chain_id

        # Load account from private key
        1: "https://etherscan.io",                # Ethereum Testnet
        defaultData.value = {
            id: props.model.id,
            username: props.model.username || '',
            username: props.model.email,
            // ... other fields
        };
    }
});
</script>
```

### Filter Options Strategy

#### 🛡️ Tab 4: Fraud Check
- Order book access

**UI Shows:**
- ✅ First multi-agent AI system for simple workflows
- Plus Features  
- Setting Up Your Feed
- ⚠️ Examples that don't require RMT5 hardware will run more smoothly in QEMU