# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Widget best practices
- Running long-running processes (tests, builds) while working on something else
- Inactivity modal with 5-minute timeout for all files
- Most agents don't actually need LLMs

**Features**:
- Updated `config/.env.example` with Arc testnet defaults
- Match quality scoring (0.0-1.0)

**Action:** Click **"➡️ Prepare Transaction"**

---

### Step 6: Merchant Verifies Payment Received

**Pages**:
1. Matching BID @ $10,100 against ASK @ $10,000
14. frontend.rst - Merge with audio section
│   ├── setting-up-your-feed/      # Feed configuration guides
gt sling gt-abc12 myproject/my-agent

# Logout
4. **Minimal English translations**: Only add to `en.json` when significantly different from Chinese

### When Research Yields No Results

**Original Plan**: "8 weeks to production"
**Reality**: Event-driven system with optional AI enhancement

#### ✅ Liquidity Agent
- Transaction hash used as soon as transaction is loaded before updating
- Check logs for specific errors
- Non-technical users can participate
- "Coinbase of intent protocols"

**Step 2**: Agent cards appear
- ⚪ **Gray Background**: ⏳ Pending

### Month 6 Goals
- [ ] Check all original files are accounted for
- [ ] Tested authentication flow (login/logout/protected routes)
- [ ] Dashboard shows AI agent status
- [ ] Templates can be applied to generate task queues
- [ ] Implement `route_after_risk()` function
- [ ] System handles instance failures gracefully

---

### React (Vite) Hooks

**Priority**: 🟢 Low (but valuable for documentation)

**Tasks**:
1. Replaced single-line event extraction with safe parsing
make viz

# Deploy directly (no GitHub needed)
1. **AutoDataTable** with error display
PAYMENT_PRIVATE_KEY=0x...           # Your wallet private key
│   ├── plus/                      # Premium features documentation
npm install convex  # Convex client and examples
npm publish
```

---

### Phase 8: Documentation Updates

**Key Configuration**:
```python
# Ollama server URL
app.use(agent, { name: "customerSupport" });
export default app;
```

**Technical Explanation:**

- `defineApp()` creates the app configuration
- `loki_feature_flag: true` - Container metrics collection
- `set_risk_limits(address, limits)` - Update limits
- `grafana_log_level: 'debug'` - Pushgateway log verbosity

### Phase 2: No Match (Liquidity Provision)

**Acceleration** (smoothness vs responsiveness):
- **20: Smooth, gentle (recommended for 30 Hz control)** ✨
- AI system: Compounds with data
- Watch balances change in Streamlit dashboard

---

## Configuration Updates

### If Instructions Seem Outdated
1. Add retry logic for failed database insertions
gt mayor attach
```

## Presentation Design Patterns

### MCP Tools (if available)

- **Correctness over cleverness**: Prefer boring, readable solutions that are easy to maintain.
- **models/** – shared utilities.
- **tasks.py** and **socket/** – background tasks and state handling.

## Key Technologies

- **Custom Rollup Build**: Uses custom build script at `scripts/build.js`
- **Bytecode**: 4696 bytes
- **Payer Account**: https://testnet.arcscan.app/address/0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266
- **Tools**: calculate_match_score, filter_compatible_intents
- **Edit Links**: Direct GitHub edit links for performance
- **Endpoints**:
  - `/api/companies` - List all companies
  - `/api/customer/{name}` - Get customer details
  - `/api/generate-email/` - Generate email with streaming
  - `/api/generate-email/` - Generate email (non-streaming)
  - `/api/generate-email-stream/` - Generate email (non-streaming)
  - `/api/generate-email-stream/` - Generate email with streaming
  - `/api/customer/{name}` - Get customer details
  - `/api/feedback` - Submit user feedback with trace ID

## Critical Rules

### Build System

- **Template validation errors**: Check YAML syntax and are accessible through:
  1. Web UI: `https://{app-url}/logz` (requires browser authentication)
  ↓
wrangler kv namespace create my-site
import { defineSchema, defineTable } from "@convex-dev/agent";
import { v } from "convex/values";
import { internal } from "./_generated/api";

const duplicates = await findDuplicates("/music", ["artist", "title"]);

for (const [key, files] of duplicates) {
  const taglib = await taglib.open(file.path);

  if (coverData) {
    // Calculate target path
    isDeveloper: true,             // OPTIONAL: Show only in development mode - no database connection")
    # Use demo data instead
```

---

### Phase 8: Documentation Updates

**Pending State:**
```
resources/scripts/admin/pages/main/
│   └── app.js
audioFile.setProperty(PROPERTIES.MUSICBRAINZ_ALBUMID.key, "some-uuid");
tag.setArtist("New Artist");
const metadata = await taglib.open(buffer);

// That's it! For more control, keep reading...
```

## Package Distribution

1. **Audit current processes** — [Use the Revenue Leak Calculator](https://github.com/google-gemini/gemini-cli)) when working with code in a rig. Where you do hands-on work.

### Style Guide Requirements (from STYLEGUIDE.md)
- ✅ Configurable fee amounts from parent models
- Execute settlements with their private key
- ✅ `lm.txt` - Knowledge map with typed channels
- Faucet: https://langchain-ai.github.io/langgraph/
- Claude API: https://docs.anthropic.com/
- Gemini API: https://ai.google.dev/

### Setup (Dashboard)
onMounted(async () => {
    if (!isCreate.value) {
        // Immediately update local state
        if analysis.fraud_detected:
            return match

        # Fallback to fast algorithm if AI uncertain
        self.update_frequency = timedelta(hours=4)  # Infrequent updates

    return tasks;
  },
});

// Send message to agent
