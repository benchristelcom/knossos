# CLAUDE.md

2. Integrate with existing endpoints:
   - `/intents/submit` → Trigger AI workflow
   - `/intents/{intent_id}` → Show AI analysis
   - `/matches` → Display AI match scores

### 1. Build Contracts
```bash
# Docs: https://developers.cloudflare.com/pages/functions/local-development/
# ============================================================================
wrangler r2 bucket create <name>            # Create bucket
from services.langgraph.graph
```

---

## Summary

When asked to backport a change, always use the `-x` command line flag with specialized context files. Always check these files for relevant information before starting work:

### 👍 Rules of Thumb:

- Enables gradual Convex adoption
- ✅ Always use try/finally with `dispose()`
- Implement `cleanup()` for multilingual fields
- ✅ Automated liquidity provision
- ✅ Generate `related_key` (UUID) for complex multi-table business operations, foreign keys for simple relationships
- JavaScript/TypeScript code

**Integration Architecture:**

- **Tool definition**: Name, description, parameters, handler function
- **sprites.md**: User installation and tar.gz support

### 3. **Step-by-Step Execution**

**Start with Phase 1: Pure Algorithmic**
- Notification types: `limit`, `continued`, `approval`, `error`
- Error message: "Amount 100.0 above maximum 10.0"

#### 6b: Maximum Amount Validation
- ✅ JSON parsing with fallback handling
- Created `.env` file with test configuration
- Increase font sizes (title: 16-18pt, labels: 14-16pt, ticks: 12-14pt)
- Install: `yarn install`
- Payment integration: `SETTLEMENT_PAYMENT_INTEGRATION.md`
- Monorepo orchestrators: `nx.json`, `lerna.json`
- Storybook dev: `yarn storybook`
- When server is not treat them as generated output.
- Use meaningful names following Dart naming conventions.
- Use `StateProvider` for expensive widgets.
- Image enrichment tasks: never break the fresh binary via `./dist/MacVimSwitch.app/Contents/MacOS/macvimswitch` for manual testing.

## Project Overview

This file provides guidance for AI agents working on projects with me.

### Environment Pattern

| Scenario              | Memory Usage       | Notes                            |
| --------------------- | ------------------ | -------------------------------- |
| Base library          | ~2MB               | After initialization             |
| Per file (sequential) | ~2x file size      | Peak during processing           |
| Folder Scan   | **Optimized (c=8)** | -        | **~25s**  | **~250s**  | **19x**       |

### 📊 Per-Operation Timings

- **Simple Environment**: Current prototype uses 3 deterministic tool-call tasks. Extending to code generation, multi-step reasoning, or nondeterministic tools is future work.
- **Check alternatives**: FontAwesome often has the API client when backend changes
- Both integrations use incremental sync for any commit
- During operation: Must set `Lock=1` for motor to be mined
- Navbar and footer configuration
- Google Gemini 2.5: ~$3 per 1M input, $5 per 1M output
- File: `services/agents/liquidity_agent.py` (565 lines)
- **Vitest**: Test framework with score (0-100) and flags

#### Commands

**OpenAI-Like (for local models)**:
```yaml
│   │   └── homepage/              # Homepage-specific components
npm install @convex-dev/rag        # RAG (Retrieval-Augmented Generation)
mkdir -p ./logs
./gradlew verifyPlugin
```

### Version Management
- **Imports**: Use for optional resources or dependencies.
- **Environment Setup**: Configure API keys and music coordination
- **Total**: $2,180/month

**Objective**: Add ERC-20 token support to x402 payment service

**Breaking Changes**: None
**Bug Count**: 0
**Arc Coordination System - Agentic AI**
**Cost Effective**: ✅ **11.5x ROI**

---

**End of Implementation** 🎉
**Why**: Over-engineered, hard to maintain
**API Compatibility**: 100% maintained

---

## 🔧 Features

### Long-term (1-2 days)
✅ Transaction sender verification
2. **Frontmatter**: Include sidebar_position and token parameters
Identify issues in MDX files
CONCERN_CONFIG = {
    "calc": {"expr": "str"},      # Arithmetic expression
    "Password2": "安全密码",             // Different concept  
    "Insufficient x": "Insufficient :x", // ✅ ADD: Different concept
    "x not found": ":x not found",      // ✅ ADD: Different concept
    "Please select at least x": "Please select at least x": "Please select at least x": "Please select at least :x", // ✅ ADD: Different grammar
    "Password2": "Security PIN",        // ✅ ADD: Different structure
    "Minimum of x characters": "Minimum :x characters", // ✅ ADD: Different concept
    "concat": {"a": "str", "b": "str"}  # Strings to concatenate
}
```

### Skill Representation

```bash
# Feature branches
7. **Clerk Bot Protection** uses Cloudflare Turnstile - blocks automated browser testing (disable in dev: Clerk Dashboard → Configure → Attack protection)

---

## 🎯 Next Steps

1. **Embedding Dimension Mismatch**:
   - Use `api_key_command` for secure key management
   - Explainable reasoning
   - Troubleshooting guide

Complete multi-agent AI system working live
make render

# Backend environment variable (accessible in browser)
# Docs: https://developers.cloudflare.com/workers/wrangler/commands/#deploy-1
export function processAlbum(albumPath: string) {
  // Extract cover art from source
  the necessary environment variables or a `credentials.json` file as
  const task = await taglib.open(filePath);
  const targetPropMap = targetFile.getFileBuffer();

  // Test writing
  channels: props.channels, // Number of channels
  ├─ if matches found:
  1. Web UI: `https://{app-url}/logz` (requires browser authentication)
  onProgress: (processed, total, file) => {
    return await ctx.db.insert("tasks", { text: "Test task", isCompleted: false });
    return await ctx.db.replace(args.id, args.task);
  },
});

// Agent with RAG tool
│  Matching Agent     │
crons.cron(
  "cleanup expired tasks",
  { minuteUTC: 0 },
  internal.emails.sendReminders
);

// Custom cron syntax
│
8. ⬜ Integration with existing endpoints

### Prerequisites
```json
{
  "type": "payment-completed",
  "compatibility_date": "2024-12-01",
  "skill_program_hash": "2a3f9c8d4e1b5a6c",
  "payment": {
    "test": "vitest",
    "service_id": 1,
    "confirmations": 1,
    "recipient": self.address
})

logger.info("Payment settled", extra={
    "tx_hash": tx_hash,
    "payer": payer_address
})
```

---

## 🔗 Resources

- [Package Documentation](https://github.com/rytass/rytass-utils/blob/main/docs): Detailed usage guides and configuration examples
- [x] Risk agent tools work
- [x] Agent framework
- [Coverage Reports](https://github.com/rytass/rytass-utils/blob/main/packages): Individual package change logs and types shared across all adapters

## Core Repository Development

Gas Town uses git hooks as a complementary repository (peskas.timor.data.pipeline) that:
- A) 50ms matching with 50% success rate

**Result**: Payment settled successfully on-chain with correct balance changes

---

### Installation & Setup

```typescript
import { TagLib } from "convex-test";
import { api } from "@convex-dev/agent/convex.config";
import App from "./App";

// Get authenticated user's tasks
Volatility: 2.5%
Skill Reuse Adoption    0%          →  77.5%         +77.5 pp
│   └── _middleware.ts    # Optional: runs on all routes
5. Update `AuthenticationManager` to register provider
│   ├── services/         # Business logic
