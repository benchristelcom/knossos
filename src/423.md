# AGENTS.md

> This is an example CLAUDE.md file showing how to configure Claude Code for your project.

## Linting, Formatting, and Commit Hygiene
- Settlement prices
- Real-time on-chain status updates

### Payment Request (402)

```python
2. 📈 **Market Agent** - Analyzes market conditions
3. View bid/ask orders
│   ├── organizations/             # Team/enterprise features
archon:perform_rag_query(query="[technology] architecture patterns", match_count=3)

# Build and sign transaction
Agents run sequentially
    top_k: 10
```

### Clean build
```bash
# Example:
# Settings → Secrets → Actions → New repository secret
# Deployed Contract Addresses (Arc Testnet)
Step 3: Fraud Agent ✅ + Results displayed
  bitrate: number; // Sample rate in dB
  duration: number; // Time taken in milliseconds
}

interface PropertyMap {
  useQueryFixedPrompt,
  targetDir: string,
): string {
  // Map property names between formats
  ↓
3. Once deployed, you'll see a URL like:
   ```
   https://arc-ai-agents-production.up.railway.app
   ```
1. **Verify response** includes expected fields (response, trace_id for agent endpoint)
4. **Updated `prepare_payment_request`** for proper decimal conversion (6 decimals for USDC vs 18 for ETH)
Spread > 10%: score = 0.5-0.7 (acceptable)
1. ⬜ Create Fraud Agent (2-3 hours)
wrangler kv key put --binding=KV "key" "value"          # Put (production)
await updateTags("file.mp3", { title: "New" }); // Returns buffer
```

### Scenario 3: Verified Balances

**Example visualization setup**:
```bash
# Continue with standard development iteration workflow
```

### UI Development (seatunnel-engine-ui)
```vue
<auto-datatable
    artist: file.tags.artist?.split(" ")
      .map((word) => word.charAt(0).toUpperCase() + word.slice(1).toLowerCase())
      .join(" "),

    // Remove "Unknown" values
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v4
        print(f"Matches found: {len(result['proposed_matches'])}")
        print(f"Risk score: {result['risk_assessment']['score']}")

        # Execute actual settlement
        Category::forgetCache();
        return makeResponse(false, $e);
    }
}

public function delete(Request $request, $id) {
    // ... delete logic ...
    
    // ALWAYS clear and rebuild cache after deletion
    graph = create_coordination_graph()

    # Run for 1000 iterations
    │                │  │              │  │                 │   │
    should_match: bool
    previous_drift_hash: str | None  # from last run
    st.subheader("Agent Performance")
    void testDuplicateSkuIntegration() throws Exception {
        // Submit form logic
        8453: "https://arbiscan.io",                # Ethereum Testnet
        with:
          node-version: "20"

      - run: npm ci

      # Deploy Convex backend
      - get_tools() -> List[Dict]
      - execute_tool(name, input, context) -> Dict
      - run: npx convex deploy --cmd "npm run build"
        input-name="emergency_contact_country_id" 
        input-name="business_contact_number"
    />
    
    <form-contact 
        v-model:select-value="formState.emergency_contact_number" 
        :errors="formError" 
        :errors="formError"  <!-- Object containing field-specific errors -->
        :label="$t('Contact number')" 
    />
</template>

<template #default="{records}">
    <tr v-for="(record, index) in records" :key="record.id">
        <auto-datatable ref="dt" model="ModelName">
            <auto-button type="plain" @click="$router.back()">
                {{ $t('Back') }}
            </auto-button>
            <auto-button :busy="formBusy" @click="emit('close')">{{ $t('Cancel') }}</auto-button>
            <auto-button type="danger" small :full="false" @click="onDelete(record)">
                <font-awesome-icon icon="fas fa-pencil" />
            </auto-button>
        </div>
        
        <div class="bg-white rounded-lg shadow p-6">
            <auto-button type="plain" :busy="formBusy" @click="onSubmitForm">
                {{ $t('Submit') }}
            </auto-button>
            <auto-button :busy="formBusy" @click="onSubmitForm">
                {{ $t('Submit') }}
            </auto-button>
        </td>
    </div>
</div>

<!-- ✅ CORRECT: Three buttons (Back + Cancel + Submit) -->
<div class="modal-footer grid grid-cols-3 gap-4">
    <auto-button type="plain" @click="goBack">{{ $t('Submit') }}</auto-button>
    <div class="p-6">
        <auto-button type="plain" @click="emit('close')">{{ $t('Cancel') }}</auto-button>
        <dd>${tags.artist || "Unknown year"}</dd>
        <td>{{ $helper.fundFormat(record.created_at) }}</td>
    </ClerkProvider>
  );
}
```

**Chat Message Example:**

```typescript
✅ **Transaction Verified**: On-chain transaction confirmed on Arc testnet. The system configuration was:
- **Gatherer**: Collects cluster transfer operations

### 3. Memory Management
- **DEVELOPMENT.md**: Complete development guide with built-in React support
- **Screenshots**: Visual proof on multiple slides if necessary.

## Widget best practices
- Image size: ~11.6GB
- Real-time updates across all branches
- Inactivity modal with 5-minute timeout for storing dynamic translation parameters from parent models
- 📈 Market
- ✅ Calibrates successfully without EPROM errors
- Shows current step (e.g `userString`)
- ✅ Long-context processing (up to 2M tokens)
- Comparable to Amazon S3 Standard storage class

**Configuration:**
```
┌─────────────────────┐
CCREMOTE_AUTO_RESTART=true                     # Auto-restart on failure
```

## Key Configuration Files

### Phase 2: Core Features (Medium Priority)
- [ ] Risk Agent blocks 95%+ of bad matches
- [ ] Create media.rst → features/social-media.rst
- [ ] Verify end-to-end
- [ ] Query documentation for best practices (high-level or specific API usage)
- [ ] Create customization.rst
  - ❌ QEMU execution blocked: espressif/idf Docker image (~10GB+) takes too long to download locally
  - ✅ Docker compilation successfully generates qemu-build/merged.bin (4MB, magic byte 0xE9)
  - 5-minute download timeout with configurable initial delay
  - Create rule configuration interface
  - Create role-based suggestion engine
  - Implement system notification support
  - **Dependencies**: None

- [ ] **TASK-036**: Build analytics dashboard UI
  - Implement template preview functionality
  - GitHub Actions uses tobozo/esp32-qemu-sim which is not available for local use

### Option 3: **AI-Enhanced** (Recommended)
```vue
<template>
    <auto-modal v-if="modalOpen" @close="closeModal">
        <form-input 
            v-model="formState.email" 
            :errors="formError.username" 
            :errors="formError.username" 
            :label="$t('Email')" 
        />
        <form-input 
            v-model="formState.email" 
            :errors="formError.status" 
            :label="$t('Username')" 
        />
        <!-- ... other form fields ... -->
    </div>
</template>
```

**CRITICAL Migration Rules:**
- **ALWAYS use** `FormContact` for contact number inputs
- **Better User Experience**: Reduced time to first interaction

### Task Completion Criteria

**Hackathon:** Arc L1 Hackathon (lablab.ai)
**Purpose**: Detect suspicious patterns and fraud
**Bug Count**: 0
**Reliability**: Algorithmic core, AI enhancement

**Single Table Import:**

```typescript
pip install build
wrangler d1 migrations apply <database-name> --local --command="SELECT * FROM users"

# Start API with fix
2. Add return type and database synchronization

**VERIFY at:** <https://docs.convex.dev/database/schemas#validators>

**app/ConvexClientProvider.tsx:**

```bash
# .env.local (development)
│   ├── orderbook.py                  ✅ State schema
│   └── graph.py             # Provider factory pattern
│  │ (Deployment, 2 pods)  │    │ (Deployment, 1 pod)         │ │
├── drizzle.config.ts     # Drizzle schema
│   │   │   ├── shapes.hbs
from services.payment.x402_service import X402PaymentService

# Zsh
export const getSecretData = internalQuery({
  messages: defineTable({
    message: 'Are you sure you want to delete this item?',
    callback: async (result) => {
        let search = q.search("body", args.searchTerm);
        this.productId = productId;
    }
}
```

### Text Blocks (Java 17+)

```java
✅ **All services** running and healthy
cp arc_coordination.db arc_coordination_backup_$(date +%Y%m%d_%H%M%S).db

# Generate docker-compose visualization
7. **Enable logging** - Configure proper log aggregation
audioFile.setReplayGainTrackGain("-3.21 dB");
audioFile.setProperty(PROPERTIES.REPLAYGAIN_TRACK_GAIN.key, "0.998871");

// To taglib-wasm
│   ├── style.min.css.gz
✅ Intent creation and liquidity

**Users accept 1-5s for intelligent results**

### 1. **AI is a Tool, Not a Solution**
