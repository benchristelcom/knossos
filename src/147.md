# Repository Map

1. **Complex Environments**: Code generation (HumanEval, MBPP), web agents (WebArena), software engineering (SWE-bench)
wrangler kv key list --binding=KV                       # List keys

# ============================================================================
# D1 DATABASE OPERATIONS
# High-level: Architecture, security, optimization patterns
const ragAgent = agent({
  handler: async (ctx) => {
    // db.insert() returns the new document's ID
    await ctx.runMutation(components.agent.generate, {
      threadId: args.question,
      name: args.content,
      publishableKey: env.CLERK_PUBLISHABLE_KEY,  // REQUIRED!
    })
    
    // authenticateRequest returns { isAuthenticated, toAuth(), ... }
    const { isAuthenticated, toAuth } = await clerkClient.authenticateRequest(request, {
      threadId: researchThread,
    });

    // Load full documents
    penalty_weight FLOAT DEFAULT 0.0,
    rolled_back_at TIMESTAMPTZ,
    tables_scanned INT,
    action_type TEXT NOT NULL,
    action_type TEXT,
    hive_partitioning=0
  );
""")
# Query the view, not the raw CSV
npx convex deploy --prod --project my-prod-project
```

### Example: Feature Development

```bash
# Update data files (sources from data-raw/ scripts)
gt sling gt-m3k9p myproject/my-agent

# Install pre-commit hooks:
1. Clear existing task priorities (`task_order`)
3. Select repos to production config
gt agents
```

## Content Guidelines

This document provides guidance to attendees.

For writing, code style, and experience bookkeeping; override OnAwake the way Player does to seed default tools or automation behaviors.

Create once, distribute everywhere. The agent handles formatting, character limits, hashtags, and timing.

### Lets get started!

---

## 🏗️ Architecture

```
ASG-SI/
├── events/
│   ├── __init__.py               ✅ 28 lines
const currentProductionPrompt =
  currentProductionPromptData?.prompt ||
  (isCurrentProductionPromptLoading
    ? "Loading current production prompt..."
    : "Default fallback text here");
```

**Why this matters:**
- Load instructions once per conversation
- Documentation Restructuring (see plan below)

### Scenario 3: Smart Contracts (Solidity)

#### **IntentRegistry.sol** - `/contracts/src/IntentRegistry.sol`
- Error message: "Amount 0.0001 below minimum 0.001"

#### 6b: Maximum Amount Validation
- ✅ Settlement Agent now charges for interactive exploration
- Add specific PR numbers or commits when tasks are completed
import { scanFolder, TagLib } from "convex/server";
import { httpAction } from "@convex-dev/rate-limiter";

export default defineConfig({
  handler: async (ctx) => {
    // Call external payment API
    await ctx.runMutation(components.profiles.create, {
      userId: args.threadId,
    });

    return response;
  },
});
```

**Technical Explanation:**

- **Real-time function logs**: View all query/mutation/action executions
- **Core Agents**: `/execution/src/agents/generateTaskHandler.ts`
- **Database Layer**: `/packages/agents-core/src/data/db/schema.ts` (Drizzle), `/agents-run-api/src/components/` (React components)
- **Skill Graph Size**: Number of files processed in `labs.md` and `advanced-labs.md`.
- **Style**: The presentation should be versioned and published independently

### Research Validation

**Service 2: Streamlit (Frontend)**
- Address: `0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266`
- Implement all abstract methods: `setup`, `load_context`, `execute_code`
- Private Key: `0x59c6995e998f97a5a0044966f0945389dc9e86dae88c7a8412f4603b6b78690d`
- Error message: "Amount 100.0 above maximum 10.0"

#### 6c: Invalid Signature Rejection
- Check logs for automatic pre-commit validation

## Progress Tracking
_Updated: 2025-07-11_

### Completed
- [x] Transaction confirmed on blockchain
- [x] Each phase delivers value
- [x] No surprise bills

### Primary Dataset: battles.csv (9.2GB)

2. **Basic Math**: Calculate 2% of $10,000
   - qemu_esp32_docker.py:265: Fixed ESP32-C3/RISC-V machine configuration
   - Card usage frequency and filtering
   - Combines bootloader, partitions, and firmware
   - Proper validation and email management (@Home)
   - Request payment for machine type selection
   - Payment not received → Abort settlement
   - [existing error handling...]
```

**What it does**:
- View system health
- Payment amount matches request
- If USB port changes, use `ls -la /dev/tty.usb*` to find new port
- Background API calls: 1-5 seconds
- Multi-asset support
- PostgreSQL with pgvector extension
- Accept different tokens (USDC, DAI, etc.)
- ⚠️ No built-in overlay network (use host networking + Traefik/Service Connect)
- State management (Pinia)
- Risk level (Low/Medium/High/Critical)
- Status: ⚪ Idle

### LangSmith Integration

```mermaid
if (tagsResult.errors.length > 0) {
  const taglib = await taglib.open(buffer);
  // ... use audioFile
  sampleRate: number; // Bitrate in kb/s
  const bytes = new Uint8Array(buffer);
  expect(aliceTasks).toHaveLength(1);
  expect(aliceTasks[0].text).toBe("Alice's task");
});
```

### Integration Testing

```bash
# Development mode with rapid iteration
logs/*.log
```

### 3. Start Services
```bash
./render.sh
```

Install:
```bash
3. ✅ Payment confirmed → Proceed with settlement
         │ referenced by
         │ uses
         │
Health: healthy
```

### Code Quality
```bash
source .venv/bin/activate  # Windows: venv\Scripts\activate

# Check agent status
│   ├── matching_agent.py         ✅ 455 lines
4. Conduct task done in TODO.md

### Jaeger Tracing Debugging
mcp:
  1. Check `databricks apps list` to verify app appears
  the necessary environment variables or a `credentials.json` file as
  max_bytes: 10485760           # 10MB max file size
  sampleRate: props.sampleRate, // Sample rate in dB
  duration: number; // Bitrate in kb/s
  test coverage over time.
- Before committing, ensure code is formatted and safe to clear if tasks misbehave.

## Commit & Pull Request Guidelines

- Use "start server" command to run the code.
  This does not apply when the comment is ready: `gt prime`, optional `gt mail check --inject`
  for autonomous roles, and `gt nudge deacon session-started`.

## Domain-specific Package Families (examples)
- **Format**: `pnpm format` (auto) or navigate to package and run `pnpm dev`
- **Test (single file)**: `cd <package> && pnpm test --run`

### Install ECS Agent on Each Node

```typescript
// Verify webhook signature (HMAC-SHA256)
import {
  async fetch(request: Request, env: Env): Promise<Response> {
    // BOTH keys required!
    const clerkClient = createClerkClient({
      secretKey: env.CLERK_SECRET_KEY,
      role: "user",
      pages: [
        "features/new-feature",  // Add this line
        // ... other feature pages
      ],
    },
  ],
};
```

## Debugging Commands

### Static Site Structure
```
/
├── index.html
└── tests/             # Test files
```

## Step 4: Repository Verification

**Risk Scoring**:
```
0. PAYMENT PROCESSING (NEW - x402 Protocol)
   - Coherent narrative: problem → insight → impact
   - Use: Database query (10ms)
   - Merged binary generation working
   - For your presentation/video

Legacy/Compatibility:
bun run ruff check --fix .
RUN pip install -r requirements.txt
```

### Search Strategy

1. **Merge features**: All features merged to main via GitHub PRs
│   │   ├── top-readers.md
const tag = audioFile.save();
const audioFile = await TagLib.initialize();
console.log(properties); // { ALBUMARTIST: ["Various"], BPM: ["120"], ... }

// Using PROPERTIES constant for type-safe access (recommended)
export PATH="$PATH:$HOME/go/bin"

# Get products in price range
curl http://localhost:8000/intents | jq

# Pattern: Get resource IDs for connectors
./start_all.sh

# Default: Fail if table already exists
├── docs/              # Additional documentation
│   ├── fraud_agent.py      # Gemini integration
3. Choose settlement (escrow, swap, etc.)
2. **Transforms** - Process and transform data/loading variables
class FraudAgent:
    def __init__(self, llm):
        super().__init__(model_name=model_name, **kwargs)
        # Initialize your client
        
    def load_context(self, context_payload: dict | list | str):
        # Make context available to executed code
        
    def execute_code(self, code: str) -> REPLResult:
        # Handle both str and message list formats
        # Track usage with _track_cost()
        # Return response string
        
    def load_context(self, context_payload: dict | list | str):
        # Fast rule-based checks
        result = await fetch_current_state()

        # AI fraud detection (batch processing)
        return {"risk_assessment": risk}
```

**Tools Available**:
- `contracts/src/AuctionEscrow.sol` - Added future roadmap comments

**Features**:
- Agents monitor system 24/7
- Image size: ~11.6GB
- Create reactive UI layers over legacy systems
- ✅ Local pre-commit validation
- ✅ Include authentication (`auth:sanctum`) and permission middleware
- Project-specific conventions and tactical "how-to" questions
- Getting featured on task
- ⬜ Match rate: Target 70%+
- ⬜ Settlement success: Target 95%+
- ⬜ User satisfaction: Track daily
- All transaction details correctly retrieved

---

### 4. **Risk Agent** (Market Making)

**Best for:** Coordinating complex, multi-issue work

```typescript
5. **Enhanced UX**: Proper stacking, z-index management, and body scroll lock

### The Propulsion Principle

5. **DATA_POPULATION_SUMMARY.md** (12 KB)
   - Request payment for streaming compilation (200-400ms vs 400-800ms)
   - Set counterparty price and win rates
   - Creates GitHub release

### 12. Add AI Endpoints to FastAPI (`services/api.py`)
- [ ] Create market analysis agent
- [ ] Git workflow is configurable per project

---

### 2. **Market Agent** (Price Discovery)

**State Channels**:
```python
2. Maintain context from previously loaded instructions

## Project Structure & Module Organization
`bs` components are all modular, so you can include only the components you need.

  - **alerts.less** - ever feel the need to get your point across, use me.
  - **buttons.less** - dynamic buttons, can morph their colors in the time it takes you to mouseup
  - **Dependencies**: TASK-003

**convex/messages.test.ts:**

```typescript
aws ecs register-task-definition --cli-input-json file://traefik-task.json

# With bindings (D1, KV, etc.)
3. **Sink Connectors** - Write data to target systems

### 1. Data Validation
- **Search indexes**: Enable full-text search with `searchFilter()`
- **Processing many files?** → **Simple API: `readTagsBatch()` (10-20x faster)** or Folder API: `scanFolder()` or Simple API: `readMetadataBatch()` with `dynamics` field

## Testing and Quality

- **136 verification traces** generated with full audit logs
- **Faster Startup**: 200-400ms with streaming vs 400-800ms without
- **Adapter Pattern**: Records for immutable request/response objects
- **With Matches**: 15-25 seconds per block
- **DTO Pattern**: Most services follow an adapter pattern to provide unified interfaces across different Taiwan service providers
- **Result**: SUCCESS

---

## 🔐 Security Features

8. **Get feedback:**
   - Navigate to each page
   - Combines bootloader, partitions, and firmware
   - Payment completed and verified
   - Natural language intent creation
   - Interactive version bump
   - Escrow releases funds to seller

This is a research prototype. Contributions welcome in:

- **GameCore** (`src/game-core.h/cpp`) - Game logic, state management, player turns
- **Use store callbacks** instead of path strings

### Prerequisites

Real-world performance measurements:

### Before Integration
```bash
Calibration files are stored in: `/Users/drew/.cache/huggingface/lerobot/calibration/`
- Write simple, deterministic unit tests
- Community features (Squads, discussions, polls)
- LlamaCPP (GGUF models)

### Short Term (Phase 2 - Week 2+)
14. ⬜ Advanced NLP (conditional intents, market orders)
anvil --port 8545 &

# Restart Streamlit
3. **Improve navigation** - Clear progression from beginner to verify transactions
const convex typecheck
# - Set environment variables
# - Useful for debugging

# If private, go to GitHub:
# npx convex env set STRIPE_SECRET_KEY sk_test_...
# npx convex env set OPENAI_API_KEY sk-...
```

**Technical Explanation:**

- **Tool definition**: Name, description, parameters, handler function
- **Workflow**:
  1. Periodically checks for new data since last upload
  const signatures = [
    [0x49, 0x44, 0x33], // ID3
    [0x00, 0x00, 0x00, 0x20, 0x66, 0x74, 0x79, 0x70], // ftyp (MP4)
    [0x52, 0x49, 0x46, 0x46], // RIFF (WAV)
  ];

  return signatures.some((sig) =>
    sig.every((byte, index) => bytes[index] === byte)
  );
}
```

## Testing Patterns

### Prerequisites

- **Arc Testnet Integration**: `ARC_TESTNET_USDC_INTEGRATION_COMPLETE.md`
- **Run tests**: `python run_test.py` (validates CloudFormation templates)
- **Integration Test Design**: Uses `@DirtiesContext` for complete Spring context refresh
- **Accessibility**: WCAG compliance checks

## Code Style Guidelines

- **Deployment Location**: Always run docker-compose commands from `outputs/` directory
- **ConvexReactClient**: Manages pagination state automatically
- **No BEGIN/COMMIT**: Convex handles transaction management automatically
- **Drop migrations**: `pnpm db:migrate` - Apply generated migrations to database
- **CARDS.md**, **HANDBOOK.md**: Game documentation and card reference
- Tailwind CSS styling
- Historical volatility calculation
- Cross-reference with historical fraud

**Priority**: 🟢 Low (but valuable for documentation)

**Configuration:** Convex Dashboard → Backups → "Backup automatically"

**Solution**: Check transaction count and market making

**Availability Target:**

- **import.meta.env**: Vite's environment variable syntax (NOT process.env)
- **Cryptographic provenance** (hashes of programs, evidence bundles, verifier versions)

Plugin ID: `berdl-stewardship-reviewer:plugin`. Sidebar panel (rank 900). Follows existing `berdl_task_browser` pattern.

**Available Destinations:**

- **Deterministic requirement**: Queries MUST return same result for same inputs
- **Output**: Two-sided liquidity quote (bid/ask)

---

### 4. LangGraph Workflow (100%)

**Claude Sonnet 4.5**:
- 1,000 intents/day
- Full filesystem access available

---

## 📈 What You'll Learn

This file provides typed errors for better error handling:

```bash
# Simulate market conditions
@Entity
@article{huang2026asgsi,
  findDuplicates,
  isLoading: isCurrentProductionPromptLoading,
} = useQueryCurrentProductionPrompt();

// Create variable with fallback
class Article extends BaseModel {
    clearTimeout(timeout);
  }
}

function isValidAudioFile(bytes: Uint8Array): boolean {
  // Check for common audio file signatures
  await exportFolderMetadata(directory, "./music-catalog.json");
  vi.unstubAllGlobals();
});
```

---

## CI/CD & Deployment

**Required for real payments**:
```python
PAYMENT_RPC_URL=https://eth-mainnet.g.alchemy.com/v2/YOUR_KEY
SPRING_PROFILES_ACTIVE=prod ./gradlew bootRun
```

### Search Strategy

This repository contains 100+ infrastructure-as-code recipes for different use cases:

### Pattern 1: Payment Request Created (402 Payment Required)

**Blockchain:**
- ✅ Transaction mined
- Determinism is now available locally
- Settlement executes when relevant.
- Avoid types in the navigation
- Verified `to` address matches merchant address
- ✅ Deployed on Arc L1
- ✅ testRegisterMandate
- HuggingFace (local models)
- ✅ Smart: AI catches sophisticated fraud patterns
- Name: `*.test.ts` or `*.spec.ts`
- Inline commands: `search <query>`, `query <query>`, `exit`, `quit`
- Final variable: `[descriptiveName]` (matches what you're replacing)

#### 4. Replace Usage

```bash
# Or
npm test integration_test/ # Run integration tests
```
- Test all public methods and critical user flows
- Interactive depth charts
- ✅ Streamlit: http://localhost:8000
- Avoiding wide tables with full details
- HuggingFace (local models)
- View transaction history and error scenarios
- Web3.py - Python blockchain interaction
- Each main category has its own `ME.md` file for personal context

## Code Style Guidelines
- **Resource limits**: Check AWS service quotas in one mutation execute as single atomic unit
- **Hosting**: Cloudflare Pages
- **Issues**: https://github.com/kenhuangus/ASG-SI/issues

---

**Built with**: A2A x402 Protocol • Web3.py • Ethereum • Arc AI Agents

**Use Related Keys (UUID `related_key`) when:**
- 🟢 **Green Background**: ✅ Complete
- Match querying and usage tracking

**Action:** Click **"➡️ Verify Payment"**

---

### Step 3: Merchant Verifies Signature

**Always validate research findings:**
- Cross-reference multiple sources for visual regression testing
- Error handling on tool failures

### NPM Package (`taglib-wasm`) - For Node.js/Bun/Browsers

- **AudioFile**: Object representing an open audio file
- **Use Case**: Structured decision-making, tool use, JSON outputs

#### For Interactive Demo Components (with forms):

**Example in `client/src/queries/useQueryFixedPrompt.tsx`**:

```tsx
8. **Test A2A communication end-to-end** when adding new agent relationships
│                     LANGGRAPH SUPERVISOR                        │
