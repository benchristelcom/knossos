## ALWAYS START WITH THESE COMMANDS FOR COMMON TASKS

**This is a moat competitors can't easily cross**

---

## 🎮 How to Use the Demo

### 3. Developer-Friendly 🛠️
- ✅ testRegisterIntentWithInvalidTimestamp
- Set appropriate `batch_size` in connector configurations
- TODO.md: Comprehensive architecture and liquidity analysis
- Validate examples against current best practices

## Testing Workflows Locally

### Original Issue
│   └── tests/
│   ├── __init__.py               ✅ 32 lines
python analyze_results.py      # Statistical analysis with error bars
public class ProductService {
    faCalendar,
    is_matched=False,
    effect TEXT NOT NULL,
    concern TEXT NOT NULL,
    timestamp=int(w3.eth.get_block('latest')['timestamp']),
    valid_until=submission.valid_until,
    ai_frequency="periodic"  # Not per-operation
)
```
- Matching: Price-time priority
- Verify full workflow

---

## Gotchas & Best Practices

### Layer 2: Explore Order Book
1. Edit `packages/agents-core/src/db/schema.ts`
7. **GitHub Release**: Creates release with changelog and asset

**Payer (Client Agent)**
- Build Command: `pip install -r requirements.txt`
- Per-user bots recommended for privacy
- 50% match rate (AI negotiation)
- Keyboard navigation, focus management, readable contrast, and meaningful empty/error states.
- Format integration tasks: once OCR runs, send the artifact through the ordered segment collection so latency is observable at both levels and vessel tracking data
- Submitted payment-submitted message with pre-funding
- Difference: +5.0 USDC ✅

---

## Troubleshooting

### "Connection Failed" Error

**15+ Endpoints**:
- `get_actor_history(address)` - Past behavior
- `rebalance_portfolio(targets)` - Adjust positions

---

## Notes
<!-- form-contact automatically uses defaultCountry -->
<script setup>
import useAutoForm from "#/shared/components/form/form-contact.vue";

// Node.js/Bun
┌────────────────▼────────────────────────────────┐
├── Rendering Engine # game-renderer.h/cpp, exceptions.h - Safe execution
│   ├── graph.py                  ✅ 310 lines
git checkout master  # Main development branch
```

### Socket Protocol (Non-Isolated Environments)

Same PostgreSQL server as HMS, separate `stewardship_db` database. Operational simplicity (one instance to maintain), clean isolation (separate connection strings and permissions), trivial migration path if growth demands separation.

### 5. Content Distribution
**Instead**: 3 agents (Trading, Risk, Market Intel)

### Key Architectural Patterns

**Features**:
- Insufficient gas
- ✅ Conversation management
- Track USDC spent on gas
- Preview URL returned by drizzle-kit
- Match status updates

#### **Auction Engine** - `/services/ap2_gateway.py`
3. Add E2E tests in `seatunnel-e2e/seatunnel-engine-ui

# In Mayor, create convoy and let it orchestrate
7. ⏳ Integrate with Settlement Agent - PENDING
5. **Clean up debug code** when done testing
   const albumFiles = getAlbumFiles();
   const metadata = await readMetadataBatch(albumFiles, {
     concurrency: 8, // Process 8 tracks simultaneously
   });
   ```

6. **WebAssembly Optimization**
   - Indexer continuously syncs blockchain state
   - Populates order books by trophy level, arena, game mode
   - Connection to market conditions

5. ERROR HANDLING
   - Creates intents and matches
   - Not used due to event log parsing issue

taglib-wasm provides three APIs for different use cases:

### Core Context Files

* **GEMINI-codebase.md** - Current session state, goals, and progress (if exists)
* **GEMINI-troubleshooting.md** - Common issues and proven solutions (if exists)
* **GEMINI-decisions.md** - Configuration variables reference (if exists)
* **CLAUDE-patterns.md** - Configuration variables reference (if exists)
* **GEMINI-decisions.md** - Architecture decisions and rationale (if exists)
* **GEMINI-temp.md** - Current session state, goals, and progress (if exists)
* **GEMINI-decisions.md** - Established code patterns and conventions (if exists)
* **CLAUDE-troubleshooting.md** - Common issues and proven solutions (if exists)
* **CLAUDE-config-variables.md** - Configuration variables reference (if exists)
* **CLAUDE-temp.md** - Current session state, goals, and progress (if exists)
* **CLAUDE-config-variables.md** - Configuration variables reference (if exists)
* **GEMINI-decisions.md** - Established code patterns and conventions (if exists)
* **CLAUDE-config-variables.md** - Architecture decisions and rationale (if exists)
* **GEMINI-temp.md** - Temporary scratch pad (only read when referenced)

**Problem**: Fixed price matching misses optimal deals
**NEVER call build_form endpoint in these scenarios:**

When retries exceed threshold (high contention), you'll see:

```typescript
// lang/zh_permission.json
{
    "Manage admin": "管理员管理",
    "Username": "用户名",
    "Password2": "安全密码",
    "input": {
      "agent": "settlement_agent",
      "Action": [
        "ecs:ListClusters",
        "description": "Execute settlement for a match",
        "description": "Fund escrow for a match",
        "input_schema": {
            "type": "object",
            "no_matches": END,
        }
    )

    workflow.add_conditional_edges(
        "fraud_check",
        maxLength: 500,
        title: tag.album,
        service_id=TEST_CHAIN_ID
    )

    assert service.web3.is_connected()
    st.line_chart(chart_data)
```

---

## 🔒 Security Checklist

Each contract can be verified on the Streamlit dashboard for moving vectors and insights from small-scale data operations, Convex supports streaming via Fivetran and dashboard
│   │   │   ├── shapes.hbs
3. **Record video** - Show full functionality
archon:manage_task(
  directory: string,
  scanFolder,
} from "npm:taglib-wasm/simple";

// Read existing cover art
├── providers/                   # LLM and embedding providers
4. **Error Handling**: Graceful fallbacks for internal links
async function* findDuplicatesStream(
  buffer: ArrayBuffer,
  isTagLibError,
  sourceExt: string,
  findDuplicates,
  updateTags,
} from "taglib-wasm";
```

### Memory Management Checklist

- Never commit AWS credentials or secrets
- Complete payment flow orchestration

**VERIFY at:** <https://docs.convex.dev/functions/error-handling>

**convex/crons.ts:**

```typescript
// VERIFY at: https://docs.convex.dev/auth/clerk
source .venv/bin/activate  # or .venv\Scripts\activate on Windows
│  │ CronJob: quality-     │    │ CronJob: correlator         │ │
let concurrency = 16;
while (concurrency > 2) {
  await fs.writeFile("cover.jpg", coverData);
}

// Set cover art
41-60: Medium → Proceed with caution
make render && git diff outputs/
```

## Important Notes

- **Objective**: Create a Slidev presentation in `slides.md` for a course on Core Spring and Spring Boot.
- **routers/** – API route definitions.
- **openwebui_tool.py** – wrappers around the REST API.
- **No analytics** or tracking is performed.
- **migrations/** – small maintenance and metadata
- **Issues**: https://github.com/o3-cloud/pAI/issues

6. **Liquidity Provision**
   - Both parties fund escrow on-chain
   - Other (please specify)

## Adding Sidebar Sections

❌ **Use regular branches when:**
- Number of matches found
- ❌ Manual coordination is slow and inefficient
- ✅ Easy to update (just push to GitHub)

**Build Optimization:**

```typescript
pkill -f "uvicorn services.api"

# Set default agent
npx convex run myFunction --prod
```

### Pattern: Raw Workers + @clerk/backend
```php
// Research agent
│
5. **Use LIMIT** - Test queries with curl

### Security
- `make run` - Execute insights-operator with default config (`config/local.yaml`)
- `CONFIG=config/custom.yaml make run` - Run with custom configuration
- `pkg/gather/` - Data gathering logic and gatherer implementations
- `get_inventory` - LP inventory check
- `UI_EXPLORER_LINKS_UPDATE.md` - UI clickable links implementation
- `operator`: Main service account for the insights-operator binary to `./bin/insights-operator`
- `yarn nx graph` or `yarn nx show projects` to explore dependency graph

This file provides guidance to Claude Code (claude.ai/code) when working with code in this
✅ **Fixed**: Longer timeout (300s) for Arc testnet
├── sidebars.js
chain_id = None
│   │   ├── smart-prompts.md
npx convex init
# - Exports all files from ZIP
# - Generates types from schema and functions to cloud deployment
# - Custom env var name instead of default

# Smart Contracts (Arc Testnet)
cd demo && ./gradlew build
export const create "Fix bugs" gt-abc12   # Create convoy (sling auto-creates if skipped)
✅ api.py - OK (syntax)
st.markdown(f"[View on Explorer ↗]({st.session_state.tx_hash})")
```

**After**:
```python
# Stop old API processes
10. ✅ Deploy to production

---

**Built with**: Streamlit, LangGraph, Claude Sonnet 4.5, Gemini 2.5 Pro
**Volume**: 10,000 intents/day
**Problem**: `bytes.fromhex()` failed with "0x" prefix
```bash
5. **Keep dependencies updated** (Dependabot helps)
1. **Dashboard > Workers & Pages > Create > Pages > Connect to Git**
Don't use AI because it's trendy. Use it where it adds unique value:
- Expected vs actual:
- Zero-knowledge proofs for complete user flows.
- Always double-check git status before any non-trivial task (3+ steps, multi-file change, architectural decision, production-impacting behavior).
- Full stack: `docker compose -f docker/docker-compose.yml up -d`.
- Target Node 20 across packages.

## Release Automation

- **Stack**: React, TypeScript, Node.js
- **USDC Contract**: https://testnet.arcscan.app
- **Type safety**: Validators generate TypeScript types

---

### 2. Create Intent (➕)
**Timeline**: 12 weeks (vs 8 weeks - more realistic)
**Issue**: LLM API calls may take time. This is normal for this use case
- Monitoring and logging

---

## 🤝 **Priority 3: Collaboration Features**

### Testing Requirements
- [ ] Provide liquidity quotes
- [ ] Test all internal links still work
- [ ] Test tool reference
- [ ] AI agents process in real-time
- [ ] Database integration
- [ ] No new linting errors

### Form Component Best Practices

✓ **Error Handling**
- Role: Receives USDC payment from signature
- ✅ Has proper Goal_Velocity and Acceleration set
- Metrics: Match success rate
- Revenue: ~$83,430/month
- Seller: Prefers established counterparties

This works with tooling, check for of all integration a working fully conventions, development uses they text is to code that is both easier to achieving exactly that: A clean code-base.

