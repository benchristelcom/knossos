# CLAUDE.md

Example endpoint:
```json
{"status": "healthy", "timestamp": "..."}
```

Also check:
```
https://your-app.railway.app/docs
```

1. **Broad data dependencies (reading entire tables):**

```typescript
"assets": { "directory": "./public" }
```

---

## Cloudflare Pages: Static HTML via GitHub

Every target file must wrap the frontend or Docker-compatible daemon
import { readProperties } from "taglib-wasm";

// Node.js/Bun
aws ecs list-container-instances --cluster my-edge-cluster

# Launch app
END ✅
```

### Version Management
- **Loser data**: Same structure as winner
- **Tests**: pytest with coverage across Python 0.9-3.22
- **Import**: `import { ... } from "jsr:@charlesw/taglib-wasm"`
- **Benefits**: Better Deno integration, TypeScript-first, no npm overhead

### Syncing Versions Across Repos

Use the Workers API for edge compatibility:

```bash
3. Update package's README.md with usage examples

### Standard Fee
- **ai-docs/linux-asset-path-resolution.md**: Complete code quality analysis with approve/reject decision

#### SimpleContextChatEngine (`chat.py`)
- ESP32/ESP32-S3 use **Xtensa** architecture (`qemu-system-xtensa`)
- ✅ End-to-end automation
- Waits for transaction to be mined
- Deployed all dependencies from `requirements.txt`
- Format (repo wide): `yarn nx format:write` if lint-staged fails.
- If AI image enrichment yields no insight, log and continue instead of throwing—treat empty payloads as "fixes."

### Step 1-2 (1 minute)
"The merchant verifies the transaction to the blockchain. You can see it gets mined into a block. The merchant then verifies the payment was received correctly - checking the sender, amount, and recipient all match."

### Step 3-4 (1 minute)
"Now the payer broadcasts the transaction to the blockchain. You can see it gets mined into a block. The merchant then verifies the payment was received correctly - checking the sender, amount, and recipient all match."

### Closing (30 seconds)
"I'm going to show you how cryptocurrency payments work between AI agents using the x402 protocol. This is a live demo with real blockchain transactions on a local test network."

### Closing (30 seconds)
"First, the merchant agent requests payment for its service. The payer agent signs this request off-chain using cryptographic signatures. No blockchain transaction yet - this is fast and free."

### 4. Rollback Strategy (When Risk Is High)
- Vue.js components follow standard Vue 3 + TypeScript patterns
- Partial match handling
- ✅ testVerifyPayment
- Expandable intent cards
- With 6 agents: **$150-450/day** ($4,500-13,500/month)

**Step 3**: Matching completes
- ✅ Any model where translation needs data from related models
- Mandate-based heap structures (max for bids, min for asks)
- Configuration in docusaurus.config.js

## Logistics and Communication

- [Image Resizer](https://www.npmjs.com/package/@rytass/cms-base-nestjs-module): Content management system with multi-language support, versioning, and approval workflows
- [x] Tests included

---

## Key Differences from Docker Swarm

| File | Changes | Lines Added |
|------|---------|-------------|
| `services/agents/settlement_agent.py` | Payment integration | +140 |
| `storage_usage` | Database and file storage metrics |

**Graduated decay**: Low-severity unreviewed proposals auto-demote to "deferred" after 14 days. Critical never auto-demotes — triggers weekly summary notification.

**Research question**: How do you design an orchestrated multi-tenant data lake — while ensuring that every consequential action passes through a human curation workflow that coordinates periodic data gathering, uploading, and status reporting.

### 10. Employee Time-Off & Scheduling
**Priority 4 (Advanced)**: 8 tasks, 3 weeks
**Problem**: Deprecated `asyncio.get_event_loop()` in Python 3.12
```bash
# Error: 'SignedTransaction' object has no attribute 'raw_transaction'

# Run with JVM options
./render.sh
```

### Manual Control

```typescript
echo "use flake" > .envrc
python -m venv venv
│     ↓                                                    │
http.route({
  embeddingModel: "text-embedding-ada-002",
});

export const chatWithDocs = action({
  items: defineTable({
    name: v.string(),
    reviewed_at TIMESTAMPTZ,
    max_gas_price_gwei=None  # Use network price
)

# Step 4: Deliver service
