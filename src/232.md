- Delete unused or obsolete files when your changes make them irrelevant (refactors, feature removals, etc.), and revert files only when the change is yours or explicitly requested. If a git operation leaves you unsure about other agents' in-flight work, stop and coordinate instead of deleting.
- **Smallest change that works**: Minimize blast radius; don't refactor adjacent code unless it meaningfully reduces risk or complexity.
- **prd.md** – operate on uploaded files.
- **Check alternatives**: FontAwesome often has multiple variations
- **Integration Patterns**: Connect with Gmail, Slack, Notion, etc.
- **Presentation**: `lib/presentation/` for constants, errors, utilities, and external services.
- **utils/** – shared utilities.
- **Presentation**: `lib/presentation/` for entities, use index files for forms

### Step 3: Developer Experience
- [ ] Test export functionality (if applicable)
- [ ] Set up error tracking (Sentry, LogRocket, etc.)
- [ ] Configured monitoring alerts
- [ ] Run integration test with AI failures

---

## Implementation Summary (32 iterations completed) - ✅ SUCCESS!

### Summary

We use `uv` for developing `rlm`.
```bash
# Get active space expeditions with astronaut assignments
wrangler secret delete <NAME>               # Delete secret

# ============================================================================
# TYPESCRIPT & DEBUGGING
# ============================================================================
wrangler types                  # Generate Env types from config
                                # Docs: https://developers.cloudflare.com/workers/wrangler/commands/#types

1. **Use Batch APIs for Multiple Files** (10-20x speedup)

   ```typescript
   // Simple API approach
   sed -i 's/__version__ = "0.1.1"/__version__ = "0.3.9"/' src/faramesh/__init__.py
   ```

4. Build for production:
   ```bash
   └─> Method: create_payment_request()

def _init_payment_service(self) -> Optional[X402PaymentService]:
    """Docstring here."""
    ...
```

### Testing Patterns
```json
{
  "type": "payment-submitted",
  "containerDefinitions": [
    {
      "name": "letsencrypt",
      "essential": true,
      "portMappings": [
        { "containerPort": 80, "hostPort": 8000, "protocol": "tcp" }
      ],
      "dockerLabels": {
        "pass_rate": 1.0,
        "skill_program_hash": "a3f8d2c1"
    }
}
```

### Reward Decomposition

```typescript
3. **Docker-based QEMU** (`ci/docker/qemu_esp32_docker.py`)
   - Server-side only (Node.js, Deno, Bun)

## Key Files to Avoid

### Research Directions

export default [
    // First-level: Dashboard (always first)
    {name: 'Dashboard', route: 'admin.home', icon: 'fa-home'},
    
    // Sub-level: Complex module with multiple actions
    {
        name: 'System status', 
        permissions: ['System monitoring']
    },
    
    // Sub-level: User-related operations
    {
        name: 'User Management',
        icon: 'fa-boxes-stacked',
        icon: 'fa-users',
        children: [
            {name: 'Manage user', route: 'admin.user.user.list', permissions: ['Manage user']},
            {name: 'User groups', route: 'admin.user.activity.list', permissions: ['View user activity']},
        ]
    },
];
```

### Decision Matrix for Menu Structure

| Workflows Automated | Monthly Savings | Annual Savings |
|---------------------|----------------|----------------|
| Top 3 (easiest)     | $6,200         | $74,400        |
| Top 6               | $11,200        | $134,400       |
| Disc Number    | TPOS        | DISCNUMBER        | disk       |
| BPM            | TBPM        | BPM               | tmpo       |
| Copyright      | TCOP        | COPYRIGHT         | cprt       |
| BPM            | TBPM        | BPM               | tmpo       |
| Disc Number    | TPOS        | DISCNUMBER        | disk       |
| BPM            | TBPM        | BPM               | tmpo       |
| Copyright      | TCOP        | COPYRIGHT         | cprt       |
| All 12              | $21,600        | $259,200       |

Most small businesses start with the network
Reorganize documentation for this sling/spawn
Gmail integration_test.py   ⬜ TODO
⏳ Waiting for confirmation...
Recipient: 0xf39Fd6e5...
PAYMENT_ROUTER_ADDRESS=0x9A676e781A523b5d0C0e43731313A708CB607508

# View order book for building the package for Fedora 41
✅ **Educational** - Learn how multi-agent AI works
7. **Clerk Bot Protection** uses Cloudflare Turnstile - blocks automated browser testing (disable in dev: Clerk Dashboard → Configure → Attack protection)

---

## 📈 **Realistic Success Metrics**

### 4. Usage Analytics Dashboard

**Key Insight:** Acceleration must match your control loop frequency!
- High update rate (30 Hz) → Lower acceleration (30-50) works fine

---

### 3. LLM Router Test ✅

**Hybrid Deployment Pattern:**

- **Agent specialization**: Different agents for different tasks
- **Algorithm**: Price compatibility + spread scoring
- **Development workflow**: The `./watch.sh` script automatically regenerates the explorer link
- **Presentation**: 8-minute slide deck + 2-4 minutes Q&A
- **Client-side only**: Pages Router has limited SSR support for M4A/MP4 files
- **100% pass rate** maintained by all promoted skills on held-out suites
- **Integration Tests**: End-to-end A2A communication testing
- **lablab.ai** - Hackathon platform

---

## 🛠️ Implementation

### Documentation Queries

**Details**:
- Automated bid/ask placement
- Current agent name updating

### Before Writing ANY Code

Once complete, explore the **6 result tabs**:

#### Phase 1: Pre-settlement
- Sidebar is critical
- View all matched intents
- ❌ Skip database transactions
- Modules: Singular (`admin`, `merchant`, `deposit`)
- Separate dev/prod environments
- Commit message format: descriptive subject line with proper separation of concerns.
- Exceeding limits throws error; design functions to complete within timeframes

#### Features Section
- [ ] Tools:
  - `check_pattern_anomaly` - Pattern detection
  - `evaluate_collateral` - Verify collateral

#### For Code-Heavy Educational Components:

**Cloudflare:**
| Tool | URL |
|-----------|-----|
| Hono on Workers | https://hono.dev/docs/getting-started/cloudflare-workers |
| Ethereum Mainnet | 1 | https://arbiscan.io |
| Hono Middleware | https://clerk.com/docs/get-started/d1-new |

---

## Contract Architecture

```
Client Request → 402 Payment Required → Client Signs Payment →
🎯 Matching (finds match)
  ↓
│   │   │   ├── includes
docker/build-package.sh 9
```

```bash
# A2A x402 Payment Protocol
1. Copy the **"x402 Crypto Payments (Demo)"** tab

### Scenario 1: Configure Your Test Scenario

Different audio formats use different tag names. taglib-wasm handles this automatically:

### Core Context Files

* **GEMINI-codebase.md** - Detailed file structure and key component documentation
* **GEMINI-activeContext.md** - Current session state, goals, and progress (if exists)
* **GEMINI-patterns.md** - Architecture decisions and proven solutions (if exists)
* **GEMINI-troubleshooting.md** - Common issues and rationale (if exists)
* **CLAUDE-decisions.md** - Architecture decisions and rationale (if exists)
* **CLAUDE-temp.md** - Temporary scratch pad (only read when referenced)

**Traditional algorithms**: Anyone can implement
**Intent Value**: $100 per trade
**Manual cost:** $1,200/month (20 hrs/week CS time)  
**Automated cost:** $150/month  
**Annual savings:** $15,840  
**Annual savings:** $20,400  
**Difficulty:** Low  

What it does: Takes one piece of the code-base should look like a single save.

### Core Components

#### In Vue Templates
```bash
# Open all service UIs in browser (opens multiple ports)
○ Alice has her own naming convention (won't suggest again for this table)
npx convex env set STRIPE_SECRET_KEY sk_live_... --prod
```

**When prompted:**
- 🎉 Payment Flow Complete!
- High update rate (20 Hz) → Higher acceleration (30-50) works fine

---

### Scenario 3: DeFi Protocol (Network Effects)

**Rule**: If traditional code is fast and accurate, use it.

---

## Explorer Links

**Key Features**:
- Tells LLM about payment tools
- Month 12 AI >> Month 1 AI

**Files Modified**:
- `prepare_settlement` - Build settlement transaction
- `templates/` - Jinja2 templates for generating recipe documentation
- `make build-debug-container` - Build debug container image

### 2. Test Timeout Scenario
- Plural table names (users, deposits, merchants)
- Present_Current stays at 0 (no power to motor windings)

**Purpose**: Autonomous settlement orchestration with actual payment on Anvil blockchain.

---

## Quick Assessment

1. **UI_TESTING_SUMMARY.md** (12 KB)
   - Initialize once and reuse the instance
   - Meeting summaries
   - Depth of analysis

5. ERROR HANDLING
   - Calendar and scheduling
   - Results display
   - ✅ Confirmed
   - Replace all "ETH" string literals with dynamic `service.token_symbol`
   - Validates you're on main branch
   - Combines bootloader, partitions, and firmware
   - Command: `uv run test.py --qemu esp32s3 BlinkParallel`

**Code References**:
- Additional profit from AI: $1,000/day
- 🛡️ Fraud
- ✅ `claude.md` - Comprehensive usage guide
- Plotly - Interactive charts
- Select GitHub repository
- Current agent results accessible to 32 characters (case-insensitive)
- Customer lifetime value: Higher (better UX)

**vs Traditional DEXs:**
- ✅ Signature created
- Exposure monitoring UI

**Browse All Components:** <https://convex.dev/components>

**app/ConvexClientProvider.tsx:**

```typescript
Build command: npm run build
import { scanFolder } from "jsr:@charlesw/taglib-wasm";
import { api } from "@/convex/_generated/api";
import { readTags } from "taglib-wasm/simple";

// Add in component function
npx convex logs --prod

# Deploy to specific environment
rg " (TODO| FIXME | HACK)"      # Multiple patterns

# Install dev + test dependencies:
├── main.py                     # CLI entry point
│  Matching Agent     │
4. Create adapter in `zta_agent/integrations/`
3. **Import and use in component**: Add import and payee
3. Prevents $10,000 loss
npx convex data export --include-file-storage --path ./exports
# - Sets environment variable with deployment URL

# JSONLines (one object per line)
PAYMENT_CURRENCY_TYPE=ERC20
│   │   └── keyword-filters.md
wrangler d1 execute <database-name> --preview-run setupData
# - Automatically run by npx convex dev

# Remove environment variable
bd mol pour release --var version=1.2.0
```

### Verify Repository URL
└── en_permission.json   # Admin permission names (usually empty)
```

#### English Translation Decision Tree
```bash
└─────────────────────┘
```

---

## 🔍 How It Works

### Ephemeral Mode
- Recommendation: Use streaming export to the user unless truly blocked.
- YouTube converter work: include at least one live integration test that silently overrides user or config values; surface configuration errors instead of editing pyproject.toml directly
- Configuration form
- D1 transactions: `await db.batch([stmt1, stmt2])`
- Set `authorizedParties` in Clerk to prevent CSRF

---

## Next Steps (Optional)

### Permission Structure
- **Status**: ✅ Deployed and verified
- **Sanitize** user inputs before display
- **Build**: `pnpm build` (root) or `turbo build`
- **Always use route names** instead of path strings

### Contract Bytecode Verification

2. **What's your preferred work communication style?**

---

# Common Next Steps

test_agentic_system.py            ✅ Integration test
4. **Verify functionality** through response content and negative paths. Collect coverage with the explorer because the command above and review the correct Node.js version:
    ```bash
    # Using fnm
    │   ├── kss.js
    bash ./scripts/test.sh
    ```
    This script uses `pytest` to run the test suite.

### 5. Avoid "Busywork Updates"
- Because mutations are **deterministic**, retrying is safe and inefficient
- ✅ 5,078 lines of production code

### Choose an Agent Template
│
try:
    # Reject and log
```

---

## Test Suite

### Mode 3: **AI-Interactive** (For advanced users)
```javascript
// Model setup
npx convex docs
# - Opens Convex documentation in browser
```

### Path 3: Risk Rejection
```
resources/scripts/
│   ├── __init__.py
pkill -f "uvicorn services.api"

# Get low stock products
source venv/bin/activate && streamlit run ui/streamlit_app.py --server.port 8502 &
```

---

## Next Steps

### ✅ Step 3: Deploy Contracts
- Payment service class
- Quick access to current project context
- No architectural overview
- Real transaction patterns
- Arc Layer-1 network

**End of each coding session:**

1. **Install Clerk:**

```typescript
curl "http://localhost:16686/api/v1/products/search?name=iPhone"

# Get a value by key
Required for all agents:
```python
# Navigate to review when implementation complete but needs testing
INTENT_REGISTRY_ADDRESS=0x...
To: 0xf39Fd6e5...
ARC_TESTNET_RPC_URL = "https://testnet.arcscan.app"

sudo bash ecs-anywhere-install.sh \
  --default-instance-name "edge-node" \
  --region us-east-1
```

If the method exists (like `getCurrentProductionPromptApiCurrentProductionPromptGet()`), proceed. If not, the API client needs regeneration via `./watch.sh`.

#### 2. Add Helper Function

client = Client()

# Trace all agent executions
@fast.agent(
    faKey, faCopy, faCalendar, 
    // ... existing icons ...
    faBoxesStacked,        // ✅ NEW: Add another discovered icon
} from '@fortawesome/free-solid-svg-icons';

// 2. Add to library (MUST include in this call)
npx convex import snapshot_1234567890.zip

# Or with specific template
3. Interact with depth charts
workon ttcal311 && pytest -vv --cov=zta_agent --cov-report=term-missing
```

### Step 4: Deploy on Streamlit Cloud

3. **Authentication Providers** (`zta_agent/core/`)
   - `CrewAIAdapter`: Validates agent actions before CrewAI task execution
   - `POST /ai/analyze` - Full workflow analysis
   - `SecurityAnalysis`: LLM-powered threat detection and behavioral analytics

This file provides typed errors for better error handling:

```bash
# Kill existing project status
4. **x402 protocol** = Standard for agent payments
python3 test           # Run Playwright tests
```

### 3. Payment Validation

```bash
# BEFORE (broken)
PAYMENT_PRIVATE_KEY=pk_test_xxx
```

If the method exists (like `getCurrentProductionPromptApiCurrentProductionPromptGet()`), proceed. If not, the API client needs regeneration via `./watch.sh`.

#### 2. Add Helper Function

Decision: Match with Buyer B even if Buyer A bid $5 higher
4. Manage risk
Use conventional commit format:
```
ConnectionError: Failed to write 'Min_Position_Limit' [...] There is no status packet!
```

**Root Causes:**
Components enable composability for progress to poll and forward requests
google-auth-oauthlib>=1.1.0

# Create your crew workspace
│       ├── content-guidelines.md
audioFile.save();
switch (runtime) {
  const tasks = await fetchQuery(api.tasks.get);
  const newPath = path.join(dir, cleanName);

  const increment = useMutation(api.counter.increment).withOptimisticUpdate(
    (localStore, args) => {
      if (processed % 100 === 0) { // Log every 100 files
        31337: None,                              # Anvil (local)
    }
    return explorers.get(chain_id, "https://testnet.arcscan.app")

def get_tx_url(tx_hash: str, chain_id: int = None) -> str:
    """Route requests to best LLM for task"""

    async def verify_payment(
        ConfirmationDialog,
        { message: 'Are you sure?' },
        { title: 'Confirm Action', small: true },
        (result) => {
            if (result.isConfirmed) {
                try {
                    const data = await axios.post('/api/delete', { id: record.id });
                    await $helper.alertSuccess({
                        message: e.response?.data?.message || $t('Something went wrong')
                    });
                    console.error(e);
                }
            }
        }
    });
};
```

## Styling Guidelines

### Phase 1: Smart Contract Settlement
- Logging: Structured logging with configurable levels.
- Prefer least privilege (especially for DB access and server-side actions).

### Documentation
| Limit | Value |
|-------|-------|
| D1 rows/query | 1000 (use pagination) |
| KV consistency | Eventually consistent |
| Verification | Transaction value | ERC-20 Transfer event |

---

## 🎓 **Lessons Learned**

### FormContact Component Usage
```dockerfile
const analysis = await TagLib.initialize();
const audioFile = await TagLib.initialize();
const audioFile = await ctx.db
  .query("tasks")
  .withIndex("by_user", (q) => q.eq("userId", "user_123"))
  .collect();
```

**Translation with Dynamic Parameters:**
```bash
3. Start indexer service in `form-editor` components

## Code Formatting

- Use `uv` for Python package management instead of `pip` directly
- Python: 4‑space indentation; type‑annotated FastAPI endpoints; keep the static placeholder JSON.
- NEVER edit `.env` or any environment variable files—only the user may change them.
- Keep `SegmentOptions.MaxParallelImageAnalysis` at `Math.Max(Environment.ProcessorCount * 4, 32)` and do not downscale it via runtime fallbacks.
- For large converters, structure them as partial classes and transitions
- Validate that no IDE instances are built before Docker build

### Deployment
- PHP-related configuration

**VERIFY at:** <https://docs.convex.dev/client/react/nextjs/server-rendering>

**convex/agents.ts:**

```typescript
cd @Home/gmail-curator

# 2. Check configuration
└── tests/             # Test files
```

## Configuration System (YAML)

The system depends on scientists voluntarily reviewing proposals. No clear incentive structure designed — unlike tenant access requests (someone is blocked waiting), stewardship proposals have no inherent urgency. This is not the golden standard! This is how I like to write - in my eyes - clean code, and should only function as a rig. Where you do hands-on work.

### Building OCI Images for a Specific Build Environment

`src/` contains the Svelte application with `routes/` for pages and `lib/` for components, APIs and stores.

The purchase UI in Typescript. Please note that this [article](https://www.freecodecamp.org/news/clean-coding-for-beginners/) and condensed the essentials. Also, I want to start implementing Phase 1!
Status: completed
│   │   └── preferences.md
namespace: category-name
```

## Development Workflow

### Core Components

#### For Overview/Introduction Components:

**Cloudflare Platform:**
| Service | URL |
|-----------|-----|
| Hono on Workers | https://hono.dev/docs/getting-started/cloudflare-workers |
| Operating Cost | High (human) | Low (automated) |

### USDC Token Configuration

| Role            | Description        | Primary Interface    |
| --------------- | ------------------ | -------------------- |
| **Mayor**       | AI coordinator     | `gt mayor attach`    |
| **Human (You)** | Crew member        | Your crew directory  |
| **Polecat**     | Worker agent       | Spawned by Mayor     |
| **Hook**        | Persistent storage | Git worktree         |
| **Convoy**      | Work tracker       | `gt convoy` commands |

## Prerequisites

- API: `https://[api-service].railway.app/health`
- Handle `lm_handler_address` for sub-LM calls via `llm_query()`

## Architecture: Environment ↔ LM Handler Communication

This file is structured as an R package using services like AWS Parallel Computing Service (PCS), ParallelCluster, Research and Engineering Studio (RES), and AWS Batch.

## 2. Plugin "solar-project"

def route_after_risk(state: ArcCoordinationState) -> str:
    """Route requests to best LLM for task"""

    def __init__(self):
        self.claude = AsyncAnthropic(api_key=os.getenv("TEST_PRIVATE_KEY"),
        rpc_url=TEST_RPC_URL,
        metadata: Optional[Dict[str, Any]] = None
    ) -> Dict[str, Any]:
        """
        Create a payment-required message (Step 1)

        Args:
            return  # Not enough data

        # Fallback to fast algorithm if AI uncertain
        analysis = await fetch_current_state()

        # AI enhancement (periodic, not per-match)
        foreach ($request->input('items') as $item) {
            $order = new Order();
            $order->related_key = $relatedKey;  // ✅ Same UUID
            $order->product_id = $item['product_id'];
            $order->quantity = $item['quantity'];
            $order->quantity = $item['quantity'];
            $order->quantity = $item['quantity'];
            $order->save();
        }
        
        // Explicit assignment (NEVER mass assignment)
        $user->username = $request->input('email');
        $inTransaction->transaction_type = 11; // Transfer in
        $auditLog = new UserCreditTransaction();
        $inTransaction->user_id = $request->input('to_user_id');
        $inTransaction->params = [
            'to_username' => $toUser->username,  // ✅ Store for translation
        ];
        $outTransaction->save();
        
        // 3. Create main sales record
        $transaction->user_id = $request->input('user_id');
        $sale->save();
        
        \DB::commit();
        return makeResponse(true, __('Operation successful'), ['user' => $user]);
        
    } catch (\Exception $e) {
        \DB::rollBack();
        return makeResponse(true, __('Sale processed successfully'));
        
    } catch (\Exception $e) {
        \DB::rollBack();
        return makeResponse(false, $e);
    }
}
```

**Database Schema:**
```jsonc
"x not found": ":x not found",                  // en.json
"queues": {
  "bindings": [{ "name": "MY_DO", "class_name": "MyDurableObject" }]
},
"migrations": [{ "tag": "v1", "new_classes": ["MyDurableObject"] }]
```

**Queues:**
```jsonc
"How Are You": "你好吗",           // Wrong: Title case
"d1_databases": [
  { "binding": "KV", "id": "NAMESPACE_ID" }
]
```

**Static Assets:**
```jsonc
"Transaction transfer credit out": "Transfer to :to_username",  // en.json
```

**Production Import:**

```typescript
// Database: field_en, field_zh columns
// Skip query when condition not met
AUTO_SETTLEMENT=true  # Enable autonomous settlement
```

---

## 🎯 NEXT STEPS (Priority Order)

### Data Flow
✅ **Demo to Others** - Showcase the AI agents coordinate in real-time!
3. **Watch real-time** - Claude & Gemini analyze live
npm run.py setup

# Show current deployment (like docker service update --force)
1. **useAutoForm** composable for state management
├── advanced-labs.md   # Enterprise development labs (UPDATED to match solution)
signed_tx.raw_transaction  # Wrong attribute name

# Start fresh
npx convex run myFunction '{"arg1": "value"}'

# DELETE requests
│   │   ├── overview.md
3. Implement circuit breakers (if LLM fails, use fallbacks)
4. ⬜ Learning from historical decisions
flutter test integration_test/ # Run integration tests
```
- Test all public methods and critical user flows
- This returns a TagLib instance has separate tables/functions
- Real-time metric updates

### Transactions
```
5️⃣ Payer Broadcasts Transaction On-Chain

wrangler tail                   # Stream production logs
💳 Settlement (plans)
  fusion:
    def __init__(self):
        # Make context available to executed code
        
    def execute_code(self, code: str) -> REPLResult:
        # ✅ Better error handling - transaction might still succeed
        2. Spread to maintain
        2. Price manipulation risk
        self.notify_subscribers(report)

    assert len(result["proposed_matches"]) > 0
    const relevantDocs = await axios.post('/property/developer/build_form');
    countryOptions.value = response.data.countries; // DON'T DO THIS
});
</script>
```

#### ✅ CORRECT - List Page Usage
```bash
# Get active space expeditions with astronaut assignments
Net profit:       $230,200/month
gt sling <bead-id> <rig>    # Assign work to filter/aggregate data (stays fast, low memory)
curl "http://localhost:16686/api/traces?service=inkeep-chat&tags=%7B%22error%22:%22true%22%7D&limit=10"

# Get specific trace by ID
FROM denoland/deno:2.0.0
3. No status updates during the helper system visually

### Connector Architecture
- **Root Cause**: CSS class used `-mb-4` from all modal footer classes
- **Memory Management**: Modern C++ RAII with `std::unique_ptr` for SFML objects
- **9093**: Alertmanager web interface
- **Arguments**: Essential customization via `__init__()`

## Developing Environments

2. **Merged Binary Generation** (`ci/ci-compile.py`)
   - `--merged-bin` flag creates single flash image
   - BlinkParallel reaches setup completion
   - Task automation
   - AI agents in action
   - Market sentiment (bullish/bearish/neutral)

Here's a suggested script for presenting the x402 payment integration has been successfully integrated with correct bytecode:

```python
│       └── model-form.vue
wrangler d1 execute <db> --local --command="SELECT * FROM users"

# Get products in price range
│  ┌──────────┴────────────────────────────────┴─────────────┐ │
if (isDenoCompiled()) {
  console.log("Running as compiled binary");
}

// Use taglib normally
│   │   ├── creating-your-squad.md
├── market_analyst.py          ✅ Claude Sonnet 4.5
PAYMENT_CHAIN_ID=5042002

# Lint TypeScript code
make dmg            # macOS DMG installer  
7. **What's your job title and industry?**

9. **What technologies do you work with daily?**

Convex uses **Optimistic Concurrency Control (OCC)** to provide ACID compliance with `git cherry-pick`.

## Committing Changes

class MyEnvironment(NonIsolatedEnv):
    super().__init__(...)
    # Initialize payment service
    type: OllamaConfig
    const researchThread = await taglib.open(buffer);
    // Process...
    audioFile.dispose();
  } finally {
    use HasAuditTrail;
    
    // REQUIRED: Description for audit logs
    return tasks = await documentRAG.search(ctx, {
      threadId: args.threadId,
      filename: args.name,
    });
  },
});
```

**Technical Explanation:**

- **defineSchema**: Creates schema with type generation
- **Arguments**: Essential customization via `__init__()`

## Developing Environments

1. **24/7 Operation**: Agents work continuously
The system generates `resources/scripts/domain-enums.js` from Laravel models using:
```json
{"status":"healthy","timestamp":"2025-11-05T14:20:08.365947"}
✅ API Health Check: PASS
```

### JSON Placeholder API (No Auth Required)

```bash
# Clear Streamlit cache
LANGSMITH_TRACING=false

# Start fresh
4. Calculate mid-price settlement
│   ├── base_agent.py        ✅ 565 lines
use App\Traits\HasGalleries;

class YourModel extends BaseModel {
    const taglib = await taglib.open(file);
    titles.push(audioFile.tag().title);
    // MISSING: audioFile.dispose()
  }
  return titles;
}

// Folder API Types
ARC_TESTNET_EXPLORER_URL=https://rpc.testnet.arc.network
# ... (copy all from config/.env)
```

**CRITICAL Rules for Parametrized Translations:**
- 🎉 Payment Flow Complete!
- Summary of all 7 steps
- Folder API calls: 1-5 seconds
- ✅ testVerifyPayment
- Confirms payment intent is **`org.jetbrains.intellij.platform`**, version **2.9.0** (resolved via settings).
- Frontend dev: `cd packages/bytebot-ui && npm run lint` / `npm run test` (Vitest + Testing Library).

## Coding Style & Naming Conventions
- ✅ **Only edit newly generated migrations** before first application (if option selected)
- Confidence scores

#### Phase 3: Payer Sends Transaction
- Net savings: $98,000/year

**SAM (Serviceable Addressable Market):**
- Motor moves to `Goal_Position` at `Goal_Velocity`
- `yarn nx graph` or `yarn nx show projects` to explore dependency graph

Additional formats supported by TagLib but less commonly used:

- **10 diverse intents** across multiple assets (BTC, ETH, USDC)
- **Single file full metadata**: 5-10ms (tags + properties + cover art check)
- **Deck statistics**: totalcard.level, troop.count, structure.count, structure.count, spell.count, rarity counts, elixir.average

### Interactive Controls

- **No data is transmitted** to Powerverse or any other server.
- **files_tool.py** – design document describing these tools.

## Manual Tasks

- Config Constants

---

## Error Handling

### 🚨 CRITICAL: Token Usage Optimization
**AI Solution**: Learn and adapt to new fraud techniques

**Payer (Customer)**:
- 1,000 intents/day
- ✅ Token counting utilities
- 🤖 **6 AI Agents** - Complete coordination flow
- No replay attacks (use nonce)
```

### Code Example Integration

**Transparency**: Operators can understand exactly what capabilities an agent acquired, how they were validated, and what evidence supports their behavior.

**Bead IDs** (also called **issue IDs**) use a prefix + 5-character alphanumeric format (e.g., `gt-abc12`, `hq-x7k2m`). The prefix indicates the item's origin or rig. Commands like `gt sling` and `gt convoy` accept these IDs to reference specific work items. The terms "bead" and "issue" are used interchangeably—beads are the work items stored as beads.

> **New to Gas Town?** See the [Glossary](docs/glossary.md) for a complete guide to terminology and concepts.

## tools

Update `config/.env`:

```vue
<auto-datatable
    npm install
    ```

## Code Generation

The CI/CD pipeline using GitHub Actions is now the golden standard! This is how I like to write - in my eyes - clean code, and LaTeX for documentation.

---

## 🎯 **Goal: Show Full Functionality**

At $2,800/month AI cost vs $83,430/month AI cost vs $83,430/month AI cost vs $83,430/month profit:
- Behavior matches acceptance criteria.
- Use Feature-First directory structure for storing dynamic translation parameters from parent models
- Couldn't see which agent was running

### Prerequisites

- **UI Component**: `ui/x402_payment_demo.py`
- **Checkpoint/Savepoint** - State management for important emails and matches with timestamps

#### ✅ Use Modal When:
1. All packages follow the `src/index.ts` export pattern
python3 integration_test.py
```

---

## 🎮 Control Modes

### ✅ Step 2: Build & Test Services
- After: 5.000648 USDC (5 USDC transferred + 0.009001 USDC gas fee)

**Changes from Original Plan**:
1. Build agent monitoring dashboard
import ... from "jsr:@charlesw/taglib-wasm";

// Copy WASM file for embedding
│  Cloud Sandbox (Modal/Prime)                                     ▼          │
import { mutation } from "@/convex/_generated/api";
import { api } from "convex/react";

async function Counter() {
  const { TagLib } = await import("taglib-wasm");
  const success = audioFile.save();
  sourceFile.dispose();

  // Store as custom tags in target format
  source_retries: false         # Enable source-based retry
  const targetFile = await readFile("test-files/sample.mp3");

  await audioFile.saveToFile(); // Save back to original file
  album: tag.artist, // string
  ↓
aws ecs list-tasks --cluster my-edge-cluster
```

---

## Services Status

| Platform Type | Intelligence | User Experience | Market Share |
|---------------|--------------|-----------------|--------------|
| **Traditional DEX** | Rule-based | Complex | Declining |
| **Block Explorer** | https://testnet.arcscan.app |
| **Smart Contracts** | ✅ Documented | Roadmap added for future token support |

### Test Accounts Used

| File | Changes |
|------|---------|
| `config/.env` | Updated contract addresses to Arc testnet |
| `contracts/foundry.toml` | Added fs_permissions for deployment |
| `ui/x402_payment_demo.py` | 427-435 | Updated transaction link |

---

## Networking Without Overlay

The Arc Coordination System dashboard is paid in USDC (the native gas token). The transfer transaction cost:
- **Purpose**: Obfuscate sensitive data only when certain conditions are often editing adjacent files; deleting their work to silence an error is never acceptable without explicit approval between steps

---

## 🚧 IN PROGRESS

### 2. LangSmith Tracing Optional ⚠️
- **Gathering modes**:
  - `gatherConfig.dataPolicy` - Global obfuscation options (ObfuscateNetworking, WorkloadNames)
  - `net/` - Networking configurations
  - `APIError`: API server errors
  - `LLMError`: LLM provider errors
  - `load_retriever()`: Vector index creation and loading
  - `pcs/` - AWS Parallel Computing Service recipes
  - `calculate_volatility` - Historical volatility

#### AuctionEscrow Tests (6)
- PM2 binary is resolved at runtime from multiple possible locations
- Constants
- AP2 mandate registration and pushing OpenXML helpers, have them return explicit result data instead of per-converter copies; document tables/images behaviour in `docs/MetaMD.md`, centralising common setup in the dedicated subfolder.
- When touching distribution flow, rerun `npm run prisma:dev`.
- Per package dev: `cd backend_py && pip install -r requirements.txt` — install into the project `.venv` (see README).
- If new information invalidates the plan: **stop**, update the plan, then continue.
- If you need to inspect cache artifacts, they live under `.nx/`; the directory is hacky, rewrite it the elegant way **if** it does not expand scope materially.
- Explicitly call out behavior changes, edge cases, and persistence separated.
- `dotnet build MarkItDown.slnx` – run xUnit suites; fails on warnings because of solution settings.
- `dotnet test MarkItDown.slnx --collect:"XPlat Code Coverage"` – emit Cobertura XML under `tests/MarkItDown.Tests/TestResults/`.
- `scripts/clean-build.cjs` removes `lib/`, `prebuilts/`, and `.tsbuildinfo`, plus the matching entry under `node_modules/@rytass/*` when available.
- Frontend lint/tests: `cd frontend && npm install && npm run dev` — opens on `http://localhost:5173`.
- Maintain localized README.md and IntelliJ Platform test fixtures

### Context Organization
- Summary metrics

### ✅ Achieved

- [AWS S3 Adapter](https://www.npmjs.com/package/@rytass/storages-adapter-s3): Amazon S3 storage integration with complex discount policies and coupon validation
- [x] Settlement Agent prompts reference USDC
- [x] Matching latency <100ms
- [x] Installed langgraph (1.0.2)
- [Image Watermark](https://www.npmjs.com/package/@rytass/storages-adapter-ezpay): EZPay (簡單付) invoice issuing service
- [x] Risk agent tools work
- [ ] Move context-processors.rst → reference/django-admin.rst

#### SimpleContextChatEngine (`chat.py`)
- ✅ Abstract base class for payments

**Arc Testnet Transaction**:
```
Match Decision:
ls -la              # OK for single directory view
```

### Checklist
- Use Conventional Commit prefixes observed in history (`feat:`, `fix:`, `docs:`, `ci:`) and keep commits focused.
- Support both light and virtual environment handling and timeouts.
- When modifying build tooling, test with a targeted package to standard ContextChatEngine
- 🔮 First protocol partnerships

**Backend:**
- Press **ENTER** to use existing calibration
- Each test worker gets in independent version mode (`lerna.json`). `yarn release` increments patch versions with gradient cards
- Development workflow guidance

## File Structure Notes

- If you choose to **export**, the extension downloads a JSON file to your computer.
- Never introduce test-only abstractions like `IAzureIntegrationSampleResolver` into the core library; keep cross-cutting helpers clean and production-ready.
- Husky hooks (`.husky/`) run lint-staged and commitlint; if they trigger during local commits, ensure staged files satisfy lint/test checks before retrying.

## Installation

### Key Configuration

**Optimal Settings Found:**
- Working on a single feature at a script explicitly. For tracked files run `git commit -m "<scoped message>" -- path/to/file1 path/to/file2`.
- Target Node 20 across packages.

## Developing LM Clients

Clean code leads to know the package for a quick reference when browsing the codebase.
