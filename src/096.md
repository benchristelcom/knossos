## ALWAYS START WITH THESE COMMANDS FOR COMMON TASKS

**Available Folders:**
- `todo` → `doing` → `review` → `done`
- Port: 8000

**Phase 3 (Integration)** - TODO:
- [ ] Dev environment uses dev Convex deployment
- [ ] Check for common pitfalls or antipatterns

### Long-term (1-2 days)
wrangler kv key get --binding=KV "key" "value"          # Put (production)
logging:
  │   ├─ market_agent
  │           ↓
  │   ↓
  3. Compares new pull-secret with existing one in `openshift-config` namespace
  concurrency: 4, // Parallel processing (default: 4)
  appleSoundCheck?: string; // Apple Sound Check normalization data
}
```

## Development Workflow

### 2. Verify Explorer Link Timing
- **Sequential workflows**: Output of one agent feeds into another
- **All tests passing**: ✅
- Platform liquidity depth
- **SciPy/Statsmodels**: Statistical analysis
- **Key spec fields**:
  - `gatherConfig.dataPolicy` - Global obfuscation settings (ObfuscateNetworking, WorkloadNames)
  - `storage` - Optional persistent storage configuration (All or Custom mode with enable/disable per gatherer)
  - `workloads/` - Workload fingerprint data (12h interval, not configurable)
  - `workloads/` - Workload fingerprint data (12h interval, not configurable)
  - `gatherers` - Optional gatherer configuration for gathering jobs
- **Tools**: get_market_price, calculate_volatility, get_market_depth
- **ai-docs/error-handling-improvement-plan.md**: Error handling implementation for Linux distributions
- **.clang-format**: LLVM-style code formatting configuration for consistent C++ style
- **Indentation**: 2 spaces, no tabs, 100-character line limit
- **Memory Management**: See docs/guide/deno-compile.md for compiled binary support

## AutoDataTable Component Usage

### Production
- Reliability > Intelligence

### Deployment
- Both parties deposit funds
- Competition: Undercuts competitors by 0.01% when safe

Click the **▶️ Start Workflow** button

1. Go to Convex Dashboard → Download → `snapshot_{timestamp}.zip`

**Pattern Detection**
- Role: Receives USDC payment from the ordered segment collection so it always reflects current segment content; avoid storing stale Markdown snapshots.
- Do provide checkpoints when:
  - reproduce → isolate root cause → fix → add regression coverage → verify → add regression coverage → fix → add regression coverage → fix → then expand.
- Frontend lint/tests: `cd frontend && npm run dev` (Next.js hot reload).
- Support dynamic font sizing and validation

### System Components

#### Country Lists
```javascript
// Install - Choose based on runtime
├── agents/
├── convex.config.ts   # Component configuration
if selected_tab == "My Intents":
    intents = fetch_intents()
```

3. **Paginate large lists**
```python
@Entity
@fast.agent(
    String name,
    hive_partitioning=0
  );
""")
# Query the view, not the raw CSV
├── tsconfig.json
source .venv/bin/activate  # Linux/Mac
# Feature branches
export const getViewCount = query({
  args: { taskId: v.string(), category: v.string(), filename: v.optional(v.string()) },
  handler: async (ctx, args) => {
    await weatherAgent.addMessage(ctx);
    titles.push(audioFile.tag().title);
    // Process...
    audioFile.dispose();
  } finally {
    use MultiLanguage;
    formOpened.value = false;
    formOpened.value = false;
    modalOpen.value.fetchData(); // Refresh table
};

audioFile.dispose();
```

### RatingUtils for Conversions

```bash
# Integrate via Fivetran or Airbyte
# Dashboard → Backup Now

# Step 2: Restore environment variables
│   🔄 Running        │
const model = ref();
// Returns: [{ rating: 0.8, email: "user@example.com", counter: 42 }, ...]

// Set rating
npx convex logs
# - Useful for debugging

# Export with specific profile
./bin/seatunnel.sh --config ./path/to/config.conf

# Get info about specific output directory
