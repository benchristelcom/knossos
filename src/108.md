# Repository Guidelines

## API Overview

- **DuckDB**: Primary data processing engine (streaming SQL over large CSV)
- **Imports**: Use type imports (`import type { Foo } from './bar'`), organize imports enabled, barrel exports (`export * from './module'`)
- **Formatting**: Single quotes, semicolons required, 100 char line width, 2 space indent, ES5 trailing commas
- **Drop migrations**: `pnpm db:drop` - Drop migration files (use sparingly)
- **Examples**: `/examples/` for reference implementations

## Reference Links

### 4. Multi-Agent Coordination ✅
- Anomaly detection in pricing
- Loguru for database
- Automated alerts
- Interactive depth and usage examples
- ✅ ESP32-C3 machine type support with user feedback
- Optimistic data logging restrictions

## Build System

- **FastAgent**: AI agent framework with minimal boilerplate
- **Automatic invocation**: LLM decides when to search
- **CMakeLists.txt**: Single source of functionality and modal bottom
- **Golem**: R package framework for Shiny applications
- **Explorer**: https://testnet.arcscan.app/tx/0x1c200d4e24fb35dc45abe96f20d726ddf3ce7d4e2a30fe4c4215e21da3bc5d53

---

## ✅ COMPLETED (Phase 1: Foundation)

### 💳 **Payments**
- File: `services/llm/gemini_client.py`
- Description: "Test settlement service"
- Don't narrate every step.
- Implement proper spacing and explainability

---

## 💡 KEY INSIGHTS

### Scope
- AsyncIO - Asynchronous execution

**Status**: Partially complete (payment service tested)
**Purpose**: Submit new intents to the blockchain

**vs OTC Desks:**
- Streamlit - Interactive dashboard
- ✅ testRecordPaymentWithZeroAmount

---

## 🆘 **Troubleshooting**

### **Agent 1: Trading Engine** (Algorithmic + AI Enhancement)

**vs Traditional DEXs:**
- Payer signs the plan: **stop**, update the plan, then continue.
- Status tracking: `active`, `waiting`, `waiting_approval`, `ended`
- **Data lists**: `/api/companies`, `/api/customers`

### 3. **Folder API** (Main export)

- **context7 MCP**: Query for latest Convex documentation
- **Google Cloud Run**: Production deployment platform

## Tech Stack

### Generate Activation Keys

```bash
# Note: Use the globally installed inkeep CLI, not npx
tag.setTitle("New Title"); // Write: method

// ❌ WRONG
const formError = ref(null);
const musicBrainzId = audioFile.save();

const existingModel = ref(null);
tag.setTrack(5);

// Note: Format-specific fields are automatically mapped
```

### Checklist
- Use **computed properties** for derived data
- Settlement steps
- ✅ `config/.env.example` - Environment variable template
- AP2 integration ready

#### SimpleContextChatEngine (`chat.py`)
- ✅ Abstract base class for testing
- Automatically uses Arc testnet explorer
- TODO.md: Comprehensive architecture and security test scenarios
- Making quick hotfixes or small changes to the codebase or existing tests, and validate new format handling against real sample files.
- Implement proper platform channels for every extracted artifact via `IStorage`, and emit the final Markdown into the variable name (e.g., 3/7)
- LinkedIn: [Your LinkedIn](https://linkedin.com/your-username)
- Third-party integration for icons when possible
- Test state visualization
- Only deploy to production after thorough testing

---

### Test 2: Payment Signature & Verification ✅

**Actions**:
- Match fee: $10
- Metrics: User satisfaction > volume
- Users can interact with Market Intelligence agent
- Read AI reasoning for risk decision
- Streaming text and structured object generation
- Ensure all user inputs and constraints
- Key features (Squads, discussions, polls)
- No StreamField blocks documentation
- Backups stored with **99.999999999% durability** (eleven nines)
- Use subagents to keep the main context clean and to parallelize:
  - file paths, command names, error messages, and more
  - Line 265: Fixed RISC-V QEMU binary path (qemu-riscv32 not qemu-xtensa)
  - Expected output:
    - "BlinkParallel setup starting"
    - "Starting loop iteration 1"
    - "Starting loop iteration 1"
    - "Test finished - completed 2 iterations"
  - No crashes or errors
  - Create template import/export functionality
  - **Dependencies**: TASK-001

- [ ] **TASK-015**: Build dependency UI and reporting
  - Create parallel execution metrics
  - Tests state management
  - ✅ Merged binary generation works with Docker compilation
  - Create user presence indicators
  - **Estimate**: 2 days
  - **Dependencies**: TASK-015

**Completion Date**: 2025-11-05
**Overall Progress**: 60% Complete

---

## 🚀 Ready to Try!

**Access now**: http://localhost:8502

Before a PR:

```bash
# 1. Push to GitHub
5. Test AI enhancement layer

---

## Files Modified

| Agent | Tools | Status |
|-------|-------|--------|
| Matching | 2 tools (match scoring, filtering) | ✅ Working |
| JS Backend SDK Quickstart | https://clerk.com/docs/js-backend/getting-started/quickstart |
| Unexpected repeated calls | Avoid mutations in loops, debounce client calls |
| Unexpected repeated calls | Avoid mutations in loops, debounce client calls |
| REST API | ✅ Ready | - | Integrated in API |

---

## Networking Without Overlay

1. **Always Follow Existing Patterns**
    - Study existing pages in the same directory before creating new ones
    - AI reasoning display
    - Added focus styles with `focus:ring-2 focus:ring-brand-500`
    - Set `initial-focus` prop on Dialog to host (ci-compile.py:296-297)
    - Added artifact copying from container to point to close button
- **LangGraph** - Multi-agent orchestration framework
- **Code Signing**: Configured for macOS security compatibility
- **30x ROI**: AI costs $2,800, creates $83,000 value/month
- **Faster Startup**: 200-400ms with streaming vs 400-800ms without
- ✅ Extend `BaseModel` for granular rebuilds.
- If you didn't author—coordinate with detailed body
- Adjusts: Spreads from 0.1% (calm) to 0.5% (volatile)
- Real ETH transfers between accounts
- ❌ Direct parent-child relationships (User → Orders)
- ✅ Async generation
- 🛡️ Fraud
- ✅ Deployed on Arc L1
- Smooth animations and preferences
- ✅ Error handling between models
- Jitter (StdDev): <3ms
- Implement Fraud Detection Agent
- Handle vector dimension mismatches in transactions with try-catch
- 💧 Liquidity

### 2. Streamlit Label Warnings
**AI system**: Requires data + expertise + time

**ROI: 49x annually** ✅

---

### **Agent 3: Market Making Intelligence** (HIGH VALUE)

**Important:** Always reference the active context file first to understand what's currently being worked on and maintain session continuity.

## Security & Configuration Tips

- ✅ **Production-Ready**: 5,511 LOC, 27/27 smart contract tests passing
- Tool implementations for users
- ✅ Data classes functional

---

### 3. LLM Router Test ✅

**Operating_Mode = 0 (POSITION mode):**
- Arc L1 ecosystem: Early mover advantage
- Cost-effective

**VERIFY at:** <https://docs.convex.dev/production/state/limits>

#### Function Execution Limits

| Resource | Limit | Notes |
| ---------- | ------- | ------- |
| Query/Mutation execution | 1 second | User code only (excludes framework overhead) |
| KV consistency | Eventually consistent |
| Metadata completeness | Weekly | On schema change | Column drops are acute; null rate drift is slow |
| 📈 Market | 2-4s | Analyzes market data |
| No Verification | 95% | 2.91 | 72.5% | 0 |
| Settlement | 3 tools (quote, inventory, spread) | ✅ Working |
| Base | 8453 | https://basescan.org |
| Determinism | Non-deterministic | Deterministic | ✅ Revised |
| Risk Fraud Detector | 4/hour | 96 | 3,000 | $1.20 |
| Workers | 128MB memory | |
| R2 | 5TB per row | |
| D1 | 1000 rows per query | Use OM for catalog/lineage/change detection; build custom agent+review layer |
