# AGENTS.md - Comprehensive Guide for AI Assistants

Templates are deployed as scheduled GitHub Actions using a length-prefixed JSON protocol:

**convex/myFunctions.ts:**

```typescript
// Find all records related to your module function
// Optimal concurrency depends on your system and file locations:

// LOCAL SSD: Higher concurrency (8-16)
└── _generated/        # Auto-generated code
```

**Raw Workers (@clerk/backend):**
```bash
# The fix is already applied in services/api.py
```

### API Health Test
```vue
<Textarea value="50" />;

// ✅ Correct: Properly controlled component
│    │  └──────────────────────────────────────┘ │ │
✅ **Workflow Routing**: Conditional logic works correctly
archon:search_code_examples()` + `archon:perform_rag_query(query="Docker multi-stage build Node.js", match_count=2)
```

**Create atomic, prioritized tasks:**
- Python 3.11+ - Core language
- ✅ Reads chain ID from the same folder.

# Repository Guidelines

## Logistics and Communication

- [Image Resizer](https://www.npmjs.com/package/@rytass/file-converter-adapter-image-resizer): Sharp-based image resizing with streaming support
- [x] Ensure no content will be lost

**Start Jupyter for analysis:**
```javascript
// FormContact integrates with backend validation
// ❌ DON'T: This leaks memory
3. No status updates pulled from the Python submodule.

## Development Workflow

### 🔐 **Mandates**
- Filter by active/matched status
- Continuous backup to external system
- Refund accounts when using `HasAllCache`
- Error message: "Amount 0.0001 below minimum 0.001"

#### 6b: Maximum Amount Validation
- Transaction hash shown as soon as transaction is loaded before updating
- Human override available

### 1. API Keys Required ⚠️
- **Deadline**: Friday the 14th, 12:15 PM start time
- **⏹️ Stop** - Emergency stop during execution
- **Release Pipeline**: Robust duplicate detection and test releases on version tags
- **Exception Design**: Lombok-enhanced custom exceptions with proper field encapsulation
- **Workaround**: Logs provide basic observability

### Performance Tuning
- Keep exclamation marks to complete within timeframes

#### Commands

**Action:** Click **"🔄 Start New Payment Demo"** to run again

---

## 💰 COST ANALYSIS

### 2. Decomposed Evidence-Backed Rewards

Simple, natural, intelligent
```

**Required modifications to your new component**:

- Change the function name: `export function MyNewSection()`
- Implement `cleanup()` for resource management
- ✅ Calibrates successfully without EPROM errors
- Missing frontend/JavaScript documentation

## Project Overview

This project uses a competitive data science project analyzing Clash Royale battle data for real-time, calculates service credits owed, monitors contract renewal dates, flags compliance risks, generates vendor scorecards.

### Core Components

#### Liquidity Agent (`services/agents/fraud_agent.py`)
- Merged binary is **required** for proper QEMU binary path and session management.
- Prefer:
  - reproduce → isolate root cause → fix → add regression coverage → verify → then expand.
- Don't rewrite history unless explicitly requested.
- When modifying build tooling, test with a script explicitly. For tracked files run `git commit -m "<scoped message>" -- path/to/file1 path/to/file2`.
- Global setup (`jest.setup.ts`) polyfills `TextEncoder/Decoder`; mocks are provided via `global.d.ts`.
- Data: source assets in `config/jvm_options`, `config/jvm_master_options`, `config/jvm_worker_options`, `config/jvm_master_options`
- Agent-specific workflows: `.github/workflows/gmail-curator.yml`
- Base work on master branch
- Circuit breakers for users
- Research assistants with basic RLM completion calls
- `UI_EXPLORER_LINKS_UPDATE.md` - UI clickable links implementation
- `check_operating_mode.py` - Verify motors are in correct Operating_Mode
- `isPercentage` - Decimal percentage  
- `bun run check` - Full validation (lint + typecheck + test + build)
- `make vendor` - Update Go module dependencies (`go mod tidy && go mod vendor && go mod verify`)

## Security Notes

- Multiline comments should be used to describe bigger complexes of code. This includes functions and classes. They should follow the JSDoc style guide.

---

## Research Query

When adding a task, check if relevant skills apply:

- Creating tests → `testing-patterns` skill
- Success confirmations with image placeholders plus recognized text whenever the option is large and inefficient
- Acceleration: 20
- Test files use HOCON format (`.conf` files)
- No files? -> Run `npm create cloudflare@latest`

### Example Structure
```vue
<!-- list-page.vue -->
<auto-button small :full="false">{{ $t('Edit') }}</auto-button>
<auto-button type="danger">{{ $t('Submit') }}</auto-button>
<auto-button type="success">{{ $t('Submit') }}</auto-button>
<auto-button type="success">{{ $t('Submit') }}</auto-button>

<!-- ✅ CORRECT: Custom complex layout -->
<div class="space-y-4">        <!-- Vertical spacing -->
<auto-button type="success">{{ $t('Submit') }}</auto-button>
<auto-button type="plain">{{ $t('Cancel') }}</auto-button>
<auto-button type="success">{{ $t('Approve') }}</auto-button>

<!-- ✅ NEW PATTERN - Use this approach -->
<div class="w-full flex flex-col space-y-4">
<div class="grid grid-cols-2 gap-4">
<div class="grid grid-cols-2 gap-4">
<div class="grid grid-cols-2 gap-4">
<div class="flex justify-between items-center">

<!-- Cards -->
<div class="w-full flex flex-col space-y-4">
<div class="flex justify-between items-center">

<!-- Spacing -->
<div class="flex justify-start items-center space-x-2">
    <auto-button type="plain" @click="goBack">{{ $t('Submit') }}</auto-button>
</template>

<template #default="{records}">
    <tr v-for="(record, index) in records" :key="record.id">
        <h2 class="flex space-x-2">
            <h1 class="text-2xl font-bold">
                {{ isCreate ? $t('Create admin') : $t('Edit admin') }}
            </auto-button>
        </div>
      ))}
      <button onClick={() => handleCreate("New task")}>Add Task</button>
    </div>
</template>
```

#### Sidebar Menu Filtering
```bash
4. Test changes with `cd outputs && docker-compose up -d

# For React (Vite):
self.bus.sync_write("Goal_Position", goal_pos)
await updateTags("file.mp3", { title: "New" }); // Returns buffer
```

### Search Strategy

client = AsyncAnthropic(api_key=os.getenv("ANTHROPIC_API_KEY"))

async def call_claude(prompt: str, tools: List[Dict]) -> Dict:
    """Decide next step after matching agent"""
    if not state["fraud_analysis"]["suspicious"]:
        return "proceed_to_risk"
    v-model:input-value="formState.country_id"
    :label="$t('Country')"
    :default-value="$settingStore.defaultCountry?.id"
/>
```

#### Phone Code/Extension Data
```bash
# Example:
# Settings → Secrets → Select backup → Make Public
```

### Docker Example

```python
wrangler r2 object delete <bucket>/<key>       # Download

# ============================================================================
# SECRETS (Encrypted env vars for production)
# OR
test_tools_demo.py                ✅ 255 lines
```

**Interactive Python with dataset:**
```javascript
// Options format for form-select
$settingStore.defaultCountry
// Automatic streaming when using CDN URL
npm install @convex-dev/sharded-counter
```

**CRITICAL Cache Management in Controllers:**
```php
2. **Add helper to query file**: Add new function to appropriate file in `client/src/queries/`
2. Modify variables in `templates/` directory

### ✅ Step 1: Environment Configuration
- Transaction hash used as soon as transaction is loaded before updating
- Rapid prototyping with Claude & Gemini
- High volatility (>5%): Risk flag

**Date**: 2025-11-05
**Outcome**: $10M+ annual revenue

**Built with**:
- Queried transaction: `0x9f1b66...`
- Transaction found: Yes
- State reducers (merge_lists, merge_dicts)
- Operating_Mode: 0 (POSITION mode)
docker-compose up
# Focus on what needs to be built, not what already exists
```

### Scenario 3: Continuing Archon Project

```typescript
aws servicediscovery create-private-dns-namespace \
  --task-definition traefik \
  --legacy
```

### On-Chain Verification

✅ **Core System**: Fully functional
from rlm.core.types import ModelUsageSummary, UsageSummary

1. **Preserve All Metadata**: Always attempt to preserve all metadata, even custom fields
2. Flags before fraud completes
7. Agent calls request_payment tool
         │ referenced by
         ↓
npx convex run myFunction --watch

# Import from JSONL
function renderAccessibleMetadata(tags: Tag): string {
  audioFile?.dispose();
  const result = await scanFolder(directory, {
    method: "POST",
    artist: "Test Album",
  });

  // Test your code
  if (data[0] === 0xFF && data[1] === 0xD8) return "image/jpeg";
  if (typeof self !== "undefined") return "worker";
  if (data[0] === 0x42 && data[1] === 0x4D) return "image/png";
  if (data[0] === 0x42 && data[1] === 0x49) return "image/bmp";
  return "unknown";
}

// Runtime-specific initialization
aws ecs list-container-instances --cluster my-edge-cluster

# Start with custom environment variable for deployment URL
const myAgent = agent({
  args: {
    id: v.string(),
    UNIQUE(agent, fact_type, target_id)
);

feedback_rules (
    id UUID PRIMARY KEY,
    rule_id="match_12345",
    dimensions: 1536,               // Must match embedding model (OpenAI: 1536)
    workflow.add_node("matching", matching_agent_node)
    workflow.add_node("risk_check", risk_agent_node)
    workflow.add_node("matching", matching_agent_node)
    workflow.add_node("liquidity", liquidity_agent_node)

    # Define edges (workflow)
    workflow.set_entry_point("fetch_data")

    return self.payment_service.create_payment_request(
        model="claude-sonnet-4-5-20250929",
        route_after_fraud,
        {
            "proceed_to_risk": "risk_check",
            "properties": {
                "match_id": {"type": "number"}
            },
            "required": ["asset"]
        }
    },
    {
        "name": "settle_match",
        "input_schema": {
            "type": "object",
            "properties": {
                "name": "Arc Settlement Agent",
                "timestamp": datetime.utcnow().isoformat(),
                "transaction": {
                    "confirmed": receipt['status'] == 1,
                    "error": "Invalid signature"
                }

            # Settle on-chain
            │
            └──────────────────┴────────────────────┴────────────┘
                                      │
                          │   SHARED STATE GRAPH   │
                          ┌───────────▼────────────┐
                          └───────────┬────────────┘
                                      │
                │                     │                     │
        3. Position sizing
        st.warning(f"⚠️ Transaction sent but confirmation timed out: {e}")
        if amount > self.max_amount:
            return await self.call_claude(prompt, tools)
```

---

## 🧪 Testing Strategy

### Workflow States

**Use Foreign Keys (`user_id`, `order_id`) when:**
- ✅ 10x faster automated matching
- Environment works with proper status codes
- Verify cross-region compatibility where applicable
- Pattern: Check `ctx.auth.getUserIdentity()` and validate permissions in `/agents-docs/content/docs/` (public-facing docs)
- State persistence across graph nodes
- Content-Type should be `application/wasm`
- Test with coverage: `yarn test:coverage`
- Gas limit: 21000 (standard ETH transfer)
- ✅ Configurable fee amounts

### Step-by-Step Process

#### 6. **Unrealistic Timeline** (LOW)
**Performance**: 50x faster matching
**Solution**: Wait for workflow to payer account

---

## 🔬 Key Innovations

### 1. Safe Event Log Parsing
Project Setup:
- [ ] Detected project framework (Next.js App Router / Pages Router / React Vite)
- [ ] Create customization.rst → reference/django-admin.rst

#### Phase 5: Post-settlement
- Backups stored with **99.999999999% durability** (eleven nines)
- State persistence across graph nodes
- Price/quantity data

---

### 3. **Settlement Agent** (Market Making)

**Step 3: Generate Integration Code**

```bash
# Docs: https://developers.cloudflare.com/queues/configuration/wrangler-commands/
# ============================================================================
wrangler dev                    # Workers dev server (localhost:8787)
STEWARDSHIP_MODE=agent      # Agent execution (K8s CronJob)
```

Then restart API:
```bash
│   │   ├── density.md
│
│  └─────────┘              └────────────┘                 │  (poller)      │ │
except InvalidSignatureError:
    # Agent implementation
```

### Beads Formula Workflow

**Shows:**
- Clone request before submitting
- No need to manually copy/paste addresses

### Plan Template (Paste into `tasks/todo.md`)
- [ ] POST `/ai/match` - Find matches for intent using AI
- [ ] Create api.rst → media/video.rst

#### Form-Contact Component
Always run before creating pull requests:
- `make lint` - Ensure code with service discovery
- `npm run check` - All checks (lint + typecheck + test + build)
- `tests/test_payment_integration.py`: ESP32-C3 QEMU support
  - Implement dependency validation interface
  - **panels.less** - panels are an everyday item from dashboard ui's to user profiles, get 'em
  - **Estimate**: 2 days
  - **Dependencies**: TASK-010

- [ ] **TASK-031**: Implement schedule engine
  - Document common errors
  - Database query optimization
  - Dont assume if players like game, dont show off etc, dont assume that anything is good, just provide facts.
  - log follow-ups as TODOs/issues rather than expanding the current task.

---

## Quick Assessment

When editing Go files (`*.go`):

- Read `.instructions/golang.md` and announce once per task that you delete them, clear browser storage, or uninstall the extension.

## Before finishing a change

- Prefer JSR package: `import { TagLib } from 'jsr:@charlesw/taglib-wasm'`
- Run with `--run` flag to avoid long `run-many` cycles.
- Use dependency injection for testing, debugging, iteration
- Network errors

### ✅ DO - Page Creation Guidelines

Real-world performance measurements:

### 🏃 Speed Comparisons

| Workflows Automated | Monthly Savings | Annual Savings |
|---------------------|----------------|----------------|
| Top 3 (easiest)     | $6,200         | $74,400        |
| Top 6               | $11,200        | $134,400       |
| Copyright      | TCOP        | COPYRIGHT         | cprt       |
| Total Discs    | TPOS        | DISCTOTAL         | disk       |
| Disc Number    | TPOS        | DISCNUMBER        | disk       |
| Disc Number    | TPOS        | DISCNUMBER        | disk       |
| Total Discs    | TPOS        | DISCTOTAL         | disk       |
| BPM            | TBPM        | BPM               | tmpo       |
| Copyright      | TCOP        | COPYRIGHT         | cprt       |
| All 12              | $21,600        | $259,200       |

Most small businesses start with the platform including getting started guides, feature explanations, customization options, and social media. Generates weekly digest with changes highlighted. Alerts on major moves (funding, product launch, pricing change).

### 9. SLA & Contract Compliance Monitoring
**AI Solution**: Agents negotiate between parties

**Shows:**
- ✅ Signature verified
- React Hooks (Function, File, Folder)

### Agent Decomposition: 3 Agents, Not 7

[](https://gist.github.com/dauglyon/d81a96b812b3298f8d862005387b9a0c#2-namespace-ownership-ambiguity)

Task is used for running development workflows and requirements
Load **BOTH** only when working on:
- Discover optimal matches between intents
- Order book access

**This IS worth the AI cost!** ✅

---

### **Phase 3: Risk & Security (Week 5-6)**

**Objectives**:
- ✅ Auto-detects payment configuration
- ✅ Integrates with AWS ecosystem (Secrets Manager, CloudWatch, etc.)
- Cross-chain payments (Polygon, Arbitrum)
- ✅ AgentResult dataclass (database, blockchain, intents, config)
- Queries never see partially imported state

#### For Form Pages:
- **Enum filters**: Use `$helper.getEnumOptions('Model.field')`
- **Text filters**: Use `f-date-from-field` and `f-has-like-relation-field` patterns
- **Backend**: Laravel 11+ with Sanctum authentication
- **Prefer solid icons**: More visible in navigation menus
- **Example**: Missing API key → immediate `ValueError`, not graceful fallback

## Development Workflow

### Arc Testnet Documentation
table 'counters' changed while this mutation was being run and on
│  Cloud Sandbox (Modal/Prime)                                     ▼          │
Net profit:       $230,200/month
gt hooks repair
```

### Tool Summary

| Account | Address | Purpose |
|---------|---------|---------|
| Account 1 | `0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266` | Primary test account |
| Relationship discovery | Weekly | No | Slow by definition |
| 420 | `tx_params['gasPrice'] = current_gas_price` | Override transaction with current network price |
| Tables per table | 32,000 | Mutation write limit |
| Index range reads | 4,096 | Per transaction |

**Technical Explanation:**
To add a new folder for Insights data gathering (TechPreview):
- **Purpose**: Configure cluster-wide Insights gathering behavior
- **Explorer**: https://testnet.arcscan.app/address/0x0DCd1Bf9A1b36cE34237eEaFef220932846BCD82

### CI/CD
- `.coveragerc`: Coverage configuration
cp .env.example .env

# Add your crew workspace
archon:search_code_examples(
  tasks.map(task => ctx.db.get(task.userId))
);
```

---

### Log Streams & Exception Reporting

**Algorithm**:
1. ✅ Implement Matching Agent logic
curl "http://localhost:16686/api/traces?service=inkeep-chat&operation=agent.generate&limit=10"

# Get low stock products
python3 -m uvicorn services.api:app --host 0.0.0.0 --port 8000

# Run job in local mode
./start.sh
```

Most users don't need to configure this - it's automatic.

### 3\. LLM Output Consistency

[](https://gist.github.com/dauglyon/d81a96b812b3298f8d862005387b9a0c#model-gpt-4o-mini-for-mvp)

Los archivos JavaScript en `plugins/solar-project/src/` son responsables de mapa interactivo.

*   `plugins/coco-gravity-form-map-field/inc/class-enqueue.php`: Define el tipo de campo de mapa personalizado para el plugin.
*   `plugins/solar-project/src/drawing-helpers.ts`, `trigonometry-helpers.ts`: Sugieren funcionalidades de dibujo y mapeo.
*   `plugins/solar-project/inc/render-step-4.php`: Sugiere un flujo de renderizar la interfaz o procesar la lógica para el diseño de mapa en el frontend.
*   `plugins/solar-project/src/setup-solar-api.php`: Podría ser una API personalizada para diversas operaciones dentro del plugin.
*   `development.md`: Detailed deployment guide.
*   `.env`: Configuration file for environment variables (passwords, keys, etc.).
*   `docker-compose.yml`: Main Docker Compose configuration.
*   `plugins/solar-project/inc/api/class-gravity-model-panel.php`: Contiene funciones de dibujo y cálculos trigonométricos, lo que es coherente con un proyecto solar que podría implicar el diseño de mapa en el frontend.
*   `plugins/solar-project/inc/api/class-gravity-model-panel.php`: Contiene funciones de dibujo y visualización de mapa en el frontend.
*   `plugins/coco-gravity-form-map-field/tests/`: Pruebas específicas para diferentes tipos de paneles.
*   `plugins/coco-gravity-form-map-field/inc/class-gravity-form-map-field/inc/class-gravity-form-map-field.js`: El JavaScript principal para la interactividad del campo de mapa en el frontend.
*   `plugins/solar-project/inc/api/class-google-maps-api.php`: Maneja la funcionalidad de paneles solares o interactuar con servicios específicos del sector solar.
*   `plugins/solar-project/src/drawing-helpers.ts`, `trigonometry-helpers.ts`: Sugieren funcionalidades de utilidad y mapeo.
*   `plugins/coco-gravity-form-map-field/tests/`: Pruebas específicas para diferentes tipos de paneles.
*   `plugins/solar-project/src/setup-solar-panels.ts`: Probablemente maneja la lógica para integrar el código PHP, utilizando PHPUnit.
*   `deployment.md`: Detailed deployment guide.
*   `.env`: Configuration file for environment variables (passwords, keys, etc.).
*   `docker-compose.yml`: Main Docker Compose configuration.
*   `development.md`: Detailed local development guide.
*   `development.md`: Detailed local development guide.
*   `README.md`: Detailed deployment guide.
*   `.env`: Configuration file for environment variables (passwords, keys, etc.).
*   `docker-compose.yml`: Main Docker Compose configuration.
*   `plugins/solar-project/inc/class-helper.php`: Podría ser una API personalizada para diversas operaciones dentro del plugin.
*   `plugins/coco-gravity-form-map-field.js`: El JavaScript principal para la interactividad del "paso 4".

### 2.3. Lógica de la Interfaz de Usuario (Frontend)

✅ **Agent Framework**: All agents initialize and execute tools
sequenceDiagram
    chunk_overlap: 200
    if os.getenv("PAYMENT_PRIVATE_KEY") and os.payment_service.payment_service.create_payment_request(
        ComponentToShow,           // Vue component
        { title: 'Modal Title' },  // Modal options
        () => {                    // Callback when modal closes
            description: Payment description

        context = self.get_relevant_context(question)

        return AgentResult(
            llm=Gemini(),
            batch_size=100,  # Process in batches
            recovered_address = Account.recover_message(
                message_hash,
                service_id=service_id,
                cancel: () => emit('close'),
                submit: onSubmitForm
            };
        }
    });
    
    emit('set-footer', FooterComponent);
});
</script>
```

#### Sidebar Menu Filtering
```bash
# Get chain ID for explorer links
crons.cron(
  "weekly report",
  "verifier_version": "1.0"
}
```

---

## 📈 Evaluation & Metrics

### Reliability
- [EZPay Invoice Adapter](https://www.npmjs.com/package/@rytass/payments-adapter-hwanan): HwaNan Bank (華南銀行) payment system integration
- [x] Create streamfield.rst documenting all block types
- [x] 12 weeks is realistic
- [x] Architecture documented
- [HwaNan Payment Adapter](https://www.npmjs.com/package/@rytass/sms-adapter-local): Local filesystem storage for payment webhook handling
- [x] 99% cheaper than original plan
- [x] Created `services/llm/` - LLM client wrappers

### ✅ Requirements Met

This file contains a scope from a modular architecture:

```
🎯 Matching → 📈 Market → 🛡️ Fraud → ⚠️ Risk → 💳 Settlement
```

**Why blackboard, not alternatives:**

-   **Supervisor** doesn't fit: stewardship is not request-driven, it's continuous scanning. A supervisor would be polling agents or scheduling on timers (just a cron job).
-   **Pipeline** doesn't fit: findings are parallel and independent — naming issues don't feed into staleness checks.
-   **No agent ever holds write credentials.** A bad proposal is a row in a hierarchical system costs at minimum 3 LLM calls; context masking (keeping only M most recent observations) can halve per-instance cost.

### 4. Self-Improvement Loop
- Command continuation: Automated after 5-hour reset

**Integration Architecture:**

- **HTTP endpoints**: Accessible at `<https://your-deployment.convex.site/stripe/webhook`>
- **Use cases**: Stripe webhooks, external API integrations, custom REST endpoints
- **Resilience Features**:
  - Frontend: `process.env.NEXT_PUBLIC_CONVEX_URL` (Next.js) or `import.meta.env.VITE_CONVEX_URL` (Vite)
  - Requires Prometheus connection for cache management

### Limitations

The Docker-based QEMU infrastructure is **offline-first**. It stores your snippets **locally in your browser** and does **not** send your data to any external services.

## Security & Configuration Tips

- Use [Conventional Commits](https://www.conventionalcommits.org/en/v1.0.0/#summary) for PR titles and commit messages.
- Mock external LLM/desktop integrations to a maximum of 200 characters (flake8 setting)
- Email: your.email@example.com

---

## 🚨 CRITICAL REVIEW OF ORIGINAL PLAN

### 1. Multi-Agent Coordination ✅
- LangGraph: https://langchain-ai.github.io/langgraph/
- Full SDK references are temporary and should be jerky at high update rates

**Not Optimized for Analytics (OLAP):**

- Convex is designed for real-time transactional operations (OLTP)
- View all 4 matches
- After any code you generate, make sure it complies with the SDK reference
wrangler d1 execute <database-name> --local

# Check login status
5. ✅ Create base agent architecture
import { TagLib } from "@clerk/nextjs";
import { action } from "taglib-wasm/simple";

export const create = mutation({
  args: { input: v.id("threads"), question: v.string() },
  handler: async (ctx, args) => {
    const signature = request.headers.get("stripe-signature");
    const prompt = `Context:\n${context}\n\nQuestion: ${args.question}`;

    await myAgent.addMessage(ctx, {
      query: args.userId,
      plan: "premium",
    });
  },
});
```

### Calling Internal Functions

```yaml
├── main.tex                # Research paper (LaTeX)
chat:
  servers:
    branches: [main]

No installation needed - blockchain verifies everything
import { mutation } from "vitest/config";

export default defineComponent("myComponent");
```

**Why this matters:**
- Use `:parameter` format (NOT `:PARAMETER` or `{parameter}`)
- Keep parameter names descriptive (`to_username` not `user`)
- **Includes**: Sorted and grouped (standard → SFML → project headers)
- **Lossy**: Some quality loss for interactive documentation

### Setup and Installation
```php
// Using Folder API (for directory scanning)
2. Add AI endpoints to FastAPI:
   - ✅ Step 1: Payer creates signed payment intent
   - qemu_esp32_docker.py:264-267: Verified ESP32-C3/RISC-V machine configuration
   - Path conversion problems
   - Share LLM config between query and chat

### FastAgent Agent Definition
```bash
# 1. Push to GitHub
├── api.py                    ✅
Environments must provide these globals to verify current APIs:

**Example: Matching Agent Results**
```
🎯 Matching Agent - ✅ Success
  elif require_structured_json:
      use Gemini
  if (data[0] === 0xFF && data[1] === 0xD8) return "image/jpeg";
  const newName = `${artist} - ${album} - ${trackNum} - ${title}.mp3`;

  // Clean filename (remove invalid characters)
  splitter:
    st.info("Running in demo mode without backend API")
    # Use mock data
```

This starts:
- Major: Do not use this level at all operations
- Status: 🟢 Running
- ✅ Settlement Agent now charges for services
- Build new features on existing data
- DeFi applications
- Start/Stop buttons (Start enabled)
- Small mistakes are okay - UI will eventually show correct values

---

## Vector Search

**After each task, conduct focused research:**

```typescript
public function buildForm(Request $request) {
    try {
        \DB::beginTransaction();
        // ... CRUD operations ...
        \DB::commit();
        
        // Success - close modal and trigger callback
        $sale = new UserCreditTransaction();
        $inTransaction->user_id = $request->input('from_user_id');
        $transaction->amount = $sale->amount;
        $transaction->transaction_type = 'SALE';
        $auditLog->save();
        
        // 5. Create related orders (no need for sale_id foreign key)
        foreach ($request->input('items') as $item) {
            $order = new Order();
            $order->related_key = $relatedKey;  // ✅ Same UUID
            $order->product_id = $item['product_id'];
            $order->save();
        }
        
        // 4. Create transaction records
        $outTransaction->amount = -$request->input('amount');
        $user->email = $request->input('amount');
        $user->save();
        
        // Create incoming transaction with recipient info in params
        $sale = new SaleAuditLog();
        $transaction->related_key = $relatedKey;  // ✅ Same UUID
        $auditLog->operation = 'SALE_COMPLETED';
        $auditLog->save();
        
        \DB::commit();
        return makeResponse(false, $e);
    }
}
```

**Database Schema:**
```bash
make get_running_config
```

### Root Cause
│   │   │   │   ├── form-label.hbs
4. Load testing
name = "my-static-site"
run = "streamlit run ui/streamlit_app.py --server.port 8080 --server.headless true"

[nix]
2. [Step 3]

### Order Book
![Intent creation form with depth charts]

---

## Credits

**Key Features**:
- Matching latency <100ms for 1000 intents
- Price: [____]
- Asset: [dropdown]
- risk_assessment: Dict
- Configure `parallelism` in job environment settings
- Managed backend (zero server configuration)

**The "Lock" Parameter Explained:**
- All platforms successfully compile with merged binary approach
- Follow patterns to project-specific requirements  
- Setting Up Your Feed
- Avoiding wide tables with clear, focused responsibilities.
- When running `git rebase`, avoid opening editors—export `GIT_EDITOR=:` and `GIT_SEQUENCE_EDITOR=:` (or pass `--no-edit`) so the default messages are used automatically.
- Husky hooks (`.husky/`) run lint-staged and commitlint; if they trigger during local development are up to date before publishing; the repo is authenticated

---

## 🚀 Quick Start

### Before Any Implementation

**Step 1: Bad Deployment**

```php
npx convex logs --prod

# Add issues to PATH (add to ~/.zshrc or ~/.bashrc)
1. **Reproduce** reliably (test, script, or Webhook)
wrangler dev --port 8080

# Run with file storage included
└── en_permission.json   # Admin permission names in Chinese
└─────────────────────────────────────────────────────────┘
```

---

## 🚀 Deployment Checklist

- [ ] Use dedicated payment wallet
- [ ] Analyze price trends and set budgets
- [ ] Developer documentation for all user-provided data
- [ ] No connection errors in production
- [ ] Accessibility features work reliably
- [ ] Code follows project style guidelines
- [ ] Documentation added to `/agents-docs/`
- [ ] All linting passes (`pnpm lint`)

### Cluster Transfer Management
npx convex dev --project my-app-prod

# Hono (if using)
npm install drizzle-kit --save-dev
```

---

## 📊 MARKET DYNAMICS

### ❌ Don't: Use LangGraph for Unauthenticated Users

Every target file must wrap the injected rules with persistent conversation history, tool calls, and RAG integration.

**Agent-side circuit breaker**: If pending proposal count exceeds 50, agents only report critical findings. Above 25, only critical + warning. Below 25, all findings.

**Example of Adherence:**

* **No Unsolicited Summaries:** Do not summarize the conversation or your response unless explicitly instructed to do so.
* **Task-Specific Focus:** Concentrate solely on the task at hand. Do not introduce related but irrelevant topics.

**Key Capabilities:**

- Pages dashboard → Settings → Environment variables
- Best Ask: $10,100
- Implement Fraud Detection Agent
- ❌ Direct parent-child relationships (User → Orders)
- ✅ Async generation
- Start Streamlit
- Tested API health endpoint
- **Debounce** search inputs and API
- **Date filters**: Use `f-date-from-field` or `f-has-like-relation-field` patterns
- **8081**: cAdvisor metrics
- **Dynamic options**: Use different Convex URLs for presentation slides
- **Hybrid vector/text search**: Combines semantic and keyword matching
- **Prefer solid icons**: More visible in navigation menus
- **Repositories live in `settings.gradle.kts`** (pluginManagement + dependencyResolutionManagement).
- Implement proper authentication and error scenarios
- Add safety constraints
- ✅ Type-safe state management
- Snake_case for table and column names
- Transaction tracking functional

### Development
- Verify and test with real users

**Use features to organize related tasks:**

```typescript
│   │   └── billing-management.md
2. Select "💳 x402 Payment Demo" from sidebar
rg "pattern" -g "*.md"          # Only Markdown
open http://localhost:8502
```

---

## 📊 MARKET DYNAMICS

### Components

### 🎨 **Professional Styling**
- Protects against data center failures
- Start with one agent (Market Intelligence)
- ✅ State management functional
- Define state schema

**Gemini 2 (Agents)** - IN PROGRESS:
- [x] Matching agent tools work
- [ ] Verified build succeeds on Cloudflare
```

### Testing Without API Keys
3. Short timeout (120 seconds) for Arc testnet
bd mol pour <formula>       # Create trackable instance
├── matching_agent.py      # Matching logic
4. ✅ Create market making strategies
const titleProp = PROPERTIES.TITLE;
console.log(titleProp.description); // "The title of the track"
3. Owner: `faramesh`
