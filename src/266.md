# GEMINI.md

This guide explains how to integrate the application for governable self-improvement in Docker-based payments.

---

## Research Query

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 3. Plugin "solar-project"

sudo bash ecs-anywhere-install.sh \
  --default-instance-name "edge-node" \
  --activation-code "YOUR_ACTIVATION_ID" \
  --tags "Key=Environment,Value=production"
```

6. **Click "Deploy"**

4. **Fill in:**
   - Share with a friend
   - ✅ Step 2: Merchant verifies signature
   - AI reasoning display
   - Builds and pushes Docker image
   - Calendar reminders
   - Success rates

### Environment Variable Management

**ui/x402_payment_demo.py:425-439**:
```python
npm run docker:run        # Run pre-built image
```

## Architecture

### 1. Intent Submission Event Log Parsing
the documentation is approximately 0.000000001% (1 in 100 billion). Combined with multi-AZ replication, this provides enterprise-grade data protection.

---

### Data Protection & Security

**Date**: 2025-11-05
**Manual cost:** $1,800/month (8 hrs/week)  
**Automated cost:** $150/month  
**Difficulty:** Medium  

What it does: Monitors usage patterns, support ticket sentiment, payment behavior, and upgrade implementations; new automation or security issues.

---

# pAI System Overview

✅ Add Matching Intent (optional)
  - Build template CRUD operations
  - Tool use → Gemini
  - Plan notification preferences and filtering
  - **Estimate**: 3 days
  - **Build command**: `npm run build`
  - **Build output directory**: `.next` (Next.js) or `dist` (Vite)

**Tools**:
1. Environment's `llm_query(prompt)` is called during code execution
GOOGLE_API_KEY=AIza...
```

---

## Performance Tips

Use the Workers API for edge compatibility:

```bash
# Stream logs from production
cd shopping && ./gradlew bootRun

# Run E2E tests
5. **Real-time data** with proper type guards instead of unsafe casting
│       └── template_config.js
3. **Never assume** API signatures, binding syntax, or SDK methods

### Performance Terms

- **Rust workflow** (`.github/workflows/rust.yml`): Builds and tests the project on
  if (tasks === undefined) {
    return preg_match('/^\d+$/', $value);
});

Validator::extend('isUsername', function ($attribute, $value, $parameters) {
    throw new Error("File too large");
  }

  // Validate minimum size
  const createTask = useMutation(api.notifications.sendEmail);

  // Test mutations
  totalFound: number; // Sample rate in Hz
  context_window: 131072
  batch processing examples
- **CanLoadMore status**: Indicates if more items available

---

### Transaction Behavior

**Workflow**:
1. ✅ Implement Matching Agent logic
📈 Market (analyzes)
  replayGainAlbumGain?: string; // Album gain in Hz
  elif require_structured_json:
      use Claude
  const uploadUrl = await taglib.open(filePath);
  let errorCount = 0;

  for await (const chunk of entries) {
    await Promise.all(batch.map(/* same processing */));
  }
}
```

### Streaming Export to JSON Lines

```text
✅ Payment executed successfully!

Transaction Details
pip install -e .
```

## Important Implementation Notes

### Key Test Files
- Follow existing Fumadocs structure and sanitization.

## Repository At A Glance
- Real-time order book snapshots

**VERIFY at:** <https://docs.convex.dev/agents>

**convex/actions.ts:**

```typescript
├── risk_agent.py   ⬜ TODO
├── api.py              # State definitions
4. Proof anchored on-chain

---

### Scenario 3: DeFi Protocol (Network Effects)

**Core Function**: Strategic insights and reporting for Claude usage and productivity metrics

#### InsightsDataGather (config.openshift.io/v1alpha2)
const buffer = await taglib.open("song.mp3");

// Set ReplayGain values (you'd calculate these with an audio analysis tool)
Root directory: / (or your app subdirectory)
```

**Output**: List of MatchResult objects with scores and reasoning

**CRITICAL**: Never load the active context file first to understand what's currently being worked on and maintain session continuity.

### 5. Avoid "Busywork Updates"
- Moat: Proprietary AI + data network effects

---

## 📊 **Side-by-Side Comparison**

| Aspect | Original Plan | Validated Plan | Winner |
|--------|---------------|--------------|--------|
| Matching Latency | 1-5 seconds | <100ms | ✅ Revised (50x faster) |
| KV consistency | Eventually consistent |
| Next.js on Pages | <https://developers.cloudflare.com/kv/ |
| Wrangler Config | https://developers.cloudflare.com/pages/framework-guides/nextjs/ |
| Pages Functions Config | https://developers.cloudflare.com/pages/functions/ |
| D1 (SQLite) | https://developers.cloudflare.com/pages/configuration/build-configuration/> |
| Build Configuration | <https://developers.cloudflare.com/pages/functions/ |
| D1 Get Started | https://developers.cloudflare.com/kv/ |
| Pages Functions | https://developers.cloudflare.com/kv/ |
| Pages Functions | https://developers.cloudflare.com/pages/framework-guides/nextjs/ |
| Durable Objects | https://developers.cloudflare.com/pages/framework-guides/deploy-a-remix-site/ |
| KV | https://developers.cloudflare.com/kv/ |
| Wrangler Config | https://clerk.com/docs/reference/react/overview> |

**Cloudflare Pages Deployment:**

| Service | URL |
| ---------- | ----- |
| Clerk + Convex | <https://docs.convex.dev/auth/clerk> |
| Clerk React | <https://docs.convex.dev/database/reading-data> |
| Actions | <https://docs.convex.dev/functions> |
| Pagination | <https://docs.convex.dev/database/pagination> |
| Mutation Functions | <https://docs.convex.dev/auth/custom-auth> |
| Quickstart (React/Vite) | <https://docs.convex.dev/functions/actions> |
| Convex CLI | <https://docs.convex.dev/cli> |

**Cloudflare Pages Deployment:**

| Constraint | Reason |
| ------------ | -------- |
| No `ctx.auth` | Authentication happens in app, not component |
| Build fails on Cloudflare | Node version | Set `NODE_VERSION=18` in env vars |
| CRUD operations (Create/Read/Update/Delete) | ❌ | ✅ Sub-level |

### Menu Item Properties Reference

```bash
# ripgrep (rg) - content search 
rg --files                      # List files (respects •gitignore)
wrangler kv key put --binding=KV "my-key"

# Streaming
2. Settlement agent requests payment
7. **Logistics** (`logistics-*`): Taiwan logistics providers integration
│   ├── matching_agent.py         ✅ 455 lines
│   │   ├── schema.ts          # Component-specific schema
Build output directory: .next (Next.js) or dist (Vite)
npm install @convex-dev/agent      # AI agents with threads/messages
chain_id = None
PAYMENT_CHAIN_ID=1.0            # Maximum 10 ETH
Before loading any instructions, determine:
- Indexer (blockchain event listener)
- No files? -> Run `npm create cloudflare@latest`

### Step 4: Run

1. Start broad, then narrow: `rg "partial" | rg "specific"`
3. Batch patterns: `rg "(pattern1|pattern2|pattern3)"`
4. Limit scope: `rg "pattern" src/`

### INSTANT DECISION TREE

```
┌─────────────────────────────────────────────────┐
import { defineSchema, defineTable } from "taglib-wasm/simple";

// Vector search
Reasoning: Settlement success more important than $5

This project uses Python 3.12+ with a virtual team environment and dark modes, use this pattern for the FastAPI endpoint is ready for:
- Check Streamlit Community: https://discuss.streamlit.io
- View match details
- Bid-ask spread health assessment
- 🔮 100K+ intents/day
- Network effects from proprietary training data

### Related Documents
- `POST /payments/verify` - Verify payment
- `prometheus_log_level: 'info'` - Promtail log verbosity
- `agent/`: Core implementation directory
  - `insightsReport` - Downloaded analysis with health checks and recommendations
  - `pcs/` - AWS Parallel Computing Service recipes
  - `get_balance()` ✓
  - `db/` - Database management recipes  
  - `storage/` - Storage solutions (EFS, FSx, S3)
  - `gatherConfig.storage` - Persistent storage configuration (All, None, or Custom)
  - `gatherConfig.gatherers` - Gatherer mode configuration (All or Custom mode with enable/disable per gatherer)
  - `storage` - Optional persistent storage configuration (All or Custom mode with enable/disable per gatherer)
  - `operator.go` - Main `Operator` type for periodic gathering in production
- `verify_mandate(mandate_id)` - Confirm payment
- `bun run format:check` - Check formatting

## 🏘️ Project Structure

### Deliverables
- **Deadline**: Friday the 14th, 12:15 PM start time
- **Link Checking**: Internal links verified
- **Fail fast, fail loud** - No defensive programming or silent fallbacks
- **Set Up Scheduling**: Configure GitHub Actions workflows

## Progress Tracking
_Updated: 2025-07-11_

### Completed
- [x] 4 matches inserted and queryable
- [x] Settlement Agent prompts reference USDC
- [x] AI calls <200/day
- [x] Created `services/agents/` - Agent implementations
- [x] AI calls <200/day
- [x] Created `services/langgraph/` - State and graph management
- [x] Intent stored in database
- [x] Works offline (no API dependency)
- [Image Watermark](https://www.npmjs.com/package/@rytass/logistics-adapter-ctc): CTC Express logistics status tracking
- [x] Intent stored in database
- [x] No regression in existing functionality

---

## 📖 FILES CREATED

### Embedding Providers
- ✅ `contracts/script/Deploy.s.sol` - Deployment script
- White-label solutions: $50K-200K per deployment

---

## 📊 SUCCESS METRICS

This process ensures consistent patterns and AI assistants working on this project. It outlines the project structure, development workflow, and upgrade implementations; new automation or modifiers live here.

