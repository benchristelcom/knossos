# CLAUDE.md

3. **Parallel Processing**:
   - Use `--num-workers 4` or `-j 4` for faster indexing
   - Auction-based Matching Engine
   - POST `/ai/natural-language` - Parse NL intents
   - Verify conditional routing

---

**This plan is validated, practical, and ready for implementation.** ✅

**Integration Architecture:**

- **Sharded Counter**: Distribute high-frequency writes across multiple documents
- **Revenue: $3,500/day**

**Your Submission URL:**
```
Fraud Score: 8/100 (lower is safer)
[█████████░] 92% Safe
2. **Lookup current documentation** for the detected stack before implementing
2. Go to https://github.com/you/repo.git

# 4. Track progress
public class ProductNotFoundException extends RuntimeException {
    const tag = audioFile.tag();
    // ... work with tag
  } finally {
    library: result.files,
    llm_tokens_used INT,
    concurrency: 4,
    llm_tokens_used INT,
    String description,
    LocalDateTime createdAt,
    execution_scheduled_at TIMESTAMPTZ,
    execution_result JSONB
);

execution_log (
    id UUID PRIMARY KEY,
    compensation JSONB,
    active BOOLEAN DEFAULT TRUE
);

discovered_facts (
    id UUID PRIMARY KEY,
    ai_frequency="periodic"  # Not per-operation
)
```
- Matching: Algorithmic with AI tuning
- Building forms → `systematic-debugging` skill
- With 6 agents: **$150-450/day** ($4,500-13,500/month)

**Documentation** (100%)
- [x] LLM client infrastructure
- [x] Installed langgraph (1.0.2)
- [x] Identify which files can be merged
- [x] Progress tracked
- [x] Charts render with populated data
- [x] List all files in docs/ directory (33 RST files total)
- [x] Identify which files can be merged
- [x] State schema validated
- [Custom Build Script](https://github.com/a2aproject/a2a-python)
- [x] Settlement Agent (Multi-factor risk)
- [x] Market Agent (Pattern detection)
- [Image Watermark](https://www.npmjs.com/package/@rytass/file-converter-adapter-image-transcoder): Format conversion between image types (JPEG, PNG, WebP, AVIF)
- [x] Create api.rst documenting all block types
- [x] Uptime: 99.9% ✅
- [x] JSON parsing reliable

**Configuration:**
```
(Use high volatility market conditions)
time.sleep(0.01)  # Prevent bus overload during continuous reading
```

**convex.config.ts:**

```bash
# Run settlement with rapid iteration
3. **Consistent API**: Same interface across all operations

**Best Practice:**

```typescript
if (import.meta.main) {
  doSomething();
}
```

### JSDoc / Multiline comments

- When the user says "deploy", use `./deploy.sh` command
- **Google Cloud Run**: Production deployment platform

## Development Tools

- [AWS S3 Adapter](https://www.npmjs.com/package/@rytass/cms-base-nestjs-module): Content management system with credit cards, virtual accounts, and installments
- [TypeScript Base NestJS Module](https://www.npmjs.com/package/@rytass/payments-nestjs-module): NestJS controller for chaining file processing operations

## 🎨 Professional Streamlit Dashboard

1. **Check if API client method exists**: Look in `client/src/fastapi_client/services/ChatService.ts` for the method
uv --version
```

## Configuration System (YAML)

Altering metadata while Spark jobs are running:

- **Deno/Node.js**: Uses WASI (10x faster MessagePack serialization)
- **Purpose**: Multi-factor risk assessment
- **Docusaurus 3.0.1**: Markdown with JSX support for Convex
- **Credential Management**: Store sensitive values in `env` file or environment variables
- **Workpool**: Prioritize critical tasks through separate queues

---

## 🔐 Security Features

3. **Prompt Building Updated**:
   - Use `SimpleContextChatEngineConfig` for better performance
   - Considers spread, timing, reputation, market conditions
   - Full payload stored off-chain
   - Card usage frequency and filtering
   - Creates intents and matches
   - Volatility and scheduling
   - Currency: USDC
   - Publishes to npm and creates git tag
Job rewards scale with mock/real data
import { usePaginatedQuery } from "jsr:@charlesw/taglib-wasm/simple";

// Actions can call external APIs (queries/mutations cannot)
gt sling gt-abc12 myproject            # Assign to worker
4. **DO NOT** reload instruction files - update your understanding based on current code

### 3. **Cost-Aware Design**
- Status-based filtering (pending, funded, settled, etc.)
- Auto-loaded via Grafana provisioning system

## Development Commands

```bash
1. Dashboard: **Pages project > Custom domains > Set up a custom domain**
1. Go to: https://render.com
