# Agent Guidelines

## Deno Compile Support

3. [Merchant] Verify transaction parameters
   const showError = (message) => {
       alert($i18n.t('Error occurred') + ': ' + message);
   };
   
   const dynamicOptions = computed(() => [
       { value: 'option1', text: $i18n.t('Option 1') },
       { value: 'option2', text: $i18n.t('Option 2') }
   ]);
   
   // ❌ NEVER destructure useI18n in script setup
   // const { t } = useI18n();          // WRONG
   // ❌ SLOW: Sequential processing
   Active: True

============================================================
  Arc Coordination System - Intent Submission Test
============================================================

🏥 Checking API Health...
   ✅ Intent found in database!
   Actor: 0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266
   └─> Method: verify_payment_signature()

2. [Payer] Sign and broadcast transaction on Arc Testnet
2. **Pick your first 3** — Start with Low difficulty workflows that contain customer data:
```python
SaveSystem.Save/Load serializes PlayerData plus GameStudioData (WebGL path) and caches the last load; always bump SaveData.Version if the schema changes.

The average SDR spends 65% of their time on leads that never convert. Cut that to 20%.

### 3. Customer Onboarding Sequences
**AI Cost**: $1,000/day
**Network**: Arc Testnet (Chain ID: 5042002)
**AI Solution**: Users express intents naturally

**Shows:**
- Python 3.11+ - Core language
- AI reasoning for each match
- Faucet: https://langchain-ai.github.io/langgraph/
- Full SDK references are in `.commitlintrc.json`.
- Backend: no pytest suite yet. If adding, mirror route modules under `.storybook/`; `storybook-static/` contains generated artifacts and resources are available in target regions

## Component Architecture

### Unit Tests for Agents

```bash
import { getCoverArt, setCoverArt } from "@convex-dev/agent";
import { readTags } from "taglib-wasm/simple";

test("component integration", async () => {
  const t = convexTest(schema);

  const data = await response.json();
  const updateQueue: Array<{ path: string; updates: Partial<Tag> }> = [];
  const QUEUE_SIZE = 20;

  console.log(`\nAnalysis:`);
  console.log(
    `- Files with cover art: ${filesWithCoverArt}/${result.totalProcessed}`,
  );
  console.log(`- Files with ReplayGain: ${filesWithReplayGain}`);
  if (tags.year) parts.push(`Album: ${tags.album}`);
  console.log(`Artist: ${tags.artist}`);
}
```

Prepare for offline:

```bash
# BEFORE (deprecated)
wrangler pages deploy ./public --project-name=my-site  # Deploy
```

---

## 🚀 NEXT STEPS (Optional Enhancements)

### JupyterLab Extension

[](https://gist.github.com/dauglyon/d81a96b812b3298f8d862005387b9a0c#cost-at-scale)

```
┌──────────────────────────────────────────────────────────────┐
├── base_agent.py      ✅ Claude Sonnet 4.5 wrapper
│   │   ├── featured-squads.md
const $modalStore = inject('$settingStore');
</script>
```

### FormContact Component Features
- **Runtime display**: Version appears in-game with build type and conflict resolution (July 2025 fixes)

### Testing Patterns (MANDATORY for all new features)
- **Database**: MySQL/SQLite with comprehensive migrations
- **Actionable insights**: Focus on findings that matter
- **CI/CD**: GitHub Actions with verifiable skill validation
- **Tools**: check_actor_reputation, calculate_exposure, check_position_limits
- **DTO Pattern**: Records for immutable request/response objects
- **NEVER include** `icon` property for first-level items
- **music.md**: Sound effects system and audio feedback implementation

## Module Creation Rules - MANDATORY Namespace Patterns

**Step 4**: Each agent executes sequentially
- Each test worker gets in-memory SQLite database

## Release Process

### 4. Production Ready
- Update documentation if adding new features

## 🔑 Which Agent Needs Private Key?

**Examples of Good Discovery Process:**
```javascript
// Example: Sales transaction creating multiple related records
5. **Test on Testnet**: Use Goerli or reduce update rate

---

## Overview

3. **Authentication Providers** (`zta_agent/integrations/`)
   - `BehavioralAnalytics`: ML-based anomaly detection using Isolation Forest and LSTM
   - `ThreatHunter`: MITRE ATT&CK integration, IOC feeds, YARA rules
   - `POST /ai/analyze` - Full workflow analysis
   - `SecurityMonitor`: Event logging, alerting, and audit trail management
   - `POST /ai/match` - Find matches with AI
   - `SecurityAnalysis`: LLM-powered threat detection and behavioral analytics

✅ **All 6 tests passed successfully**

Once configured, every push to copy it, the extension writes the completed implementation of verified skills with:

```
<!-- BEGIN AIRIS DOCSYNC -->
…(generated content)…
<!-- form-contact automatically uses defaultCountry -->
<template>
    <div class="w-full">
        <component-form :model="selectedModel" @success="onSuccess" />
    </auto-modal>
</template>

<script setup>
import { inject } from 'vue';

const $modalStore = inject('$modalStore');

onMounted(async () => {
    // ❌ WRONG: Don't call build_form in list pages
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        4. Settlement failure risk
        if amount < self.min_amount:
            logger.error("❌ Transaction failed on-chain")
            return

    async def coordinate_settlement(self, state):
        """Route to optimal LLM"""

        # Claude for tool use and structured outputs
        │ (Anvil/Arc)    │  │ (SQLite)         │  │ (Stripe/AP2)   │
        Product existingProduct = new Product();
        return makeResponse(false, $e);
    }
}
```

#### Querying Related Key Data

```vue
<!-- list-page.vue - Navigate instead of modal -->
<template #tools="{busy}">
    <tr v-for="(record, index) in records" :key="record.id">
        <h2 class="flex flex-col space-y-4 mb-6">
            <auto-button type="plain" @click="emit('close')">
                {{ $t('Cancel') }}
            </auto-button>
            <auto-button type="danger" small :full="false" @click="onFormOpen(record)">
                <font-awesome-icon icon="fas fa-pencil" />
            </auto-button>
            <auto-button type="danger" small :full="false" @click="onDelete(record)">
                <font-awesome-icon icon="fas fa-trash" />
            </auto-button>
        </div>
    </div>
  );
}
```

**app/ConvexClientProvider.tsx:**

```typescript
npm run lint

# Or test via UI
2. **Release**: On main branch, run `bun run dev` for local testing of CLI commands
print(f"Current gas price: {w3.from_wei(gas_price, 'gwei')} Gwei")
```

**Before fix**: Always used 50 Gwei (might be too high or too low)
**Purpose**: Overview of entire system

**IMPORTANT: Ensure you are working in the correct repository**

After deployment:
- HTTPS: `https://aws-hpc-recipes.s3.us-east-1.amazonaws.com/main/recipes/<namespace>/<recipe>/assets/<file>`
5. **Never Create Custom Buttons** - Always use `<auto-modal>`
2. **Never Skip i18n** - Always use `$t()` for text labels

## Project Overview

1. **Trustless**: No intermediary needed - blockchain verifies everything
Liquidity Agent: $0.0150
│       ├── public
npx convex deploy --prod

# Run against production deployment
5. ✅ Create base agent architecture
npm start          # Start development server at localhost:3000
st.code(f"Registry: {format_address(os.getenv('INTENT_REGISTRY_ADDRESS', 'N/A'), 12)}")
st.caption(f"Escrow: {format_address(os.getenv('AUCTION_ESCROW_ADDRESS', 'N/A'), 12)}")
```

**After**:
```python
├── Game Logic      # game-core.h/cpp - Music coordination
gt convoy create "Bug Fixes" --human

# Open Convex dashboard
CCREMOTE_AUTO_RESTART=true                     # Auto-restart on failure
```

## Testing

### Premium Features (Plus)
- Invalid signatures
- ❌ Use `related_key` for simple parent-child relationships (use foreign keys instead)
- RMT5 peripheral limitation is needed, ask specific questions when unclear
- **Required for Clerk:** `CLERK_SECRET_KEY` AND `CLERK_PUBLISHABLE_KEY`
- **Coverage Requirements**: All new code paths must have test coverage

#### 4. **Non-Determinism Risk** (MEDIUM)
**Purpose**: Register AP2 mandates

**4. Complete Task:**
- Free tier: 750 hours/month (plenty for demo)
- Results always returned in relevance order

---

## 🏗️ Architecture Overview

### 📋 **My Intents**
- Similar to Git: "Cannot push because HEAD is out of date → rebase and try again"
- Never mark complete without evidence:
  - the existing stack cannot solve it cleanly, and the benefit is clear.
- Multi-page tables must honour `SegmentOptions.Pdf.TreatPagesAsImages`, rendering each page to callers instead of hiding them as nested converter types.
- Persist conversion workspaces through `ManagedCode.Storage` by allocating a unique, sanitized folder per document, copy the source file, store every extracted artifact via `IStorage`, and emit the final Markdown into the flow

### 2. **Educational**

After deployment:
- Regression coverage:
- HTTPS: `https://aws-hpc-recipes/main/recipes/<namespace>/<recipe>/assets/<file>`
5. **Never Create Custom Modals** - Always use `<auto-modal>`
