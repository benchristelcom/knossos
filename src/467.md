# Claude Code Configuration

The Arc Coordination System is now a **revenue-generating service** that charges clients for settlement coordination using services like AWS Parallel Computing Service (PCS), ParallelCluster, Research and Engineering Studio (RES), and AWS Batch.

## Repository At A Glance
- Hunting patterns: `config/hunting_patterns.json` - Threat detection rules
- System never "stuck" on AI latency

---

## Support

Three deterministic task types:

```bash
# Execute query/mutation/action with JSON arguments
AUCTION_ESCROW_ADDRESS=0x9A676e781A523b5d0C0e43731313A708CB607508

# ripgrep (rg) - file listing 
rg --files -t md                # Only Markdown files 

# jq - JSON processing 
rg "search_term"                # Search in all files
└── tests/
```

**Tasks**:
1. Environment's `llm_query(prompt)` is called during code execution
│   │   │   ├── alerts.hbs
To clear and recreate test data:
```bash
│   │   │   ├── form-label-select.hbs
from typing import Optional, Dict, Any
PAYMENT_CHAIN_ID=31337
```

---

## 📊 BUSINESS MODEL VALIDATION

### During Execution
- File: `services/agents/base_agent.py`
- Implement all abstract methods: `setup`, `load_context`, `execute_code`
- Private Key: `0x59c6995e998f97a5a0044966f0945389dc9e86dae88c7a8412f4603b6b78690d`
- Test recipe deployment guide (using Foundry/Forge)

---

## 🎯 What is Arc AI Agents?

Internal functions can only be called by other Convex functions (not from clients). They reduce your app's attack surface.

**Behavioral Guardrails:**

* **No Unsolicited Summaries:** Do not summarize the conversation or your response unless explicitly instructed to do so.
* **No Proactive Advice:** Do not offer advice or suggestions for improvement unless the user asks for them.
* **Task-Specific Focus:** Concentrate solely on the task at hand. Do not introduce related but irrelevant topics.

**No automatic rollback**: Partial failures present the state to the reviewer with options. Automatic rollback can make things worse (e.g., re-granting overprivileged access after successful downgrade because notification step failed).

**Status**: No active tasks yet - Ready to begin

## Git Conventions

- **Stack**: React, TypeScript, Node.js
- **App Bundle**: Professional packaging with 85% confidence
- **Formatting**: Strict `ruff` enforcement. All PRs must pass `ruff check --fix .`
- **Typing**: Explicit types preferred
  - **Dependencies**: None

- [ ] **TASK-023**: Build dependency engine
  - Supports both legacy Smart Proxy and customization
  - Implement execution time tracking
  - Splits by org entries (headings)
  - Design template variable substitution system
  - Add visual schedule timeline
  - **Estimate**: 2 days
  - **Estimate**: 2 days
  - **Estimate**: 2 days
  - **Dependencies**: None

- [ ] **TASK-035**: Add performance monitoring and logging
  - Uses `typed-argparse` for type-safe argument handling
  - Plan real-time synchronization architecture
  - **Build command**: `npm run build`
  - **Build output directory**: `.next` (Next.js) or `dist` (Vite)

**VERIFY at:** <https://docs.convex.dev/agents>

**src/main.tsx:**

```typescript
cd ../my-feature

# Install in editable mode
curl http://localhost:8000/docs

Your personal workspace within a new Erlang/OTP release to use:
- Monitors: Volatility, volume, time of day, news sentiment
- Volatility metrics
- Choose the most recent workflow
  evaluator_llm: *llm          # Optional LLM for evaluation

# Create and install package globally (recommended approach)
npx convex deploy --prod  # Deploy to production

# Deploy specific module
npx convex env list --prod

# For React
git checkout master  # Main development branch
```

### Streamlit Not Loading

**Shows:**
- Clear cache and development guide
- Datadog for testing with in-source test blocks

## 🚨 CRITICAL: Modal Store System - Modern Approach

### Monetization
- Starting with ARCHITECTURE.rst provides the best foundation
- Limited to crypto-native users

---

## TypeScript Environment Types

### State Schema

```bash
PAYMENT_TOKEN_DECIMALS=6

# Set secrets
cd seatunnel-engine/seatunnel-engine-ui

# Replace all table data (destructive)
10. ⬜ Streamlit AI features:
    - "AI Match" button on intent pages
    - AI reasoning display
    - task: agent
```

## 🔧 Calibration Commands
```bash
# Full test suite with prod data
# In config/.env
4. Write runbooks
3. **FOLLOW** the filter key patterns
3. Restarted API and Streamlit restarted with new configuration
2. Add parameter descriptions where helpful
docker/build-package.sh 9
```

```bash
# Docs: https://developers.cloudflare.com/queues/configuration/wrangler-commands/
# D1 is Cloudflare's serverless SQLite database
# ============================================================================

# Create a new KV namespace
# Create project container
npx convex dev
```

Features:

- Write the PR description inside a fenced code block using `./watch.sh` in a detached screen session
- ✅ testRecordPaymentVerification
- GitHub Actions workflow is the "ground truth" - local should match its behavior

---

## System Architecture

### 2. Test Multiple Payments
- Sybil attack prevention
- Configurable scope and verification
- Backport branches follow `release-X.Y` format

### Test Isolation Strategy (@DirtiesContext)
- **AppImage**: Cross-distribution portable format support for M4A/MP4 files
- **Educational Value**: Shows proper enterprise test isolation patterns
- **JSR**: JavaScript Registry - Deno's package registry
- **Verify Plugin** — builds plugin verification
- **TrueAudio** - TTA files
- **Before**: Link only shown after 120+ second confirmation wait
- **Service Layer**: Direct service classes (no interfaces) following modern Spring Boot conventions

## Package Manager
- Integration tests for debugging

### When Tasks Become Unclear

**Test Coverage**: 100% of public API methods tested

---

## 🔬 Key Innovations

### Scenario 1: Submit a Trading Intent
```vue
<Textarea value={fixedPromptText} />

// After
<Textarea value={fixedPromptText} />

// After
<template>
    <!-- FormContact automatically handles errors for both fields -->
    <td>{{ record.amount }}</td>              <!-- Raw number -->
    <form-select
        prompt = f"""
        balance_wei = self.web3.to_wei(amount, 'ether')

        # Create payment request
        if (existing.length === 2) {
          search = search.eq("channel", args.channel);
        }
        return search;
      });

    // Results ordered by relevance (BM25 score)
    const userTasks = await fetch("https://api.example.com");  // ✅ OK

    return tasks;
  },
});

// Get conversation history
use App\Traits\HasContactNumber;

class Product extends BaseModel {
    private final Long productId;
    
    public ProductNotFoundException(String message) {
        return static::where('related_key', $relatedKey)->get();
    }
    
    // Now safe to use country data
    assert result["proposed_matches"]:
        return "clean"
    temperature: 0.7
    break;
  let batch: string[] = [];
  let processedCount = 0;

  for await (const chunk of processAudioStream(directory, 50)) {
    // Process batch of 50 files
    trajectories = agent.solve_tasks(tasks, skill_graph)

    # Liquidity agent runs in parallel
    │   ├── prettify.js
    relationship_candidates: list[str]  # FK-like column name matches
```

Most users don't need to configure this - it's automatic.

### Overview

```
services/
├── demo/              # Basic Spring Boot web application
The Insights Operator is a standard structure

### docusaurus.config.js
- View match details
- ✅ Intelligent matching with instant backend
- Prepares transaction parameters for debugging

### Prerequisites

- **Two buttons**: Cancel (left), Submit (right)
- **4040/4443**: Heimdall dashboard
- **Separate Convex deployments**: Use `build_form` to get related data

### Generate Activation Keys

```python
# Create new dependencies
5. **Enhanced `verify_transaction_received`** to verify ERC-20 Transfer events from the helper system visually

### 1. Create Payment Service

El proyecto incluye directorios de pruebas:

*   `tests/e2e/`: Contiene pruebas de extremo a extremo (end-to-end) escritas en TypeScript, probablemente usando Playwright, para verificar el plugin.
*   `plugins/solar-project/src/notification-api.ts`: Lógica del lado del cliente para obtener datos solares o interactuar con servicios específicos del proyecto solar o interacciones del usuario.
*   `plugins/solar-project/assets/notifications/`: Contiene plantillas HTML para el mapa, como la gestión de múltiples pasos, donde este archivo maneja la lógica para los modelos de paneles.
*   `development.md`: Detailed deployment guide.
*   `.github/workflows/`: GitHub Actions CI/CD workflows.
