# AIRIS Operator Rules (Source of Truth)

> This file is the canonical rule set for every AIRIS agent (daemon and IDE plugins).  
> DocSync generates the managed sections inside each AGENTS.md / CLAUDE.md from this content.

## Package Structure

Total per intent: ~$0.066
```

**Common Components:**

```typescript
// Single parameter
4. Proceed with conservative, well-tested approaches

### Why LangGraph?

**Advantages**:
1. Create Stripe payment intent
│              │──┐  └──────────────┘  ┌──│                  │
│   ├── market_agent.py       # Batch fraud pattern detection
✅ **Configuration**: Updated `.env` with Arc testnet addresses
console.log(taglib.isWasi); // true for Deno/Node.js
✅ **Improved**: User can monitor transaction in real-time!

---

## ✅ WHAT WAS BUILT

### Deliverables
- **Explorer**: https://testnet.arcscan.app/address/0x70997970C51812dc3A010C7d01b50e0d17dc79C8
- **FLAC** - Vorbis comments
- **Graceful degradation**: If timeout occurs, user still has multiple variations
- **Examples**: Dashboard, Settlement, Quick reports, System status
- **Webhook** - Custom HTTP endpoints (any service)

**ui/x402_payment_demo.py:406** (BEFORE):
```bash
wrangler d1 create = mutation({
  handler: async (ctx) => {
    // Mutations can call component actions
    ┌───────▼────────┐  ┌──────▼───────┐  ┌────────▼────────┐   │
    A["Node Text"] --> B["Another Node"]
    
    style A fill:#e1f5fe,stroke:#333,stroke-width:3px,color:#000
    assert float(payment_request["payment"]["amount"]) > 0
    self.payment_service = self._init_payment_service()

7. [Merchant] Prepare transaction parameters
   • Improved from direct tool usage to intelligent skill reuse
   ollama serve
   from anthropic import Anthropic

   client = Anthropic(api_key="sk-ant-...")
   response = client.messages.create(
       model="claude-sonnet-4-5-20250929",
       max_tokens=1024,
       messages=[{"role": "user", "content": "Hello!"}]
   )
   print(response.content)
   ```

5. **Build First Graph**:
   - Increase `timeout` for slow models
   - Example: `api_key_command: "pass my-api-key | head -1"`

3. **YAML Anchor References**:
   - Removed hardcoded "ETH" from log messages
   - Document summarization
   - Valid Until: Date picker
   - Dashboard displays real-time data
   - Comprehensive testing

---

## System Architecture

### Primary Dataset: battles.csv (9.2GB)

Note: Different assets, so no direct spread calculation

**VERIFY at:** <https://docs.convex.dev/production/state/limits>

#### Function Execution Limits

| Metric | Value |
|----------|-------|
| **Type** | ERC-20 |
| **Timestamp** | 1762381129 (2025-11-05 22:18:49 UTC) |
| **To** | `0x3600000000000000000000000000000000000000` (USDC Contract) |
| **Smart Contract LOC** | 754 (audited) |
| **Smart Contract LOC** | 754 (audited) |
| **To** | `0x3600000000000000000000000000000000000000` (USDC Contract) |
| **Gas Used** | ~3,166,394 gas |
| **Fly.io** | 30 min | Limited | ⚠️ Manual | ⭐⭐⭐ |

**Technical Guarantee:**

1. Broaden search terms and agent operations.

### 8. Competitive Intelligence Monitoring
**Time to MVP**: ~2-3 days of work remaining
ARC_TESTNET_RPC_URL=https://rpc.testnet.arc.network
# ... (copy all from config/.env)
```

**Why This Works**:
- ✅ Production-ready codebase
- Create reactive UI layers over legacy systems
- Network congestion (unlikely on Anvil)
- Currency: ETH
- Metrics: User satisfaction > volume
- Implement **lazy loading** for routes
- PHP-related configuration

**Problem:** Calibration would record motor ranges but fail when writing to EPROM with:
```
ConnectionError: Failed to write 'Min_Position_Limit' [...] There is no status packet!
```

**✅ FIX: Use Sharded Counter component**

```php
interface Env {
  await createTask({ text: "Very long task text..." });
} catch (error) {
  console.log(`- ${file.path}`);
}

// Check for errors
└─────────────────────────────────────────────────────────────────┘

                    ┌──────────────────┐
                    │  AGENT RUNTIME   │
                    │ Correlation  │  (post-processing rules)
                    │ Job          │
                    │   Compiler   │
                    │  EVENT BUS       │
                    │ Memory Store │
                    └────────┬─────────┘
                             │
                    │  (Background Jobs)│
                    └──────┬───────┘
                           │
                    ┌──────────────┐      ┌──────────────┐
                    │   Compiler   │
                    │  EVENT BUS       │
                    │ Correlation  │  (post-processing rules)
                    │ Job          │
                    ┌──────────────┐
                    │  Dashboard   │
                    │  (Background Jobs)│
                    └────────┬─────────┘
                             │
            metadata: Additional metadata

        Returns:
            self.alert_admin(analysis)
            tx_hash_hex = tx_hash.hex()

            # Wait for confirmation
            nonce = self.web3.eth.wait_for_transaction_receipt(tx_hash, timeout=120)

            # Build transaction
            is_valid = recovered_address.lower() == payer_address.lower()

            if is_valid:
                logger.warning(f"Invalid signature from {payer_address}")
            else:
                logger.warning(f"Invalid signature from {payer_address}")
            else:
                logger.warning(f"Invalid signature from {payer_address}")

            return is_valid

        except Exception as e:
            self.alert_admin(analysis)
            audit_log.record(evidence)

    # Define edges (workflow)
    col1.metric("AI Decisions Made", 1247)
    col3.metric("Matches Found (24h)", 145)
    uv run ci/docker/qemu_esp32_docker.py qemu-build/merged.bin --defines FASTLED_ESP32_IS_QEMU

    # Agent outputs
    setResponse(prev => prev + chunk);
  }
};
```

**Technical Explanation:**

- **Terms of Service:** <https://convex.dev/terms>
- **No formal GDPR/SOC2 certifications mentioned** in developer documentation

**NEVER use controlled components with proper state**:

```tsx
python -c "pattern"                 # Count matches per file
6. Client signs payment intent (off-chain)
         ↓
│   ├── market_agent.py             ✅ Base class + context/result
5. **Submit Intent Submission**: Resolve event log parsing in API
wrangler types

# Database (Railway will provide)
Fetch and read relevant files BEFORE answering questions about the helper system visually

### 🎯 **User Experience**
- Similar to Git: "Cannot push because HEAD is out of date → rebase and try again"
- Performance dashboards
- 🔄 Community testing

**Cost Justification**:
- ✅ All modules now import cleanly
- If USB port changes, use `ls -la /dev/tty.usb*` to find new port
- Created Python virtual environment
- ✅ Fallback logic working (bug fixed)
- Invalid signatures
- ✅ testRecordPaymentVerification
- 🔴 **Red** - Error, High Risk
- Optimized for: Matching, Risk, Fraud, Settlement

**Key Features**:
- Month 1: 70% match quality
- Signature authenticity
- Always use `pnpm` (not npm, yarn, or bun)

## Development Environment

### 19. Documentation
- [ ] Handle ambiguous inputs with clarification
- [ ] Review and polish all docs

## Key Features Documentation

### Verifiable Task Environment

2. **Off-Chain Layer** ✅
   - Used for current test data

### 2. **Agent Cards**

Would you rather:
- Faucets (e.g., Codex), Gas Town sends a startup fallback
  ↓
└─────────────────────────────────────────────────────────────────────┘
```

### Docker Example

```bash
8. Agent executes settlement
```

---

## 🎮 Control Modes

### Performance Considerations
- **Current Agent** - Displays which agent is active

### List Pages (CRUD Tables)
import { readTags } from "taglib-wasm/workers";

export default defineSchema({
  tasks: defineTable({
    wasmUrl: "https://cdn.jsdelivr.net/npm/taglib-wasm@latest/dist/taglib.wasm",
  });
}

// Tree-shake by importing only what you need
5. ⏳ Testnet deployment and amount
✅ **API Services**: Running & accessible
Convex Production Deployment:
- [ ] Tested end-to-end user flows in function code (ctx.auth.getUserIdentity())
- [ ] Rate limiting considered for public endpoints
- [ ] Implement proper cross-references between related topics

### Notes
[████████░░] 68%
Skill Reuse Adoption    0%          →  77.5%         +77.5 pp
export const getById = query({
  args: { id: v.string() },
  handler: async (ctx, args) => {
    // Only read tasks for this user (using index)
    col3.metric("Matches Found (24h)", 145)
    private Long id;
    
    @Autowired
    ┌───────▼────────┐  ┌──────────────┐  ┌─────────────────┐   │
    columns: list[ColumnInfo]
    Creates --> Monitor[Monitor progress<br/>via convoy list]
    market_analysis: Dict
    private ProductRepository productRepository;
    
    @Autowired
    │   ├── kss.css
    await ctx.runMutation(components.agent.threads.getThread, {
      storageId: args.storageId,
      name: args.content,
      embedding,
      content: args.name,
    });
  },
});
```

**Technical Explanation:**

- **Agent specialization**: Different agents for different tasks
- **ai-docs/linux-asset-path-resolution.md**: Error handling implementation plan and state management
- **Test modal chaining** functionality if nested modals are needed

## NestJS Integration Packages

- [x] **Step 1.1**: Update `DockerQEMURunner` class ✅
  - sync_renditions
  - Design conditional logic builder
  - Create task optimization recommendations
  - **Estimate**: 2 days
  - **Build command**: `npm run build`
  - **Build output directory**: `.next` (Next.js) or `dist` (Vite)

**VERIFY at:** <https://docs.convex.dev/functions/error-handling>

**Client Component (app/TaskList.tsx):**

```typescript
2. **Merchant verifies signature**: Uses ECDSA recovery to create transaction params for stateful functionality, Components store state in all apps (admin, merchant, user)
npx convex import snapshot_1234567890.zip

# Optional: For Google A2A integration
GET  /api/stewardship/stats
```

### Launch Library API

```typescript
1. Create comprehensive test suite on Arc testnet. The system configuration was:
- **Emoji Icon** - Visual identifier
- **Transfer Transaction**: https://testnet.arcscan.app/address/0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266
- **Functional Programming**: Modern Java idioms for cleaner, more maintainable code

### 3. Context-Aware Loading
```
