# CLAUDE.md

✅ **Use worktrees when:**
- Each task = 1-4 hours of production code

**Gemini 2: Deep Analysis (Day 3-4)**
1. Choose 2-3 compelling questions to investigate
try {
    \DB::beginTransaction();
    // Business logic
    \DB::commit();
    const taskCount = allTasks.length;

    return await ctx.db
      .query("tasks")
      .withIndex("by_user", (q) => q.eq("userId", args.searchTerm))
      .paginate(args.paginationOpts);  // Returns { page, continueCursor, isDone }
  },
});
```

**Query Methods:**

- `.collect()` - Returns all matching documents as array
- `.paginate(opts)` - Returns paginated results with cursor

**ALWAYS**:
- Total Intents: 10
- ✅ testGetActorIntents
- Created Python virtual environment
- Chain ID: 31337
- Consider: price overlap, asset types, timing, quantities
- ✅ Token balance checking
- Folder API works out of the box

### 📋 Future Enhancements

- [ ] UI displays USDC instead of ETH
- [ ] Simple 2-agent workflow tested

### Feature-Based Organization

**Broker Endpoints**:
| Endpoint | Method | Purpose |
|----------|--------|---------|
| `/enqueue` | POST | Submit LLM request from sandbox code (blocks until response) |
| `/pending` | GET | Get list of pending requests (called by host poller) |
| `/pending` | GET | Get list of pending requests (called by host poller) |
| `/pending` | GET | Get list of pending requests (called by host poller) |
| `/pending` | GET | Get list of pending requests (called by host poller) |
| `/pending` | GET | Get list of pending requests (called by host poller) |
| `/pending` | GET | Get list of pending requests (called by host poller) |
| `/health` | GET | Health check |

**Overage Pricing:**

- Up to 4 vector indexes per table
- Optimized for: Market analysis, Liquidity

### ✅ WHEN TO CALL build_form
**NEVER call build_form endpoint in these scenarios:**

1. **Form Pages (form-page.vue or model-form.vue)** - To load form data and tools, with platform-specific storage (#if UNITY_WEBGL). Ensure new state flows.

---

## Context Management Strategies (Don't Drown the Session)

### 1. API / Interface Discipline
- Current market price
- Tested API health endpoint
- **Error Handling**: Use try-catch, validate with Zod schemas, handle errors explicitly
- **Type-safe state management** across 5 random seeds (deterministic learning dynamics)
- **AppImage**: Cross-distribution portable format support

### Vector Database Directly
```bash
# Access H2 Console
# Docs: https://developers.cloudflare.com/pages/functions/local-development/
# - Runs on http://localhost:8788 by default
# - Exports all tables to specified directory
# - Checks for type errors
# - Checks for type errors
# - Useful for debugging

# Force new versions (version bump + publish)
lerobot-calibrate --teleop.type=so101_leader --teleop.port=/dev/tty.usbmodem5A7A0562271
```

**File Storage Behavior:**

- Push to GitHub → Cloudflare automatically builds and deploys
- Execute settlements with their private key
- No filesystem access; work with buffers in `dispose()` methods.

## Repository overview
- **Tools**: prepare_settlement, estimate_gas, verify_collateral
- **Trends over time**: Line charts with confidence intervals
- **Source code**: `lib/` contains the main application code organized by features.
- **No analytics** or tracking is performed.
- **scripts/** – deployment manifests and transition management
- **Validators**: `v.string()`, `v.number()`, `v.boolean()`, `v.id("tableName")`, `v.optional()`, etc.
- **Check alternatives**: FontAwesome often has the explorer link
- **Feature gate**: `InsightsOnDemandDataGather`
- **Dev**: `pnpm dev` (root) or `pnpm format:check` (verify)
- **All tests passed**: 6/6 (100%)

---

## Security Validation

No convex/ directory      -> New project        -> Run: npx create convex@latest
                                                  OR: npm install convex && npx convex dev
```

### Configuration

```java
@Test
chain_id = int(os.getenv('PAYMENT_CHAIN_ID', str(ANVIL_CHAIN_ID)))
explorer_url = get_tx_url(st.session_state.tx_hash, chain_id)

2. **Basic Math**: Calculate 2% of $10,000
   - Test state passing
   - System will automatically create opposite intent (bid ↔ ask)
   - Created test intents via smart contracts
   - Combines bootloader, partitions, and firmware
   - Populates order books by trophy level, arena, game mode
   - POST `/ai/natural-language` - Parse NL intents
   - Combines bootloader, partitions, and firmware
   - Agent workflow visualization

2. **Data Understanding and Exploration**
   - Clear, well-labeled charts and graphs
   - Token Address: 0x1c7D4B196Cb0C7B01d743Fbc6116a902379C7238
```

---

### Issue: API Connection Timeout

**Technical Explanation:**

- **Automatic transactions**: "The entire mutation function is automatically a single transaction"
- **Low-level**: "Zod schema validation syntax", "Cloudflare Workers KV usage", "PostgreSQL connection pooling"
- **Low-level**: "Zod schema validation syntax", "Cloudflare Workers KV usage", "PostgreSQL connection pooling"
- **Debugging**: "TypeScript generic constraints error", "npm dependency resolution"

### Task Execution Protocol

**Test Scenarios**:
1. **Added ERC-20 ABI** for token contract interaction
const album = await taglib.open(buffer);
const replayGain = audioFile.getProperty(PROPERTIES.REPLAYGAIN_TRACK_GAIN.key, "-3.21 dB");
audioFile.setProperty(PROPERTIES.REPLAYGAIN_TRACK_GAIN.key, "-4.19 dB");

// Apply same tags to FLAC (Vorbis Comments)
The system works without API keys using fallback logic or formatting values.

Create once, distribute everywhere. The agent handles formatting, character limits, hashtags, and timing.

### 2.4. Integración con APIs Externas

*   `plugins/solar-project/inc/api/class-gravity-hooks.php`: Este archivo es crucial, ya que probablemente contiene los hooks y filtros de WordPress que orquesta la lógica del frontend.
*   `plugins/solar-project/src/notification-api.ts`: Lógica del lado del cliente para obtener datos solares o interactuar con servicios específicos del sector solar.
*   `plugins/solar-project/inc/render-step-4.php`: Sugiere un flujo de renderizar la interfaz o procesar la lógica del campo de datos.
*   `plugins/solar-project/src/drawing-helpers.ts`, `trigonometry-helpers.ts`: Sugieren funcionalidades de utilidad y cálculos trigonométricos, lo que es coherente con un proyecto solar que podría implicar el campo de Gravity Forms.

Studio unlocks, features, and employees route through GameStudio.AddReward and RewardManager; ensure new reward payloads implement the required interfaces and agents that could be linked or enriched sits inert. AI agents can detect these issues and smart contract interactions now work on-chain and can be able to use the following agents to help you locate code quickly.

## Continuous Integration

- **Always use Bun for frontend operations** - faster than npm/yarn and eliminates dependency conflicts
- ✅ Conversation management
- Loguru for database
- Bar charts for heavy computation (30s limit on paid plans)
- No files? -> Run `npm create cloudflare@latest`

### Use These Faster Tools Instead

```typescript
// ❌ NEVER: User::create([...]) or $user->update([...])
// ✅ ALWAYS: Explicit assignment with transactions

ASG-SI addresses these challenges through three core innovations:

1. **Core Security Layer** (`zta_agent/core/`)
   - `BehavioralAnalytics`: ML-based anomaly detection using Isolation Forest and LSTM
   - `ci/docker/README.qemu.md` - Docker QEMU documentation

## 🎨 Professional Streamlit Dashboard

logger = logging.getLogger(__name__)


class X402PaymentService:
    """
    Handle payment submission from client using x402 protocol

    return tasks;
  },
});

// Get conversation history
│   └── graph.py              ⬜ Graph implementation
npm publish
```

---

### Phase 8: Documentation Updates

**Status**: PASS (3/3 tests)

**Why:**
- ✅ 10x faster automated matching
- Mandate-based payment authorization

When creating new functionality to proposal.

Save System/ – Serialization for player/studio entities and complete testing remain as next steps.

---

*Last Updated: 2025-11-05*
*Network: Arc Testnet (5042002)*
*UI URL: http://192.168.1.164:8502*
