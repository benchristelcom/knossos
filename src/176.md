# SO-101 Setup & Configuration Guide

## Deployment

The site uses a git repository and dark modes, use this workflow:

1. Edit `app_params.yml` for text and variable definitions
npm run typecheck    # Check types

# Instead of os.getenv()
api_key = st.secrets.get("ANTHROPIC_API_KEY", "")
```

Update your app to check for Streamlit secrets first:
```powershell
✅ Proper error handling and logging
