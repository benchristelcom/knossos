# CLAUDE.md - Cloudflare Quick Template

## Progress Tracking & Status Updates

### Mayor Workflow

```
┌─────────────┐     ┌──────────────┐     ┌─────────────┐
All services support configurable log levels in `seatunnel-e2e/seatunnel-connector-v2-e2e`
5. **Set payment limits** - Adjust `MIN_PAYMENT_AMOUNT` and `MAX_PAYMENT_AMOUNT`
2.  Install the Python dependencies using fallback logic, but for full AI capabilities:

```bash
# Should output: 5042002 (for Arc testnet)
```

### Step 2: Existing Project - Adding Archon

```toml
# services/run_agents.py
const album = await initializeForDenoCompile();

// ID3v2 specific
const dt = ref({
    // Single contact pattern
    Start([Start Mayor]) --> Tell[Tell Mayor<br/>what to build]
    Rig1 --> Polecats1[Polecats]

    Rig1 --> Crew1[Crew Member<br/>Your workspace]
    Monitor --> Done{All done?}
    Done -->|No| Monitor
    style Town fill:#f3e5f5,stroke:#333,stroke-width:3px,color:#000
```

**Business Value**:
- ✅ Router initializes correctly
- DeFi applications
- Requires technical knowledge
- Show loading indicator on buttons
- Update `PAYMENT_DEMO_GUIDE.md` with Arc testnet explorer

### Complete Settlement + Payment Flow

```
1️⃣ Payer Creates Payment Intent
│   ├── graph.py                  ✅ State schema
│   └── img/                       # Images and icons
curl -X POST https://jsonplaceholder.typicode.com/users/1
echo "use flake" > .envrc
└── ...
```

**Example `functions/api/hello.ts:**
```php
// Transaction A: Reads Alice's account (v1: $14)
const audioFile1 = await getCoverArt("song.mp3");
console.log(`Album: ${album.tracks[0]?.album}`);
console.log(`Tracks: ${album.trackCount}`);
console.log(`Tracks: ${album.trackCount}`);
console.log(`Duration: ${Math.round(album.totalDuration / 60)} minutes`);
console.log(`- Apple Sound Check: ${soundCheckFiles.length} files`);
console.log(`Successfully processed ${result.totalProcessed}`);

// Access metadata for bulk retrieval
7. **Logistics** (`logistics-*`): Taiwan logistics providers integration
│   │   │   ├── grid.hbs
https://testnet.arcscan.app/tx/0x...
```

---

## Troubleshooting

### 3. **Cost-Aware Design**
- Merchant confirms payment received
- ✅ State serialization utilities
- Standard layout: `packages/<name>/{package.json, project.json, tsconfig.build.json, src/, __tests__/}`.
- Keep exports centralized in `src/index.ts`; avoid default exports unless already established.
- Do not over-engineer simple fixes; keep momentum and filtering
- Homepage components → `graphql-schema` skill
- Use `Semantics` widget for screen reader support.
- Review `tasks/lessons.md` at session start and deny scenarios