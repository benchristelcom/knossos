# GEMINI.md - Reference Agent Guidelines

## Instruction Loading Strategy

### Commits

### Error Handling Pattern
```json
// Define in models (MUST be static)
export PATH="$PATH:$HOME/go/bin"

# Start fresh
WARNING:services.llm.claude_client:No valid Anthropic API key found
9. Verifies transaction on blockchain
         ↓
ANTHROPIC_API_KEY=sk-ant-api03-...
GOOGLE_API_KEY=AIza...
Signature: 0x937a66dcb0b4f99614...
Method: ECDSA

class MyClient(BaseLM):
    super().__init__(...)
    # Initialize payment service
    fraud_detected: bool
```

### 2. Restart API
```php
// Model setup
wrangler pages project create my-site
rg -c "
from services.payment.x402_service import SettlementAgent
npm test test/widget/     # Run widget tests only
aws ecs create-cluster \
  --iam-role ECSAnywhereRole \
  --service-name my-edge-cluster \
  --cluster my-edge-cluster \
  --cluster-name traefik \
  --broadcast \
  --cluster my-edge-cluster \
  --vpc vpc-xxxxx

# Clean restart
✅ PaymentRouter: 2942 bytes
WARNING:services.llm.claude_client:No valid Anthropic API key found
```

**CRITICAL FormContact Rules:**
- **ALWAYS include** `icon` property for first-level items
- **Deadline**: Friday the 14th, 12:15 PM

### Technical Achievements
- Multi-agent systems
- Account information

---

## Summary

See `rlm/environments/modal_repl.py` as the canonical reference implementation.

