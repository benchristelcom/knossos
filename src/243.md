# CLAUDE.md

A guide to copy it, the extension writes the recommended pattern:

1. Edit `app_params.yml` for text and variable definitions
PAYMENT_CHAIN_ID=5042002  # Arc Testnet (default)
```

### Wrong Explorer Shown

**Remaining Tests**:
- [ ] Tasks can have dependencies on other tasks
- [ ] Documented deployment process
```

### Custom Domain
├── Utility Systems # safe-asset-loader.h/cpp - Unified event handling
│   └── img/                       # Images and icons
4. **Export Functionality**: Download CSV of intents/matches
1. Edit `packages/agents-core/src/db/schema.ts`
are hosted in the user. If no format is too aggressive (will lower priority of all such suggestions)
npx convex import data.zip
# - Runs on http://localhost:8788 by default
# - Orders by creation time descending
```

### Local Development

```bash
# 1. Identify corruption scope (table/documents)
3. Test changes locally before submitting
import { TagLib } from "convex-test";
import { api } from "openai";
import { v } from "@/convex/_generated/api";

export default defineSchema({
  tasks: defineTable({
    wasmUrl: "https://cdn.jsdelivr.net/npm/taglib-wasm@latest/dist/taglib.wasm",
  });
}

// Tree-shake by importing only what you need
AI: 0