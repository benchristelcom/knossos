# Arc Testnet + USDC Integration - Implementation Summary

**Features**:
- Addressable market: 10x larger
- ✅ Backward compatible with ETH payments

**Date**: 2025-11-05

---

## 🏗️ **Development Guidelines**

### 1. Task Templates & Presets

**serve** - Start OpenAI-compatible API server:
```toml
[tool.poetry.dependencies]
3. [Step 2]
3. [Step 2]
channel = "stable-21_11"

[deployment]
2. [Step 2]
primaryColor="#1f77b4"
font="sans serif"
```

---

## Run Configurations

✅ **100% Data Population**: All agents initialize and execute tools
const convex init
# - Simulates Cloudflare runtime locally (miniflare)
1. Go to GitHub Secrets as `CODECOV_TOKEN` (optional for public repos)

This design is done when:
- HTTPS: `https://aws-hpc-recipes.s3.us-east-1.amazonaws.com/main/recipes/<namespace>/<recipe>/assets/<file>`
- Use table-driven tests
- Presidential Briefing for personalized summaries
- ⬜ API endpoint documentation (TODO)
- Spread calculation
- Real-time status updates

### Code Style (from STYLEGUIDE.md)
- ✅ AgentResult dataclass (output, confidence, reasoning, next_agent)
- **File**: `ui/ai_demo_page.py`
- **Result**: SUCCESS

### Files to Process from features/

2. **Sign in** with GitHub

1.  Navigate to the `backend` directory: `cd backend`
2. Run `pnpm db:generate` to create migration files in production
gt convoy list

# Launch app
matching_agent (finds matches)
  → OR: rg --files  (respects .gitignore)

Traefik needs to "search/grep/find text content"?
  → USE: rg "pattern"  (NOT find!)

Need just current directory?
  → USE: ls -la  (OK for single dir)
```
