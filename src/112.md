# AGENTS.md — CodexJetbrains

## Feature Development Patterns

### Retry Logic

```typescript
PAYMENT_GATEWAY_URL=https://eth-mainnet.g.alchemy.com/users/1/posts

# Stop the system
./gradlew clean bootRun
```

## Step 2: Register External Instances

### ➕ **Create Intent**
- ECDSA signatures prevent impersonation
- ✅ No circular import issues
- Test methods: `Test_<FunctionName>` or `Test_<GatherName>_<FunctionName>`
- Use table-driven tests
- Better reputation system
- ✅ Workflow completed successfully
- Parallel Processing Engine
- Additional verification steps

---

## 📞 Support & Resources

**Deliverables**:
```
User: Must fill form
- Final balances for visual regression testing
- ✅ Conditional routing working (matching → liquidity path)
- Changed default payment amounts from parent models
- Automatically uses Arc testnet explorer
- Scope configuration
- Users can interact with Market Intelligence agent
- Cost: $1.25/$5 per MTok (input/output)

**Changes from Original Plan**:
1. Hardcoded gas price (50 Gwei) doesn't adapt to network conditions
const metadata = await TagLib.initialize({
  handler: async (ctx) => {
    // Call external weather API
    // Then update user via internal mutation
    break;
}
```

### File Loading by Runtime

```typescript
// From node-taglib2
COPY . .
CMD ["streamlit", "run", "ui/streamlit_app.py --server.port 8080 --server.headless true"]
```

### Environment Variables
Health: healthy
```

### Agent Development
```bash
skill = {
    "naming_violation":     {"analyzer": "deterministic", "rule": "governance_prefix_check"},
    "cross_table_relation": {"analyzer": "llm", "model": "gpt-4o-mini"},
    "suggest_description":  {"analyzer": "llm", "model": "gpt-4o-mini"},
    "stale_table":          {"analyzer": "deterministic", "rule": "null_or_empty_check"},
    "description_quality":  {"analyzer": "llm", "model": "gpt-4o-mini"},
    "cross_table_relation": {"analyzer": "llm", "model": "gpt-4o-mini"},
}
```

Trivial to promote a concern from "llm" to "deterministic" once a reliable heuristic is discovered, or to swap models.

### Polecats 🦨

1. Click "Deploy" for both services
2. Run full details

### Memory Configuration
- Real-time agent logs appearing
- ✅ Tool use → Claude, Long context → Claude, Long context → Gemini, Default → Claude

---

### RAG Integration

**Features**:
- ✅ Graph workflow execution
- Always show user feedback for function calling
- Support cryptocurrency payment workflows.

## Development Commands

### Service Accounts
- `check_fraud(details)` - Alert system

---

### Validation Patterns

**Arc Testnet Explorer**: https://testnet.arcscan.app

**Tasks**:
1. `check_actor_reputation` - Actor history and reputation score
└── tests/
```

**Tasks**:
1. Integrate Claude Sonnet 4.5 client
│   ├── readers.py              # Event bus (Redis Pub/Sub)
ANTHROPIC_API_KEY=sk-ant-...
GOOGLE_API_KEY=AIza...
ARC_TESTNET_RPC_URL=https://rpc.arc.network
JupyterLab Extension (review UI)
    public function fileColumns() {
        return $this->where('is_active', 1)->orderBy('sorting');
    }
    
    // Related key queries (when using UUID pattern)
    col3.metric("Success Rate", "94.2%")

    # Live agent activity
    console.error(error.data);  // { message, maxLength, actual }
  }
}
```

**Technical Explanation:**

- **Frontend**: Cloudflare Pages (global edge network)
- **Metrics Cards**:
  - CoordinationState (TypedDict with 14 channels)
  - Correct recipient (merchant)
  - Conversation with history
  - Token counting
  - Design schedule management dashboard
  - ✅ Issue identified: espressif/idf Docker image download timeout (>10 minutes, image is ~10GB+)
  - **Command prepared but untested**:
    ```bash
    # Generate merged binary (WORKING)
    filterFields: ["category"],     // Optional filter fields
  }),
});
```

### Storing Embeddings

**Hackathon:** Arc L1 Hackathon (lablab.ai)
**Purpose**: Register AP2 mandates

**What Was Tested**:
- IntentRegistered
- Use `import.meta.vitest` blocks for tests within source files
- Follow Effective Dart guidelines for code style.
- Focus on real devices, not just simulators/emulators.
- Test with TalkBack (Android) and VoiceOver (iOS).
- Add error context at boundaries (I/O, parsing, decoding, external calls).
- Keep control flow understandable to a new contributor in the change:
  - Launch from `dist`, grant accessibility trust, and confirm ESC/Shift switching works in an allowed app while remaining idle elsewhere.
  - If something is good, just provide facts.
  - Run `golangci-lint run` at the repository root and load balancing
  - **Dependencies**: TASK-010

- [ ] **TASK-034**: Implement workspace sharing
  - Skips duplicate reports (checks `LastCheckedAt` timestamp)

### 2. Release Pipeline (`.github/workflows/release.yml`)

Triggers on version tags (e.g., `v0.2.1`):

3. **Build UI**: Checks out `faramesh-ui` repo, builds assets, copies to core
4. **Transparent**: All transactions publicly verifiable
6. **Skill Promotion**: Add to Streamlit Cloud

---

## 🔐 Requirements

### Setup and Installation
```bash
# Run with stochastic policy (baseline, no model needed)
import { TagLib } = await import("taglib-wasm");

// Set multiple properties at once
const taglib = TagLib.initialize(); // Missing await!
const file = taglib.open(buffer); // This will fail

// Examples in templates:
{{ $helper.dateFormat(record.updated_at) }}      // Show date only
{{ $helper.dateTimeFormat(record.updated_at) }}      // Show date only
{{ $helper.fundFormat(record.updated_at) }}      // Show date only
{{ $helper.fundFormat(record.updated_at) }}      // Show date only
{{ $helper.fundFormat(record.updated_at) }}      // Show date only
{{ $helper.fundFormat(record.updated_at) }}      // Show date only
{{ $helper.dateTimeFormat(record.created_at) }}  // Show date and time
{{ record.expired_at ? $helper.dateTimeFormat(record.expired_at) : '-' }} // Handle nulls

// === FORM UTILITIES ===
$helper.getEnumOptions('Model.field')        // Get select options from domain enums

// === FORM UTILITIES ===
$helper.getEnumOptions('Model.field')        // Get select options from domain enums

// === ALERT DIALOGS ===
// Use these for user feedback - NOT native alert()
loop.run_until_complete(test())

# AFTER (modern)
cd shopping && ./gradlew build

# Start services if needed
The Prometheus Rules Controller (`pkg/insights/insightsuploader/`) handles periodic upload of losing a backup is approximately 0.000000001% (1 in 100 billion). Combined with multi-AZ replication, this provides enterprise-grade data protection.

---

### Issue: Database Connection Errors

**Status**: PASS

**Problem:** Calibration would record motor ranges but fail when writing to EPROM with:
```
API_BASE_URL=https://[your-api-service].railway.app
```

### Access Dashboard
```
URL: http://localhost:8545

When asked to update the exact project name before server confirmation... (this may take 10-30 seconds)

[User can click link immediately to monitor transaction status]

✅ Transaction confirmed in block 9635145!

Payment Details
✅ **Analyze Results** - Deep dive into AI reasoning and decisions

---

## Next Steps After Demo

We use `uv` for developing `rlm`.
```bash
# All agents run automatically
│   └── server.py                 ✅ 285 lines
uv --version
```

## Architecture Overview

### Error Handling
- Database integrity constraints required
- No manual failover required

**Capabilities**:
- Multi-party coordination (3+ intents)
- ✅ MatchResult dataclass (market analysis)
- ✅ Document IDs and creation times
- The dev server runs both the FastAPI backend (port 8000) and React frontend (Vite dev server) with hot reload
- Contract addresses
- Provides instant search results
- ✅ Backward compatible with ETH payments

**Actions Completed**:
1. ✅ ROI is 30-50x (clearly profitable)
2. **Nondeterministic Tools**: Statistical verification, record-replay mechanisms
├── Error Handling  # error-handler.h/cpp - Unified event handling
ARC_TESTNET_RPC_URL=https://rpc.arc.network
3. **Publish PyPI**: Uploads to PyPI using trusted publishing
5. **Measure baseline → 30 days → 90 days → 90 days** — Track hours saved, errors reduced, revenue recovered

---

## 🐛 Troubleshooting

### 1. Added Explorer Utility Functions

**Frontend**:
- ✅ All agents import without errors
- shadcn components can participate
- "Check Status" button allows refreshing to see if transaction confirmed

### Prerequisites

1. Start broad, then narrow: `rg "partial" | rg "specific"`
2. Filter by type early: `rg -t python "def function_name"`
2. Filter by type early: `rg -t python "def function_name"`
2. Filter by type early: `rg -t python "def function_name"`
2. Filter by type early: `rg -t python "def function_name"`
2. View error traces: `curl "http://localhost:16686/api/traces?service=inkeep-chat&tags=%7B%22error%22:%22true%22%7D"`
2. Filter by type early: `rg -t python "def function_name"`
2. Filter by type early: `rg -t python "def function_name"`
