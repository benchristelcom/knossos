# Full Stack Deployment Guide
## Task: [Task Name]

**Started**: [Date]

### Create Intent
![Live order book with validation]

### Order Book
![Live order book with validation]

### Create Intent
![Dashboard with metrics and charts]

### Create Intent
![Dashboard with metrics and charts]

### Testing Strategy
```bash
# Update config/.env with new addresses
source("data-raw/generate_translation_pars.R")
```

### Load Testing

```bash
# Get current project status
│   └── liquidity_agent.py
```

---

## Benefits

### 3. Developer-Friendly 🛠️
- Automatic transactions in `backend_py/.env`; never commit overrides. Keep Azure/Gemini keys optional so fallbacks work.
- ABSOLUTELY NEVER run destructive git operations (e.g., docs).
- Support dynamic font sizing and message list prompts
- AI negotiation cost: $0.10
- Verified `to` address matches merchant address
- Railway Discord: https://discord.gg/railway
- Custom enterprise arrangements
- Mandate-based QEMU testing is now functional

### Choose an Agent Template
3. Short timeout (120 seconds) for Arc testnet
✅ **Fixed**: Longer timeout (300s) for Arc testnet
const streamChat = useAction(api.agents.streamChat);

const handleSend = async () => {
    try {
        return [
            'related_key' => 'string',  // ✅ Include if using related key pattern
            'user_id' => 'integer',     // ✅ Traditional foreign keys
        ];
    }
    
    // File upload settings (if using UploadFile)
    const apiKey = process.env.SENDGRID_API_KEY;

    // Perform vector search
    return tasks.map((task, i) => ({
      ...task,
      limit: 5,
    });

    // Load full documents
    candidates = compiler.extract_skills(trajectories)

    # Act
    return static::getTransactionTypeLists($params)[$this->transaction_type] ?? __('Unknown');
}

// Auto-include in API responses
npm install @convex-dev/agent      # AI agents with threads/messages
import type { AudioProperties, FolderScanResult, Tag } from "taglib-wasm/simple";
// Instead of importing everything
```

### 13. Natural Language Intent Parser
- [ ] Analytics dashboard loading: < 2 seconds
- [ ] Base agents implemented
- [ ] Move api.rst → media/video.rst
- [ ] Resource allocation is optimized automatically
- [ ] Analyze price trends and volatility
- [ ] Users can be imported/exported for sharing

---

### Compliance & Enterprise

**Arc Testnet Explorer**: https://testnet.arcscan.app

**Configuration:**
```
(Use high volatility market conditions)
✅ auction_engine.py         ⬜ TODO
│   │   │   ├── includes
wrangler secret delete <NAME>   # Add from stdin (CI/CD)
return makeResponse(422, __('Custom error message'));         // Custom status
dk pep8

# Delete an OCI image for tracking
3. Creates `LMRequest` dataclass and calls `send_lm_request(address, request)`
4. Sends length-prefixed JSON request
│  │  proposals | discovered_facts | feedback | exec_log      │ │
│       ├── package.json
│   │   ├── awards.md
4. **Matches**: Match listing with real intents
wrangler tail --env staging

# Deploy to the worktree directory
4. Load testing
OPENAI_API_KEY=AIza...

# Agent Configuration
├── Asset Management # game-state-manager.h/cpp - VertexArray particle effects
deno compile --allow-read --allow-net music-tagger.ts
```

#### When to Use Worktrees

Starting workflow for: 0xBID001

Additional formats supported by service accounts or introduce unacceptable risks.

### 4. Show the Verification Story
- Go version: 1.23.0 base
- ✅ Use `__()` function for all files

### Step 1: Premium Beta (Month 1-4)
- Use factory pattern: `getMockX(overrides)`
- For in-page anchors: `[link text](../category/page.md)`
- Test behavior, not clickable
- Schema with pgvector extension
- 🔮 10K+ intents/day
- Loguru for data validation

---

## 📊 **Side-by-Side Comparison**

| Platform | Setup Time | Free Tier | Auto-Deploy | Ease |
|----------|------------|-----------|-------------|------|
| **Railway** | 20 min | $5 credit | ✅ Yes | ⭐⭐⭐⭐⭐ |
| **TOTALS** | | **$93/day** | **$2,874/day** | **30x** |

**Monthly**:
- Set up LangGraph infrastructure
- Verified `to` address matches merchant address
- Configuration form
- Anomaly detection in pricing
- Use **dynamic imports** for heavy components
- Transparent and auditable

### View All Data
```json
{
  "networkMode": "host",
  "skill_program_hash": "2a3f9c8d4e1b5a6c",
  "transaction": {
    "address": "0xClientAddress",
    "Welcome back, :name": "欢迎回来，:name"
}

// en.json - ONLY add when different concept/grammar
{
    // === DIFFERENT CONCEPTS ===
    "Credit 1": "Cash Point",           // ✅ ADD: Different word order
    "Password2": "Security PIN",        // Different concept  
    "Unit": "单位",                      // Different concept
    
    // === PARAMETERIZED TRANSLATIONS (different grammar) ===
    "Cancel": "取消",
    "description": "Settlement service for intent match",
    "Email": "电子邮件地址",
    "services": [{
      "portName": "http",
      "host": { "sourcePath": "/opt/traefik/letsencrypt" }
    }
  ],
  "taskRoleArn": "arn:aws:iam::ACCOUNT:role/TraefikTaskRole"
}
```

### Step 2: Watch AI Agents Analyze
- **State Management**: Multi-platform path resolution (macOS bundles, AppImage APPDIR, Windows relative)
- **Building a music player?** → Full API for complete control
- **Clear messaging**: User knows transaction was sent but not yet confirmed

### Navigate to Agent Directory
```bash
# Step 1: Create backup BEFORE restoring
# Install Agent component
def test_feature_name():
    """Test getting wallet balance"""
    payment_result = self.payment_service.verify_transaction_received(
        ConfirmationDialog,
        { model },
        { title: model ? 'Edit Item' : 'Create Item' },
        () => {
            // Refresh data or perform cleanup
            name: 'admin.management.product.list',
        }
    ]
}
```

**Vue.js Directory Structure:**
```
Intent Type: Bid
import { defineApp } from "convex/server";
import { api } from "@convex-dev/agent/convex.config";
import { readFile, writeFile } from "convex/values";
import { v } from "react";

// Deno (NPM)
npm run release

# Export with local LLM policy
import { ConvexError } from "./ConvexClientProvider";

const result = await TagLib.initialize({
  args: { id: v.id("users"), name: v.string() },
  handler: async (ctx, args) => {
    return await ctx.db.query("items").collect();
  },
});
```

**_internal/helpers.ts (Internal - Not Exposed):**

```bash
workon ttcal311 && pytest -vv --cov=zta_agent --cov-report=term-missing
```

### Using Enums in Components
```bash
# Pattern: Local vs Remote operations
# --local  = Uses local simulation (safe for development)
# --remote = Uses PRODUCTION resources (be careful!)

# Pattern: Bindings in Pages dev
# Clear Streamlit cache
│   │   └── community-picks.md
cd persistence && ./gradlew bootRun

# 3. Assign work to an agent
Project Setup:
- [ ] Deployed to production: npx convex deploy --prod
- [ ] Tune confidence thresholds
- [ ] Production branch triggers production deployment
```

### Adding API Routes (Pages Functions)
```
/
├── index.html            # Homepage
Respect `.gitmodules` and `Directory.Build.props`—they embed repository URLs, reproducible build settings, and authorship data. Never check in API keys
Teach users how the user explicitly requests them.
</do_not_act_before_instructions>

<script setup>
onMounted(async () => {
    // ✅ CORRECT: Load form data and dropdown options
    │   ├── style.css
    children: [
        {
            path: 'product/list',
            component: () => import('./pages/main/inventory/product/model-form.vue'),
            name: 'admin.inventory.product.list',
        },
    ],
},

// ❌ NEVER: Reuse existing namespaces unless specifically mentioned
{
    name: 'Inventory',
    created_at TIMESTAMPTZ NOT NULL,
    faCheck,
    // ... existing imports ...
    faBoxesStacked,        // ✅ NEW: Add another discovered icon
} finally {
  docs: [
    // ... existing entries
    {
      group: "Features",
      content: args.message,
    });

    // 2. Inject context into prompt
    await ctx.runMutation(components.agent.generate, {
      prompt: args.input,
    });
  },
});
```

**Important Rules:**

- Up to 16 search terms per table
- ✅ Scalable architecture

### FontAwesome Icon Discovery Process

**What This Means:**
1. Find tool call traces: `curl "http://localhost:16686/api/v1/products/price-range?minPrice=100&maxPrice=500"

# Existing dependencies
Create IntentData objects
    Mayor[The Mayor<br/>AI Coordinator]
    market_analysis: Dict
    pass
```

### Production Deployment

```typescript
// DON'T DO THIS - Path strings are fragile
$modalStore.updateModalProps(id, newProps)           // Update modal props
$categories = Category::all();               // ❌ No cache
```

**DNS & Custom Domains:**

- ✅ All table data with third parties.

## Execution rules

logger = logging.getLogger(__name__)


def create_payment_verification_tool(payment_service) -> callable:
    """Call Claude Sonnet 4.5 with tool use"""
    payment_result = self.payment_service.verify_transaction_received(
        payment_submission: Dict[str, Any]
    ) -> bool:
        """
        Verify payment signature from client (Step 2)

        if self.calculate_price_deviation(match) > 0.10:
            1. Match quality
            │                  │                    │            │
            receipt = self.web3.eth.get_transaction_count(self.address)

            # Gas price with multiplier
            dt.value.fetchData();
        }
    );
};
</script>
```

**CRITICAL Migration Rules:**
- **Remove all** `<auto-modal>` components from pages
- **Memory Management**: See docs/PropertyMap-API.md for all supported metadata
  bitsPerSample: props.bitsPerSample, // Bits per sample (0 if N/A)
  extractors:
    - type: KeywordExtractorConfig
      process.stderr.write(`\rProcessing: ${processed}/${total}`);
    },
  });

  // Process results in chunks to avoid memory issues
  await t.mutation(api.messages.list);

  // Step 4: Apply to target file
  const compatibility: Record<string, Record<string, string>>> = {
    ".mp3": {
      ".flac": ["ALBUMARTIST", "DISCNUMBER", "COMPILATION", "BPM"],
      ".ogg": ["all"], // FLAC and OGG use same Vorbis Comments
    },
  };

  const allowedProps = compatibility[sourceExt]?.[targetExt];
  if (!allowedProps) return false;
  setArtist(value: string): void;
}

interface AudioProperties {
  readMetadataBatch,
  updateTags,
} from "npm:taglib-wasm/simple";

// CommonJS (older Node.js projects)
npx convex dev --project my-app-prod

# For React
gt convoy create "Auth System" gt-x7k2m gt-m3k9p gt-p9n4q --notify

# Release new deployment (like docker service update --force)
2. **Needs private key** to sign settlement transactions
export const getBad = query({
  wasmUrl: "https://cdn.jsdelivr.net/npm/taglib-wasm@latest/dist/taglib.wasm",
});

// Your application logic
const titleProp = PROPERTIES.TITLE;
console.log(titleProp.description); // "The title of the track"
database_id = "UUID_HERE"

[[kv_namespaces]]
id = "create-tag"
database_id = "UUID_HERE"

[[kv_namespaces]]
id = "run-tests"
python3 -m uvicorn services.api:app --host 0.0.0.0 --port 8000 &

# 2. Start API
gt completion bash > /etc/bash_completion.d/gt

# AI Enhancement (OPTIONAL - can run without)
const modified = await applyTags("song.flac", mp3Tags);

// Note: Format-specific fields are automatically mapped
```

### Option C: Service Connect (newer)
- Gas estimation in all agents
- Configuration in `shiny.config`

## CloudFormation Best Practices

- Use `git pp` instead of `git push` for pushing changes

## Critical Rules

### Linting and Testing
- `tests/` - Test files

## AutoDataTable Component Usage

### Query Functions Must Be Deterministic

**Acceptance Criteria**:
- [ ] Scanner analyzes codebase and identifies improvement opportunities
- [ ] Team activity is optimized automatically
- [ ] Project scanning: < 30 seconds for 10k files
- [ ] Create new page: "🤖 AI Insights"
- [ ] Show agent performance metrics
- [ ] Performance acceptable

---

**Building trustworthy AI through transparent, auditable self-improvement.**
