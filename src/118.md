# x402 Payment Demo - Arc Testnet Integration Fixed

**Use Foreign Keys (`user_id`, `order_id`) when:**
- ✅ Child models that reference parent data in responses
- Most agents don't actually need LLMs

**VERIFY at:** <https://docs.convex.dev/auth/clerk>

**❌ DON'T DO THIS:**

```typescript
Added try-catch block to host
import { scanFolder } from "taglib-wasm/simple";
import { components } from "jsr:@charlesw/taglib-wasm/simple";

// useQuery returns T | undefined during loading
fd . -t f           # Lists ALL files recursively (FASTEST)
# WARNING: This uses PRODUCTION data! Use with caution.
