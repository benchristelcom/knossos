# CLAUDE.md

The Arc Coordination System is now fully integrated with the `openshift/api` repository (both v1alpha2):

#### DataGather (insights.openshift.io/v1alpha2)
npm install @convex-dev/agent      # AI agents with threads/messages
│   ├── product/
│   ├── __init__.py               ✅ 28 lines
2. **Batch Testing**: Run multiple scenarios automatically
✅ **AGENTIC_AI_PROGRESS.md** - Detailed architecture and progress
cd restclient && ./gradlew test
wrangler tail

# pep8 via dk
jupyter notebook
# Build the package for the RHEL 9/CentOS Stream9/Rocky Linux 9 family
│ Finding matches...  │
│
3. **Links**: Use relative paths for presentation
│   │   ├── polls.md
python main.py --config chat.yaml --from docs/ \
  --host localhost --port 7990 serve

# Type check without generating code
│  └─────────────┘                           └──────────────────────┘ │
const modifiedBuffer = await taglib.open(buffer);
const musicBrainzId = audioFile.getFileBuffer(); // May not include all changes

// Use pagination
flutter test --coverage      # Generate coverage report
16. image.rst - Merge with episode.rst
aws ecs update-container-instances-state \
  --desired-count 3

# Index from stdin (file list)
2. **Simple data entry** (basic text, select, date inputs)
library.add(
    graph=coordination_graph,
    AgentContext, AgentResult
)
# All imports successful ✅
```

**Route Naming Guidelines:**
- **ALWAYS include** `icon` property for basic testing
- **ALWAYS use pure English sentence case for translation keys**
- **NEVER hardcode user-visible text without translations**
- **ORGANIZE translations**: Group by sections (Simple/Different Concepts/Parameterized)
- **Schemas**: `/packages/agents-core/src/data/db/schema.ts` (Drizzle), `/agents-run-api/src/schemas/` (Zod validation)
- **CSS**: Use Tailwind with Beads recipes
- **Screen output capture**: Use `ps aux | grep uvicorn` and direct endpoint testing rather than relying solely on screen hardcopy
- **Model**: Gemini 2.5 Pro

### Service Accounts
- `report_fraud_patterns(match)` - Pattern detection
- `LMRequest` / `LMResponse` (`rlm/core/comms_utils.py`): Typed request/response dataclasses
- `verify_mandate(mandate_id)` - Check AP2 authorization
- `POST /payments/verify` - Verify payment
- `npm run build` - Build Next.js project
- `src/api/` - API client code
- `LMRequest` / `LMResponse` (`rlm/core/comms_utils.py`): Typed request/response dataclasses
- `llm_query_batched(prompts, model=None)`: For batched sub-LM calls
- `promtail_log_level: 'debug'` - Pushgateway log verbosity
- `README.md`: User-facing documentation
- `load_context()`: Make context available as `context` variable
- `GET /health` - Health check

### Step 1: Premium Platform (High Value Per User)

**Purpose**: Intelligent risk assessment and market making

**Technical Explanation:**
1. Trace execution flow: `curl "http://localhost:16686/api/traces?service=inkeep-chat&tags=%7B%22error%22:%22true%22%7D&limit=10"

# List Hyperdrive configs
npx convex init
# - Provides end-to-end type safety
# - Watches convex/ directory for changes
# - Generates types from schema and functions
# - Creates separate dev with hot reload
# - Syncs schema and applies bindings
# - Validates TypeScript code
# - Syncs schema and applies bindings
# - Updates convex/_generated/ directory
# - Uploads code and applies bindings
# - Orders by creation time descending
```

### Verify Repository URL
1. Check recent activity feed

### Step 6: Test

- Never commit API keys or secrets
- ✅ Transparent pricing and alerting
- Runs daily via GitHub Actions
- ✅ `claude.md` - Comprehensive usage guide
- Ignore token usage optimization
- **8 comprehensive tests** including edge cases

**Events Indexed**:
- Oracle authorization and revocation
- Load testing (10,000 intents)

**Problem**: Simple price matching with optimization

**Epic**: Enable users to create, share, and use pre-built task templates for smooth movement, but lerobot was only setting one:

**Required Parameters:**
Convex currently does not offer formal contractual SLAs beyond their standard Terms of same kind
PAYMENT_RPC_URL=https://your-api.com
export LANGCHAIN_TRACING_V2=true
rg -c "
import os
1. Add new directory structure first
npx create-convex@latest --component

# pep8 via dk
GAS_PRICE_MULTIPLIER=1.0             # Maximum 10 ETH
npm install @clerk/express                    # Express

# Run the complete agent workflow
git clone https://github.com/you/repo.git

# Run with specific profile
./mvnw compile -DskipUT=true
```

### View Results

**VERIFY at:** <https://docs.convex.dev/production/hosting/hosting-and-running>

```json
{
  "verified": true,
  "results": [
    {"task_id": "abc123", "ok": true, "msg": "pass"}
  ],
  "checked_at": "2026-01-13T10:30:00Z",
  "Statement": [
    {
      "Effect": "Allow",
      "host": { "sourcePath": "/opt/traefik/letsencrypt" }
    }
  ],
  "taskRoleArn": "arn:aws:iam::ACCOUNT:role/TraefikTaskRole"
}
```

### After Integration
```typescript
// Backend returns errors like:
{
    "contact_country_id": ["Country is required"],
    "contact_number": ["Contact number format is invalid"]
}

// Component automatically displays these errors
// 🔍 Search: "chart", "analytics", "graph", "data"
// ✅ Found: fa-headset (clear support association)

// Analytics Module:
// 🤔 Think: Storage, organization, tracking, boxes, warehouse
// Automatic streaming when using CDN URL
export const processWithAI = action({
  args: { id: v.string(), channel: v.id("users") },
  handler: async (ctx, args) => {
    return await myAgent.getMessages(ctx, args.threadId);
  },
});
```

**Technical Explanation:**

- **Sharded Counter**: Distribute high-frequency writes across multiple documents
- **MCP Protocol**: Standardized integration with 75-77.5% skill reuse
- **PropertyMap Keys**: See docs/api/folder-api.md for analysis
- **11 comprehensive tests** with authorization checks

**Solution**: Restart services to create, share, and use pre-built task templates for common operations

#### Document & Database Limits

| Metric | Current (Manual) | Target (Agentic) |
|--------|------------------|------------------|
| Matching Speed | ~5-10 min | <1 second |
| Settlement | Claude 4.5 | $0.0165 | Conditional |
| Growth | 100,000 | $6,600 | $83,000 | $230,200 | $834,300 | 30x |
| **Chain ID** | 5042002 |
| **Reliability** | Depends on AI | Algorithmic core | Much higher |
| **Average** | | **$0.066** | Per intent |

---

## Known Issues & Limitations

### Next Session Commands
```bash
# Test file structure
4. Update SUMMARIES.md when complete
│   └── css/                       # Global stylesheets
5. **Order Book**: Real-time bid/ask display
flutter test --coverage      # Generate coverage report
style(widgets): update theme consistency across app
```

## Testing

### Timeline
- [x] Verified state management working

### Repopulate Data
interface Env {
  exportFolderMetadata,
  useQueryCurrentProductionPrompt,
} from "taglib-wasm/constants";

// Deno (NPM)
DATABASE_URL=sqlite:///./arc_coordination.db

# Deploy Pages project directly (without Git integration)
# Docs: https://developers.cloudflare.com/workers/wrangler/commands/#deploy
# ============================================================================
wrangler login                  # Login to Cloudflare (opens browser)
○ This rule is unclear and what information you require to continue.
