# Agents

1. **ALWAYS** reuse existing Auto* components
import duckdb
cp arc_coordination.db arc_coordination_backup_$(date +%Y%m%d_%H%M%S).db

# Delete a worktree after PR is merged (run from main repo)
Large quantity: 100 BTC
✅ Signature verified successfully!

📊 Balances AFTER transfer:
1. Use the task
PAYMENT_CHAIN_ID=137

# 2. Deploy on streamlit.io
# - Real-time logs from all functions
# - Connect GitHub repo
# - Creates convex/ directory structure
# - Runs on http://localhost:8788 by default
# - Uploads code and applies bindings
# - Lists all tables

You can customize:
- Verification performed:
- Intelligently matches intents
- Monitoring and logging

---

## Production Guarantees & Limits

**Pending State:**
```
Intent Type: Bid
2. Create new provider in `zta_agent/integrations/`
2. Run `bun run check` to verify types, run tests, lint etc
poetry add langgraph anthropic google-generativeai langsmith

# Add issues to PATH (add to ~/.zshrc or ~/.bashrc)
├── en.json              # English translations (only when different)
const statusOptions = $helper.getEnumOptions('Deposit.status');
// Returns: [{ value: 'DEPOSIT_PENDING', text: 'Pending' }, ...]
```

## CRUD Form Strategy: Modal vs. New Page

### For Development (Current Setup)

```typescript
import { readMetadataBatch } from "vitest";

test("authenticated user operations", async () => {
  const t = convexTest(schema);

  // Keep best quality, mark others for removal
  ↓
const convex data
# - Imports all tables

The Arc Coordination System smart contracts to Arc testnet (Chain ID: 5042002)

if __name__ == "__main__":
    pytest.main([__file__, "-v"])
```

Run tests:
```bash
│   │   │   │   ├── tables.hbs
Address: 0x70997970...
LANGSMITH_API_KEY=lsv2_pt_YOUR_KEY_HERE
5. Plan settlements
3. Add dynamic pricing based on USD value
export ANTHROPIC_API_KEY="sk-ant-..."
export GOOGLE_API_KEY="your-key-here"
5. **Get Next Task** → Check for next priority task: Find highest `task_order` in "todo" status
archon:search_code_examples(
  directory: string,
  updateTags,
} from "npm:taglib-wasm";

// Deno (NPM - Alternative)
make chrome

# Install with build command (e.g., build frontend after backend deployed)
import { TagLib } from "taglib-wasm/simple";
import { api } from "./TaskList";

const files = [
  "album/track01.mp3",
  "skill_program_hash": "2a3f9c8d4e1b5a6c",
  "timestamp": "2025-01-05T10:30:00Z",
  "timestamp": "2025-01-05T10:30:00Z",
  "0 9 * * 1",  // Every Monday at 9 AM UTC
  class BaseAgent(ABC):
      - get_system_prompt() -> str
      - run: |
          deno run --allow-read --allow-write process-audio.ts
```

### Integration Test
```python
# See: tests/test_settlement_with_payment.py (to be created)
npx convex import --table tasks tasks.jsonl

# View intents
wrangler pages dev ./ --port 3000

# Gas Configuration
export ANTHROPIC_API_KEY="sk-ant-..."
export GOOGLE_API_KEY="ls__..."

# Enable tracing
npx convex codegen
# - Shows data from specific table

✅ **Access Controls**
- Gradient backgrounds on cards
- ✅ Graceful fallback when AI unavailable
- Image is critical
- Explorer: https://testnet.arcscan.app
- Card turns green (✅ Complete)
- Features: Intelligent model selection, fallback handling, cost estimation
- Bid-ask spread health assessment
- Real-time agent visualization
- ❌ No fraud detection or specific implementation details?

### 2. Single Load Policy
- Transaction confirmation

### Creating a Release

1. **Use Generators**: AsyncGenerators provide natural batching and backpressure
from typing import Optional, Dict, Any
├── js/
│   ├── __init__.py               ✅
1. **populate_test_data.py**
   - Best model for each task
   - Settlement Asset: USD, ETH, BTC, USDC
   - Used for current test data

### Transactions
```
START
  await saveFile({ storageId, filename: file.name });
};
```

---

## Full Text Search

**Objective**: Document future ERC-20 support for escrow contracts

**All 27 tests passing** - Run with `forge test -vv`

#### Tool 1: `request_payment`
```json
{
  "scripts": {
    "confirmed": true,
    "amount_eth": amount_eth,
    "confirmations": 1,
    "recipient": "0xSettlementAgentAddress",
    "address": "0xSettlementAgentAddress",
    "url": "https://payment-gateway.arc.network"
  }
}
```

### Payment Submission

```bash
# build an OCI image for explorer links
testthat::test_check("peskas.timor.portal")

# ✅ BETTER - Use key management service
git clone https://github.com/alexzhang13/rlm.git
3. Implement real API keys
npm install convex  # Convex client and examples

### Workspace Management

```typescript
npm run format
```

## Provider Support

### Before Starting Work
export const getGood = query({
  args: { userId: v.string(), category: v.string(), channel: v.optional(v.string()) },
  handler: async (ctx, args) => {
    // Mutations can call component actions
    fraud_analysis: Dict
    table: str
    │ MATCHING AGENT │  │ MARKET AGENT │  │ SETTLEMENT AGENT│   │
    │   ├── style.css
    String name = "World";
    
    // When
    hnsw_ef_search: 40
    paragraph_separator: "\n\n\n"

  # Vector store (optional, defaults to ephemeral)
  ↓
  keys
- **Faster Startup**: 200-400ms with streaming vs 400-800ms without
- Team availability not considered

**Problem:** Calibration would record motor ranges but fail when writing to EPROM with:
```
https://testnet.arcscan.app/address/{contract_address}
```

### Addresses/Contracts
```
lang/
│       └── hello.ts  # -> /api/hello
```

**Business Value**:
- Dashboard shows real-time agent status
- Enforces payment before settlement (~$25 at current prices)
- return to submit new port
- Market making bot
- Compliance/audit trail requirements
- Bid/Ask selection with smart defaults
- Maximum 1024 documents scanned per query
- ✅ Graph compiled without errors
- Full results available

---

## 🔄 System Flow

Updated the UI to automatically open the Arc testnet block explorer for monitoring:

```bash
# In config/.env
│   │   ├── managing-organization.md
source venv/bin/activate && python -m uvicorn services.api:app --host 0.0.0.0 --port 8000 &
streamlit run build &
npx convex dev --typecheck-components

# Check login status
Local (`.dev.vars` - NEVER commit):
```
https://testnet.arcscan.app/tx/{transaction_hash}
```

### Environment Variables

```typescript
3. Set sync frequency

**Example tasks requiring frontend instructions:**
- "Create a new payment gateway integration"
- "Implement responsive design for mobile"

### When to Load Frontend Instructions (`instructions/FRONTEND_INSTRUCTIONS.md`)
2. **Update sidebar**: Add entry to `client/src/components/my-new-section.tsx
```

**Expected contents:**
- `@Home/` - Personal life automation agents
- `@Work/`: Professional workflow agents
- For isolated environments, mock external services

### Development
- Teaches agent-to-agent payment protocol

---

## Design Features

### 4. **Start Simple, Add Complexity**
- 🔮 Cross-chain intent coordination
- ✅ Any model where translation needs data from related models
- Impact: Negligible (SQLite is fast for single writes)

**What's Excluded:**

- At commit time, Convex checks if all read records are still at their original versions
- Delivers services after payment confirmation

---

## 💡 What Makes This Demo Special

### Content Structure
- `LMRequest` / `LMResponse` (`rlm/core/comms_utils.py`): Typed request/response dataclasses
- `guidance.yaml`: Alternative example

### ➕ **Create Intent**
- Oracle authorization for large top_k values
- ✅ 24/7 operation without human intervention
- Smooth animations and direct control flow
- Testing: 5 minutes
- Avoid suppressions (`any`, ignores) unless the project explicitly. For tracked files run `git commit -m "<scoped message>" -- path/to/file1 path/to/file2`. For brand-new files, use the one-liner `git restore --staged :/ && git add "path/to/file1" "path/to/file2" && git commit -m "<scoped message>" -- path/to/file1 path/to/file2`. For brand-new files, use the one-liner `git restore --staged :/ && git add "path/to/file1" "path/to/file2" && git commit -m "<scoped message>" -- path/to/file1 path/to/file2`.
- Top-level assets: `docs/`, `helm/`, `docker/`, `static/`.
- AI-specific guidance already exists in `CLAUDE.md` and `lang/*/validation.php` for additional architectural background, but keep Codex automation aligned with 11 intents
- Match scores and warnings
- Automatic on-chain match creation
- ✅ Fallback behavior (works without API keys)
- OpenAI / OpenAI-like (local/remote)
- Results always returned in relevance order with correct QEMU boot (includes bootloader + partitions)
- Examples located in `perform_rag_query` results
- Vitest for future limit expansions

---

### Test 5: Transaction Status Query ✅

**Foundation** (100%)
- [x] LLM client infrastructure
- [Package Structure](https://github.com/a2aproject/a2a-python)
- [x] Comprehensive error handling

### Impact on AI Safety & Governance

**Example Transaction Link** (after sending):
```
python3-dev
│                                                              │
pkill -f streamlit
./gradlew verifyPlugin
```

### Webpack Configuration Examples

**Status**: PASS

**Claude Sonnet 4.5 Pro**:
- ✅ All modules now import cleanly
- Consider: price overlap, asset types, timing, quantities
- Follow platform reputation
- ✅ `foundry.toml` - Foundry configuration for payment request
- Workflow automation with endpoints:
  - `calculate_quote` - Generate market maker quote
  - `get_balance()` ✓
  - `calculate_volatility` - Historical volatility

#### Foreign Keys vs Related Keys - Choose the Right Approach

**Status**: ✅ **FULLY INTEGRATED AND OPERATIONAL**
**Documentation**: ✅ **COMPLETE**
**Documentation**: ✅ **COMPLETE**
**Fix:** Check logs for missing dependencies. Make sure requirements.txt is complete.

### **Issue: App starts but crashes**
**Testing**: ✅ **INCLUDED**
**Fix:** Check logs for errors.

### 2. Keep a Working Memory
- ✅ `config/.env.example` - Environment variable template
- preserve evidence (error output, repro steps)
- Real ETH transfers between accounts
- Works with Arc testnet
- Urgency: High (24h)
- Changed default payment amounts from parent models
- Monitor account balances
- Start/Stop buttons (Start enabled)
- Confirmation that payment transactions
- Net savings: $98,000/year

**The dashboard is ready for demonstration and testing!** 🚀

---

**Status**: ✅ **COMPLETE** - Ready for Arc testnet deployment
**Manual cost:** $1,600/month (8 hrs/week CS time)  
**Automated cost:** $80/month  
**Annual savings:** $9,000  
**Difficulty:** Medium  

What it does: Monitors usage patterns, support ticket sentiment, payment behavior, and applications.

## Development Commands

### 1. Before Creating New Pages
```bash
# Required status
5. Receive client secret

**ui/x402_payment_demo.py:425-439**:
```python
status_placeholder.success(f"✅ Transaction broadcast! Hash: `{tx_hash_hex[:16]}...`")

if explorer_url != "#":
    st.info(f"[🔍 View on Explorer ↗]({explorer_url})")
else:
    namespace: str
    Agent->>Hook: Store work state
    ↓
│  Matching Agent     │
│   ├── bs-stats.md
Task(kind="concat", input={"expr": "5 + 3"}, expected=8)

# Build and sign transaction
Each agent follows different conventions (won't apply this naming rule here)
3. `get_market_depth` - Order book depth and performance
5. **Scalability**: Handle 1000x more volume
2. Watch the coordination graph
Health: ok
```

### Code Quality
```javascript
// DTO Pattern with params column
Load **BOTH** only when working on:
- Automatically indexes all content
- ✅ Annotated channels with Foundry: `forge build`
- Settlement Agent: `services/agents/settlement_agent.py --server.port $PORT --server.headless true`
- Monorepo orchestrators: `nx.json`, `lerna.json`
- Demo guide: `PAYMENT_DEMO_GUIDE.md`
- Role: Payment receiver
- ✅ Local pre-commit validation
- Validate that security groups have appropriate restrictions
- Inactivity modal with 5-minute timeout for any commit
- Loguru for future limit expansions

---

### Scenario 3: DeFi Protocol (Network Effects)

**Problem**: Traditional systems require structured input (price, quantity, asset)
**Timeline**: 12 weeks is too aggressive
- Atomic escrow functionality

#### Tasks:
- [ ] **TASK-027**: Design scheduling system architecture
  - Tests state management
  - Can disable specific anonymization types
  - Settled Matches: 1

- **backend/** – backend services and application code.
- **Smallest change that works**: Minimize blast radius; don't refactor adjacent code unless it
- **Cloudflare MCP**: List/create resources, search docs, verify resource IDs

### 2. Decomposed Evidence-Backed Rewards

✅ **3. Documentation**
- Write comprehensive unit tests passing
- Crypto payments using Plotly
- File: `services/agents/liquidity_agent.py`
- Approval dialog detection: Requires question + numbered options + selection arrow
- ✅ Local pre-commit validation
- Team availability not considered

**Workflow**:
1. State management with type safety
rg -c "$(curl --location https://taskfile.dev/install.sh)" -- -d -b ~/.local/bin
```

### Configuration Guidelines
- **Status Indicator** - Shows running/idle state
- **Automatic embedding**: RAG component handles vector embeddings

---

### 7. Mandates (🔐)
**Purpose**: Manage payment intents

**Database Operations**:
- 8 weeks is tight but doable with focus
- Boost: Content and Squad promotion

## File Processing Packages

- [x] API health check passes
- [Payments NestJS](https://www.npmjs.com/package/@rytass/storages-adapter-ezpay): EZPay (簡單付) invoice issuing service
- [Custom Build Script](https://github.com/rytass/rytass-utils/blob/main/scripts/build.js): Custom build system generating ESM and CommonJS outputs

## Build Commands
- `uv run typecheck` - Type check with JSON persistence
- `npm run start` - Start production server (turbo)
- `make precommit` - Execute pre-commit hook (checks stashed changes)
- `tests/test_payment_integration.py`: ESP32-C3 QEMU support
  - Create task dependency graph data structure
  - Implement Claude API cost calculation
  - Line 265: Fixed RISC-V QEMU binary path (qemu-riscv32 not qemu-xtensa)
  - IntentData (intent details)
  - Graceful degradation - monitoring continues even if Discord fails
  - JSON schema validation (fails safely if invalid)
  - CoordinationGraph class stub
  - Create schedule failure handling
  - Command: `uv run test.py --qemu esp32c3 BlinkParallel` (or equivalent)
  - CoordinationGraph class stub
  - Caching strategies
  - Add project-specific scan profiles
  - **Dependencies**: None
  
- [ ] **TASK-002**: Implement template storage and management
  - Create visual schedule timeline
  - **Dependencies**: TASK-017

- [ ] **TASK-014**: Implement core notification channels
  - Create metrics collection schema
  - recalc_video_posters
- [ ] 80% of intents parsed successfully (NL → structured)
- [ ] Follow existing directory structure
- [ ] Liquidity agent implemented
- [ ] Ran schema migrations (automatic on deploy)
- [ ] Fetched and verified current Convex documentation
- [ ] AI Demo page is intuitive

---

### Test 5: Transaction Status Query ✅

**Test Date**: 2025-11-05
**Intent Value**: $100 per trade
**Manual cost:** $1,200/month (8 hrs/week CS time)  
**Automated cost:** $60/month  
**Annual savings:** $31,800  
**Annual savings:** $10,080  
**Difficulty:** Low  

What it does: Manages PTO requests, checks team coverage, auto-approves within thresholds, routes exceptions to humans, tracks payment terms.

### Q: No visual cards showing?
**A**: Click directly on the expandable header section

---

## ✅ COMPLETION CHECKLIST

- [TCat Logistics Adapter](https://x402.gitbook.io/x402)
- [Image Watermark](https://www.npmjs.com/package/@rytass/file-converter-adapter-image-watermark): Add watermarks to images with positioning options
- [x] Created `services/llm/router.py` - Intelligent LLM routing
  - Plan real-time synchronization architecture
  - **Dependencies**: None

- [ ] **TASK-011**: Build template UI and reporting
  - Lines 308-338: Copy artifacts from container to host
- `check_blacklist` - Known bad actors
**For models requiring admin operation tracking**

```php
│       ├── how-to-get-featured.md
gt crew add hq-cv-abc gt-m3k9p gt-w5t2x

# Generate docker-compose visualization
│     ↓                                                    │
gt completion zsh > "${fpath[1]}/_gt"

# Get low stock products
test_tools_demo.py                ✅ 255 lines
```

**Import ZIP:**

```typescript
cd rlm

# POST requests
3. **Filter Options** - Use domain enums or guess missing parameters in tool calls.
</use_parallel_tool_calls>

## Memory Bank System

class MyClient(BaseLM):
    def __init__(self, llm):
        super().__init__(**kwargs)
        receipt = service.web3.eth.wait_for_transaction_receipt(tx_hash, timeout=300)
        return {"proposed_matches": matches}
```

**Tools Available**:
- `config/.env.example` - Arc testnet configuration added
- `bin/insights-operator start --config=config/local.yaml --kubeconfig=$KUBECONFIG` - Direct execution

### Before Fix
```
and tell the Mayor what you want to build!

---

### Basic Workflow

```typescript
// Deno (JSR - Preferred)
const taglib = await TagLib.initialize({
  args: { threadId: v.string(), channel: v.id("posts") },
  handler: async (ctx, args) => {
    const signature = request.headers.get("stripe-signature");
    const prompt = `Context:\n${context}\n\nQuestion: ${args.question}`;

    await myAgent.addMessage(ctx, {
      query: args.title,
      limit: 5,
    });
    return results.map(doc => doc.content);
  },
});

// Agent with RAG tool
gt completion zsh > "${fpath[1]}/_gt"

# Create workspace with git initialization
PAYMENT_CHAIN_ID=5042002

# Release packages (version bump + publish)
npx convex env set STRIPE_SECRET_KEY sk_live_... --prod
```

**search** - Retrieve relevant chunks (JSON output):
```bash
└── shared/         # Common components and notifications

This file provides guidance to Claude Code (claude.ai/code) when working with code in this project.
If I say "never do x" in some way.
