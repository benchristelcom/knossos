# Laravel Backend Development Instructions

## Module Creation Rules - MANDATORY Namespace Patterns

**1. Get Task Details:**
```bash
✅ **100% Service Health**: All services running without errors
self.bus.disable_torque()
# Unlock EPROM for writing by setting Lock=0
│   │   └── model-form.vue
import {
  providers: [
    {
      domain: "https://your-issuer-url.clerk.accounts.dev", // From JWT template
      args:
        - -y
        - '@gongrzhe/server-gmail-autoauth-mcp'
```

## Form Creation Patterns

### ❌ DON'T - Page Creation Anti-Patterns

Edit `.github/CODEOWNERS` with actual GitHub usernames or progress bars in `convex/_generated/`:

```typescript
// VERIFY at: https://docs.convex.dev/client/nextjs/app-router
│   └── tests/
├── zh.json              # Chinese translations (ALL keys must be here)
Spread > 10%: score = 0.7-0.7 (acceptable)
RatingUtils.POPM_STAR_VALUES; // [0, 1, 64, 128, 196, 255]
```

### After Fix
```
https://testnet.arcscan.app/address/{contract_address}
```

### Addresses/Contracts
```
