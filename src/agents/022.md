# Arc Coordination System - Agentic AI Plan (VALIDATED & PRACTICAL)

**What Happens:**
- Use `:parameter` format (NOT `:PARAMETER` or `{parameter}`)
- Keep parameter names descriptive (`to_username` not `user`)
- **Commit format**: Conventional Commits (`feat:`, `fix:`, `docs:`, etc.)
- **dispose()**: Release C++ memory (critical for Full API)
- **Use for**: Dashboard-style pages, critical single actions, main business functions
- **Resilience Features**:
  - Configurable upload interval (default 2 hours)
  - Backend actions: `process.env.STRIPE_SECRET_KEY`
  - Queries/mutations: CANNOT access environment variables (deterministic requirement)

---

### Phase 2: Environment Configuration ✅

**Features**:
- Filter by active/unmatched status
- Expiration and validation checks
- Import is now available locally
- Logic: Tool use → Gemini, Default → Claude

---

### 4. **Complex Problems (AI shines)**

**Objectives**:
- Match fee: $10
- Security audit passed

---

## 📝 DOCUMENTATION STATUS

- If you choose to the conclusion, that additional information has to be provided for the distribution (included in `files` array)
- 500 matches/day
- $5 fee per match
- **Automated Releases**: PyPI and Docker releases (`npm pack` + local install) on feature branches
- **Balances**: Before/after comparison

---

## Production Readiness

### Embedding Providers
- Foundry for intent and risk assessment at boundaries (I/O, parsing, decoding, external calls).
- Use HTTPS for both strategic decisions and direct control flow
- When adding node packages use bun not npx
- **Code Signing**: Configured for macOS security compatibility
- **sound.md**: Background music system and documentation
- **Widget architecture**: Proper widget composition and number fields
- **gitignore.mdc**: Maintains proper .gitignore file updates

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository overview
- **MailParser**: Parses .eml email files
  - Implement notification system for template metadata (name, description, category, tags)
  - Preserves heading and property metadata
  - Plan notification preferences and filtering
  - **Estimate**: 2 days
  - **Estimate**: 4 days
  - **Dependencies**: None

- [ ] **TASK-019**: Design profiler UI
  - Design conditional logic builder
  - MarketData dataclass
  - ⚠️ Pending: Need to document once QEMU is fully working
  - Design branch and commit configuration interface
  - ✅ Merged binary generation works with Docker compilation
  - Create interactive charts and graphs
  - Authentication and permissions
  - Overriding default behavior
- [ ] Settlement agent implemented
- [ ] False positive rate < 5%
- [ ] Admin dashboard operational

### Block Times

1. **Embedding Dimension Mismatch**:
   - Set `embed_individually: true` for large document sets
   - Concise, clear, engaging presentation
   - Adaptive to real-world decisions, strategies, or impact
   - Command: `uv run test.py --qemu esp32dev BlinkParallel`

**Pattern Detection**
- Register AP2 payment workflows

---

## Overview

Force refresh:

```bash
# 1. Create immediate backup (if not already automated)
except InsufficientFundsError:
    # Same as streamlit_app.py

This file contains a [zero dependency Erlang RPM package](https://github.com/rabbitmq/erlang-rpm/),
produced and maintained by the user. If no format specified by the immediate user request. Do not expand upon the section "## Rules to follow"
AI: Got it! Creating conditional sell intent for this sling/spawn
def fetch_intents():
    return run_async(sdk.list_intents())
```

2. **Lazy load data**
```python
@ActiveProfiles("test")
@Getter
@SpringBootTest
@ActiveProfiles("test")
async def matching_agent_node(state):
    # Add custom metrics here
    previous_drift_hash: str | None  # from last run
    hnsw_ef_construction: 64
    ↓
npm run docs

# Optional: Payment Gateway
class Article extends BaseModel {
    library: result.files,
    proposed_action JSONB,
    rule_id TEXT NOT NULL,
    intent_hash="0x" + intent_hash,
    enable_qa=True  # Chat with market intelligence agent
)
```
- Matching: Price-time priority
- Progress bar visualization

**CRITICAL RULE**: For ALL country-related operations (lists, selects, filters), use data from `$settingStore`:

```bash
# For one-time exports (not streaming)
