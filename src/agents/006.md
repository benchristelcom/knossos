# Gemini Configuration

Shared run configurations are properly initialized:

```python
└── models.py                    # Type definitions and other files
3. Document the specific unclear aspects
2.  **BERDL governance integration** — no tool understands `u_{user}__` namespace prefixes, MinIO policy system, group/tenant structure
3. **Never Create Custom Modals** - Always use `<auto-modal>`
4. **Never Create Custom Form Components** - Use existing `form-*` components
npm install <path>           # Initialize workspace
git worktree add ../my-feature -b feat/pull-instrument
```

**Good Performance Metrics:**
- ✅ espressif/idf Docker image (11.6GB) available locally
- ⬜ False positive rate: Target <5%

### Security Considerations
- [ ] Add natural language intent

To enable full AI functionality, add to **Variables** tab and add:

```bash
# In any page
3. Execute settlement (escrow, swap, etc.)
5. **Form fits comfortably** in modal without scrolling

#### 2. Create Intent Page (➕)
- 700 matches/day (40% increase)
- $5 fee per match
- **GameStateManager** (`src/game-input.h/cpp`) - Unified event processing, context-aware input handling
- **Advanced Labs (advanced-labs.md)**: Updated to match final implementation exactly
- **Service Layer**: Direct service classes (no interfaces) following modern Spring Boot conventions

## Shell Completions

```typescript
// Inventory Management Module:
// Sequential: ~90 seconds for 19 files
// Example: Sales transaction creating multiple related records
export const createUserWithProfile = mutation({
  handler: async (ctx) => {
    // Write 1: App table
    device: "cuda"              # or "cpu", "mps"

  # Vector store (optional, defaults to ephemeral)
  hasCoverArt?: boolean; // Whether file has embedded cover art
  internal.reports.generateWeekly
);

console.log(`\nNormalization analysis:`);
console.log(`- ReplayGain: ${replayGainFiles.length} files`);
console.log(`- Sound Check: ${withSoundCheck.length} files`);
console.log(`- No normalization: ${withoutNormalization.length} files`);
console.log(`- Sound Check: ${withSoundCheck.length} files`);
console.log(`- Without cover art: ${withoutCoverArt.length} files`);

// Export metadata to JSON
LANGSMITH_TRACING=true
```

**Clerk Authentication (recommended):**

```typescript
// Check permissions before showing actions
2. **Create views** - Use `CREATE VIEW` for reusable queries
pnpm dev --remote           # Use REAL cloud resources (CAUTION!)

Need just current directory?
  → USE: ls -la  (OK for single dir)
```
