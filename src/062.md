# CLAUDE.md - Spring Boot Labs

☑️ Add Matching Intent disabled
```

**Status Colors:**
- Payer signs the payment data with balloons animation
- ✅ No intermediary needed
- The dev server runs both the FastAPI backend (port 8000) and React frontend (Vite dev server) with hot reload
- Doesn't see which agent was running

### Learning Loop

```python
│   │   ├── browser-extension-installation.md
✅ **Bug Fixes**: 4 critical bugs fixed
├── langgraph/
wrangler deploy --env staging   # Deploy to environment

index.html (no package.json) -> Static HTML     -> Use Pages Git Integration (no build step required).

### 9. SLA & Contract Compliance Monitoring
**File Modified**: `services/api.py:134`
**For repeating validation patterns, create custom rules:**

```typescript
The operator has three main gatherers that run periodically:
- **Purpose**: Manages Red Hat entitlement certificates for exports