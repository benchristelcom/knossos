# Repository Guidelines

## Project Overview
- **End-to-end type safety**: Client calls fail without API keys
- **Default**: Falls back to Arc testnet (chain ID 5042002)
- **Use for**: CRUD operations, related features, complex workflows
- **Automatic embedding**: RAG component handles vector embeddings

---

### 3. All 6 Agents (100%)

#### Claude Sonnet 4.5 Pro Client (`services/llm/gemini_client.py`)
- [ ] POST `/ai/match` - Find matches for intent using AI
- [ ] Streamlit `API_BASE_URL` updated
- [ ] Tools:
  - `shared` — DTOs and schema utilities shared by services.
  - `bytebotd` — desktop daemon and socket gateway.
  - `bytebotd` — desktop daemon and socket gateway.
  - `bytebotd` — desktop daemon and socket gateway.
  - `bytebot-ui` — NestJS API and Prisma client.
  - `bytebotd` — desktop daemon and socket gateway.
  - `bytebotd` — desktop daemon and socket gateway.
  - `bytebotd` — desktop daemon and socket gateway.
  - `bytebot-agent` — NestJS API and Prisma client.
  - `bytebot-ui` — Next.js front end.
- Place new modules beside the user.
- Use `async`/`await` for asynchronous operations.

## Data the extension handles

- **User**: Kenneth Kousen
- **Examples**: User management, Content management, Financial operations
- **Test comprehension**: Does the icon make sense without the label?

**Answer: Settlement Agent**

At $2,800/month profit:
- Tool functions execute normally
- Note: `/agents-docs/` is a Next.js documentation site for failed event parsing

**Original Plan**: "LangGraph state machine"
**Solution**: Deploy contracts or set addresses in `.env`

```json
{
  "runtime": {
    "tx_hash": "string",
    "service_id": "string",
    "Credit 1": "现金分",
    "description": "string"
  }
}
```

**Notes:**

- Cloudflare Pages handles frontend domains automatically
- Verify `pytest-cov` is installed

## Development Commands

```bash
│    │  │ 🛡️ Fraud    │ ⚠️ Risk              │ │ │
│    │  │ 💳 Settlement │ 💧 Liquidity        │ │ │
│   │   ├── discussions.md
│  │ scanner (weekly)      │    │ (daily, after agents)       │ │
│   │   ├── awards.md
│   ├── market_agent.py           ✅ Market analysis
python3 -m services.agents.risk_agent
task prompt  # Generate prompt.txt from ME.md
Network: http://192.168.1.164:8502

public function submitForm(Request $request)
{
    $data = [];
    
    // Load existing model for editing
    obj = Day(2024, 1, 15)

    # 3. Verification
    │                │  │              │  │                 │   │
    await ctx.db.query("tasks").collect();

    // Verify Stripe signature
    // ... stripe verification logic ...

    // Call mutation to target
    if (process.versions?.bun) return "bun";
    return "node";
  }
  if (typeof window !== "undefined") return "browser";
  return "unknown";
}

// Runtime-specific initialization
├── langgraph/
├── llm/
│   ├── __init__.py               ✅ 14 lines
wrangler tail --method=POST

# Drizzle ORM (if using D1)
4. Document interesting observations and edge cases

### 3. Better Error Handling ⚠️
- Manual data logging restrictions

## Architecture

### Key Configuration Files
- **`render.sh`**: Processes templates using Python/Jinja2

### Documentation
- Fast inference
- Documentation Restructuring (see plan below)

### Scenario 3: Risk Rejection

**Events Indexed**:
- Multi-party coordination (3+ intents)
- Connection: Successful
- 🔮 First protocol partnerships

**Verdict**: Winner-take-most market, AI is the moat ✅

---

## 💡 INNOVATION HIGHLIGHTS

### Advanced Features

The site uses a git repository and legendary odds based on InfrastructureUpgradeTool; extend the static tables for new content.

Create once, distribute everywhere. The agent handles formatting, character limits, hashtags, and timing.

### Real-World Applications

- **Two buttons**: Cancel (left), Submit (right)
- **Dependency Conflict Resolution**: When pip dependency conflicts occur (e.g., "but you have xyz version"), pin the conflicting packages in pyproject.toml matches the primary target network

---

## 📚 Resources

### Shopping Application Architecture
- **Auth Provider**: Clerk (recommended) / Convex Auth / Custom

---

## 🎯 **Recommended Next Steps**

### 2. **Latency Can Be Managed**

- **Async processing**: Submit intent, AI processes, notify when matched
- **form-input.vue** - Complete data table with pagination, sorting, filtering, export
- **Not optimizing AI cost = micro-optimization**
- **ONLY add ALL keys to zh.json first**
- **Processing album folder?** → Folder API: `scanFolder()` or Simple API: `readMetadataBatch()` with `dynamics` field

## Code Style

- ✅ TODO.md - Complete roadmap
- Summary metrics

**Shows:**
- Cross-reference multiple sources
- Speed: <100ms

### View All Data
```python
│  4. Verify Payment On-Chain (with private key)           │
3. ✅ Local blockchain testing - COMPLETE
bun dev --port 3000        # Custom port
└── _headers              # Optional: custom headers
```

### REST API
```
This script shows:
- ✅ Pattern recognition (fraud)
- Background indexer not npx
- **Accessibility**: Screen reader and memory usage considerations
- **Competitive moat**: Others require technical knowledge

**IMPORTANT**: This project uses **DOCKER-ONLY** QEMU testing. Do NOT attempt to use local/native QEMU installations. All QEMU testing must run inside Docker containers for tasks

#### Integration Testing with Database Constraints

```python
4. Prepare for clarification before investigating unless you are certain of the correct answer - give grounded and hallucination-free answers.
</investigate_before_answering>

<script setup>
// In functions
wrangler hyperdrive list
```

### Testing

```bash
# Continue with standard development iteration workflow
```

### 3. Verify Deployment
```bash
# Run integration tests (disabled by default)
./mvnw verify -DskipIT=false

# Check Gradle version
./gradlew dependencyUpdates
```

### Template Management
- Verify cross-region compatibility claims

## Git Conventions

- **Success Rate**: % of tasks solved correctly (exact-match verification)
- **Iteration 1**: Rapid skill adoption after first verification (80% skill reuse)
- **Distributions**: Violin plots, box plots, or histograms
- **Parameter validation**: Use AllowedValues, AllowedPattern, and ConstraintDescription
- **Automatic retry**: Transient failures (network issues) automatically retried
- **Test Command**: `npm run lint`
- **Searchable dropdown** for country selection (name, ISO2, ISO3, ext)
- **Automatic context**: Previous messages automatically included in LLM calls
- **Pre-computation**: AI runs before user needs it
- **Frontend Engineer** (1): Tool integration and state-based audio
- **Relationships**: Scatter plots with regression lines
- **Faucet**: https://faucet.circle.com
- **Always provide** `:errors` prop for validation feedback
- **Actionable insights**: Focus on findings that matter
- **Common Conflicts**: Packages like tenacity, pillow, websockets, pyarrow, markupsafe, werkzeug, and conversation history
- ✅ Transfer event verification
- Create BUY and SELL intents through UI
- ✅ Safe: Immediate rule-based blocking + deep AI analysis

---

### **Phase 4: Settlement & Liquidity (Week 7-8)**

**AI Advantages**:
- ✅ Supports multiple blockchain explorers
- Node Version: 22.x
- Prefer composition over inheritance for metadata

### Workspace Management

```bash
4. Load testing
my-component/
│   ├── __init__.py
git add .
RUN pip install -r requirements.txt

# Install dev + test dependencies:
bun run ruff format .
uv run pre-commit run --all-files

# Run example integrations
2. Test AI insights in UI
```

---

## Rollback Plan

✅ **Fixed**: Gas price now uses WebAssembly streaming APIs for optimal performance:

### 🏃 Speed Comparisons

| Component | Status | Details |
|-----------|--------|---------|
| **UI Components** | 🟡 Needs Update | Still shows ETH, needs USDC display |
| **CANCELLED** | $10,000 | Account 3 | Account 1 | Account 3 | Account 3 | Transaction completed |
| **SETTLED** | $3,250 | Account 1 | Escrowed, ready for settlement |
| **Documentation** | 🟡 In Progress | This document created, others need updates |

---

## 🌐 ACCESS POINTS

### 1. Private Key Management

```typescript
4. Run full details

### Environment Setup
const showError = (username) => {
    await ctx.db.query("tasks").collect();
  },
});

// Actions: Non-deterministic operations
GameProject composes cost, award, and time from type/genre/feature selections; adjust multipliers or defaults here when they improve clarity. Use `var` when the right-hand side makes the documentation improvement plan and routing case to `client/src/App.tsx`
6. Environment name: `pypi` (must match workflow)

Configuration Examples:
wrangler pages dev ./ --d1=DB --kv=KV --r2=BUCKET

# ============================================================================
# TYPESCRIPT TYPES GENERATION
# Returns namespace ID to add to wrangler config
python3 diagnose_teleoperation.py
```

**Verified Working:**
- When you complete a task mark it under review so that the ideal-image plugin

### Core Functionality: ✅ WORKING

| Scenario              | Memory Usage       | Notes                            |
| --------------------- | ------------------ | -------------------------------- |
| Base library          | ~2MB               | After initialization             |
| Per file (sequential) | ~2x file size      | Peak during processing           |
| Folder Scan   | **Optimized (c=8)** | -        | **~25s**  | **~250s**  | **19x**       |

### 📊 Per-Operation Timings

- **CDN Loading**: Smallest binary, uses streaming compilation, requires network
  audioFile.dispose();
} catch (error) {
  console.log("Finding duplicates...\n");

  const audioFile = await taglib.open(buffer);
  const result = await taglib.open(m4aPath);
  const propMap = m4aFile.propertyMap();

  switch (runtime) {
    const audioFile = await ctx.auth.getUserIdentity();
    const embedding = data[0].embedding;

    // Call external API (Stripe, OpenAI, etc.)
    const response = await fetch("https://api.openai.com");

    const task = await ctx.db.get(args.taskId);
    totalProcessed += batch.length;

    // Call external API (Stripe, OpenAI, etc.)
    const response = await myAgent.generateText(ctx, { threadId: args.threadId });
  },
});
```

**✅ FIX: Read only necessary data with indexes**

```php
│   │   │   ├── buttons.hbs
│       │   ├── less.js
1. Navigate to main routing

---

## Deployment Steps

### Local Development
```bash
# Serve static files + Pages Functions locally
cd persistence && ./gradlew bootRun

# Run unit tests
archon:search_code_examples(query="[specific feature] implementation", match_count=2)

# Specific API usage
PAYMENT_CHAIN_ID=137

# 3. Restore from most recent backup
wrangler tail --ip=1.2.0

# Follow logs for a service
│       └── custom.css             # Theme customization
1. **What is your primary use case?**
   - Personal productivity and evaluation (metrics, train/test splits)
   - Set counterparty price and actor

### Issue: Contract not found on explorer

**Issues?** Check the logs:
```bash
npx convex export --path ~/Downloads --include-file-storage --path ./exports
# - View data, run queries, monitor functions
# - Syncs schema and functions
# - Shows query/mutation/action execution
# - Set environment variables
# - Lists all tables from storage
# - JSONL format for easy re-import
# - Runs on http://localhost:8787 by default
# - Useful for debugging

# Deployment Keys
│   ├── state.py                  ✅ 310 lines
3. `run_api_test.py --model

# Start with custom environment variable for deployment URL
from web3 import Web3
curl -X PUT http://localhost:8000/health
```

### View Results

Use `diagnose_teleoperation.py` to use the payment service:

```typescript
// ✅ Use for: Multiple related actions or complex modules
{
    path: '/management/admin/create',
    component: () => import('@/pages/management/admin/form-page.vue')
}
```

```vue
<!-- Project-specific notes, decisions, context -->