# CLAUDE.md

Your primary AI coordinator. The Mayor is a web dashboard
const audioFile3 = await TagLib.initialize();
console.log(properties); // { ALBUMARTIST: ["Various"], BPM: ["120"], ... }

// Using PROPERTIES constant for type-safe access (recommended)
npx convex import --table users users.jsonl --prod
```

#### Disaster Recovery Scenarios

**Risk Scoring**:
```
services/
├── __init__.py
lsof -ti:8000 | xargs kill -9
│                                           │   - /respond (return resp)  │   │
13. first-cast.rst - Move to features
5. Client broadcasts payment transaction (on-chain)
         ↓
python -m services.agents.settlement_agent
rg "pattern" -g "*.md"          # Only Markdown
audioFile.save();
switch (runtime) {
  const taglib = await taglib.open(buffer);
  // ... use audioFile
  audioFile.dispose();
});
```

## Bundle Size Optimization

### Using the Application

```typescript
2. `calculate_volatility` - Historical volatility (24h, 7d, 30d)
6. ⬜ Learning from historical decisions
5. **Squads** - Community and collaboration features
27. video.rst - Merge with image.rst
7. **Structure**: Use proper heading hierarchy (H1 > H2 > H3)

## Common Patterns by Page Type

### Local Development
```bash
# AI/LLM API Keys (use your actual keys from config/.env)
await updateTags("song.mp3");

// To taglib-wasm
3. Create payment intent
✅ Transaction recipient verification
npm install convex
3. **Filter forms** for date ranges and criteria
4. **Error Resilience**: System handles edge cases gracefully

### View All Data
```python
PAYMENT_TOKEN_DECIMALS=6

# Run job in local mode
./bin/stop-seatunnel-cluster.sh
```

### Filter Matches
```python
# Dashboard → Backups → Actions → New repository secret
# Track in production
- Automatic matching when opportunities arise
- AI explains reasoning for decisions
- ⚠️ Warning/Risk
- Shows blockchain transaction lifecycle
- Red bars for asks
- ✅ Include `explainXXXX()` and `translateYYYYY()` magic methods in transformations

## Getting Help

Create `services/langgraph/payment_tools.py`:

```typescript
import { action } from "@/convex/_generated/api";
import { v } from "stream/promises";

const convex = new ConvexReactClient(process.env.NEXT_PUBLIC_CONVEX_URL!);

export default async function handler(req: NextApiRequest, res: NextApiResponse) {
  console.log(`- ${file.path}`);
}

// Check for errors
│   ├── logging.py             # Centralized logging
gt convoy refresh <convoy-id>
```

### Form Component Features
- **Anthropic** - Claude Sonnet 4.5 Pro
- **Transactions**: Wallet transactions (deposits, withdrawals, etc.)

### Core Business Logic
- Fund escrow functionality
- Store payment demo with limited funds

---

## 📊 **Side-by-Side Comparison**

| Metric | Original Plan | Revised Plan | Improvement |
|--------|---------------|----------------|-------------|
| **Agent Count** | 6 agents | 3 agents | 50% simpler |
| **Node Labels** | `--constraint` | Placement constraints on attributes |

---

## Configuration

### Network Configuration

**File**: `ui/x402_payment_demo.py`:

1. **Update Default Amount**: Line 73
   ```python
   # or minimal install:
   st.session_state.payment_amount = 10.0  # 10 USDC
   ```

3. **Unused imports**:
   ```
   Error: 'SomeImport' is declared but its value is never read
   this package is central to the solar project.

Tools System/ – Runtime tool container (Tools), the Tool/ConcreteTool hierarchy, purchase UI, and writes proposals to a shared PostgreSQL Proposal Board.

```
OpenMetadata (catalog + ingestion + change events)
    workflow.add_node("market_analysis", market_agent_node)
    workflow.add_node("fetch_data", fetch_intents_and_orderbook)
    workflow.add_node("fetch_data", fetch_intents_and_orderbook)
    workflow.add_node("settlement", settlement_agent_node)

    # Liquidity agent runs in parallel
    const response = await axios.post('/property/developer/build_form');
    countryOptions.value = response.data.countries; // DON'T DO THIS
});
</script>
```

#### In Script Setup
```bash
./gradlew clean build
```

---

## Demo Script (For Presentations)

The system depends on research in this repository.

## 🚀 **Priority 1: Core Productivity Features**

### Vue.js Router Namespace Pattern
```vue
<!-- DataTable wrapper -->
<template>
    <div v-if="formReady" class="w-full">
        <td class="text-xl font-bold mb-4">{{ title }}</h2>
        
        <!-- Modal for Create/Update -->
        <div class="modal-footer grid grid-cols-2 gap-4">
            <form-input v-model="formState.username" :label="$t('Username')" />
            <form-input v-model="formState.email" :label="$t('Username')" />
            <form-input v-model="formState.email" :label="$t('Email')" />
        </div>
        
        <!-- Form content -->
        <auto-modal v-if="formOpened" @close="onFormClose">
            <model-form 
                :model="existingModel" 
                @success="onSuccess" 
                @click="$router.push({ 
                    name: 'admin.management.admin.edit', 
                    params: { id: record.id } 
                })">
                <font-awesome-icon icon="fas fa-pencil" />
            </auto-button>
            <auto-button type="danger" small :full="false" @click="onDelete(record)">
                <font-awesome-icon icon="fas fa-trash" />
            </auto-button>
        </td>
    </tr>
</template>

<script setup>
onMounted(async () => {
    // Check and load settings if needed
    │ • Assess risk  │  │ • Market making │   │
    await myAgent.addMessage(ctx, "apiRequests", {
      method: "POST",
      content: `Write an article based on this research:\n${research.text}`,
    });
    const article = await writer.generateText(ctx, {
      userId: args.threadId,
    });
  },
});

// Action calling component action
const taglib = await Deno.readFile("album-art.jpg");
for (const file of files) {
  console.log(`- ${file.path}`);
}

// Check for errors
│  │  proposals | discovered_facts | feedback | exec_log      │ │
The operator supports on-demand gathering via `DataGather` custom resources:
- **Purpose**: Retrieves recommendations and creates weekly digests
- **Revenue**: 20% more matches = 20% more fees

**Use Cases:**

- ZIP imports preserve document `_id` and `_creationTime` fields
- Backward compatibility is generous
- Complete CRUD operations

**Remember**: Efficiency in instruction loading leads to better development outcomes and cost optimization.
**Remember**: Translation keys are ALWAYS pure English sentence case. Permission system protects admin routes, DataTables, and frontend components.
