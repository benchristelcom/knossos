# CRITICAL: ARCHON-FIRST RULE - READ THIS FIRST
  const preloadedTasks = await TagLib.initialize();
  activeFiles--;
}

// Pattern 3: Detailed error logging
Your ME.md file provides personal context for AI reasoning
const analysis = await getCoverArt("song.mp3");
audioFile.setProperty(PROPERTIES.REPLAYGAIN_TRACK_PEAK.key, "-3.5 dB");
for (const track of tracks) {
  const titles = [];
  for (const file of musicFiles) {
    return (float) $value > 0;
});
```

**CRITICAL**: Update translation files in `lang/*/validation.php`:
```toml
[profile.default]
python = "^3.12"

# Async support
API_PORT=$PORT
```

**Technical Explanation:**

- Dashboard → Workers & Pages → Create → Pages → Connect to Git
- ✅ Any model where translation needs data from related models
- Quick stats in isolation with 100ms polling interval
- One change per intent: Target <$0.10
- NEVER swallow errors silently
- White-label solutions: $50K-200K per deployment

---

## 🎨 UI Features

### Phase 2: Core Features (Medium Priority)
- [ ] Risk Agent blocks 95%+ of bad matches
- [ ] Tested local development with npx convex dev
- [ ] AI decisions are auditable

---

## Contact & Support

**The Arc AI Agents are ready to impress!** 🚀

---

**Quick Start**: Read [A2A_X402_INTEGRATION_GUIDE.md](./A2A_X402_INTEGRATION_GUIDE.md)
**Issue**: Financial systems need predictable behavior
- Full end-to-many or many-to-many relationships

**Development Tools:**
- `todo` → `doing` → `review` → `done`
- Port: 8501

### Phase 1: Premium Platform (High Value Per User)

**VERIFY at:** <https://docs.convex.dev/production/state/>

### Setup Steps

**Deliverables**:
```
https://testnet.arcscan.app/tx/{transaction_hash}
```

**Tools Available**:
- `get_recent_activity(period)` - Recent transactions
- `verify_signature` - Cryptographic verification
- `backend_py/app` — FastAPI service: routes in `routes/`, request models in `models.py`, shared logic under `services/` (Azure OpenAI, catalog, reranker).
- `codex_withJava=false` → compatible with IDEs that ship Java (IDEA, Android Studio…).
- `frontend/src` — React + Vite: feature components in `components/`, hooks in `hooks/`, API shims in `services/`, shared types in `types.ts`.
- Manual conversion diagnostics: persist manual harness output once resolved (unless it's genuinely useful long-term).

---

## 🧪 TESTING

### 0. CI Pipeline (`.github/workflows/ci.yml`)

3. **Create `convex/auth.config.ts` (file-based auth config):**

```bash
2. Use the folder name in `form-editor` components

## Style notes
- Gas used: 21000

#### For Code-Heavy Educational Components:

**Public Sharing Alert**: Redis pubsub subscriber on sharing mutations from MinIO Manager Service. `make_public` events create immediate critical proposals + Slack notification. Deterministic Python, no LLM.

**Notification design**: One weekly Slack digest per reviewer. No per-proposal notifications (causes alert fatigue within 2 weeks). Critical findings get a detailed remediation plan for workarounds.

---

### Using Components in Your Code

**File**: `ui/streamlit_app.py:192-217`

#### Tool 1: `request_payment`
```json
{
  "serviceConnectConfiguration": {
    "test": "vitest",
    "x not found": ":x不存在",
    "Email": "电子邮件地址",
    "metadata": {
      "tx_hash": "0x...",
      "payment_submission": {...}
    },
    "reasoning": "Verify payment received on-chain"
  },
  "step_2": {
    "action": "prepare_settlement",
    "Credit 1": "现金分",
    "test:coverage": "vitest run --coverage"
  }
}
```

### Payment Submission

```python
3. **Form Initialization** - To get dropdown options (countries, categories, etc.) for forms

### 3. Better Error Handling ⚠️
- Cross-reference with historical fraud

**Key Achievements:**
- Cross-reference multiple sources for validation
- ✅ Responds naturally to leader arm movements

**Features**:
- After matching: Matches found? → market+fraud, else → liquidity
- Trigger: Price < $9,500
- No visibility into progress
- ESLint rules live in `coverage/`.
- AI-specific guidance already exists in `CLAUDE.md`.