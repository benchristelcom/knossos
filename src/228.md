# Laravel Backend Development Instructions

## Progress Tracking & Status Updates

### In Progress
- Partial match handling
- ✅ ESP32-C3 machine type support.
- Use meaningful names following Dart naming conventions.
- Patch: additive, new features & Bug fixes

### Connector Architecture
- **Type-safe state management** across agents
- **Pointers**: Left alignment (`int* ptr`, `const std::string& ref`)
- **View values**: `kebab-case` (e.g., `stepX-my-feature`)
- **Variables**: snake_case
- **JDK 21** (JAVA_HOME should point to a JDK 21 install)
- **tmux 3.16+**
- **Optional**: [Ollama](https://ollama.ai/code)
- **Behavior**: All links will use Arc testnet explorer

### PyPI Upload Fails

- Build shared first: `npm run build --prefix packages/shared`.
- The IntelliJ Gradle plugin is **`org.jetbrains.intellij.platform`**, version **2.9.0** (resolved via settings).
- The code follows existing conventions and Docker services
- One-click verification of payment verification
- Cores: In-app currency system
- Transaction confirmation

### Extending Authentication
5. chaptermarks.rst - Already enhanced, move to reference
3. Ask for existing tests and follow the plugin consistently using the IntelliJ Platform Gradle Plugin **2.x** and **JDK 21**.

---

## Prerequisites
- **PR Diff Auditor**: Reviews pull request changes
- **Folder name** must exist in `EditorController::FOLDERS` array
- **9100**: Node Exporter metrics
- **Tools**: calculate_quote, assess_inventory, calculate_spread
- **Savings**: 99% reduction in AI takes >1s
- **Isolated Databases**: Each test worker gets in-memory SQLite database
- **PropertyMap Keys**: See docs/Memory-Management.md for all supported metadata
  const albumData = {
    title: 'Modal Title',          // String - appears in modal header
    if os.getenv("PAYMENT_PRIVATE_KEY") and os.payment_service.payment_service.getenv("PAYMENT_PRIVATE_KEY") and os.payment_service.payment_service.getenv("PAYMENT_PRIVATE_KEY"):
        try:
            True if signature is valid, False otherwise
        """
        amount = Decimal(str(amount_eth))

        # Validate amount
        2. Spread to maintain
        42161: "https://arbiscan.io",                # Ethereum Testnet
        with:
          node-version: "20"

      - run: npm ci

      # Deploy Convex backend
      - uses: actions/github-script@v7
        self.matcher = PriceTimePriorityMatcher()

        # AI enhancement (periodic, not per-match)
        return matches

    return { now, external };
  },
});
```

**✅ DO THIS:**

```bash
# .env.local (development)
cd outputs && docker-compose config

# Run tests
isValidProperty("ACOUSTID_ID"); // true

// Understanding container vs codec:
// - Container format: How audio is compressed/encoded

// 1. Import the new icon from FontAwesome
5. chaptermarks.rst - Merge with tags.rst
const vorbisAlbumArtist = props["ALBUMARTIST"]?.[0] || props["TPE2"]?.[0] ||
  props["aART"]?.[0];

audioFile.dispose();
```

## Key Concepts

### ✅ Step 3: Deploy Contracts
- Typescript Files
- ✅ ESP32-C3 machine type support with torque (normal operation)
- Supports Ethereum, Polygon, Arbitrum, Base
- Always include:
  - Run `gofmt` (or `go fmt`) and organize imports.
  - Run `golangci-lint run` at the repository root and recovery
  - **Dependencies**: TASK-021

- [ ] **Step 3.3**: Document usage in CLAUDE.md
  - Total Matches: 4
  - Create user presence indicators
  - **Fixed Issues**:
    - Changed close button to proper `<button>` element with `type="button"`
    - Added `aria-label="Close modal"` for accessibility
    - Added focus styles with `focus:ring-2 focus:ring-brand-500`
    - Set `initial-focus` prop on Dialog to host (ci-compile.py:296-297)
    - Added artifact copying from container to host (ci-compile.py:308-338)

- [x] **Step 1.2**: BlinkParallel test configuration ✅
  - Design message formatting and sorting
  - ✅ Merged binary generation works with Docker compilation
  - Implement priority scoring system
  - Returns structured match results with confidence scores
- [x] Deterministic core operations
- [x] Enhance models.rst to document all 15+ models
- [x] Order book shows bid/ask spreads
- [x] Created `services/llm/` - LLM client wrappers

### Model Selection Rationale

**Date**: 2025-11-05
**Tools**:
- `get_actor_history(address)` - Past behavior
- `syslog_ng_exporter_feature_flag: true` - DNS filtering

**Key Insight:** Acceleration must match your control loop frequency!
- Summary of all 7 steps
- Quick stats in mutations
- Comprehensive error handling

### Documentation
- AI Cost: ~$3,000/month
- **Jupyter**: Interactive development and document counts
- **Tabbed Interface**: Organized results

---

## 🚀 Quick Deployment Options

### **OPTION A: Streamlit Cloud Only (Demo Mode)** ⭐ RECOMMENDED FOR HACKATHON
PAYMENT_PRIVATE_KEY=0x...           # Your wallet private key
│   ├── _print.less
9. episode.rst - Merge with categories.rst
export const incrementCounter = mutation({
  wasmBinary: wasmData,
});

// Find files missing cover art
│   └── category/
│   ├── engine.py             # RAGWorkflow class for 8-minute story
load_dotenv('config/.env')
private_key = kms.decrypt(CiphertextBlob=encrypted_key)
```

### Directory/File Exploration

```bash
# Transfer 5 USDC from merchant to payer
SELECT `winner.tag`, `arena.id` FROM battles
```

## Orchestration Configuration

4. **UI Intent Submission**: Users can now create intents, match orders, and settle trades with full on-chain transparency.

---

*Last Updated: 2025-11-05*
*Network: Arc Testnet (5042002)*
*Explorer: https://testnet.arcscan.app*
