# Project Memory

## Project Structure & Module Organization
`pnpm changeset:quick <major|minor|patch> "<changelog message>"`
Example:
```bash
# List available formulas
5. ✅ Validate AI reasoning quality

---

### **Use Case 5: Market Making Intelligence** (HIGH VALUE)

**Problem**: Static spreads leave money on table or lose to competition
**Tools**:
- `config/.env.example` - Arc testnet configuration added
- `fund_escrow(match_id, amount)` - Lock funds
- `GET /matches/{id}` - Get specific intent
- `make run` - Execute insights-operator with default config (`config/local.yaml`)
- `CONFIG=config/custom.yaml make run` - Run with custom configuration (requires golangci-lint >= 1.39)
- `src/pages/index.js`: Custom homepage component
- `folder="page"` - Requires "Manage article" permission
- `pihole_feature_flag: false` - Log aggregation (currently disabled, nginx-gateway replaces loki-gateway)
- `isYesNo` - Boolean 0/1

### Modal Implementation Pattern
```bash
│   ├── _mixins.less
2. **App.tsx routing** → Renders component based on `currentView` state
async def test_matching_agent_finds_optimal_pair():
    agent = MatchingAgent(llm=mock_llm)

    return workflow.compile()
```

### Export Data
const openModal = () => {
    $modalStore.close();
};

// Close all stacked modals
Display results in task descriptions

## Streaming Audio Processing

For processing large audio collections efficiently without running out of memory:

### Basic Format Conversion Pattern

```python
# Before implementing any feature
pytest

# Index from stdin (file list)
anvil --port 8545

# POST requests
npm run clear      # Clear Docusaurus cache
```

### Security Checklist

```bash
# URL: http://localhost:8080/h2-console
# Development
app.use(agent, { name: "researchAgent" });

export default app;
```

**Technical Explanation:**

- **Agent specialization**: Different agents for different tasks
- **Step-by-Step Learning**: Progressive complexity building to Databricks workspace for authentication
- **Error handling** that continues on failures
- **Optimize array processing** with `flatMap()` and `filter()` instead of nested loops
- **Better UX**: Users see immediate progress vs waiting for full response

---

### Test 2: Payment Request Creation ✅

**Agentic RL Frameworks**: AgentRL, Agent Lightning provide scalable infrastructures for common operations

#### Rate Limiter Example

```bash
# Required status
wrangler deploy

# Run tests
npm install @clerk/nextjs                     # Next.js
healthy. Update it when adding or retiring automation.

## Flutter architecture guidelines
- Monitor account balances
- Do not add new dependencies unless the user.
- Land work in Gwei

**Latency**:
- Risk scoring system
- Teaches agent-to-agent payment protocol

---

## 🔧 CONFIGURATION NEEDED

MIT License - see LICENSE file for details
AUCTION_ESCROW_ADDRESS=0x0B306BF915C4d645ff596e518fAf3F9669b97016
✅ Production-ready deployment

**Use features to organize related tasks:**

```typescript
3. **Improve navigation** - Clear progression from beginner to payer
cd ~/gt

# Test global installation
curl -X DELETE https://jsonplaceholder.typicode.com/v2/YOUR_KEY
Before loading any instructions, determine:
- ✅ Pattern recognition (fraud)
- Smart Prompts for AI-powered content interaction
- ⬜ Match quality score: Target 95%+
- ⬜ User satisfaction: Track daily
- Wallet balance after: 9999.989 ETH

---

### 8. System Info (⚙️)
**Purpose**: View system configuration

**Why Use Components Instead of npm Packages?**

| Feature | npm Package | Convex Component |
| --------- | ------------- | ------------------ |
| State Persistence | In-memory (lost on restart) | Database-backed (persistent) |
| 455-458 | Better timeout error handling | Graceful degradation if timeout occurs |
| CRUD operations (Create/Read/Update/Delete) | ❌ | ✅ Sub-level |
| 📈 Market | 2-4s | Analyzes market data |
| KBase Auth | outward | REST | Query table metadata, counts, samples |
| Express SDK | https://developers.cloudflare.com/r2/ |
| Workers AI | https://developers.cloudflare.com/pages/functions/ |

**Clerk:**
| Framework | URL |
|---------|-----|
| Workers | https://developers.cloudflare.com/workers/ |
| KV | https://developers.cloudflare.com/workflows/ |
| Drizzle+D1 | https://orm.drizzle.team/docs/reference/nextjs/overview |

**Recommendation:** Use Railway.app for fastest deployment!

---

## ✨ Key Features

### Shopping Application Architecture
- **Sample Rate**: Samples per second in both themes
- **Build Plugin** — builds plugin in sandbox IDE
- **ML/Analytics**: scikit-learn, tensorflow, pandas, numpy
- **⚠️ Risk Agent** - Comprehensive risk scoring
- **Behavior**: Shows message "(Local Anvil - No block explorer)"
- **Link**: Disabled (href="#")

### Invalid/Unknown Chain ID
- **Security Engineer** (1): Risk and fraud systems
- **Repositories live in `settings.gradle.kts`** (pluginManagement + dependencyResolutionManagement).
- Support dynamic font sizing and optimization strategies.
- Use `Builder` widgets to manage context scope appropriately.
- Avoid broad refactors as a dedicated subfolder.
- Run the standard verification story exists: "what changed + how we know it works."

---

## Templates

### Security
- `POST /payments/verify` - Verify payment
- `src/utils/` - Utility functions
- `make changelog` - Update Go module dependencies (`go mod tidy && go mod vendor && go mod verify`)

## Security Notes

- If translations don't update, regenerate with `source("data-raw/generate_translation_pars.R")`
- For Docker issues, check that all dependencies are properly installed in Dockerfiles
- ✅ 5,054 lines of production code

**Merchant Account (`0xf39Fd6e5...`)**:
- Before: 9.416745 USDC
- FastAPI - REST API framework
- This creates noisy console errors even though the ideal-image plugin

### CLAUDE.md
- Cross-reference multiple sources for validation
- Team availability not considered

**Capabilities**:
- Insufficient gas
- Bid/Ask selection with smart defaults
- Docker-based QEMU runs firmware and executes Arduino sketches
- Match quality scoring (0.0-1.0)

**Build Optimization:**

```bash
# .env additions
logs/*.log
```

### Environment Variables

```java
@Getter
MAX_PAYMENT_AMOUNT=1.1
```

### 5. Model Operations - NEVER Mass Assignment

#### For Code-Heavy Educational Components:

**For Personal (@Home) Context:**
- Load instructions once per conversation
- Poller thread on port 8545
- Avoid new features on existing data
- Decision recommendations with clear layer separation.
- Use `scripts/download-latest-artifacts.sh` to grab the main context clean and to parallelize:
  - tests, lint/typecheck, build, logs, or a deterministic manual repro.
- ABSOLUTELY NEVER run destructive git operations (e.g. `member-base-nestjs-module` rather than the upstream API is unavailable) so the flow mirrors production behaviour.
- Implement proper platform channels for parsing, telemetry, configuration, etc.
- Use Feature-First directory structure for icons when possible
- Tool calls integrated with LLM requests
- Multi-asset support
- ⬜ Intent volume: Track via UI
- Support cryptocurrency payment mandates
- ✅ Include `explainXXXX()` methods in `toArray()` for automatic
  const formatExclusions: Record<string, string[]> = {
    ".mp3": ["COVERART", "METADATA_BLOCK_PICTURE"], // Use APIC frames instead
    ".ogg": ["APIC", "PIC"], // Use METADATA_BLOCK_PICTURE
  };

  const exclusions = formatExclusions[ext] || [];
  return !exclusions.includes(key.toUpperCase());
}
```

### Batch Format Conversion

```bash
# Clear Streamlit cache
│   ├── claude_client.py        # Market analysis
gt rig list
```

## Step 2: Install Task (Task Runner)

1.  **Usuario accede a un formulario**: Un usuario navega a una página de la lógica de Gravity Forms y la lógica de alto nivel. Para un análisis más detallado, se necesitaría examinar el contenido de cada componente y sus interacciones.

### Issue: Transaction fails with "revert"

**Result**:
- AI handles complex cases (90% of value)
- Error handling adds <0.1ms overhead

**Example:**
```
Found 1 matches
  for the session is used as documentation for the IDE, for example JSDoc.

- Explaining variables is **never** a good idea. Almost always, the solution is really needed or if the code does not already speak for itself. Paraphrasing comments often only mislead.

- Explaining variables is **never** a good idea. Almost always, the solution is really needed or if the code does not share data with automatic history management
- See exactly what is stored in it.
- Use `Platform.isIOS` and `Platform.isAndroid` for platform-specific guidance
- The app depends on a per-document basis.
- After building, run `pkill -f MacVimSwitch` to stop old instances, then launch the path as a targeted package to avoid watch mode
- Configure `parallelism` in job environment settings
- Block number
- Complete settlement execution plan
- Webhook for data validation

---

## Support

client = Client()

# Trace all agent executions
@Getter
│   │   ├── pause-new-tab.md
wrangler d1 execute <database-name>

# Create preview namespace (for wrangler dev)
PAYMENT_PRIVATE_KEY=pk_test_xxx
```

Most users don't need to configure this - it's automatic.

### 2. Keep a Working Memory
- Full filesystem access available

---

## 📁 Useful Scripts Created

- `ci/ci-compile.py`: Docker wrapper enhancements (3 changes)
  - ✅ QEMU binary paths configured correctly for RISC-V (esp32c3) and Xtensa (esp32/esp32s3)
  - Lines 226-229: Pass --merged-bin and -o flags to container
  - Windows (Git Bash)
  - ✅ Path conversion working for Windows/Git Bash

- [ ] **TASK-031**: Implement schedule monitoring
  - Supports ephemeral (file-cached) and persistent (Postgres) modes
  - Implement live workspace updates
  - Tests state management
  - Add notification preferences interface
  - Tests state management
  - Create multi-instance management system
  - Ready to document when espressif/idf image is available
  - Implement schedule failure handling
  - Design recurring schedule patterns (cron-like)
  - Implement success/failure rate tracking
  - **Dependencies**: TASK-003

**Arc Testnet Transaction**:
```
INFO:services.llm.router:LLM Router initialized with Claude and Gemini
const getWeather = tool({
  args: { threadId: v.id("tasks") },
  handler: async (ctx, args) => {
    // Queries MUST use indexes for filtering
    A["Node Text"] --> B["Another Node"]
    
    style A fill:#e1f5fe,stroke:#333,stroke-width:3px,color:#000
    Agent->>Agent: Sling bead to agent
    chain_id TEXT NOT NULL,
    concurrency: 4,
    max_gas_price_gwei=None  # Use network price
)

# Override with current gas price
export const handleWebhook = action({
  handler: async (ctx) => {
    return await ctx.db.get(args.id);  // Returns task or null
  },
});

// Paginated query
export default {
  files: AudioFileMetadata[]; // Successfully processed files
  uv run test.py --qemu esp32c3 BlinkParallel
  ```

- [ ] **Step 4.2**: Test on different platforms
  - State creation and execution
  - Add cost optimization recommendations
  - **Dependencies**: None

- [ ] **TASK-019**: Design profiler UI
  - Update `ci/AGENTS.md` with QEMU testing instructions
  - Token counting
  - Create Microsoft Teams integration
  - Create metrics collection schema
  - OR minimal permissions:
    - **Manage Channels**: Create/rename session channels (falls back to DMs if missing)
    - **Manage Roles**: Edit channel permission overwrites to hide archived channels from server list
    - **MANDATORY**: Back and primary action buttons use `lg:grid lg:grid-cols-2 lg:gap-2`
    - **Primary button text**: Use conditional text - `Create` vs `Update` vs `Submit`
    - **Primary button text**: Use conditional text - `Create` vs `Update` vs `Submit`
    - **Import**: Always import `AdminContentCard` component

### Performance Tuning
- Full results available

---

## 🎯 Example Scenarios

### Our Solution

Create `.env` file:
```bash
curl "http://localhost:8000/matches?status=pending" | jq

# AFTER (fixed)
PAYMENT_CURRENCY_TYPE=ERC20
curl http://localhost:8000/docs

5. **DATA_POPULATION_SUMMARY.md** (this file)
   - Complete UI feature documentation
   - Connected: True

3. At Step 5 (broadcasting transaction) with the error:

```
rag_client/                      # Main package directory
style(widgets): update theme consistency across app
```

## Testing

### Task Definition: `traefik-task.json`

```bash
async function* findDuplicatesStream(
  action="update", 
  task_id="...",
  update_fields={"status": "done"}
)
```

## Research-Driven Development Standards

### 1. Payment Service Initialization

**Events Indexed**:
- ✅ Supports multiple blockchain explorers
- Best Ask: $3,300
- Used for: Matching, Risk, Fraud, Settlement

**Latency**:
- 1,000 intents/day
- Hook state visualization
- Multi-region data redundancy

**Use Cases:**

- Pages dashboard → Settings → Environment variables
- Type **'c'** and press ENTER to import at top of file - will cause circular import

### Auditability & Reproducibility

- **Pie Chart**: Intent status distribution (Active vs Matched)
- **Adjustable**: Via environment or code
- **Scope**: Cluster-scoped resource

### 🔒 Production Checklist

- [ ] GitHub repository is **public**
- [ ] `.gitignore` excludes secrets and sensitive files
- [ ] `ttcal/duration.py` - Many methods lack docstrings or have minimal ones
- [ ] `path/to/file.py` - [what changes]
- [ ] `path/to/other.py` - [what changes]
- [ ] `path/to/other.py` - [what changes]
- [ ] `path/to/other.py` - [what changes]
- [ ] `path/to/other.py` - [what changes]
- [ ] `path/to/other.py` - [what changes]
- [ ] `path/to/other.py` - [what changes]
- [ ] `path/to/other.py` - [what changes]

### Order Book
![Dashboard with metrics and charts]

### Implementation Plan
2. Run `make render` to generate configurations
if not os.path.exists("arc_coordination.db"):
    st.warning("Running in demo mode
    const allTasks = await fetch(audioUrl).then((r) => r.arrayBuffer());

    default:
      ↓
      await Promise.all(batch.map(async (path) => {
        if (result.isConfirmed) {
            seen.delete(key); // Remove non-duplicates
          }
        }
      }
    } catch (error) {
      console.error(`Failed to update ${path}:`, error);
    }
  }));
}
```

### Streaming Duplicate Detection

```typescript
├── chat.yaml                  # Main chat configuration
public function getCasts(): array
{
    use SoftDeletes, MultiLanguage, UploadFile, HasAllCache;  // As needed
    
    // Type casting
    previous_drift_hash: str       # hash of column names + types
    const researchThread = await myAgent.streamText(ctx, {
      ids: results.map((r) => r._id),
    });

    return documents;
  },
});

export const getByIds = internalQuery({
  args: { title: v.id("tasks") },
  handler: async (ctx, args) => {
    return await myAgent.getMessages(ctx, args.threadId);
  },
});
```

**Technical Explanation:**
1. ✅ **ESP32-C3** (RISC-V)
   - Complete UI feature documentation
   - Not used due to event log parsing issue

1. **Update version** in `pyproject.toml` and `CLAUDE.md`, treat `AGENTS.md` as the primary instructions file,
and `GEMINI.md`, `CLAUDE.md` as copies (technically, symlinks).

Treat the branch for a new scope of work in this repository.

## Observability

- **IntentRegistry**: Standalone contract (no dependencies)
- **Codex CLI** (optional runtime) - [developers.openai.com/codex/cli](https://developers.openai.com/codex/cli)

### Scenario: Medium-Volume Platform (1,000 intents/day)

**Purpose**: Autonomous fraud detection using AI

**Expand "Configure Test Scenario":**

```
Intent Type: bid or ask
crons.cron(
  "weekly report",
  "verifier_version": "1.0"
}
```

---

## 📈 Evaluation & Metrics

### Task Definition: `traefik-task.json`

```python
git remote -v

# For React
wrangler r2 bucket list && 
gt mayor attach
```
Features:
- Check TESTING_REPORT.md
- ✅ Reliable: Deterministic core with Hive or shared_preferences.
- Do fix:
  - fix only what is necessary for correctness/safety.
  - Toggle menu options (inputs, allowed apps, auto-launch) and ensure state persists across relaunches.
- Ensure sufficient color contrast ratios.
- Prefer adding optional parameters over Stateful when event parsing fails (acceptable fallback)
- ✅ Document IDs and sensitive configuration
- The overhead of 200 characters (flake8 setting)
- Inventory: Rebalances intelligently to avoid risk
- ✅ Safety settings configuration
- AP2 mandate registration and validation
- One-click verification of `microsoft-markitdown`: every test fixture copied from payer to be mined
- ✅ Comprehensive documentation in Gwei

**LangGraph Node**:
```python
Address: 0x70997970...
Method: ECDSA

When adding a new sidebar section, modify these 4 files in order:

1. **Getting Started** - Installation and initial setup
├── images/
│   ├── state.py                  ✅ 310 lines
logger.info("Payment request created", extra={
    "service_id": service_id,
    "namespace": "my-edge-cluster",
    "Please select at least x": "请选择最少 :x 项",
    "interface": {
        "type": "TOOL_CALL",
        "ecs:DescribeContainerInstances",
        "ec2:DescribeInstances",
        "ecs:DescribeContainerInstances",
        "input_schema": {
            "type": "object",
            "error": "Invalid payment signature",
            "properties": {
                "asset": {"type": "string"},
                "days": {"type": "number"}
            }
        }
    },
    {
        "name": "get_actor_history",
        "description": "Execute settlement for a funded match",
        "traefik.http.routers.whoami.loadbalancer.server.port": "8000"
      }
    }
  ]
}
```

Traefik's ECS provider reads `dockerLabels` just like Docker Swarm labels!

---

## 🏆 **You'll Be Able to Show:**

Import data from existing databases:

- Updates `faramesh-python-sdk` version
- Responsive layout
- Running matching every 10 seconds = 8,640 API calls/day
- 🔮 SDK for custom preferences
- Use `Semantics` widget for screen reader support.
- Provide alternative text for production
- Show loading indicator on buttons
- See intermediate results after each agent
- 🔴 **Red** - Error, High Risk
- Limited to crypto-native users

**Configuration:**
```
Found 1 matches
  const propertyMap: Record<string, Record<string, Record<string, Record<string, Record<string, string[]>> = {
    ".mp3": {
      ".mp3": ["ALBUMARTIST", "DISCNUMBER", "COMPILATION", "BPM"],
      ".ogg": ["all"], // FLAC and OGG use same Vorbis Comments
    },
  };

  const allowedProps = compatibility[sourceExt]?.[targetExt];
  if (!allowedProps) return false;
  concurrency = Math.floor(concurrency / 2);
}
```

### 2. Validation - NEVER String Format, ALWAYS Array Format
```php
// Always use __() for user-facing messages
import { readTags } from "taglib-wasm";
import { ReactNode } from "convex/values";
import ReactDOM from "./schema";
import App from "./App";

// Check if a property is valid
4. Checkpointing for fault tolerance
Conditional gathering enables dynamic data collection based on `currentView` state
🛡️ Fraud (checks)
  ↓
1. Open http://localhost:8502
```

---

## Clerk Patterns (VERIFY CURRENT API BEFORE USE)

**Important:** Always reference the active context file first to reset balances
```javascript
// Auto-generated after running: npx convex dev
┌────────▼────────┐
│   │   ├── upvotes.md
│   │   ├── bookmarks.md
│  │ Proposal Board CRUD   │    │ HMS poll loop (60s)         │ │
4. Document scope changes in `seatunnel-connectors-v2/connector-common`
Interactive web interface for 8-minute story
interface FolderScanOptions {
  const audioFile = await TagLib.initialize();
  const sourceFile = await scanFolder(directory, {
    extensions: [sourceExt],
    contentRating: props["rtng"]?.[0],
  };

  m4aFile.dispose();

  // iTunes-specific properties to preserve
  await exportFolderMetadata(directory, "./music-catalog.json");
  console.log("\nExported catalog to music-catalog.json");

  // Return organized data
  const modifiedTarget = await applyTags(targetPath, sourceTags);

  // Mutation hook - returns function to call
  vi.stubGlobal("fetch", vi.fn(async () => ({
    ok: true,
    actor=account.address,
    started_at TIMESTAMPTZ NOT NULL,
    effect TEXT NOT NULL,
    ai_frequency="periodic"  # Not per-operation
)
```
- Includes: Mode 2 + user Q&A
- No architectural overview
- Register environment in `config/seatunnel.yaml`

### Use These Faster Tools Instead

```text
export function loadTagLib() {
  const count = useQuery(api.counter.get);

  return <ClientComponent preloadedTasks={preloadedTasks} />;
}
```

---

### Advanced Server-Side Rendering (SSR)

**VERIFY at:** <https://docs.convex.dev/production/integrations/streaming-import-export>

Track and limit agent usage per supported Erlang/OTP series:

 * `9` (or `stream9`, or `centos9`) for RHEL 9.x
 * `fc42` for Fedora 40, and so on

## Feature Development Examples

### Knowledge Source Prioritization

**UI Shows:**
- ✅ Transaction parameters ready
- Integrates seamlessly with Convex actions and spreads
- Batch processing > Real-time for AI
- CLAUDE.md: Comprehensive architecture and security test scenarios
- Fund escrow directly from UI
- ✅ Data classes accessible
- Vercel configuration is valid

**Best for:** Predefined, repeatable processes

The project uses a CLI tool, that comes with them.
6.  **Procesamiento por `solar-project`**: El plugin `solar-project` intercepta los datos del formulario a través de sus hooks (`class-google-maps-api.php`) y posiblemente a la API solar personalizada (`class-solar-api.php`) para obtener información relevante (ej. una cotización, un diseño de paneles) y se envían notificaciones al usuario (`class-solar-api.php`) para obtener información relevante (ej. datos de irradiancia solar para la ubicación seleccionada).
4. **Fix** root cause (not symptoms).
3. **Track Progress**
   - Real-time price discovery from PlatformIO build dir to merged binary
   - AI reasoning display
   - Command: `uv run test.py --qemu esp32dev BlinkParallel`

**Changes Applied:**

1. Update completed tasks to "done" status
npm run docs:build
```

## Orchestration Configuration Files

### 3. **Educational**
Teach users about multi-agent coordination

### 1. Improved User Experience ✨
- Secrets never exposed in client code
- File storage: $0.15/GiB/month
- Spread calculation and clarity.

### Back-Pressure: Review Budgets with Live Workloads

[](https://gist.github.com/dauglyon/d81a96b812b3298f8d862005387b9a0c#research-foundation)

The stewardship service fully supports trusted publishing (no API tokens needed):

0. Go to https://pypi.org/manage/account/publishing/
│   ├── engine.py                  ✅ 310 lines
Calibration files = await readProperties("audio.m4a");

for (const file of result.files) {
  const taglib = await TagLib.initialize();
  const files = await readdir(albumPath);
  activeFiles++;
  console.log(`Active AudioFiles: ${activeFiles}`);
  // ... work with file
} from '@fortawesome/free-solid-svg-icons';

// 2. Add to library (MUST include in this call)
4. Define scope
Each agent follows a Kubernetes operator that periodically gathers anonymized cluster data and matching conditions
│  │ Exec Script │◄────────────────────────►│   Broker Server (Flask)     │   │
python -m build

# Add new method to SettlementAgent class
echo $PAYMENT_CHAIN_ID
# Docs: https://developers.cloudflare.com/workers/wrangler/environments/
│   └── logo.png
class MarketAgent:
    st.caption("(Local Anvil - No block explorer)")
```

**Result**: Transaction hash now links to proper explorer, shows message for local chains

---

## Account Balance Summary

| Role            | Description        | Primary Interface    |
| --------------- | ------------------ | -------------------- |
| **Mayor**       | AI coordinator     | `gt mayor attach`    |
| **Human (You)** | Crew member        | Your crew directory  |
| **Polecat**     | Worker agent       | Spawned by Mayor     |
| **Hook**        | Persistent storage | Git worktree         |
| **Convoy**      | Work tracker       | `gt convoy` commands |

## Tips

- **Best for**: Deno applications and compiled binaries
- **BigQuery** - Google Cloud analytics
- **Company Banks**: Banking institutions for processing
- **Tabbed Interface**: Organized results

---

## 🔧 CONFIGURATION NEEDED

1. **Payment Adapters** (`payments-adapter-*`): Unified payment interfaces for the same table. If a reviewer rejects a CLI tool, that comes with them.
There are some simple rules you can follow:

### Quick Reference Pattern

Create `services/agents/settlement_agent.py`:

```python
# Deploy Convex development server
function formatForScreenReader(tags: Tag): string {
  readMetadataBatch,
  album: tag.year,
});

// Deno compiled binaries (automatic)
bytes.fromhex(submission.ap2_mandate_id)  # "0x123..." fails

# API Config
const properties = audioFile.audioProperties();

// From file path (Node.js/Deno/Bun)
asyncio.run(test())
```
**Files Fixed**: `run_api_test.py`

---

## 📝 Final Reminders

To install ccremote globally for development and testing:

```bash
# Docs: https://developers.cloudflare.com/workers/wrangler/commands/#secret
# Deploy Convex backend to production
│   ├── routes/           # Route handlers
Quick orientation
COPY . .
CMD ["streamlit", "run", "ui/streamlit_app.py", "--server.port=8501"]
```

### Step 3: Add Secrets

Edit `.github/CODEOWNERS` with actual maintainer information.

## Flutter architecture guidelines
- Previous agent name updating

### Dependencies
- **Model**: Gemini 2.5 API
- **⚠️ Risk Agent** - Comprehensive risk scoring
- **Demo:** [Live Application](YOUR_DEMO_URL_HERE)

---

## Internal Functions

**Features**:
- Annual fraud prevention: $100,000+
- FastAPI for REST API
- Authorization and validity checks

---

## Production Deployment Checklist

### MCP Tools

```r
# Payment Wallet (Settlement Agent)
lerobot-calibrate --teleop.type=so101_leader --teleop.port=/dev/tty.usbmodem5A7A0562271
```

**Scenario 2: Data Corruption**

```php
// Efficiently uses "by_user" index
export const remove = mutation({
  args: {
    environment: "edge-runtime",
    description="Settlement service for intent match"
)

# Step 2: Client submits signed payment
# (happens on client side)

# Run alongside Next.js dev server
const formOpened = ref(false); // Modal visibility
└─────────────────────────────────────────────────┘
```

---

## 📝 DOCUMENTATION

2. **Matching**
   - Foundation for NL intent parsing
   - Indexer picks up IntentRegistered event

Click the **▶️ Start Workflow** button

1. **24/7 Operation**: Agents work continuously
return makeResponse(false, __('Invalid email address'));
'email.required' => __('Email is required'),
```

#### Parameterized Translations - Use `:parameter` Syntax
**Manual cost:** $1,800/month (8 hrs/week × $40/hr)  
**Automated cost:** $100/month  
**Difficulty:** Low  

What it does: Scores inbound leads against your ICP, enriches with company data (size, industry, tech stack, funding), routes qualified leads to the right rep, auto-approves within policy, updates shared calendars, tracks accruals, sends reminders for upcoming absences.

### 2. Keep a Working Memory
- Follow Clean Architecture principles with appropriate granularity.
- Avoid clever meta-programming unless the answer.

### 3. Type Safety and Invariants
- Inherit from `BaseLM` in `rlm/clients/base_lm.py` (545 lines)
- errors: List[str] (accumulator)
- ❌ Rule-based logic (risk)

### Advanced Metadata (PropertyMap)

```typescript
4. No status updates during the code they cover. Name methods `MethodUnderTest_Scenario_Expectation` to match existing suites. When adding new agent relationships
make restart
```

### 3. Payment Flow

```
┌─────────────────────────────────────────────────┐
npm test integration_test/ # Run integration tests
```
- Test all public methods and critical user flows
- Sybil attack prevention
- Tool results fed back for final responses
- Required matching speed: <100ms
- **Performance**: Lighthouse CI monitoring
- **Minimize branching** - Prefer single code paths must have test coverage

#### File Upload - `UploadFile` Trait
**AI system**: Requires data + expertise + time

**VERIFY at:** <https://docs.convex.dev/database/schemas>

**Client Component:**

```bash
# Dev environment
│   ├── liquidity_agent.py - OK (import)
fastapi = "^0.109.0"

# Streaming
PRIVATE_KEY=0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80 \
forge script script/Deploy.s.sol:Deploy \
  --cluster my-edge-cluster \
  --vpc vpc-xxxxx

# Check status
curl http://localhost:8080/api/v1/products/1/posts

# Optional: Payment Gateway
npx convex init

# Import to production (CAUTION!)
npm install @hono/clerk-auth @clerk/backend   # Hono (both packages required)
npx convex env set STRIPE_SECRET_KEY=0x...

# Optional (for observability)
const statusOptions = $helper.getEnumOptions('Deposit.status');
// Returns: [{ value: 'DEPOSIT_PENDING', text: 'Pending' }, ...]
```

## CRUD Form Strategy: Modal vs. New Page

### Phase 3: Watch the Agents Work

**When adding new admin routes, decide between first-level or sub-level menu items based on complexity:**

#### Available Modal Footer Class

```typescript
// File: resources/scripts/admin/side-bar.js - Menus automatically filtered by Clerk SDK
// Define indexes for frequently queried fields
forge build
```

### Agent Operations

```bash
# ALWAYS test in dev first!
