# CLAUDE.md

4. Should open Arc testnet explorer in `submit_intent` endpoint
npx convex import --table tasks tasks.jsonl --replace

# Get Hyperdrive config details
aiohttp = "^3.9.0"  # For event bus
cd contracts
Gmail integration_test.py         ✅ 455 lines
│         Streamlit UI (Real-time viz)            │
PAYMENT_RPC_URL=https://your-deployment.convex.cloud

# Staging (separate project)
import { TagLib } from "taglib-wasm/simple";
import { TagLib } from "convex/values";
import { agent } from "convex/values";
import { components } from "./_generated/api";

// Create RAG component
Added direct database insertion
│   ├── claude_client.py   ⬜ TODO
npx create cloudflare@latest my-app
