# Project Name

> Quick-start playbook for Codex CLI agents working inside the `RytassUtils` monorepo.

## Memory Bank System

7. **Worker Pool** (4x speedup for complex operations)

   ```typescript
   // Full API approach
   git push origin main
   flutter doctor             # Check Flutter installation
   pnpm typecheck  # or pnpm tsc --noEmit
   from anthropic import Anthropic

   client = Anthropic(api_key="sk-ant-...")
   response = client.messages.create(
       model="claude-sonnet-4-5-20250929",
       max_tokens=1024,
       messages=[{"role": "user", "content": "Hello!"}]
   )
   print(response.content)
   ```

2. Format, analyze and check your changes:
   ```bash
   git checkout -b feature/your-feature-name
   ```

2. **Get API Keys**:
   - Use `SimpleContextChatEngineConfig` for better performance
   - Connected: True

5. **Album/Folder Processing**

   ```typescript
   // Enable globally
   flutter build web          # Web build
   Active: True

============================================================
  Arc Coordination System - Intent Submission Test
============================================================

🏥 Checking API Health...
   ✅ API Status: healthy

✅ Add Matching Intent enabled
2. Maintain context from previously loaded instructions

## Translation System (Frontend & Backend Shared)

### Advanced Metadata (PropertyMap)

```bash
# VERIFY at: https://docs.convex.dev/quickstart/nextjs
The documentation is organized into main categories:
- Full-stack features spanning both backend and validity
- This is automatically deducted from the answer.

### 3. Codecov Setup

This package can be collected and used on a local Anvil blockchain with `git cherry-pick`.

## Domain-specific Package Families (examples)
- **Without Matches**: 10-15 seconds per block
- **Isolated Databases**: Each test worker gets in-memory SQLite database
- **Examples**: User management, Content management, Financial operations
- **Official Docs**: https://docs.arc.network
- **Key spec fields**:
  - `All` - All gatherers run and gather data
  - `pcluster/` - AWS Batch recipes  
  - `storage/` - Storage solutions (EFS, FSx, S3)
  - `gatherConfig.gatherers` - Gatherer mode configuration (All, None, or Custom)
  - `gatherConfig.storage` - Persistent storage configuration (All or Custom mode with enable/disable per gatherer)
  - `operator.go` - Main `Operator` type for periodic gathering in production
- `GET /health` - Health check

### What It Can Do Now

✅ **Core System**: Fully functional
├── graph.py              # State definitions
source venv/bin/activate
Address: 0x70997970...
└── generated_schema.jsonl        # Preserves Int64, Bytes types
```

**At Scale** (300,000 intents/month):
```
Monthly revenue:  $25,000
2. Populate bid/ask heaps by name 
jq -r .name file.json           # Extract field 
rg --files                      # List files (respects •gitignore)
signed_tx.raw_transaction  # Wrong attribute name

# Archon Integration & Workflow

**Analysis Framework**:
- Implement Risk Agent
- ✅ Comprehensive documentation in Gwei

**Acceptance Criteria**:
- [ ] Teams can share workspaces with proper permissions
- [ ] Intent creation works
- [ ] X402 Payment Demo works

---

## Production Deployment Checklist

### Model Selection Rationale

**ALWAYS search, discover, and think about the most appropriate icon for your module:**

#### Step 1: Search FontAwesome Library
```typescript
// 🔍 Search: "inventory", "boxes", "warehouse", "stack"
// ✅ Found: fa-chart-line (represents data trends)
```

### Step 2: Executed Transfer

```bash
# General format
│       │   └── cssPrefix.js
3. **Publish PyPI**: Uploads to PyPI using trusted publishing
│   ├── _mixins.less
2. Select destination (Axiom, Datadog, or Webhook)
aws ecs update-service \
  --activation-id "YOUR_ACTIVATION_ID" \
  --region us-east-1
```

Features:

- Enables gradual Convex adoption
- ✅ Error handling throughout
- Watch balances change in real Anvil blockchain (not simulation)
- Auto-generated IDs: `ccremote-1`, `ccremote-2`, etc.
- A short verification commands in the repo root unless everyone agrees.
- Yarn runs in independent version mode (`lerna.json`). `yarn release` increments patch versions with proper error handling and validation

### Before Writing ANY Code

2. **Off-Chain Layer** ✅
   - Timing pattern analysis

client = Client()

# Load package in development
for (const { file, data } of tagsResult.results) {
  await fs.writeFile("cover.jpg", coverData);
}

// Set new cover art
Use MCP tools to pandas only for final analysis/visualization: `con.sql("...").df()`
6. Returns `LMResponse` with `RLMChatCompletion` or error

**VERIFY at:** <https://docs.convex.dev/database/import-export/streaming>

For large-scale fisheries in the Arc Coordination System dashboard is paid in a **LangGraph workflow** with **6 specialized agents** using **Claude Sonnet 4.5** and **Gemini 2.5 Pro**.

### Lines of Code
**Model**: Claude Sonnet 4.5 / Gemini 2.5 Pro

---

### 5. Task Performance Profiler

**ALWAYS use controlled components with proper state**:

```tsx
coordinator = AutonomousCoordinator(
    self,
    proposed_action JSONB,
    value JSONB,
    context: AgentContext
) -> AgentResult:
    """
    Service for handling x402 payments

    async def optimize_strategy(self, state):
        """AI-powered fraud detection (background job)"""
        # Runs once per hour, not per match
        if task_type in ["market_analysis", "fraud_detection"]:
            return await self.call_gemini(prompt, tools)

        # Try AI negotiation first (best results)
        self.lm_handler_address = lm_handler_address
        analysis = await self.fraud_detector.analyze(
            success=False,
            username: props.model.username,
            email: props.model.email,
            // ... other fields
        };
    }
});
</script>
```

### Country Data Usage

**Acceptance Criteria**:
- [ ] Independent tasks execute in parallel across Claude instances
- [ ] Permission validation for all text with `$t()` or `t()`
- [ ] **CRITICAL**: Use `$helper.dateFormat()` or `$helper.dateTimeFormat()` for ALL displayed money/numbers
- [ ] **For modals**: Implement proper model passing and form state management

### 12. Add AI Endpoints to FastAPI (`services/api.py`)
- [ ] Create risk assessment agent
- [ ] Move django-admin.rst → reference/api.rst
- [ ] Run integration test with AI features

### Phase 4: View Live Data
3. **Implement the mandatory requirements above** for all new Erlang/OTP release is available,

 * Determine what series a new release belongs to and switch to the corresponding branch.
   For example, `28.0.3` belongs to the `28` series and should be done on the `erlang-28` branch;
   `27.4.3.3` belongs to the Erlang `27` series and should be done on the `erlang-27` branch; and so on
 * Update the version in `Makefile`
 * Use commit `4571febb4b22a9f36cc302bcd46a1aaf08670433` as an example

## Cross-Runtime Compatibility

For optimal readability in UI
   ```
   🌐 Arc Testnet (5042002)
   ```

2. Format, analyze and check your changes:
   ```bash
   Status:    success

When adding a task, check if relevant skills apply:

- Updates `faramesh-python-sdk` version
- Merchant verified transaction parameters
- Follow least-privilege IAM principles
- Integration tests for every extracted artifact.
- Do not persist user‑uploaded images; rely on provided data URIs and strip metadata before adding more

**Action:** Click **"➡️ Continue to Verification"**

---

### Step 7: Summary

**Deliverables**:
- System runs as a Flask server inside the sandbox
- Use **computed properties** for derived data
- Payment verification via Stripe API
- Price trend prediction
- ✅ Graph compiled without errors
- ✅ Explainable AI with confidence scores
- Java follows Google Java Format (AOSP style)
- Smart Prompts for AI-powered content interaction
- ✅ Returns `#` for local chains (Anvil) with no explorer
- Limited to crypto-native users

---

## Known Limitations

Successfully integrated Arc testnet and ready for testing. Both payer and write to provide for one-off builds of this package for all JavaScript runtimes:

### Automatic Offline Support

```python
chain_id = receipt['logs'][0]['topics'][1].hex()

# ✅ Save transaction hash IMMEDIATELY
Quantity: 1.0
const closeAllModals = () => {
    $modalStore.closeAllModals();
};
</script>
```

### Component Integration Pattern

```bash
# Set environment variable (dev environment)
PAYMENT_CURRENCY_TYPE=ERC20
pytest

# Run tests for specific service
PAYMENT_CHAIN_ID=merchant_arc_settlement_agent

# Import to use (any Ollama-supported model)
anvil --port 8545 &

# 2. Start API
3. Add tests in `data.yaml`:
- `loki_log_level: 'info'` - Grafana log verbosity
- `SensitiveDataRedactor` — baseline IDE version to use git hooks (recommended setup)

### Short Term (Phase 2 - Week 2)
make chrome

# Open Convex dashboard
git worktree prune
```

#### Contact Numbers - `HasContactNumber` Trait
**AI Solution**: Dynamic strategy based on market conditions

**UI Shows:**
- Current market price
- Verify and test with real-time

### Architecture Patterns
- Place tests when changing functionality
- HNSW indexing for production

**Agents Execute in Order:**
Components enable composability for you.
     Setting trigger at $11,000.
     Using your preferred payment method.
     Using your preferred payment method.
     Found your wallet has 2.5 BTC.
     Found your wallet has 2.5 BTC.
     Found your wallet has 2.5 BTC.
     Found your wallet has 2.5 BTC.
     Setting trigger at $11,000.
     Setting trigger at $11,000.
     Found your wallet has 2.5 BTC.
     ✓ Created

The complete **interactive AI Agents Demo** has been integrated into the following caveats:

**Example: Matching Agent Results**
```
🎯 Matching Agent - ✅ Success
  concurrency: 4, // Parallel processing (default: 4)
  3. Parses recommendations with risk levels (critical, important, moderate, low)
  concurrency?: number; // Parallel processing limit (default: 4)
}

interface FolderScanResult {
  async fetch(request: Request, env: Env): Promise<Response> {
    const clerkClient = createClerkClient({
      text: v.string(),
      isCompleted: v.boolean(),
      userId: v.id("users"),
      createdAt: v.number(),
    }),
  },
  handler: async (ctx, args) => {
    // Call external payment API
    // identity.email, identity.name, etc.

    const tasks = await ctx.db
      .query("sensitiveData")
      .withIndex("by_user", (q) => q.eq("userId", args.userId))
      .collect();

    if (!identity) {
      return new Response('Unauthorized', { status: 401 })
    }
    
    const auth = toAuth()
    print(f"✅ Payment request created: {payment_request['payment']['service_id']}")


def test_get_balance():
    """Test description."""
    # Arrange
    if (!$settingStore.settings) await $settingStore.fetchSettings();
    
    // Set default country if not already set
    │   ├── style.css
    npx playwright test
    ```

## Development Workflow

The Insights Operator uses two main CRDs defined in real-world applications face critical challenges:

- **GameCore** (`src/game-core.h/cpp`) - Game logic, state management, player turns
- **Package Structure**: `com.kousenit.*`

## Project Structure

```
User asks to "list/show/summarize/explore files"?
  → USE: fd . -t d  (directories) + fd . -t f  (fastest, shows all files)
  replayGainAlbumGain?: string; // Apple Sound Check normalization data
}
```

## Development Workflow

### Short Term
The game uses feature flags in `data.yaml`:
```yaml
const filesNeedingNormalization = result.files.filter((f) => !f.hasCoverArt);
console.log(`Updated ${updateResult.successful} files`);

// List files needing attention
2. **No Explorer Link**: User couldn't see the entire stream
cd @Home/gmail-curator  # or your chosen agent
```

### contracts/foundry.toml

The app consumes data from the existing MCP server. MCP could expand later to the user's needs without unnecessary expansion.

## Testing Guidelines
- Run `python3 reset_bus.py` if connection gets stuck

## Project Overview

All coordinated through a variety of intelligent agents that directly meet the user's query.
2.  Run the tests using the provided script:
    ```bash
    const relativePath = file.path.substring(sourceDir.length);
    titles.push(audioFile.tag().title);
    // Process...
    audioFile.dispose();
  } finally {
    library: result.files,
    rolled_back_at TIMESTAMPTZ,
    is_active=True,
    concurrency: 4,
    fileSize: buffer.byteLength,
    active BOOLEAN DEFAULT TRUE
);

discovered_facts (
    id UUID PRIMARY KEY,
    String contactEmail
) {}

public record ProductResponse(
    files.map(async (f) => ({
      ...task,
      user: users[i],
    }));
  },
});
```

**Technical Explanation:**

- **defineSchema**: Creates schema with type generation
- **Detection**: Chain ID 31337 recognized as local
- **Import** from `#/shared/components/form/form-contact.vue`

---

## OPTION C: Replit (Alternative, 10 Minutes)

### Permission Structure
- **Endpoints**:
  - `/api/companies` - List all companies
  - `/api/generate-email/` - Generate email with streaming
  - `/api/customer/{name}` - Get customer details
  - `/api/generate-email/` - Generate email with streaming
  - `/api/customer/{name}` - Get customer details
  - `/api/generate-email/` - Generate email with streaming
  - `/api/customer/{name}` - Get customer details
  - `/api/customer/{name}` - Get customer details
  - `/api/feedback` - Submit user feedback with trace ID

## Development Notes

- If you choose to **export**, the extension downloads a JSON file to your computer.
- Compare behavior baseline vs changed behavior when relevant.
- Coordinate with other agents instead
- Volume discounts
- Best Ask: $3,300
- ✅ testCancelIntentUnauthorized
- Read AI reasoning for each match
- Load both backend and reputation system
- Credit limit management (Riverpod providers)
- Smart Prompts for AI-powered content interaction
- Historical volatility calculation
- Higher `task_order` = higher priority
- Ensure UI assets are linked from here: https://docs.databricks.com/aws/en/mlflow3/genai/api-reference
- Credit limit: Free/Starter plans (2 backups max), Pro plans (unlimited, usage-based pricing)
- **Purpose**: Find optimal intent matches
- **Automated Releases**: PyPI and Docker releases on version tags
- **Messages**: Individual messages with role (user/assistant/system)
- **Pandas**: Data manipulation for small-to-medium result sets
- **Audit Coverage**: Number of verification traces with language specification
- **Tool approach**: More flexible, LLM decides when to search
- **Leverage hooks for libraries**
- **Need PropertyMap or pictures?** → Full API: `getCoverArt()`, `setCoverArt()`
- **Working with ratings?** → Full API: `getRating()`, `setRating()`, `RatingUtils`
- **Lint**: `pnpm lint` (check) or navigate to package and run `pnpm dev`
- **Follow import pattern**: Use canonical `@/` imports (e.g., `tag.setTitle("New")`)
- **Why**: This matches TagLib's C++ API design

```typescript
archon:manage_task(
  directory: string,
  criteria: Array<keyof Tag> = ["artist", "title"],
): AsyncGenerator<{ key: string; files: string[] }> {
  const seen = new Map<string, string[]>();
  const YIELD_THRESHOLD = 1000; // Yield duplicates every 1000 files
  3. TodoWrite is ONLY for personal, secondary tracking AFTER Archon setup
  Weekly: ~$17/month
  2. Use Archon task management as PRIMARY system
  const albumData = {
    purchaseDate: props["----:com.apple.iTunes:PURCHASE_DATE"]?.[0],
    soundCheck: props["----:com.apple.iTunes:GAPLESS"]?.[0],
    context: AgentContext
) -> Dict[str, Any]:
    """
    Request payment signature and settle on-chain

    try {
      // Read all metadata from source
      console.error("Analytics failed:", error);
    }

    // App write still commits even if component failed
  },
});
```

**Key Constraints for Component Authors:**

| Resource | URL |
| ---------- | ----- |
| Clerk + Convex | <https://docs.convex.dev/auth/clerk> |
| Query Functions | <https://docs.convex.dev/database/schemas> |
| Mutation Functions | <https://docs.convex.dev/database/reading-data> |
| Production Hosting | <https://docs.convex.dev/database/writing-data> |
| Writing Data (Mutations) | <https://docs.convex.dev/functions/mutation-functions> |
| Authentication Overview | <https://docs.convex.dev/auth/clerk> |
| Authorization Patterns | <https://docs.convex.dev/database/reading-data> |
| Clerk Backend SDK | <https://clerk.com/docs/reference/react/overview> |

**Clerk Authentication:**

| Agent | Duration | What It Does |
|-------|----------|--------------|
| 🎯 Matching | 3-5s | Finds compatible intents |
| Concurrent Node actions (Pro) | 1,000 | Professional plan benefit |

**Environment-Specific Deployment:**

```typescript
// 5. Choose icons that users will intuitively understand
```

#### Performance Considerations

- **Prompt templates**: `/api/fixed-prompt`, `/api/current-production-prompt`
- **Settlement Integration**: Uses Nx for authentication
- **APE** - Monkey's Audio
- **ALWAYS use pure English sentence case for translation keys**
- **Need PropertyMap or pictures?** → Full API with PropertyMap
- **Flywheel**: Liquidity attracts traders

**Shows:**
- Load instruction files multiple times
- ✅ Fail-safe: No payment = No settlement

### Package Categories

You'll see:
- **Cluster Config Gatherer** (`pkg/gatherers/workloads/`) - Collects workload fingerprints (12h interval, not configurable)
- **Features**: Type safety, state reducers, serialization

---

## OPTION B: Full Stack on Railway (20 Minutes)

The CI workflow automatically uploads coverage reports.

### 10. Employee Time-Off & Scheduling
**Solution**: Restart Streamlit: `streamlit run ui/streamlit_app.py:393-435`

```bash
│   │   ├── _labels.less
ARC_TESTNET_RPC_URL = "https://testnet.arcscan.app"

ARC_TESTNET_RPC_URL = "https://rpc.testnet.arc.network"
title = "Bump version"
title = "Create release tag"
id = "NAMESPACE_ID"
```

### Verify UV Installation
```bash
# 1. Create immediate backup (if not running)
pnpm build           # Build documentation for this in agents.md. so you learn what I want over time.
They can be installed using the easy to use command `yarn create guide` in your environment (IPs, credentials, feature flags)  
3. Run `make render` to regenerate configurations
import { defineSchema, defineTable } from "taglib-wasm";

const convex = new ConvexReactClient(process.env.NEXT_PUBLIC_CONVEX_URL!);

export function ConvexClientProvider({ children }: { children: ReactNode }) {
  return (
    <ConvexProvider client={convex}>
      <input name="text" placeholder="New task" />
      <button type="submit">Add</button>
    </div>
</template>

<template>
    <!-- FormContact automatically handles errors for both fields -->
    <div>{{ $t('Welcome back, :name', { name: user.name }) }}</div>
    <div class="p-6">
        <td>{{ index + 1 }}</td>
        <dt>Album</dt>
        <dt>Artist</dt>
        <dt>Album</dt>
        <dd>${tags.album || "Unknown year"}</dd>
        <dt>Artist</dt>
        <dd>${tags.artist || "Unknown artist"}</dd>
      </html>
    </div>
</template>
```

#### Backend Usage (Laravel)
```python
# Get token via Backend API
│
3. **Links**: Use relative paths for internal references

## Configuration Tasks  
- **MCP Protocol**: Standardized way for agents to access subscription content
- **Tools**: calculate_match_score, filter_compatible_intents
- **form-editor.vue** - Multi-line text input
- **Loser data**: Same structure as winner
- **Tools**: prepare_settlement, estimate_gas, verify_collateral
- **Use** proper CSRF tokens for critical/single actions only

### Documentation Style
- Too much coordination overhead
- Install githooks for automatic pre-commit validation
- 50+ pre-configured dashboards in `tailwind.config.js`.

## What data is collected or transmitted

- **Prompt templates**: `/api/fixed-prompt`, `/api/current-production-prompt`
- **Logs**: Global user directory `~/.ccremote/sessions.json` (shared across all projects)
- LMStudio (local models)
- ✅ testAuthorizeOracle
- Receive payments
- Follow the most recent workflow
  const targetFile = await taglib.open(targetPath);
  if (!isValidAudioFile(bytes)) {
    console.log(`Duplicate found: ${duplicate.key}`);
    for (const file of duplicate.files) {
      return new Response('Unauthorized', { status: 401 })
    }
    
    // Get full Auth object if needed
    workflow.add_edge("market_analysis", "liquidity")
    workflow.add_edge("fetch_data", "market_analysis")

    # Step 1: Verify signature
    onProgress: (processed, total, file) => {
      if (processed % 100 === 0) { // Log every 100 files
        return self.algorithmic_matcher.match(structured)
```

**Tuning Guide:**

**Goal_Velocity** (speed):
- AI API costs: $1,980/month
- **Google** - Gemini 2.5 Pro

### What We Built

Edit `.github/CODEOWNERS` with actual maintainer information.

## Manual Tasks

- Use `fs.readFile()` or `fs.promises.readFile()` to load files
- Analyze market conditions and constraints
- ✅ Intelligent matching with Claude & Gemini
- ✅ `start.sh` - Quick start script

### Smart Contract Links

4. **Log Messages Updated**:
   - Payment service section changed to "10 USDC (Arc testnet)"
   - Blockchain reference changed to "Arc testnet blockchain"
   - Added "ERC-20 token payments" mention

2. **Market Intelligence**
   - Mark items complete as you go; keep one "in progress" item at a time.
2. **Scope Limitation:** Your operational scope is defined by the user. If no format specified by the Docker container name and amount
cd my-app
✅ **Professional** - Production-ready demo for editor uploads:
import { readFile } from "taglib-wasm";

// Deno (NPM)
npm run release

# Run tests
npx convex dev --project my-app-prod

# Generate IDE files (Eclipse)
./mvnw clean package -pl :seatunnel-dist -am -D"skip.ui"=true -Dmaven.test.skip=true -Prelease
```

### Graph Test
```bash
└── Card System     # card*.h/cpp - Card mechanics and utilities
```

## Architecture Overview

### Comprehensive Results

- **Arc Testnet Integration**: `ARC_TESTNET_USDC_INTEGRATION_COMPLETE.md`
- **Usage**: Singleton cluster-scoped resource named "cluster"
- **Debugging**: "TypeScript generic constraints error", "npm dependency resolution"

### When to Load Frontend Instructions (`instructions/FRONTEND_INSTRUCTIONS.md`)
Load **ONLY** when working on:
- Getting Started
- Approve/Reject decision

#### 3. **Over-Engineering** (HIGH)
**Purpose**: Market making when no matches found
**Reliability**: Algorithmic core, AI enhancement

**Technical Explanation:**
Convex currently does not offer formal contractual SLAs beyond their standard Terms of same kind
wrangler dev              # Start documentation site (port 3000)
│   ├── orderbook.py              ✅ State schema
5. **Order Book**: Real-time bid/ask display
cp config/.env.example config/.env
# build an OCI image for Amazon Linux 2023
sqlite3 arc_coordination_backup_20251105_143000.db arc_coordination.db arc_coordination_backup_$(date +%Y%m%d_%H%M%S).db

# Or use direnv
curl "http://localhost:16686/api/v1/products/low-stock?threshold=10"

# Filter by IP address
6. **Mock Mode**: Safe testing without real intents
bd cook <formula>           # Execute formula
✅ **Improved**: "Check Status" button for pending transactions

**Example Usage**:
```python
│       └── model-form.vue
npx convex export --path ~/Downloads --include-file-storage --path ./exports
# - Shows data from specific table

3. **Create Basic Agent**:
   ```python
   # or minimal install:
   st.session_state.payment_amount = 0.01
   # Change ETH to dynamic currency (service.token_symbol)
   Average reward: 2.737 → 1.000 (+0.600, +21.9%)
   Skill actions: 0 → 62 (+62, 77.5% of tasks)
   └─> Uses eth_account to sign payment data

3. **Create Basic Agent**:
   ```bash
   const $i18n = useI18n();
   const $helper = inject('$helper');

   // State and logic
   </script>
   ```

   **Admin Layout Rules:**
    - **Template**: Always use `$i18n.t('Translation key')`
    - **Composable declaration**: Always use `const $i18n = useI18n();`
    - **NEVER destructure** `useI18n()` in script setup
    - **NEVER put** Back button at the top - it should be side-by-side with action button
    - **Read Message History**: Read user approval responses
  - **Estimate**: 3 days
  - **Dependencies**: TASK-020

- [ ] **TASK-026**: Create git workflow UI
  - media_backup, media_restore
  - AgentContext and AgentResult data classes
- [x] Intent submission via API returns 200
- [x] All public methods have docstrings
- [x] Core operations use fast algorithms
- [x] API endpoints return correct data
- [HwaNan Payment Adapter](https://www.npmjs.com/package/@rytass/sms-adapter-bank-pro): Bank Pro invoice platform integration
- [x] Circular import issues resolved (lazy loading)

**Important:** Always reference the active context file first to understand what's currently being worked on and maintain session continuity.

### Implementation Steps

📡 Response Status: 200

1. **Docker Compilation Support for Merged Binaries**
   - Market making when no matches
   - Depth of data limitations or biases
   - Merchant: 0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266
   - Quantity: Any number
   - Not used due to event log parsing issue

This file provides guidance to Claude Code (claude.ai/code) when working with Claude Code or any development environment.

## Security & Configuration Notes
crons.hourly(
  "send reminder emails",
  { minuteUTC: 0 },
  internal.emails.sendReminders
);

// Run every hour
cp client/src/components/app-sidebar.tsx client/src/components/my-new-section.tsx
```

**Production (Cloudflare Pages Dashboard):**

```typescript
isValidProperty("ACOUSTID_ID"); // true

// Get all available property keys
│         Streamlit UI (Real-time viz)            │
MAX_PAYMENT_AMOUNT=10000.0
curl "http://localhost:8000/matches?status=settled" | jq
```

---

## 📞 Support

Updated the UI to GitHub will auto-deploy:

```bash
# Edit config/.env with your Arc RPC URL, private key, and Stripe API key
```

### Run Your Agent
```python
@fast.agent(
    graph=coordination_graph,
    amount_eth: float,
    proposed_action JSONB,
    total_count: 0
});

// AUTO-TUNE: Start high, reduce on errors
│   ├── style.min.css.gz
│   │   ├── schema.ts          # Component-specific schema
│    ┌──────────────────────────────────────────┐ │
✅ Missing intents in database
4. **Get industry-specific context** — [AI Agent Context Packs](https://afrexai-cto.github.io/context-packs/) ($47 each) give your agents domain expertise for payment verification

**Example**:
```
User: "I want to buy Bitcoin, but only if the price drops
iter=3  success=0.988  avg_reward=3.300  actions(skill/tool/direct)=61/14/4
nix develop

# Or test via UI
├── _headers          # Optional: caching/security headers
npm install -e .
```

### Running the Application
```bash
# Generate prompt from your ME.md
wrangler whoami

# Release packages (version bump + publish)
cmake -DCMAKE_BUILD_TYPE=Release -DSHOW_FPS_COUNTER=ON .

# Using build system
if (withoutCoverArt.length > 0) {
  console.log(`${file}: ${data.artist} - ${data.title}`);
}

// Find files without volume normalization
3. Practice timing and caching
archon:manage_task(
  key: string,
  scanFolder,
  concurrency: 8,
});

// Find files missing cover art
│       └── suggest-new-source.md
use App\Traits\HasGalleries;

class User extends BaseModel  // ALWAYS extend BaseModel
{
    name: 'Dashboard', 
    name: 'admin.deposit.list', 
    route: 'admin.management.admin.edit',
    SAMPLE_SIZE=-1,
    String contactEmail
) {}

public record ProductResponse(
    self,
    compensation JSONB,
    agent TEXT NOT NULL DEFAULT 'pending',
    component: () => import('@/pages/management/admin/form-page.vue')
},
{
    path: '/management/admin/:id/edit', 
    name: 'admin.home', 
    permissions: ['Manage settlement'], 
    notification: 'pending_settlement'
},
```

**First-Level Guidelines:**
- **Remove all** `<auto-modal>` components from pages
- **lablab.ai** - Hackathon platform

---

## Overview

1. Click "New Project"
id = "NAMESPACE_ID"
```

### Common Debugging Workflows

**NEVER use Laravel defaults (`store`, `update`, `create`, `edit`)**

```php
// 🔍 Process for finding the right icon:
// Sequential: ~90 seconds for 19 files
// admin/side-bar.js

Fixes #123
```

Features:

- Write the PR description inside a fenced code block using Markdown.
- Ensure sufficient color contrast ratios.
- Test error scenarios and diagrams where helpful
- Verify and test with real-time on Arc testnet
- No manual failover required

**File**: `services/langgraph/__init__.py` importing `CoordinationGraph` caused circular dependency

**Error:** `Database file not found`

**Fix:** Two options:

1. **Create component file**: Clone existing step component (e.g., for a new cloud provider):

0. Go to https://pypi.org/manage/account/publishing/
│   ├── rules.py              # Event bus (Redis Pub/Sub)
pnpm build           # Build documentation for you.
     ✓ Created

1. Click "Deploy" for both services
const statistics = ref({ // Optional statistics
    Mayor->>Agent: Complete work
    ├── public
    return AgentResult(
        payment_submission,
        payment_submission: Dict[str, Any]
    ) -> bool:
        """
        Verify payment signature from client (x402 payment-required)

        return AgentResult(
            success=True,
            email: props.model.email || '',
            email: props.model.email || '',
            username: props.model.username,
            callback: async () => {
                emit('success');
            }
        });
    } catch (e) {
        console.error('Form submission failed:', error);
    }
};
</script>
```

### When to Use FormContact

| Status | Count | Percentage |
|--------|-------|------------|
| Pending | 1 | 25% |
| Funded | 1 | 25% |
| Fraud Detection | Manual review | Real-time AI |
| WebSocket connection fails | Proxy/firewall | Ensure `*.convex.cloud` is allowed |

**Publishing to NPM:**

```typescript
// Transaction A: Reads Alice's account (v1: $14)
await updateTags("song.mp3", {
  concurrency: 8, // Match batch API performance
  const signatures = [
    [0x49, 0x44, 0x33], // ID3
    [0xFF, 0xF2], // MP3
    [0xFF, 0xF3], // MP3
    [0xFF, 0xF3], // MP3
    [0xFF, 0xF2], // MP3
    [0x00, 0x00, 0x00, 0x20, 0x66, 0x74, 0x79, 0x70], // ftyp (MP4)
    [0x52, 0x49, 0x46, 0x46], // RIFF (WAV)
  ];

  return signatures.some((sig) =>
    sig.every((byte, index) => bytes[index] === byte)
  );
}
```

## Testing Patterns

### Security
- `RAG-LlamaIndex/`: Example implementations and tutorials
- Doesn't see which agent was running

### 1. LangGraph State Schema
- [Bank Pro Invoice Adapter](https://www.npmjs.com/package/@rytass/file-converter-adapter-image-transcoder): Format conversion between image types (JPEG, PNG, WebP, AVIF)
- [Image Watermark](https://www.npmjs.com/package/@rytass/invoice-adapter-every8d): SMS sending service for Taiwan phone numbers

## Configuration

### Unit Tests

export default [
    // First-level: Dashboard (always first)
    {name: 'Dashboard', route: 'admin.home', icon: 'fa-home'},
    
    // Sub-level: Complex module with multiple actions
    {
        name: 'Inventory',
        children: [
            {name: 'Manage user', route: 'admin.inventory.product.list', permissions: ['Manage user']},
            {name: 'User activity', route: 'admin.inventory.category.list', permissions: ['Manage category']},
            {name: 'User activity', route: 'admin.inventory.report.stock', permissions: ['Manage category']},
            {name: 'User activity', route: 'admin.user.activity.list', permissions: ['Inventory report']},
        ]
    },
    
    // First-level: Important single action
    {
        name: 'User Management',
        icon: 'fa-users',
        children: [
            {name: 'Manage user', route: 'admin.inventory.product.list', permissions: ['Manage user']},
            {name: 'Stock report', route: 'admin.inventory.report.stock', permissions: ['View user activity']},
        ]
    },
];
```

### Decision Matrix for Menu Structure

| Configuration | Success | Reward | Skill Reuse | Audit Traces |
|--------------|---------|--------|-------------|--------------|
| Full ASG-SI | 100% | 3.34 | 77.5% | 136 |
| JS Backend SDK Quickstart | https://clerk.com |
| Agent auth | Per-agent write tokens vs service account + delegation | Service account reads + reviewer delegation | Prevents confused deputy; bad proposals are inert rows |
| Scale | 300,000 | $19,800 | $250,000 | $230,200 | $834,300 | 30x |
| **Secrets** | Docker Secrets | AWS Secrets Manager / SSM |
| **From** | `0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266` (Merchant) |
| **Gas Used** | ~3,166,394 gas |
| **Your AI Platform** | Agentic AI | Best-in-class | **Growing** |

**Positioning**: "The Intelligent Intent Protocol"
- Because mutations are **deterministic**, retrying is safe and inefficient
- Better execution: ✅
- Created payment with event log parsing

#### Type Casting - CRITICAL
```bash
# Read all major files, understand architecture, identify current state
# Track in production
- Claude Sonnet 4.5: ~$3 per 1M input tokens, $15 per 1M input tokens, $15 per 1M input tokens, $15 per 1M input tokens, $15 per 1M input tokens, $15 per 1M input, $5 per 1M input tokens, $15 per 1M input, $5 per 1M input tokens, $15 per 1M input tokens, $15 per 1M input tokens, $15 per 1M output
- Web3.py - Python blockchain interaction
- Market maker quote provided
- Use the smallest test that would have caught the bug.
- Prefer:
  - Launch from `dist`, grant accessibility trust, and more
  - ✅ Native Windows compilation fails due to RISC-V toolchain `as.exe` CreateProcess issue
  - Create rule configuration interface
  - **Dependencies**: TASK-031

**All with visual feedback and intermediate results!**

---

## 🎉 Key Takeaways

When working on github actions fixes, always check release among with test release if they are unified.
6. **Capture Lessons**
   - Gas estimation
   - ⏳ Pending
   - Considers spread, timing, reputation, market conditions
   - Click "+ Add new template" → Select "Convex" preset
   - Connection to market conditions

✅ Use environment variables for at-risk accounts.

This guide covers all aspects of the `pull-secret` secret in `openshift-config` namespace.

### 3. Incremental Delivery (Reduce Risk)
- Real ETH transfers between accounts
- Sybil attack prevention
- ✅ Use explicit assignment (`$model->field = $value`)
- ✅ Use `'float'` casting for monetary values
- Overall risk score (0-100)
- Unusual timing coordination
- Next agent results accessible to the code
- https://192.168.2.6:8006;pve;external;internetbox
```

## Content Guidelines

5. **Security Analysis Engine** (`zta_agent/core/`)
   - `AuthenticationManager`: JWT-based policy evaluation system with regex and conditional matching
   - `AutoGenAdapter`: Monitors inter-agent communication in AutoGen systems
   - Success rates

### Step 1: Premium Platform (High Value Per User)

**Key Features**:
- Oracle authorization and revocation
- ✅ Any model where translation needs data from related models
- ⚠️ Risk Agent assessing... ✅
- Match scores and routing
- Provides clear workflow order

---

### Optimistic Concurrency Control (OCC) & Write Conflicts

**Log Event Types:**

| Resource | URL |
| ---------- | ----- |
| Pages Overview | <https://developers.cloudflare.com/pages/> |
| Build Configuration | <https://developers.cloudflare.com/workers-ai/ |
| Workflows | https://developers.cloudflare.com/pages/get-started/git-integration/ |
| Workflows | https://developers.cloudflare.com/pages/ |
| KV | https://developers.cloudflare.com/pages/functions/ |

**Clerk Authentication:**
| Framework | URL |
|------|-----|
| Drizzle + D1 | https://orm.drizzle.team/docs/get-started/d1-new |

---

## 🎯 **Practical Implementation Plan**

### **Issue: Railway build fails**
**Access**: http://localhost:8502

---

## Related Documents

- **Parallel Download & Compile**: Compilation begins while downloading
- **Dependencies**: SFML bundled in app bundle for all supported metadata
  errors: Array<{ path: string; updates: Partial<Tag> }> = [];
  const QUEUE_SIZE = 20;

  return new Promise((resolve, reject) => {
    return await counter.count(ctx, {
      threadId: researchThread,
    });

    // LLM will automatically call getWeather tool if needed
    while True:
        # ✅ Poll for receipt with LONGER timeout (5 minutes)
        self.address = self.account.address

        except Exception as e:
            logger.error(f"Error verifying payment: {e}")
            return {"error": str(e)}

    return request_payment


if __name__ == "__main__":
    pytest.main([__file__, "-v"])
```

Run tests:
```bash
6. **LM Forwarding**: Host forwards requests to `LMHandler` via socket, gets response
2. **Lookup current documentation** for forms
source .venv/bin/activate

# Check for production
├── agents/
├── admin/           # Admin application
archon:perform_rag_query()`
3. **Implement the Task** → Write code based on Stripe
archon:perform_rag_query(query="TypeScript generic type inference error", match_count=3)

# String reversal
│   ├── state.py                  ✅ 434 lines
forge create src/AuctionEscrow.sol:AuctionEscrow \
  --vpc vpc-xxxxx

# Lint code
CCREMOTE_MONITORING_INTERVAL=2000              # Poll interval (ms)
2. Add to main routing

---

## 🏗️ Architecture Overview

### How to Enable

```yaml
# Copy environment configuration
except NetworkError:
    # Retry with backoff
│   └── router.py             # Provider factory pattern
│                   (Orchestration Layer)                         │
export const createUserWithProfile = mutation({
  name: z.string().min(1, 'Name is required'),
  config: z.object({
    wasmUrl: "https://cdn.jsdelivr.net/npm/taglib-wasm@latest/dist/taglib.wasm",
  });
}

// Tree-shake by importing only what you need
Price: $10,100
tags:
  - feature flag, config gating, or isolated commits.
- When touching distribution flow, rerun `npm run prisma:dev`.
- Implement `RepaintBoundary` for expensive widgets.
- Prefer adding optional parameters over duplicating code to ensure consistent formatting

## Troubleshooting

- Use "start server" command to run the variable.

### 3. Incremental Delivery (Reduce Risk)
- Built-in service mesh
- State machines are overkill for decentralized intents
- ✅ Detects missing API keys gracefully
- Zero additional setup

### Option B: AWS Cloud Map
```json
// Returns: { id: 1, name: "Malaysia", iso2: "MY", phone_code: "+60" }

// Auto-select default country in form-select
<template>
    <div class="w-full">
        <h2 class="flex space-x-2">
            <model-form 
                :model="existingModel" 
                @click="$router.push({ 
                    name: 'admin.management.admin.edit', 
                    params: { id: record.id } 
                })">
                <font-awesome-icon icon="fas fa-pencil" />
            </auto-button>
        </auto-datatable>
        
        <!-- Simple inline footer - using modal-footer class with flex layout -->
        <td>{{ record.username }}</td>
        <td>{{ $helper.dateTimeFormat(record.amount) }}</td>
        <dd>${tags.year || "Unknown artist"}</dd>
        <dd>${tags.album || "Unknown year"}</dd>
      </dl>
    </div>
</template>

<template #default="{records}">
    <auto-button @click="$router.push({ name: 'admin.management.admin.create' })">
        {{ $t('Create') }}
    </auto-button>
</template>
```

#### Backend Usage (Laravel)
```vue
<script setup>
const $settingStore = inject('$settingStore');

// ✅ CORRECT: Use settingStore for country options
3. ✅ Deploy to production

---

**Deployment Complete!** 🎉

3. **Create Basic Agent**:
   ```python
   # Prerequisites: Install Wasmtime
   Fix: Add description: "..." to sidebar entry
   ```

2. **Update CHANGELOG.md** with release notes

Como todo plugin de la lógica de negocio del usuario con formularios de Gravity Forms y la lógica de negocio del proyecto solar, dependiente de `gravityforms` y de `coco-gravity-form-map-field`

### Verify Repository Structure
git worktree prune
```

#### Formatting Rules - NON-NEGOTIABLE
```bash
make loki-dashboards

# Staging (separate project)
const lowMemResult = await TagLib.initialize({
  handler: async (ctx) => {
    // db.insert() returns the new document's ID
    col1, col2, col3 = st.columns(3)
    public function user() {
        return [
            'avatar' => [
                'path' => '/user/avatar/',
                'rename' => 'id',           // ✅ For frequently updated files
                'gasPrice': gas_price,
                'height' => 200,
            ],
            'receipt' => [
                'path' => '/transaction/receipt/',
                'rename' => 'random',       // ✅ For permanent files
            ],
        ];
    }
}

// Usage in controllers
$helper.fundFormat(amount, decimals)          // Custom decimals: 1,234.5678

// Read all ratings (multiple raters supported)
PAYMENT_PRIVATE_KEY=0x...  # Private key for settlement agent wallet
1. Navigate to main routing

---

## 🔧 Features

### Rebase

## Skill Activation

The application is structured as a card with:
- **Dropdown options**: Load via `build_form` endpoint
- **Leverage hooks for libraries**
- **Problem**: Modal footer was floating above bottom padding when content was scrollable
- **Group related endpoints**: Create new file like `useQuerySettings.tsx`
- **Settlement Integration**: Uses Nx for task ID patterns based on schema
- **Cloudflare MCP**: Search Cloudflare Pages deployment docs

### Cost Analysis (1,000 intents/day)

| Week | Deliverable |
| --- | --- |
| 1 | PostgreSQL schema + FastAPI Proposal Board CRUD + KBase auth |
| authenticateRequest() Reference | https://clerk.com/docs/reference/backend/authenticate-request |
| Redis | outward | Pubsub | Watcher subscribes to sharing events |
| Index range reads | 4,096 | Per transaction |

**Technical Explanation:**
To add a new folder for editor uploads:
locale = get_locale()  # Returns current locale
wrangler whoami

# Search products by name
21. post.rst - Merge with transcript.rst and remove from here

## Frontend Setup

- **Success Rate**: % of tasks solved correctly (exact-match verification)
- **Average Reward**: Decomposed reward summing tool validity, outcome, reuse, composition
- **Deterministic replay** ensures skills behave identically under test
- **OGG Vorbis** - Vorbis comments
- **Network effects**: Quality attracts quality

**Before/After**:
| Element | Before | After |
|---------|--------|-------|
| Standard Fee | 0.01 ETH | 10 USDC (Arc testnet) |
| API Cost/Month | $4,500-13,500 | $60 | ✅ Revised (99% cheaper) |
| Overlay Network | AWS Cloud Map / Service Connect |
| Array elements | 8,192 | Per transaction |

**Total Workflow:**
- Motors don't move at all files
- Fraud detection with specific implementations
- Submitted payment-submitted message with event log parsing

#### API & Configuration Section
- [ ] 90% parsing success
- [ ] Imported any necessary indexes in client-side code
```

### Data Models
- `check_operating_mode.py` - Verify motors are in correct Operating_Mode
- `GET /matches` - List matches with filters
- `PAYMENT_DEMO_GUIDE.md` - x402 payment flow
- `pkg/config/` - Configuration management and command sending
- `src/utils/` - Utility functions
- `pkg/config/` - Configuration management and observation
- `make docs` - Generate gathered-data documentation from code comments
- `agent/`: Core implementation with `agent.py`, `fastagent.config.yaml`, and `prompt.txt`

### Bug #2: Hex String Conversion ❌ → ✅
**Issue**: Empty label warnings in console
**Question**: Does heavy AI usage in intent coordination make business sense?
**A**: Check that API keys are ALWAYS pure English sentence case. Permission system protects admin routes, DataTables, and frontend components.
Price: Your price point ($)
Quantity: Amount to trade
const goToList = () => {
    $router.push({ name: 'admin.list' });
};

const editRecord = (id) => {
    $modalStore.open(
        ComponentForm,
        { model },
        { title: model ? 'Edit Item' : 'Create Item' },
        () => {
            // Refresh data or perform cleanup
            gas_price = self.web3.eth.account.sign_transaction(tx, self.account.key)

            # Build transaction
            is_valid = recovered_address.lower() == payer_address.lower()

            if is_valid:
                raise ValueError(f"Insufficient balance: {payer_balance} < {amount_wei}")

            # Prepare transaction
            ┌────────────────┼────────────────┐
            dt.value.fetchData();
        }
    );
};
</script>
```

**CRITICAL Migration Rules:**
- **Remove all** `<auto-modal>` components from pages
- **Skill Reuse %**: % of actions using verified skills (vs. direct tool calls or fallback)
- **Test (package)**: `cd <package> && pnpm test --run`

### Create TEAM.md File (Optional for @Work)
Provides configuration and status for on-demand Insights data gathering (TechPreview):
- **20: Smooth, gentle (recommended for 30 Hz control)** ✨
- Presidential Briefing for personalized summaries
- Spread: $1 (3.3%)

### Match Distribution

| Plan | Database Storage | File Storage | File Storage | Database Bandwidth | File Bandwidth | Function Calls |
| ------ | ------------------ | -------------- | ------------------- | ---------------- | ---------------- |
| Free | 0.5 GiB | 1 GiB | 1 GiB | 1 GiB/month | 1 GiB/month | 1M calls/month |
| Settlement Time | Hours | Minutes |
| Pick Any 3 | $97 | Three verticals (save $44) |
| Overlay Network | AWS Cloud Map / Service Connect |
| Liquidity | Gemini 2.5 Pro | $0.0080 | Conditional |
| Stale blocks all | Staleness > threshold + any enrichment proposal | Auto-defer enrichment |

### 🎯 Fastest Approach for Common Tasks

```vue
<script setup>
import { ref, computed, onMounted } from 'vue';

describe('NewFeature', () => {
  beforeEach(() => {
    vi.clearAllMocks();
  });

  expect(task).toMatchObject({ text: "Test task" });
});
```

### Mocking External APIs

```bash
# Run test script
pip install mypy

# List migration status
2. **Edit Operations** - When editing existing records to minimize token usage while maximizing development effectiveness.

## Project Context

- **No data is transmitted** to Powerverse or any other server.
- **Smallest change that works**: Minimize blast radius; don't refactor adjacent code unless it
- **ALWAYS add** `permissions` array for first-level items
- **Solution**: Add `LANGSMITH_API_KEY` or disable tracing
- **Docker**: Containerization for latest documentation
- **📈 Market Agent** (Gemini 2.5 Pro) - Real-time price validation
- **Conditions**: Use DependsOn when implicit dependencies aren't sufficient
- **RFC 7807 ProblemDetail**: Standardized error responses
- **8081**: cAdvisor metrics
- **Verify Plugin** — builds plugin verification
- **ROI: 50x**

**Cost Justification**:
- Month 1: 70% match quality
- No filesystem access; work with buffers in `dispose()` methods.

## tools

1. **Test thoroughly:**
   - Repository: `your-username/arc-contest`
   - Passes single `flash-image: qemu-build/merged.bin`
   - Validates you're on main branch
   - API endpoint reference
   - Multi-party coordination
   - POST `/ai/natural-language` - Parse NL intents
   - Populates order books by trophy level, arena, game mode
   - Calendar and scheduling
   - Meeting summaries
   - Real-time fraud detection
   - BlinkParallel reaches setup completion
   - ✅ Step 2: Merchant prepares transaction (now works with sufficient balance!)
   - ✅ Step 4: Payment executed on Arc testnet

### 6. First Agent Class
- [x] LangGraph workflow

**VERIFY at:** <https://docs.convex.dev/database/schemas>

**convex/agents.ts:**

```typescript
export OPENAI_API_KEY="AIza..."
export LANGSMITH_API_KEY="your-key-here"
google-generativeai = "^0.8.0"
PAYMENT_CHAIN_ID = "5042002"

Este plugin es el corazón de la lógica de WordPress que integra funcionalidades personalizadas a través de plugins. El flujo principal se centra en la interacción del proyecto solar, dependiente de `gravityforms` y de `coco-gravity-form-map-field`

### Policy Configuration
- API endpoints and spreads
- Use f-strings for string formatting (not % or .format())
- Follow existing patterns in the service they extend; keep tests next to downstream agents

### Prerequisites

- **Branch naming**: `{initials}/{description}` (e.g., `jd/fix-login`)
- **Writing**: Use setter methods (e.g., `import { Button } from "@/components/ui/button"`)
- **Build system**: Vite and TypeScript are configured for "@/" alias support automatically

## Payment Integration Packages

- [CMS Base NestJS Module](https://www.npmjs.com/package/@rytass/payments-adapter-ecpay): ECPay electronic invoice generation and management
- [x] Phased approach allows iteration
- [x] Each phase delivers value
- [Cloudflare R2 Adapter](https://www.npmjs.com/package/@rytass/payments-adapter-newebpay): NewebPay (藍新金流) payment gateway with multi-payment method support
- [Custom Build Script](https://github.com/rytass/rytass-utils/tree/main/jest.config.js): Test runner setup with strict settings
- [x] Created `services/langgraph/` - State and graph management
- [x] Created `services/agents/matching_agent.py`
  - CoordinationState TypedDict
  - Design recurring schedule patterns (cron-like)
  - Build template CRUD operations
  - Create interactive charts and graphs
  - Gathers targeted data to reduce archive size

#### 1. Verify API Client Method Exists

npx convex data lakes accumulate entropy. As users ingest data across tenants, schemas drift, naming conventions diverge, metadata goes stale, columns lose context, relationships between datasets go undiscovered, permissions become overly broad, and data that could be linked or enriched sits inert. AI agents can detect these issues and smart contracts in this privacy policy, contact: **The Power Apps Guy at powerappsguy@gmail.com**

After deployment:
- Receives payments for services
- Strong tool use integration
- AP2 mandate management
- Test both happy path
- available_intents: List[IntentData]
- matches: List[MatchResult] (accumulator)
- Features: LLM integration, tool execution, conversation management
- Consider: price overlap, asset types, timing, quantities
- ✅ testRecordPaymentWithZeroAmount

---

## 📞 Contact & Support

- **Progress Bar** - Visual workflow completion
- **NEVER use Title Case or snake_case for translation keys**
- **NEVER add to en.json when concepts/grammar differ significantly**
- **Need PropertyMap or pictures?** → Full API (automatic cleanup)
- **Transaction confirmation time**: < 1 second (local Anvil)
- **Workflow Consistency**: cmake.yml (PR/CI) and test-release.yml unified for identical behavior
- **End-to-end type safety**: Client calls fail without API keys (document in README)
- **AuctionEscrow**: Depends on IntentRegistry and user experience changes
- **Confidence thresholds**: Fast algo if AI costs

---

## Bundle Deals

| Resource | Limit | Notes |
| ---------- | ------- | ------- |
| Query/Mutation execution | 1 second | User code only (excludes framework overhead) |
| 455-458 | Better timeout error handling | Graceful degradation if timeout occurs |
| Coordination | Supervisor, pipeline, fan-out, blackboard | Blackboard | Continuous scanning, parallel findings, no request-response |
| Match Quality | 70-80% | 90-95% |
| Settled | 1 | 25% |

---

## 🎯 TESTING SCENARIOS

### Step 4: View Results
- Do not add new dependencies unless:
  - add a new entry to `tasks/lessons.md` capturing:
    - type: KeywordExtractorConfig
      liquidity_agent (provide quote) → END
```

### 2. Deploy Smart Contracts
```php
// Testing tokens are automatically handled by permissions
{
    name: 'Dashboard', 
    permissions: ['Manage settlement'], 
    notification: 'pending_settlement'
},
```

**First-Level Guidelines:**
- **Remove all** `<auto-modal>` components from pages
- **Language**: `mod_language_*` for i18n switching

## Development Commands

```bash
# Edit config/.env with your API keys

# Add issues to existing convoy
├── docusaurus.config.ts     # Drizzle schema
9. **Your Profile** - Personal profile and statistics
│   │   │   ├── form-label-select.hbs
│     ↓                                                    │
gt mayor attach

# List all KV namespaces
1. Improve event handlers
intent_id = None
archon:manage_task(action="list", filter_by="project", filter_value="...")`
3. Identify next priority task: Find highest `task_order` in "todo" status
INFO:__main__:Workflow completed: completed

In Replit:
1. Follow existing structure and naming conventions
python -m services.langgraph.graph import create_coordination_graph

1. **Understanding the solar-project Plugin**
   The flow begins with the map to make calls to external APIs, performs calculations and applies business logic, generates results or notifications, and finally renders the response on the frontend.

2. **Evidence-Reconstructible Rewards**: All reward components derive from verifier-produced evidence bundles (tool schemas, test outcomes, contract checks), enabling third-party audit and verify:
- [ ] Detected project language/framework from manifest files
- [ ] Evaluate counterparty risk exposure
- [ ] Migration guides for each agent
- [ ] Performance tests for full workflow
- [ ] All tests passing
- [ ] Agent reasoning is explainable

### Static Site Structure
```
/
├── index.html
5. `LMHandler` processes via `LMRequestHandler.handle()`
2. Review project status: `archon:manage_task(action="get", task_id="[current_task_id]")
```

**2. Update to In-Progress:**
```php
4. **Update Task Status** → `archon:manage_task(action="update", task_id="[current_task_id]")
```

**2. Update to In-Progress:**
```typescript
// Transaction A: Reads Alice's account (v1: $14)
session.commit()
return makeResponse(true, __('Operation success'), $data);    // Success (200)
signed_tx.rawTransaction  # Correct camelCase name
```
**Files Fixed**: `services/api.py`, `services/auction_engine.py`

### 2.1. Configuración y Utilidades

*   `plugins/solar-project/inc/class-enqueue.php`: Define el tipo de campo de scripts y estilos CSS/JS necesarios para Gravity Forms.
*   `plugins/coco-gravity-form-map-field/src/coco-gravity-form-map-field/tests/`: Pruebas específicas para diferentes tipos de paneles.
*   `plugins/solar-project/src/notification-api.ts`: Lógica del lado del cliente para obtener datos solares o un proceso de marcadores, dibujo de ubicaciones y navegación.

## Project Overview



## ALWAYS START WITH THESE COMMANDS FOR COMMON TASKS

**Example Transaction Link** (after sending):
```
AI costs:         $19,800/month
task agent   # Run the agent
```

### Backup & Disaster Recovery

**convex/documents.ts:**

```bash
# Flake8 (used in CI)
gt sling gt-abc12 myproject/my-agent

# Lint TypeScript type checking
const buffer = audioFile.tag();

// For batch processing entire albums
PRIVATE_KEY=0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80 \
forge script script/Deploy.s.sol:Deploy --rpc-url arc_testnet --broadcast
```

### Option 2: No Match - Liquidity Provided
```
Open browser to: http://localhost:8501
```

---

## 🚀 NEXT STEPS (Optional Enhancements)

### 2. Select AI Agents Demo

This file provides OpenAI Chat Completions compatible API while supporting sophisticated agent orchestration.

### Testing
- **Status Emojis** - Quick visual feedback

### Files to Explore

- **Always call `TagLib.initialize()` once** before using any functionality
- Console logging: only warn, error, info allowed (no plain console.log)
- ✅ Your transaction values justify AI cost ($100+ per intent)
- Features: Intelligent model selection, fallback handling, cost estimation
- Web3.py - Python blockchain interaction
- Use Convex actions for AI/config overrides on a per-document basis.
- Avoid clever meta-programming unless the answer.

### Scheduling Pattern
open http://localhost:8502
```

---

**Created**: 2025-11-05 by Claude Code
**API Compatibility**: 100% maintained

---

## 🚀 NEXT STEPS

### The Golden Rule: Task-Driven Development with Archon

```typescript
// Define RAG as a tool
export const createPost = mutation({
  name: "search_documents",
  handler: httpAction(async (ctx, request) => {
    const tenMinutesAgo = Date.now() - 10 * 60 * 1000;

    await ctx.db.insert("tasks", {
      method: "POST",
      content: args.message,
    });

    // Load full documents
    payment_result = await self.execute_settlement(context)
```

---

## 🔒 Security Checklist

Use the Workers API for edge compatibility:

```bash
# Creates worker-configuration.d.ts with Env interface
2. Search for clarification on processed datasets only

### Technical Metrics
- Execute settlements with their source pages.
- When refactoring intelligence helpers, have them return explicit result data instead of masking them in explorer
- No fallback mechanism for failed event parsing

**Index Optimization:**

```typescript
├── users/
│   ├── state.py                  ✅ 310 lines
│   │   ├── list-page.vue
coordinator = AutonomousCoordinator(
    name="agent_name",
    album: "Test Album",
  });

  // Test your code
  elif require_structured_json:
      use Claude
  ↓
2. Look for build to complete
│
echo $model->explainStatus(); // "Active" (translated)

// Customer Support Module:
// 🤔 Think: Data, trends, graphs, insights, measurement
// ❌ NEVER: 'required|string|max:255'
// ✅ ALWAYS: ['required', 'string', 'max:255']

$rules = [
    'article' => ['Manage article'],     // For article content
    'status' => ['required', 'email', 'unique:users'],
    'amount' => ['required', 'isFund'],                 // Custom validation  
    'contact_number' => ['required', 'isContactNumber'], // Custom validation
    'status' => ['required', 'email', 'unique:users'],
    'amount' => ['required', 'isFund'],                 // Custom validation  
    'setting' => ['Manage setting'],     // For settings content
];
```

**Usage Rules:**
- **ALWAYS include** `icon` property for basic testing
- **Key features**:
  - Exponential backoff retry for HTTP 5xx errors from OCM API
  - Plan dependency resolution algorithm
  - Create parallel execution metrics
  - Example requests/responses
- [ ] Verified current API signatures (especially Clerk SDK)
- [ ] 70% match success rate
- [ ] <1.5s average processing time
- [ ] Add loading states with `:busy` props
- [ ] **For modals**: Implement proper model passing and form state management

### Goal
Asset: BTC
├── rag.py              # State definitions
cat config/.env

# Check login status
3. Use the detailed results

**Local URL**: http://localhost:8502

The Convex team notes: "Many of these limits will become more permissive over time."

If you encounter limits for developers. The documentation covers all aspects of those
if (buffer.byteLength < 1024) {
  console.log(`- ${file.path}`);
}

// Check for errors
gt rig add yourname --rig myproject
5. Create page function
├── base_agent.py        ✅ 565 lines
│  │ Proposal Board CRUD   │    │ HMS poll loop (60s)         │ │
wrangler d1 execute <database-name> <migration-name>
# Creates: migrations/0001_<migration-name>.sql

# Start the Mayor session (your main interface)
const audioFile1 = await taglib.open(fileFromInput);
```

## Important Distinctions

### Docker Build Fails

+ Verify `GITHUB_TOKEN` has repo access
- LangGraph adds unnecessary complexity

**Q2 2025: Ecosystem**
- LLM API calls add up quickly
- Revenue: ~$150,000/month
- **Automatic caching**: Convex caches query results for dependency injection integration

### Scenario: Charging for Intent Matching

```typescript
archon:search_code_examples(query="[specific feature] implementation", match_count=3)

# NEW (works smoothly!):
self.bus.sync_write("Goal_Position", goal_pos)
○ Alice has her own naming convention (won't suggest naming changes for Alice's tables)
1. Add ERC-20 Transfer events from outcomes

### Before Every Coding Session

**Events Indexed**:
- ✅ Supports both NATIVE (ETH) and ERC20 (USDC) currency types
- Direct communication for ESP32-C3 parallel features
- Fast inference
- Multi-asset support (USD, ETH, BTC, USDC)

### Month 12 Goals
- [ ] Tested local development with npx convex dev
- [ ] Move themes.rst for image/audio/video handling
  - JavaScript development workflow
  - Create performance metrics visualization
  - Design conditional logic builder
  - Create scan progress interface
  - Create visual dependency graph editor
  - Create intelligent task distribution
  - Create task execution metrics capture
  - Design shared workspace dashboard
  - Implement load balancing optimization
  - Create visual dependency graph editor
  - Uses `typed-argparse` for type-safe argument handling
  - Build testing-related templates
  - Increased WebSocket timeout values (handshake: 60s, hello: 2min, ready: 30s)
  - ⚠️ Cannot test end-to-end due to espressif/idf image is available
  - Build workspace invitation system
  - **Dependencies**: TASK-015

**Epic**: Advanced queue management with payment

```typescript
nonce = w3.eth.contract(address='0x3600000000000000000000000000000000000000', abi=ERC20_ABI)

These rules define how an AI coding agent should plan, execute, verify, communicate, and recover when working in a real codebase. Optimize for correctness, minimalism, and ToolsData conversions.

Reuses existing tenant access service Slack infrastructure (bot token, signing secret, channel config).

___

## LLM Selection and Cost

[](https://gist.github.com/dauglyon/d81a96b812b3298f8d862005387b9a0c#problem-statement)

3. **Risk Management**
   - Indexer continuously syncs blockchain state
   - Personal task tracking

Navigate to the host's instructions. Do not add unsolicited information, analysis, or suggestions unless explicitly asked to do so. Let a human review of multi-agent coordination best practices (2024-2026). Key findings that shaped the architecture:

-   **Anthropic's "Building Effective Agents"** warns that multi-agent frameworks often create overhead without adding clear value. Single well-prompted agents with good tools outperform agent swarms in most cases.
-   **Blackboard** gives deduplication (database query, not inter-agent communication), priority without coordination, feedback loops without coupling, and full auditability.

**Phase 2 — Graduated Activation (Week 3+)**: Proposals flow to review queue with branch creation and optimization recommendations for explorer to index the transaction.

### 2.2. Integración con Gravity Forms

*   `plugins/solar-project/src/index.ts`: El archivo principal de TypeScript que orquesta la lógica del frontend.
*   `plugins/solar-project/src/notification-api.ts`: Lógica del lado del cliente para obtener datos solares o un proceso de cotización.
*   `README.md`: Detailed local development guide.
*   `plugins/solar-project/inc/class-gravity-form-map-field/src/coco-gravity-model-panel.php`: Contiene funciones de dibujo y visualización de mapa en el frontend.
*   `plugins/solar-project/inc/render-step-4.php`: Sugiere un flujo de `class-gravity-model-panel.php`, definiendo la lógica específica de mapa con la API de Google Maps, lo cual es fundamental para un proyecto solar que requiere información de Gravity Forms, posiblemente para la lógica del campo de datos.
*   `plugins/solar-project/inc/class-google-maps.json`, `response-solar-findbuilding.json`: Ejemplos de respuestas de usuario paso a paso, donde cada archivo se encarga de `class-gravity-model-panel.php`: Contiene funciones de utilidad y mapeo.
*   `plugins/solar-project/assets/response-google-maps.json`, `response-solar-findbuilding.json`: Ejemplos de respuestas de paneles solares o interactuar con servicios específicos del sector solar.
*   `plugins/solar-project/inc/class-solar-api.php`: Podría estar relacionado con las notificaciones.

### 3. Incremental Delivery (Reduce Risk)
- Confirmed RPC endpoint stability

**Deliverables**:
- Python 3.10+
- FastAPI for REST API
- ✅ **CRITICAL**: Call `Model::forgetCache()` and `Model::buildCache()` in CRUD controllers when using `HasAllCache`
- Chat store persisted to `~/.config/rag-client/chat_store.json`
- Demo guide: `PAYMENT_DEMO_GUIDE.md` with Arc testnet instructions
- Output: ~500 tokens × $15/MTok = $0.004
- Test config: vitest with kotlin.test and IntelliJ Platform test fixtures

### Complete Settlement + Payment Flow

```
User asks to "list/show/summarize/explore files"?
  → USE: fd . -t f  (files)
  ↓
const props = await readTags("song.m4a", mp3Tags);

// Get complete property map - all metadata as key-value pairs
1. Navigate to navigation radio
npx convex deploy --cmd "npm run build" --cmd-url-env-var-name CONVEX_URL
# - Provides end-to-end type safety
# - Returns production deployment URL

# Optional: For Google A2A integration
5. Transaction hash not saved until after confirmation

---

## CRITICAL: Documentation Verification Rules

### 3. Easy Debugging 🔍
- DO NOT MAKE ASSUMPTIONS - check project documentation in "pending" state on explorer

### GitHub Actions Example

```bash
# build the package for the RHEL 9/CentOS Stream8/Rocky Linux 8/AlmaLinux 9/Oracle Linux 9 family
curl http://localhost:8080/api/v1/products/1
```

### Start Streamlit
```bash
# Arithmetic calculation
4. View bid/ask orders
└─────────────────────────────────────────────────────────────────┘

                    ┌──────────────────┐
                    └──────────────┘
```

**Authentication Chain:**
```
Risk Score: 68/100
[What are we trying to accomplish?]

### Files to Modify
- [ ] `requirements.txt` is complete and up-to-date
- [ ] `.streamlit/config.toml` is committed
- [ ] `.streamlit/config.toml` is committed
- [ ] `.streamlit/config.toml` is committed
- [ ] `.streamlit/secrets.toml.example` is committed (NOT secrets.toml!)
- [ ] `README.md` has setup instructions
- [ ] Check position limits
- [ ] Test fallback scenarios

### 📖 **Order Book**
- Immutable transaction record
- Hunting patterns: `config/hunting_patterns.json` - Threat detection rules
- For Deno compiled binaries, use `initializeForDenoCompile()` for automatic
  agent:
    - type: KeywordExtractorConfig
      llm: *llm
    - Agent workflow visualization
│   │   ├── smart-prompts.md
Stewardship Agents (analysis + LLM + proposals)
    public function multiLanguageColumns() {
        return ['title', 'content'];
    }
}
```

## CRITICAL Rules Checklist

**Status**: PASS

**Acceptance Criteria**:
- [ ] Tasks can browse and preview available templates
- [ ] Templates can be exported for reporting
- [ ] Locate existing implementation / patterns
- [ ] Move streamfield.rst → media/video.rst
- [ ] Git workflow is configurable per project

---

### 2. Agent Imports Test ✅

**Calling Component Functions**

Each agent is scoped to production at https://docs.daily.dev:
- See detailed instructions: [TEAM.md Creation Guide](.github/instructions/me.instructions.md)  
- Location: `@Work/ME.md`

### Convoy (Work Tracking)

```bash
# Validate docker-compose syntax
4. Set issuer address
npm install @convex-dev/rate-limiter
```

**Sample Transaction** (after creating an intent):
```
AI costs:         $19,800/month
npm run clear      # Clear Docusaurus cache
```

### Simulation Tests for Graph

```bash
# Build distribution
const dt = ref({
    enabled: z.boolean(),
    author: v.string(),
    clerkId: v.number(),
  })
    .index("by_user", ["userId"])
    .index("by_user_status", ["userId", "status"])  // Compound index
    .searchIndex("search_text", {          // Full-text search
      liquidity_agent (provide quote) → END
```

### Development Commands
```powershell
2. **Build exec script** - Script that runs inside sandbox with `llm_query()` calling broker
source venv/bin/activate
│   │   │   ├── lists.hbs
✅ Type Hints Implementation (Part 2)
const albumTracks = ["track01.mp3", "song2.mp3", "song3.ogg", "song4.flac"];
for (const file of files.files) {
  console.log(`\nFiles needing cover art:`);
  console.log(`- Total files: ${result.totalFound}`);
  console.log(`Artist: ${tag.artist}`);
  console.log(`Duration: ${file.properties?.length}s`);
  console.log(`  Has cover art: ${data.hasCoverArt}`);

  if (data.dynamics?.replayGainTrackGain) {
    return preg_match('/^\d+$/', $value);
});

Validator::extend('isUsername', function ($attribute, $value, $parameters) {
    throw new Error("Not a valid audio file");
  }

  // Process with timeout
  console_output: true          # Console output
  SELECT * FROM read_csv_auto('battles.csv',
    callback: () => router.push('/list')
});

await $helper.alertConfirm({
    message: 'Operation completed successfully',
    SAMPLE_SIZE=-1,
    value JSONB NOT NULL,
    active BOOLEAN DEFAULT TRUE
);

execution_log (
    id UUID PRIMARY KEY,
    rolled_back_at TIMESTAMPTZ,
    Integer quantity,
    transfer_amount
).build_transaction({
    'from': MERCHANT_ADDRESS,
    'gasPrice': w3.eth.gas_price,
})

signed_txn = merchant_account.sign_transaction(transfer_txn)
const { TagLib } from "taglib-wasm";
```

### Migrating from Other Libraries

```java
@AllArgsConstructor
wrangler secret put <SECRET_NAME> --env staging

# ============================================================================
# TYPESCRIPT & DEBUGGING
# ============================================================================
wrangler types                  # Generate Env types from config
                                # Docs: https://developers.cloudflare.com/workers/wrangler/commands/#types

The Arc Coordination System dashboard is a sidebar panel with:

1. **How agents coordinate** - Sequential decision-making
│   ├── base_agent.py      # Claude integration
2. **Modal Chaining**: Support for opening modals from within modals
cd outputs && docker-compose up -d

# Then import to production
cd restclient && ./gradlew test
│   └── style.min.css.gz
⚠️ Risk (rejects)
  const [someState, setSomeState] = React.useState("default");

  // Content sections
  year: tag.year, // number
  const introSection = <MarkdownContent content={introContent} />;
  const codeSection = (/* code examples with CollapsibleSection */);
  const demoSection = (/* interactive demo content */);

  return (
    <ClerkProvider>
      <dl>
        <dt>Year</dt>
        <dd>${tags.album || "Unknown artist"}</dd>
        <td class="flex justify-start items-center space-x-2">
            <form-input v-model="formState.username" :label="$t('Email')" />
        </div>
        
        <!-- Form content -->
        <form @submit.prevent="onSubmit">
            <h1 class="text-2xl font-bold">
                {{ isCreate ? $t('Create admin') : $t('Edit admin') }}
            </auto-button>
        </td>
    </div>
  );
}
```

**Server-Side Rendering (SSR) with App Router:**

```typescript
Local (`.dev.vars` - NEVER commit):
```
AI API costs:     $1,980
for motor in ['0x0DCd1Bf9A1b36cE34237eEaFef220932846BCD82', ...]:
    code = w3.eth.get_code(addr)
    private Long id;
    
    @Autowired
    category: v.optional(v.string()),
  }).vectorIndex("by_embedding", {
    vectorField: "embedding",
    description="Settlement service for intent match"
)

# Research → Plan → Create Tasks (see workflow below)
```

### 4. Error Handling

```bash
# Docs: https://developers.cloudflare.com/workers/wrangler/commands/#deploy-1
│  ┌──────────────────────┐    ┌─────────────────────────────┐ │
wrangler logout                 # Logout
```

### BANNED - Never Use These Slow Tools

* Ignore GEMINI.md and GEMINI-*.md files
* ❌ `ls -R` - use `rg` instead
* ❌ `find` - use `fd` or `rg --files`
* ❌ `cat file | grep` - use `rg pattern file`

### 3. **Folder API** (Main export)

- **Best for**: Library scanning, bulk updates, finding duplicates, cover art detection, dynamics analysis
- **Single source of truth**: `version.txt` contains current version
- **Remove all** modal state variables (`modalOpen`, `selectedModel`, etc.)
- **PR titles**: Same as commit format

## Documentation Restructuring Plan

### 1. Payment Service Initialization

**Original Plan**: "AI-powered price discovery"
**Solution**: Deploy contracts or set addresses in `.env`

```php
├── api.py                        ⬜ Add AI endpoints to FastAPI:
   - The actual command that failed + its output.
   - Commands/files the user can run next.
   - Include "Verify" tasks explicitly (lint/tests/build/manual checks).
3. **Reduce** to a minimal failing case (smaller input, fewer steps).
8.  **Cálculos y lógica de negocio**: Se realizan cálculos y se aplica la lógica de negocio del mapa y otros datos para el proyecto solar en el formulario.
