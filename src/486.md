# Agent Guidelines

## Common Recipes

### 11. Routing Logic
- [ ] Tools:
  - `clusterconfig/` - Regular cluster configuration gathering (default 2h interval)
  - `workloads/` - Workload fingerprint data (12h interval, not configurable)
  - `gatherers` - Optional gatherer configuration for gathering jobs
- **Monitoring Setup**: Track agent performance and separation of concerns.
- **No advertising** SDKs are used.

## Common Development Commands

### Node.js
- Spring Boot Extension Pack
- This returns a TagLib instance has separate tables/functions
- Testing: 5 minutes
- Use **dynamic imports** for heavy components
- Output: ~600 tokens × $1.25/MTok = $0.004
- Read time: <2ms
- LangSmith - Tracing (optional)

**LLM Router**
- LLM API calls add up quickly
- ⚠️ Yellow: Warnings (if any)

### 1\. Reviewer Engagement

[](https://gist.github.com/dauglyon/d81a96b812b3298f8d862005387b9a0c#decision-log)

| Metric | Cost | Value | Net Profit | ROI |
|--------|------|-------|------------|-----|
| Daily | $930 | $28,740 | $27,810 | 30x |
| Documents written per transaction | 16,000 | Mutation write limit |
| Indexer | ✅ Ready | - | Can be started independently |
| 412 | `max_gas_price_gwei=None` | Use network gas price instead of hardcoded 50 Gwei |
| Settlement Time | Hours | Minutes |
| Display-only contacts | ❌ Use formatted display | Show `full_contact_number` with country flag |

### User Experience: 🟡 IN PROGRESS

| Plan | Database Storage | File Storage | Database Bandwidth | File Bandwidth | Function Calls |
| ------ | ------------------ | -------------- | ------------------- | ---------------- | ---------------- |
| Free | 0.5 GiB | 1 GiB/month | 1 GiB/month | 1M calls/month |
| Durable Objects | Single-threaded per instance | Use for coordination, WebSockets |

### 3. Audited Skill Graph

Este proyecto es el corazón de cada componente y sus interacciones.

### 4. Dependency Discipline
- Optimistic data tracing and debugging needs
- ⚠️ Risk Agent assessing... ✅
- Check CORS is formatted and safe to clear if tasks misbehave.

## Overview

| Operation | Gas Limit | Notes |
|-----------|-----------|-------|
| **Native ETH Transfer** | 21,000 | Standard transfer |
| **Chain ID** | 5042002 |
| **Secrets** | Docker Secrets | AWS Secrets Manager / SSM |
| **ERC-20 Transfer** | 65,000 | Token contract call |
| **AI Calls/Day** | 8,640+ | <200 | 98% reduction |
| **Reliability** | Depends on AI | Algorithmic core | Much higher |
| **Settlement Agent** | ✅ Complete | Prompts updated for USDC payments |
| **Secrets** | Docker Secrets | AWS Secrets Manager / SSM |
| **Faucet** | https://faucet.circle.com |
| **Matching Speed** | 1-5 seconds | <100ms | 50x faster |
| **ERC-20 Transfer** | 65,000 | Token contract call |
| **Standard Fee** | 10 USDC (~$10) |

### Business Impact

1. **Policy Runtime**: Interacts with tasks using direct actions, tool calls, skill invocations, and match outcomes.

**Reward Shaping**: Progressive reward shaping (PRS) provides stage-wise feedback. ASG-SI incorporates bounded memory in the BlinkParallel.ino example.

## Development Workflow

### Basic Commands

```bash
# Core (always needed for local dev)
INTENT_REGISTRY_ADDRESS=0x...
AUCTION_ESCROW_ADDRESS=0x9A676e781A523b5d0C0e43731313A708CB607508
```

### Beads Integration

```yaml
✅ **Visual Feedback** - Colors and initializes correctly
lsof -ti:8502 | xargs kill -9
└── en_permission.json   # Admin permission names (usually empty)
```

#### English Translation Decision Tree
```javascript
// Verify webhook signature (HMAC-SHA256)
npm install @convex-dev/component-name
```

**convex/convex.config.ts:**

```bash
# First, analyze existing codebase thoroughly
# Password: (empty)

# Security considerations  
npm run serve      # Serve built site locally
gt install @clerk/nextjs

# List tables
.exit
```

### Verify Repository URL
Start with the response

**Actions Completed**:
- Filter by status (pending, funded, settled, etc.)
- Interactive match cards
- Price compatibility analysis (bid >= ask)
- No StreamField blocks documentation
- State passed correctly between agents
- Docusaurus Documentation: https://docusaurus.io/docs
import { action } from "vitest";

export function* scanDirectoryStream(
  action="create",
  provider: new OpenAI({ apiKey: process.env.OPENAI_API_KEY }),
  instructions: "You are a helpful customer support assistant.",
});

// Start new conversation thread
3. **Never assume** API signatures, function syntax, or hook patterns

### Objective
async def test_full_coordination_flow():
    return run_async(sdk.list_intents())
```

2. **Lazy load data**
```python
@app.post("/ai/match")
Session = sessionmaker(bind=engine)
4. Define scope
curl http://localhost:8080/api/v1/products/1
```

### Backup & Disaster Recovery

**VERIFY at:** <https://docs.convex.dev/agents>

Integrate Retrieval-Augmented Generation (RAG) that augments LLM interactions with React components without planning overhead.

## Current Documentation System Tips

```typescript
💳 Settlement (plans)
  appleSoundCheck?: string; // Album peak value (0.0-1.0)
  extensions: [".mp3", ".flac"], // File types to include
  await asAlice.mutation(api.external.callApi, { input: "test" });

  const aliceTasks = await asAlice.query(api.tasks.getMyTasks);
  expect(aliceTasks).toHaveLength(1);
  if (!isValidAudioFile(bytes)) {
    console.error("Unsupported format:", error.format);
  } else if (isFileOperationError(error)) {
    try {
      const tags = await readTags(filePath);

      // Create composite key
      llm: *llm
    - task: agent
```

## Pull Request Guidelines

- 🎮 **Interactive Controls** - Start/stop workflows
- When adding new tooling.

## Clipboard

Edit `MAINTAINERS.md` with actual maintainer information.

## DocSync markers

TOTAL CODEBASE: ~5,054 lines of production-ready AI code
```

---

## 🚀 Quick Start Guide

### Daily Development Routine

**Details**:
- ✅ Supports both NATIVE (ETH) and ERC20 (USDC) currency types
- Lead with GitHub Actions workflow (tobozo2 merged-bin approach)
- Frequent JOIN queries needed
- README.rst: Project overview and configuration
- Contract addresses
- Add the smallest test that would have caught the bug.
- Apply MVVM pattern with proper separation of the flow

### Live Demo (Judges Click Here!)

**Demo URL:** [YOUR_DEMO_URL_HERE]

✅ Transaction confirmed in block 9635145!

The following command now works successfully:
```vue
<template #tools="{busy}">
    <tr v-for="(record, index) in records" :key="record.id">
        <auto-datatable ref="dt" model="ModelName">
            <auto-button type="plain" :busy="formBusy" @click="onSubmitForm">
                {{ $t('Submit') }}
            </auto-button>
        </td>
        <!-- Data columns -->
        <div class="modal-footer grid grid-cols-2 gap-4">
            <auto-button small :full="false" @click="onFormOpen(record)">
                <font-awesome-icon icon="fas fa-trash" />
            </auto-button>
        </auto-datatable>
        
        <!-- Form content -->
        <auto-modal v-if="formOpened" @close="onFormClose">
            <model-form 
                :model="model" 
                @success="onSuccess" 
                @close="onFormClose" 
                @close="$router.back()" 
            />
        </div>
    </form>
  );
}
```

**app/ConvexClientProvider.tsx:**

```bash
# .env.local (development)
orderbook = await sdk.submit_intent(payload, valid_until, mandate_id, asset)
✅ ap2_gateway.py            ✅ 520 lines
2. ✅ Cost is manageable (2-3% of revenue)

**VERIFY at:** <https://docs.convex.dev/database/writing-data>

**convex/tasks.ts:**

```typescript
const showError = (item) => {
    alert(t('x not found', { x: username }))
}
</script>
```

#### 4. Test and Fix Common Issues

**Objective**: Add ERC-20 token support to x402 payment service

**Tasks**:
1. Accepts amount, service ID, description
python test --coverage      # Generate coverage report
tools = [
    {
        "name": "calculate_spread",
        "input_schema": {
            "type": "object",
            "properties": {
                "match_id": {"type": "number"},
                "days": {"type": "number"}
            }
        }
    },
    {
        "name": "get_actor_history",
        "ssm:DescribeInstanceInformation"
      ],
      "Resource": "*"
    }
  ]
}
```

### Register and Run Traefik

```bash
# Install wrangler
├── main.tex                # Research paper (LaTeX)
rg --files | rg "pattern"       # Find files by name 
rg -A 3 -B 3 "error"            # Context lines
class MarketAgent:
    type: SimpleContextChatEngineConfig
    return AgentResult(
        tx_hash,
        plan: "premium",
      });
    }
  },
});
```

**When to Use Internal Functions:**

- Queries/mutations have 1-second limit because they're transactional (must be fast)
- Use factory pattern: `getMockX(overrides)`
- For in-page anchors: `[link text](#section-id)`
- For external links: `[link text](https://example.com/in/your-profile)
- Fiat on-ramps

### 🎯 **User Experience**
- Filter by active/matched status
- Explorer: https://testnet.arcscan.app
- ✅ Use `loopLanguageForColumn()` for multilingual fields
- API Response: 500 Internal Server Error
- Good at financial analysis

**VERIFY at:** <https://docs.convex.dev/testing>

### Generated Types

✓ **Cryptographic Signature Verification**
- Status-based filtering (pending, funded, settled, etc.)
- Interactive match cards
- All 5-6 agents displayed
- Cost: ~$2/day (depends on usage)
- ✅ Your transaction values justify AI cost ($100+ per intent)
- ✅ You're targeting price-sensitive users

---

## 🚀 COMPETITIVE POSITIONING

### ✅ Step 1: Environment Configuration
- Inspect all referenced AMIs and is gitignored.

## Development Commands

### Performance Targets
- [ ] Create content/organization.rst
- [ ] Run integration test with API keys
- [ ] Assess bid-ask spread
- [ ] Implement rate limiting
- [ ] Multi-currency payment support
- [ ] Audit payment flow on testnet first
- [ ] Tools:
  - `load_config()`: YAML configuration loading with validation
  - `replace()`: Complete overwrite - removes fields not in new object

---

### 9. Parallel Processing Engine

**LangGraph Node**:
```bash
# Get chain ID from logs
language = "python3"
4. Progress through steps 1-4
2. Enable LangSmith for complex features without building from scratch, while maintaining data isolation and transactional guarantees.

---

## Communication Guidelines (User-Facing)

### ✅ Remaining Issue RESOLVED

**Shows:**
- ✅ Free tier is generous
- Check CORS is formatted and linted:
  - feature flag, config gating, or isolated commits.
- Explicitly call out behavior changes, edge cases, and Prettier integration. Expect strict typing (`@typescript-eslint/explicit-function-return-type`, `prefer-readonly`, no `any`).
- Prettier is configured via `.prettierrc` (120-column width, semicolons, single quotes). Run manually with `yarn nx format:write`
- Base work on master branch
- Custom agreements
- Do not add new dependencies unless:
  - feature flag, config gating, or isolated commits.
- Tests/lint/typecheck/build (as relevant) pass or you have a documented reason they were not run.
- Prefer:
  - Maximum trading volume
  - Design channel configuration dashboard
  - Extracts From, Date, Subject headers
  - Example: `<https://happy-animal-123.convex.cloud`>

**3. Deploy:**

- Database storage: $1.00/GiB/month
- ✅ Dual-LLM architecture
- Keep dependencies updated
- Configure `parallelism` in job environment settings
- ✅ Payment integrated into agent reasoning
- Use existing components and utilities when possible
- Poller thread on port 8545
- ✅ Base classes accessible
- Create monitoring dashboards

**Date**: 2025-11-05

---

## 🔄 DATA MANAGEMENT

### CLAUDE.md
- Each main category has its own directory
- Payment completed message
- ✅ Local pre-commit validation
- Tool calls integrated with LLM requests
- Nonces and images should be set for movement

**VERIFY at:** <https://docs.convex.dev/agents>

### Defining Vector Indexes

npx convex dev --tail-logs disable
# - Creates separate file per table

# Get product by ID
6. **Learning**: Agents improve over time

---

## Production Deployment Checklist

### Step 1: Detect Stack
├── README.md                     # Project README
├── storage/                     # Storage backends
2. Contract addresses are correct in `.env`
