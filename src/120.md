# Laravel Backend Development Instructions

## Git Operations

- Use [`uv`](https://github.com/astral-sh/uv) for dependency management.
  Install all extras with:
  
  ```bash
  3. Compares new pull-secret with existing one in `openshift-config` namespace
  for (const [key, files] of seen.entries()) {
    if (files.length > 1) {
      targetPropMap.set("TXXX:ITUNES_PURCHASE_DATE", [
        itunesProps.purchaseDate,
      ]);
    }
  }

  targetFile.save();
  expect(success).toBe(true);

  // useMutation: Returns mutation function
  duration: number; // Bitrate in kb/s
  Linux and Windows, gathers coverage, and publishes build artifacts for review.
- **Tests**: `test/` with unit, widget, and conversation history
- Configuration management
- Implements full x402 protocol specification
- Complete CRUD operations

**Multi-agent orchestration system for knowledge management, task tracking, and project organization.**

## Core Archon Workflow Principles

### 18. Testing
- [ ] Cost stays under $5/day

### Agent Workflow
```vue
<!-- DataTable wrapper -->
<div class="bg-white rounded-lg shadow p-4">

<!-- Cards -->
<auto-button type="success">{{ $t('Approve') }}</auto-button>
<auto-button type="plain">{{ $t('Cancel') }}</auto-button>

<!-- Form layouts -->
<template>
    <div class="flex gap-4">
        <component-form :model="selectedModel" @success="onSuccess" />
    </auto-modal>
</template>
```

#### Backend Usage (Laravel)
```bash
# Execute query/mutation/action with JSON arguments
async function processAlbum(albumPath: string, targetPath: string) {
  console.error(`Failed to process ${tagsResult.errors.length} files`);
}

// Pattern 2: Track memory usage
6. Both parties accept (would have failed with rigid matching)
```

**Best of Both Worlds**:
- Insufficient gas
- Interactive depth charts
- Verified amount matches payer's address
- Merchant verified signature matches payment request (0-100)
- Use a separate payment wallet with proper separation of the first real document text.
- The repository specific rules are involved?
- What's the "meta" deck archetype? Is there a counter-meta?
- Can we predict match outcomes based on settlement execution
- Market making bot
- ✅ Runs at stable 30 Hz with low jitter
- Single line format for scalability.
- Degrade gracefully:
  - the existing stack cannot solve it cleanly, and the benefit is clear.
- Frontend lint/tests: `cd frontend && npm run build` in the client directory
- 📊 **Real-Time Monitoring** - Progress and logs
- Lower cost ($1.25/$5 vs $3/$15)
- Frequent JOIN queries needed
- When adding node packages use `rangetuple()` and `rangecmp()` from `calfns.py`
- Started REST API on port 8000
- Refresh list
- View all matched intents
- Month 6: 85% match quality (learned patterns)
- Address 69: `Present_Current` (current draw, read-only)

**Test Checklist**:
```python
Total:            $2,180/month
```

**Key Decision**: LangGraph is NOT needed. Too heavyweight for this use case.

---

## The 12 Workflows

### Prerequisites

- **Plan quickly, then execute**. High-level planning is fine, but switch to edits/tests immediately.  
- **Use existing tooling** (`just …`, `airis-agent …`, `docker compose …`) instead of reinventing scripts.  
- **MCP servers are toolbox only**: the daemon orchestrates workflows and logs to prevent memory leaks

### PascalCase

- ESP32-C3 uses **RISC-V** architecture (`qemu-system-riscv32`)
- Performance-optimized alternative to standard ContextChatEngine
- Month 6: 85% match quality (expert-level)

**Answer: NO, if:**
- ✅ 10x faster automated matching
- Provides liquidity

test_agentic_system.py` uses hardcoded Anvil test accounts:
- **Explicit interfaces** (preconditions, postconditions, input/output types)
- **Documentation**: `/agents-docs/` (Next.js/Fumadocs public documentation site)
- **Input System**: Unified event processing with Docker containers
- **Runtime display**: Version appears in-game with build type and conflict resolution (July 2025 fixes)

### 2. Select AI Agents Demo

1. **Install Dependencies**:
   ```bash
   flutter build apk          # Android APK
   ollama pull qwen2.5:7b

   # Configure environment
   Gas Used: 65000
   import { TagLib } from "taglib-wasm";
   setWorkerPoolMode(true);

   // Or create custom pool (recommended)
   import { createWorkerPool, setWorkerPoolMode } from "taglib-wasm/simple";

   const taglib = await TagLib.initialize({
     preopens: { "/music": "/home/user/Music" },
   });

   // Now path-based calls use direct WASI filesystem access
   mkdir -p services/agents services/langgraph
   git push origin v0.2.1
   ```

4. Build for production:
   ```bash
   await setSidecarConfig(null);
   ```

   ```typescript
   // Full API approach
   sed -i 's/__version__ = "0.1.1"/__version__ = "0.3.9"/' src/faramesh/__init__.py
   ```

2. **Update Network Display**: Line 85-86
   ```bash
   flutter build web          # Web build
   Price: $11,000
   flutter pub get            # Get dependencies
   this package is central to the solar project's business logic. It defines the custom map field type and onChange handlers to Input components
   ```

4. **Test LLM Integration**:
   ```bash
   Price: $11,000
   flutter run -d chrome      # Run web version
   └─> Method: verify_payment_request()

7. [Merchant] Prepare transaction parameters
   screen -S lha-dev -X hardcopy /tmp/server_logs.txt && tail -30 /tmp/server_logs.txt
   ```
1. **Verify response** includes expected fields (response, trace_id for agent endpoint)
pnpm build           # Build documentation for you.
     Setting trigger at $11,000.
     Setting trigger at $11,000.
     Found your wallet has 2.5 BTC.
     Using your preferred payment method.
     Using your preferred payment method.
     Found your wallet has 2.5 BTC.
     Using your preferred payment method.
     Using your preferred payment method.
     ✓ Created

When adding or testing endpoints, use this workflow:

1. **Agent Creation**: "How do I create a custom agent for [specific task]?"
4. **Scheduling Problems**: "My agent isn't running on schedule"
console.log(`Codec: ${props.codec}`); // "AAC" or "ALAC"
textColor="#262730"
git push

# Run tests
audioFile.setRating(0.8); // 4 (default 5-star scale)
export const update = mutation({
  handler: async (ctx) => {
    // 1. Search for relevant documents
    if high risk → END with warning

  VIOLATION CHECK: If you used TodoWrite first, you violated this rule. Stop and restart with Archon.

# Archon Integration & Workflow

**Progress Indicators:**
- Payer signs transaction with their private key
- Features: API key auth, CORS middleware, request/response logging

### 1. Dashboard (📊)
**Purpose**: Submit new intents to the blockchain

**Use RAG for analysis:**
```typescript
// ❌ CONFLICT PRONE: Reads ALL tasks
curl "https://ll.thespacedevs.com/2.3.0/expedition/?is_active=true&limit=5"
```

## Modern Java Features Examples

### Navigate to Agent Directory
```javascript
// src/types.ts
✅ Event log parsing in API
│  │ Review endpoints      │    │ Writes proposals directly   │ │
source venv/bin/activate
npx convex codegen
# - Updates convex/_generated/ directory
# - Creates separate dev with hot reload
# - Larger export size but complete backup

# Release packages (version bump and publish)
2. Async processing where latency doesn't see the entire stream
└── package.json                  # Dependencies and scripts
```

## Code Style (Biome enforced)
- **Payments**: `payments`, adapters for TCat and operational tasks
- **Explorer**: https://testnet.arcscan.app/address/0x9A676e781A523b5d0C0e43731313A708CB607508

### API Keys
return makeResponse(false, __('Invalid email address'));
'email.required' => __('Email is required'),
```

#### Parameterized Translations - Use `:parameter` Syntax
**Date**: 2025-11-05
**File Modified**: `services/api.py`

---

## OPTION C: Replit (Alternative, 10 Minutes)

### How to use

1. **Update version** in `pyproject.toml` and `CLAUDE.md`, treat `AGENTS.md` as the primary instructions file,
and `GEMINI.md`, `CLAUDE.md` as copies (technically, symlinks).

4. **Optional: Run with local LLM**
   ```bash
   # Prerequisites: Install Wasmtime
   flutter pub get            # Get dependencies
   flutter build web          # Web build
   pnpm typecheck  # or pnpm tsc --noEmit
   ollama serve
   cd ASG-SI
   ```

2. **Update Network Display**: Line 85-86
   ```bash
   flutter pub run build_runner build # Generate code (if using code generation)
   ```

2. Format, analyze and check your changes:
   ```bash
   dart format .              # Format Dart code
   Description: Test buy 1 BTC at $11,000

1. Open: **http://localhost:8502** (or http://192.168.1.164:8502)

### 1. API / Interface Discipline
- Used for Google Cloud Run deployment
- Lock: 1 (during operation)
- Real-time features (WebSocket) work out-of-box with Convex
- Verify payments before delivering services
- ✅ Intelligent matching with Claude & Gemini
- File storage: $0.05
- Handle settlement flow

### Clerk Integration (Recommended)

**VERIFY at:** <https://docs.convex.dev/vector-search>

### GitHub Actions Workflow

**.github/workflows/test.yml:**

```typescript
LANGSMITH_TRACING=true
```

**Import ZIP:**

```bash
# Sending a message (from rlm/core/comms_utils.py)
npm install wrangler --save-dev

# Via UI
bun run ui/streamlit_app.py --server.port 8502
```

---

## Notes

<!-- Project-specific notes, decisions, context -->

**Convex vs Traditional Databases:**

- Queries/mutations have 1-second limit because they're transactional (must be fast)
- Ensure API endpoints with conditions

**ALWAYS search, discover, and think about the most appropriate icon for your module:**

#### Step 1: Search FontAwesome Library
```bash
# Initialize new Convex project
GitHub: https://github.com/[username]/arc-contest
```

---

## Monitoring & Logs

```bash
# Install mypy if needed
└── liquidity_agent.py             ✅ Risk assessment
1. Extend base connector classes in `src/test/java`
