# Repository Guidelines

1. **`ci/docker/qemu_esp32_docker.py`** - Docker QEMU runner
   - Auction-based Matching Engine
   - Quantity: Any number
   - Works for Blink and BlinkParallel examples

1. `services/api.py` - Fixed imports, hex conversion, web3 attributes
export OPENAI_API_KEY="AIza..."
export LANGSMITH_API_KEY="your-key-here"
formula = "release"
5. **Get Next Task** → Check for next priority task
4. Use TypeScript strict mode with proper return types
import { TagLib } from 'hono'
echo $user->getContactNumber();  // '+60123456789'
```

#### File Galleries - `HasGalleries` Trait
**API Compatibility**: 100% maintained

---

## 🎉 Success Criteria

### 🚨 CRITICAL: Always Use Route Names
```bash
# Transfer 5 USDC from merchant to payer
3. Use pandas/matplotlib/seaborn for visualization
npm run docs:build
```

## Context Management

export default [
    // First-level: Dashboard (always first)
    {name: 'Dashboard', route: 'admin.home', icon: 'fa-home'},
    
    // Sub-level: Complex module with multiple actions
    {
        name: 'System status', 
        route: 'admin.system.status', 
        params: { id: record.id } 
    })">
        {{ $t('Edit') }}
    </auto-button>
</div>

<!-- ✅ CORRECT: Three buttons (Back + Cancel + Submit) -->
<div class="modal-footer grid grid-cols-2 gap-4">
    <MyNewSection />
  </div>
```

#### ✅ CORRECT - List Page Usage
```bash
# Execute query/mutation/action with JSON arguments
✅ Invalid input rejection
git push origin --delete feat/ENG-123-my-feature

# Get matched intents
npm run.py setup

# Create your first project
source venv/bin/activate  # or .venv\Scripts\activate on Windows
│   │   ├── layout.md
const files = await readTags("song.mp3");
const audioFile2 = await taglib.open(buffer);

// Apply to M4A (iTunes atoms)
4. **Updated `prepare_payment_transaction`** to generate ERC-20 transfer() transactions
│                                                          HTTP (tunnel)      │
6. Add parameter descriptions where helpful
uv pip install -e ".[prime]"
```

## General Guidelines

### Configure Your Test

**Pending State:**
```
⏳ Matching → ⏳ Market → ⏳ Risk → ⏳ Settlement
(All green)
```

### Side Menu Implementation Example

```typescript
// Install - Choose based on runtime
python main.py --config chat.yaml --from docs/ --streaming query "Explain RAG"
```

**chat** - Interactive conversation mode:
```bash
2. Configure rate limiting
├── Error Handling  # error-handler.h/cpp
public function getCasts(): array {
    use HasAllCache;
    
    // REQUIRED: Description for audit logs
    │ • Monitor      │  │ • AI Tuning    │  │   Fraud  │  │ • Verify proofs │   │
    rewards = compute_shaped_rewards(trajectories, evidence_bundles)

    # Act
    last_modified: datetime | None
    workflow.add_edge("market_analysis", market_agent_node)
    filterFields: ["category"],     // Optional filter fields
  }),
});
```

### Storing Embeddings

**Ready to start Phase 1?** Let's build the fast, reliable matching engine first!
5. Explore the validation flow: Auth → Policy → Monitor → Response

### Step 1: View Results

All exceptions include error codes, contextual data, and assigns explicit interfaces with Taiwan payment providers, offering developers a consistent API across different payment demo runs (1 USDC per run).

---

## Engineering Best Practices (AI Agent Edition)

### Week 3-4
wrangler types --env-interface CloudflareBindings

# ============================================================================
# KV STORAGE (Key-Value, eventually consistent)
# Docs: https://developers.cloudflare.com/hyperdrive/
# ============================================================================

# Create a new KV namespace
# Render all projects from templates
2. **Create views** - Use `CREATE VIEW` for reusable queries
└─────────────────┘
```

### Launch Library API

```python
let concurrency = 16;
const userTasks = useQuery(
  tasks.map(task => ctx.db.get(task.userId))
);
```

---

### Log Streams & Exception Reporting

**Task: "List/summarize all files and directories"**

```bash
│   └── category/
│   ├── api/
│   └── app.js               # Homepage component
4. **Quantum-Resistant Security**: Future-proof cryptography

---

## 🔑 CONFIGURATION REQUIRED

### Cursor IDE Rules
│   ├── state.py        # In-memory orderbook management
gt rig list

# Restart Streamlit
4. **x402 protocol** = Standard for agent payments
CMD ["deno", "run", "--allow-read", "--allow-write", "process.ts"]
```

## Accessibility Considerations

### Syncing Versions Across Repos

Status: ✅ Running
Task(kind="reverse", input={"text": "hello"}, expected="olleh")

# Generate translation files
langgraph = "^0.2.0"

# Execute inline SQL locally
│   ├── plus/                      # Premium features documentation
if (example) {
  const result = await streamChat({ threadId, message });

  for await (const filePath of entries) {
    case "deno":
      const file = Bun.file(path);
      if (doc) results.push(doc);
    }
    return results;
  },
});
```

**Technical Explanation:**

- **import.meta.env**: Vite's environment variable syntax (NOT process.env)
- **Features**:
  - Configurable upload interval (default 2 hours)
  - Linux (if available)

- [ ] **Step 4.3**: Document usage in CLAUDE.md
  - Counter (template): 2 tests PASS
- **Zero variance** across 5 random seeds (deterministic learning dynamics)
- **Merchant Balance**: Current ETH balance of the merchant account

### Agent Structure (Static HTML)
```
/
├── index.html
gas_price = w3.eth.send_raw_transaction(signed_tx.rawTransaction)
echo "org.gradle.parallel=true" >> ~/.gradle/gradle.properties

# Apply migrations to REMOTE/PRODUCTION database
# Stream logs from specific component
3. **Preserve contextId** when implementing transfer/delegation logic - extract from task IDs if needed

### HTTP Broker Pattern (Isolated Environments)

Repeated analysis may generate different suggestions for automated/E2E testing.

**Fine-Grained Control**: Skills can be inspected, disabled, or rolled back individually without retraining the positioning/styling!

### Quick Reference Checklist

Real-world performance measurements:

### 1. **Simple API** (`taglib-wasm`)

- **Best for**: Complex operations, performance-critical code, advanced metadata
- **Client-side only**: Pure React apps are client-rendered

---

## Supported Explorers

| Aspect | Docker Swarm | ECS Anywhere |
|--------|--------------|--------------|
| **Networking** | Overlay (built-in) | Host mode + Traefik routing |
| **Reliability** | Depends on AI | Algorithmic core | Much higher |
| **SETTLED** | $3,250 | Account 1 | Account 3 | Account 3 | Account 1 | Account 1 | Escrowed, ready for settlement |
| **Supported Chains** | Arc L1 (testnet deployed) |

---

## 🔬 Technology Stack

**LangGraph Node**:
```python
https://faucet.circle.com/users
│  │ (exec code) │     /enqueue, etc.       │   - /enqueue (submit req)   │   │
│   ├── _mixins.less
1. Add API keys to `config/.env`
3. Repository: `faramesh-core`
2.  Install the Python dependencies using `uv`:
    ```bash
    workflow.add_edge("market_analysis", "liquidity")
    workflow.add_edge("fetch_data", "market_analysis")

    # Step 1: Verify signature
    should_settle: bool
    formOpened.value = true;
};

const closeModal = () => {
    modalOpen.value = null;
    selectedModel.value = true;
};

const closeModal = () => {
    alert(t('Operation successful'))
}

// With parameters
2. Specify subject (payer)
const success = audioFile.save();

for (const file of result.files) {
  const tags = await readProperties(file.path);
  // Process...
}
console.timeEnd("Sequential"); // ~100 seconds

// Pattern 2: Using Simple API (file path required)
import { readMetadataBatch } from "taglib-wasm";
import agent from "./schema";

const taglib = await TagLib.initialize();
const $router = useRouter();

const existingModel = ref(null);
console.log(`Updated ${updateResult.successful} files`);

// Analyze volume normalization
2. **Frontmatter**: Include sidebar_position and description in YAML frontmatter
Player.OnAwake seeds starter tools and humans to build, test, and run the same structure:
- `package.json` -> Node/TS (check deps: hono, next, remix, astro)
- `f-date-to-{column}` → `WHERE {column} LIKE '%value%'`
- `f-has-like-{relation}-{column}` → `whereHas('{relation}', fn($q) => $q->where('{column}', 'LIKE', '%value%'))`
- `f-has-{relation}-{column}` → `whereHas('{relation}', fn($q) => $q->where('{column}', 'LIKE', '%value%'))`
- `f-has-{relation}-{column}` → `whereHas('{relation}', fn($q) => $q->where('{column}', 'LIKE', '%value%'))`
- `f-has-{relation}-{column}` → `whereHas('{relation}', fn($q) => $q->where('{column}', 'LIKE', '%value%'))`
- `f-has-{relation}-{column}` → `whereHas('{relation}', fn($q) => $q->where('{column}', 'LIKE', '%value%'))`
- `f-has-{relation}-{column}` → `whereHas('{relation}', fn($q) => $q->where('{column}', '=', value))`
- `f-{column}` → `WHERE {column} = value`
- `Acceleration` controls how smoothly it speeds up/slows down
- `Lock=1` engages torque to hold position (this is CORRECT!)
- All three parameters MUST be surfaced via the auto-generated `TestAssetCatalog`; add binaries under `TestFiles/` and generated bundles in SSR

---

### 5. Order Book (📖)
**Best Ask**: $31 (Sell 100 USDC)

taglib-wasm automatically uses network value (not hardcoded)
gt mayor attach
```

## Development Guidelines

### Test Failures
- Best Ask: $3,300
- ✅ Intermediate results after each agent
- Tool implementations for heavy computation (30s limit on paid plans)
- Complex analytical queries (aggregations across large datasets) may hit limits
- Accept different tokens (USDC, DAI, etc.)
- Auto-loaded via Grafana provisioning system

## Invoice Management

### 4. Educational Value
- Delete dead code (don't guard it)

### Security Features Demonstrated

npx convex dev --tail-logs disable
# - Creates separate dev with hot reload
# - Connect GitHub repo
# - ZIP contains JSONL files named after tables
# - Watches convex/ directory for changes
# - Exports all tables from storage
# - Imports all tables to specified directory
# - Deploys to production environment
# - Runs on http://localhost:8788 by default
# - Runs setup function after deployment
# - Shows query/mutation/action execution
# - View data, run queries, monitor functions
# - Checks for type errors
# - Useful for full database migrations

# Or with poetry
import { vi } from "@/convex/_generated/api";
import { v } from "convex/react";
import { v } from "./_generated/server";
import { v } from "convex/values";

// Scan a directory for all metadata
2. Lock parameter must be preserved
4. Run full payment flow test on Arc testnet

1. **Intelligent Matching**
   - User submits intent via UI/SDK
   - Escrow with dispute resolution

2. Run through payment flow

✅ **Contract Addresses**: Now clickable in sidebar and testing:

```bash
# Install Agent component
load_dotenv('config/.env')
private_key = kms.decrypt(CiphertextBlob=encrypted_key)
```

### Key Formatting Rules
```javascript
// ❌ DON'T: Forget await
gt completion zsh > "${fpath[1]}/_gt"

# List keys in namespace
Respect `.gitmodules` and `Directory.Build.props`—they embed repository URLs, reproducible build settings, and authorship data. Never check in API keys or document samples that contain customer data. Configuration overrides belong in `MarkItDownOptions`; guard new options with sensible defaults to keep the library safe for unattended execution.
6. **Capture Lessons**
   - Both parties fund escrow on-chain
   - Optimal value depends on CPU cores

### MCP Tools

```typescript
pytest -v --cov=zta_agent --cov-report=term-missing
```

### Adding API Routes (Pages Functions)
```
/
├── index.html
4. Add WebSocket support for Django Cast. The goal is to the newly generated SQL file if needed due to drizzle-kit limitations
async function loadAudioFile(path: string): Promise<Uint8Array> {
  let lastError: Error;

  for await (const filePath of scanDirectoryStream(rootDir)) {
    totalFiles++;
  }

  // Second pass: process files
  audioFile.dispose();
}
```

### 3. File Loading

- If translations don't update, regenerate with `source("data-raw/generate_translation_pars.R")`
- For Docker issues, check that all dependencies are properly installed in Dockerfiles
- Site metadata and usage examples
- ✅ All services running (Anvil, API, Streamlit)
- Updates as you progress through the shared `IChatClient` prompt constants, capture a thorough visual description first when requirements are completed
6. **Tone**: Professional, developer-friendly, clear and concise
Schema::table('user_credit_transactions', function (Blueprint $table) {
    $table->uuid('related_key')->nullable()->index();  // ✅ Same pattern
    // If parent mutation fails, both rollback
  },
});
```

**Technical Explanation:**

- Code written for Convex 1.0+ will continue to work without modification
- Transaction tracking functional

### Main Entry Point
- **NumPy**: Numerical operations
- **Contract addresses**: Anvil addresses ❌
- **Use Case**: Long-context analysis, market data, document processing

#### Import from Custom Data Sources

**Acceptance Criteria**:
- [ ] Independent tasks execute in parallel across Claude instances
- [ ] Tools:
  - `POST /v1/chat/completions`: Chat completions (streaming supported)
  - `conditional/` - Conditional gathering based on external rules from console.redhat.com
- `PAYMENT_DEMO_GUIDE.md` - x402 payment flow
- `grafana_log_level: 'debug'` - Pushgateway log verbosity
- `log_generator_feature_flag: false` - Syslog-ng log processing
- `grafana_log_level: 'info'` - Prometheus log verbosity  
- `CLAUDE.md` - Repository guidance for Claude Code
- `RAG-LlamaIndex/`: Example implementations and status management
- `make test` or `make unit` - Runs unit tests with race detection and observation
- `Taskfile.yml`: Task definitions for recipe management
- `fund_escrow(match_id, amount)` - Lock funds
- `GET /matches` - List matches with filters
- `Policy`: Condition-based rules with priority and effects
- `docs/`: All documentation markdown files organized by category
- `create_match(bid_id, ask_id, price)` - Execute match
- `calculate_exposure(address)` - Current positions
- `README.md`: User-facing documentation
- `POST /payments/create-intent` - Create payment
- `verify_signature` - Cryptographic verification
- `check_operating_mode.py` - Verify motors are in correct Operating_Mode
- `dotnet restore MarkItDown.slnx` – compile all projects with analyzers enforced.
- `dotnet test MarkItDown.slnx` – compile all projects with analyzers enforced.
- `commitlint.config.js` enforces Conventional Commits with a 100-character header limit; use `yarn commit` with Commitizen (`.cz.json`) if desired.
- Include comment-based help for public functions:
  ```python
  2. Scanning deploy.sh output for success/failure indicators
  offline support

### Performance Targets
- [ ] Volume discounts

---

## 🎉 CONCLUSION

When working with code in this
1. **What personal workflows would you like to automate?**
   - Newsletter summarization
   - Combines bootloader, partitions, and firmware
   - Status updates
   - Save (template auto-configures claims, don't edit manually)
   - Fallback resilience

Railway will detect multiple services. Add:

**Cloudflare Platform:**
| Resource | URL |
|---------|-----|
| Workers | https://developers.cloudflare.com/workers/ |
| D1 Get Started | https://developers.cloudflare.com/d1/ |
| Pages Functions Config | https://developers.cloudflare.com/pages/ |
| Workflows | https://developers.cloudflare.com/pages/functions/ |

**Clerk:**
| Resource | URL |
|-----------|-----|
| Hono | https://hono.dev/docs/getting-started/cloudflare-workers |
| Complex sale with orders + transactions + logs | ❌ | ✅ `related_key` |

### Requirements
- Convex deployment URL remains `*.convex.cloud`
- Signature verification correctly returned `False`
- Import groups: stdlib, external dependencies, current project
- Supports keyboard shortcuts (Cmd/Ctrl + K)
- market_data: Optional[MarketData]
- Role: Full-stack developer & AI engineer
- ✅ Dual-LLM architecture
- Verify payments
- Market sentiment (bullish/bearish/neutral)

**Answer: NO, if:**
- Framework: Docusaurus 3.9.0.0 --port $PORT`
- Settlement Agent: `services/agents/settlement_agent.py`
- History limit: 1000 entries

**ui/x402_payment_demo.py:442-458**:
```python
# ripgrep (rg) - content search 
fd -e md -x ls -la {}           # Example with ls 

# fd - file finding 
rg --files                      # List files (respects •gitignore)
│   │   └── boost.md
const album = await getCoverArt("song.mp3");
console.log(tags); // { title, artist, album, year, ... }

// Apply tags to get modified buffer
bun dev --remote           # Use REAL cloud resources (CAUTION!)

Need just current directory?
  → USE: ls -la  (OK for single dir)
```
