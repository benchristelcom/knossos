# Arc AI Agents - Hackathon Submission
## Arc AI Agents - Hackathon Demo

---

## 📊 SYSTEM CAPABILITIES

### Asset Management

- **`app.R`**: Entry point that calls `run_app()`
- **`R/run_app.R`**: Main application runner with `start_fun()` for initialization
- **`data.yaml`**: Master configuration with all UI text and variables
- **`app_params.yml`**: Configuration for all UI text and variable definitions
- **`inst/golem-config.yml`**: Golem framework configuration
- **`auth/`**: Contains service account keys for Google Cloud access
- **`prometheus/prometheus.yml`**: Prometheus scrape configs
- **`grafana/provisioning/`**: Auto-provisioned dashboards and datasources

### Run tests
```javascript
// MP3 → Both container and codec
// Returns: { id: 1, name: "Malaysia", iso2: "MY", phone_code: "+60" }

// Auto-select default country in form-select
<template>
    <!-- ✅ ALWAYS: Use FormContact for contact number inputs -->
    <td>{{ record.balance || 0 }}</td>        <!-- Unformatted number -->
    
    <!-- ✅ CORRECT - Use helper alerts -->
    <auto-button @click="$router.push({ name: 'admin.deposit.create' })">
        {{ $t('Create deposit') }}
    </auto-button>
</template>

<script setup>
// Standard modal handlers
scripts/           # Build and utility scripts
│   ├── db/
├── matching/
├── __init__.py
anvil --port 8545
```

### "Transaction Failed" Error

**Best for:** Predefined, repeatable processes

To complete full end-to-end testing:

1. **File Format**: Use Markdown (.md) or MDX (.mdx) for documentation
tail -f logs/streamlit_payment_demo.py          # Main implementation (single-file prototype)
○ Not right now (may suggest again later)
```

| Scope | Effect | TTL |
| --- | --- | --- |
| `this_target` | Suppress this rule for this table/column | 30-90 days (by concern type) |
| `this_owner` | Suppress this rule for this user's tables | 30-90 days |
| `storage_usage` | Database and file storage metrics |

**Environment-Specific Deployment:**

```typescript
3. Click **Create Intent**

### Core Principle: **Hybrid Approach**

> **Use traditional algorithms for core operations (fast, cheap, deterministic)**
>
> **Use AI for strategic decisions, insights, and anomalies (high-value, infrequent)**

---

## 🏗️ **Practical Architecture**

```
┌─────────────────────────────────────────────────────────────────┐
├── State Management # game-state-manager.h/cpp
5. Follow the established folder structure: `src/`, `__tests__/`, `tsconfig.build.json`
4. Workflow filename: `.github/workflows/release.yml`
7. **GitHub Release**: Creates release with changelog and file bandwidth (same as user file storage costs).

#### Restore Process

**File**: `client/src/components/demo-overview.tsx`
**Location**: Lines 238-268

3. **Parallel Processing**:
   - Ensure running: `ollama serve`
   - test.py:139-143: Added --merged-bin and -o flag pass-through to Docker compilation
   - Example command:
     ```bash
     uv run ci/ci-compile.py esp32c3 --examples BlinkParallel --merged-bin -o qemu-build/merged.bin
     ```

### What's Not Working
gt completion bash > /etc/bash_completion.d/gt

# Run release workflow (dry-run)
const albumArtist = props["TPE2"]?.[0] || props["TPE2"]?.[0] ||
  props["aART"]?.[0];

audioFile.dispose();
```

## Key Concepts

### Using act (GitHub Actions locally)

```python
python -m services.agents.market_agent
```

### Docker Example

```bash
# Run tests
6. **Full E2E**: Intent submission needs event log extraction fix
make logs

# Run all matches
5. **Squads** - Community and collaboration features
npx convex env set STRIPE_SECRET_KEY sk_test_...

# Production environment
import { convexTest } from "jsr:@charlesw/taglib-wasm";
import { readFile } from "stream/promises";

const result = await scanFolder("/messy-music");

console.log(`Container: ${props.containerFormat}`); // "MP4"
title = "Run tests"
2. Click "Run workflow"
textColor="#262730"
description = "Run ./scripts/bump-version.sh {{version}}"

[[steps]]
id = "publish"
4. See success message with 2 agents

---

### **Agent 2: Risk & Security (Week 5-6)**

**Goals**: Add agent monitoring to UI, comprehensive testing

**Fix Applied**: Lazy loading using `__getattr__` in `services/langgraph/__init__.py`

**Code References**:
- ✅ All agents import without errors
- Database migrations or seeders
- File: `services/agents/risk_agent.py`
- Error message: "Amount 100.0 above maximum 10.0"

#### 6c: Invalid Signature Rejection
- Gas used
- Circuit breakers for match status
- Price manipulation indicators

Transform the version sync workflow:

1. **Create component file**: Clone existing step component (e.g., `~/gt/`). Contains all projects, agents, and acceptance criteria.

## Getting Started in 60 Seconds

```typescript
import { useQuery, useMutation } from "taglib-wasm/workers";

export default defineComponent("myComponent");
```

**Why this matters:**
- ✅ 10x faster automated matching
- Deployment settings

### 1. Data Validation
- **Python**: Follow PEP 8 conventions, use type hints where appropriate
- **Remove all** modal state variables (`modalOpen`, `selectedModel`, etc.)
- **Verification**: Advanced verification strategies (formal methods, statistical testing)
- **Test (package)**: `cd <package> && pnpm test --run <file-path>` (use `--run` to avoid watch mode)
- **Alternative Rejected**: `@Transactional` + `@Rollback(false)` causes Hibernate session conflicts
- **TestContainers** - Integration testing with AI fraud detection
- **Common Conflicts**: Packages like tenacity, pillow, websockets, pyarrow, markupsafe, werkzeug, and integration test directories.
- **Smallest change that works**: Minimize blast radius; don't refactor adjacent code unless it
- **Option 3**: Quick forms with simple submit/cancel buttons

### Performance Metrics

| Base Class | When to Use | Key Methods |
|---------|------------|-------------|-------------|
| **Non-isolated** | `NonIsolatedEnv` | Local execution, same machine | `setup`, `load_context`, `execute_code` |
| **Explorer Link** | https://testnet.arcscan.app |
| **Annual** | **$334,800** | **$10,011,600** | **30x** |

**Conclusion**: At scale, AI costs are negligible compared to value ✅

---

### Environment Variables

**.env.local (Local Development):**

```typescript
// lang/zh_permission.json
{
    "Manage admin": "管理员管理",
    "address": "0xSettlementAgentAddress",
    "Welcome back, :name": "欢迎回来，:name"
}

// en.json (English - ONLY when different concept)
{
    "Cancel": "取消",
    "Username": "用户名",
    "Save": "保存",
    "Save": "保存",
    "Insufficient x": ":x 不足"
}

// en.json (English - ONLY when different concept)
{
    "Cancel": "取消",
    "Insufficient x": ":x 不足"
}

// en.json - ONLY add when different concept/grammar
{
    // === DIFFERENT CONCEPTS ===
    "Insufficient x": ":x 不足",
    "command": "codex",
    "interface": {
        "pass_rate": 1.0,
        "payment_request": payment_request,
        "skill_program_hash": "a3f8d2c1"
    }
}
```

### Reward Decomposition

```typescript
// No additional frontend validation needed
```

**For high-frequency counters that would cause OCC conflicts:**

```typescript
2. Match when bid price ≥ ask price
try {
    \DB::beginTransaction();
    // Business logic
    \DB::commit();
    const now = Date.now();
    return { temperature: data.temp, conditions: data.conditions };
  },
});

// Create agent with tool
│       ├── helpers
class FraudAgent:
    runs-on: ubuntu-latest
    route: 'admin.module.action',   // REQUIRED: Vue route name (for direct links)
    uv run ci/ci-compile.py esp32c3 --docker --examples BlinkParallel --merged-bin -o qemu-build/merged.bin --defines FASTLED_ESP32_IS_QEMU

    # Step 2: Request payment (402)
    workflow.add_node("risk_check", fraud_agent_node)
    workflow.add_node("fetch_data", fetch_intents_and_orderbook)
    public function multiLanguageColumns() {
        return static::loadAllFromCache();
    }
}

// Usage - ALWAYS use this instead of Model::all()
$categories = Category::loadAllFromCache();  // ✅ Cached
$product->attach($request->file('image'), ['group' => 'gallery', 'key' => 'main']);
$galleries = $product->attachmentsGroup('gallery');
$mainImage = $product->gallery('uuid-here');
```

### 8. Data Relationship Patterns

#### Foreign Keys vs Related Keys - Choose the Right Approach

**ROI: 332x daily** ✅

---

### **Use Case 2: Intelligent Negotiation** (HIGH VALUE)

**What needs an LLM (~30-40%):**

-   Generating suggested descriptions from column names and sample data
-   Schema truncation — LLM returns structured JSON, parsed and validated; unparseable output is logged and discarded

### 3. Circular Import Fixed ✅
- **CARDS.md**, **HANDBOOK.md**: Game documentation and separation of concerns.
- **No analytics** or tracking is performed.
- **Tests**: `test/` with unit, widget, and conversation history
- Month 6: 85% match quality (learned patterns)
- Confidence scores

#### 4. **Non-Determinism Risk** (MEDIUM)
**Purpose**: Submit new intents to the blockchain

**What Happens:**
- The directory name and transfers USDC to merchant

**Phase 2 (Agents)** - IN PROGRESS:
- [x] Matching agent tools work
- [x] Verified all imports working

### Run Integration (Recommended)

**Answer: NO, if:**
- Complex multi-table business transactions
- Key features (Squads, discussions, polls)
- Speed: <100ms (queries are async)

---

## 🚀 Next Steps

### During Development
- Summary metrics

**Everything is working!** 🎉

This guide covers all aspects of the Settlement Agent is **ready for development and testing**. All major components are deployed, verified, and ready for testing. Users can now create intents, match orders, and settle trades with full on-chain transparency.

---

*Last Updated: 2025-11-05*
*Network: Arc Testnet (Chain ID: 5042002)*
*Transfer TX: 0x1c200d4e24fb35dc45abe96f20d726ddf3ce7d4e2a30fe4c4215e21da3bc5d53*
