# GEMINI.md

1. **Payment Adapters** (`payments-adapter-*`): Unified payment interfaces for private keys, but these accounts don't automatically have clickable links that don't have frontend integration yet:

### Clone the Official Repository
```python
INFO:__main__:→ Executing liquidity_agent
│   ├── _constants.less
wrangler d1 execute <db> --local --command="SELECT * FROM users"

# Filter by IP address
│   ├── base_agent.py            ✅ 520 lines
│   ├── limits.py            # Rule-based risk engine
✅ Signature verified successfully!

Intent ID: 0x... (clickable link to explorer)
3. ⬜ Create Fraud Agent (2-3 hours)
st.markdown(f"[View on Explorer ↗]({st.session_state.tx_hash})")
```

**After**:
```python
const runtime = detectRuntime();
const audioFile = await taglib.open(buffer);

// From browser File object
3. Verifies transaction on blockchain
         ↓
├── tsconfig.json
git remote -v

# Tail specific agent test
3. **Executes final transactions** - Responsible for on-chain operations
Asset: BTC
└── en_permission.json   # Permission names in English (usually empty)
```

### Directory/File Exploration

```typescript
3. 🛡️ **Fraud Agent** - Detects suspicious patterns
│                                                                  │          │
Create `.streamlit/config.toml`:
```toml
[What are we trying to accomplish?]

### Files to Modify
- [ ] `ttcal/day.py` - Some methods need better documentation
- [ ] `ttcal/day.py` - Several properties and methods need documentation
- [ ] `ttcal/calfns.py` - Helper functions need documentation
- [ ] `path/to/file.py` - [what changes]
- [ ] `path/to/other.py` - [what changes]
- [ ] `path/to/other.py` - [what changes]
- [ ] `path/to/other.py` - [what changes]
- [ ] `path/to/other.py` - [what changes]

### Implementation Plan
├── Game Logic      # game-core.h/cpp - VertexArray particle effects
