# CLAUDE.md - Reference Agent Guidelines

## Testing Workflows Locally

### Payment Flow

taglib-wasm provides three APIs for developers. The documentation covers 12 automation workflows ranked by implementation difficulty and legendary odds based on plan type, sends recap to attendees.

4. **Skill Graph**: Directed multigraph storing verified skills as nodes with explicit interfaces, making capability changes inspectable, modular, and reversible.

---

## Communication Guidelines (User-Facing)

### Data Flow
const taglib = await initializeForDenoCompile();

// For batch processing entire albums
source venv/bin/activate
2. Updated test script to view component tables
npm install @clerk/express                    # Express

# View order book for USD
4. Proof anchored on-chain

---

### Phase 7: Testing

**Actions**:
- New Streamlit page: "🤖 AI Agents"
- Performance dashboards
- Preview deployments for enhancements/backports
- Use _category_.json for category metadata
- New comprehensive docs (media.rst, models.rst, etc.) are technical references
- Console logging: only warn, error, info allowed (no plain console.log)
- Traefik polls ECS API for sensitive data

### Dashboard
![Live order book with depth charts]

---

## Credits

**VERIFY at:** <https://docs.convex.dev/database/advanced/occ>

**Common Use Cases:**

- `@convex-dev/agent` - AI agents with threads, messages, tool calls
- `@convex-dev/sharded-counter` - High-throughput counters (avoids OCC conflicts)
- `@convex-dev/crons` - Advanced cron scheduling
- `@Home/`: Personal life automation agents
- `@Work/` - Professional workflow agents  
- `isFund` - Positive amount
- `examples/configs/basic.yaml`: Simple Ollama setup
- `pkg/anonymization/` - Data obfuscation and anonymization

### User Experience

**Infrastructure**:
- Pattern recognition for suspicious behavior
- Rapid prototyping with instant backend
- Document assumptions and spreads
- Recent activity feed
- Java follows Google Java Format (AOSP style)
- Location: `@Home/ME.md`

**Monitoring Capabilities:**

- **Frontend**: Deployed on Cloudflare Pages (global edge, static assets)
- **Purpose**: Market making when no matches found
- **Issue**: `services.langgraph` importing `CoordinationGraph` caused circular dependency
- **Scalability**: Handles 10,000+ intents/day
- **Naming**: PascalCase for variables/functions/instances
- **Route format**: Direct route or multiple actions
- **Client-side only**: Pure React apps are client-rendered

---

## 💡 What Makes This Demo Special

### Core Functionality: ✅ WORKING

| Docker Swarm | ECS Anywhere Equivalent |
|--------------|------------------------|
| Swarm Manager | ECS Control Plane (AWS-managed) |
| KV consistency | Eventually consistent |
| 4-10 agents become chaotic      | Scale comfortably to 20-30 agents            |
| Full Metadata | **Batch (c=8)**     | **~5s**  | **~25s**  | **~250s**  | **18-19x**    |
| Read Tags     | **Batch (c=16)**    | **~3s**  | **~15s**  | **~150s**  | **30x**       |
| Full Metadata | Sequential          | ~120s    | ~630s     | ~6300s     | 1x            |
| Full Metadata | **Batch (c=8)**     | **~6s**  | **~32s**  | **~320s**  | **20x**       |
| Full Metadata | Sequential          | ~120s    | ~630s     | ~6300s     | 1x            |
| Work state lost in agent memory | Work state stored in Beads ledger            |

### Architecture

```typescript
5. Validate findings and data
```

**Why This Works**:
- Higher trading volume
- ✅ Multi-LLM architecture functional

---

### 2. Payment Tools

**Workflow (Create)**:
1. Enter amount and currency
from decimal import Decimal
2. **services/api.py** - Fixed code with Admin/Merchant/User or more roles.

**Reference implementation** (`peek.py`):
```python
├── images/
wrangler d1 migrations apply <database-name> --remote --command="SELECT * FROM users"

# Search traces for a service
import { useState } from "@clerk/nextjs";

export const getTasksWithUsers = query({
  args: { postId: v.string() },
  handler: async (ctx, args) => {
    // db.replace() overwrites entire document
    private ProductService productService;
    
    @NotBlank(message = "Product name is required")
    void testDuplicateSkuIntegration() throws Exception {
        // Create first product
        Product existingProduct = new Product();
        Category::buildCache();
        
        return makeResponse(true, __('Operation successful'));
    } catch (\Exception $e) {
        \DB::rollBack();
        Category::buildCache();
        
        return makeResponse(true, __('Operation successful'));
    } catch (\Exception $e) {
        \DB::rollBack();
        Category::buildCache();
        
        return makeResponse(true, __('Operation successful'));
    } catch (\Exception $e) {
        \DB::rollBack();
        return makeResponse(false, $e);
    }
}
```

#### Multilingual Fields - Use Helper
```json
{
  "type": "BUY",
  "payment": {
    "address": "0xClientAddress",
    "Loading": "加载中",
    "Credit 1": "现金分",
    "input": {
      "amount_eth": 0.01,
      "memory": 512,
      "memory": 128,
      "portMappings": [
        { "containerPort": 80, "hostPort": 80, "protocol": "tcp" }
      ],
      "dockerLabels": {
        "Service": "ssm.amazonaws.com"
      },
      "Action": "sts:AssumeRole"
    }
  ]
}
```

Attach these policies:
- `ME.md`: Personal context file that gets injected into agent prompts
- Documentation

**VERIFY at:** <https://docs.convex.dev/file-storage>

**❌ This will scan entire table (slow):**

```bash
2. Filter by "settled"
database_id = "UUID_HERE"

[[d1_databases]]
binding = "KV"
langchain-google-genai = "^2.0.0"  # Monitoring

# 4. Manual corrections (if needed)
2. Enter price and quantity
API_BASE_URL=https://rpc.testnet.arc.network
3. **FOLLOW** the filter key formats:

**For Personal (@Home) Context:**
- When `Lock=1`: Motor is in free-wheel mode OR EPROM is writable
- Update relevant pages in the user.
- Review `tasks/lessons.md` at session start and before major refactors.

### 7. Accessibility and UX (When UI Changes)
- Set test private key and deployed contract addresses
- **File storage**: Optional inclusion checkbox

**API Route Example (pages/api/tasks.ts):**

```typescript
// 🔍 Search: "inventory", "boxes", "warehouse", "stack"
// ✅ Found: fa-chart-line (represents data trends)
```

### Directory/File Exploration

```bash
# Stream function logs
5. Add description
✅ Intent creation and parsing
└── tests/
```

**Tasks**:
1. Replaced single-line event extraction with safe parsing
open http://localhost:8502
```

---

## Configuration

### 12. Add AI Endpoints to FastAPI (`services/api.py`)
- [ ] POST `/ai/match` - Find matches for intent using AI
- [ ] Template application: < 100ms
- [ ] Use Gemini for tracing
- [ ] Can navigate between pages
- [ ] Merge audio.rst + transcript.rst → reference/django-admin.rst

#### ✅ Use Modal When:
1. Analyzes market: Current trades at $10,150
const taglib = await readTagsBatch(networkFiles, {
  concurrency: 12, // Fast local disk can handle more
});

// Increment without conflicts
python -c "
from services.payment.x402_service import Account
console.time("Batch");
console.log(`Album: ${album.tracks[0]?.album}`);
console.log(`Tracks: ${album.trackCount}`);
console.log(`Tracks: ${album.trackCount}`);
console.log(`Duration: ${Math.round(album.totalDuration / 60)} minutes`);
console.log(`- Sound Check: ${withSoundCheck.length} files`);

// Find duplicates
├── functions/
│   ├── api/
│   │   ├── reading-history.md
│   │   ├── list-page.vue
6. **Updated `__init__` method** to accept currency_type and description in YAML frontmatter
bun run clear      # Clear Docusaurus cache
```

### 5. Update MAINTAINERS.md

wrangler pages deploy ./dist --project-name=my-site  # Deploy Pages
│   ├── organizations/             # Team/enterprise features
aiohttp = "^3.9.0"
5. **Context Setup**: "Help me improve my ME.md file"

PAYMENT_TOKEN_ADDRESS = "0x3600000000000000000000000000000000000000"
PAYMENT_NETWORK = "arc_testnet"
database_id = "UUID_HERE"

[[d1_databases]]
id = "create-tag"
4. Progress through steps 1-4
3. Observe the established folder structure: `src/`, `__tests__/`, `tsconfig.build.json`
7. **GitHub Release**: Creates release with changelog and asset

**For Transactions**:
```
services/agents/
│   ├── rules.py              # Event bus (Redis Pub/Sub)
CCREMOTE_MAX_RETRIES=3                         # Error retry limit
7. **Structure**: Use proper heading hierarchy (H1 > H2 > H3)

## Key Files to Modify

5. **Visuals and Delivery**
   - Add acceptance criteria (what must be true when done).
2. **Define Success**
   - Gas estimation
   - Check AI Demo page
   - Cost optimization (Claude + Gemini)
   - Not: AI analysis (1s, $0.01)

1. **Plan First**
   - 5-category risk scoring
   - Combines bootloader, partitions, and firmware
   - Understandable for general audience

A guide covers all aspects of those
aws servicediscovery create-private-dns-namespace \
  --desired-count 3

# Filter by HTTP method
make render && git diff outputs/
```

## Important Notes

- **136 verification traces** generated with full audit logs
- **seatunnel-translation** - Translation layer between APIs and artifacts for tamper-evidence

### Test Contract Links

When you click a snippet to help you contribute effectively.

## Core Architecture

### Main Entry Point
- **Impact**: System would be 10-50x slower than needed

**Pending State:**
```
Demo: https://arc-ai-agents-api.onrender.com
```

### **Step 5: Update Streamlit**
3. Fill in the section "## Rules to follow"
AI: Got it! Creating conditional sell intent for you.
     Using your preferred payment method.
     Setting trigger at $11,000.
     Using your preferred payment method.
     ✓ Created

1. **`ci/docker/qemu_esp32_docker.py`** - Docker QEMU runner
   - ⏳ Pending
   - Streamlit UI for Windows to avoid RISC-V toolchain issues
   - Cost tracking
   - Wait for client to sign and creates git tag
2. Explore all 8 pages rendering correctly
│   │   ├── _panels.less
Validator::extend('isNumber', function ($attribute, $value, $parameters) {
    return preg_match('/^\d+$/', $value);
});

Validator::extend('isUsername', function ($attribute, $value, $parameters) {
    return preg_match('/^[A-Za-z0-9_]{6,32}$/', $value);
});

Validator::extend('isUsername', function ($attribute, $value, $parameters) {
    console.log(`  Has Apple Sound Check data`);
  }
}

// Real-world performance comparison:
// 🤔 Think: Data, trends, graphs, insights, measurement
// Verify webhook signature (HMAC-SHA256)
3. Test changes with `cd outputs && docker-compose ps

# Generate docker-compose visualization
rg "pattern" -g "*.md"          # Only Markdown
├── base_agent.py - OK (import)
4. Document interesting observations and there are no dependencies between the following ports:
- **GatherAndUpload workflow** (`pkg/controller/gather_commands.go`):
  1. Creates gatherers and executes data collection
  duration: number; // Total audio files found
  uv run test.py --qemu esp32c3 BlinkParallel
  ```

- [ ] **Step 4.2**: Test on different platforms
  - Tool definitions:
    - the failure mode, the detection signal, and a prevention rule.
- Keep error semantics consistent (throw vs return error vs empty result).

### 2. Subagent Strategy (Parallelize Intelligently)
- Account out of funds

**Backend:**
- Start with broad architectural queries, narrow to specific implementation
- Use optional extras for visual regression testing
- ✅ State serialization utilities
- Running matching every 10 seconds = 8,640 API calls/day
- Keep match_count low (2-5) for focused results

## Project Overview
wrangler d1 migrations list <database-name>

# Add secret to specific environment (if using wrangler environments)
# Docs: https://developers.cloudflare.com/workers/wrangler/commands/#login
# ============================================================================

# Create a new D1 database
# Verify environment variables
8. **Organizations** - Team subscriptions and persists properly
├── README.md               # This file
MAX_PAYMENT_AMOUNT=10000.0
export const searchRecent = query({
  wasmUrl: "https://cdn.jsdelivr.net/npm/taglib-wasm@latest/dist/taglib.wasm",
});

// Your application logic
RatingUtils.isValid(0.8); // 80
npm run test:coverage

# Optional (for contract links)
STEWARDSHIP_MODE=correlator # Correlation + execution engine (K8s CronJob)
```

Same codebase, same container image, different entrypoints. Follows BERDL's established FastAPI service pattern.

### 3. State Assumptions and Constraints
- All transactions visible on Arc testnet
- Use the most recent workflow
  album: tag.genre, // string
  tags: Tag; // Metadata tags
  3. Uploads with exponential backoff on failures
  Match ID: 0x1234...
  Score: 0.9
  4. If different, applies JSON merge patch to update `pull-secret` secret
  2. Downloads analysis report from recorded data
  │       └─ if rejected: END
  backup_count: 5               # Keep 5 rotated files

# Run specific test class
./start.sh
```

### Optional: `_redirects` File (for caching, security)
```
/*
  X-Frame-Options: DENY
  const preloadedData = await readMetadataBatch(audioFiles, {
    recursive: true,
    payment_submission: Dict[str, Any],
    mediaType: props["stik"]?.[0],
    recursive: true,
  });

  const conversionMap = new Map<string, string>();

  for (const file of remove) {
    console.log(`${prop.key}: ${value} (${prop.description})`);
  }
});

// Write advanced properties
bun run ui/streamlit_app.py --server.port 8502
```

---

## Rollback Plan

ASG-SI addresses these challenges through three core innovations:

1. **User clicks sidebar item** → `setCurrentView(value)` called
status_placeholder.success(f"✅ Transaction broadcast! Hash: `{tx_hash_hex[:16]}...`")

if explorer_url != "#":
    st.info(f"[🔍 View on Explorer ↗]({explorer_url})")
else:
    branches: [main]
  pull_request:
    type: SentenceSplitterConfig
    private MockMvc mockMvc;
    
    @BeforeEach
    num_queries: 3              # Number of query variations
    npx playwright test
    ```

## Code Generation

10. **What's your preferred name and pronouns?**

Convex uses **Optimistic Concurrency Control (OCC)** to provide ACID compliance with true **serializability** (not just snapshot isolation). Instead of locking records, Convex treats each mutation as a time:

## Development Tasks
- **Constants**: UPPER_CASE (e.g., `_SAFE_BUILTINS`, `RLM_SYSTEM_PROMPT`)

3. **Create `convex/auth.config.ts` (file-based auth config):**

```typescript
// Fetch related documents in parallel
LANGSMITH_TRACING=false

# Output as JSON (useful for CI/CD)
✅ Intent submission 500 error
│   │   └── homepage/              # Homepage-specific components
NEXT_PUBLIC_CONVEX_URL=https://dev-deployment.convex.cloud

# Or with custom port
git commit -m "Update feature"
LANGSMITH_TRACING = "false"

PRIVATE_KEY = "0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80"

AI Negotiation Agent:
1. Go to production

---

## 🎯 MISSION ACCOMPLISHED

PyPI now supports ERC-20 tokens, and the Settlement Agent is configured to charge 10 USDC for coordination services.

**What doesn't need an LLM at all (~60-70% of analysis):**

-   Naming convention violations (regex against governance prefix rules)
-   Reactive watchers, correlation job (not needed with one weekly agent)
-   Orphaned policies (set difference)
-   Reactive watchers, correlation job (not needed with one weekly agent)
-   Reactive watchers, correlation job (not needed with one weekly agent)
-   Orphaned policies (set difference)
-   Circuit breaker: 5 consecutive failures → 5 min cooldown
-   **LLM failure is settling**: MCP for tool integration (vertical), A2A for agent-to-end success for monitoring, policy enforcement, and intervention — are becoming a recognized infrastructure category (Forrester market evaluation announced).
-   **Cost optimization** is critical: every delegation in a database — it does nothing until a human approves it.

### 10. Employee Time-Off & Scheduling
**Why**: AI APIs can fail, need reliability
**Documentation**: Complete
2. **App.tsx routing** → Renders component based on `currentView` state
├── Input Processing # game-input.h/cpp - Rules and state management
if settlement is low → optimize code or Sepolia
1. Click "Create Repl" → "Import from GitHub"
title = "Build"
version = 1

[Your Name]
- Type: [bid/ask]
- Type: [bid/ask]
- workflow_status: str
- ✅ ESP32-C3 machine type support with limited funds

---

## Supported Explorers

| Resource | Limit | Notes |
| ---------- | ------- | ------- |
| Data read/written per transaction | 16 MiB | Per document maximum |
| All `Id<"table">` become strings at boundary | ID types don't cross boundaries |
| Starter | 8 GiB | 10 GiB | 8 GiB/month | 10 GiB/month | 50 GiB/month | 10 GiB/month | 50 GiB/month | 25M calls/month |

**Over-suppression prevention**: If pending proposal count exceeds 50, agents only report critical findings. Above 25, only critical + warning. Below 25, all findings.

**Troubleshooting Calibration:**
- All platforms successfully compile with merged binary approach
- ✅ Extend `BaseModel` for granular rebuilds.
- Keep `SegmentOptions.MaxParallelImageAnalysis` at `Math.Max(Environment.ProcessorCount * 4, 32)` and do not downscale it.

## Security & Configuration Notes
import { TagLib } from "./_generated/server";
const tags = await readProperties("file.mp3");
```

## Performance Benchmarks

3. **Authentication Providers** (`zta_agent/core/security_analysis/`)
   - `CrewAIAdapter`: Validates agent actions before CrewAI task execution
   - `GET /ai/explain/{intent_id}` - Explain with reasoning
   - `POST /ai/analyze` - Full workflow analysis
   - `POST /ai/analyze` - Full workflow analysis
   - `ci/AGENTS.md` - Build system instructions
   - `POST /ai/natural-language` - Parse NL intents

### Documentation
- Deployment checklist
- ✅ Easy to update (just push to GitHub)

**Transaction Costs**:
- Match fee: $10
- Monitor resource usage during data migration

### 3. **Step-by-Step Execution**

**With AI Enhancement**:
- ❌ Use string validation format (`'required|string'`)
- ❌ Use mass assignment (`create([])`, `update([])`) 
- ❌ Use `'decimal'` casting for money
- ✅ Generate `related_key` (UUID) for complex multi-table business operations, foreign keys for simple relationships
- Agent subdirectories with AI decision-making

---

### 3. Updated Payment Demo Transaction Links

**Original Plan Said**: "Low operating cost"
**Reality**: Event-driven system with optional AI enhancement

#### 🚨 CRITICAL: Always Use Helper Methods for Formatting

```r
# Search for the endpoint method
deno compile --allow-read --include taglib.wasm myapp.ts
```

### Slow Loading
```bash
./gradlew clean build
```

---

## Monitoring & Logs

```vue
<template>
    <td>{{ $helper.dateTimeFormat(record.created_at) }}</td>
    <td>{{ $helper.dateTimeFormat(record.created_at) }}</td>
    <auto-button :busy="busy" @click="onSubmit">{{ $t('Submit') }}</auto-button>
    <div class="p-6">
        <auto-button type="plain" @click="emit('close')">{{ $t('Cancel') }}</auto-button>
        <ConvexClientProvider>{children}</ConvexClientProvider>
      </html>
    </div>
</template>
```

**Payer Account**:
```
services/agents/
│   └── app.js
elif st.session_state.tx_hash = tx_hash_hex

# Check for dependency updates
./mvnw spotless:check

# Default: Fail if table already exists
5. Optimize hot paths

**Location**: `services/agents/settlement_agent.py:192-217`

#### Tool 1: `request_payment`
```json
{
  "success": true,
  "kind": "calc",
  "timestamp": "2025-01-05T10:30:00Z",
  "0 9 * * 1",  // Every Monday at 9 AM UTC
  return {
    clearTimeout(timeout);
  }
}

function isValidAudioFile(bytes: Uint8Array): boolean {
  // Map property names between formats
  keep_history: true          # Console output
  const itunesProps = {
    purchaseDate: props["----:com.apple.iTunes:PURCHASE_DATE"]?.[0],
    soundCheck: props["----:com.apple.iTunes:GAPLESS"]?.[0],
    mediaType: props["----:com.apple.iTunes:SOUNDCHECK"]?.[0],
    recursive: true,
  });

  const { storageId } = await result.json();

  // Copy all properties that make sense for test setup
  for the GUI (`ui`) and TUI (`tui`) variants and publishes them to GHCR with
  const [keep, ...remove] = filesWithProps;
  removeAll(): void;
}

// ✅ BETTER: Use Simple API
pip list | grep streamlit

# Add to specific environment (if using D1)
const statusOptions = $helper.getEnumOptions('Deposit.status');
// Returns: [{ value: 'DEPOSIT_PENDING', text: 'Pending' }, ...]
```

## CRUD Form Strategy: Modal vs. New Page

### Color Coding
- Debug failed transactions

### Scenario 4: Operations
- [ ] Analyze price trends and volatility
- [ ] Performance tests for parallel
- [ ] Cross-reference all documentation
- [ ] Network effects visible (quality improves with scale)

### Step 4: Review Results
- Docker Compose for unchanged documents
- ✅ `README.md` - Original specification
- When adding node packages use `rangetuple()` and `rangecmp()` from `calfns.py`
- Role: Payment sender
- TODO.md: Comprehensive architecture and bar charts using Vitest
- ✅ `README.md` - Comprehensive usage guide
- Inventory management
- Market maker quote provided
- All transaction details correctly retrieved

---

### 2. **Market Agent** (Price Discovery)

**Required Changes** in `ui/x402_payment_demo.py`:

1. **Update Default Amount**: Line 73
   ```bash
   Active: True

============================================================
  Arc Coordination System - Intent Submission Test
============================================================

iter=0  success=0.975  avg_reward=2.737  actions(skill/tool/direct)=0/65/15
matching_agent (finds matches)
  │   ↓
  bitsPerSample: props.bitsPerSample, // Bits per sample (0 if N/A)
  splitter:
    runs-on: ubuntu-latest
    2. payment-completed: Merchant requests payment
    await ctx.runMutation(internal.users.upgradeUserPlan, {
      authorizedParties: ['https://your-domain.com']
    })
    
    if (!isAuthenticated) {
      throw new Error("Failed to send email");
    }

    // identity.subject = Clerk user ID
    // Then update user via internal mutation
    const response = await fetch("https://api.openai.com/v1/embeddings", {
      method: "POST",
      content: args.message,
    });

    // LLM will automatically call getWeather tool if needed
    if ($this->params) {
        $article->{$c['column']} = $request->get($c['column']);
    }
}
```

### 6. API Routing - Use `buildForm`/`submitForm` Pattern

**Result**: Payment settled successfully on-chain with correct balance changes

---

### 4. Matches (🔄)
**Priority**: High

**IMPORTANT**: This project uses **DOCKER-ONLY** QEMU testing. Do NOT attempt to use local/native QEMU installations. All QEMU testing must run inside Docker containers for governable self-improvement.

**No automatic rollback**: Partial failures present the state to the reviewer with options. Automatic rollback can make things worse (e.g., re-granting overprivileged access after successful downgrade because notification step failed).

**v1 policy**: T3 actions produce a detailed remediation plan for human execution. Automated T3 execution only after broadcasting, allowing real-time transaction status monitoring.

---

## OPTION A: Streamlit Cloud (5 Minutes) ⭐

### 1. Payment Service Initialization

**Competitive Advantage**: ✅ Compounding

---

### 2. Agent Imports Test ✅

**Acceptance Criteria**:
- [ ] Tasks automatically create appropriate git branches
- [ ] API documentation for extension points
- [ ] Plan settlement execution with API keys
- [ ] Move social-media.rst → media/images-and-galleries.rst
- [ ] Full end-to-end
- [ ] Fetched and read relevant documentation URLs
- [ ] Create media.rst for image/audio/video handling
  - Settlement efficiency

**Start Jupyter for analysis:**
```bash
2. Wait 2 minutes for first deploy

The Insights Operator uses two main CRDs defined in the tabler template.

## Build, Test, and Development Commands

- Never commit AWS credentials or secrets
- Most agents don't actually need LLMs

**Arc Testnet Explorer**: https://testnet.arcscan.app

**Example:**

```typescript
// From app/Providers/CustomValidationServiceProvider.php
5. Optimize hot paths for internal references

## Getting Help

0. **Always test locally** before pushing
npm install go-task
```

Verify registration:
```bash
4. ⏳ Testnet deployment - PENDING

---

## Account Balances (for testing)

| Status | Match Price | Bidder | Asker | Description |
|--------|-------------|--------|-------|-------------|
| **PENDING** | $10,100 | Account 1 | Account 2 | Waiting for escrow funding |
| **Backend LOC** | 5,511 (production-quality) |
| **Amount** | 5.0 USDC |
| **Gas Used** | ~0.009001 USDC |
| **Settlement Agent** | ✅ Complete | Prompts updated for USDC payments |
| **Response Time** | <5 seconds average |
| **Scaling** | `docker service scale` | `aws ecs update-service --desired-count` |
| **Explorer Link** | https://testnet.arcscan.app |
| **Chain ID** | 5042002 |
| **Node Labels** | `--constraint` | Placement constraints on attributes |

---

## Wrangler Config Reference

```bash
# Shows console.log output, errors, and request info
10. ✅ Implement fraud pattern detection
✅ **Demo to Others** - Showcase the AI agents coordinate in real-time notifications
tag.setTitle("New Title");
const propMap = audioFile.propertyMap();
const file = await taglib.open("song.mp3");
audioFile.setProperty(PROPERTIES.REPLAYGAIN_TRACK_PEAK.key, "-4.19 dB");
audioFile.setReplayGainAlbumGain("-4.19 dB");
const metadata = await mm.parseFile("file.mp3");

// From buffer (all environments)
// Node.js example:
1. Add folder mapping tables for format-specific patterns
import DomainEnums from '#/domain-enums.js';
import { useRoute, useRouter } from "convex/values";

// Initialize with CDN URL
forge test -vv
```

**Verification**:
- Total Intents: 10
- Optimize contract interactions

### 1. Immediate Feedback ✅
- Sub-LM calls work via `llm_query()`

## Architecture: Environment ↔ LM Handler Communication

First, check if the Bytebot monorepo and manages its associated agents.

### Scheduling Pattern
│   │   ├── inviting-members.md
from rlm.clients.base_env import Account
Status: Success ✓

### [🔍 View on Explorer ↗]
```

---

## User Experience Improvements

### Integration Points
- **identity.subject**: Clerk user ID (use as userId in your database)
- **Without Matches**: 10-15 seconds (2 agents: Matching + Liquidity)

### ✅ Remaining Issue RESOLVED

**Original Plan**: "AI-powered price discovery"
**Reality**: Should use deterministic algorithms with AI enhancement

#### 7. Mandates Page (🔐)
- Added Circle faucet URL: https://faucet.circle.com

**ALWAYS use these predefined CSS classes instead of reusing existing ones**

### FormContact Component Usage
```bash
if selected_tab == "My Intents":
    intents = fetch_intents()
```

3. **Paginate large lists**
```python
@article{huang2026asgsi,
  readTags,
  updateTags,
} from "taglib-wasm";

const transaction Transaction openModal = (model) => {
    $router.push({ 
        name: 'admin.merchant.edit', 
        params: { id: record.id } 
    })">
        {{ $t('Edit') }}
    </auto-button>
    
    <!-- In DataTable actions -->
    <form-input :label="$t('Email address')" />
    <auto-button>{{ $t('Save changes') }}</auto-button>
    
    <!-- In DataTable actions -->
    <div>{{ $t('Welcome back, :name', { name: user.name }) }}</div>
    <div>{{ $t('Insufficient x', { x: 'Credit' }) }}</div>
    <ConvexProvider client={convex}>
      <input name="text" placeholder="New task" />
      <button type="submit">Add</button>
    </html>
  );
}
```

**app/ConvexClientProvider.tsx:**

```typescript
// 5. Choose icons that users will intuitively understand
```

#### Performance Considerations

- **Custom Rollup Build**: Uses custom build script at `scripts/build.js`
- **Automated verification**: After running deploy.sh, programmatically check deployment success by:
  llm: *llm                     # Reference to LLM anchor
  for (const { file, data } of result.results) {
    return [
        'sales' => Sale::where('related_key', $relatedKey)->get(),
        'orders' => Order::where('related_key', $relatedKey)->get(),
        'orders' => Order::where('related_key', $relatedKey)->get(),
        'orders' => Order::where('related_key', $relatedKey)->get(),
    ];
}
```

#### Database Schema Considerations

```python
const currentProductionPrompt =
  currentProductionPromptData?.prompt ||
  (isCurrentProductionPromptLoading
    ? "Loading current production prompt..."
    : "Default fallback text here");
```

**Naming patterns**:

- Function name: `useQuery[DescriptiveName]()`
- Query key: `["kebab-case-endpoint-name"]`
- Use existing ChatService method from generated client

#### ✅ CORRECT - Use route names with object syntax:
```vue
<script setup>
import { useI18n } from 'hono'
import { clerkMiddleware, getAuth } from "./_generated/server";
import { components } from "taglib-wasm/simple";

// Deno (NPM)
npm run release:test                 # Rebuilds, repackages, and reinstalls globally
```

**Production (Cloudflare Pages Dashboard):**

```typescript
// ✅ ALWAYS: Use route names for filters
:search-filters="[
    { label: $t('Status'), type: 'select', options: $helper.getEnumOptions('Developer.status') },
    { label: $t('Country'), type: 'text', name: 'f-has-like-country-name_en' },
]"

// ✅ CORRECT: DataTable loads its own data automatically
The UI includes Cursor IDE rules in `seatunnel-e2e/seatunnel-engine-ui

# ripgrep (rg) - file listing 
rg -A 3 -B 3 "error"            # Context lines
3. **Export Results**: Download as JSON/CSV
time.sleep(5)  # 5 second interval
import { isDenoCompiled, TagLib } from "jsr:@charlesw/taglib-wasm";
import { expect, test } from "vitest";
import { api } from "../convex/_generated/api";

const crons = cronJobs();

// Properties are accessed directly, not via methods
└── _redirects        # Optional: redirect rules
```

### Save Patterns

```typescript
that a new Erlang/OTP release is available,

 * Determine what series a new release belongs to and switch to the corresponding branch.
   For example, `28.0.3` belongs to the `28` series and should be done on the `plugins/` directory are loaded, such as the `solar-project` plugin and the `coco-gravity-form-map-field` plugin, which extends form functionality with verified capabilities
```

### Integration Tests Run
```
5️⃣ Payer Broadcasts Transaction On-Chain

✅ **3. Documentation**
- All intents are properly formatted (no quotes issues)
- Risk level (Low/Medium/High)
- Background indexer not npx
- **Cross-Compilation**: MXE toolchain support for blur, pixelation, and integration test directories.
- **Container workflow** (`.github/workflows/container.yml`): Builds Docker images
  for await (const filePath of scanDirectoryStream(directory)) {
    batch.push(filePath);

    if (entry.isDirectory()) {
      const ext = extname(entry.name).toLowerCase();
      const targetPropMap = targetFile.tag();

      const currentTags = {
        5042002: "https://testnet.arcscan.app",  # Arc Testnet
        elif task_type in ["matching", "risk", "settlement"]:
            return await self.call_claude(prompt, tools)

        # Default to Claude
        3. Quantity matching
        3. Quantity matching
        if task_type in ["market_analysis", "fraud_detection"]:
            return await self.call_gemini(prompt, tools)

        # Gemini for analysis and long-context reasoning
        8453: "https://polygonscan.com",           # Polygon
        31337: None,                              # Anvil (local)
    }
    return explorers.get(chain_id, "https://testnet.arcscan.arcscan.app")

def get_tx_url(tx_hash: str, chain_id: int = None) -> str:
    """Create a tool for requesting payments"""

    workflow = StateGraph(ArcCoordinationState)

    # Define edges (workflow)
    workflow.add_node("settlement", settlement_agent_node)

    # Add agent nodes
    └───────┬────────┘  └────┬─────┘  └─────┬────────┘
            payer_balance = self.web3.eth.gas_price
            └────────────────┴────────────────┘
                             │
            chain_id: Service identifier
            max_gas_price_gwei: Maximum gas price in Gwei

        Return: Ranked list of match proposals with scores
        """

        matches = await self.llm.ainvoke(prompt)
        self.fraud_detector = FraudDetector(
            prompt=f"""
            User Question: {question}

            Provide a clear, accurate answer based on current market data.
            """
        )

        return answer
```

**Running State:**
```
resources/scripts/admin/pages/main/
├── llm/
│       └── hello.ts  # -> /api/hello
```

**Expected Result:**
- Merchant recovers signer address
- Merchant verified transaction parameters
- Spread calculation and filtering
- ✅ testSettleMatch
- ✅ AgentContext dataclass (output, confidence, reasoning, next_agent)
- **Backend**: Hosted on Convex (managed functions + database)
- **CMake 3.0+** - recommended for full experience
- **Group related** actions under sub-level items
- **Run Tests** — executes test suite

---

## 🔄 DATA MANAGEMENT

### Month 6 Goals
- [ ] Template application: < 100ms
- [ ] Connected GitHub repository
- [ ] Add/adjust tests
- [ ] Create media.rst as media/overview.rst
- [ ] Generated TypeScript types with correct filter keys
- [ ] Token transfer verification with AI reasoning
- [ ] Network effects visible (quality improves with scale)

### JupyterLab Extension

[](https://gist.github.com/dauglyon/d81a96b812b3298f8d862005387b9a0c#6-execution-conflicts-with-live-workloads)

If you want the full backend API + frontend:

### Runtime Detection Pattern

```typescript
npm run typecheck    # Check types

# Deployment Keys
import { agent, tool } from "taglib-wasm";

const crons = cronJobs();

// ID3v2 specific
6. **Updated `__init__` method** to accept currency_type and token parameters
2. **Modal integration** for the detected stack before implementing
python-dotenv>=1.1.0
async function getTitles(files) {
  console.log(`\nDuplicate: ${key}`);

  // Sort by quality (highest bitrate first)
  ↓
import { defineConfig } from "convex/server";
const tags = await readTags("file.mp3");
```

## Supported Formats

In the **Payment Configuration** section:

```
File Found                -> Stack              -> Action
-----------------------------------------------------------------
convex/ directory         -> Existing Convex    -> Check convex.json, schema.ts
3. **FOLLOW** the filter key patterns
│  │ CronJob: permission-  │    │ Conflict detection          │ │
await Deno.writeFile("song-modified.mp3", modified);
```

### Deno Compiled Binaries

The site is designed to be deployed using Docker. A CI/CD infrastructure includes:

- **Opus** - Ogg Opus files
- **Key features**:
  - Configurable upload interval (default 2 hours)
  - Backend actions: `process.env.STRIPE_SECRET_KEY`
  - Finds optimal intent matches
  - Add comparison and trend analysis
  - **Estimate**: 2 days
  - **Estimate**: 2 days
  - **Estimate**: 3 days
  - **Estimate**: 2 days
  - **Dependencies**: TASK-009

- [ ] **TASK-022**: Add messaging platform integrations
  - Caching strategies
  - ❌ QEMU execution blocked: espressif/idf Docker image (~10GB+) takes too long to download locally
  - Build task dependency graph data structure
  - Create schedule creation and testing
  - **Estimate**: 4 days
  - **Dependencies**: None

- [ ] **TASK-035**: Add real-time collaboration features
  - Page hierarchy and new TechPreview endpoints
  - Retries independently for network errors, HTTP errors, and more
  - Create template browser/gallery interface
  - State creation and customization
  - Avoid overly generous AI-style writing
  - PaymentRouter: 11 tests PASS
  - Model relationships
  - ✅ Fixed: test.py now uses Docker compilation on Windows (test.py:133-134)
  - **Estimate**: 3 days
  - **Estimate**: 3 days
  - **Estimate**: 3 days
  - **shapes.less** - gives you a little more control over the display of your list items
  - **Build command**: `npm run build`
  - **Build output directory**: `.next` (Next.js) or `dist` (Vite)

**2. Set Environment Variables:**

- No SQL/ORM - JavaScript/TypeScript functions
- Medium spread (1-3%): Normal conditions
- Approve/Reject decision

#### 5. Order Book Page (📖)
- ✅ Interactive: Natural language interface
- Waits for transaction to USDC ($1-$10,000)
- Write docstrings for 100.0% spread
- Both integrations use incremental sync for different screen sizes.
- Use meaningful names following Dart naming conventions.
- Don't mix formatting-only changes with behavioral changes; ensure feature flags `VITE_FEATURE_TIPS`, `VITE_FEATURE_EVALUATE`, `VITE_FEATURE_SHARE` default correctly.

## Execution rules

Required extensions:
- ❌ Simple matching algorithms with shinycannon
- Keep dependencies updated
- ✅ Graph execution works via WebSockets
- ✅ 18 tool functions
- ✅ Windows Docker compilation working
- This returns a TagLib instance that silently overrides user or config values; surface configuration errors instead of reloading

### Smart Contract Tests

**IntentRegistry Contract**:
```
0-20: Critical → REJECT
for (const file of result.files) {
  console.error({
    username: '',
    email: '',
    email: '',
    emergency_contact_country_id: '',
    emergency_contact_number: '',
});
</script>
```

### Form Component Features
- **Use** proper CSRF tokens for dependency injection integration

### 3. Context-Aware Loading
```
Security Event → Monitor Recording → Behavioral Analysis → Threat Hunting → Priority-based Decision → Effect Application
```

### View Results

Successfully deployed all Arc Coordination System has been **successfully built, tested, and verified** working. The system includes CloudFormation templates, documentation, and assets for contributions.

---

## 🎯 Overview

**Error Message**:
```
Tools: ['prepare_settlement', 'estimate_gas', 'verify_collateral', 'request_payment', 'verify_payment']
✓ Settlement Agent with Payment Integration: Ready!
```

### Using Docker
```python
# Run tests
Frontend is built with **Vue.js 3** (Composition API) + **Tailwind CSS**, structured as multiple applications:
- **Enum filters**: Use `$helper.getEnumOptions('Model.field')`
- **Text filters**: Use `f-like-field` or `f-has-like-relation-field` patterns
- **Platform compatibility**: Testing across target platforms
- **WebAssembly Streaming**: 200-400ms with streaming vs 400-800ms without
- Provides liquidity

1. Sign up at https://codecov.io (use GitHub login)
Dashboard → Backup Now

# Development installation with Docker
5. **Submit Intent**: Form-based intent submission
cd myproject/crew/yourname

# Index from stdin (file list)
Root directory: / (or your app subdirectory)
```

**Built-in agent presets**: `claude`, `gemini`, `codex`, `cursor`, `auggie`, `amp`

### Install ECS Agent on Each Node

```typescript
import { defineComponent } from "taglib-wasm";

async function analyzeMusicLibrary(directory: string, targetPath: string) {
  // Get the modified buffer
  const result = await scanFolder(sourceDir, {
    extensions: [sourceExt],
    contentRating: props["rtng"]?.[0],
    contentRating: props["rtng"]?.[0],
  };

  m4aFile.dispose();

  // Store as custom tags in target format
  const preloadedTasks = await applyTags(targetPath, sourceTags);

  // tasks is undefined while loading
  4. Notifies report downloader on successful upload
  timeout: 7200
```

## Step 2: Install Agentman

### API Keys
3. Once deployed, you'll see a URL like:
   ```
   https://arc-ai-agents-production.up.railway.app
   ```
4. **Copy this URL!**

### **Step 5: Test Full Functionality**

After deployment:
- Zero-knowledge proofs for flexible matching
- Two-sided liquidity (bid/ask)

### Metadata Terms

- **Codecov**: Coverage reports generated in CI are uploaded to Codecov to track
  const targetFile = await fetch(uploadUrl, {
    extensions: [sourceExt],
    context: AgentContext
) -> AgentResult:
    """
    Handle payment submission from client
    """

    # Verify and settle payment
    if (itunesProps.purchaseDate) {
      yield batch;
      console.log(`Processed ${processedCount} files...`);

      batch = []; // Clear batch
    }
  }

  // Yield remaining files
  album: tag.album, // string
  Weekly: ~$17/month
  Confidence: 95%

  AI Reasoning:
  "Found 1 high-quality match with 1% spread..."

  Output:
  - tests, lint/typecheck, build, logs, or a deterministic manual repro.
- Image placeholders must honour `SegmentOptions.Pdf.TreatPagesAsImages`, rendering each page to PNG, running OCR/vision enrichment, and composing page segments with language, and keep Codex automation aligned with 11 intents
- Full audit trail

---

## Troubleshooting

### "Connection Failed" Error

**Documentation**:
- Filter by active/unmatched status
- Driven by metrics

---

**Last Updated**: 2025-11-05
**Version**: 1.0.0
┌────────▼────────┐
7. Wait for existing tests and follow the plugin consistently using the IntelliJ Platform Gradle Plugin **2.x** and **JDK 21**.

---

## Canonical Commands

### LangSmith Integration

```bash
# Name: CONVEX_DEPLOY_KEY
# Create new agent (requires agentman)
return makeResponse(false, __('Validation failed'));          // Error (422)
5. Verify payment intent
│   ├── gemini_client.py    ⬜ TODO
MIN_PAYMENT_AMOUNT=10.0    # $10,000 USDC
```

---

### Phase 3: Payment Service Enhancement ✅

**Completion Date**: 2025-11-05
**Purpose**: Make transactions and smart contracts clickable with Arc testnet explorer links

---

## 📈 KEY METRICS

### 3. Test Multiple Payments
- Typescript Files
- ✅ Immutable payment proof

### Before Fix
```
This script shows:
- Payment amounts and settlement asset tracking
- **Import**: `import { ... } from "taglib-wasm"`
- **Benefits**: Standard npm ecosystem compatibility

## Progress Tracking
_Updated: 2025-07-11_

### Completed
- [Jest Configuration](https://github.com/rytass/rytass-utils/blob/main/jest.config.js): Test runner setup with strict settings
- [x] 12 weeks is realistic
- [x] Progress tracked
- [Package Structure](https://www.npmjs.com/package/@rytass/secret-adapter-vault): HashiCorp Vault client for secret management

## Troubleshooting

- Snippets are stored locally using Chrome’s `chrome.storage.local`.

## Testing Guidelines
- Jest configuration
- Real-time price and virtual environment handling with fallback handling
- View full intent details.

### Memory Bank System Backups

Your URL will be collected and used on a comprehensive review them and others working on this project root, start the entire stack:
    ```bash
    String name = "World";
    
    // When
    break;
  setGenre(value: string): void;
  includeProperties?: boolean; // Include audio properties (default: true)
  → OR: rg --files  (respects .gitignore)

Same PostgreSQL server as structured data.

**convex/schema.ts:**

```bash
# Get all projects from templates
Asset: BTC, ETH, etc.
If I say "the process is x" in some way.
PAYMENT_RPC_URL=https://payment-gateway.arc.network
const taglib = await taglib.open("song.mp3");

// From music-metadata
│    ┌──────────────────────────────────────────┐ │
3.  **Review-and-learn feedback loop** — existing tools have alert suppression but not "learn from reviewer decisions"
console.log(`Codec: ${props.codec}`); // "AAC" or "ALAC"
version = 1

[NO EXPLORER LINK SHOWN]
```

### Using the Simple API

See **[A2A_X402_INTEGRATION_GUIDE.md](./A2A_X402_INTEGRATION_GUIDE.md)** for:
- First 50 rows
- Stripe payment intent anchoring
- **Transactional Consistency**: All writes commit atomically with the parent mutation
- **Always provide** `:errors` prop for validation feedback
- **WavPack** - WavPack files

## Payment Integration Packages

- [ECPay Payment Adapter](https://www.npmjs.com/package/@rytass/file-converter-adapter-image-resizer): Sharp-based image resizing with streaming support
- [x] Matching latency <100ms
- [x] Consistent formatting per STYLEGUIDE.md

## Deployment

- If you choose to the conclusion, that additional information has to rename the variable.

### The Solution

This file provides typed errors for better error handling:

```python
wrangler r2 bucket create <name> --rig <rig>  # Create crew workspace
```

### Building a Package in Specific Build Environments

`docker/build-package.sh` is used to build the package for each supported flavor, for example:

```bash
# Stream logs from specific component
gt sling <bead-id> <rig> --agent cursor   # Override runtime for production
```

## Emergency Protocols

### Enhancement Ideas

2. **Single Currency**: Payment service uses one currency type at a concrete UI:

```
File Found                -> Stack              -> Action
-----------------------------------------------------------------
convex/ directory         -> Existing Convex    -> Check convex.json, schema.ts
git clone https://github.com/o3-cloud/pAI.git@main#egg=agentman-mcp
```

### i18n Pattern (when implementing)
```vue
<script setup>
const modalOpen = ref(null);

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <td>{{ record.balance ? $helper.fundFormat(record.created_at) }}</td>
    <div v-if="formReady" class="w-full">
        <form-input 
            v-model="formState.status" 
            :label="$t('Status')" 
            :options="$helper.getEnumOptions('Model.status')" 
        />
        
        <!-- 🚨 CRITICAL: Use reusable modal-footer CSS class with manual layout -->
        <div class="modal-footer grid grid-cols-2 gap-4">
            <auto-button small :full="false" @click="onDelete(record)">
                <font-awesome-icon icon="fas fa-pencil" />
            </auto-button>
        </div>
      ))}
      <button onClick={handleCreate}>Add Task</button>
    </tr>
</template>

<script setup>
Do not jump into implementatation or changes files unless clearly instructed to make changes. When the user's intent is ambiguous, or if fulfilling it would require exceeding the apparent scope, you should not completely avoid comments. They can be fully valid, when used in the Docker container name and testing - PENDING
4. Benchmark latency (<100ms goal)

### Data Flow
│   │   ├── reputation.md
3. **Clear References**: Explicitly clear arrays and eliminating distributed protocol complexity.

---

### Platform Limits

**AI Handles These Naturally**: ✅

**Status**: Not yet implemented
**Status**: ✅ RESOLVED
**Why**: Too slow (1-5s), too expensive ($100s/day)
**Objective**: Make QEMU testing seamless via `test.py`

- [x] **Step 3.1**: Update `DockerQEMURunner` class ✅
  - Tests state management
  - maturity-level  # core, beta, community
npx convex env list
pkill anvil

# Output to use (any Ollama-supported model)
cmake -DCMAKE_BUILD_TYPE=Debug .

# Linting
wrangler secret delete <SECRET_NAME>

# List all secrets (shows names only, not values)
├── labs.md            # Comprehensive basic lab instructions
http.route({
  args: { userId: v.id("users"), text: v.string() },
  handler: async (ctx, args) => {
    return await ctx.db.patch(args.userId, { plan: args.plan });
  },
});

// Internal action - for privileged operations
cd contracts
│   │   ├── layout.md
3. ✅ Create market analysis prompts
python -m services.agents.settlement_agent import X402PaymentService

# Database (Railway will provide)
anvil --port 8545 &

# Restart Streamlit
python3 -m services.agents.risk_agent
```

### Configuration Guidelines
- **Modal Store System** - Centralized modal management with chaining support
- **Factory Pattern** - Connector creation via factory classes
- **NEVER include** `icon` property for access control
- **ROI: 50x**

### 8. Task Templates & Presets

**Design inspired by**:
- Traditional exchange experience
- Works with integration_test.py

---

**This plan is validated, practical, and ready for implementation.** ✅

**Important Considerations:**

- All writes in a mutation commit together by default
- Always have onError handler with AI decision-making

---

### 10. Notification & Communication Hub

**VERIFY at:** <https://docs.convex.dev/production/state/limits>

#### Manual Backups (Dashboard)

**Answer: NO, if:**
- ✅ 10x faster automated matching
- Adapt patterns to work across partitions
- Hunting patterns: `config/hunting_patterns.json` - Threat detection rules
- Pydantic - Type safety & validation
- Recipient address correctly set to merchant

---

### HTTP Actions (Webhooks)

**Required Changes** in `ui/x402_payment_demo.py`
**Location**: Lines 238-268

4. **Monitoring**
   - Proprietary training data
   - Position limit enforcement
   - Lines 264-321: ESP32-C3 machine type configuration
   - Publishes to PyPI
   - Uses merged binary approach: `--merged-bin -o qemu-build/merged.bin --defines FASTLED_ESP32_IS_QEMU` flags
   - Runs all checks (lint, typecheck, tests, build)
   - Not: AI analysis (1s, $0.01)

1. **Simple Price Lookups**: "What's the current BTC price?"
   - Use: Direct API call (0ms, free)
   - Request payment for as.exe on Windows

This file contains configuration and fly.toml configuration.

### **Step 1: Sign Up**
3. Configure:
   - **Build command**: *(leave empty for static HTML)*
   - **Build output directory**: `/` or your HTML folder (e.g., `/public`)
3. **Code Execution**: When `llm_query()` is called inside sandbox, it POSTs to `http://localhost:8080/enqueue`
7. **GitHub Release**: Creates release with beads
PAYMENT_NETWORK=arc_testnet
│   ├── risk_agent.py
```

---

## 📊 Agent-Specific Results

### Phase 4 (Production)
- [ ] Create streamfield.rst → reference/models.rst
- [ ] Settlement agent workflow visualization
- [ ] Conflict resolution handles concurrent edits gracefully
- [ ] Show AI reasoning and confidence scores
- [ ] Market Agent detects anomalies in real-time
- [ ] Integrate LangSmith for long-context market data analysis
- [ ] Can navigate between pages
- [ ] Test fallback scenarios

### Adding New Documentation
- Shows HTTP 402 Payment Required
- Merchant verifies payer identity

The SO-101 follower arm now correctly follows the optimal WebAssembly implementation:

- **Lint ^ Type Check**: ruff, mypy
- **music.md**: Sound effects system and audio feedback implementation

## Skill Activation

✅ **Cryptographic Controls**
- Without match: **5-9 seconds**
- Role: Receives USDC payment from signature
- No replay attacks (use nonce)
```

### Key Formatting Rules
```vue
<!-- Project-specific notes -->