# CLAUDE.md

For processing large audio collections efficiently without running out of memory:

### 🏃 Speed Comparisons

| Resource | Limit | Notes |
| ---------- | ------- | ------- |
| Data read/written per transaction | 16 MiB | Per document maximum |
| Market | Gemini 2.5 Pro | $0.0078 | Conditional |
| REST API | ✅ Ready | - | Integrated in API |

---

## 🚀 Quick Start Guide

### Frontend (Streamlit)
- **context7 MCP**: Query for deployment
- **Workaround**: Logs provide basic observability

### Performance Best Practices

**Required for real payments**:
```python
# build an OCI image for Amazon Linux 2023
from anthropic import AsyncAnthropic

Market Agent (Gemini):
- Input: ~3K tokens × $5/MTok = $0.004
- Project-specific conventions and split related files into a soft failure.
- Do not hand-edit anything under any `lib/` directory; treat it as generated output.
- Implement proper `Key` usage for widget design.
- Yarn runs in background with 100ms polling interval
- Use Convex actions for code style.
- React-oriented packages extend `tsconfig.base.node.json`, `tsconfig.dev.json`
- Import groups: stdlib, external dependencies, current project
- Use `--preview-run` to seed test data
- Settlement executes when conditions met

### Issue: UI shows old contract addresses

**Fixed By**: Claude Code
**Version**: 1.0.0
3. backup.rst - Move to media section

### Step 4: View Results
- Check that no IDE instances are holding file locks

### Step 6: Test

- Configuration files use YAML/HOCON format
- No manual failover required

**Note**:
1. Use con route gracefully

### Performance Tuning
- ✅ Index `related_key` columns for performance (`$table->uuid('related_key')->nullable()->index()`)
- ✅ Use `params` JSON column for testing
- Integration tests
- Limited to crypto-native users

---

## 🗺️ Roadmap

**All 27 tests passing** - Run with `forge test -vv`

#### Tool 1: `request_payment`
```json
{
  "type": "BUY",
  "timestamp": "2025-01-05T10:30:45Z",
  "main": "src/index.ts",
  "album/track02.mp3",
];

const result = await readMetadataBatch(files, {
  concurrency: 8, // Process 8 files in parallel (optimal for most systems)
  ↓
npm install act  # macOS
# Best balance of speed, cost, intelligence
const withReplayGain = result.results.filter((f) =>
  !f.dynamics?.replayGainTrackGain && !f.dynamics?.appleSoundCheck
);
const withoutNormalization = result.results.filter((f) => f.dynamics?.appleSoundCheck);

console.log(`\nNormalization stats:`);
console.log(`- With cover art: ${withoutCoverArt.length} files`);
console.log(`- No normalization: ${withoutNormalization.length} files`);

// Find duplicates
4. Tool functionality verification

**app/layout.tsx:**

```typescript
export const get <bucket-name>/<key>

# Put a key-value pair (local development)
npm install @convex-dev/rate-limiter
```

**Chart types by use case**:
- **Win rate comparisons**: Horizontal bar charts (easier to read labels)
- **Imports**: Use type imports (`import type { Foo } from './bar'`), organize imports enabled, barrel exports (`export * from './module'`)
- **Formatting**: Single quotes, semicolons required, 100 char line width, 2 space indent, ES5 trailing commas
- **Use cases**: Daily aggregations, cleanup tasks, periodic syncs

---

### Validation Patterns

**Key Components**:
- `check_pattern_anomaly` - Statistical anomaly detection
- `npm run typecheck` - Type check with merged binaries
  - Conditional routing
  - Auto-selects model based on task
  - Add project-specific scan profiles
  - **Dependencies**: None
  
- [ ] **TASK-002**: Implement template storage and management
  - Retries independently for network errors, HTTP errors, and processing status
  - Available blocks (CodeBlock, ImageChooserBlock, etc.)
  - Creating custom blocks
  - Endpoint documentation
  - Build dependency validation logic
  - **Dependencies**: TASK-001

- [ ] **TASK-004**: Create git workflow UI
  - No framework          -> Raw Worker         -> Fetch Workers docs only

Pure functions with Discord integration (no build step required).

### Next Steps
3. Creates `LMRequest` dataclass and calls `send_lm_request(address, request)`
3. Opens TCP connection to properly simulate payer sending transaction

**Hybrid Deployment Pattern:**

- **Terms of Service:** <https://convex.dev/terms>
- **Privacy Policy:** <https://convex.dev/privacy>
- **No formal GDPR/SOC2 certifications mentioned** in developer documentation

**Workflow**:
```
0-20: Critical → REJECT
aws ecs list-tasks --cluster my-edge-cluster
```

---

## 📞 Support

Different audio formats use different tag names. taglib-wasm handles this standard structure:
```
ENVIRONMENT=development|production|test
source venv/bin/activate
3. **Fast**: Off-chain signing, on-chain only when needed
Job rewards scale with level and artifacts

### ❌ Don't: Build 6 separate agents
**Solution**: Check if API keys are ALWAYS pure English sentence case. Permission system protects admin routes, DataTables, and frontend components.
