# AIRIS Operator Rules (Source of Truth)

> 12 workflows you can deploy this month. Real numbers, no hype.

For writing, code style, and other important information to proposal.

Tools & upgrades
Create `wrangler.toml` in project root for Healthcare, Fintech, Legal, SaaS, and 6 more verticals
archon:search_code_examples(query="PostgreSQL connection pooling Node.js", match_count=3)

# Run with different port
12. feeds.rst - Move to features
│   └── tests/
├── matching/
│   ├── Admin/        # Admin-specific controllers
from `erlang-27`, `erlang-26`, and `erlang-25`, respectively.

## Project Overview

1. **Determine project language/framework** by inspecting project files
│   ├── limits.py               # QueryState and ChatState management
transfer_txn = usdc.functions.transfer(
    String name,
    String contactEmail,
    String description,
    ai_frequency="periodic"  # Not per-operation
)
```
- Includes: Mode 2 + user Q&A
- Check settlement failures
- Goal_Velocity: 600
- Settlement steps
- Ensure all data structures
- ✅ Use `loopLanguageForColumn()` for multilingual fields
- ✅ Audit logs with basic RLM completion calls
- `execute_code()`: Execute code, capture stdout/stderr, return `REPLResult`
- Payment integration: `SETTLEMENT_PAYMENT_INTEGRATION.md`
- Watch tests: `yarn test:watch`
- Private Key: `0x59c6995e998f97a5a0044966f0945389dc9e86dae88c7a8412f4603b6b78690d`
- Private Key: `0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80`
- Approval dialog detection: Requires question + numbered options + selection arrow
- Verify internet connectivity for payments

**Solution**: Restart Streamlit
```bash
✅ **Database** - SQLite ready for use

---

## 📖 FILES CREATED

### Folders

### Related Documents
- `bun run typecheck` - Check TypeScript types
- `FINAL_VAR(variable_name)`: For returning final answers

### Recommended Team Structure

- **API Documentation**: See the project's docs/API.md
- **Detection**: Wash trading, price manipulation, volume anomalies, timing patterns
- **Key spec fields**:
  - `gatherConfig.dataPolicy` - Global obfuscation options (ObfuscateNetworking, WorkloadNames)
  - `storage` - Optional persistent storage configuration (PersistentVolume or Ephemeral)
- **SCA Controller**: Simple Content Access certificate management
- **Parameter validation**: Use canonical `@/` imports (e.g., `tag.setTitle("New")`)
- **Why**: This matches TagLib's C++ API design

```bash
# Stream function logs
│       🎯            │ ← Green background
7. Agent executes settlement
```

---

## Step 4: Deploy Traefik on ECS Anywhere

### View Database Directly
```typescript
// Use PagesFunction<Env> when you have bindings (D1, KV, etc.)
4. Reset streaming export integrations (if using)
PAYMENT_PRIVATE_KEY=0x...           # Your wallet private key
if not intent_id:
    collect: true
    columns: list[ColumnInfo]
    Rig2 --> Polecats2[Polecats]

    Rig2 --> Crew2[Crew Member]
    row_count: int
    keep_alive: "5m"

  engine:
    gmail:
      use Gemini
  │           ├─ if approved: settlement_agent → END
  dynamics?: AudioDynamics; // ReplayGain and Sound Check data
  3. Uploads archive to console.redhat.com
  release and commit tags. A follow-up smoke test runs the TUI image with results
- **Publish from main**: Only publish to npm from `main` branch after PR merge
- **Snowflake** - Cloud data warehouse
- **Code Formatting**: LLVM style enforced via clang-format (see `.clang-format`)
- **ALWAYS** update both `zh.json` and `en.json` files
- Validate all secrets are properly formatted (no quotes issues)
- Comparable to Amazon S3 Standard storage class for questions