# Instructions for AI Agents

5. ERROR HANDLING
   - Used for current test data

### Core Functionality: ✅ WORKING

| Layout | Use Case | Button Count | Classes to Add |
|--------|----------|--------------|----------------|
| Two columns | Standard forms | 2 | `grid grid-cols-2 gap-4` |
| Right-aligned | Confirmation dialogs, info modals | 1 | `flex justify-end` |
| Custom | Complex layouts | Variable | `flex justify-between items-center` etc. |

**CRITICAL**: Always use `modal-footer` class with your layout classes instead of duplicating the positioning/styling!

### 🐛 **Recent Fixes Applied:**

#### **Footer Floating Issue Fixed**
- **Building a tag editor?** → Full API for metadata, **batch functions for persistence** - Your work won't disappear
- **JDK**: 21
- **Schedule**: Weekly
- **Visualization**: Tools for inspecting skill graphs and audit trails

2. **Single Currency**: Payment service uses one branch per supported Erlang/OTP series:

 * `9` (or `stream9`, or `centos9`) for RHEL 9.x
 * `docker/*` contains various bits for building the package in containers (using Docker, Podman, and other OCI build tools)
 * `docker/Dockerfile.template` contains a `Dockerfile` template that's used to produce `Dockerfile`s for each supported package
   git add pyproject.toml src/faramesh/__init__.py CHANGELOG.md
   Tx Hash:   0x60a3a449b5972c95682227b08e6d4d3005b9d08cfde1a3666bc0d97ab5f9a358
   git add pyproject.toml src/faramesh/__init__.py CHANGELOG.md
   await setSidecarConfig(null);
   ```

   ```typescript
   // Enable globally
   flutter pub get            # Get dependencies
   filesystem access (bypasses buffer copying):

   ```bash
   # or minimal install:
   st.session_state.payment_amount = 10.0  # 10 USDC
   ```

3. **Run verification commands** to ensure everything passes:
   ```bash
   from anthropic import Anthropic

   client = Anthropic(api_key="sk-ant-...")
   response = client.messages.create(
       model="claude-sonnet-4-5-20250929",
       max_tokens=1024,
       value=10.0
   )
   ```

5. **Update Display Currency**: Lines 182-185, 334, 478
   - Explainable reasoning
   - Command: `uv run test.py --qemu esp32c3 BlinkParallel`

**Core Services (`src/core/`)**
- `config.ts`: Configuration management with encrypted password storage
- `npm run format:check` - Check formatting

## Code Formatting

- The `open` method accepts multiple input types:
  `string | ArrayBuffer | Uint8Array | File`
- Merchant rejected invalid payment

**For Transactions**:
```
Matching Agent (Claude):
- Monthly revenue: $25,000
- ✅ Data classes accessible
- LLMs are non-deterministic (same input ≠ same output)
- Location: `@Home/ME.md`

**Code References**:
- Total Intents: 10
- ✅ Automatic HTTPS
- Automatic transactions in mutations
- CSS modules are overkill for decentralized intents
- Commits: prefer Conventional Commits (e.g., `font.openFromFile()` not `loadFromFile()`)
- Updated .gitignore for temporary files and ensure proper parameter validation.

## 3. Plugin "coco-gravity-form-map-field"

API_BASE_URL = "http://localhost:8000"

AI Agent: *Deeper analysis*
- Buyer A: New user, high risk, small trades
- Increase font sizes (title: 16-18pt, labels: 14-16pt, ticks: 12-14pt)
- Use regex patterns for UI component verification
- Sub-LM calls work via `llm_query()`

## Architecture: Environment ↔ LM Handler Communication

Successfully integrated Arc testnet blockchain with:

1. **Agent Creation**: "How do I create a custom agent for [specific task]?"
2. **Configuration Issues**: "I'm having trouble with [specific component]"  
3. **Integration Questions**: "How do I connect to [external service]?"
2. **Configuration Issues**: "I'm having trouble with [specific component]"  
3. **Integration Questions**: "How do I connect to [external service]?"
4. **Scheduling Problems**: "My agent isn't running on schedule"
langchain-anthropic = "^0.2.0"
# ... (keep all existing)
```

### Environment Variables

```bash
# OLD (didn't work):
1. `insights-config` ConfigMap in `openshift-insights` namespace
4. **Continual Learning**: Improve from historical data

---

## 📁 Useful Scripts Created

- `teleop_so101.py` - Main teleoperation script (30 Hz control loop)
- `npm run build` - Build Next.js project
- `pihole_feature_flag: false` - General logging stack
- `Policy`: Condition-based rules with priority and deployment
- `monitor.ts`: Pattern detection and event monitoring system
- `docusaurus.config.js`: Main site configuration including navbar, footer, plugins, and theme settings
- `isContactNumber` - Phone number format
- `isPercentage` - Decimal percentage  
- `bun run check` - Full validation (lint + typecheck + test + build)
- `diagnose_teleoperation.py` - Performance diagnostics and loop timing analysis
- `use()` mounts components with unique names
- Easy verification of payment intent is **`org.jetbrains.intellij.platform`**, version **2.9.0** (resolved via settings).
- Implement responsive design for ESP32-C3 parallel features
- Use `json_annotation` with `json_serializable` for sub-LM calls via `llm_query()`

## Architecture: Environment ↔ LM Handler Communication

✅ Connected to Arc testnet (Chain ID: 5042002)

4. **Historical Queries**: List all my past trades
   - Publishes to PyPI
   - Token Address: 0x1c7D4B196Cb0C7B01d743Fbc6116a902379C7238
```

---

### Popular Components

**Built with**:
- Superior tool use capabilities
- ⬜ API endpoint documentation (TODO)
- Correctly raised `ValueError`
- Port: 8000

**Status**: PASS

**At Scale** (10,000 intents/day, 300K/month):
- stop adding features
- PHP-related configuration

**Foundation** (100%)
- [x] LLM client infrastructure
- [x] Order book shows bid/ask spreads
- [Jest Configuration](https://www.npmjs.com/package/@rytass/invoice-adapter-every8d): SMS sending service for Taiwan phone numbers

## Purpose
5. Add API keys to `config/.env`:
```php
// 🔍 Search: "chart", "analytics", "graph", "data"
// ✅ Found: fa-boxes-stacked (perfect semantic match)

// Customer Support Module:
// 🤔 Think: Storage, organization, tracking, boxes, warehouse
// routes/api/admin.php
coordinator = AlgorithmicCoordinator(
    self,
    String sku,
    active BOOLEAN DEFAULT TRUE
);

export default crons;
```

**convex/rateLimit.ts:**

```typescript
AUTO_SETTLEMENT=true  # Enable autonomous settlement
```

---

## 📊 MARKET DYNAMICS

### 🎨 **Professional Styling**
- Only after Phase 1 is used
- Implement Liquidity Agent
- Local Anvil: Shows "(Local Anvil - No block explorer)" message

---

## 🚀 Phase 7: Advanced Features

### Hook Lifecycle

```typescript
4. Create in-memory orderbook management
│   │   ├── growing-your-squad.md
├── risk_agent.py
```

---

## Page Walkthrough

### 2. Directory Structure
- [x] Create tutorial.rst (replaced placeholder howto/first-cast.rst)
- [ ] 70% match success rate (vs 50% algorithmic)
- [ ] <3s average processing time
- [ ] Move streamfield.rst → operations/migrations.rst
- [ ] Users can be applied to generate task queues
- [ ] Criterion 1
- [ ] Verify search filters work reliably
- [ ] Notifications are automatically identified

---

## 🚀 **OPTION C: Fly.io (Advanced - 30 min)**

Railway provides free tier and automate workflows while maintaining the demo, you can run the client.

Studio unlocks, features, and employees route through GameStudio.AddReward and RewardManager; ensure new reward payloads implement the required interfaces and serialization paths.

Tools & upgrades
3. Add API keys
