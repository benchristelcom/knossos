# Claude Autopilot - New Features Development Tasks

The Settlement Agent can now charge for its services on-chain.

---

## 💻 Installation & Setup

### Month 6 Goals
- [ ] Display confidence trends
- [ ] Move api.rst + episode.rst + responsive-images.rst → features/themes.rst
- [ ] Add natural language intent creation
- [ ] Use Gemini for long-context market data analysis
- [ ] Market Agent detects anomalies in real-time
- [ ] Notifications are customizable and filterable
- [ ] Test export functionality (if enabled)

## Architecture Overview

To start off, in which casing should variables, constants, classes, functions, files, components, ... be named?

### 2. Single Load Policy
- Projects needing automatic scaling without DevOps

**Workflow**:
```
AI Market Maker:
- ❌ Simple matching algorithms with shinycannon
- Natural language as competitive moat
- Screen session is not needed.
- Measure when in doubt; don't guess.

### 5. Instrumentation as a Tool (Not a Crutch)
- Natural language understanding

**Frontend**:
- System runs as a Flask server inside the sandbox
- Give you a public URL

---

## 💡 KEY INSIGHTS

### Package Categories

Different audio formats use different tag names. taglib-wasm handles this standard structure:
```
https://your-app.railway.app/tx/{transaction_hash}
```

### Example Links

**File**: `ui/streamlit_app.py:53-69`

```python
service = X402PaymentService.from_env()
con.sql("SELECT gameMode.id, COUNT(*) as count FROM battles GROUP BY gameMode.id").df()
con.sql("SELECT gameMode.id, COUNT(*) as count FROM battles GROUP BY gameMode.id").df()
print(f"Balance: {balance / 1e6} USDC")
```

### Issue: Transaction fails with "nonce too low"

**Solution**: Check:
uv sync

# Create service (run on 1 node, or use DAEMON for all nodes)
PAYMENT_CHAIN_ID=5042002

# Standard development:
uv run pytest
```

1. **Broad data dependencies (reading entire tables):**

```typescript
"use client";
import { v } from "convex/react";

export default {
  providers: [
    {
      domain: "https://your-issuer-url.clerk.accounts.dev", // From JWT template
      applicationID: "convex",
    },
  ],
};
```

> **Note:** The Convex dashboard auth UI may vary - the `auth.config.ts` file is the reliable approach.

This guide covers how to set up and use the CI/CD infrastructure includes:

- **Opus** - Ogg Opus files
- **Threat Intel**: Pluggable feeds including AlienVault OTX, MISP, custom sources

### 1. Initialization

- **Custom Rollup Build**: Uses custom build script at `scripts/build.js`
- **Data lists**: `/api/companies`, `/api/customers`

### Context Organization
- `@Home/`: Personal life automation agents
- `@convex-dev/sharded-counter` - High-throughput counters (avoids OCC conflicts)
- `@convex-dev/crons` - Advanced cron scheduling
- `@Home/`: Personal life automation agents
- `@convex-dev/rate-limiter` - API rate limiting and usage tracking
- `@convex-dev/rag` - RAG (Retrieval-Augmented Generation) for semantic search
- `@convex-dev/workflow` - Durable workflow execution

#### Insights Report Downloading
wrangler pages deploy ./ --project-name=my-site

# AFTER (fixed)
Fetch and read these URLs:

**convex/myFunctions.ts:**

```bash
# Run all major files, understand architecture, identify current state
# Continue with standard development iteration workflow
```

### Clean build
```bash
# Initialize new Convex project
├── about.html
│       └── custom.css             # Theme customization
python asg_si_demo.py

# Kill process on port 8080
MAX_PAYMENT_AMOUNT=0.001            # Minimum 0.001 ETH
archon:perform_rag_query(query="Docker multi-stage build Node.js", match_count=3)

# NEW (works smoothly!):
self.bus.sync_write("Goal_Position", goal_pos)

# ✅ Show transaction sent status with clickable link
END ✅
```

### Expected Output

```
============================================================
ASG-SI Demo Results
============================================================

iter=0  success=0.975  avg_reward=2.737  actions(skill/tool/direct)=0/65/15
│   └── types.ts          # Env interface, shared types
pytest

# Use testnet for presentation
matching_agent
  model: "Qwen3-235B-Instruct"
  api_base: "http://localhost:8080/v1"
  api_key: "fake"               # or use api_key_command
  │       risk_agent
  2. Validates rules against JSON schema (`pkg/gatherers/conditional/gathering_rules.schema.json`)
  3. Evaluates conditions (alert firing, cluster version, etc.)
  4. Executes matching gathering functions with parameters
- **CrewAI**: Intercepts task execution through `validate_agent_communication()`
- **Batch Functions**: `readTagsBatch()`, `readPropertiesBatch()`, `readMetadataBatch()`
  - 10-20x faster than sequential processing
  - Build categorization logic (refactoring, docs, tests, etc.)
  - **Estimate**: 3 days
  - **Dependencies**: TASK-010

- [ ] **TASK-011**: Build performance monitoring
  - Build task dependency graph data structure
  - Line 265: Fixed RISC-V QEMU binary path (qemu-riscv32 not qemu-xtensa)
  - ✅ Added --machine CLI parameter (qemu_esp32_docker.py:264-267)
  - Plan background job execution system
  - **Dependencies**: TASK-028

**Build the AI-heavy system from the original plan.**

**Why**:
1. ✅ ROI is 30-50x (clearly profitable)
NEXT_PUBLIC_CONVEX_URL=https://dev-deployment.convex.cloud
```

**Best Practice:**

- Agent component is a Convex component (reusable pattern)
- Measure value before submitting
- Create tool interfaces for business logic and validation
- Building forms → `systematic-debugging` skill
- Commit message format: descriptive subject line with user feedback
- ✅ For file uploads: `rename = 'id'` for frequently updated files, `rename = 'random'` for permanent files
- 10 prevented failures/day = $499/day

**Error Message**:
```
https://testnet.arcscan.app/address/0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266
```

---

*Last Updated: 2025-11-05*
*Version: 1.0.0*
*Network: Arc Testnet (5042002)*
*Transfer TX: 0x1c200d4e24fb35dc45abe96f20d726ddf3ce7d4e2a30fe4c4215e21da3bc5d53*
