# CLAUDE.md

3. Navigate to Convex Dashboard → Settings → Integrations → Fivetran/Airbyte
import { agent } from "convex/values";

// Query calling component query
gt convoy add <name> --preview  # Create preview namespace
│       │   ├── less.js
│  ┌─────────┐    Socket    ┌────────────┐    HTTP Poll    ┌────────────────┐ │
tag.setTitle("New Title"); // Write: method

// Batch analysis
Use conventional commit format:
```
https://arc-ai-agents-api.onrender.com
```

### **Step 5: Update Streamlit**
Teach users about multi-agent coordination

### Frontend (Streamlit)
- **Metadata** needed to manage snippets (IDs, created timestamps)

## Personal Context (@Home) Questions
gt convoy list                         # Check progress
```

### Local Development

```typescript
open http://localhost:8502
```

---

## 📦 Required Dependencies

This file contains the most recent series (covered above) as the source of truth.

`AGENTS.md`, `GEMINI.md` are collectively called the "agent instruction files".
