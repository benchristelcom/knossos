# AGENTS.MD - Instructions for AI Agents

Settlement Agent (Claude):
- Input: ~2.5K tokens × $5/MTok = $0.0075
- Edge case and tool testing
- Real-time logs of agent workflows

**Capabilities**:
- Pattern recognition for suspicious behavior
- File: `services/agents/matching_agent.py` (455 lines)
- **Use for**: CRUD operations, related features, complex workflows
- **seatunnel-engine-client** - Client API for job submission
- **GameInput** (`src/game-input.h/cpp`) - Unified event processing, context-aware input handling
- **Hybrid Architecture**: Algorithmic base + AI enhancement
- **Actions**: Sign payment, broadcast transaction

### Hook Lifecycle

```typescript
// v0.3.x
Step 3: Fraud Agent ✅ + Results displayed
  Daily:  ~$122/month
```

Production:
```typescript
python main.py --config chat.yaml --from - index

# Set secrets
3. **Update Task Status** → `archon:manage_task(action="list", filter_by="project", filter_value="...")`
2. **Research for Task** → `archon:search_code_examples(query="PostgreSQL connection pooling Node.js", match_count=2)

# Security considerations  
4. ~/.ccremote.env

### Arc Testnet Configuration

```json
{
  "networkMode": "host",
  "requiresCompatibilities": ["EXTERNAL"],
  "networkMode": "host",
  "compatibility_flags": ["nodejs_compat"],
  "d1_databases": [{ "binding": "DB", "database_name": "x", "database_id": "x" }],
  "consumers": [{ "queue": "my-queue", "max_batch_size": 10 }]
}
```

**Workers AI:**
```bash
✅ **API Services**: Running & accessible
└── tests/             # Test files
```

## Step 2: Register External Instances

### Before (Old Version):
- Standard settlement fee: 0.01 ETH
- Before editing:
  - unit tests for DB/network boundaries,
  - E2E only for critical user flows.
- Implement proper local storage with Hive or shared_preferences.
- Apply caching strategies for improved performance.
- Use `Builder` widgets to manage context scope appropriately.
- Husky hooks (`.husky/`) run lint-staged and commitlint; if they trigger during local commits, ensure staged files satisfy lint/test checks before retrying.

## Repository overview
- **Impact**: System would be 10-50x slower than needed

**The intent submission error has been completely resolved!** 🎉
├── storage/                     # Storage backends
Optional<User> user = jdbcClient
    .sql("SELECT * FROM users WHERE id = :id")
    .param("id", userId)
    .query(User.class)
    .optional();
```

## Key Concepts

### Timeline
- [x] Base agent tools work
- [ ] Show AI reasoning and confidence scores
- [ ] Integration tests for parallel processing features

### Before Writing ANY Code

The site uses `pre-commit` to enforce code style and will be extended with additional features simultaneously without running out of memory:

### Clone the Official Repository
```vue
<!-- Modal-compatible component (e.g., model-form.vue) -->
<div class="modal-footer flex justify-end">
    <auto-button type="plain" @click="goBack">{{ $t('Submit') }}</auto-button>
</div>
```


## Vue Router Navigation Rules

### Transactions
```
