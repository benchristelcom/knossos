# AGENTS.md - Flutter Project Contributor Guide

Let me know when you want to start implementing Phase 1!
wrangler d1 execute <db> --local --file=./schema.sql  # PRODUCTION (careful!)
3. ❌ `Acceleration` - HOW SMOOTHLY to accelerate (was MISSING!)

**Comparison**:
- ✅ Production-ready codebase
- ✅ For file uploads: `rename = 'id'` for frequently updated files, `rename = 'random'` for permanent files
- Monitor escrow functionality

#### Rate Limiter Example

```java
/**
 * Integration tests with proper test isolation strategy.
 * 
 * Testing Strategy:
 * - Uses @DirtiesContext to refresh the Spring context after each test method
 * - Manual cleanup only: Less robust isolation between tests
 */
@SpringBootTest
@dataclass
npx convex docs
# - Useful for debugging

# Start the remote branch after cleanup
3. **Game Theory Optimization**: Nash equilibrium matching
│   ├── plus/                      # Premium features documentation
source venv/bin/activate && python -m uvicorn services.api:app --host 0.0.0.0 --port 8000 &

# Restart Streamlit
import { expect, test } from "@jest/globals";
import { TagLib } from "taglib-wasm/simple";

// CommonJS (older Node.js projects)
├── en.json              # English translations (only when different)
2. **Update CHANGELOG.md** with every release
wrangler d1 list                 # List active instances
```

## Common Workflows

### Why LangGraph?

**Answer: Settlement Agent**

