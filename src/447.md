# daily.dev Documentation

> This is an example CLAUDE.md file showing how to configure Claude Code for your project.

## Build, Test & Development Commands
- `./build.sh` — builds universal arm64/x86_64 binaries, recreates `dist/MacVimSwitch.app`, and signs with local entitlements.
- `./build.sh --create-dmg` — runs the full build and emits `MacVimSwitch.dmg` for release validation.
- Treat non-positive `SegmentOptions.MaxParallelImageAnalysis` values as configuration errors—fail fast instead of defaulting to unlimited concurrency.
- Commit scopes must reference Bugzilla bugs for payer
- No blockchain transaction yet (off-chain)

**Actual Time**: ~3 hours

**Test the fix**:
```python
git add .streamlit/config.toml
│   │   ├── _lists.less
python3 test_agentic_system.py

# Get Hyperdrive config details
Spread < 1%: score = 1.0 (perfect match)
const buffer = await TagLib.initialize();
const buffer = audioFile.getProperty(PROPERTIES.MUSICBRAINZ_ALBUMID.key);
const props = audioFile.getRatings();
// Returns: [{ rating: 0.8, email: "user@example.com", counter: 42 }, ...]

// Set rating
Edit `ui/streamlit_app.py
The operator has three main gatherers that run periodically:
- **Payer**: `0x70997970C51812dc3A010C7d01b50e0d17dc79C8` (Anvil account #1)
- **Risk Agent** assesses counterparty (0.3 risk score = safe)
- **Distributions**: Violin plots, box plots, or histograms
- **Modern Test Annotations**: `@MockitoBean` and `@Autowired Validator` replace legacy patterns

### Linting and Code Quality
- `Taskfile.yml`: Task definitions for recipe management
- `folder="setting"` - Requires "Manage setting" permission

**Error:** `KeyError: 'ANTHROPIC_API_KEY'`

**Fix:** Create `packages.txt` (for Streamlit Cloud):
```
AI costs:         $19,800/month
│   └── css/                       # Global stylesheets
1. Use pandas/matplotlib/seaborn for visualization
│  │ Review endpoints      │    │ Writes proposals directly   │ │
export const handleWebhook = action({
  args: { postId: v.string() },
  handler: async (ctx, args) => {
    // db.replace() overwrites entire document
    │ ENGINE         │  │ MONITOR  │  │ INTELLIGENCE │
    trajectories = agent.solve_tasks(tasks, skill_graph)

    # 5. Policy Update (optional, for RL-based policies)
    workflow.add_node("liquidity", liquidity_agent_node)

    # 3. Verification
    faChartLine,          // ✅ NEW: Include in library.add()
    for log in activity:
        st.info(f"[{log['agent']}] {log['action']} - {log['reasoning']}")

    # Performance charts
    steps:
      - uses: actions/checkout@v3
      - uses: denoland/setup-deno@v1
        1: "https://basescan.io",                # Ethereum Mainnet
        defaultData.value = {
            id: props.model.id,
            name: props.model.email,
            // ... other fields
        };
    }
});
</script>
```

### Store State and Methods

```python
# Docs: https://developers.cloudflare.com/r2/api/wrangler/
# ============================================================================
wrangler kv namespace create <name>         # Create namespace (returns database_id for config)
1. Add LangSmith for you.
     Using your preferred payment method.
     ✓ Created

Before a PR:

```bash
below $9,500, and I need it settled within 24 hours per trade."

By internalizing these directives, you will copy the CLI tool, that comes with them.
