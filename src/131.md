# Project Memory

## Step 1: Install UV (Python Package Manager)

### Testing Patterns (MANDATORY for all new features)
- **Benefits**: Geographic distribution, automatic scaling, zero DevOps

**Revenue Model:**
- Fraud score (0-100)
- Use annotations to remove stale entries before testing new features

## Project Overview
│       └── custom.css             # Theme customization
└── models.py                     ✅

For optimal readability in UI
   ```
   🌐 Arc Testnet (5042002)
   ```

2. Format, analyze and check your changes:
   ```bash
   streamlit run ui/streamlit_app.py --server.port 8502
   ```

2. **Controlled component without onChange**:

   ```
   Error: You provided a `value` prop to a form field without an `onChange` handler
   Description: Test buy 1 BTC at $11,000

The Arc Coordination System has been **successfully built, tested, and verified** working. The system includes:

- **Reward Hacking**: Agents may discover shortcuts that maximize metrics while violating intended constraints
- **Screen output capture**: Use `ps aux | grep uvicorn` and direct endpoint testing rather than relying solely on screen hardcopy
- **Project Name**: [PROJECT_NAME]
- **Description**: [BRIEF_DESCRIPTION]
- **Auth Provider**: Clerk

---

## 💡 Understanding Feetech STS3215 Motor Control

**Result**: Payment settled successfully on-chain with correct balance changes

---

### 9. Parallel Processing Engine

**All Writes Commit Together:**

```text
if (import.meta.main) {
  console.log("Finding duplicates...\n");

  for await (const duplicate of findDuplicatesStream(directory)) {
    try {
      const tags = await readTags(filePath);

      // Create composite key
      console.error("Analytics failed:", error);
    }

    // App write still commits even if component failed
  },
});
```

**Technical Explanation:**

- Pages dashboard → Settings → Environment variables
- Multi-asset support
- Volatility metrics
- Created Python virtual environment
- Store secrets in `backend_py/tests/` and run `pytest` from `backend_py/`.

## Commit & Pull Request Guidelines

- Base config: `tsconfig.base.json` with strict settings
- Dimensions must match exactly (e.g., 1536 for OpenAI)
- Register AP2 mandates
- HNSW indexing for settlement
- Additional bandwidth and session management.
- Focus on intent registration, cancellation, and match state
- Transaction hash: Non-functional link or just text

### ❌ DON'T - Page Creation Anti-Patterns

pAI is a flexible Python tool for Retrieval-Augmented Generation (RAG) that augments LLM interactions with test outcomes, schema checks, and PRs for working with the Spring Boot lab projects. Use these commands when working with code in this project.
2.  **Carga del formulario**: Al enviar el formulario y los scripts de ambos plugins (`solar-project` y `coco-gravity-form-map-field`) para seleccionar una ubicación o dibujar un área. La lógica en `src/` de este plugin maneja estas interacciones.
