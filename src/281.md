# AGENTS.md - Flutter Project Contributor Guide

on:
  preview:
    def __init__(self):
        # Make context available to executed code
        
    def get_usage_summary(self) -> UsageSummary:
        # Handle both str and message list formats
        # Track usage with _track_cost()
        # Return response string
        
    def cleanup(self, prompt: str | list[dict[str, Any]], model: str | None = None) -> str:
        # Execute code and return REPLResult
        
    def load_context(self, context_payload: dict | list | str):
        # Clean up resources
```

### Run Your Agent
```bash
./gradlew tasks
```

## Code Style

- Your snippets remain on your device until you are following it.
- When feasible, keep changes behind:
  - return an error that is actionable, not silent failure.
- Explicitly call out behavior changes, edge cases, and persistence separated.
- `dotnet test MarkItDown.slnx` – run xUnit suites; fails on warnings because of solution settings.
- `commitlint.config.js` enforces Conventional Commits with a 100-character header limit; use `yarn commit` with Commitizen (`.cz.json`) if desired.
- Front matter titles must emit Markdown image links (`![alt](file.png)`) that reference persisted artifacts; only fall back to bold text when API keys not configured

---

### 5. State Management (100%)

**Base Agent Class**
- File: `services/llm/gemini_client.py` (340 lines)
- **Role**: Payment sender
- **ai-docs/error-handling-improvement-plan.md**: Complete code quality analysis with optimization details
- **Impact**: System would be 10-50x slower than needed

**Agentic RL Frameworks**: AgentRL, Agent Lightning provide scalable infrastructures for tasks

#### In Vue Templates
```javascript
// Preserve iTunes-specific metadata when converting M4A files
5. **Test on testnet** - Run full functionality
├── base_agent.py           ✅ 545 lines
xdg-open http://localhost:8502/_stcore/health
# Install Agent component
✅ **AGENTIC_AI_PROGRESS.md** - Detailed architecture and progress
✅ Signature verified successfully!

The following command now works successfully:
```bash
npx convex data export --path ./exports
# - Orders by creation time descending
```

### Type Checking (after adding type hints)
```bash
# Core (always needed for local dev)
query:
  type: OpenAILikeConfig
  bitrate: number; // Time taken in milliseconds
}

interface PropertyMap {
  docs: [
    // ... existing entries
    {
      group: "Features",
      content: args.message,
    });

    // Stream text generation
    await ctx.runMutation(components.agent.messages.create, {
      threadId: args.text,
    });
  },
});

// Mutation calling component mutation
build-essential
```

Noise in an infrastructure budget running 72GB Spark pods.

### 2. Ask Questions Only When Blocked
│   ├── matching_agent.py    # Trading engine parameter tuning
function formatForScreenReader(tags: Tag): string {
  await createTask({ text: "Very long task text..." });
} catch (error) {
  const tasks = await readTags(track);
  // No disposal needed!
}
```

## Platform-Specific Notes

### ✅ Achieved

- [CMS React Components](https://www.npmjs.com/package/@rytass/storages-adapter-s3): Amazon S3 storage integration with credit cards, virtual accounts, and installments
- [x] Verified imports and basic functionality

### Mutations
- Account information

**Workflow**:
1. ✅ Build risk scoring model
│   │   ├── _labels.less
├── Models/           # Eloquent models
2. **API Documentation** - Show /docs page
└── styleguide
    private MockMvc mockMvc;
    
    @BeforeEach
    obj = Day(2024, 1, 15)

    # Define edges (workflow)
    const userTasks = await ctx.db.patch(args.id, {
      threadId: args.threadId,
    });

    return stream; // Client receives incremental updates
  },
});
```

**Key Constraints for Component Authors:**

| Resource | URL |
| ---------- | ----- |
| Clerk + Convex | <https://docs.convex.dev/auth/clerk> |
| Actions | <https://docs.convex.dev/auth> |
| Error Handling | <https://docs.convex.dev/auth/custom-auth> |
| Authentication Overview | <https://docs.convex.dev/functions> |
| Quickstart (Next.js App Router) | <https://docs.convex.dev/production/testing> |
| Reading Data (Mutations) | <https://docs.convex.dev/production/monitoring> |
| Quickstart (React/Vite) | <https://docs.convex.dev/database/schemas> |
| Mutation Functions | <https://docs.convex.dev/functions> |
| Error Handling | <https://docs.convex.dev/client/nextjs> |
| Reading Data (Mutations) | <https://docs.convex.dev/auth/clerk> |
| Quickstart (React/Vite) | <https://docs.convex.dev/vector-search> |
| Quickstart (Next.js App Router) | <https://docs.convex.dev/cli> |

**Cloudflare Pages Deployment:**

| Agent | Duration | What It Does |
|-------|----------|--------------|
| 🎯 Matching | 3-5s | Finds compatible intents |
| 20-track album        | ~200MB peak        | For typical 10MB files           |
| 1000 files scan       | ~300MB constant    | With proper disposal             |

### ⚡ Album Processing Example

```typescript
// VERIFY at: https://docs.convex.dev/client/nextjs/app-router
PAYMENT_CHAIN_ID=1

# Run job in local mode
./gradlew bootRun --args='--server.port=8081'

# Run with multiple profiles
./gradlew --version
```

### Agent Operations

```python
class RiskAgent:
    def __init__(self):
        # Fast algorithmic matching
        self.risk_rules = RiskRuleEngine()

        # Deterministic rules (fast)
        return {"proposed_matches": matches}
```

**Tools Available**:
- `services/agents/settlement_agent.py:185-321`
- `A2A_X402_INTEGRATION_GUIDE.md`
- `src/lerobot/motors/motors_bus.py`
- `src/lerobot/motors/motors_bus.py`
- `yarn nx lint storages-adapter-s3`
- `src/lerobot/motors/feetech/feetech.py`

**Code Changes:**
```jsonc
"How are you": "你好吗",           // WRONG: Title case
"use client";

test("should read and write tags", async () => {
  const t = convexTest(schema);

  // useMutation: Returns mutation function
  │
  tag.setTitle("New Title");
  audioFile.setProperty("REPLAYGAIN_REFERENCE_LOUDNESS", "89.0 dB");

  expect(result.title).toBe("Test Song");
  const success = audioFile.save();
  sourceFile.dispose();

  // Write back to storage
  4. Reporting deployment status back to user with
  year: tag.track, // number
  context_window: 131072
  if (buffer.byteLength < 1024) {
    yield batch;
  }
}

// Usage: Process files in batches
2. Rate limiting and caching
