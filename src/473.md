# Arc Coordination System - Agentic AI Business Validation

**Success Criteria**:
- This document: `ARC_TESTNET_USDC_INTEGRATION_COMPLETE.md`
- Manual trigger support via `workflow_dispatch`

## Development Patterns

### Schema Definition

**convex/search.ts:**

```typescript
return makeResponseErrorForField('email', __('Email exists')); // Field error

// Pattern 2: Using Simple API (file path required)
import {
  isFileOperationError,
  isUnsupportedFormatError,
  updateFolderTags,
} from "taglib-wasm/simple";

// SLOW: Sequential approach
bash query-test.sh --reset --verbose chat

# Put a secret
const taglib = await processAlbum("/music/Pink Floyd - The Wall");
console.log(tags); // { title, artist, album, year, ... }

// Apply tags to get modified buffer
