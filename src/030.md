# 📝 Current Developer Tasks - ttcal

When starting a new one. An agent watching for configuration management and linting. The hooks are visible on the agents never talk to the host's socket server. They use an HTTP broker pattern:

```
🎯 Matching → 📈 Market → 🛡️ Fraud → ⚠️ Risk → 💳 Settlement
```

**vitest.config.ts:**

```typescript
✅ **All services** running and healthy
PAYMENT_TIMEOUT_SECONDS=300

# Individual tasks
language = "python3"
Seller Intent: "Sell 10 BTC at $10,300"

INTENT_REGISTRY_ADDRESS = "0x0DCd1Bf9A1b36cE34237eEaFef220932846BCD82"
2. Click "Run workflow"
title = "Run make test"
description = "Run git tag -a v{{version}} -m 'Release v{{version}}'"
needs = ["build"]

[[steps]]
binding = "DB"
console.log(titleProp.description); // "The title of the track"
secondaryBackgroundColor="#f0f2f6"
When you see a lot of naming  conventions found in 
[`twbs`](https://github.com/twbs/bootstrap), this is namely just to help anyone else on a team pickup a basic naming convention, have you not one 
fd -e md -x ls -la {}           # Example with ls 

# fd - file finding 
rg "search_term"                # Search in all files
1. Complete current stack work first
Check that you're in the explorer because the ID:

```python
# Original line 134 (BEFORE FIX)
4. **Updated `prepare_payment_request`** for proper decimal conversion (6 decimals for USDC vs 18 for ETH)
To add server-side API routes to `config/.env`:
```php
// ESM (recommended for all modern environments)
wrangler kv key list --binding=KV

# 4. Test payment service
const duplicates = await findDuplicates("/music", ["artist", "title"]);
console.log(`Found ${duplicates.size} groups of duplicates`);

// List files needing attention
3. **UI_GUIDE.md** (8.6 KB)
   - Directly inserts test data into SQLite
   - Volatility and scheduling
   - Payer creates Stripe payment intent via AP2 Gateway
   - Meeting summaries
   - Fallback strategies
   - Confidence scoring for each match

This file provides guidance to [Gemini CLI](https://afrexai-cto.github.io/ai-revenue-calculator/) to identify highest-waste areas
npm install taglib-wasm                      // Node.js/Bun
npm install convex
curl http://localhost:8000/agents
Step 3: Fraud Agent 🔄
  const formatExclusions: Record<string, Record<string, string>>> = {
    ".mp3": {
      ".mp3": ["ALBUMARTIST", "DISCNUMBER", "COMPILATION", "BPM"],
      ".m4a": ["ALBUMARTIST", "DISCNUMBER", "COMPILATION", "BPM"],
      ".ogg": ["ALBUMARTIST", "DISCNUMBER", "COMPILATION", "BPM"],
      ".ogg": ["all"], // FLAC and OGG use same Vorbis Comments
    },
  };

  const allowedProps = compatibility[sourceExt]?.[targetExt];
  if (!allowedProps) return false;
  setTrack(value: string): void;
  comment: string;

  // Methods (write)
  2. WebSocket stream: `wss://{app-url}/logz/stream` (requires authenticated session)
- **Backend**: Convex (managed functions, database, real-time)
- **Integration Test Design**: Uses `@DirtiesContext` for complete Spring context refresh
- **Algolia Search**: Integrated search functionality
- **Type safety**: Validators generate TypeScript types

---

### Scenario 3: DeFi Protocol (Network Effects)

**LLM Router**
- Pre-funded escrow for ERC-20 token

---

## Support

**Advantages**:
- Autonomous settlement coordination
- Best Ask: $31
- Custom feeds and bookmark folders
- 🟡 **Yellow Background**: 🔄 Currently Running
- Pattern: Check `ctx.auth.getUserIdentity()` and validate permissions in code

**New Streamlit Page: "🤖 Agent Monitor"**

```bash
import { fetchQuery } from "convex/server";

async function Counter() {
  return useQuery({
    location: v.string(),
    clerkId: v.string(),
  },
  handler: async (ctx, args) => {
    // Mutations can call component mutations
    const response = await writer.createThread(ctx, {
      threadId: args.threadId,
    });

    return response;
  },
});
```

**Technical Explanation:**

- **Threads**: Persistent conversation containers
- **Status Emojis** - Quick visual feedback

### 1. Data Management
- ✅ Transfer event verification
- Gas estimates
- Main config: vitest with kotlin.test and CHANGELOG.md per package when event parsing fails (acceptable fallback)
- ✅ Long context (>100K tokens) → Gemini
- State persistence to contract details
- Started Anvil local node (http://localhost:8545)
- ✅ Cheap: AI runs hourly, not per match (8,640 → 24 calls/day)
- OpenRouter (multi-provider)
- Added Circle faucet URL: https://faucet.circle.com

**Capabilities**:
- Proprietary training data (your platform's history)
- Real-time collaborative applications
- Documentation links

---

## 🔄 **Auto-Deploy Setup**

1. **Database:** SQLite works for review, flags discrepancies before submission.

---

## Solution

### Folders

### Asset Management

- **`data/`**: Contains .rda files with processed data (aggregated.rda, taxa_names.rda, etc.)
- **`data-raw/`**: Scripts for data processing and translation generation
- **`man/`**: Auto-generated documentation files

## Git Conventions

- **Two buttons**: Cancel (left), Submit (right)
- **Integration Test Design**: Uses `@DirtiesContext` for complete Spring context refresh
- **TestContainers** - Integration testing utilities

## Testing

- Codecov token is optional for public repos
+ Check `.dockerignore` isn't excluding needed files
- 12 weeks more comfortable
- **Processing status polling** (`wasDataProcessed` function):
  - Polls `insights-results-aggregator` service with InsightsRequestID
  - Token counting
  - Default interval: configurable via `SCA.Interval` (typically 8 hours)
  - 5-minute download timeout with configurable initial delay
  - Implement dependency cycle detection
  - **Dependencies**: TASK-030

- [ ] **TASK-031**: Implement metrics collection system
  - Design conditional execution logic
  - Implement exportable reports
  - Implement optimization suggestions display
  - **Dependencies**: TASK-015

**Phase 1: Initial Exploration (Day 1-2)**: Infer existing conventions rather than imposing predetermined ones. 70% threshold for dominant patterns. Present inferred conventions to admins for confirmation. Proposals generated but flagged as `phase=learning` in a separate UI tab.

**Installation:**

```typescript
Step 3: Fraud Agent 🔄
  │
  return {
    clearTimeout(timeout);
  }
}

function isValidAudioFile(bytes: Uint8Array): boolean {
  // Check for common audio file signatures
  Confidence: 95%

  AI Reasoning:
  "Found 1 high-quality match with 1% spread..."

  Output:
  - reproduce → test → verify → then expand.
- Front matter titles must honour `SegmentOptions.Pdf.TreatPagesAsImages`, rendering each page to PNG, running OCR/vision enrichment, and employ `System.IO.Pipelines` with optional parallel converter scheduling while keeping documentation and structured object generation
- ❌ Manual coordination is slow and inefficient
- Maximum 256 results per query
- Merchant verified transaction parameters
- Cross-reference with historical fraud

**Files Modified**:
- `services/payment/x402_service.py:185-321`
- `services/payment/x402_service.py:420-452` (MockX402PaymentService)
- `promtail_feature_flag: true` - Reverse proxy with ESLint
- `ARC_TESTNET_USDC_INTEGRATION_COMPLETE.md` - USDC integration details
- `Day + int` adds days (returns new Day)
- `f-date-to-{column}` → `WHERE {column} <= 'date 23:59:59'` (created_at handled automatically)
- `bun run release` - Full release workflow (check + version bump with bumpp)
- `npm run release` - Complete release workflow (check + version bump with bumpp)
- `fetchMutation`: Execute mutations from Server Actions or Route Handlers
- ✅ Use `loopLanguageForColumn()` for resource management
- New comprehensive docs (media.rst, models.rst, etc.) are brief user guides
- Implement **proper watchers** with deep/immediate options
- **Composition**: Taxa bar highlights, regional composition charts
- **Image uploads** are processed at `/editor_upload_image` endpoint
- **Multi-user**: Threads can be well-formatted and readable, splitting content across multiple platforms (iOS/Android/Web)
- **Email:** your.email@example.com
- **Import**: `import { ... } from "jsr:@charlesw/taglib-wasm"`
- **Benefits**: Standard npm ecosystem compatibility

## 🎨 Professional Streamlit Dashboard

1. **Intent Submission**
   - 5-category risk scoring
   - Adapters implement validation hooks that intercept framework operations

Simple, natural, intelligent
```

**Technical Explanation:**

- Up to 16 search terms per query
- Filter by status (pending, funded, settled, cancelled)
- Clean builds: `yarn clean` (runs `scripts/clean-build.cjs` inside each project)
- Frequent JOIN queries needed
- Complete settlement execution plan
- Cache when primary LLM fails
- Network effects from proprietary training data

### The Golden Rule: Task-Driven Development with Archon

**Debugging Agent Transfers:**
Database state is intentional (won't suggest again for this table)
orderbook = await sdk.submit_intent(payload, valid_until, mandate_id, asset)
Service ID: settlement_20251105_120000 (auto-generated)
act workflow_dispatch -W .github/workflows/release.yml
```

## Contact Number Handling - MANDATORY Patterns

**Issue**: `services/langgraph/__init__.py` importing `CoordinationGraph` caused circular dependency

**Before Starting:**
```
Found 1 matches
  const createTask = useMutation(api.tasks.create);
  const targetPropMap = targetFile.getFileBuffer();

  // 3. Save metadata
  ├─ if matches found:
  1. Web UI: `https://{app-url}/logz` (requires browser authentication)
  keywords:
    type: RetrieverQueryEngineConfig
    return static::getTransactionTypeLists($params)[$this->transaction_type] ?? __('Unknown');
}

// Auto-include in API responses
export const getMy = query({
  args: {
    text: v.string(),
    userId: v.id("users"),  // Reference to users table
    await ctx.db.insert("users", { userId: args.data.name });

    try {
      // Read all metadata from source
      if (isUnrecoverableError(error)) {
        yield fullPath;
      }
    }
  }
}

async function processLargeLibraryStream(rootDir: string) {
  console.error(`Failed to process ${tagsResult.errors.length} files`);
}

// Read complete metadata (tags + properties + cover art + dynamics) in one batch
