# Payment Integration Quick Start

## API Endpoint Testing Methodology

engine = create_engine("sqlite:///arc_coordination.db")
class ProductServiceTest {
    
    @MockitoBean
    ↓
export const search = query({
  args: { searchTerm: v.string() },
  handler: async (ctx, args) => {
    // Queries MUST use indexes for filtering
    self.payment_service = self._init_payment_service()

5. [Payer] Sign payment intent (off-chain)
   Average reward: 2.737 → 1.000 (+0.025)
   Direct actions: 15 → 4 (-11, -73.3%)

🔧 Skill Development:
   st.session_state.payment_amount = 10.0  # 10 USDC
   ```

3. **Standard Page Structure**
   ```vue
   <auto-datatable />     // For data tables
   <template>
       <!-- ✅ ALWAYS use $t() in templates -->
       <h1>{{ $t('Page title') }}</h1>
       <form-input :label="$t('Username')" />
       <auto-button>{{ $t('Submit') }}</auto-button>
   </template>

   <script setup>
   import AdminContentCard from "#/admin/components/admin-content-card.vue";
   // ... other imports
   </script>
   ```

4. **i18n Translation Rules - MANDATORY Usage Patterns**
   ```bash
   pnpm build ios          # iOS build
   Active: True

============================================================
  Arc Coordination System - Intent Submission Test
============================================================

iter=0  success=0.975  avg_reward=2.737  actions(skill/tool/direct)=0/65/15
└── tests/
```

**Tasks**:
1. Replaced single-line event extraction with safe parsing
docker/build-package.sh 8
```

```bash
# OLD (didn't work):
VITE_CONVEX_URL=https://your-deployment.convex.cloud

# 3. Streamlit UI
from web3 import Client

1. **Better State Management**: Single source of truth.

`AGENTS.md`, `GEMINI.md` are collectively called the "agent instruction files".
