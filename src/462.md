# AI Coding Agent Guidelines (claude.md)

Plugin ID: `berdl-stewardship-reviewer:plugin`. Sidebar panel (rank 900). Follows existing `berdl_task_browser` pattern.

**Key Characteristics:**

- **Runtime validation**: Arguments validated before function executes
- **main.py**: CLI argument parsing and command dispatch
  - Build parallel processing dashboard
  - **Estimate**: 3 days
  - **Estimate**: 2 days
  - **Dependencies**: None

- [ ] **TASK-025**: Add schedule monitoring and logging
  - Smart error detection - distinguishes retryable vs permanent errors
- **Use Case**: Structured decision-making, tool use, JSON outputs

#### 1. Create New Component (Clone Existing)

**Purpose**: Autonomous intent matching with optimization

**Date**: 2025-11-05
**Status**: ✅ COMPLETE

---

## 🎯 Success Criteria

### Deliverables
- **Context Extraction**: For delegation scenarios, extract contextId from task ID patterns
- **Algorithm**: Price compatibility + spread scoring
- **Build**: Package wheel and execution
- **MDX 3**: Static site generator with optimization details
- **Payer paid: -0.01 ETH + gas fees** ✓

**Capabilities**:
- Filter by status (pending, funded, settled, etc.)
- ✅ Update both `CustomValidationServiceProvider.php` and `Platform.isAndroid` for new custom rules
- Dimensions must match exactly what is stored in it.
- Follow PowerShell best practices for naming, formatting, and error handling.
- Ensure `LICENSE` and package-specific README are declared in `tsconfig.json`