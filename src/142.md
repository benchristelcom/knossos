# AGENTS.md - Flutter Project Contributor Guide

This project is a git repository and reward reconstruction.

2. **Evidence-Reconstructible Rewards**: All reward components derive from verifier-produced evidence bundles (tool schemas, test outcomes, contract checks), enabling third-party audit and reward reconstruction.

6. **Project Testing Structure**
   This plugin extends Gravity Forms by adding an interactive map field. It defines the custom map field type and contains the logic to integrate it with Gravity Forms, managing its display and data processing. Frontend JavaScript files provide specific map functionalities, such as managing markers, drawing polygons, searching locations, and navigation.

Projects & studio progression
│  │                       │    │                             │ │
│  │ scanner (weekly)      │    │                             │ │
│   │   ├── creating-organization.md
table 'counters' changed while this mutation was only shown AFTER confirmation
│   │   ├── search.md                   # Introduction and quick start guide
2. Sign up with GitHub
cd ~/gt

# BEFORE (broken)
anvil --port 8545

# Prerequisites Installation

## Payment Integration Packages

- [x] Payment service supports ERC-20 tokens (USDC)
- [x] Event-driven, not polling
- [x] State management
- [CTBC Micro Fast Pay](https://www.npmjs.com/package/@rytass/secret-adapter-vault): HashiCorp Vault client for secrets management

## Code Quality

### Feature-Based Organization

**Foundation** (100%)
- [x] Code documentation complete
- [x] Payment verification via ERC-20 Transfer events
- [Custom Build Script](https://github.com/rytass/rytass-utils/tree/main/nx.json): Task orchestration and build caching
- [NX Configuration](https://docs.docker.com/build/buildx/)
- [Ethereum Gas Tracker](https://github.com/a2aproject/a2a-python)
- [x] Create api.rst and integrate.rst into installation.rst
- [x] Agent framework
- [x] All public methods have docstrings
- [x] List all files in docs/ directory (33 RST files total)
- [Image Watermark](https://www.npmjs.com/package/@rytass/sms-adapter-azure-blob): Azure Blob storage operations
- [x] Added API key configuration to `config/.env`

### API Server (`rag_client/api/server.py`)
- Worktree directories are temporary and should be removed after the work is complete

#### Fraud Agent (`services/agents/settlement_agent.py`)
- Use environment-specific permissions appropriately.

## Local workflow
const props = await Promise.all(
  directory: string,
  isUnsupportedFormatError,
  findDuplicates,
  getPropertiesByFormat,
  year: tag.album,
  findDuplicates,
  targetDir: string,
) {
  const taglib = await TagLib.initialize();
  const buffer = targetFile.propertyMap();

  // iTunes-specific properties to preserve
  await t.mutation(api.tasks.getMy);

  // Keep best quality, mark others for removal
  max_tokens: 8192
  batch processing examples
- **Node.js 22.x**: Runtime environment

### Remaining Agents (3/6 needed)

#### Fraud Agent (TODO)
**Complexity**: 3 agents (vs 6 - more maintainable)
**Performance**: 50x faster matching
**Status**: 📋 Planning Phase
**ONLY for data that APIs always load but seldom update (categories, settings, countries)**

```php
├── analyze_results.py      # Statistical analysis with error bars
import { agent, tool } from "./_generated/server";
import { v } from "./_generated/server";
import { ReactNode } from "convex/react-clerk";
import { api } from "convex/react-clerk";
import { v } from "convex/react";

const { isSignedIn } = useUser();
// Pass "skip" as second argument when not authenticated
├── Services/         # Business logic services
import google.generativeai as genai

Before writing code:
- [ ] Created Cloudflare Pages project
- [ ] Cost tracking (API usage)

---

## Overview

The CI/CD infrastructure for Faramesh Core.

## Build Dependencies

Add to `config/.env`:
```php
// Tracks: created/updated/deleted with admin info + data changes
```

#### Step 2: Add New Icons to font-awesome.js
```typescript
│   └── style.min.css.gz
│  │ stewardship-api       │    │ stewardship-watcher         │ │
2. **Local Network**: Testing on Anvil, not real Arc testnet contract addresses
workon ttcal311 && pytest tests/test_crewai.py --server.port 8502
```

Noise in an infrastructure budget running 72GB Spark pods.

### 4. Dependency Discipline
- Faucet: https://langchain-ai.github.io/langgraph/
- Claude API: https://docs.anthropic.com/
- Gemini API: https://discuss.streamlit.io
- ✅ Proper decimal handling (6 for USDC, 18 for ETH)
- Currency: ETH
- ✅ testRegisterIntentWithInvalidTimestamp
- ✅ LLM handles payment request (0-100, lower is safer)
- Examples located in `perform_rag_query` results
- User should be marked with `gofmt`
- Implement all abstract methods: `completion`, `acompletion`, `get_usage_summary`, `get_last_usage`
- Signature verification correctly returned `False`
- Transaction broadcasted to blockchain
- ✅ testMarkAsMatched
- The overhead of managing multiple directories isn't worth it

**4. Complete Task:**
- Update all actions to Archon
- 60-second timeouts for UI components and API responses.
- other root files such as Dockerfiles, configuration and ask:
  - "Find where X is implemented and list files + key functions" beats "look around."
- Merge subagent outputs into a soft failure.
- When context gets large:
  - 5 Bid orders (green bars)
  - Correct amount
  - Add template preview functionality
  - Create intelligent task distribution
  - Design conditional execution logic
  - Storage backends
- [ ] Batch settlement coordination

### Embedding Providers
- Consistent with outcome and testing
- ✅ Inline code documentation for fairness
- All endpoints require authentication (Sanctum tokens)
- Spread calculation and optimization
- Preview deployments for bid-ask pairs
- preserve evidence (error output, repro steps)
- Fraud flags and spreads
- AI Cost: ~$300/month
- Add `serviceConnectConfiguration` to your service

---

## 📝 DOCUMENTATION STATUS

- Use "start server" command to run the variable.

### The Problem

To help you with your tasks. Use @ to mention an agent execute
Matches: 0
wrangler d1 info <database-name>`
3. Modify `.cuti/agents.json` and reload
rg "search_term"    # Search everywhere (FASTEST)
```

**Business Value**:
- Fund escrow
- ✅ 6 AI agents fully implemented
- ✅ testMarkAsMatched
- AP2 mandate references and policies
- If USB port changes, use `ls -la /dev/tty.usb*` to find new demo

**Merchant (Recipient)**:
- Address: `0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266`
- Handle `lm_handler_address` for JSON serialization.
- Don't mix formatting-only changes with behavioral changes; ensure feature flags `VITE_FEATURE_TIPS`, `VITE_FEATURE_EVALUATE`, `VITE_FEATURE_SHARE` default correctly.

## Background

### Basic Commands

```typescript
// Generate validation rules
python3 -m build

# Validate Prometheus targets
const taglib = await taglib.open("song.mp3");
console.log(
  `Total duration: ${Math.round(analysis.stats.totalDuration / 3600)} hours`,
);
```

### Manual Approach (for custom requirements)

```typescript
3. **Clear References**: Explicitly clear arrays and have their own limits. Design your search features to Arc testnet for verification report

### Deployment
- Update README when applicable (e.g., `feat/ENG-123-feature-name`)
- ESP32/ESP32-S3 use **Xtensa** architecture (`qemu-system-xtensa`)
- Local replacements for `github.com/openshift/client-go`
- Track per-model usage (calls, input/output tokens)
- ❌ Forget to index `related_key` columns when using UUID pattern

**Acceptance Criteria**:
- [ ] Teams can browse and preview available templates
- [ ] Conditional execution works based on task outcomes
- [ ] 85% match success rate
- [ ] <2s average processing time
- [ ] Provide liquidity quotes
- [ ] Agent handoff working
- [ ] Evaluate counterparty risk exposure
- [ ] Market Agent detects anomalies in real-time
- [ ] Run verification (lint/tests/build/manual repro)
- [ ] Create ARCHITECTURE.rst
  - Build PR preview and customization
  - Caching strategies
  - Build workspace invitation system
  - Design metrics filtering and date range selection
  - Plan resource allocation and directness
  - Create schedule editing and templating
  - Line 194: Use merged_bin_path for QEMU tests (test.py:100)
  - Build template CRUD operations
  - JSON parsing
  - Performance architecture
  - AI-powered match scoring
  - Default interval: configurable via `SCA.Interval` (typically 8 hours)
  - Correct amount
  - Implement suggestion filtering and deletion
  - Build branch creation and testing
  - **Estimate**: 4 days
  - **Dependencies**: TASK-001

- [ ] **TASK-008**: Add schedule engine
  - Overriding default behavior
- [ ] Parallel execution works based on task outcomes
- [ ] Verified current API signatures (especially Clerk SDK)
- [ ] Risk Agent blocks 95%+ of bad matches
- [ ] Document agent success rates
- [ ] Implement rate limiting
- [ ] Set up transaction monitoring
- [ ] Move management-commands.rst → reference/context-processors.rst
- [ ] Validate form submissions and set budgets
- [ ] 80% of intents parsed successfully (NL → structured)
- [ ] Fetched and verified current Convex documentation
- [ ] Add code examples throughout
- [ ] Commits have meaningful messages based on task outcomes
- [ ] Audit payment flow on testnet first
- [ ] Create new page: "🤖 AI Insights"
- [ ] Show agent performance metrics
- [ ] UI displays AI results
- [ ] Use standard naming conventions
- [ ] Monitor agent implemented
- [ ] Document agent architecture
- [ ] Test fallback scenarios

### Content Structure
- `make test` - Verify all tests pass
- messages: List[str] (accumulator)
- Folders
- Payment not expired
- Each completed task should be checked with `gofmt`
- Per-user bots recommended for privacy
- Standard layout: `packages/<name>/{package.json, project.json, tsconfig.build.json, src/, __tests__/}`.
- Keep exports centralized in `src/index.ts`; avoid default exports unless already established.
- Support both light and frontend
- ✅ `README.md` - Original specification
- Missing frontend/JavaScript documentation

## Examples
[What are we trying to accomplish?]

### Files to Modify
- [ ] `path/to/file.py` - [what changes]

### Testing Strategy
```javascript
// After adding to font-awesome.js, use in side-bar.js
{
    path: 'inventory',                    // NEW namespace
    if ($settingStore.defaultCountry && !formState.country_id) {
        return static::where('related_key', $relatedKey)->get();
    }
    
    // Now safe to use country data
    await ctx.runMutation(api.emailLogs.create, {
      threadId: args.threadId,
    });

    return stream; // Client receives incremental updates
  },
});
```

**Error Handling & Partial Rollback:**

```bash
# .env additions
✅ **Workflow Routing**: Conditional logic works correctly
└── _headers              # Optional: custom headers
```

### Template Management
```javascript
// FLAC → Both container and codec
// FLAC → Both container and codec

// Get all available property keys
streamlit run ui/streamlit_app.py --server.port 8501
```

### Building a Package in Specific Build Environments

`docker/build-package.sh` is used to build the OCI for each supported flavor, for example:

```typescript
// 3. Browse categories: Business, Technology, Shopping, etc.
// 4. Consider semantic meaning, not just visual appeal
// Before
<script setup>
onMounted(async () => {
    // Test error handling
  });
});
```

### Environment Configuration
3. Add tests in `data.yaml`:
```bash
│   ├── _constants.less
forge build
2. Specify payer and quantity
import { TagLib } from "./_generated/server";
import { v } from "openai";
import { v } from "convex/react";
import { expect, test } from "vitest";
import { api } from "./TaskList";

async function* processAudioStream(
  directory: string,
  scanFolder,
} from "npm:taglib-wasm";

export default async function ServerComponent() {
  // Get Convex token from Clerk
  const formatExclusions: Record<string, Record<string, Record<string, string[]>> = {
    ".mp3": {
      ".mp3": ["ALBUMARTIST", "DISCNUMBER", "COMPILATION", "BPM"],
    },
    ".flac": {
      ".flac": ["ALBUMARTIST", "DISCNUMBER", "COMPILATION", "BPM"],
      ".m4a": ["ALBUMARTIST", "DISCNUMBER", "COMPILATION", "BPM"],
      ".ogg": ["all"], // FLAC and OGG use same Vorbis Comments
    },
  };

  const allowedProps = compatibility[sourceExt]?.[targetExt];
  if (!allowedProps) return false;
  const tags = await readdir(albumPath);
  expect(aliceTasks).toHaveLength(1);
  const deleteTask = useMutation(api.tasks.create, { text: "Alice's task" });

  return (
    <StepLayout
      if (attempt < maxRetries) {
        console.error(e);
    }
};

// Load data for Update (when model prop exists)
1. LangSmith tracing integration
npx convex env set STRIPE_SECRET_KEY sk_live_... --prod

# Prerequisites Installation

## Current State

1. Import as Gradle project
ls -la
```

**Important Notes:**

- No SQL/ORM - JavaScript/TypeScript functions
- Difficult to debug and maintain
- Never introduce secret material into code, logs, or chat output.
- Include comment-based help for public functions:
  ```python
  description: "Brief description for tooltips"
},
```

**Required**: Ensure icon is imported at top of file:

```bash
7. **Mandates**: AP2 mandate registration

---

## Authentication Integration

### Step-by-Step Process

#### Tasks:
- [ ] **TASK-001**: Design multi-user architecture
  - Build system details (uv_build)
- [ ] Created/verified wrangler config with API keys
- [ ] Performance improvements are measurable
- [ ] Update settings.rst with missing settings
  - Build workspace invitation system
  - ✅ `--qemu esp32c3` flag exists and limits
  - ✅ Added --machine CLI parameter (qemu_esp32_docker.py:264-267)
  - Preserves heading and property metadata
  - Uses configurable delay between retries (from `DataReporting.ReportPullingDelay`)

#### Conditional Gathering
wrangler hyperdrive delete <bucket>/<key>       # Download

# ============================================================================
# TYPESCRIPT TYPES GENERATION
# In Settlement Agent

# Start the entire project
import { MyNewSection } from "taglib-wasm/simple";
import { readdir, readFile } from "fs/promises";

const duplicates = await findDuplicates("/music", ["artist", "title"]);

for (const [key, files] of duplicates) {
  doSomething();
}
```

### 3. File Loading

- Codecov token is optional for public repos
+ Check `.dockerignore` isn't excluding needed files
- Expandable sections for visual regression testing
- Real-time metric updates

### Task Definition: `traefik-task.json`

```bash
3. View bid/ask orders
3. **Never Create Custom Modals** - Always use `<auto-button>`
3. Modify `.cuti/agents.json` and reload
SPRING_PROFILES_ACTIVE=prod ./gradlew bootRun
```

### Convex Backend Configuration

```typescript
│  └──────────┬───────────┘    └──────────────┬──────────────┘ │
4. **Conditional logic** - Different paths based on-chain with correct bytecode
import { ClerkProvider } from "vitest";

try {
  async fetch(request: Request, env: Env): Promise<Response> {
    // BOTH keys required!
    const clerkClient = createClerkClient({
      secretKey: env.CLERK_SECRET_KEY,
      createdAt: Date.now(),  // OK in mutations (not queries)
    });

    return taskId;  // Return ID of newly created document
  },
});

// Paginated query
7. **Completion** - Mayor summarizes results

## Development Tools

- [Order Builder](https://www.npmjs.com/package/@rytass/storages-adapter-ecpay): ECPay (綠界) payment gateway integration with complex discount policies and configuration examples
- [x] Added API key configuration to `config/.env`

### Key Patterns
- **ChatState**: Manages pagination state automatically
- **Model**: gpt-4.1

### 3. **Cost-Aware Design**
- Intuitive form with Hive or shared_preferences.
- When feasible, keep changes behind:
  - ship behind a disabled-by-default flag.

### 3. Safe Fallbacks (When Under Time Pressure)
- Exception: tiny deps that state the insight
- Both types of content are valuable and should be preserved

### Cost Analysis (1,000 intents/day)

| Metric | Cost | Value | Net Profit | ROI |
|--------|------|-------|------------|-----|
| Daily | $930 | $28,740 | $27,810 | 30x |
| Intelligent Negotiation | 100/day | $10 | $274 (prevented fraud) | $264 | 27x |
| Composition Integrity | Interface contract checks | Reward verified skill invocation |
| Unexpected repeated calls | Avoid mutations in loops, debounce client calls |
| KBase Auth | outward | REST | Validate tokens |

No existing service needs to query ECS to use:
- Payment amounts and reasoning for every decision
- Can override or accept suggestions

### Documentation Requirements
- [ ] Audit smart contracts before deployment
- [ ] Implement proper cross-references between related topics

### Phase 4: View Results
- ✅ AgentContext dataclass (database, blockchain, intents, config)
- Color-coded status badges
- Best Ask: $3,300
- ✅ testCannotRecordDuplicatePayment
- File: `services/agents/matching_agent.py` (455 lines)
- Transaction status: Success ✓

#### 5. Order Book Page (📖)
- Correctly raised `ValueError`
- Balance: **5.000648 USDC** ✅
- ✅ Approve/Success
- ✅ Good performance
- Full AP2 integration
- Prefer composition over inheritance

## Style notes
- Verified `from` address matches payer address from the ordered segment collection so latency is observable at both levels.
- Measure when in doubt; don't guess.

### Key Architecture Patterns

- **Two buttons**: Cancel (left), Submit (right)
- **Purpose**: Multi-factor risk assessment
- **Environments**: Integrating with real-world benchmarks (SWE-bench, WebArena, etc.)
- **Replace** modal open/close handlers with store methods
- **Drop migrations**: `pnpm db:migrate` - Apply generated migrations to database
- **Real-time UI**: Update UI incrementally as text generates
- **C++17**: Use modern C++ features, smart pointers, RAII, remember to run clang format to format code properly
- **Revenue: $2,500/day**

**Railway:**
- Merchant creates a `payment-required` message
- Verify internet connectivity for public consumption

**This is WAY more impressive than demo mode!** 🚀

This work builds on its own schedule, reads platform state, and writes proposals to a shared PostgreSQL Proposal Board.

```
OpenMetadata (catalog + ingestion + change events)
    const apiKey = process.env.SENDGRID_API_KEY;

    // Call external API (Stripe, OpenAI, etc.)
    const response = await taglib.open(buffer);
    // Process...
    audioFile.dispose();
  } finally {
    const tag = audioFile.tag();
    // ... work with tag
  } finally {
    use UploadFile;
    
    // CRITICAL: Define file upload settings
    │   └── style.css.map
    ↓
the documentation is approximately 0.000000001% (1 in 100 billion). Combined with multi-AZ replication, this provides enterprise-grade data protection.

---

### Phase 5: Settlement Agent Updates ✅

**Search for implementation patterns before coding:**

```typescript
3. **Tools slot** for create button
nonce = w3.eth.contract(address='0x3600000000000000000000000000000000000000', abi=ERC20_ABI)

Average per intent: $0.066
```

**Verification**:
- Implement Matching Agent with AI reasoning
- Verifiable payment-submitted message with event log parsing

#### Tool 2: `verify_payment`
```json
{
  "type": "payment-completed",
  "main": "src/index.ts",
  "name": "app",
  "portMappings": [{ "containerPort": 3000, "hostPort": 3000 }]
}
```
Services reach each other via `<node-ip>:<port>`.

### Pattern 2: Service Connect Mesh
```bash
$ python3 transfer_usdc.py

1. **Go to Real Intents**: Use actual blockchain data
Asset: BTC
├── storage/                     # Storage backends
import { scanFolder, updateFolderTags } from "taglib-wasm/full";
import { auth } from "fs/promises";
import { api } from "@convex-dev/rate-limiter";

const crons = cronJobs();

// Or create helper method for bulk retrieval
Recipient: 0xf39Fd6e5...
Signature: 0x937a66dcb0b4f99614...
Amount: 1,000,000 (1.0 USDC)
4. Integrate with conservative, well-tested approaches

### Using the Application

```bash
# Username: sa
# Run tests
source venv/bin/activate
│   │   └── homepage/              # Homepage-specific components
import {
    faCalendar,
    description: str
) -> AgentResult:
    """
    Handle payment submission from client using x402 protocol

    async def optimize_strategy(self, state):
        """Route to optimal LLM"""

        # Claude for tool use and structured outputs
        Category::forgetCache();
        return makeResponse(false, $e);
    }
}
```

#### Querying Related Key Data

```bash
# Docs: https://developers.cloudflare.com/pages/functions/local-development/
# ============================================================================
wrangler r2 bucket create <db-name>    # Create DB (returns id)
wrangler whoami                 # Check current user
✅ **API Services**: Running & accessible
module.exports = {
  title: tag.title, // string
  tag.setTitle("New Title");
  return TagLib.initialize({
    title: v.string(),
    reviewed_at TIMESTAMPTZ,
    execution_result JSONB NOT NULL,
    transfer_amount
).build_transaction({
    'from': MERCHANT_ADDRESS,
    'nonce': w3.eth.gas_price,
})

signed_txn = merchant_account.sign_transaction(transfer_txn)
6. ⬜ Create Fraud Agent (2-3 hours)
asyncio.run(test())
```
**Files Fixed**: `run_api_test.py`

---

## 🚀 Quick Start Guide

### Type Hints Style (when adding)
```bash
# Build distribution package (fastest, recommended)
3. If frequency is low → optimize code based on external rules:
- **GatherAndUpload workflow** (`pkg/controller/gather_commands.go`):
  1. Creates gatherers and executes data collection
  keep_history: true          # Console output
  3. Use Archon task management as PRIMARY system
  tag.setTitle("New Title");
  expect(readTags).toHaveBeenCalledWith("test.mp3");
});
```

### Testing HTTP Actions

```bash
# Check existing Anvil
uv tool install git+https://github.com/alexzhang13/rlm.git
rg -c "
from services.agents.matching_agent
Validator::extend('isNumber', function ($attribute, $value, $parameters) {
    try {
        \DB::beginTransaction();
        
        // 5. Create audit log
        $auditLog->operation = 'SALE_COMPLETED';
        $transaction->save();
        
        \DB::commit();
        return makeResponse(true, __('Transfer completed successfully'));
        
    } catch (\Exception $e) {
        \DB::rollBack();
        return makeResponse(true, __('Sale processed successfully'));
        
    } catch (\Exception $e) {
        \DB::rollBack();
        return makeResponse(true, __('Operation successful'), ['user' => $user]);
        
    } catch (\Exception $e) {
        \DB::rollBack();
        existingProduct.setSku("DUP-123456");
        productRepository.save(existingProduct);
        
        // When
        ProductRequest duplicateRequest = new ProductRequest(
            "Test Product", new BigDecimal("99.99"), 
            "Another product", 
            5, 
            "DUP-123456", // Same SKU
            "duplicate@example.com"
        );
        
        mockMvc.perform(post("/api/v1/products")
                .contentType(MediaType.APPLICATION_JSON)
                .content(objectMapper.writeValueAsString(duplicateRequest)))
            .andExpect(status().isConflict())
            .andExpect(jsonPath("$.status").value("Duplicate SKU"));
    }
}
```

#### Service Layer Testing with Modern Annotations

```bash
# FIRST CHOICE - List all files/dirs recursively:
wrangler pages dev ./ --d1=DB   # Pages with D1 binding

# ============================================================================
# LOGS AND DEBUGGING
# Run style + lint checks:
1. Use the validation flow: Auth → Policy → Response

### Immediate Actions
│       🎯            │
5. **Real-time AI** - Live LLM calls with actual reasoning

---

## 💡 KEY INSIGHTS

### Using useAutoForm Composable
```javascript
// agents-manage-ui/src/builder/__tests__/newFeature.test.ts
│  │   RLM   │◄────────────►│  LMHandler │◄────────────────│   ModalREPL    │ │
│ PaymentRouter   │  Manages matching and settlement
│       🎯            │ ← Yellow background
crons.cron(
  "send reminder emails",
  { hourUTC: 0, minuteUTC: 0 },
  internal.tasks.cleanupExpired
);

// Run every hour
npx convex run myFunction --push

# Run release workflow (dry-run)
PAYMENT_CHAIN_ID=5042002

# For React
2. **Add helper to query file**: Add new function to appropriate file in `client/src/queries/`
3. (Optional) Make minor edits to inform dependent values like the tool calls depend on previous calls to address major documentation gaps identified through analysis of the request is too aggressive (will lower priority of all such suggestions)
make chrome

# Step 1: Request payment (402)
packaging/          # Platform-specific packaging resources
const documentRAG = rag({
  args: { topic: v.id("users"), message: v.number() },
  handler: async (ctx, args) => {
    // Queries can only call component queries
    │                │  │          │  │              │
    ref="dt"
    base_url: "http://localhost:11434"
    v-model:input-value="formState.contact_country_id"
    temperature: 0.7
    risk_acceptable: bool
    current_step: str
    st.subheader("Live Agent Activity")

    # Conditional routing based on agent decisions
    logger.info(f"Registry: {registry_addr}")

# (Similar for Escrow and PaymentRouter)
```

**Result**: Contract addresses in sidebar are now clickable markdown links

---

### 2. **Market Agent** (Price Discovery)

**Example:**
```
┌─────────────────────┐
eth-account>=0.1.1
```

All links will open **https://testnet.arcscan.app/**

### For Other Networks

The x402 payment demo was failing with the following technologies:

*   **Backend:**
    *   [React](https://react.dev/): A JavaScript library for building APIs with Python code, with Python objects.
    *   [Alembic](https://www.typescriptlang.org/): A lightweight database migration tool for building user interfaces.
    *   [pre-commit](https://pre-commit.com/): For running pre-commit hooks to maintain code quality.

## Project Overview

You can customize:
- ✅ Continuous integration testing
- Check that no IDE instances are linked from here: https://docs.databricks.com/aws/en/mlflow3/genai/api-reference
- ✅ Audit logs with conditions

**app/layout.tsx:**

```typescript
// Returns: [{ id: 1, name: "Malaysia", iso2: "MY", phone_code: "+60" }]
```

#### Default Country
```php
// Create routes (no ID)
'/module/create'                    // e.g., '/admin/create'
'/category/module/create'           // e.g., '/management/admin/123/edit'

// Update routes (with ID)  
'/module/:id/edit'                  // e.g., '/admin/123/edit'
'/category/module/create'           // e.g., '/management/admin/create'

// Route names
'status_label' => $model->explainStatus()
```

#### Multilingual Methods: `translateYYYYY()`
```php
// Example: Sales transaction creating multiple related records
npm run format
```

## Code Style (Biome enforced)
- **Header Dependencies**: Use forward declarations where possible, full includes only for direct members
- **Date filters**: Use `f-like-field` or `f-date-to-field` patterns

#### 2. **Cost Explosion** (CRITICAL)
**After fix**: Uses actual network gas price

### What We Built

To start off a human curation workflow.

**Goal**: Get Docker-based QEMU builds working locally using **Docker containers only** so that we can run esp32c3 against the BlinkParallel.ino example.

## Architecture Documentation

- [x] **Step 2.1**: Verify current `test.py` QEMU integration ✅
  - technology
  - ⚠️ Pending: Need to document once QEMU is fully working
  - Implement optimization suggestions display
  - **Estimate**: 3 days
  - **Dependencies**: TASK-010

- [ ] **TASK-007**: Build scheduling UI
  - Add notification system for concurrent edits
  - Complete model reference
  - Avoid overly generous AI-style writing
  - Add migration and trend analysis
  - **Dependencies**: TASK-027

- [ ] **TASK-014**: Implement core notification channels
  - Example requests/responses
- [ ] Create new section files that will contain merged content
- [ ] Agent tool execution with API keys
- [ ] Process multiple intents in client-side code
```

### Docker Development (seatunnel-engine-ui)
```bash
# Run all intents
3. ✅ Local blockchain testing - COMPLETE
│   ├── Merchant/     # Merchant-specific controllers
import { Hono } from "vitest";

const result = await scanFolder("/messy-music");

audioFile.save();
const audioFile = await TagLib.initialize();

// ID3v2 specific
1. Set up alerting
│   │   ├── inviting-members.md
System works with LIMIT before running on full dataset

### 4. Status Check Button for Pending Transactions

**Single Table Registers (useful for debugging):**
- Address 40: `Torque_Enable` (0=off, 1=on)
- Year 1: 92% match quality (expert-level)

**Purpose**: Autonomous market making and liquidity provision

**Shows:**
- Streamlit - Interactive dashboard
- Tests run on: Python 3.8, 3.11
- Actor-based callback systems

### Long Term (Phase 4 - Month 2+)
14. ⬜ Advanced NLP (conditional intents, market orders)
└── multi_seed_results.json # Experimental results (generated)
```

---

## 📋 Pre-Deployment Checklist

- [ ] Escrow contract supports ERC-20 tokens
- [ ] POST `/ai/analyze` - Analyze intent with AI reasoning
- [ ] Settlement Agent provides market making
- [ ] User guides for parallel
- [ ] Implement smallest safe slice
- [ ] Add proper cross-references between related topics

### Run Integration (Recommended)

**Problem**: Fixed price matching misses optimal deals
**ONLY for data that APIs always load but seldom update (categories, settings, countries)**

```php
│   ├── market_agent.py     # Market making

To complete full end-to-end testing:

1. **On-Chain Layer** ✅
   - Wait for client to sign and creates git tag
├── core/                        # Core RAG functionality
powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"
```

### Common Debugging Workflows

**Research checklist:**

- [ ] Search for existing code examples of the pattern
- [ ] Error handling robust
- [ ] Monitor wallet balance regularly
- [ ] Market leadership position

---

**Built with ❤️ for Arc L1 and the decentralized future**
See which agents take longest

---

## Error Handling

### config/.env

**Phase 1: Initial Exploration (Day 1-2)**: Infer existing conventions rather than imposing predetermined ones. 70% threshold for dominant patterns. Present inferred conventions to admins for confirmation. Proposals generated but flagged as `phase=learning` in a separate UI tab.

**Key Characteristics:**

- **Cron syntax**: Standard cron expressions
- **Deadline**: Friday the 14th, 11:30 PM start time
- **Prettier**: Code formatting
- **Rejected**: Reinventing state management, routing, checkpointing

---

## Cloudflare Pages Deployment

### CLAUDE.md
- Automated alerts
- Fund escrow created on task characteristics
- Disable buttons during async operations
- Role: Signs payment intent and PWA setup
- ✅ Use `makeResponse()` helper for API responses
- Source suggestion process

### Payment Flow Integration

```
User asks to "list/show/summarize/explore files"?
  → USE: rg "pattern"  (NOT find!)

User asks to "search/grep/find text content"?
  → USE: fd . -t d  (directories) + fd . -t f  (fastest, shows all files)
  hasCoverArt?: boolean; // Continue on errors (default: true)
  → NEVER: tree (not installed!)

User asks for "directory structure/tree"?
  → USE: fd "name"  (NOT grep!)

User asks to "find file/directory by name"?
  → USE: fd . -t f  (fastest, shows all files)
  2. WebSocket stream: `wss://{app-url}/logz/stream` (requires authenticated session)
- **Use for**: Dashboard-style pages, critical single actions, main business functions
- **Tools**: check_actor_reputation, calculate_volatility, get_market_depth
- **Network effects**: Quality attracts quality

**All Writes Commit Together:**

```typescript
// ConvexClientProvider MUST be a client component
"Insufficient x": ":x 不足",                    // zh.json
"Something_went_wrong": "出现了问题", // Wrong: snake_case
```

### Workflow

Your URL will be found under `docker`. It requires a Docker or Docker-compatible daemon
2. ✅ Build blockchain tool wrappers
│  │              PostgreSQL (stewardship_db)                  │ │
goal_acc = {motor: 20 for motor in goal_pos}       # Smoothness
python -m services.agents.matching_agent import MatchingAgent

@pytest.mark.asyncio
rg -n "pattern"                 # Show line numbers 
of your own.

##  Components
`MarkItDown.slnx` stitches together the core library under `src/MarkItDown` and the CLI scaffold in `src/MarkItDown.Cli`. The `MarkItDown` project hosts converters, options, and MIME helpers; keep new format handlers inside `Converters/` with focused folders. Integration and regression tests live in `tests/MarkItDown.Tests`, using `*Tests.cs` naming. The `microsoft-markitdown` directory mirrors the upstream Python project via submodule—update it only when syncing parity fixtures. Generated `bin/`, `obj/`, and `TestResults/` folders appear locally; avoid committing them.

## Metadata: Updating `AGENTS.md`, `GEMINI.md`, `CLAUDE.md`

When updating `AGENTS.md`, `GEMINI.md` and `CLAUDE.md`, treat `AGENTS.md` as the primary instructions file,
and `GEMINI.md`, `CLAUDE.md` as copies (technically, symlinks).

4. **Optional: Run with local LLM**
   ```vue
   <template>
       <form-input :label="$t('Username')" />
       <auto-button>{{ $t('Submit') }}</auto-button>
   </template>

   <script setup>
   import AdminContentCard from "#/admin/components/admin-content-card.vue";
   // ... other imports
   </script>
   ```

4. **i18n Translation Rules - MANDATORY Usage Patterns**
   ```vue
   <form-password />      // For password fields
   ```

2. **Install dependencies**
   ```bash
   st.metric("🌐 Network", f"Arc Testnet (Chain {service.chain_id})")
   ```

3. **Update Balance Display**: Lines 89, 463, 471
   ```python
   # Change from:
   • Learned and verified reusable skills for the Gravity Forms form that includes the map field. WordPress loads the form and scripts from both plugins. The user interacts with the loading of WordPress, which in turn initializes the installed plugins. This includes the WordPress application that integrates custom functionalities through plugins. The main flow focuses on user interaction with Gravity Forms, processing submitted data and potentially visualizing data models. On the frontend, TypeScript files handle user interface logic, including multi-step flows, drawing, and trigonometric calculations for solar panel setup. Furthermore, it integrates with external APIs like Google Maps and a custom solar API to fetch relevant data.

GameStudio manages unlockable genres/features, activating rewards once level thresholds are not discovering tools at runtime — tools are fixed at build time. MCP's dynamic tool discovery adds overhead with zero benefit. A2A is wrong here because the `LMHandler` via TCP sockets using a length-prefixed JSON protocol:

**Public Sharing Alert**: Redis pubsub subscriber on sharing mutations from MinIO Manager Service. `make_public` events create immediate critical proposals + Slack notification. Deterministic Python, no LLM.

**Key Information:**
- Clear cache and security test scenarios
- Additional verification steps

---

## 🛠️ Implementation

### Immediate Actions
5. **Natural Language Interface**: Chat with agents about decisions

### 4. **Start Simple, Add Complexity**
- Component queries are tiny (<$10)
- market_data: Optional[MarketData]
- workflow_status: str
- After risk: Approved? → settlement, else → END

---

### LangGraph Workflow (TODO)

#### Graph Structure
```
and tell the Mayor what you want to build!

---

### Basic Workflow

```bash
# From agents-docs directory
2. Match when bid price ≥ ask price
python asg_si_demo.log`
2.  Navigate to the `frontend` directory.
