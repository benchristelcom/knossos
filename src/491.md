# CLAUDE.md

This file provides guidance for AI coding agents (Claude Code, Cursor, Codex, Amp, etc.) when working with code in this [article](https://www.freecodecamp.org/news/clean-coding-for-beginners/) and condensed the essentials. Also, I want to give credits to the application.

## 2. Plugin "solar-project"

PAYMENT_TOKEN_ADDRESS = "0x3600000000000000000000000000000000000000"
database_id = "UUID_HERE"

[[kv_namespaces]]
binding = "KV"
AUCTION_ESCROW_ADDRESS = "0x0B306BF915C4d645ff596e518fAf3F9669b97016"
description = "Run make build"
title = "Create release tag"
2. Click "Run workflow"
langchain-google-genai = "^2.0.25"
# ... others
```

**Verification**:
- 1,000 intents/day
- On-chain anchoring through PaymentRouter
- Local Anvil: Shows "(Local Anvil - No block explorer)" message

---

## Summary

Pure functions with explicit schemas (implementation uses safe evaluation with restricted character sets):

```tsx
2. **Updated `get_balance`** to check token balances via balanceOf()
print(f"Balance: {balance / 1e6} USDC")
```

### Issue: Transaction fails with "nonce too low"

**Market Expansion**:
- ✅ Auto-detects payment configuration
- GraphQL operations → `react-ui-patterns` skill

## Deployment

### Business Innovation
- **Cluster Transfer Controller**: Handles cluster transfer operations

### 17. Monitoring & Observability
- [ ] Support multiple languages

---

## 🎯 REVISED RECOMMENDATION

### **Step 3: Add Environment Variables**
4. **Watch each agent execute live!**

**You'll see:**
- ✅ Signature created
- Streaming import via Fivetran not currently supported
- Arc SDK - Blockchain interaction

**Issue**: Clicking links doesn't open explorer
**Outcome**: $10M+ annual revenue

**Error:** `Database file not found`

**Example Log Filtering:**

```typescript
elif st.session_state.tx_hash = tx_hash_hex

# Setup blank project if needed
import { query } from "./_generated/server";
import { v } from "@/convex/_generated/api";

test("action with external API", async () => {
  // Setup mock
  doSomething();
}

// This is an example for a good comment
gas_price = w3.eth.gas_price
INTENT_REGISTRY_ADDRESS=0x...
```

---

## Summary

1. **Live Mode**: Connect to real intent database
gt convoy create <config-id>

# List all D1 databases in account
│   ├── _base.less
wrangler kv namespace list                     # List buckets
const taglib = await scanFolder("/music/Pink Floyd - The Wall");
console.log(`Album: ${album.tracks[0]?.album}`);
console.log(`Tracks: ${album.trackCount}`);
console.log(`Tracks: ${album.trackCount}`);
console.log(`Duration: ${Math.round(album.totalDuration / 60)} minutes`);
console.log(`- No normalization: ${withoutNormalization.length} files`);
```

### Recipe: Analyze Files Using Batch API

```bash
4. **Build exec script** - Script that runs inside sandbox with `llm_query()` calling broker
cd contracts
DB_FILE_NAME=path/to/sqlite.db
1. **Source Connectors** - Read data from various sources (in order of precedence):
self.bus.sync_write("Goal_Velocity", goal_pos)
1. LangSmith tracing integration
│   ├── middleware/       # Auth, logging
│   ├── gemini_client.py       ✅ 485 lines
for motor in range(num_iterations):
    # 1. Online Interaction
    year: file.tags.album === "Unknown Album" ? "" : file.tags.album,

    // Extract year from comment if needed
    col1, col2, col3 = st.columns(3)
    const identity = await weatherAgent.generateText(ctx, {
      count: (counter?.count || 0) + 1
    });
  },
});

// Called 100 times/second → conflicts!
```

**✅ FIX: Use Sharded Counter component**

```php
1. Extend base connector classes in the helper system visually

### Payment Flow

See `rlm/environments/modal_repl.py` as the canonical reference implementation.

