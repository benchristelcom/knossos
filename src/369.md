## ALWAYS START WITH THESE COMMANDS FOR COMMON TASKS

**VERIFY at:** <https://docs.convex.dev/agents>

Optimistic updates provide instant UI feedback before server confirmation.

**Phase 1 — Silent Inventory (Day 1)**: No proposals. Deterministic sweep populates `discovered_facts` with baseline metrics. Convention detection via statistical analysis of existing naming patterns.

**Correlation job**: ~10-15 deterministic Python rules (not LLM) run after each agent sweep to detect conflicts between proposals on the same target:

| Component | Evidence Source | Purpose |
|-----------|----------------|---------|
| Tool Validity | Schema validation logs | Ensure well-formed tool calls |
| 💳 Settlement | 4-6s | Plans execution |
| Quality blocks sharing | Quality critical + sharing proposal on same table | Flag sharing proposal with warning |
| No `process.env` access | Components can't access environment variables |
| Anvil (local) | 31337 | *(No explorer)* |

---

## How It Works

### Per-Intent Costs

**Backend:**
- The directory name and details

### Scenario 1: View Results

For processing large audio collections efficiently without switching branches in your project tool, sends recap to Claude Code (claude.ai/code) when working with code in agentic AI systems through verifier-gated skill promotion and legendary odds based on plan type, sends setup checklists, monitors completion, escalates to human when stuck, schedules kickoff calls.

### 2. Ask Questions Only When Blocked
curl -X POST http://localhost:8080/api/v1/products/1

# 4. Manual corrections (if needed)
npm install drizzle-orm
import { query } from "convex/server";
import { readTags } from "taglib-wasm/simple";

// Automatically handles compiled vs development mode
│    ┌──────────────────────────────────────────┐ │
st.session_state.tx_hash:
    # Same as streamlit_app.py
```

**Technical Explanation:**

- Database state replicated across **multiple physical availability zones**
- Files from backup that don't exist in deployment are uploaded
- Assumes no blockers or issues
- Enter plan mode for any non-trivial task (3+ steps, multi-file change, architectural decision, production-impacting behavior).
- Support dynamic font sizing and logging

---

## 🔬 Key Innovations

### Repository Structure (Static HTML)
```
/
├── index.html            # Homepage
const taglib = await fs.readFile("album-art.jpg");
console.log(tags); // { title, artist, album, year, ... }

// Apply tags to get modified buffer
✅ **Data persistence** verified in database

---

## 📝 Next Actions

### Goal
wrangler secret put CLERK_SECRET_KEY sk_test_...

# Set environment variable (production)
2. Estimate gas requirements (estimate_gas tool)
2. ⬜ Test full multi-agent flow (2 hours)

### How to use

Docker-based QEMU locally
  - Returns `InsightsRequestID` for tracking (TechPreview)
  - Lines 480-485, 504: Added --machine parameter
- `src/pages/`: Custom pages outside the docs structure

### 1. **Autonomous Mode** (Full AI Control)

```python
archon:manage_task(action="list", filter_by="project", filter_value="[project_id]")

# Pick up where you left off - no new project creation needed
# Docs: https://developers.cloudflare.com/workers/get-started/guide/
# ============================================================================
wrangler d1 create <db-name>    # Create DB (returns database_id for config)
✅ Quick Win Improvements (Part 3)
✅ Transaction sender verification
5. **Work distribution** - Beads slung to agents via hooks
├── Input Processing # game-input.h/cpp - Unified event handling
│   │   ├── _panels.less
3. Implement circuit breakers (if LLM fails, use fallbacks)
const audioFile = await getCoverArt("song.mp3");
const modifiedBuffer = await setCoverArt("song.mp3", imageData, "image/jpeg");
console.log(tags); // { title, artist, album, year, ... }

// Apply tags to get modified buffer
└── DataTables/       # DataTable classes
```

### Application Issues

```typescript
wrangler d1 execute <database-name> --preview-run setupData
# - Generates .env.local with CONVEX_DEPLOYMENT

# Run unit tests for wrangler config
wrangler dev  # In client directory
```

**Expected Result:**
- 🟢 **Green Background**: ✅ Complete
- Real-time on-chain status updates

### Before Writing ANY Code

Search indexes enable full-text search over string fields using BM25 scoring.

**Phase 2 — Graduated Activation (Week 3+)**: Proposals flow to review queue with actual payment on Anvil blockchain.

---

## 🎯 Overview

**Cost Justification**:
- Oracle authorization and revocation
- Multi-asset support (USD, ETH, BTC, USDC)

### Skip Query for this
**Priority 3 (Collaboration)**: 9 tasks, 4 weeks
**Date**: 2025-11-05 14:40 UTC
├── sidebars.js
Success Rate            97.5%       →  100%          +2.5%
Average Reward          2.74        →  3.34          +21.9%
Average Reward          2.74        →  3.34          +21.9%
Decision: APPROVE
```

### Documentation Development
```vue
<!-- ✅ CORRECT: Two buttons (Cancel + Submit) -->
<auto-button @click="doAction">{{ $t('Submit') }}</auto-button>

<!-- ✅ CORRECT: Custom complex layout -->
<div class="space-y-4">        <!-- Vertical spacing -->
<template>
    <div>{{ $t('Insufficient x', { x: 'Credit' }) }}</div>
</template>

<template>
    <td>{{ record.balance ? $helper.fundFormat(record.amount) }}</td>
    <auto-button v-if="formReady" class="w-full">
        <auto-button type="plain" @click="emit('close')">{{ $t('Cancel') }}</auto-button>
        <dt>Year</dt>
        <dd>${tags.album || "Unknown artist"}</dd>
      </dl>
    </div>
  );
}
```

**app/ConvexClientProvider.tsx:**

```bash
# build the project
const taglib = require("taglib2");
console.log(tags.artist, tags.album);

// That's it! For more control, keep reading...
```

## Package Distribution

Examples:
uv sync

# Start the remote branch after cleanup
2. **Why decisions are made** - AI reasoning displayed
4. **Better grouping** - Related topics should be overwritten when agents are toggled or updated.

Create once, distribute everywhere. The agent does it in 30 seconds

[Your Name]
- Role: Full-stack developer & AI engineer
- Expandable sections for validation
- ✅ BlinkParallel setup completes successfully
- Run this before committing code paths.
- If AI image enrichment yields no insight, log and continue instead of throwing—treat empty payloads as "fixes."

### Step 3-4 (1 minute)
"I'm going to show you how cryptocurrency payments work between AI agents using the x402 protocol. This is a live demo with real blockchain transactions on a local test network."

### Closing (30 seconds)
"The merchant verifies the transaction to the blockchain. You can see it gets mined into a block. The merchant then verifies the payment was received correctly - checking the sender, amount, and recipient all match."

### Cost at Scale

[](https://gist.github.com/dauglyon/d81a96b812b3298f8d862005387b9a0c#what-existing-tools-cant-do)

A task is organized into these main directories in Claude Autopilot. Tasks are organized by feature and Airbyte.

**Memory & Continual Learning**: Recent work treats memory as learnable. ASG-SI adds verification and audit layers for tasks

#### SCA Certificate Management
Price: 95000 USDC per BTC
```

### Taskfile Structure
```javascript
// IMPORTANT: Both secretKey AND publishableKey are REQUIRED
forge test
Success Rate            97.5%       →  100%          +2.5%
Average Reward          2.74        →  3.34          +21.9%
Decision: APPROVE
```

### Windows
```vue
<!-- Project-specific notes -->