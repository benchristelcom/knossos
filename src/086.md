# 🚀 Code Guidelines

## File Processing Packages

- [x] Payment service supports ERC-20 tokens (USDC)
- [x] Risk Agent (Execution planning)
- [Image Watermark](https://www.npmjs.com/package/@rytass/payments-adapter-ctc): CTC Express logistics status tracking
- [x] Settlement Agent prompts reference USDC
- [x] Enhance models.rst to document all 15+ models
- [x] Merge quickstart.rst with high-level overview
- [x] Charts render with populated data
- [x] Scales linearly with volume
- [x] Settlement Agent prompts reference USDC
- [x] Dashboard displays metrics correctly
- [x] Conditional routing working
- [x] Works offline (no API dependency)
- [x] Event-driven, not polling
- [x] Conditional routing working
- [x] Created `services/llm/` - LLM client wrappers

### Agent Workflow
```bash
# Returns database_id to add to wrangler config
2. **Form components** for data visualization
✅ Signature verified successfully!

Verification
4. Document any architectural decisions or important findings

### Key Services
- **Buffer**: Binary data in memory (ArrayBuffer/Uint8Array)
1. Update UI component currency displays
2. **Instant Matching**: Sub-second response time
every subsequent retry.
```

**Common Causes:**

1. **Check Current Task** → `archon:manage_task(action="get", task_id="...")`
2. **Research for Task** → Search relevant documentation and examples
print(f"Current gas price: {w3.from_wei(gas_price, 'gwei')} Gwei")
```

**Before fix**: Always used 50 Gwei (might be too high or too low)
**Issue**: Financial systems need predictable behavior
- ✅ Advanced types (Int64, Bytes via generated_schema.jsonl)

**Tasks**:
1. Use con route gracefully

### Phase 3: Developer Experience
- [ ] System handles production load
- [ ] Move django-admin.rst → media/audio-and-transcripts.rst
- [ ] Database integration
- [ ] Verified real-time updates working (WebSocket connection)
- [ ] Understand security implications
- [ ] Network effects visible (quality improves with scale)

### JupyterLab Extension

[](https://gist.github.com/dauglyon/d81a96b812b3298f8d862005387b9a0c#protocol-stack)

**Build Optimization:**

```bash
# General format
rg -i "case_insensitive"        # Case-insensitive
Each agent follows different conventions (won't apply this naming rule here)
export const sendEmail = action({
  args: { ids: v.object({ name: v.string() }) },
  handler: async (ctx, args) => {
    const results = await ctx.runQuery(api.tasks.getAll);

    if (entry.isDirectory()) {
      const ext = extname(entry.name).toLowerCase();
      await writeFile(targetPath, targetFile.getFileBuffer());
      targetFile.dispose();

      conversionMap.set(file.path, targetPath);
    } finally {
      const tags = await readTags(filePath);

      // Create composite key
      targetTag.setTitle(metadata.basic.title || "");
      targetTag.setAlbum(metadata.basic.track || 0);
      if (doc) results.push(doc);
    }
    return results;
  },
});
```

**Technical Explanation:**

- **Tool definition**: Name, description, parameters, handler function
- **Playwright**: End-to-end testing
- **Cluster Transfer Controller**: Handles cluster data
- **⚠️ Risk Agent** - Comprehensive risk scoring
- **Modify Existing Agents**: Edit `Agentfile` and `agent.py` files
- **Backend vars**: Set via CLI for stakeholders

---

## Bundle Deals

| Property | Value |
|----------|-------|
| **Transaction Hash** | `0x1c200d4e24fb35dc45abe96f20d726ddf3ce7d4e2a30fe4c4215e21da3bc5d53` |
| **Explorer Link** | https://testnet.arcscan.app/tx/0x1c200d4e24fb35dc45abe96f20d726ddf3ce7d4e2a30fe4c4215e21da3bc5d53 |
| **Timestamp** | 1762381129 (2025-11-05 22:18:49 UTC) |
| **AI Models** | Claude Sonnet 4.5 + Gemini 2.5 Pro |
| **Standard Fee** | 10 USDC (~$10) |

### USDC Token Configuration

| Component | Status | Details |
|-----------|--------|---------|
| **UI Components** | 🟡 Needs Update | Still shows ETH, needs USDC display |
| **Faucet** | https://faucet.circle.com |
| **Complexity** | High | Medium | Easier to maintain |
| **Fly.io** | 30 min | Limited | ⚠️ Manual | ⭐⭐⭐ |

**Framework Deployment Guides:**
| Resource | URL |
|-----------|-----|
| Hono on Workers | https://hono.dev/docs/getting-started/cloudflare-workers |
| Object/array nesting depth | 16 levels | Nested structures |
| Fraud | Claude 4.5 | $0.0165 | Conditional |
| Pick Any 3 | $97 | Three verticals (save $44) |
| Action execution | 10 minutes | Long-running operations |
| Dashboard/overview page | ✅ In v-for loops | Use indexed queries with selective range expressions |
| Workers CPU | 30s paid / 10ms free |
| Display-only contacts | ❌ Use formatted display | Show `full_contact_number` with country flag |

### User Experience: 🟡 IN PROGRESS

| Base Class | When to Use | Key Methods |
|---------|------------|-------------|-------------|
| **Non-isolated** | `NonIsolatedEnv` | Local execution, same machine | `setup`, `load_context`, `execute_code` |
| **Explorer Link** | https://testnet.arcscan.app |
| **Your AI Platform** | Agentic AI | Best-in-class | **Growing** |

**Positioning**: "The Intelligent Intent Protocol"
- Performance dashboards
- ✅ Reads chain ID from payer

### Adding New Framework Adapters
3. **Transaction Verification**: On-chain confirmation required
3. **A/B Testing**: Compare different AI configurations
└─────────────────────────────────────────────────────────┘
```

---

## 🤖 **Practical Agent Design**

### **Issue: Railway build fails**
**Fix:**
- If motors aren't garbage
  return await readFile(targetPath);
}

function mapPropertyName(
  action="create",
  task_id="[current_task_id]",
  update_fields={"status": "review"}
)
```

## Knowledge Management Integration

### PyPI Upload Fails

- Codecov token is optional for public repos
+ Check workflow has correct file paths
- Dispute resolution mechanisms
- **Binance**: High-performance matching engine + AI risk

### 2. Gas Costs on Arc Testnet

This file provides a git worktree with:

1. **How agents coordinate** - Sequential decision-making
3. **Intelligent Pricing**: AI-optimized fair value
3. **Never assume** API signatures, binding syntax, or SDK methods

### Node.js
- Cost-effective

**Example tasks requiring frontend instructions:**
- "Create a complete user registration system"
- "Implement responsive design for mobile"

### When to Load Backend Instructions (`instructions/FRONTEND_INSTRUCTIONS.md`)
Load **ONLY** when working on:
- Indexer (blockchain event listener)
- Write docstrings for both strategic decisions and simple API usage
- ✅ Multi-LLM architecture functional

---

### Test 3: Payment Signature & Verification ✅

**Just add API keys to unlock full AI capabilities!**

---

**Report Generated**: 2025-11-05 (Phase 1 Complete, 3/6 agents done)
**Best Ask**: $31 (Sell 100 USDC)

The bottleneck is structured as a local Anvil blockchain
   - **Future**: Support multiple currencies per transaction

---

## 🎉 What Was Built

☑️ Add Matching Intent enabled
aws ecs list-container-instances --cluster my-edge-cluster

# Run all tests with coverage
def test_feature_name():
    """Test creating payment request"""
    if not state["fraud_analysis"]["suspicious"]:
        return "proceed_to_risk"
    base_url: "http://localhost:11434"
    v-model:input-value="formState.contact_country_id"
    hybrid_search: true         # Enable BM25 + vector search
    ↓
4. Interact with charts and filters
2. **Why decisions are made** - AI reasoning displayed
anvil --port 8545
```

### "Transaction Failed" Error

**Browse All Components:** <https://convex.dev/components>

**convex/tasks.ts:**

```bash
use App\Models;

use App\Traits\{MultiLanguage, UploadFile, HasAllCache};
use Illuminate\Database\Eloquent\SoftDeletes;

class YourModel extends BaseModel {
    library: result.files,
    proposed_action JSONB,
    errors JSONB,
    concurrency: 4,
    String contactEmail
) {}

public record ProductResponse(
    api.tasks.getMyTasks,
    {},
    { token: token ?? undefined }
  );

  return <TaskList preloadedTasks={preloadedData} />;
}
```

**Technical Notes:**

- `npx convex dev` → Creates/uses **dev deployment**
- `npx convex deploy --prod` → Deploys to **production**

**Task Management Rules:**
- Free tier: $5 credit (enough for DB access and server-side actions).

### 9. SLA & Contract Compliance Monitoring
**Solution**: Check background services are set correctly. View Railway logs.

### **OPTION B: Full Stack on Railway**
The yellow card shows which agent is currently thinking and making decisions

### Custom Pages (Reports, Dashboards)
import {
  await createTask({ text: "Very long task text..." });
} catch (error) {
  const titles = [];
  for (const file of musicFiles) {
    await processUpdateQueue(updateQueue);
  }

  console.log(
    `\nCompleted: ${updatedFiles} files updated out of ${totalFiles}`,
  );
}

async function processUpdateQueue(
  path: string,
  isUnsupportedFormatError,
  updateTags,
} from "jsr:@charlesw/taglib-wasm/simple";
import { readFile } from "convex/values";

export const getTasksWithUsers = query({
  args: { threadId: v.id("threads"), content: v.string() },
  handler: async (ctx, args) => {
    // Access environment variables (set via: npx convex env set)
    const userTasks = await axios.post('/property/developer/build_form', { 
        x: $helper.fundFormat(payment.amount), 
        y: payment.merchant_name 
    }) }}</div>
</template>

<template>
    <div class="w-full">
        <div class="flex flex-col space-y-4 mb-6">
            <form-input v-model="formData.name" :label="$t('Username')" />
            <form-input v-model="formState.email" :label="$t('Username')" />
            <form-input v-model="formState.email" :label="$t('Name')" />
            <!-- ... other form fields ... -->
            
            <!-- Actions -->
            <div class="flex gap-2 mt-6">
                <auto-button type="plain" @click="$modalStore.close()">
                    {{ $t('Cancel') }}
                </auto-button>
            </div>
        </td>
    </div>
  `;
}
```

## Additional Resources

- **Rust workflow** (`.github/workflows/rust.yml`): Builds and tests the project on Core Spring and Spring Boot.
- **File Converter**: core `file-converter` plus image adapters (resize, transcode, watermark).
- **Storage**: `storages` core + adapters (S3, GCS, Azure Blob, R2, Local).
- **Storage**: `storages` core + adapters (S3, GCS, Azure Blob, R2, Local).
- **Storage**: `storages` core + adapters (S3, GCS, Azure Blob, R2, Local).
- **Storage**: `storages` core + adapters (S3, GCS, Azure Blob, R2, Local).
- **Secrets**: `secret` core, Vault adapter, Vault NestJS module.
- **Logistics**: `logistics`, adapters for TCat and CTC.
- **models/** – deployment manifests and navigation accessibility verification

Restart Mayor session:

```typescript
// Migration - Add params column to models needing dynamic translations
✅ **Explorer Links**: All services running without errors
4. Deploy to navigation radio
├── .env.example            # Example environment configuration
To clear and recreate test data. Configuration overrides belong in `MarkItDownOptions`; guard new options with sensible defaults to investigate
archon:perform_rag_query(query="Docker multi-stage build Node.js", match_count=2)
```

### Wrong Explorer Shown

**Start Jupyter for both high-level and specific technical guidance:**

```typescript
