# Conversations
4. **Blockchain Integration**: Transactions are sent and confirmed on Anvil
if settlement is guaranteed within 24 hours using my
Recipient: 0xf39Fd6e5...
✅ Transfer successful!

Block: 9635145
├── api.py                    ⬜ Add AI endpoints to FastAPI
   - `GET /ai/explain/{intent_id}` - Explain with reasoning
   - `POST /ai/analyze` - Full workflow analysis
   - `AutoGenAdapter`: Monitors inter-agent communication in AutoGen systems
   - Social providers: Google, GitHub, Microsoft Entra ID

Documentation & Tests:
1. Analyzes market: Current trades at $10,150
⚠️ **Natural Language**: Parsing NL intents and management
└── query-test.sh --reset --verbose chat

# Run specific test file
const modifiedBuffer = await Deno.readFile("album-art.jpg");
tag.setAlbum("New Album");
const propMap = audioFile.propertyMap();
app.use(rateLimiter, { name: "agent" });
export default app;
```

**Technical Explanation:**

- **Frontend**: Cloudflare Pages (global edge network)
- **Conditional Gatherer** (`pkg/gatherers/workloads/`) - Collects workload fingerprints (12h interval)
- **Payments**: `payments`, adapters for TCat and PaymentRouter
  - Exposes recommendations as Prometheus metrics for alerting
  - Create user presence indicators
  - **Dependencies**: TASK-013

- [ ] **TASK-015**: Build template UI components
  - ✅ Fixed: test.py now uses Docker compilation on Windows (test.py:133-134)
  - **Estimate**: 3 days
  - **Dependencies**: TASK-031

**NEVER**:
- 1,000 intents/day
- API Documentation: (if available)
- Gas estimation in all operations
- Advanced Analytics
- Getting featured on task
- Updates `faramesh-node-sdk` version
- Bookmarks and help text
- Monorepo powered by Yarn 4.9 (node-modules linker), Nx 21, and Lerna (independent versions) on Node.js 22 (`.nvmrc`).
- Prettier is configured via `.prettierrc` (120-column width, semicolons, single quotes). Run manually with `yarn nx format:write`
- When server is not needed.
- Keep feature logic that you use for all, it is reserved for deployment to see the exact scope string before committing.

## Project Structure

- `src/components/` - React components
- `send_lm_request()` / `send_lm_request_batched()`: Helper functions for socket communication

### Option 4: End-to-End Tests

Each agent context directory contains:
- `Agentfile`: Agent configuration using FastAgent/Docker syntax
- `npm run lint:fix` - Fix linting issues automatically
- `bun run lint:fix` - Fix linting issues automatically
- `use()` mounts components with unique names
- Dashboard shows AI reasoning
- Content-Type should be `application/wasm`
- No custom domain needed for Convex (frontend proxies requests)

**Transaction Costs**:
- Green bars for bids
- ✅ Error handling and timeouts.
- Prefer:
  - Run `gofmt` (or `go fmt`) and organize imports.
  - If something is not fact, dont write about this.

#### 3. Update App Routing

**Logging & System Monitoring:**
- `isNumber` - Digits only
- `calculate_match(bid_id, ask_id, price)` - Execute match
- `test_goal_velocity.py` - Empirical test proving Goal_Velocity is required

---

## Version Information
- **Components**: Functional components with React hooks, no class components
- **Critical Test**: Verify that explorer link appears BEFORE "Transaction confirmed" message
- Cancel intent data is stored correctly

### VS Code

After completing the monitoring stack functionality.