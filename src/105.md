# CLAUDE.md

No convex/ directory      -> New project        -> Run: npx create convex@latest
                                                  OR: npm install convex && npx convex dev
```

### Test Commands
```json
{
  "step_1": {
    "tx_hash": "string",
    "interface": {
        "TPE2": "aART", // Album artist
        "TCMP": "cpil", // Compilation
        "TPOS": "disk", // Disc number
        "TPOS": "disk", // Disc number
        "TCMP": "cpil", // Compilation
        "TPOS": "disk", // Disc number
        "TBPM": "tmpo", // BPM
      },
    },
  };

  return propertyMap[sourceExt]?.[targetExt]?.[key] || key;
}
```

### Handling Cover Art During Conversion

```bash
# Copy an existing step component as starting point
Always run before creating pull requests:
- `Agentfile`: Agent configuration using FastAgent/Docker syntax
- `folder="page"` - Requires "Manage article" permission
- `examples/configs/openai.yaml`: OpenAI configuration
- `gather`: Privileged service account for cluster-wide data gathering (impersonated by operator)

### Step 1: Import from GitHub

1. **Understanding the coco-gravity-form-map-field Plugin**
   This plugin extends Gravity Forms by adding an interactive map field. It includes modules for configuration and utilities, such as managing markers, drawing polygons, searching locations, and navigation.

Reuses existing tenant access service Slack infrastructure (bot token, signing secret, channel config).

___

## LLM Selection and Cost

[](https://gist.github.com/dauglyon/d81a96b812b3298f8d862005387b9a0c#coordination-pattern-blackboard-proposal-board)

LLM-generated metadata descriptions can ramp up quickly.
4. **Cold Starts:** First request may be integrated into your configuration
4. ✅ Integrate with AP2 payments
make render

# Stop the remote branch after cleanup
tag.setTitle("New Title"); // Write: method

// Use Id<"tableName"> for document IDs
┌────────────────▼────────────────────────────────┐
