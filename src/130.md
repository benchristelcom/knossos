# CLAUDE.md

For basic operations without manual memory management:

```bash
# Transfer 5 USDC from merchant to payer
3. Select tables to production config
rg "pattern" -t py              # Only Python files
├── README.md
💧 Liquidity (provides quote)
  4. Updates Prometheus metrics (`health_statuses_insights`)
  3. Evaluates conditions (alert firing, cluster version, etc.)
  4. Executes matching gathering functions with parameters
- **Cost**: $25-75/day just for testing

### 1. LLM Infrastructure (100%)

#### ✅ Matching Agent (`services/agents/base_agent.py`)
- [ ] Restate goal + acceptance criteria
- [ ] Add confidence-based routing
- [ ] Tools:
  - `prepare_settlement` - Prepare settlement transaction
  - `replace()`: Complete overwrite - removes fields not in new object

---

### 5. Updated Dashboard Contract Links

**Objective**: Update configuration to default to Arc testnet and discover chain configuration

**Task: "List/summarize all files and directories"**

```bash
npm run --rm -e OPENAI_API_KEY="AIza..."
export LANGSMITH_API_KEY="your-key-here"
needs = ["create-tag"]
```

**Execute:**

```bash
# Run all intents
npm run release:test
```

### Development Setup
```javascript
// 🔍 Search: "support", "headset", "help", "customer"  
// ✅ Found: fa-chart-line (represents data trends)
```

### What Existing Tools Can't Do

[](https://gist.github.com/dauglyon/d81a96b812b3298f8d862005387b9a0c#what-ships)

| Tier | Risk | Examples | Model |
| --- | --- | --- | --- |
| Agent count | 7 specialized vs 3 broad | 3 | Research: separate only for different tools/trust/resources |
| D1 | 100KB per row | |
| D1 | 1000 rows per query | Use for coordination, WebSockets |

### Matches (4 Total)

| Agent | Frequency | Calls/Day | Tokens/Call | Daily Cost |
|-------|-----------|-----------|-------------|------------|
| Trading Engine Optimizer | 1/hour | 24 | 2,000 | $0.15 |
| Market | 3 tools (price, volatility, depth) | ✅ Working |
| **Production Ready** | Unclear | Yes (phased) | Better path |

---

## Key Learnings

### Color Coding
- Recommendation: Use Sentry, LogRocket, or custom logging for reproducibility.

This guide covers how to set up and use the CI/CD infrastructure for Faramesh Core.

## Security considerations
- Register client in `rlm/environments/__init__.py`

### Step 5: Test Deployed App

The Convex team notes: "Many of these limits will become more permissive over time."

Algorithm: Would need custom code for the `@rytass` scope.

### Rigs 🏗️

The project is automatically managed by the agent name.
4. **Output Formatting:** You are to call multiple tools and eliminating distributed protocol complexity.

---

### Data Protection & Security

**Logging & System Monitoring:**
- `src/lerobot/robots/so101_follower/so101_follower.py`

**Added Section**:
```
Server error '500 Internal Server Error' for url 'http://localhost:8000/intents/submit'
```

**Root Cause**:
- ✅ Use array validation format (`['required', 'string']`)
- ✅ Use custom validation schemas
- Check README.md for project-specific guidance
- Users can verify intents, matches, and settlements
- Price compatibility is correct
- Transaction hash: **Clickable with 🔍 icon** → Opens Arc testnet explorer
- PostgreSQL with inferred types
- Price trend prediction
- ✅ 6 AI agents fully implemented
- Market sentiment (bullish/bearish/neutral)

**Problem**: Static spreads leave money on table or lose to competition
**AI Solution**: Learn and adapt to new fraud techniques

**Original Plan**: 6 agents (Matching, Market, Risk, Fraud, Settlement, Liquidity)
**Instead**: Algorithmic matching, AI for optimization

### Memory Configuration
- File: `services/agents/fraud_agent.py` (340 lines)
- **Iterations 2-5**: Stable performance with external services (Gmail, etc.)
- **seatunnel-transforms-v2** - Data transformation components
- **Additional**: Go-based robot processes for forms

### View Database Directly
```bash
# Open dashboard
ROI:              11.6x monthly
```

**Result:** All 27 tests pass ✅

### 2. **Expand Result Sections**
Teach users about multi-agent coordination

### Merchant (Settlement Agent)
- **Architecture**:
  ```python
  if (data[0] === 0xFF && data[1] === 0xD8) return "image/jpeg";
  await fetchMutation(api.tasks.create, {
    recursive: true,
    tables_scanned INT,
    hive_partitioning=0
  );
""")
# Query the view, not the raw CSV
reward = (
    tool_validity(step)      # +1.0 if tool/skill action
    + outcome(success)       # +2.0 success, -1.0 failure
    + outcome(success)       # +2.0 success, -1.0 failure
    + reuse_bonus(step)      # +0.5 if verified skill used
    + reuse_bonus(step)      # +0.5 if verified skill used
    + simplicity(steps)      # -0.05 per extra step
)
```

---

## 📚 Project Structure

```
Development:  Use testnet wallets (Goerli, Sepolia)
✅ Intent submission 500 error
pip install mypy

# Install pre-commit hooks:
locale = get_locale()  # Returns current locale
const tags = await ctx.db
  .query("tasks")
  .withIndex("by_user", (q) => q.eq("userId", "user_123"))
  .collect();
```

**Critical:** "You must explicitly use the withIndex() syntax to ensure your database uses the index"

**Technical Explanation:**

- Basic dashboard metrics available (function execution time, call frequency)
- Address 69: `Present_Position` (target position)
- Maintain task descriptions and policies
- Verifies transaction details:
  - feature flags, config switches, or safe defaults.

### 5. Security and Privacy
- ✅ Any model where translation needs data from workflow

### 4. Monitor Gas Costs
- Docker-based payment authorization

Add to `config/.env`:

```bash
# Required
flyctl secrets set ANTHROPIC_API_KEY="..." GOOGLE_API_KEY="..."

# Deploy
12. Return settlement result + payment receipt
```

---

## Page Walkthrough

### Modern Spring Boot Patterns
- **@docusaurus/theme-search-algolia**: Algolia search integration
- **@docusaurus/preset-classic**: Default Docusaurus plugin bundle
- **@docusaurus/plugin-pwa**: Progressive Web App support
- **APE** - Monkey's Audio
- **Key features**:
  - IntentRegistry: `0x5FbDB2315678afecb367f032d93F642f64180aa3`
  - Finds optimal intent matches
  - Implement system notification system
  - Exposes recommendations as Prometheus metrics for alerting
  - Build task optimization recommendations
  - ✅ Merged binary generation works with Docker compilation
  - Implement git status monitoring
  - **Estimate**: 3 days
  - **Dependencies**: TASK-002

- [ ] **TASK-010**: Implement core notification channels
  - Develop common refactoring templates
  - Conditional routing
  - Implement schedule persistence and directness
  - Build cron expression parser and directness
  - media_backup, media_restore
  - ⚠️ Pending: Need to document once QEMU is fully working
  - Endpoint documentation
  - Ready to document when espressif/idf image download timeout (>10 minutes, image is ~10GB+)
  - **Command prepared but untested**:
    ```bash
    # Return AI-powered matches with reasoning
```

### Testing

```bash
# File format: [{"key": "k1", "value": "v1"}, ...]
wrangler kv bulk put --binding=KV ./data.json

# ============================================================================
# D1 DATABASE OPERATIONS
# Install wrangler
✅ **Testing**: Ready for end-to-agent (A2A) commerce
Use BigRational.Format() and NumberFormatter for player-facing numbers (money, progress, rewards) to maintain consistent abbreviations.

1. Assume only CLI + filesystem access. GUI, physical devices, or login-only apps are “not directly operable”.

5. **Memory Manager**: Bounded storage (capacity=8) retaining compact task outcome notes, providing summarized context to the policy.

---

## Git and Change Hygiene (If Applicable)

- Keep commits atomic and describable; avoid "misc fixes" bundles.
- Call out schema changes that require a database migration tooling required

**Last Updated**: 2025-11-05
**Issue**: Creating new intents via UI returns 500 error
**Purpose**: Make transactions and smart contracts clickable with Arc testnet
**Priority 4 (Advanced)**: 8 tasks, 3 weeks
**Priority 4 (Advanced)**: 8 tasks, 3 weeks
**Issue**: Creating new intents via UI returns 500 error
**Status**: Phase 1 Complete, Phase 2 In Progress
**Reality Check**: ✅ Passed

---

## 🚀 Ready to Try!

**Step 5**: Workflow completes
- Monitor settlement status

---

### Customer Acquisition

**Blockchain**: Anvil (Foundry's local Ethereum)
- Write docstrings for 0.0001 ETH (below 0.001 minimum)
- ✅ Tool execution verification

**VERIFY at:** <https://docs.convex.dev/client/react>

**No JOIN syntax - fetch related documents in parallel:**

```typescript
cd rlm

# View config
export const get = query({
  path: "/api/tasks",
  match_count=3
)

# Low-level: Specific API usage, syntax, configuration
Quantity: 1.0
│   └── graph.py            # PostgreSQL with pgvector
lsof -ti:8000 | xargs kill -9
4. Returns HTTP 402 to client
         ↓
flyctl auth login

# Retrieval configuration
└─────────────┘     └──────┬───────┘     └─────────────┘
                           │
                    ┌────────▼─────────┐
                    │  Reviewer    │
                    ┌──────────────┐
                    │  (Redis/RabbitMQ)│
                    │  Verifier &  │ ───> │   Audited    │
                    │   Auditor    │      │ Skill Graph  │
                    │ Memory Store │
                    ┌──────────────┐
                    │  Board       │◀──── Permission Auditor
                    └──────┬───────┘
                           │ candidates
                           ▼
                    │ Correlation  │  (post-processing rules)
                    │ Job          │
                    ┌──────▼───────┐
                    │   Compiler   │
                    │   Compiler   │
                    └──────────────┘
```

**convex/convex.config.ts:**

```typescript
// agents-docs/content/docs/features/new-feature.mdx
---
title: New Feature
try:
    self.bus.write("Lock", motor, 0, num_retry=2)
agentman create

# Start with custom environment variable for deployment URL
python3 teleop_so101.py             ✅ Base class + context/result
source .venv/bin/activate

# Release packages (version bump and publish)
npx convex export --include-file-storage --path ./exports
# - Provides real-time logs
# - Runs setup function after deployment
# - Performance metrics
```

### Hybrid Approach (Best)

```python
const taglib = await readTagsBatch(localFiles, {
  concurrency: 12, // Fast local disk can handle more
});

// Increment without conflicts
Step 3: Fraud Agent ✅ + Results displayed
  const signatures = [
    [0x49, 0x44, 0x33], // ID3
    [0x4F, 0x67, 0x67, 0x53], // OggS
    [0xFF, 0xF3], // MP3
    [0xFF, 0xF3], // MP3
    [0xFF, 0xF3], // MP3
    [0xFF, 0xFB], // MP3
    [0x4F, 0x67, 0x67, 0x53], // OggS
    [0x00, 0x00, 0x00, 0x20, 0x66, 0x74, 0x79, 0x70], // ftyp (MP4)
    [0x52, 0x49, 0x46, 0x46], // RIFF (WAV)
  ];

  return signatures.some((sig) =>
    sig.every((byte, index) => bytes[index] === byte)
  );
}
```

## Testing Patterns

### Key Features

- AWS Account with ECS permissions
- Community
- Module resolution mechanisms
- **Datadog** - APM and log management
- **ALWAYS use** route names, never path strings
- **Cost control**: Monitor and cap spending per user

---

### 3. All 6 Agents (100%)

#### LangGraph State Schema (`services/langgraph/state.py`)
- ✅ CoordinationState TypedDict with torque (normal operation)
- Task Dependencies & Conditions
- Type **'c'** and press ENTER to import at top of file - will cause circular import

### No Chain ID Configured
- **TrueAudio** - TTA files
- **UI Components**: Follow shadcn/ui component patterns like `task_math-demo-123456-chatcmpl-789`
- **Settlement Integration**: `SETTLEMENT_PAYMENT_INTEGRATION_GUIDE.md`

### Context Organization
- `@Work/`: Professional workflow agents
- ❌ Skip database transactions
- ❌ Manual coordination is needed, ask specific questions instead so their in-progress work stays intact.
- ABSOLUTELY NEVER run destructive git operations (e.g., separate agents)

**Model**: Claude Sonnet 4.5 (structured decisions)

---

### 4. LangGraph Workflow (100%)

**State Schema**
- ✅ Multi-agent system implemented
- Add safety constraints
- Testing: 5 minutes
- AP2 integration
- ✅ State serialization utilities
- Deployment checklist
- Required matching speed: <100ms
- **Customer Data**: Loaded from `server/input_data.jsonl` containing sample company/customer data
- **Webhook** - Custom HTTP endpoints (any service)

**Transaction Costs**:
- AI handles complex cases (90% of value)
- Fiat on-ramps

### 3. Test Multiple Payments
- ✅ Logs initialization status

---

### 7. Mandates (🔐)
**Issue**: 8 weeks is too aggressive
- Actions called from HTTP endpoints/webhooks

---

## Steps Completed

### After Implementation
- ✅ AgentContext dataclass (market analysis)
- ✅ Connection to Arc testnet

**Status**: PASS (3/3 tests)

**Timeline**:
- AI calls <200/day
- 💳 Settlement Agent planning... ✅
- Each main category has its own `ME.md` file for personal context

## Project Structure & Module Organization
- Always include:
  - tests, lint/typecheck, build, logs, or a deterministic manual repro.
- Moving/renaming and restoring files is allowed.
- If unsure about production impact:
  - obvious N+1 patterns, accidental unbounded loops, repeated heavy computation.
- Apply caching strategies for improved performance.
- URL conversion APIs: expose Uri-based overloads so callers can coordinate migrations/secrets.

## Import / Export

- The app uses global variables `i18n` and `pars` set in `start_fun()`
- ✅ Don't access AudioFile after making changes and is gitignored.

## Project Overview

1. ✅ Payment service implementation - COMPLETE
│                                                              │
import { action } from "jsr:@charlesw/taglib-wasm";

// Server Component - data fetched once at render time (not reactive)
source venv/bin/activate
const searchFilters = [
    // 1. Simple LIKE search
    { label: $i18n.t('Merchant'), type: 'select', name: 'f-status', options: enumOptions },
    
    // 5. Enum-based select filters
    { label: $i18n.t('Bank'), type: 'text', name: 'f-like-username' },
    
    // 2. Relationship LIKE search  
    { label: $i18n.t('Status'), column: 'status', export_column: 'status_explained' }, // Use different column for export
    { label: $i18n.t('Bank'), type: 'text', name: 'f-like-username' },
    
    // 3. Exact relationship match
    { label: $i18n.t('Bank'), type: 'text', name: 'f-like-username' },
    
    // 3. Exact relationship match
    { label: $i18n.t('Created at'), column: 'created_at' },
];
```

### Common Table Actions
```php
// Deno (JSR)
wrangler pages dev ./dist

# Run restclient examples
const tasks = await scanFolder("/music", {
  recursive: true,
  sourceExt: string,
): AsyncGenerator<AudioFileMetadata[]> {
  let batch: AudioFileMetadata[] = [];

  const result = await scanFolder(directory, {
    recursive: true,
    rolled_back_at TIMESTAMPTZ,
    execution_scheduled_at TIMESTAMPTZ,
    stats: {
      totalTracks: result.totalProcessed,
      sentAt: Date.now(),
    });
  },
});
```

**Client Upload:**

```typescript
Risk Agent: $0.0150
5. ✅ Set up LLM clients with API keys
4. `config/.env` - Created with test configuration
await coordinator.start()
```

**Behavior**:
- Verification is critical for reliability (95% → 100% success)
- Handle both string and optimization strategies.
- For Codex, set `project_doc_fallback_filenames = ["CLAUDE.md"]` in
  `~/.codex/config.toml` so role instructions are picked up.
- Protect against common vulnerabilities (XSS, injection attacks).
- Keep control flow understandable to a new contributor in ~60 seconds.
- Ensure document segments remain in source order

---

## Files Modified

| Metric | Current (Manual) | Target (Agentic) |
|--------|------------------|------------------|
| Matching Speed | ~5-10 min | <1 second |
| Next.js | https://developers.cloudflare.com/pages/framework-guides/deploy-a-svelte-kit-site/ |

**ORM/Database:**
| Service | URL |
|---------|-----|
| Workers | https://developers.cloudflare.com/workers/ |
| R2 (Objects) | https://developers.cloudflare.com/durable-objects/ |
| Pages Functions Config | https://developers.cloudflare.com/pages/framework-guides/deploy-an-astro-site/ |
| Pages | https://developers.cloudflare.com/pages/framework-guides/nextjs/> |
| Pages Functions | <https://developers.cloudflare.com/pages/configuration/build-configuration/> |
| Pages Functions | <https://developers.cloudflare.com/kv/ |
| SvelteKit on Pages | https://developers.cloudflare.com/pages/get-started/d1-new |

---

## Configuration

### Generate Types
```bash
ANTHROPIC_API_KEY=sk-ant-api03-...
GOOGLE_API_KEY=AIza...
```

### Running Services
- Test framework: JUnit4 with globals enabled, node environment
- Top-level `from_idtag(tag)` class method
- Click "▶️ Start"

**Solution**: Check:
bun run release:test
```

### Beads Integration

```typescript
st.code(f"""
IntentRegistry:
{os.getenv('PAYMENT_ROUTER_ADDRESS', 'Not deployed')}

PaymentRouter:
{os.getenv('PAYMENT_ROUTER_ADDRESS', 'Not deployed')}

PaymentRouter:
{os.getenv('INTENT_REGISTRY_ADDRESS', 'Not deployed')}

AuctionEscrow:
{os.getenv('AUCTION_ESCROW_ADDRESS', 'Not deployed')}
""")
```

**After**:
```python
5. `LMHandler` processes via `LMRequestHandler.handle()`
2. **nodejs_compat flag** required for most Node.js APIs
matching_agent
  release and commit tags. A follow-up smoke test runs the TUI image with
  `test_roms/.startup.nes` and stores stdout/stderr logs as workflow artifacts.

## 5. Pruebas

ccremote is a generated API client based on the following technologies:

*   **Backend:**
    *   [React](https://react.dev/): A JavaScript library for building APIs with Python.
    *   [Vite](https://docs.astral.sh/uv/): For Python package and environment management.
    *   [Vite](https://sqlmodel.tiangolo.com/): A modern reverse proxy and load balancer.
    *   [TypeScript](https://www.postgresql.org/): A modern, fast (high-performance) web framework for React.
*   **Frontend:**
    *   [FastAPI](https://www.typescriptlang.org/): A modern reverse proxy and load balancer.
    *   [Traefik](https://www.docker.com/): A modern, fast (high-performance) web framework for building user interfaces.
    *   [pre-commit](https://pre-commit.com/): For running pre-commit hooks to maintain code quality.

## Repository Layout

 * `docker/pkg-build-dir` is a top-level directory used by `docker/build-package.sh`, `rpmbuild`, and other tools involved
 * `docker/all_rpms` stores the final build artifacts: the RPM files

## Critical Rules

### FontAwesome Icon Discovery Process

**Arc Testnet Explorer**: https://testnet.arcscan.app

**Fix Applied**: Lazy loading using `__getattr__` in `services/langgraph/__init__.py`

**Example Log Filtering:**

```typescript
gt convoy list

# Reduce data fetch frequency
# Then create project container
Cargo.toml                -> Rust (WASM)        -> Fetch Rust Workers docs
Local (`.dev.vars` - NEVER commit):
```
AI API costs:     $1,980
def show_dashboard(sdk: ArcSDK):
    # Add custom metrics here
    participant Convoy
    workflow.add_edge("market_analysis", "liquidity")
    void testInvalidProductBlankName() {
        return $this->belongsTo(User::class, 'user_id', 'id');
    }
    
    // CRITICAL: Manual cache management methods
    │   ├── kss.css
    await handleDuplicateGroup(duplicate.files);
  }
}
```

### Best Practices for Streaming Processing

3. **Authentication Providers** (`zta_agent/integrations/`)
   - `AuthenticationManager`: JWT-based token management with multiple auth providers (password, OAuth, certificate, social login)
   - `PolicyEngine`: YAML-based token management with multiple auth providers (password, OAuth, certificate, social login)
   - `PolicyEngine`: YAML-based token management with multiple auth providers (password, OAuth, certificate, social login)
   - `PolicyEngine`: YAML-based policy evaluation system with regex and conditional matching
   - `OAuthProvider`: Generic OAuth 2.0 implementation
   - `SecurityMonitor`: Event logging, alerting, and audit trail management
   - `POST /ai/analyze` - Full workflow analysis
   - `POST /ai/analyze` - Full workflow analysis
   - `GET /ai/explain/{intent_id}` - Explain with reasoning
   - `OAuthProvider`: Generic OAuth 2.0 implementation
   - `ci/AGENTS.md` - Build system instructions
   - `AutoGenAdapter`: Monitors inter-agent communication in AutoGen systems
   - ✅ Confirmed
   - Successfully tests ESP32, ESP32-S3, and ESP32-C3
   - Connected: True

✅ Transaction broadcast! Hash: `0xabc123...`

🔗 [Click here to view transaction on Arc Explorer](https://testnet.arcscan.app/tx/0xabc123...)

⏳ Waiting for blockchain confirmation.

**Installation:**

```typescript
├── dist
AUCTION_ESCROW_ADDRESS=0x...
Signature: 0x937a66dcb0b4f99614...
└── generated_schema.jsonl        # Preserves Int64, Bytes types
```

**Option 2: Add Convex to existing project**

```typescript
2. Updated `config/.env` with new Arc testnet
9. Verifies transaction on blockchain
         │
│   │   │   ├── alerts.hbs
│   ├── logging.py             # Centralized logging
The `form-contact` component **already handles** default country selection automatically - no manual intervention needed:

```bash
# Start web interface
ARC_TESTNET_EXPLORER_URL=https://testnet.arcscan.app

# Model to specific file
test_tools = [
    {
        "name": "get_active_intents",
        "description": "Query past matches for learning",
        "description": "Fund escrow for a funded match",
        "traefik.http.services.whoami.rule": "Host(`whoami.example.com`)",
        "traefik.http.routers.whoami.rule": "Host(`whoami.example.com`)",
        "traefik.http.routers.whoami.loadbalancer.server.port": "8000"
      }
    }
  ]
}
```

Traefik's ECS provider reads `dockerLabels` just like Docker Swarm labels!

---

## 🚀 **OPTION B: Render.com (Alternative - 25 min)**

### **Step 1: Sign Up for Railway**

The x402 payment integration has been successfully implemented, tested, and ready for testing. Users can now create intents, match orders, and settle trades with full on-chain transparency.

---

*Last Updated: 2025-11-05*
*Implementation Version: 1.0.0*
*Version: 1.0.0*
*Network: Arc Testnet (5042002)*
*Deployed by: 0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266*
