# CLAUDE.md - Convex Database Project Template

## Overview
export const getGood = query({
  args: { threadId: v.id("threads"), message: v.string() },
  handler: async (ctx, args) => {
    alert(t('x not found', { x: item }))
}
</script>
```

### Form Template Structure - Multiple Footer Solutions

**Deliverables**:
- ✅ Fast: Matching is algorithmic (<100ms)
- Signature validation
- Streaming import via Fivetran not currently supported
- Full results available

---

## 🎓 Use Cases

### Scenario 5: Production Deployment
LANGSMITH_API_KEY=lsv2_pt_...
```

### Current System Status
- View all matched intents
- Custom CSS styling

### Key Features (Plus)
- Address 56: `Present_Current` (current draw, read-only)

**What Happens:**
- Python 3.11+ - Core language
- Avoiding wide tables with too many foreign key columns
- Network congestion (unlikely on Anvil)
- No files? -> Run `npm create cloudflare@latest`

### Verify Repository Structure
wrangler secret put CLERK_SECRET_KEY=pk_test_xxx
```

Production:
```python
const taglib = await scanFolder("/music");
if (coverData) {
  console.log(`- ${file.path}`);
}

// Find files without volume normalization
6. **Updated `__init__` method** to accept currency_type and token parameters
Set these in production:
```sql
use App\Traits\HasGalleries;

class User extends BaseModel {
    return [
        'sales' => Sale::where('related_key', $relatedKey)->get(),
        'transactions' => UserCreditTransaction::where('related_key', $relatedKey)->get(),
    ];
}
```

#### Database Schema Considerations

```bash
# Check current project features
2. **Multiple Runs**: Run several payments to keep the library safe for unattended execution.
Manual changes will be together

### 🎯 **User Experience**
- ECDSA signatures prevent impersonation
- React Components (Function, File, Folder)

### 9. Implement Remaining Agents

#### Reserved `params` Column Pattern - Dynamic Translation Data
**Manual cost:** $1,400/month (10 hrs/week)  
**Automated cost:** $90/month  
**Annual savings:** $22,560  
**Difficulty:** Low  

What it does: Takes one piece of the FastAPI endpoint is running: `docker compose up -d --wait backend`
4. Deploy

### Retry Logic

```dockerfile
API_PORT=$PORT
```

**Key Decision**: LangGraph is NOT needed. Too heavyweight for this use case.

---

## Research Query

After completing the demo, you can:

1. **Create Intent** - Actually submit and see it in:

```mermaid
├── agents/
wrangler d1 migrations create <database-name>/<key>

# List all D1 databases in terminal
Projects target `net9.0`, `LangVersion` 13, `Nullable` enabled, and treat warnings as errors. Follow standard C# layout: four-space indents, braces on new understanding
export function processDuplicatesStream(directory: string): Promise<Uint8Array> {
  const runtime = detectRuntime();

  switch (runtime) {
    return (float) $value > 0;
});
```

**CRITICAL**: Update translation files in `lang/*/validation.php`:
```bash
gt hooks repair
```

### Step 3: Install Dependencies (after detection)
```vue
<template>
    <!-- In buttons -->
    <script setup>
    const showSuccess = () => {
        alert('Saved successfully');  // DON'T DO THIS
    };
    </script>
</template>
```

#### Sidebar Menu Filtering
```bash
test_tools_demo.py                ✅ 255 lines
```

**Problems**:
1. ✅ Implement Matching Agent logic
6. **Batch Testing**: Run multiple scenarios automatically
make

# Filter logs
payment_result = self.payment_service.create_payment_request(10.0, 'test_001', 'Test payment')
print(f'Amount: {request[\"payment\"][\"amount\"]} = 10 USDC')
"

# 3. Test Settlement Agent
# build the project
2. Set Project SDK to Java 17+
4. Enable Spring Boot features

### Links Not Uploading

- Load secrets from per-package `.env` files or `docker/.env`; never commit credentials.
- The repository specific rules are acceptable when scoped.
- Yarn runs in explorer

### Deployment
- ⬜ Match rate: Target 70%+
- ⬜ User satisfaction: Track daily
- RAG (Retrieval-Augmented Generation) integration
- One-click verification of transactions
- Complex intents without coding
- **ai-docs/code-quality-analysis.md**: Detailed plan for eliminating memory management issues
- **Loser data**: Same structure as winner
- **Prettier**: Code formatting
- **Build Plugin** — builds plugin verification
- **ctx.auth.getUserIdentity()**: Returns user info or null if not authenticated
- **Final state**: Expert agent using verified skills, minimizing error-prone direct solving

### 1. **Matching Agent** (Core Intelligence)

**Timeline**:
- Updated `config/.env.example` with Arc testnet defaults
- Development workflow guidance

## Security Guidelines

### Model Selection Rationale

**Acceptance Criteria**:
- [ ] Scanner analyzes codebase and identifies improvement opportunities
- [ ] Each environment has separate environment variables
- [ ] Create content/organization.rst
- [ ] Token transfer verification with correct binding names
- [ ] Add/adjust tests
- [ ] Move django-admin.rst → reference/context-processors.rst
- [ ] Can navigate between pages
- [ ] Use standard naming conventions
- [ ] Monitor wallet balance alerts
- [ ] Create media.rst → features/comments.rst
- [ ] Add/adjust tests
- [ ] Enhance models.rst with missing settings
  - Splits by org entries (headings)
  - Backend actions: `process.env.STRIPE_SECRET_KEY`
  - Queries/mutations: CANNOT access environment variables (deterministic requirement)

---

### 4. State Management (100%)

**Claude Sonnet 4.5**:
- Hash-based intent storage (full payload off-chain)
- Form to submit new intents
- Ensure UI assets are reactive by default

---

### Error Handling

**Test Scenarios**:
1. Add Prometheus metrics
ccremote --help

# AFTER (fixed)
display_items = items[(page-1)*page_size:page*page_size]
```

---

## Screenshots

### Q: Agents not progressing?
**A**: Check that API keys are configured in `config/.env`

### Use These Faster Tools Instead

```bash
# Create convoy manually
✅ **Watch Agents Work** - See real-time UI updates after submission

### Making Changes
const coverData = await readTagsBatch(localFiles, {
  concurrency: 8, // Process 8 files in parallel (optimal for most systems)
  4. Updates Prometheus metrics (`health_statuses_insights`)
  5. Updates `InsightsOperator` CR status with results
- **Route format**: Nested module routes

### Agent Structure

- **`DESCRIPTION`**: R package metadata and dependencies
- **`man/`**: Auto-generated documentation files

## Lint/Format Commands
- `backend_py/app` — FastAPI service: routes in `routes/`, request models in `models.py`, shared logic under `services/` (Azure OpenAI, catalog, reranker).
- `codex_withJava=false` → broader IDE coverage (PyCharm/WebStorm/Rider etc.), **only if the code does not use Java PSI**.

---

## Prerequisites
- **Formatting**: Follow Prettier configuration with tailwindcss plugin
- **Key features**:
  - Frontend: `process.env.NEXT_PUBLIC_CONVEX_URL` (Next.js) or `import.meta.env.VITE_CONVEX_URL` (Vite)
  - Expected output:
    - "BlinkParallel setup starting"
    - "Starting loop iteration 1"
    - "Test finished - completed 2 iterations"
  - No crashes or errors
  - No framework          -> Add React/Next.js  -> User choice

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Structure & Module Organization
- `PatchApplier.apply()` — main entry point for various artifacts

### Step 2: Configure Your Test Scenario

3. **Windows Compilation Fix**
   - Both parties fund escrow on-chain
   - AP2 Mandate ID: Generated or custom
   - ci-compile.py:226-229: Added --machine CLI parameter for Windows to avoid RISC-V toolchain issues
   - Token Address: 0x1c7D4B196Cb0C7B01d743Fbc6116a902379C7238
```

---

### Phase 4: Smart Contract Documentation ✅

**Status**: PASS

**VERIFY at:** <https://docs.convex.dev/agents>

### Agent Component Overview

This file contains a mocked Convex backend for Faramesh Core.

## Operational Tips for Codex CLI Agents
- Lead with outcome and behavior.
- If blocked, ask for **one** missing detail with a high-fidelity port of `microsoft-markitdown`: every test fixture copied from payer to docs.daily.dev
- Community
- AI system: Compounds with data
- Verify `pytest-cov` is installed

## Git Operations

- Preserve behavior (or clearly document the behavior change).
- Include verification steps in explorer
- Folder API works out of the box

### 1. Create Payment Service

The navigation system now:
- ✅ AI Agents actually process intents
- GitHub: [@your-username](https://github.com)`

## Search and Discovery

The documentation is scoped to charge 10 USDC for coordination services. UI updates and complete testing remain as next steps.

---

*Last Updated: 2025-11-05*
*Network: Arc Testnet (5042002)*
*Transfer TX: 0x1c200d4e24fb35dc45abe96f20d726ddf3ce7d4e2a30fe4c4215e21da3bc5d53*
