# Instructions for AI Agents

Formulas are mirrored to S3 for their services on-chain.

---

## Next Steps

### 1. Test x402 Payments
- Prepares transaction parameters for all branches
- Consider: price overlap, asset types, timing, quantities
- ❌ No explainability for decisions

### Unit Testing with JUnit 5

```typescript
inkeep push
```

### Hybrid Approach (Best)

```bash
# Fetch and analyze data
Production:   Dedicated production wallet with 2 agents

---

### **Use Case 2: Intelligent Negotiation** (HIGH VALUE)

**Step 3: Generate Integration Code**

```php
// Fetch related documents in parallel
AGENTIC_AI_PROGRESS.md            ✅ This file
```

---

## 🎮 Control Modes

### 3. Context-Aware Loading
```
User Configuration
    fnm use

    # Run in Docker QEMU (BLOCKED - need espressif/idf image)
    return response.text
```

### 3. Payment Flow

```
┌─────────────────────────────────────────────────┐
5. 💳 **Settlement Agent** - Plans execution
const taglib = await readTagsBatch(files, { concurrency: 8 });
```

## Which API Should I Use?

- **Reading tags from one file?** → Simple API: `readTags()`
- **AutoGen**: Monitors agent communication through `validate_agent_communication()`
- **Processing many files?** → **Simple API: `readTagsBatch()` (10-20x faster)** or Folder API: `scanFolder()`
- **Type safety**: Schema automatically generates TypeScript types in `convex/_generated/`

---

### Queries (Reading Data)

**Hono (@hono/clerk-auth):**
```typescript
└── types.py                    # Type definitions and matching conditions
The system generates `resources/scripts/domain-enums.js` from Laravel models using:
```python
# Docs: https://developers.cloudflare.com/workers/get-started/guide/
# ============================================================================
wrangler kv namespace create <name>            # Create bucket
test_tools = [
    {
        "name": "calculate_spread",
        "ecs:DescribeTasks",
        "traefik.http.services.whoami.entrypoints": "web",
        "traefik.http.routers.whoami.entrypoints": "web",
        "description": "Fetch all active intents from the blockchain",
        "ecs:ListTasks",
        "description": "Execute settlement for a funded match",
        "description": "Query past matches for learning",
        "argmap": {"expr": "expr"}  # Maps task input to tool arg
    },
    "verified": True,
    "finalized": false
  }
}
```

---

## 🔒 Security Best Practices

### Example Structure
```
resources/scripts/
├── asg_si_demo.py

# Increase memory for testing
Security Verification:
- [ ] All tests pass (`flutter test`)
- [ ] Code is formatted (`dart format .`)
- [ ] App builds successfully on target platforms
- [ ] Implement rate limiting
- [ ] Handle conditional intents
- [ ] Implement `route_after_matching()` function
- [ ] Move video.rst → features/themes.rst
- [ ] Production branch triggers production deployment
```

### Linux
```bash
# Edit page refresh intervals in code
```

---

## Services Status

| Resource | Limit | Notes |
| ---------- | ------- | ------- |
| Data read/written per transaction | 16 MiB | Total transaction size |
| Market Intelligence | 6/day | 6 | 5,000 | $1.20 |
| Functions timeout | Edge runtime limits | Use Convex for heavy processing, not Pages Functions |
| Relationship discovery | Weekly | No | Changes slowly |

___

## Service Architecture

[](https://gist.github.com/dauglyon/d81a96b812b3298f8d862005387b9a0c#core-schema)

```typescript
PAYMENT_ROUTER_ADDRESS=0x5FbDB2315678afecb367f032d93F642f64180aa3
└────────────────┬────────────────────────────────┘
                 └─▶│  Proposal    │◀──┘
                    │  AGENT RUNTIME   │
                    ┌──────▼───────┐
                    │  Board       │◀──── Permission Auditor
                    └──────┬───────┘
                           ▼
                    └──────────────┘      └───────┬──────┘
                                                   │ reuse
                                                   └──────┐
                                                         ▼
                                               (back to Policy Agent)
```

### What Existing Tools Can't Do

[](https://gist.github.com/dauglyon/d81a96b812b3298f8d862005387b9a0c#research-foundation)

6. **Update Wei Conversion**: Lines 334, 470, 477-478
   ```python
   # Change ETH to 5.3.2
   └─> Payer signs with own key and sends ETH to merchant

To start off, in which casing should variables, constants, classes, functions, files, components, ... be named?

### Column Access

Templates are TOML-defined workflows stored in a local Anvil blockchain
   - **Price**: 95000 USDC per BTC
import DomainEnums from '#/domain-enums.js';
const statuses = DomainEnums.Deposit.status;

// Helper method (recommended)
PAYMENT_CHAIN_ID=5042002  # Arc Testnet (default)
```

### Directory/File Exploration

```typescript
// Translation keys in lang/*.json files
"use client";

test("action with external API", async () => {
  // Setup mock
  let processedCount = 0;

  for await (const filePath of scanDirectoryStream(directory, 50)) {
    // Process batch of 50 files
    participant Convoy
    chain_id TEXT NOT NULL,
    trackCount: result.results.length,
    total_count: 0
});

// NETWORK/HDD: Lower concurrency (4-8)
✅ Quick Win Improvements (Part 3)
Description: Payment for Arc Settlement Service
```

This starts:
- Check TESTING_REPORT.md
- Missing REST API documentation
- Revised Plan: $2/day = $60/month
- **Lack of Auditability**: No way to inspect what the agent learned or why a capability was added

The Arc Coordination System is **fully implemented and tested**, providing:

- **GameCore** (`src/game-core.h/cpp`) - Game logic, state management, player turns
- **ALWAYS add new icons** to `resources/scripts/admin/font-awesome.js` before using
- **Tools**: prepare_settlement, estimate_gas, verify_collateral
- **Never trust** client-side validation only
- **Log Stack**: Loki (configurable), Promtail, Syslog-ng
- **Current Agent** - Displays which agent is active

### Main Entry Point
- **Workflow**:
  1. Periodically queries OCM API for accepted cluster transfer requests
  5. Updates last reported timestamp
- **Total**: $2,180/month

**Gemini 2 (Agents)** - IN PROGRESS:
- [x] Matching agent tools work
- [ ] Schedule history and status are tracked
- [ ] System handles production load
- [ ] Test sorting and pagination
- [ ] 80% of intents parsed successfully (NL → structured)
- [ ] Create index_new.rst with new structure

#### File Upload - `UploadFile` Trait
**Location**: `services/agents/settlement_agent.py --server.port 8502`

---

## 📝 Final Reminders

on:
  test:
    collect: true
    const response = await myAgent.streamText(ctx, {
      threadId: writerThread,
    });

    return response;
  },
});
```

**Client Error Handling:**

```typescript
// Translation keys with parameters from parent models
"Transaction transfer credit out": "Transfer to :to_username",     // en.json

"Insufficient x": "Insufficient :x",            // en.json
"saveChanges": "保存更改",          // WRONG: camelCase
```

#### Contact Numbers - `HasContactNumber` Trait
**Solution**: Check that `PAYMENT_CHAIN_ID` is set in `.env`

```json
{
  "scripts": {
    "amount": "10000000000000000",  // Wei (0.01 ETH)
    "currency": "ETH",
    "Minimum of x characters": "最少 :x 个字符",
    "command": "codex",
    "address": "0xSettlementAgentAddress",
    "test:coverage": "vitest run --coverage"
  }
}
```

### Payment Submission

```bash
# Edit page refresh intervals in code
```

---

## 📊 MARKET DYNAMICS

### 1. Safe Event Log Parsing
7. Click **▶️ Start Workflow**
