# Django Cast Documentation Tasks

## Development Iteration Workflow

### Workspace Management
- Live data updates
- Refresh buttons on all branches
- Blockchain interaction unchanged

---

## 🚀 GO-TO-MARKET STRATEGY

### Key Services
- **sprites.md**: Background music system and memory usage considerations
- **Solution**:
    - Added --merged-bin and -o flag support to Docker wrapper (ci-compile.py:226-229)
    - Added artifact copying from container to host (ci-compile.py:296-297)
    - Fixed UTF-8 encoding in Docker output streaming (ci-compile.py:296-297)
    - Fixed UTF-8 encoding in Docker output streaming (ci-compile.py:308-338)

- [x] **Step 1.1**: Verify current `test.py` QEMU integration ✅
  - Available blocks (CodeBlock, ImageChooserBlock, etc.)
  - Creating custom blocks
  - Completes in < 2 minutes

1. Go to Convex Dashboard → Settings → Integrations
npx convex deploy --prod --project my-prod-project
```

### Test Frameworks
- **WavPack** - WavPack files
- **Dark border color** (`stroke:#333`) works well with all backgrounds
- **Status Indicator** - Shows running/idle state
- **Google** - Gemini 2.5 Pro
- **Gmail Newsletter**: Processes newsletters and execution engines

### Form Component Best Practices

**VERIFY at:** <https://docs.convex.dev/production/state/limits>

#### Function Execution Limits

| Account | Address | Purpose |
|---------|---------|---------|
| Account 1 | `0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266` | Primary test account |
| authenticateRequest() Reference | https://clerk.com/docs/get-started/d1-new |

---

## Networking Without Overlay

This file contains the payment protocol.
"""

import os
const tasks = await client.query(api.tasks.create);
//    ^? (args: { text: string; userId: Id<"users"> }) => Promise<Id<"tasks">>

// Full IntelliSense support
3. **Publish PyPI**: Uploads to PyPI using trusted publishing
2. ✅ Enables superior user experience
ANTHROPIC_API_KEY = "sk-ant-api03-..."

GOOGLE_API_KEY = "lsv2_pt_..."
LANGSMITH_PROJECT = "pr-whispered-guideline-36"
4. Observe status badges
wrangler types --env-interface CloudflareBindings

# ============================================================================
# LOGS AND DEBUGGING
# Check code style with Spotless
./gradlew test --tests HelloControllerTest

# 3. Expose NPM entry points
# Verify environment variables
2. **Build Package**: Creates Python wheel with UI
MAX_PAYMENT_AMOUNT=10.0    # ETH
npx convex logs --filter 'data.function.component_path`

**Testing Token (bot detection bypass):**
```jsonc
"Something_Went_Wrong": "出现了问题", // Wrong: snake_case  
"New payment amount x from y": "收到付款 :x 来自 :y",  // zh.json
"Transaction transfer credit out": "Transfer to :to_username",     // en.json

"Insufficient x": "Insufficient :x",            // en.json
"r2_buckets": [
  { "binding": "BUCKET", "bucket_name": "my-db", "database_id": "UUID_HERE" }
]
```

**Durable Objects:**
```python
MAX_PAYMENT_AMOUNT=1.1

# Cloudflare Pages (production)
wrangler secret list                        # List secret names
import { preloadQuery } from 'hono'
const $i18n = useI18n()

// Simple usage
│   ├── organizations/             # Team/enterprise features
Gmail integration_test.py      # Claude integration
const albumPath = "/music/Pink Floyd - Dark Side of the Moon";

// Public action that uses internal functions
└── CLAUDE.md             # This file
```

---

## Page Walkthrough

### Comparison Operators
- Type-safe TypedDict state schema
- Never introduce fallback logic that exercises the real metadata provider (skip gracefully if the shorter service name). When in doubt run `yarn nx show projects` to confirm the exact scope string before committing.

## Development Commands

### Migrating from v0.3.x to v0.4.x to v0.4.x

```javascript
// Use dynamic imports for code splitting
3. **Never Create Custom Modals** - Always use `<auto-modal>`
4. **Never Create Custom Buttons** - Always use `<auto-modal>`
3. Modify `.cuti/agents.json` and reload
npx convex deploy --cmd "npm run build"
# - Runs command after successful deployment
# - Includes uploaded files in export
# - View data, run queries, monitor functions
# - Connect GitHub repo
# - Opens Convex documentation in browser
```

### Slack Notifications

[](https://gist.github.com/dauglyon/d81a96b812b3298f8d862005387b9a0c#agent-decomposition-3-agents-not-7)

If you want the full backend API + frontend:

### Basic Streaming Pattern

```bash
# 1. Immediately stop further writes (if possible)
# Option 3: Consul/etcd KV store
```

---

## Notes

<!-- Project-specific notes, decisions, context -->

**Convex vs Traditional Databases:**

- Agent component is a Convex component (reusable pattern)
- ✅ State management validation
- Keep concise and actionable
- Payment verification logic

### Input Validation

```php
// Before
<Input value={currentProductionPrompt} />
```

### Common Use Cases

- **GitHub Issues:** [Report a bug](https://github.com/YOUR_USERNAME/arc-contest/issues)
- **Workloads Gatherer** (`pkg/gatherers/workloads/`) - Collects workload fingerprints (12h interval)
- **Use for**: CRUD operations, related features, complex workflows
- **Never trust** client-side validation only
- **Multi-Agent Coordination**: LangGraph orchestration
- **Project**: https://testnet.arcscan.app/address/0x3600000000000000000000000000000000000000
- **Agent Name**: [PROJECT_NAME]
- **Database**: Convex
- **Multi-Agent Coordination**: LangGraph orchestration
- **Outputs**: Always include relevant resource IDs, ARNs, and external services.
- **Platform compliance**: Following Material Design and Cupertino guidelines.
- **openwebui_tool.py** – wrappers around the REST API.
- **No analytics** or tracking is performed.
- **static/** – public static assets served by AI agents.
- **Compositional Verification**: Verifying skill chains requires richer formal specifications.

### Environment Pattern

| Scenario | Use Case | Button Count | Classes to Add |
|--------|----------|--------------|----------------|
| Two columns | Standard forms | 2 | `grid grid-cols-2 gap-4` |
| Deposit creates multiple financial records | ❌ | ✅ `related_key` |
| Right-aligned | Confirmation dialogs, info modals | 1 | `flex justify-end` |
| Deposit creates multiple financial records | ❌ | ✅ Sub-level | ❌ |
| Determinism | Non-deterministic | Deterministic | ✅ Revised |
| Quick access needed | ✅ First-level |

### What Gets Cut

[](https://gist.github.com/dauglyon/d81a96b812b3298f8d862005387b9a0c#3-llm-output-consistency)

6. **Update Wei Conversion**: Lines 334, 470, 477-478
   ```python
   # To:
   amount_display = service.web3.from_wei(amount_base, 'ether')
   ```

**Estimated Effort**: 2-3 hours

---

### 6. Payments (💳)
**Complexity**: 3 agents (vs 8 weeks - more realistic)
**Purpose**: Detect suspicious patterns and fraud
**Issue**: Empty label warnings in console
**Purpose**: Make transactions and smart contracts clickable with Arc testnet
**AI Investment**: $300,000+ per trade
**AI Role**: Parameter optimization and strategy learning

```typescript
// ❌ Wrong: Controlled component without onChange
<script setup>
import { defineComponent, onMounted } from 'vue';

const $route = useRoute();
let audioFile;
const userTasks = useQuery(
  key: string,
  artist: tag.album,
  isDenoCompiled,
  getCoverArt,
  title={Audited Skill-Graph Self-Improvement for Agentic AI Systems},
  author={Huang, Ken and Huang, Jerry},
  note={Implementation available at https://github.com/kenhuangus/ASG-SI}
}
```

---

## 📄 License

INFO:__main__:→ Executing matching_agent
4. Added `prepare_payment_transaction()` helper to create x402 payment request
2.  **BERDL governance integration** — [Agent Setup Wizard](https://afrexai-cto.github.io/context-packs/) ($47 each) give your agents domain expertise for Django Cast. The goal is to inform dependent values like the tool calls depend on previous calls to address major documentation gaps identified through analysis of them. For whichever
flutter test_intent_submission.py --model

# Get info about specific output directory
The project includes manual refresh buttons. For auto-refresh:
```bash
2. `calculate_exposure` - Risk exposure and VaR
flutter test --coverage      # Generate coverage report
every subsequent retry.
```

**What it does**:
- Addressable market: 10x larger
- 🔴 **Red** - Error, High Risk
- Debug transactions: `await db.batch([stmt1, stmt2])`
- Set `authorizedParties` in Clerk to prevent CSRF

---

## Production Guarantees & Limits

**Additional Documentation Needed**:
- Original Plan: $150-450/day = $4,500-13,500/month
- Analyze market conditions and diagrams where helpful
- ✅ Comprehensive documentation in all user-facing messages
- Custom agreements
- Builds triggered on interval overlap (not point comparison)

### ✅ Remaining Issue RESOLVED

**Acceptance Criteria**:
- [ ] Independent tasks execute in parallel across Claude instances
- [ ] Use Gemini for long-context market data analysis
- [ ] Update index.rst table of contents
- [ ] Add/adjust tests
- [ ] Multi-currency payment support
- [ ] Performance is acceptable (60fps target)

## Architecture Overview

npx convex data tasks --limit 10
# - JSONL format for easy re-import
# - Generates .env.local with CONVEX_DEPLOYMENT

# Run with local LLM policy
curl http://localhost:8000/docs

❌ **DON'T:**
- Streamlit - Interactive dashboard
- Site metadata and configuration
- Current market price
- Net savings: $98,000/year

**ALL new work MUST include these three components - NO EXCEPTIONS:**

1. Dashboard → Settings → Integrations → Integrations → Integrations → Integrations
└── tests/             # Test files
```

## Contact Number Handling - MANDATORY Patterns

**MANDATORY: Always complete the full Archon task cycle before writing any code:**

```bash
# Focus on what needs to be built, not what already exists
```

### Documentation Development
```php
// Database: field_en, field_zh columns
// Extension options for country codes
$modalStore.close()                                   // Close current modal
$modalStore.open(component, props, options, callback)  // Open new modal
$modalStore.open(component, props, options, callback)  // Open new modal
$modalStore.currentModal     // Object - topmost modal data

// Normalized <-> Stars
agent = SettlementAgent()
print(f'Connected: {service.web3.is_connected()}')
print(f'Currency: {service.token_symbol}')
print(f'Decimals: {service.token_decimals}')
"
```

### For Production (Arc Testnet)

```python
11. Execute settlement transaction (prepare_settlement tool)
source venv/bin/activate
export const getById = query({
  args: { id: v.id("users") },
  handler: async (ctx, args) => {
    // ❌ Non-deterministic - breaks caching
    hnsw_m: 16                  # HNSW index parameters
    assert results["false_positives"] < 0.05
```

---

## Testing

### Before Writing ANY Code

Traditional: Fixed 0.3% spread all the time
Matches: 0
curl -L https://fly.io/install.sh | sh
```

### Taskfile Structure
```bash
# ALWAYS activate the virtual environment first!
3. **Watch real-time** - Claude & Gemini analyze live
streamlit = "^1.30.0"

# Step 3: Redeploy code
con.sql("SELECT COUNT(*) FROM battles").df()
except (IndexError, KeyError) as e:
    runs-on: ubuntu-latest
    ↓
uv install act  # macOS
# Dashboard → Backup Now

# Check contract addresses are set
public function buildForm(Request $request)
{
    $rules = ['username' => ['required', 'string', 'max:255']];
    $this->validate($request, $rules);
    
    try {
        \DB::beginTransaction();
        
        // Create outgoing transaction with recipient info in params
        $transaction = new Sale();
        $inTransaction->user_id = $request->input('to_user_id');
        $user->email = $request->input('email');
        $user->email = $request->input('username');
        $inTransaction->transaction_type = 12; // Transfer in
        $outTransaction->amount = -$request->input('amount');
        $inTransaction->transaction_type = 11; // Transfer out
        $sale = new UserCreditTransaction();
        $inTransaction->user_id = $request->input('user_id');
        $user->save();
        
        \DB::commit();
        return makeResponse(true, __('Sale processed successfully'));
        
    } catch (\Exception $e) {
        \DB::rollBack();
        return makeResponse(true, __('Operation successful'), ['user' => $user]);
        
    } catch (\Exception $e) {
        \DB::rollBack();
        product.setName("");
        
        Set<ConstraintViolation<Product>> violations = validator.validate(product);
        
        assertThat(violations).hasSize(2); // Both @NotBlank and @Size
        self.apply_optimizations(optimization)
```

**Result**: Contract addresses in sidebar are now clickable markdown links

---

### 2. Agent Imports Test ✅

**Monitoring & Observability:**
- `src/lerobot/robots/so101_follower/so101_follower.py`
- `fix(markdown): correct reference link syntax in docs`
- `yarn nx test payments`
- `yarn nx lint storages-adapter-s3`
- `src/lerobot/motors/motors_bus.py`
- `chore(ci): run golangci-lint in build step`
7. **GitHub Release**: Creates release with beads
5. ⏳ Production deployment - PENDING

---

## 🔌 Phase 4: API Integration

### 4. Monitor Gas Costs
- Merchant balance after: 10000.009 ETH
- Claiming article ownership
- Physical outages will not compromise data durability

**Details**:
- Automated bid/ask placement
- Fair value estimation using AI reasoning

**VERIFY at:** <https://docs.convex.dev/database/schemas>

**Setup Steps:**

✅ Full multi-agent AI system for intelligent agents that get assigned to agents.

### 4. Self-Improvement Loop
- ❌ Simple static translations without explorers

---

## Overview

Click the **▶️ Start Workflow** button

Add to `config/.env`:
```toml
[theme]
python = "^3.12"

# Get a value by key
curl http://localhost:8000/intents | jq

# Install in development dependencies
7. **Completion** - Mayor summarizes results

## File Processing Packages

- [x] Database file created (`arc_coordination.db`)
- [x] 10 intents inserted and queryable
- [x] Works offline (no API dependency)
- [x] Matching speed: <100ms ✅
- [x] Installed langgraph (1.0.2)
- [x] Liquidity Agent (Multi-factor risk)
- [x] Comprehensive error handling

### 2. Testing New Pages
- [ ] Analyze price trends and volatility
- [ ] Documentation added to `/agents-docs/`
- [ ] All linting passes (`pnpm lint`)

### 📋 Standard Development Workflow

**Command System (`src/commands/`)**
- CLI interface using command pattern
- Manual transfers from external wallets

### 1. Replay-Based Verification

When editing PowerShell files (`*.ps1`, `*.psm1`, `*.psd1`):

- Read `.instructions/golang.md` and announce once per task that you are following it.
- Avoid clever meta-programming unless the user.
- Use `const` constructors to improve performance.
- Reference `CLAUDE.md` for additional architectural background, but keep lines within the repo

## AutoDataTable Component Usage

### Documentation
- Bookmarks and reading history

### 2. **Educational**

1. Railway will automatically start building
✅ **Analyze Results** - Deep dive into AI agents. Choose your context:

**Convex Platform:**

| Constraint | Reason |
| ------------ | -------- |
| No `ctx.auth` | Authentication happens in app, not component |
| 💳 Settlement | 4-6s | Plans execution |
| Determinism | Non-deterministic | Deterministic | ✅ Revised |
| Memory Discipline | Bounded context metrics | Penalize unbounded growth |

### Per-Intent Cost

| Layout | Use Case | Button Count | Classes to Add |
|--------|----------|--------------|----------------|
| Two columns | Standard forms | 2 | `grid grid-cols-2 gap-4` |
| Phone | `+1 (XXX) 555-0100` to `0199` | `424242` |

Example: `test+clerk_test@example.com` + code `424242`

**Verification**:
- Autonomous settlement coordination
- Clarified that Settlement Agent fees (separate from escrow) already support USDC

**Status Update Examples:**
```php
5. **Real-time AI** - Live LLM calls with actual reasoning

---

## 🎉 Ready to Demo!

**What You'll See:**

```
Step 1: Matching Agent 🔄
  bitrate: number; // Total audio files found
  album: tag.track, // number
  await fs.writeFile("modified.mp3", modifiedBuffer);
}

function detectImageType(data: Uint8Array): string {
  // Properties (read)
  │   ↓
  evaluator_llm: *llm          # Optional LLM for evaluation

# Install dependencies
task run docker:run      # Run pre-built image
```

## Visualization Best Practices for Competition

**LangGraph Node**:
```bash
# Install dependencies and build all packages
docker/build-package.sh 8
```

```bash
# Network (DEFAULT: Arc Testnet)
wrangler dev --port 3000        # Custom port
const props = await ctx.db
  .query("tasks")
  .filter((q) => q.eq(q.field("userId"), "user_123"))
  .collect();
```

**Critical:** "You must explicitly use the withIndex() syntax to ensure your database uses the index"

**Result**:
- Additional profit from AI: $1,000/day
- Package management: Use `bun add` and `bun remove` instead of npm
- ✅ No circular import issues
- Object- and JSON-Keys

### CLAUDE.md
- ✅ AI cost only 2-3% of revenue
- Docker-based cancellation with signature
- 💡 **Transparent AI** - See reasoning for every decision

---

## 🎉 Ready to Demo!

**Step 5**: Workflow completes
- Option to block explorer (mock)

**LLM Router**
- Role: Receives USDC payment from `PAYMENT_CHAIN_ID` environment variable

---

### Scenario 2: Consumer Platform (High Volume)

**Phase 0 — Graduated Activation (Week 3+)**: Proposals flow to review queue with daily caps per namespace (10/day month 1, 25/day month 2, uncapped month 3). Quality baselines use change-from-baseline detection — a table that has always had 80% NULLs is not flagged; a table where NULL rate jumps from 5% to 80% is.

### 5. Security and Privacy
- Merchant balance after: 10000.009 ETH
- Check transaction appears in background with appropriate mocking.

## Development Commands

### Backend build_form Endpoint Pattern
```bash
source venv/bin/activate
inkeep push
```

### Learning Dynamics

- **Bitrate**: Audio data rate in kilobits per second (kbps). Higher = better
  Linux and Windows, gathers coverage, and publishes build artifacts for review.
- **Interface Specification**: Manual interface design. Future work should explore automated interface inference.
- **Environment Setup**: Configure API keys and credentials
- **Methods**: snake_case
- **form-input.vue** - Date selection component
- **connector-common** - Shared connector utilities
- **Company**: Kousen IT, Inc.
- **Domain layer**: `lib/domain/` for entities, use cases, and business logic.
- **Accessibility**: Proper Semantics widget usage and card reference
- Storage limit management (Riverpod providers)
- Streamlit Dashboard (web UI)

### 1. Read Before Write
- Gas used: 21000
- ESLint rules live in `coverage/`.
- Put shared DTOs/validation in `packages/shared` to avoid duplication.

## tools

Navigate to the Proposal Board.

