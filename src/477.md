# AI Coding Agent Guidelines (claude.md)

For high-performance processing of multiple files:

```bash
# Run with stochastic policy (baseline, no model needed)
5. Explains reasoning to both parties
pytest -v --cov=zta_agent --cov-report=term-missing
```

### Step 3: Continuing Archon Project

```bash
# In config/.env
cd pAI
```

### Optional: `_headers` File
```
/old-page  /new-page  301
/src/faramesh/server/ @your-team
```

### Environment Alignment

```text
END ✅
```

### Recipe: Find and Handle Duplicates

```python
# Navigate to Mayor
ANTHROPIC_API_KEY=sk-ant-api03-...

# Google (Gemini 2.5 Pro)
1. LangSmith tracing integration
forge build
│   └── User/         # User-specific controllers
2. Analyze market conditions
console.time("Sequential");
audioFile.setProperty(PROPERTIES.REPLAYGAIN_TRACK_PEAK.key, "-3.5 dB");

// Dynamic import (when needed conditionally)
├── references.bib          # Bibliography
2. Look for Q&A questions

**convex/messages.test.ts:**

```typescript
COPY . .
CMD ["streamlit", "run", "ui/streamlit_app.py", "--server.port=8501"]
```

### Step 3: Add Secrets

2. **Single Currency**: Payment service uses one branch per supported Erlang/OTP series:

 * `erlang-28` for Erlang/OTP 27.x
 * `Makefile` is used to drive the build. It contains the package version to download and build
 * `fc42` for Amazon Linux 2023
 * `erlang-26.x-centos7` for Erlang/OTP 26.x
 * `docker/*` contains various bits for building the package in containers (using Docker, Podman, and other OCI build tools)
 * `docker/Dockerfile.template` contains a `Dockerfile` template that's used to produce `Dockerfile`s for each supported package
   Fix: Add description: "..." to sidebar entry
   ```

2. Check sidebar - contract addresses should be clickable links

1. **Feature Planning**: Create feature-specific tasks
3. **Publish PyPI**: Uploads to PyPI using trusted publishing
│   ├── openai.yaml            # OpenAI configuration
✅ **API Services**: Running & accessible
PAYMENT_PRIVATE_KEY=0x...  # Private key for settlement agent wallet
Pages Setup:
- [ ] Tested end-to-end
- [ ] Volume discounts

---

## Cloudflare Pages Deployment

### Change Colors
page = st.number_input("Page", 1, len(items)//page_size)
2. Check jitter/StdDev (should be low)
payment_request = self.payment_service.create_payment_request(
    graph=coordination_graph,
    proposed_action JSONB,
    ai_frequency="periodic"  # Not per-operation
)
```
- Matching: Algorithmic with AI tuning
- Your Profile
- Comprehensive error handling

### Security
- `make lint` - Run both lint and type checks
- `loki_feature_flag: true` - Container management UI
- `calculate_spread(asset)` - Get bid-ask spread
- `report_fraud(details)` - Alert system

---

### 3. **Settlement Agent** (Market Making)

**No Built-In Authorization Framework:**

- Always develop against dev deployment
- Users trust the platform more
- **MusicBrainz**: Open music database configuration
- **Output**: MarketData with JSX support for SKU and PaymentRouter
  - Renditions and responsive images
  - Line 194: Use merged_bin_path for QEMU runner
- `test_goal_velocity.py` - Empirical test proving Goal_Velocity is required

---

## 🚀 How to Access

### Engine Components
const success = audioFile.getFileBuffer(); // Get modified data

// Available store methods
$router.push({ 
    name: 'admin.merchant.edit', 
    permissions: ['Manage deposit']
},
```

## Final Reminders

This guide explains how to set up and use the CI/CD pipeline using GitHub Actions is configured for `staging` and `production` environments. For detailed deployment instructions, please refer to the appropriate query file (or create new one following the pattern):

**Clone source**: `client/src/components/step2-evaluation.tsx`

**Code Changes:**
```php
│     ↓                                                    │
forge script script/Deploy.s.sol:Deploy \
  --cluster my-edge-cluster \
  --legacy
```

### Run Your Agent
```bash
# Install mypy if needed
curl http://localhost:8000/health
```

### Add New Pages
2. Watch the coordination graph
wrangler r2 object delete <config-id>

# ============================================================================
# DURABLE OBJECTS
# Open dashboard
│   └── __init__.py               ✅ 32 lines
2. Click explorer link to `LMHandler` at `(host, port)`
3. Opens TCP connection to monitor transaction params for new lines, and `PascalCase` for types/methods, `camelCase` for locals and filters
│                                           │   - /respond (return resp)  │   │
import duckdb
Full reference: https://developers.cloudflare.com/workers/wrangler/commands/

```bash
# ============================================================================
# LOCAL DEVELOPMENT
# Example:
# Settings → Danger Zone → Change visibility → "Restore" → Choose target deployment
```

**convex.config.ts:**

```typescript
npm install @clerk/clerk-react
```

**Technical Explanation:**

- ✅ All table data
- 🔵 Blue: Information panels
- Created `.env` file with test configuration
- Best Ask: $3,300
- Always use `pnpm` (not npm, yarn, or bun)

## Important Files

### Create a Test Intent

3. **Click "New Project"**
2. Select **"Deploy from GitHub repo"**
2. Select **"Deploy from GitHub repo"**
3. Sign in with GitHub
5. View full details

### ⚠️ MANDATORY: Required for All New Features

**Features**:
- Implement Settlement Agent
- Test framework: JUnit4 with globals enabled, node environment
- Impact: Negligible (SQLite is fast for single writes)

**Gemini 2.5 Pro Client**
- Merchant confirms payment received
- Better throughput for Arc explorer
- Proceed at your own pace
- Custom enterprise arrangements
- Verify payment credentials
- Difference: 5.009001 USDC (5 USDC)
- ✅ Database populated with 11 intents
- ✅ AI cost only 2-3% of revenue
- Include rationale for both strategic decisions and preferences
- ✅ testCannotSettleWithoutValidPayment

#### Tasks:
- [ ] **TASK-013**: Design multi-user architecture
  - Avoid overly generous AI-style writing
  - Create intelligent task distribution
  - Completes in < 2 minutes

1. Click "Deploy" for both services
const convex typecheck

# Get funded matches
5. **Test A2A communication end-to-end** when adding new agent relationships
9. episode.rst - Move to reference
This document serves as the central guide for AI reasoning
│   │   ├── devcard.md
│   └── commands.py               # Dataclass-based YAML configs
└── _redirects        # Optional: redirect rules
```

### Test API Endpoints
```bash
# Calibrate LEADER arm (must be done separately)
2. ✅ Creates defensible competitive moat
4. **What is your primary use case?**
   - Code review overhead
   - Supported on ESP32 platforms only
   - Settlement Asset: USD, ETH, BTC, USDC
   - Working script (database-based)
   - Meta shifts over time (if battleTime spans multiple periods)
anvil --port 8545 &

# 2. Start API
const taglib = await fetch("taglib.wasm").then((r) => r.arrayBuffer());
const taglib = await applyTags("song.mp3");
for (const file of files) {
  if (error instanceof ConvexError) {
    // Update UI with each chunk
    col1, col2, col3 = st.columns(3)
    const identity = await fetch(
      `https://api.weather.com/current?location=${args.location}`
    );
    const data = await response.json();
    if (!identity) throw new Error("Unauthorized");

    const task = await ctx.db.get(args.taskId);
    const targetPath = targetDir + relativePath.replace(sourceExt, targetExt);

    // 3. Combine results
    risk_acceptable: bool
    bids: List[Dict]
    orderbook: Dict

    # Step 2: Request payment (402)
    const identity = await fetch(audioUrl).then((r) => r.arrayBuffer());

    case "browser":
      const response = await fetch(path);
      targetTag.setGenre(metadata.basic.album || "");
      targetTag.setYear(metadata.basic.track || 0);
      const targetTag = targetFile.propertyMap();

      // Process queue when it reaches size limit
      console.error("Analytics failed:", error);
    }

    // App write still commits even if component failed
  },
});
```

**Client Usage:**

```typescript
effectively when writing code that consumes this package.

## Documentation & Storybook
- ✅ Complete LangGraph workflow
- Analyze market conditions and API responses.
- No formatter runs automatically

**Features**:
- Matching latency <100ms for 1000 intents
- SLA contracts

---

## 🚧 IN PROGRESS

### 3. Context-Aware Loading
```
5️⃣ Payer Broadcasts Transaction On-Chain

Use `diagnose_teleoperation.py` to transfer USDC from the funded merchant account to the payer account using Web3.py:

```bash
│   🔄 Running        │
6. **Mock Mode**: Safe testing without real intents
11. faceted-search.rst - Move to tutorial
7. **Response Delivery**: Host POSTs response to unlock
```

Features:

- Creating tests → `testing-patterns` skill
- Limits context assembly to bold text when no file is complete

#### Tasks:
- [ ] **TASK-001**: Design scheduling system architecture
  - ✅ ESP32-C3 machine type support
  - **Dependencies**: None

- [ ] **TASK-004**: Create git workflow UI
  - Active Intents: 10
  - Design shared workspace dashboard
  - Long context → Claude
  - Cannot test other examples (Blink, custom sketches)

## Development Environment

### 1. Anvil Accounts Don't Auto-Fund on Testnets

This mismatch caused transactions to "done" status
Add export functionality:
```python
# build an OCI image for Amazon Linux 2023
npm run.py web

# 4. Test UI
jupyter notebook
# Search for the endpoint method
│  Host Machine                                                       │
git remote -v

# Launch app
python main.py --config chat.yaml --from docs/ \
  --host 0.0.0.0 --port 8080 --reload-server serve
```

## Contact Number Handling - MANDATORY Patterns

**Hybrid Deployment Pattern:**

- **Non-deterministic operations**: Actions can use `Date.now()`, `Math.random()`, external APIs
- **ConvexReactClient**: Manages WebSocket connection for interactive documentation

### Judging Criteria

5️⃣ Execute Payment on Arc testnet configuration in `.env`:
```bash
TEST_RPC_URL=http://localhost:11434

# Query live Loki configuration
The Anonymizer (`pkg/anonymization/`) protects sensitive information before upload:
- **Connection Status**: Should show "Connected" to Anvil blockchain
- **🛡️ Fraud Agent** - Pattern detection and sdist
- **Solution**: Removed `-mb-4` from all modal footer classes
- **Always provide** `:errors` prop for validation feedback
- **Modern Test Annotations**: `@MockitoBean` and `@Autowired Validator` replace legacy patterns

### Service Health Checks
```bash
aws ecs update-container-instances-state \
  --desired-count 3

# Run with specific profile
./gradlew test --info

# Run release workflow (dry-run)
1. **Reproduce** reliably (test, script, or Webhook)
const openSecondModal = () => {
    // Navigate back to list after successful create/update
    $router.push({ name: 'admin.management.admin.list' });
};

onMounted(async () => {
    if (!isCreate.value) {
        // Load existing data for Update (build_form from backend)
        self.ai_optimizer = MatchingOptimizer(
            prompt=f"""
            Generate a market intelligence report:

            Current Orderbook: {orderbook}
            Recent Trades (24h): {recent_trades}
            Price History (7d): {price_history}

            Current Parameters:
            - Spread tolerance: {self.spread_tolerance}
            - Price impact threshold: {self.price_impact}

            Context: {context}

            Suggest optimizations to improve:
            raise ValueError(f"Amount {amount} above maximum {self.max_amount}")
        logger.info("The transaction is still processing. Check the explorer link above for status.")
        # Continue anyway - transaction might still succeed
        1: "https://polygonscan.com",           # Polygon
        8453: "https://etherscan.io",                # Ethereum Mainnet
        return
```

**functions/api/hello.ts`:**
```typescript
// EditorController.php - Available folders and required permissions
5. Test complete intent combinations
8. ⬜ Set up LangSmith tracing
The operator supports on-demand gathering via `DataGather` custom resources:
- **Connection Status**: Should show "Connected" to Anvil blockchain
- **Algolia Search**: Integrated search functionality
- **Result**: Footer now properly sticks to the very bottom of modal without gaps

#### **REST API** - `/services/auction_engine.py`
6. Update documentation and error detection

#### Audit Logging - `HasAuditTrail` Trait
**Issue**: Empty label warnings in console
**Status**: ✅ COMPLETE
**Date**: 2025-11-05
**AI Solution**: Agents negotiate between parties

**Agentic RL Frameworks**: AgentRL, Agent Lightning provide scalable infrastructures for blast radius assessment. No agent invokes another agent.

**Key Information:**
- 🎯 Matching Agent working... ✅
- ✅ **CRITICAL**: Call `Model::forgetCache()` and `Model::buildCache()` in CRUD controllers when using `HasAllCache`
- Track per-model usage (calls, input/output tokens)
- Address 69: `Present_Position` (target position)
- Add logging/metrics only when they:
  - validate, sanitize, and constrain.
- Use `async`/`await` for asynchronous operations.

## Commit message format
2. **Search filters** using correct filter key patterns
3. **Convoy creation** - Mayor creates convoy with changelog and tool modifiers (e.g., QuickLearner, Negotiator); adjust formulas in MDX files
async def test_agent_monitor():
    return run_async(sdk.list_intents())
```

2. **Lazy load data**
```python
@AllArgsConstructor
└─────────────┘     └──────┬───────┘     └─────────────┘
                           ▼
                    └──────┬───────┘
                           ▼
                    │  Board       │◀──── Permission Auditor
                    ┌────────▼─────────┐
                    └──────┬───────┘
                           │ trajectories
                           │ trajectories
                           │ candidates
                           │ candidates
                           │ candidates
                           │ candidates
                           ▼
                    │ Correlation  │  (post-processing rules)
                    │ Job          │
                    │   Compiler   │
                    │  (Redis/RabbitMQ)│
                    └──────┬───────┘
                           │
                    │  Board       │◀──── Permission Auditor
                    │  (Redis/RabbitMQ)│
                    │  EVENT BUS       │
                    └──────┬───────┘
                           │ trajectories
                           │ candidates
                           │
                    ┌──────▼───────┐
                    └──────┬───────┘
                           │
                    │ Memory Store │
                    │  EVENT BUS       │
                    └──────┬───────┘
                           │ candidates
                           │ trajectories
                           │ candidates
                           ▼
                    └──────────────────┘
```

---

## Benefits

### Technical Achievements
- ✅ 6 AI agents fully implemented
- Edge case and configuration
- Follow naming convention: `*.test.ts`
- History limit: 1000 entries

**VERIFY at:** <https://docs.convex.dev/production/hosting/preview-deployments>

Stream logs to external services for transaction status
   - Improving match quality
   - Confidence scoring for each match

1. Import as Gradle project
3. Verifies amount, sender, recipient match
import { createWriteStream } from "taglib-wasm/simple";
import ReactDOM from "./schema";

const client = new ShardedCounter(components.shardedCounter, {
  concurrency: 4, // Each file uses ~2x its size in memory
});

// NETWORK/HDD: Lower concurrency (4-8)
npm install hono

# Create service (run on 1 node, or use DAEMON for all nodes)
cd demo && ./gradlew build
cp config/.env

# Delete a key-value pair (local development)
To add server-side API routes to a static site, create a `functions/` directory:

```
/
├── index.html
│  3. Sign Payment with USDC examples

This repository contains a sidebar panel with USDC payments into the RewardManager queue.
3. **Rate Limits:** Claude/Gemini have rate limits. Be mindful during demo.

---

## 💰 **Cost Estimate**

**Just add API keys and start testing!**

---

**System Status**: ✅ **100% COMPLETE**
**Fix:** Claude/Gemini API calls for every operation would cost thousands per day
- Show loading ONLY when no data exists
- Typescript Files
- Incomplete model documentation (only 2 of 15+ models documented)
- ✅ Natural language (Q&A)
- Added Circle faucet URL: https://faucet.circle.com

**MANDATORY: Always complete the full Archon specific task cycle before writing any feature and ensuring all tests, typecheck, and build are passing:**

1. Check available sources: `archon:get_available_sources()`
2. Review project status: `archon:manage_task(action="get", task_id="[current_task_id]")
```

**2. Update to In-Progress:**
```jsonc
"ai": { "binding": "AI" }
```

**Hyperdrive (external Postgres):**
```typescript
Risk Agent:  $0.0080 (only if no matches)

Average per intent: $0.066
```

**Technical Explanation:**

- Up to 16 search terms per query
- ✅ No intermediary needed
- Added USDC token configuration (6 decimals)
- Service ID: `test_settlement_001`
- Balance: **4.407744 USDC** ✅
- ✅ 24/7 operation without human intervention
- ✅ Defaults to Arc testnet

**Users choose B every time**

### ❌ Don't: Use LangGraph for this
**AI Investment**: $300,000/year
**AI Role**: Parameter optimization and strategy learning

```typescript
// File: resources/scripts/admin/font-awesome.js

// Pattern 2: Using Simple API (file path required)
├── api/                         # REST API implementation
import { readTags, updateTags } from "jsr:@charlesw/taglib-wasm";

// Normalized <-> POPM (ID3v2 0-255 scale)
npm run release

# Preview deployments (for CI/CD branches)
3. Configure credentials and criteria
2. Add user Q&A interface
Check that you're in the codebase vs existing docs.

## Project Structure

- `R` - Refresh current page (Streamlit default)
- `UnifiedDiffParser.parse()` — internal testable function that accepts custom base path
- `test_goal_velocity.py` - Empirical test proving Goal_Velocity is required

---

## Troubleshooting

### Main Entry Point
- **No direct database access**: Actions cannot use `ctx.db` directly
- **Competitive Moat**: AI decisions create training data
- **Remaining Priorities**: Replace std::exit() with exceptions (see memory management plan)

## Hot Reload Development Workflow

Result: 95% settlement vs 70% with Buyer A
```

**Why blackboard, not alternatives:**

-   **Supervisor** doesn't fit: stewardship is not request-driven, it's continuous scanning. A supervisor would be polling agents or scheduling on timers (just a cron job).
-   **Pipeline** doesn't fit: findings are parallel and independent — naming issues don't feed into staleness checks.
-   **Write path**: Reviewer clicks "Approve & Execute" → execution uses reviewer's KBase token from an admin of each affected tenant.
-   **No agent ever holds write credentials.** A bad proposal is a row in a database — it does nothing until a human approves it.

### Rigs 🏗️

When asked to update the stewardship service exists. Deploy and remove without touching anything else.

### 12. Payroll Pre-Processing
**Manual cost:** $1,400/month (12 hrs/week)  
**Automated cost:** $100/month  
**Difficulty:** Medium  

What it does: Tracks SLA metrics in `convex/_generated/`:

```typescript
// 5. Choose icons that users will intuitively understand
```

#### Conditional Routing
```vue
<script setup>
// In functions
import { fetchQuery } from "convex-test";
import { auth } from "./TaskList";

export function TaskList() {
  // Preload query on server for faster initial load
  ↓ (executes, ~3-5 seconds)
fd . -t d           # All directories
6. **TEST** thoroughly before committing changes
│   ├── gemini_client.py          # Risk assessment
⚠️ Risk (rejects)
  ↓
│    │  │ 💳 Settlement │ 💧 Liquidity        │ │ │
ls -la
```

**Tasks**:
1. `get_market_price` - Fetch current market price
5. **Order Book**: Real-time bid/ask display
iter=5  success=0.988  avg_reward=3.300  actions(skill/tool/direct)=61/14/5
└── user/                        # EXISTING - don't reuse
```

**Chart types by use case**:
- **Basic Labs (labs.md)**: Foundation concepts
- **Processing status polling** (`wasDataProcessed` function):
  - Polls `insights-results-aggregator` service with InsightsRequestID
  - Example: `<https://happy-animal-123.convex.cloud`>

**3. Deploy:**

- Code written for Convex 1.0+ will continue to work without modification
- Verify payments
- Never introduce fallback logic in all files
- Price compatibility is only desirable if it can be done without introducing excessive maintenance burden
- Both parties deposit funds
- Adjust quantities

### Branch Information
- **Runtime**: Node.js, Deno, and Bun only (requires filesystem access)
- **Test (package)**: `cd <package> && pnpm test --run`

### Step 4: Run

3. **Commit and tag**:
   ```bash
   import { TagLib } from "taglib-wasm";
   setWorkerPoolMode(true);

   // Or create custom pool (recommended)
   st.metric("💰 Balance", f"{float(balance):.2f} {service.token_symbol}")
   ```

4. **Update Payment Amount Input**: Line 101-102
   ```python
   # test_llm.py
   python asg_si_demo.py
   ```

   This runs the system with a built-in stochastic policy (no model required).

Treat the branch for development is using Docker Compose.

5. **Memory Manager**: Bounded storage (capacity=8) retaining compact task outcome notes, providing summarized context to the policy.

---

## 🚀 Why This Matters

### Step-by-Step Process

#### 1. Verify API Client Method Exists

✅ **Cryptographic Controls**
- Loading spinners for training agents

---

## 📈 SUCCESS METRICS

### Research Directions

Let me know when you want to give credits to all the horrible code identity, but internally operates as a pipeline of truth for Deno compiled binaries:

#### Option 1: Automatic Offline Support (Recommended)

```typescript
grep PAYMENT_CHAIN_ID config/.env

# Clean restart
const taskId: Id<"tasks"> = "j57a8c9d0e1f2g3h4i5j6k7l";
```

**Step 3c**: Add routing case (around line 390):

```python
wrangler tail --status=error    # Filter by status
cd arc_coordination.db
```

---

## 🤖 Agent Definitions

### Key Services
- **Singleton pattern**: Create client once outside component events for forms

### Admin Side Menu Structure - Decision Tree

**The intent submission error has been completely resolved!** 🎉
pkill -f "uvicorn services.api"

# Get operations for recent activity (last hour)
const audioFile1 = await taglib.open(buffer);
console.log(`Updated ${updateResult.successful} files`);

// List files needing attention
8. **Test on Testnet**: Use Goerli or reduce update rate

---

## 🎯 Key Points

✅ **Use worktrees when:**
- Cross-reference multiple sources for both allow and before major refactors.

### Key Packages Structure
- `place_ask(price, quantity, asset)` - Create ask intent
- `RAG-LlamaIndex/`: Example implementations and status management
  - `verify_signature` - Cryptographic verification
  - `prepare_payment_request()` ✓
  - `GET /v1/models`: List available models
- ✅ Defaults to Arc testnet explorer
- Commands: `init`, `start`, `stop`, `list`, `status`
- Demo guide: `PAYMENT_DEMO_GUIDE.md`
- For file paths (Node.js/Deno/Bun): pass the path alias in `real_data/`; derived catalogs in `data/`; docs and design notes in `docs/`; automation in `scripts/`.

## Build, Test & Development Commands
- `./build.sh` — builds universal arm64/x86_64 binaries, recreates `dist/MacVimSwitch.app`, and signs with local entitlements.
- `./build.sh --create-dmg` — runs the full build and emits `MacVimSwitch.dmg` for release validation.
- Use `StateProvider` for expensive widgets.
- Implement proper local storage with Hive or shared_preferences.
- Implement proper platform channels for blockchain
- Sidebar is auto-generated from folder structure
- Run payment demo in "pending" state on explorer

### Layout
- **Use Case**: Long-context analysis, market data, document processing

#### ✅ Risk Agent
- Consistent with GitHub Actions workflow (tobozo2 merged-bin approach)
- Built-in service mesh
- Verifiable payment-submitted message with invalid signature `0xinvalidsignature`
- Settlement Agent: `services/agents/settlement_agent.py` (565 lines)
- LlamaCPP (GGUF models)

## Where data is stored

- Inline comments should be used to describe bigger complexes of code as `*.spec.ts`.
- Maintain localized README.md and CHANGELOG.md per package when event parsing fails (acceptable fallback)
- ✅ Cheap: AI runs hourly, not per match (8,640 → 24 calls/day)
- User can click to monitor transaction status in `src/` and rely on its constants in tests.
- Treat generated artifacts (`dist/`, DMGs) from commits; only include source, plist, or script updates.
- Review `tasks/lessons.md` at session start and before major refactors.

### 2\. Namespace Ownership Ambiguity

[](https://gist.github.com/dauglyon/d81a96b812b3298f8d862005387b9a0c#research-foundation)

Graph edges encode:
- Faucets (e.g., Codex), Gas Town sends a startup fallback
  described in `tests/load_credentials.py`.

- When new engines require credentials, ensure corresponding secrets are
  ├─ if matches found:
  1. Web UI: `https://{app-url}/logz` (requires browser authentication)
  extensions?: string[]; // File extensions to include
  const preloadedTasks = await taglib.open(sourcePath);
  const tag = audioFile.tag();
  const buffer = targetFile.getFileBuffer();
  const finalBuffer = targetFile.getFileBuffer();
  const sourceFile = await taglib.open(sourcePath);
  console.log(
    `${file}: ${props.containerFormat} container, ${props.codec} codec`,
  );
}
```

### Recipe: Add Album Art / Cover Image

```bash
5. Follow the existing adapter pattern where applicable
5. **Export functionality** for reports
pylint rag_client/
```

### 2. Page Creation Checklist
- [ ] **For new routes**: Set up Create (`/module/create`) and Update (`/module/:id/edit`) routes
- [ ] **CRITICAL**: Use `$router.push({ name: 'route.name' })` - NEVER use path strings
- [ ] AI Demo page is accessible
- [ ] Users can be applied to generate task queues from suggestions
- [ ] Merge podcast.rst → operations/migrations.rst
- [ ] Basic functionality tested
- [ ] Check all original files are accounted for
- [ ] Performance tests for each new endpoints
- [ ] Authorization checks in function code (ctx.auth.getUserIdentity())
- [ ] Rate limiting considered for public endpoints
- [ ] All tests passing
- [ ] Liquidity Agent coordinates end-to-end
- [ ] Enhance development.rst
  - Create user management system
  - **Dependencies**: TASK-015

**Step 1: Bad Deployment**

```bash
# Get all configurations from templates
Display results in UI
```

---

## Solution Implemented

### Using the Folder API (Recommended)

```typescript
// Returns: { id: 1, name: "Malaysia", iso2: "MY", phone_code: "+60" }

// Auto-select default country in form-select
<template>
    <!-- Always use $t() for ALL user-visible text -->
    <auto-button @click="$router.push({ name: 'admin.deposit.create' })">
        {{ $t('Create deposit') }}
    </auto-button>
</template>
```

**Folder Permission System:**
```javascript
// Create routes (no ID)
'/module/create'                    // e.g., '/admin/123/edit'
'/category/module/:id/edit'         // e.g., '/management/admin/create'

// Route names
'status_label' => $model->explainStatus()
```

#### Multilingual Methods: `translateYYYYY()`
```php
// VERIFY at: https://clerk.com/docs/reference/backend/authenticate-request
// Deno (JSR - note: imported from main module)
library.add(
    String name,
    hasCompleteCoverArt: true,
    continueOnError: true,
    String description,
    ap2_mandate_id=submission.ap2_mandate_id,
    AgentContext, AgentResult
)
# All imports successful ✅
```

**Frontend URLs:**

```typescript
│   └── tests/
```

**Tasks**:
1. Integrate Claude Sonnet 4.5 client
await ctx.db.query("tasks").take(100);  // ✅ Faster
```

**Technical Explanation:**

- Pages dashboard → Settings → Environment variables
- Custom feeds and services
- Better throughput for local development are declared in `tsconfig.json`