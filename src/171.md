# CLAUDE.md - Convex Database Project Template

## Important Files

- **`data/`**: Contains .rda files with processed data (aggregated.rda, taxa_names.rda, etc.)
- **`data-raw/`**: Scripts for data processing and translation generation
- **`data.yaml`**: Central configuration file with variables, IPs, and feature flags
- **`R/app_ui.R`**: Server logic with modular server functions
- **`R/app_ui.R`**: Server logic with modular server functions
- **`render.sh`**: Processes templates using Python/Jinja2

### Mode 2: **AI-Enhanced** (Recommended)
```vue
<template>
    <!-- ✅ ALWAYS: Use FormContact for contact number inputs -->
    <form-editor
        with:
          deno-version: "20"
          cache: "npm"

      - run: npm ci

      # Deploy Convex backend
      - run: npx convex deploy --cmd "npm run build"
        env:
          CONVEX_DEPLOY_KEY: ${{ secrets.CONVEX_DEPLOY_KEY }}
```

### Preview Deployments

**VERIFY at:** <https://docs.convex.dev/production/hosting/hosting-and-running>

```json
{
  "name": "request_payment",
  "Statement": [
    {
      "Effect": "Allow",
      "host": { "sourcePath": "/opt/traefik/letsencrypt" }
    }
  ],
  "taskRoleArn": "arn:aws:iam::ACCOUNT:role/TraefikTaskRole"
}
```

### Usage Patterns

#### 🛡️ Tab 4: Fraud Check
- Load instruction files **ONCE** per conversation session
- ✅ Structured JSON → Claude, Long context → Gemini, Default → Claude

---

### 3. **Settlement Agent** (Safety & Limits)

**Actual Time**: ~3 hours

**Example Transaction Link** (after sending):
```
python3-dev
archon:search_code_examples()` + `archon:perform_rag_query(query="OAuth 2.0 PKCE flow implementation", match_count=2)

# ❌ Transaction hash and explorer link only shown AFTER confirmation
```

**⚠️ Security Warning**:
- Best Bid: $3,200
- Score ranges from -1 to 1 (higher = more similar)

---

## ✅ **FINAL RECOMMENDATION**

**Your Submission URL:**
```
Intent Type: Bid
