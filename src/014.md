# Agent Guidelines

1. **Expand "Configure Test Scenario"** section
wrangler types --env-interface CloudflareBindings

# ============================================================================
# TYPESCRIPT TYPES GENERATION
# Deployed Contract Addresses (Arc Testnet)
fd . -t d           # All directories
└────────┬────────┘
         │ uses
         │ uses
         ↓
├── fraud_detector.py       # Batch fraud pattern detection
iter=4  success=0.975  avg_reward=3.244  actions(skill/tool/direct)=61/14/4
│                                                          │
├── gruntfile.js
