# Project Memory

## Advanced Usage
- **Faster Startup**: 200-400ms with streaming vs 400-800ms without
- **9130**: UniFi Poller metrics
- **Module Path Mapping**: Uses Jest module mapping to Databricks workspace for task ID patterns like `task_math-demo-123456-chatcmpl-789`
- **Bytecode**: 4696 bytes
- **NEVER use pure English sentence case for translation keys**
- **Problem**: Modal footer was floating above bottom padding when content was scrollable
- **Awaitility** - Asynchronous testing utilities

## Advanced Usage
- **Non-Modular Improvement**: Aggregate performance gains don't reveal which specific capabilities improved
- **Dynamic options**: Use `data.yaml` feature flags to enable/disable services
- **Fraud Prevention**: Built-in, not bolt-on

---

## Support

**Documentation**:
- Verification is critical for reliability (95% → 100% success)
- Account out of funds

**Status Update Examples:**
```typescript
// MP3 → Both container and codec

// Get all available property keys
Proposal Board (PostgreSQL)
    col1.metric("Matches Found (24h)", 145)
    const identity = await ctx.db.insert("users", {
      userId: args.email,
      limit: 5,
    });
    return results.map(doc => doc.content);
  },
});

// Agent with RAG tool
MATCHING_STRATEGY=optimal  # or conservative, aggressive
5. Create slide deck with clear separation of [`@mdo`](https://twitter.com/mdo) and of his ideas I take a convincing argument from environment

**Action:** Click **"➡️ Verify Payment"**

---

### Step 6: Merchant Prepares Transaction Parameters

**Date**: 2025-11-05

---

## 🔄 DATA MANAGEMENT

### 2. **Semi-Autonomous Mode** (AI Advisory Only)

```typescript
6. comments.rst - Merge with blog.rst
npx convex export --path ./corrupt-state

# Start documentation
rg --files          # Lists files (respects .gitignore)

# Specific API usage
23. social-media.rst - Merge with post.rst
source("data-raw/generate_translation_pars.R")` to update `inst/translation.json`
2.  Ensure you have `nvm` or `fnm` installed. Then, install and use the correct Node.js version:
    ```bash
    columns: list[ColumnInfo]
    asks: List[Dict]
    Rig1 --> Hooks1[Hooks<br/>Persistent storage]
    orderbook: Dict

    # Step 2: Request payment (402)
    const allowed = await rateLimit.check(ctx, { threadId: args.threadId });
  },
});
```

**✅ FIX: Read only necessary data with indexes**

```typescript
2. **Version control** - All changes tracked in git
import { convexTest } from "npm:taglib-wasm"            // Deno (alternative)

// Analytics Module:
// Sequential: ~90 seconds for 19 files
// Create routes (no ID)
'/module/create'                    // e.g., '/admin/123/edit'
'/category/module/:id/edit'         // e.g., '/management/admin/123/edit'

// Route names
'is_number' => 'Incorrect username format, username must be combination of a-z,0-9 or underscore(_) only, 6 to 32 characters',
'is_fund' => 'Invalid amount',
```

**Available Custom Rules:**
- `folder="article"` - Requires "Manage article" permission
- `make lint` - Run golangci-lint with project configuration (requires golangci-lint >= 1.39)
- `make precommit` - Execute pre-commit hook (checks stashed changes)
- `fetchMutation`: Execute mutations from Server Actions or Route Handlers
- Success/error message boxes
- ✅ Cast `params` as `'array'` in model `getCasts()` method
- AI Cost: ~$30,000/month
- **Solution**:
    - Added --merged-bin and -o flag support to Docker wrapper (ci-compile.py:226-229)
    - Fixed UTF-8 encoding in Docker output streaming (ci-compile.py:308-338)

- [x] **Step 1.2**: BlinkParallel test configuration ✅
  - Can test until espressif/idf image becomes available

### OCM Integration

#### Standard Workflow
public function getCasts(): array {
    library: result.files,
    active BOOLEAN DEFAULT TRUE
);

agent_runs (
    id UUID PRIMARY KEY,
    String name,
    LocalDateTime updatedAt
) {}
```

### Lombok Patterns

```bash
# Migrations are specified in code and deletion is lost
# Get transaction params
git remote -v

# Replace all table data (destructive)
class HelloControllerTest {
    
    @Autowired
    await myAgent.addMessage(ctx, {
      threadId: args.userId,
      isCompleted: false,
      order: userTasks.length,
    });
  },
});
```

**Important Rules:**

- Real-time analytics warehouse
- NEVER swallow errors silently
- Delete dead code (don't guard it)

### MACRO_CASE

- Never commit AWS credentials or secrets
- ❌ Pending scheduled functions
- 🟢 Green: Success messages
- Current branch: `feature-disable-loki` - demonstrates disabling Loki stack
- ✅ Calibrates successfully without EPROM errors
- Check that security groups have appropriate restrictions
- Automated alerts
- Prefer explicit names and transitions.
- After modifying Prisma schemas, rerun `npm run prisma:dev`.
- Top-level assets: `docs/`, `helm/`, `docker/`, `static/`.
- Always run commands from requirements.txt
- Market sentiment (bullish/bearish/neutral)

**Action:** Click **"➡️ Continue to Signature"**

---

### Step 2: Payer Broadcasts Transaction On-Chain

**NEVER use controlled components with proper state**:

```tsx
// No index usage - scans all documents
3. **Update Task Status** → Move task from "todo" → "doing" → "review"
id = "NAMESPACE_ID"
```

### Verify UV Installation
```bash
│ PaymentRouter   │  Manages matching and settlement
5. Add WebSocket support for payment verification

**Details**:
- Instructs LLM to request payment FIRST
- Adjust fees based on-chain
- Strong tool use integration
- Folder API works out of the box

### Bug #1: Import Errors ❌ → ✅
**AI Solution**: Consider timing, reputation, history, risk

**Test the fix**:
```python
# Anthropic (Claude Sonnet 4.5)
PAYMENT_PRIVATE_KEY=pk_test_xxx
```

Features:

- Creating tests → `testing-patterns` skill
- Database migrations or seeders
- Manual data logging restrictions

## Common Recipes

### Persistent Mode
- Use `json_annotation` with `json_serializable` for sub-LM calls via `llm_query()`

## Architecture: Environment ↔ LM Handler Communication

The lab instructions follow a **progressive implementation strategy**:
- **Win rate comparisons**: Horizontal bar charts (easier to read labels)
- **MCP Servers**: gmail
- **Permission definitions**: Stored in `lang/zh_permission.json`
- **Checkpoint/Savepoint** - State management for track identification

### State Management
if (withoutCoverArt.length > 0) {
  const audioFile = await taglib.open(buffer);
  if (value !== undefined) {
    foreach (loopLanguageForColumn($column) as $c) {
        $article->{$c['column']} = $request->get($c['column']);
    }
}
```

### 6. API Routing - Use `buildForm`/`submitForm` Pattern

**Pending State:**
```
✅ Matching → ✅ Market → ✅ Risk → ✅ Settlement
(Green)     (Yellow)   (Gray)     (Gray)    (Gray)
```

**Usage Guidelines:**
- ✅ Transaction mined
- System handles 10,000 concurrent intents
- <100ms P99 latency for matching

---

### **Use Case 4: Fraud Prevention (Evolving Patterns)** (HIGH VALUE)

**Purpose**: Intelligent price analysis and market making

**When requirements evolve:**

1. **Check Current Task** → `archon:manage_task(action="get", task_id="...")`
3. Identify next priority task
├── api.py                      # Legacy monolithic implementation (deprecated)
3. Explore key dimensions:
   - Amount: 10000000 (base units) = 10 USDC
   - Works for Blink and BlinkParallel examples

For basic operations without manual memory management:

```python
│  ┌─────────────┐       Socket (TCP)        ┌──────────────────────┐ │
3. Cache common patterns (reduce API calls by 30%)
3. State creation and code clarity.

### Feedback Loop: Structured Rejection Scopes

[](https://gist.github.com/dauglyon/d81a96b812b3298f8d862005387b9a0c#2-namespace-ownership-ambiguity)

When rejecting, reviewer selects a git worktree with one click
const files = ["song1.mp3", "track02.mp3"];

// Read tags from all files (10-20x faster than sequential)
3. If both are good → adjust Acceleration (lower = smoother)
source venv/bin/activate && python -m uvicorn services.api:app --host 0.0.0.0 --port 8000 &
```

---

## 📊 Technical Specifications

### Retry Logic

```typescript
// Using Simple API batch functions (for specific file lists)
2. **Signature Verification**: ECDSA signatures checked
const taglib = require("music-metadata");
const results = await readTags("file.mp3");
```

## Supported Formats

3. **Prompt Building Updated**:
   - Set `embed_individually: true` for large document sets
   - Works for Blink and BlinkParallel examples

index.html (no package.json) -> Static HTML     -> Use Pages Git Integration (no build step). Docs: https://developers.cloudflare.com/pages/get-started/git-integration/

### Setup (Dashboard)
│
│     ↓                                                    │
https://faucet.circle.com/users/1
```

### REST API
```
START
  show_citations: false         # Include source-based retry
  ↓
... continues until complete
```

### 3. Verify Deployment
```bash
2. **Take screenshots** - Capture working features
2. **Use in component**: Replace hardcoded values with query data. Best for judging/showcase.

### **Issue: App starts but crashes**
**Fix:** Check environment variables are set correctly. View Railway logs.

### **Step 1: Sign Up**
Wait 10-15 minutes for deployment
│   ├── logging.py             # Centralized logging
ccremote --help
```

## Provider Support

### 1. Plan Mode Default
- ✅ Real-time agent logs appearing
- Stores data in SQLite for widget identity.
- Changelogs are maintained per package (see `packages/**/CHANGELOG.md`) and aggregated root-level `CHANGELOG.md` as needed.

## Developing LM Clients

muestrame el flujo del codigo de este proyecto

*Session: cbaa56dfc59ba3a0cb5ce17d841cd999 | Generated: 03/07/2025, 11:14:22*

### Analysis Summary

Gas Town includes built-in utilities for creating compiled Deno binaries:

### JSR Package (`@charlesw/taglib-wasm`) - Preferred for Deno

- **AI/ML Engineer** (2): Agent design and LLM integration
- **Use cases**: Daily aggregations, cleanup tasks, periodic syncs

---

### 4. Circular Import Fix ✅

**When requirements evolve:**

1. **List Pages (list-page.vue or model-form.vue)** - To load form data and approval requests.

## Project Overview

DeerPortal is a hybrid board/card game built with SFML 3.0 and C++17. It's a cross-platform game supporting 0-4 players with AI agents working on projects with explicit schemas (implementation uses safe evaluation with restricted character sets):

```tsx
) : currentView === 'stepX-my-feature' ? (
  <div className="w-full h-full">
    <auto-button type="plain" @click="goBack">{{ $t('Back') }}</auto-button>
    <div class="w-full">
        <div class="flex space-x-2">
            <h1 class="text-2xl font-bold">
                {{ isCreate ? $t('Create admin') : $t('Edit admin') }}
            </h1>
            <auto-button :busy="formBusy" @click="emit('close')">{{ $t('Cancel') }}</auto-button>
            <auto-button type="danger" small :full="false" @click="onDelete(record)">
                <font-awesome-icon icon="fas fa-trash" />
            </auto-button>
        </div>
      ))}
      <button onClick={() => handleCreate("New task")}>Add Task</button>
    </tr>
</div>

<!-- Form layouts -->
<script setup>
import useAutoForm from "#/shared/components/form/form-contact.vue";

// Error handling utilities
OPENAI_API_KEY=AIza...
```

---

## Performance Tips

wrangler tail                   # Stream production logs
npm run docs

# Step 1: Request payment (402)
LANGSMITH_API_KEY=AIza...

# LangSmith (Tracing/Observability)
const statusOptions = $helper.getEnumOptions('Deposit.status');
const bankOptions = $helper.getEnumOptions('CompanyBank.bank_code');

// Basic modal opening
gt convoy create CACHE  # Output includes namespace id
# BEFORE (deprecated)
