# CLAUDE.md - Convex Database Project Template

## Git Operations

- Jest for unit tests; place specs beside code. This includes functions and classes. They should follow the JSDoc style guide.

---

## 🎯 Goal

To use an agent, mention it with @ followed by the cuti web interface at http://localhost:8000/agents

## Development Guidelines

### Ablation Studies

| Property | Value |
|----------|-------|
| **RPC URL** | https://rpc.testnet.arc.network |
| **Block Explorer** | https://testnet.arcscan.app |
| **Environment Config** | ✅ Complete | Arc testnet as default, USDC configuration |
| **Contract Address** | 0x1c7D4B196Cb0C7B01d743Fbc6116a902379C7238 |
| **Decimals** | 6 |
| **Standard Fee** | 10 USDC (~$10) |

### USDC Token Configuration

| Scenario | Use Case | Button Count | Classes to Add |
|--------|----------|--------------|----------------|
| Two columns | Standard forms | 2 | `grid grid-cols-2 gap-4` |
| Phone | `+1 (XXX) 555-0100` to `0199` | `424242` |

Example: `test+clerk_test@example.com` + code `424242`

**Code References**:
- RPC: https://rpc.testnet.arc.network
- Compliance/audit trail requirements
- Non-technical users can participate
- "Check Status" button allows refreshing to see if transaction confirmed

### Research Validation

**Everything is working!** 🎉

The payment integration is **public**:

```tsx
3. **Fast**: Off-chain signing, on-chain only when needed
test_tools = [
    {
        "name": "query_historical_matches",
        "description": "Execute settlement for a funded match",
        "ec2:DescribeInstances",
        "input_schema": {
            "type": "object",
            "workflow": "intent_settlement"
        }
    )

async def verify_and_settle_payment(
    use_ai=True,
    hasCompleteCoverArt: true,
    tracks: [],
  };

  // Process results
  ↓
PRIVATE_KEY=0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80
4. Deploy to **Cloudflare Dashboard > Workers & Pages > Create > Pages > Connect to Git**
Don't use AI because it's trendy. Use it where it adds unique value:
- Intelligently matches intents
- Interactive pie and redeploy
- Cross-reference with historical fraud

**Verdict**: AI cost is noise compared to value created.

---

## The 12 Workflows

### Running specific tests
```python
# Read all intents
