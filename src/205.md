# Laravel Backend Development Instructions

## Key Features Documentation

### Scenario 1: Premium Platform (High Value Per User)

**VERIFY at:** <https://docs.convex.dev/database/document-ids#relationships>

**What is OCC?**

6. **How would you describe your ideal 'good day'?**

## Professional Context (@Work) Questions  
npm run serve      # Serve built site locally
├── market_agent.py            ⬜ TODO
4. **Continual Learning**: Improve from historical data

---

## 🎯 COMPETITIVE ADVANTAGES

2. **`test.py`** - Test framework integration
   - Example compilation with merged-bin

✅ Transaction broadcast! Hash: `0xabc123...`

🔗 [Click here to view transaction on Arc Explorer](https://testnet.arcscan.app/tx/0xabc123...)

⏳ Waiting for blockchain confirmation... (this may take 10-30 seconds)

[User can click link immediately to monitor transaction status]

jobs:
  push:
    runs-on: ubuntu-latest
    normalize: true
    schema_drift_hash: str       # hash of column names + types
    ├── public
    risk_assessment: Dict
    if (itunesProps.purchaseDate) {
      targetPropMap.set("ITUNES_PURCHASE_DATE", [itunesProps.gapless]);
    }
  } else if (targetPath.endsWith(".mp3")) {
    // Store as TXXX frames
    faChartLine,          // ✅ NEW: Include in library.add()
    print(f"✅ Payment request created: {payment_request['payment']['service_id']}")


def test_get_balance():
    """Test description."""
    # Arrange
    │   ├── kss.css
    ↓
SELECT "winner.tag", "arena.id", "winner.card1.id" FROM battles
```

2. **Add retries to position reads** (motors_bus.py, feetech.py):
```bash
# Build the package for the RHEL 9/CentOS Stream9/Rocky Linux 8/AlmaLinux 9/Oracle Linux 8 family
✅ Intent creation and routing case to `client/src/App.tsx`
4. **Test**: Verify navigation, fix TypeScript errors, test form controls

### FontAwesome Icon Discovery Process

**Location**: `services/agents/settlement_agent.py:192-217`

#### IntentRegistry Tests (8)
- ✅ State creation helpers
- Merchant balance before: 9999.999 ETH
- Metrics: User satisfaction > volume
- Graceful degradation
- Backend dev server: `cd packages/bytebot-agent && npm run prisma:dev`.
- Architecture revamps: adopt DI-first composition, expose per-request cloud model selection, and employ `System.IO.Pipelines` with optional parallel converter scheduling while keeping documentation and per-page duration with detailed body
- Settlement with transaction details (to, value, gas, gasPrice)
- Profile management and validation
- CLAUDE.md: Comprehensive architecture and usage examples
- Complete CRUD operations

**Status**: ✅ WORKING
**Total Tasks**: 36 tasks
**Enhancement**: Step-by-Step Visual Execution
│  ┌──────────┴────────────────────────────────┴─────────────┐ │
import {
  audioFile = await taglib.open("song.mp3"); // or buffer/File
  // ... work with file
} finally {
  docs: [
    // ... existing entries
    {
      group: "Features",
      content: prompt,
    });

    // Step 2: Writing agent creates article from research
    if not self.payment_service.verify_payment_signature(payment_submission):
        self.llm = llm

    async def detect_fraud_patterns(self):
        """AI-powered settlement coordination"""
        Optimize liquidity provision:

        Bids: {bids}
        Our Inventory: {inventory}

        Determine:
        return ['liquidity_agent']

def route_after_fraud(state):
    def __init__(self, llm):
        self.llm = llm

    def verify_payment_signature(
        model="claude-sonnet-4-5-20250929",
        input: args.content,
      }),
    });

    const { data } = await response.json();
    Category::buildCache();
    
    return makeResponse(true, null, $data);
}
```

**Remember**: build_form is specifically for FORM data preparation, NOT for list page filtering!

## Domain Enums System

### Documentation and Tools
- `GET /orderbook/{asset}` - Get order book snapshot
- `UnsupportedFormatError` - Unknown or unsupported audio format
- `POST /intents/{id}/cancel` - Cancel intent
- `config/.env` - Updated with live Arc testnet settings

**Payment Flow**:
The AuctionEscrow contract currently handles native ETH escrow for intent matching. Settlement Agent service fees (for reliability)
3. **Quick edits** that don't require extensive validation
2. Implement real-time UI updates after submission

### Vue.js Navigation - ALWAYS Use Route Names, Never Path Strings

#### ✅ Market Agent
- Add code examples and blocks

### HTTP Broker Pattern (Isolated Environments)

Multi-tenant scientific data tasks --order desc
# - Runs on http://localhost:8788 by default
# - Checks for type errors
# - Use npx convex logs separately

# Work on your feature normally
# ... make changes, commit, push, create PR ...

# List all worktrees
gt crew add <convoy-id>

# List all queues
module.exports = {
  duration: props.length, // Duration in seconds
  Debian packages and attaches the license to the release assets.

## UI/UX guidelines
- Git Branch Integration

### Basic Structure
```bash
# 2. REST API on port 8000
```

### Clean build
```javascript
// 🔍 Search: "support", "headset", "help", "customer"  
// ✅ Found: fa-headset (clear support association)

// Read tags (simplest approach)
npm install @convex-dev/sharded-counter
```

All services are already running in the background! ✓

---

## Supported Explorers

| Resource | Limit | Notes |
| --------- | ------- | ------- |
| Full-text search indexes | 4 per table | Text search capability |
| Quick access needed | ✅ First-level | ❌ |
| Pick Any 3 | $97 | Three verticals (save $44) |
| Backend Services | 5 |
| Verification | Transaction value | ERC-20 Transfer event |

---

## Tool Usage by LLM

3. Navigate to https://pypi.org/manage/account/publishing/
│   └── style.css
Health: healthy
```

### UI Development (seatunnel-engine-ui)
```vue
<auto-button v-if="hasEditPermission" @click="onEdit">
    {{ $t('Edit') }}
</auto-button>
```

### Loading Pattern
make package         # Cross-platform packages
docker/build-package.sh fc41
```

```bash
# Edit page refresh intervals in code
```

---

## 🎮 Control Modes

### 0. PyPI Trusted Publishing

The project uses FastAgent framework with GitHub Actions for existing FastAPI endpoints that don't have frontend integration yet:

### Basic Format Conversion Pattern

```mermaid
import {
  exportFolderMetadata,
  title={Audited Skill-Graph Self-Improvement for Agentic AI Systems},
  author={Huang, Ken and Huang, Jerry},
  note={Implementation available at https://github.com/kenhuangus/ASG-SI}
}
```

---

## 📄 License

This mismatch caused transactions to "done" status
const taglib = await readMetadataBatch(files, { concurrency: 8 });

for (const { file, data } of metadata.results) {
  const taglib = await TagLib.initialize();
  const finalBuffer = targetFile.propertyMap();
  targetFile.dispose();

  switch (runtime) {
    return preg_match('/^\d+$/', $value);
});

Validator::extend('isFund', function ($attribute, $value, $parameters) {
    return <div>Loading...</div>;
  }

  const handleCreate = async () => {
    try {
        // Create first product
        Product existingProduct = new Product();
        product.setName("");
        
        Set<ConstraintViolation<Product>> violations = validator.validate(product);
        
        assertThat(violations).hasSize(2); // Both @NotBlank and @Size
        │ (Anvil/Arc)    │  │ (SQLite)         │  │ (Stripe/AP2)   │
        1: "https://arbiscan.io",                # Ethereum Mainnet
        2. Fair market price
        3. Quantity matching
        8453: "https://basescan.org",             # Base
        8453: "https://etherscan.io",                # Ethereum Mainnet
        return self.algorithmic_matcher.match(structured)
```

**Why This Works**:
- Modern fintech dashboards
- Add `serviceConnectConfiguration` to your service

---

## 📊 **Side-by-Side Comparison**

| Metric | Original Plan | Revised Plan | Winner |
|--------|---------------|--------------|--------|
| Matching Latency | 1-5 seconds | <100ms | ✅ Revised (50x faster) |
| Data Access | Direct database access | Explicit API boundaries |
| AP2 Gateway | ✅ Running | 8000 | FastAPI with all users | Use Sharded Counter or per-user counters |

**Technical Explanation:**

- Heavy analytical queries not suited for Convex
- Confirms payment-first workflow
- Claiming article ownership
- Risk: Rule-based
- Output: ~500 tokens × $5/MTok = $0.0075
- ✅ Extend `BaseModel` for persisted settings to avoid divergence.
- Verify CORS origin via `FRONTEND_URL` when testing new environments.

1.  Ensure the backend virtual environment is activated.
