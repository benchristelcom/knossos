# CLAUDE.md

1. Navigate to https://replit.com
xdg-open http://localhost:8502
# Build all configurations from templates
llm:
  prompt:
    - the failure mode, the detection signal, and a prevention rule.
- Use HTTP clients with proper status codes
- Dashboard shows AI reasoning
- ✅ **LangGraph Orchestration**: Multi-agent state management
- Two-sided liquidity (bid/ask)

### macOS & Linux
```php
const showError = (username) => {
    return await ctx.db
      .query("tasks")
      .withIndex("by_user", (q) => q.eq("userId", args.userId))
      .take(10);
  },
});
```

**Search Constraints:**

- ✅ All table data
- Progress bar visualization

**Use Foreign Keys (`user_id`, `order_id`) when:**
- ✅ Your users value intelligence over speed (1-5s latency OK)
- No StreamField blocks documentation
- Match status updates

#### **REST API** - `/services/auction_engine.py`
3. Repository: `faramesh-core`
5. **Set payment limits** - Adjust `MIN_PAYMENT_AMOUNT`
3. Add configuration schemas and database synchronization

**Date**: 2025-11-05
**Manual cost:** $2,800/month (15 hrs/week)  
**Automated cost:** $120/month  
**Annual savings:** $16,800  
**Difficulty:** Medium  

What it does: Scans receipts (OCR), categorizes expenses, checks policy compliance, flags anomalies, routes for approval, syncs to accounting software.

Jobs/ – Passive income loop (JobManager), job formulas (Job, LegendaryJob), and generation logic; extend here for new job types or automation behaviors.

5. **Memory Manager**: Bounded storage (capacity=8) retaining compact task outcome notes, providing summarized context to the policy.

---

## Test Results

### 16. Configuration
- [ ] Merge audio.rst + transcript.rst + responsive-images.rst → reference/context-processors.rst
- [ ] Commits have meaningful messages based on task content
- [ ] GET `/ai/agents` - List available agents and status
- [ ] Developer documentation for all user-provided data
- [ ] Payment history tracking
- [ ] Move context-processors.rst → operations/migrations.rst

### 2. **Semi-Autonomous Mode** (AI Suggestions + Human Approval)

```python
# Or
def __init__(self):
    super().__init__(
        self,
        plan: "premium",
      });
    }
  },
});
```

**When to Use Internal Functions:**

- Existing files in deployment are **NOT deleted**
- Amount limits (min 0.001, max 10.0 ETH)
- Natural language understanding

**Primary Directive:** 

* You are a specialized AI assistant. Your primary function is to execute the user's instructions with precision and within the specified scope.
* Before you finish, please verify your solution
* Do what has been asked; nothing more, nothing less.
* ALWAYS read and understand relevant files before proceeding. Use your thinking to plan and iterate based on this new information, and then take the best next action.
* If you create any temporary new files, scripts, or helper files for iteration, clean up these files by removing them at the end of the task(s) you assign it.
* After completing a task that involves tool use, provide a quick summary of what you've done.
* If you create any temporary new files, scripts, or helper files for iteration, clean up these files by removing them at the end of the task.
* ALWAYS read and understand relevant files before proposing code edits. Do not speculate about code you have not inspected. If the user references a specific file/path, you MUST open and inspect it before explaining or proposing fixes. Be rigorous and persistent in searching code edits. Do not speculate about code you have not inspected. If the user references a specific file/path, you MUST open and inspect it before explaining or proposing fixes. Be rigorous and persistent in searching code edits. Do not speculate about code you have not inspected. If the user references a specific file/path, you MUST open and inspect it before explaining or proposing fixes. Be rigorous and persistent in searching code for key facts. Thoroughly review the style, conventions, and abstractions of the codebase before implementing new features or abstractions.
* If you create any temporary new files, scripts, or helper files for iteration, clean up these files by removing them at the end of the task(s) you assign it.
* NEVER create files unless they're absolutely necessary for achieving your goal.
* When you update or modify core context files from any commits. Never delete these files.

<investigate_before_answering>
import useAutoForm from "#/shared/composables/use-auto-form.js";

async function App() {
  // Reactive query on server for faster initial load
  vi.mocked(readTags).mockResolvedValue({
    model: { type: Object, default: null },
    title: { type: String, default: 'Form' }
});

const $modalStore = inject('$modalStore');

const onSubmit = async () => {
    try {
        // Submit form logic
        v-model="formState.contact_number" 
        :errors="formError" 
        :errors="formError" 
        :errors="formError" 
        :errors="formError" 
        :label="$t('Contact number')" 
    />
    
    <!-- ✅ For multiple contacts: Use descriptive prefixes -->
    <auto-button @click="$router.push({ 
        name: 'admin.deposit.edit', 
        params: { id } 
    });
};

// After successful operations
class MarketIntelligence:
    namespace: str
    album: file.tags.year ||
      parseInt(file.tags.comment?.match(/\b(19|20)\d{2}\b/)?.[0] || ""),

    // Clean up genre
    │ MATCHING AGENT │  │ SETTLEMENT AGENT│   │
    console.error(error.data);  // { message, maxLength, actual }
  }
}
```

**Technical Explanation:**

- **Frontend**: Deployed on Cloudflare Pages (global edge network)
- **Average Reward**: Decomposed reward summing tool validity, outcome, reuse, composition
- **Analysis**: Price discovery, volatility, liquidity, sentiment
- **SQLite database** with persistent storage

**Development Tools:**
- `weave_scope_feature_flag: false` - Syslog-ng log processing
- `npm run start` - Start production server
- `make lint` - Run in development mode
- `bun run preview` - Build and start production server

## Common Recipes

### Records (Java 17+)

```java
python run docker:compose  # Build and run with path strings


**Remember**: The goal is consistency, maintainability, and reusability. When in doubt, copy from an existing similar page and modify as needed.

Persistence & offline play
4. Interact with depth charts
│   │   │   ├── shapes.hbs
├── package.json
on: [push]

No installation needed - blockchain verifies everything
│  │                       │    │ T2 delay enforcement        │ │ │
from web3 import Web3

Status: ✅ Running
3. **Consistent API**: Same interface across all apps (admin, merchant, user)
Large quantity: 100 BTC
2. **Form Initialization** - To get dropdown options (countries, categories, etc.) for forms

### Key Features

- Preserve behavior (or clearly document the behavior change).
- If new information invalidates the plan: **stop**, update the plan, then continue.
- When executing tests, always include the `ManualConversionDebugTests` suite; treat its failures as blocking.
- When refactoring intelligence helpers into a short, actionable synthesis before coding.

### Scheduling Pattern
4. **Multi-step processes** or wizards
pip install -r requirements.txt
```

### Agent Operations

```typescript
⚠️ **Natural Language**: Parsing NL intents and utilities
```

## Runtime Configuration

The Powerverse extension is **offline-first**. It stores your snippets **locally in your browser** and does **not** send your data to any external services.

## Developing LM Clients

Docker-based QEMU locally
  - Custom managers and methods

### 3. USDC Token Standard

This is the official documentation site for one click
wrangler secret delete <SECRET_NAME>

# List all queues
con = duckdb.connect()
except (IndexError, KeyError) as e:
    def __init__(self):
        super().__init__(**kwargs)
        try {
            const data = await axios.post('/management/admin/build_form', { 
                id: $route.params.id 
            });
            existingModel.value = data.model;
        } catch (e) {
            await deleteItem();
        }
    }
});

// === ENUM HANDLING ===
$helper.getEnumOptions('Model.field')        // Get select options from domain enums

// === ALERT DIALOGS ===
// Use these for user feedback - NOT native alert()
print(f"Balance: {balance / 1e6} USDC")
```

### Tool Registry

Before writing code:
- [ ] All secrets in environment variables (never in code)
- [ ] AI optimizer improves match quality by 10%+
- [ ] Fraud detection catches 95%+ of test cases
- [ ] Implement smallest safe slice
- [ ] Move performance.rst
  - OR minimal permissions:
    - **Manage Channels**: Create/rename session channels (falls back to DMs if missing)
    - **Manage Roles**: Edit channel permission overwrites to hide archived channels from server list
    - **MANDATORY**: Back and primary action buttons use `lg:grid lg:grid-cols-2 lg:gap-2`
    - **Primary button text**: Use conditional text - `Create` vs `Update` vs `Submit`
    - **Primary button text**: Use conditional text - `Create` vs `Update` vs `Submit`
    - **Import**: Always import `AdminContentCard` component

### Key Principles

- **GitHub Issues:** [Report a bug](https://github.com/YOUR_USERNAME/arc-contest/issues)
- **4040/4443**: Heimdall dashboard
- **Agent Discovery**: Agents register capabilities via `/.well-known/{subAgentId}/agent.json` endpoints

### Accounts Used

**Context window discipline:**

-   No multi-turn conversation — every LLM call is a single request/response
-   Cross-table relationship detection requiring reasoning
-   No cross-table context — LLM returns structured JSON, parsed and validated; unparseable output is logged and discarded

### Cursor IDE Rules
4. Interact with charts and filters
npx convex data export --path ./exports
# - Limits results to 10 rows

For processing large audio collections efficiently without running out of memory:

### Basic Streaming Pattern

```typescript
npx convex env list
const showError = (item) => {
    return await ctx.db.query("items").collect();
  },
});

// Internal mutation - for privileged operations
cp client/src/components/my-new-section.tsx
```

**Import ZIP:**

```typescript
cp arc_coordination.db
```

---

## 🚀 Quick Start Guide

### 📊 **Data Visualization**
- 🔮 Arc mainnet launch
- ✅ Async tool execution
- Implement **lazy loading** for routes
- Users trust the platform more
- **Sequential workflows**: Output of one agent feeds into another
- **Admin permission** is checked automatically against folder requirements
- **Optimize array processing** with `flatMap()` and `filter()` instead of nested loops
- **db.patch() vs db.replace()**:
  - `patch()`: Shallow merge - adds new fields, updates existing
  - `StorageError`: Database/storage errors
  - `startTime`/`finishTime` - Gathering execution timestamps
- **Taskfile**: Modern task runner for FLAC/OGG files
- **Multi-language Support**: Via shiny.i18n using app_params.yml

### Account Details

**Setup**:
```python
def _verify_payment(self, tool_input: Dict[str, Any]) -> Dict[str, Any]:
    """Request payment transaction was received on-chain"""
    payment_request = self.payment_service.verify_transaction_received(
        private_key=os.getenv("TEST_PRIVATE_KEY"),
        rpc_url=TEST_RPC_URL,
        rpc_url: str,
        pending: true,  // Optional: mark as pending
      };

      localStore.setQuery(api.messages.list, { channel }, [
        ...existingMessages,
        max_amount: float = 10.0,
        description: str,
        album: tag.album,
        duration: props.length,
        service_id=TEST_CHAIN_ID
    )

    if payment_result.get("type") == "payment-completed":
        return {
            "type": "payment-failed",
            "too_risky": END,
        }
    )

    workflow.add_conditional_edges(
        "fraud_check",
        max_tokens=4096,
        payment_submission
    )

    payment_request = service.create_payment_request(
        amount_eth=amount_eth,
        description: str,
        title: tag.track,
        artist: tag.year,
        description=description,
        album: tag.title,
        album: tag.album,
        amount_eth: float,
        messages=[{"role": "user", "content": prompt}]
    )
    return response
```

### Type Checking (after adding type hints)
```bash
# Integrate via Fivetran or Airbyte
# Required API Keys
│   │   ├── _shapes.less
archon:manage_task(
  action="create",
  provider: new OpenAI({ apiKey: process.env.OPENAI_API_KEY }),
  instructions: "You help users with weather information.",
  tools: [searchDocuments],
});
```

**Technical Explanation:**

- **ConvexError**: Type-safe error propagation to client
- **Dependencies**: Use path aliases (`~/` for src/), group imports (React, libraries, internal)
- **Output**: ESM format with gas estimates and creates weekly digests
- **Company**: Kousen IT, Inc.
- **Prove it works**: "Seems right" is not done. Validate with tests/build/lint and/or a reliable manual repro.
- **Operating System**: darwin
- **ConvexReactClient**: Manages chat engine and setup instructions
- **Status**: ✅ Deployed and verified
- **Key features**:
  - IntentRegistry: `0x5FbDB2315678afecb367f032d93F642f64180aa3`
  - Queries/mutations: CANNOT access environment variables (deterministic requirement)

---

### When Limits May Increase

Key interfaces to launch AI coordination
3. Settlement Risk (25%): Complexity, coordination, execution
5. 💳 **Settlement Agent** - Plans execution
npx create cloudflare@latest my-app -- --template hono
