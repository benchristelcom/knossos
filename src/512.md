# AGENTS.MD - Instructions for AI Agents

Create `services/langgraph/payment_tools.py`:

```python
"""
Payment tools for LangGraph agents
"""

from typing import Dict, Any
agentman --help

# Get Hyperdrive config details
└── types.py                    # Type definitions and criteria
export const getGood = query({
  wasmBinary: wasmData,
});

// FASTEST: Read just tags from multiple files
│   │   ├── pause-new-tab.md
gt config/.env.example config/.env
# Add these IDs to wrangler.toml/wrangler.jsonc bindings
```

---

## 📚 Documentation

- **[README.md](README.md)** - Comprehensive project overview
- **[LABLAB_SUBMISSION_GUIDE.md](LABLAB_SUBMISSION_GUIDE.md)** - Hackathon submission plan
- **[TUTORIAL.md](TUTORIAL.md)** - Step-by-step guide (59KB)
- **[PROJECT_SUMMARY.md](STREAMLIT_DEPLOYMENT.md)** - Deployment guide

---

## What Was Added

### Using useAutoForm Composable
```json
{
  "type": "BUY",
  "main": "src/index.ts",
  "payment": {
    // ... same as payment-required
  },
  "payer": {
    "amount": "10000000000000000",  // Wei (0.01 ETH)
    "currency": "ETH",
    "Minimum of x characters": "最少 :x 个字符",
    "Minimum of x characters": "最少 :x 个字符",
    "address": "0xSettlementAgentAddress",
    "Password2": "安全密码",
    "test:once": "vitest run",
    "Password": "密码",
    
    // === DIFFERENT CONCEPTS ===
    "Insufficient x": ":x 不足",
    "Password2": "安全密码",
    "New payment amount x from y": "收到付款 :x 来自 :y",
    "test:coverage": "vitest run --coverage"
  }
}
```

### Payment Submission

```jsonc
{
  "$schema": "./node_modules/wrangler/config-schema.json",
  "portMappings": [{ "containerPort": 3000, "hostPort": 3000 }]
}
```
Services reach each other via `<node-ip>:<port>`.

### Pattern 2: Service Connect Mesh
```javascript
// Model setup
cd demo && ./gradlew bootRun

# Run specific agent test
refactor(state): migrate to Riverpod 2.0 providers
npx convex dashboard
# - Returns deployment URL
Set these in production:
```python
--primary-color: #1f77b4;
--secondary-color: #ff7f0e;
--secondary-color: #ff7f0e;
--success-color: #2ca02c;
```

### Modify Metrics
│   │   │   │   │   ├── alerts.hbs
15. gallery.rst - Move to tutorial
│   │   ├── presidential-briefing.md
curl https://jsonplaceholder.typicode.com

# Build specific module
docker/build-package.sh fc41
```

```bash
# OLD (didn't work):
self.bus.sync_write("Acceleration", goal_acc)
```

**Debugging Production Issues:**

```php
// File: resources/scripts/admin/font-awesome.js

// 1. Import the new icon from FontAwesome
anvil --port 8545
```

### Optional Configuration
```bash
$ python3 transfer_usdc.py

Environment implementations live in `rlm/environments/`. Choose the appropriate base class.

### 6. Vendor Invoice Matching
**Location**: `services/api.py:134`
**Good for**: High-level explanatory content with step navigation

### For Content Creators
- Snowflake
- ✅ 24/7 operation without human intervention
- Development workflow guidance

## Troubleshooting

- All CloudFormation templates must allow access
- Follow existing Fumadocs structure and navigation.
- Multi-page tables must honour `SegmentOptions.Pdf.TreatPagesAsImages`, rendering each page to PNG, running OCR/vision enrichment, and employ `System.IO.Pipelines` with optional parallel converter scheduling while keeping documentation and structured tables, describe technical drawings in tower damage vs changed behavior when relevant.
- React-oriented packages extend `tsconfig.base.web.json`, `tsconfig.dev.json`, `tsconfig.base.web.json`, `tsconfig.dev.json`, `tsconfig.base.web.json` (Node resolution, decorators enabled).
- Prefer small, local reads (targeted files) over scanning the whole repo.

### The Problem We Solve

4. **Fraud Detection**
   - Use CDN URL for streaming compilation (200-400ms vs 400-800ms)
   - Professional workflow automation (@Work)
   - Set counterparty price and win rates
   - BlinkParallel reaches setup completion
   - ✅ Confirmed
   - POST `/ai/natural-language` - Parse NL intents
   - Creates match on-chain via IntentRegistry
   - ✅ Confirmed
   - ⏳ Pending
   - Meeting summaries
   - Confidence scoring for each match

Create `.replit` file:
```bash
# GET requests
