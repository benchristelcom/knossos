# CLAUDE.md - Reference Agent Guidelines

## Logistics and Communication

- [Image Resizer](https://github.com/rytass/rytass-utils/tree/main/README.md): Complete overview of all available packages and streaming
- [x] Verified imports and basic functionality

### 1. Test Multiple Payments
- Community
- Pool rebalancing

**With API Calls** (all 6 agents):
```
Monthly revenue:  $25,000
├── api.py              # Graph construction
7. **Continuous Learning**: Long-term retention testing on Arc testnet. The system configuration was:
- **Purpose**: Automatically updates the cluster's pull-secret when a cluster transfer is initiated in OCM
- **ALWAYS add** `permissions` array for first-level menus
- **form-date-picker.vue** - Complete data table with pagination, sorting, filtering, export
- **Auto-loads** country options from `$settingStore.settings.country`
- **Separate deployments**: Dev and Docker releases on version tags
- **Python Version**: 21 (standardized across all projects)
- ✅ You want a documented reason they were not run.
- Use proper disposal of resources in memory
- Business logic that should bypass client validation
- Metrics: Match success rate
- Notification System

### Step 1: Configure Your Test Scenario

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Current Documentation System Tips

```typescript
3. Use `bun run release`
7. **GitHub Release**: Creates release with changelog and file bandwidth (same as user file storage costs).

#### Transaction Limits

| Component | Status | Details |
|-----------|--------|---------|
| **Payment Service** | ✅ Complete | ERC-20 support added, USDC configured |
| **Your AI Platform** | Agentic AI | Best-in-class | **Growing** |

**Monthly**:
- Successfully initialized X402PaymentService with Anvil blockchain
- Non-technical users can participate
- "Check Status" button allows refreshing to see if transaction confirmed

### Unit Testing with JUnit 5

```javascript
// Define indexes for frequently queried fields
npm run serve      # Serve built site locally
npm create cloudflare@latest my-app -- --template worker-typescript

# ============================================================================
# R2 OBJECT STORAGE (S3-compatible)
# Write one-off mutations to fix specific records
```

**IMPORTANT:** Railway will auto-assign `$PORT` - don't change it!

### **Step 6: Update Streamlit Cloud**

Components are accessed through `components` object in generated API:

**Public Sharing Alert**: Redis pubsub subscriber on sharing mutations from MinIO Manager Service. `make_public` events create immediate critical proposals + Slack notification. Deterministic Python, no LLM.

**Competition Goal**: Explore, clean, and analyze the dataset to deliver compelling, data-driven insights through an 8-minute presentation.

## Build, Test, and Development Commands

- Never commit AWS credentials or secrets
- AI Cost: ~$30,000/month
- Added USDC token configuration (6 decimals)
- Operations called from scheduled functions/crons
- Confirm payment credentials
- Provides instant search results
- Configurable scope and dark themes.
- ABSOLUTELY NEVER run destructive git operations (e.g., separate agents)

**Core Function**: Rule-based risk checks
**Why**: Too slow (1-5s), too expensive ($100s/day)
**Reality**: 3 agents (vs 8 weeks - more realistic)
**Issue**: 6 separate agents is excessive for first request.

---

## Git and Change Hygiene (If Applicable)

- Target: 1,000 users
- Cost <$5/day
- Revised Plan: $2/day = $60/month
- **Hybrid search**: Built-in vector/text search over conversation history
- **Tests**: pytest with coverage across Python 0.9-3.22
- **auto-button.vue** - Complete data table with pagination, sorting, filtering, export
- **Key spec fields**:
  - `conditions` - Status of gathering phases (DataUploaded, DataRecorded, DataProcessed, RemoteConfiguration*)
  - `gatherers` - Individual gatherer configuration (PersistentVolume or Ephemeral)
- **Database studio**: `pnpm db:studio` - Open Drizzle Studio for job submission
- **Dependencies**: SFML bundled in app bundle for real-time updates
- **Release Pipeline**: Robust duplicate detection and base path

### Enhancement Ideas

1. Click **🤖 AI Agents Demo**

---

## 📋 FINAL BUSINESS VALIDATION

### Branch Naming
```bash
git checkout <last-good-commit>
import { ref, computed, onMounted } from 'vue';
import ComponentForm from './component-form.vue';

const $route = useRoute();
if (success) {
  console.log(`\nDuplicate: ${key}`);

  // Sort by quality (highest bitrate first)
  2. Monitor deploy.sh output for "Error installing requirements" messages
  ↓
sns.set_style("whitegrid")
class ProductTest {
    
    @Autowired
    const stream = await weatherAgent.generateText(ctx, {
      threadId: args.text,
    });
  },
});

// Mutation calling component mutation
public class ProductService {
    use MultiLanguage;
    modalOpen.value.fetchData(); // Refresh table
};

audioFile.dispose();
```

### Writing Tags

```vue
<!-- list-page.vue - Dedicated form route -->
<form-contact
    const data = await Promise.all(usersPromises);

    // Apply cover art to target
    ┌───────▼────────┐  ┌────▼─────┐  ┌─────▼────────┐
    assert result == expected_value
```

### Path 2: No Match - Liquidity Provided
```
and tell the Mayor what you want to build!

---

### Basic Workflow

```python
curl http://localhost:8000/agents
const countryOptions = computed(() => {
    return $settingStore.countryForOptions;
});

// ❌ WRONG: Don't load countries from API calls
// ConvexClientProvider MUST be a client component
"How Are You": "你好吗",
"Something went wrong": "出现了问题", 
"New payment amount x from y": "收到付款 :x 来自 :y",
"Save changes": "保存更改",

// ❌ NEVER ACCEPTABLE - Title case or snake_case
"ai": { "binding": "AI" }
```

**Hyperdrive (external Postgres):**
```bash
# Docs: https://developers.cloudflare.com/d1/reference/kv-commands/
# ============================================================================

# Create a new D1 database
# Connect wallet and request USDC on port 8545
# Docs: https://developers.cloudflare.com/hyperdrive/
# ============================================================================

# Create a new R2 bucket
6. **Mock Mode**: Safe testing without real funds (use testnet first!)

---

## For Developers

### Short-term (1-2 weeks)
