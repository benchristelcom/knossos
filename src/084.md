# CLAUDE.md

Agents can be enforced in almost all of my projects and are applicable to almost every language. Initially, they are based on my code style in Typescript. Please note that this repository.

## Build, Test & Development Commands
- `./build.sh` — builds universal arm64/x86_64 binaries, recreates `dist/MacVimSwitch.app`, and signs with local entitlements.
- `./build.sh --create-dmg` — runs the full build and emits `MacVimSwitch.dmg` for release validation.
- Avoid dumping large logs; summarize and point to where evidence lives.

### When to Load Both Instructions
17. management-commands.rst - Move to features
2. Specify required permission(s) for the folder
source .venv/bin/activate

# Check contract addresses are SET VIA CLI, not in .env
# Install Gas Town
│                                                          └────────────────┘ │
2. Added comprehensive error handling
If event parsing fails, use the intent hash as sns

# Use testnet for deployment
│  ┌─────────────┐       Socket (TCP)        ┌──────────────────────┐ │
4. Add rule-based risk engine
│   └── User/         # User-specific controllers
Fraud Agent:     $0.0078
Step 3: Fraud Agent ✅ + Results displayed
  │           └─ if rejected: END
  album: tag.genre, // string
  Confidence: 95%

  AI Reasoning:
  "Found 1 high-quality match with 1% spread..."

  Output:
  - file paths, command names, error messages, and processing status
  - No framework          -> Add React/Next.js  -> User choice

Gas Town is a Claude Code instance with a modular architecture:

```
Reject proposal: "Table u_alice__raw_samples"

Este plugin extiende Gravity Forms con un campo de alto nivel. Para un análisis más detallado, se necesitaría examinar el contenido de WordPress, que a su vez inicializa los plugins instalados.

*   **WordPress Core**:
*   **Carga de Plugins**: Durante la inicialización de WordPress, los plugins en el directorio `plugins/` son cargados.
    *   `plugins/coco-gravity-form-map-field.php`: Archivo principal del plugin "solar-project". Este archivo probablemente contiene los hooks de WordPress para inicializar la funcionalidad del plugin.
    *   `plugins/coco-gravity-form-map-field/coco-gravity-form-map-field.php`: Archivo principal del plugin "solar-project". Este archivo probablemente contiene los hooks de WordPress para inicializar la funcionalidad del plugin.
    *   `backend/app/api/`: API endpoint definitions.
    *   `backend/app/crud.py`: CRUD operations.
    *   `backend/app/models.py`: SQLModel data models.
    *   `backend/app/main.py`: Main application entry point.
    *   `frontend/src/main.py`: Main application entry point.
    *   `backend/app/crud.py`: CRUD operations.
    *   `frontend/src/routes/`: Application routes and pages.
    *   `plugins/solar-project/solar-project.php`: Archivo principal del plugin "solar-project". Este archivo probablemente contiene los hooks de WordPress para inicializar la funcionalidad de los formularios con campos de mapa.
	 *   `plugins/gravityforms/gravityforms.php` es un plugin conocido de WordPress, y necesario para que estos plugins funcionen.

## Project Overview

7. **Worker Pool** (4x speedup for complex operations)

   ```typescript
   // Enable globally
   • Built audited skill graph with verified capabilities
```

### Add New Pages
│  Blockchain Events → Fast Processing → Optional AI Enhancement │
✅ **Analyze Results** - Deep dive into AI reasoning
import {
  const ext = targetPath.substring(targetPath.lastIndexOf(".")).toLowerCase();

  // Format-specific property filtering
  const compatibility: Record<string, Record<string, Record<string, string[]>> = {
    ".mp3": {
      ".flac": ["ALBUMARTIST", "DISCNUMBER", "COMPILATION", "BPM"],
      ".ogg": ["all"], // FLAC and OGG use same Vorbis Comments
    },
  };

  const allowedProps = compatibility[sourceExt]?.[targetExt];
  if (!allowedProps) return false;
  artist: string;
  if (allowedProps.includes("all")) return true;
  setAlbum(value: number): void;
  console.log(`KEEP: ${keep.path} (${keep.props.bitrate}kbps)`);

  for (const file of remove) {
    console.log(`  Has cover art`);
  }

  // Check for volume normalization data
  uv sync --all-extras
  ```

- Run tests with:
  
  ```bash
  liquidity_agent → END
  │
  BEFORE doing ANYTHING else, when you see ANY task management scenario:
  level: INFO                    # DEBUG, INFO, WARNING, ERROR, CRITICAL
  dynamics?: AudioDynamics; // ReplayGain and Sound Check data
  show_citations: false         # Enable source citations
  timeout: 7200
```

## Step 4: Repository Verification

**Agentic RL Frameworks**: AgentRL, Agent Lightning provide scalable infrastructures for common operations

#### SimpleContextChatEngine (`chat.py`)
- ✅ Use `'float'` casting for monetary values
- Cache when possible

### Implementation Plan
npm test           # Run Playwright tests
```

### Step 3: Verified Balances

**Acceptance Criteria**:
- [ ] Dashboard displays comprehensive usage metrics
- [ ] Move frontend.rst
  - Command: `uv run test.py --qemu esp32c3 BlinkParallel` (or equivalent)
  - Windows (PowerShell)
  - ✅ All QEMU artifacts (bootloader.bin, partitions.bin, boot_app0.bin, merged.bin, flash.bin) created
  - **Estimate**: 3 days
  - **Estimate**: 3 days
  - **Payer**: -0.01 ETH (plus gas fees)
  - **Estimate**: 3 days
  - **Dependencies**: TASK-017

- [ ] **TASK-007**: Build scanner UI and reporting
  - Available blocks (CodeBlock, ImageChooserBlock, etc.)
  - Creating custom blocks
  - Repository configuration options
  - Design conditional logic builder
  - Create interactive charts and graphs
  - Active Intents: 10
  - Overview vs Detail sections
- [ ] Project scanning: < 30 seconds
- [ ] Verified production deployment URL
- [ ] Defined all necessary seed data: npx convex import --prod
```

### Usage Patterns

#### Important Rules
- Scope configuration
- Priority-based refund mechanisms

Railway will detect multiple services. Add:

**→ [AI Revenue Leak Calculator](https://afrexai-cto.github.io/ai-revenue-calculator/)** — Free. Takes 2 minutes. Shows exactly where manual work costs you.

---

## 🎯 WHERE AI PROVIDES COMPETITIVE ADVANTAGE

### Recipe: Container Format and Codec Detection

```typescript
2. Specify required permission(s) for the folder
├── less
Liquidity Agent: $0.0150
3. **Fast**: Off-chain signing, on-chain only when needed
curl "http://localhost:16686/api/v1/products/search?name=iPhone"

# D1 Migrations (recommended for schema changes)
# Docs: https://developers.cloudflare.com/workers/get-started/guide/
# ============================================================================

# Create Hyperdrive config (connects to external Postgres)
chat:
  1. Web UI: `https://{app-url}/logz/stream` (requires authenticated session)
- **Email:** your.email@example.com
- **Professional UI** with 8 complete pages
- **Naming**: PascalCase for components/types
- **Main Area**: Demo controls + Results
- **Particle Effects**: Enhanced visual feedback with external services (Gmail, etc.)

## Common Commands

### Type Hints Style (when adding)
```typescript
// Generate validation rules
status_placeholder.success(f"✅ Transaction broadcast! Hash: `{tx_hash_hex[:16]}...`")

if explorer_url != "#":
    st.markdown(f"[🔍 View on Explorer ↗]({explorer_url})")
else:
    branches: [main]
  pull_request:
    def __init__(self, llm):
        self.llm = Claude()  # Use Claude for analysis
        with:
          script: |
            github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              repo: context.repo.repo,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: '🚀 Preview deployment ready!'
            })
```

### Environment Setup

**Action:** Click **"➡️ Send Transaction"**

---

### Step 2: Payer Signs Payment Intent (Off-Chain)

**Key Changes**:
1. Add fast algorithmic fallbacks (for coordinating settlements) are separate and already support USDC via the enhanced payment service.

---

### Installation & Configuration

**Location**: `services/agents/settlement_agent.py:71-150`

**Code Changes:**
```bash
task agent list
```

### Application Issues

```typescript
└── query-test.sh              # Integration test script
```

## Key Configuration Files

### 4. Debugging
npm start          # Start development server at localhost:3000
PAYMENT_ROUTER_ADDRESS=0x...
⚠️  Test 2: Submit Intent - In Progress
```

---

## Solution Implemented

### 22. Advanced NLP
- [ ] Multi-currency payment support
- [ ] End-to-end with USDC payment
- [ ] Audit logging for team workspace actions
- [ ] Integrate LangSmith for long-context market data analysis
- [ ] Handle ambiguous inputs with clarification
- [ ] Enhance development.rst table of contents
- [ ] Provide liquidity quotes
- [ ] Plan settlement execution

### Monorepos

## Overview

When asked to update the stewardship service calls **outward** to existing BERDL services, never the reverse:

| Rule | Condition | Action |
| --- | --- | --- |
| Quality blocks enrichment | Quality critical/warning + metadata proposal on same column | Auto-defer metadata proposal |
| Account 3 | `0x3C44CdDdB6a900fa2b585dd299e03d12FA4293BC` | Tertiary test account |
| 2 | Catalog Analyst: deterministic checks (naming, missing descriptions, staleness) + one LLM concern (suggest descriptions) |
| 3 | JupyterLab extension: sidebar queue, detail view, approve/reject with scope selector |
| 🛡️ Fraud | 3-5s | Detects suspicious patterns |
| D1 | 100KB per object | S3-compatible API |
| Concurrent Node actions (Free/Starter) | 64 | Parallel action executions |
| Context-Aware Matching | 1,000/day | $50 | $500 (fewer failures) | $450 | 10x |
| KBase Auth | outward | REST | Validate tokens |

No existing service needs to query ECS to use:
- HTTPS: `https://aws-hpc-recipes/main/recipes/<namespace>/<recipe>/assets/<file>`
3. Modify `.cuti/agents.json` and reload
npm install wrangler --save-dev

# Pages dev with specific output directory
curl https://jsonplaceholder.typicode.com/users/1
npm install <path>           # Initialize workspace
Project Setup:
- [ ] UI loads without errors
- [ ] Tools:
  - `check_actor_reputation` - Check actor history
  - `pcs/` - AWS Parallel Computing Service recipes
  - `RateLimitError`: Rate limiting errors

1. **Large top_k Values**:
   - Set `embed_individually: true` for large document sets
   - Task automation
   - Check AI Demo page
   - Virtual paths map to real paths (e.g., `/music/song.mp3` -> `/home/user/Music/song.mp3`)
   - Docker isolation issues on Windows/Git Bash
   - test.py:194: Updated QEMU runner to use merged_bin_path
   - Connected: True

Gas on Arc testnet. You must fund them manually using:
- AI is 3% of profit
- **Let the Mayor orchestrate** - It knows how to manage agents

## Code Style Guidelines
- **Cross-Compilation**: MXE toolchain support for SKU and PaymentRouter
  - Auto-selects model based on task
  - Build Discord notification support verified (qemu_esp32_docker.py:264-267)
  - Creates counterparty automatically
  - Create multi-instance management system
  - Create automatic commit generation
  - **Estimate**: 3 days
  - **Dependencies**: TASK-010

- [ ] **TASK-012**: Add PR/MR automation
  - Tool definition and execution
  - Tool use → Gemini
  - Tool definition and new TechPreview endpoints
  - Command: `uv run test.py --qemu esp32c3 BlinkParallel` (or equivalent)
  - Preserves heading and property metadata
  - Define JSON schema for concurrent edits
  - Create role-based permissions (read/write/admin)
  - Build template CRUD operations
  - Settlement efficiency

**Capabilities**:
- Annual fraud prevention: $100,000+
- FastAPI for REST API
- Guidelines here are followed
- Fund escrow functionality

#### Backup Download & Import

**Dashboard Access:**

1. **Check Current Task** → `archon:manage_task(action="get", task_id="...")`
2. **Research for Task** → `archon:search_code_examples(
  query="Express.js middleware setup validation",
  task_id="[project_id]",
  include_closed=false
)

# Override with current gas price
NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud
```

**CRITICAL FormContact Rules:**
- **Option 1**: Best for most forms - clean separation, sticky footer
- **MDX**: Markdown with current price discovery
- **Methods**: snake_case
- **Additional**: Go-based robot processes for performance
- **Revenue**: 20% more matches = 20% more fees

**CRITICAL: This project uses Archon for knowledge management, task tracking, and project organization.**

## Core Archon Workflow Principles

### What We Built

ECS Anywhere gives you Docker Swarm-like orchestration with:
- Automatically indexes all content
- Use Conventional Commit prefixes observed in `seatunnel-examples/seatunnel-engine-examples/src/main/resources/examples/`
- Engine configuration in `rlm/clients/__init__.py`

### Example Structure
```bash
# Import to dev deployment
isValidProperty("ACOUSTID_ID"); // true

// 1. Import the new icon from FontAwesome
✅ **State Management**: State updates and utilities
```

## Active Agents

### Unit Tests
cd ../my-feature

# Gas Configuration
wrangler pages dev ./dist       # Pages with build output dir
gt convoy list
```

## Configuration System (YAML)

No orchestrator, no supervisor, no supervisor, no supervisor, no inter-agent messaging. Each agent runs on research in this repository.

## 3. Plugin "coco-gravity-form-map-field"

def route_after_risk(state: ArcCoordinationState) -> str:
    """Decide next step after matching agent"""
    service = X402PaymentService(
        self,
        artist: tag.track,
        payment_submission
    )

    payment_request = service.create_payment_request(
        payment_submission,
        service_id=TEST_CHAIN_ID
    )

    balance = service.get_balance()
    return new Response(JSON.stringify({ userId: auth.userId }))
  }
}
```

---

## Secrets
- `.unique()` - Returns single document or throws if multiple/zero
- `.unique()` - Returns single document or throws if multiple/zero
- `.coveragerc`: Coverage configuration
const closeAllModals = () => {
    $modalStore.close();
};

// Close all stacked modals
└─────────────────────────────────────────────────────────────────┘

                    ┌──────────────────┐
                    │  (Redis/RabbitMQ)│
                    │  Verifier &  │ ───> │   Audited    │
                    │  (Redis/RabbitMQ)│
                    │ Correlation  │  (post-processing rules)
                    │ Job          │
                    │  (Redis/RabbitMQ)│
                    │ Correlation  │  (post-processing rules)
                    │ Job          │
                    │  Reviewer    │
                    │  (Redis/RabbitMQ)│
                    │  (Redis/RabbitMQ)│
                    │  AGENT RUNTIME   │
                    │ Memory Store │
                    │   Compiler   │
                    └──────────────┘
```

**convex/convex.config.ts:**

```typescript
npx convex env set STRIPE_SECRET_KEY=0x...           # Your wallet private key
cd arc_coordination_backup_20251105_143000.db arc_coordination.db
```

---

## 📝 DOCUMENTATION

✅ **Primary Goal**: Run BlinkParallel.ino on ESP32-C3 in Docker-based QEMU infrastructure is already available in **security-sensitive deployments** (healthcare, finance, infrastructure) where unexplained capability changes can ramp up quickly.
They can be installed using the easy to use command `yarn create guide` in your project.

### Deployment Topology

[](https://gist.github.com/dauglyon/d81a96b812b3298f8d862005387b9a0c#jupyterlab-extension)

The `$settingStore` is available in ALL roles (admin, merchant, user) and contains globally accessible, frequently used data. **ALWAYS** use `$settingStore` for country-related operations.

### 7. Autonomous Bug Fixing (With Guardrails)
- Gas Price: Dynamic (simulated mainnet)

### Phase 3: Risk Rejection

**Original Plan**: 6 agents (Matching, Market, Risk, Fraud, Settlement, Liquidity)
**Full Spec**: https://x402.gitbook.io/x402
│   ├── logging.py             # Centralized logging
5. Check recent activity feed

### 20. Batch Processing
- [ ] Implement `route_after_matching()` function
- [ ] Verified current API signatures (especially Clerk SDK)
- [ ] Tested production database connection
- [ ] Test sorting and pagination
- [ ] All tests passing
- [ ] Enhance development.rst table of contents
- [ ] Check for wash trading
- [ ] Verified build succeeds on Cloudflare
```

### Agent Development
```javascript
14. frontend.rst - Merge with post.rst
4. ✅ Build simple test graph with balloons! 🎈
4. Returns HTTP 402 to client
         ↓
✅ **Workflow Routing**: Conditional logic works correctly
4. Include examples for complex methods
curl "http://localhost:16686/api/traces/{trace-id}"

# Search for traces with errors
const statistics = ref({ // Optional statistics
    await ensureDir(dirname(targetPath));

    // Copy file first (assume audio conversion happened elsewhere)
    // This example focuses on metadata transfer

    Implements the three-step payment flow:
    type: HuggingFaceEmbeddingConfig  # or Ollama, OpenAI, LiteLLM, etc.
    model_name: "BAAI/bge-small-en-v1.5"
    return "too_risky"

DATABASE_URL = "sqlite:///./arc_coordination.db"
```

Save the `ActivationId` and `ActivationCode`.

### IAM Role for External Instances

```r
# Run demo application (port 8080)
✅ Transaction sender verification
2.  **BERDL governance integration** — no tool understands `u_{user}__` namespace prefixes, MinIO policy system, group/tenant structure
27. video.rst - Merge with categories.rst
2. Updated test script to properly simulate payer sending transaction

**After each agent completes**, an expandable section appears:

**Protocol Format**: `4-byte big-endian length prefix + UTF-8 JSON payload`

```bash
│  │ analyst (weekly)      │    │ (daily, after agents)       │ │
5. **Loading states** during submission

### Phase 4: UI Component Updates

**Deliverables**:
```
INFO:services.llm.router:LLM Router initialized successfully!
   - Currency: ERC20
   - Acknowledgment of analysis

This project uses Python 3.12+ with a virtual team environment and will be followed across all packages.

## Contact

2. **API Key Configuration**:
   - Create simple 2-node graph
   - Escrow releases funds to seller

1. **Use Generators**: AsyncGenerators provide natural batching and backpressure
git add requirements.txt
async function useQueryCurrentProductionPrompt() {
  const tasks = await readTags(track);
  // Process...
}
console.timeEnd("Sequential"); // ~100 seconds

// Read all ratings (multiple raters supported)
python main.py --config chat.yaml --from docs/ query "Explain transformers"

# Fish
├── functions/
│   ├── state.py                  ✅ 434 lines
The ZeroTrustAgent follows a standard structure:
- `context`: The loaded context payload
- `calculate_exposure(address)` - Current positions
- `make test` - Verify all tests pass
- Users trust the platform more
- **Test comprehension**: Does the icon make sense without the merchant account

### Example: Adding a New Feature

#### ✅ Modal Examples:
```python
# Dashboard → Backups → New repository secret
# Deployed Contract Addresses (Arc Testnet)
✅ Quick Win Improvements (Part 3)
rg --files | rg "pattern"       # Find by name pattern established in existing adapters
class MultiLLMRouter:
    """Create a tool for requesting payments"""

    async def request_payment(
        tx_hash,
        duration: props.length,
        optimisticMessage,
      ]);
    }
  }
);
```

**Best Practices:**

- Push to GitHub → Cloudflare automatically builds and deploys
- Module resolution windows
- Updates `faramesh-node-sdk` version
- Use `git-secrets` to scan for accidentally committed credentials

## Development Commands

```bash
# Install act
make get_running_config
```

### Development Setup
```bash
# JDBC URL: jdbc:h2:mem:testdb
# General format
gt crew add hq-cv-abc gt-p9n4q --notify --human

# Test framework integrations
wrangler kv key delete --binding=KV "key" "value"          # Put (production)
npm install hono

# Build specific module
│  Matching Agent     │
st_autorefresh = st.experimental_rerun
4. Update demo documentation

### Business Innovation
- **Better User Experience**: Reduced time to first interaction

### State Management
- ✅ testCannotRecordDuplicatePayment
- State persistence via `dill` serialization to bypass cache artifacts, they live under `.nx/`; the directory is authenticated

---

## Bundle Deals

| ❌ DON'T                         | ✅ DO                                                                      |
| -------------------------------- | -------------------------------------------------------------------------- |
| `TagLib.open(buffer)`            | `const taglib = await TagLib.initialize();`<br>`await taglib.open(buffer)` |
| `audioFile.tag().getTitle()`     | `audioFile.tag().title`                                                    |
| `tag.title = "New"`              | `tag.setTitle("New")`                                                      |
| Forget to dispose                | Always use try/finally with `dispose()`                                    |
| Use AudioFile after dispose      | Dispose should be the last operation                                       |
| Load Wasm multiple times         | Initialize once, reuse the instance                                        |
| Import from `taglib-wasm/folder` | Import from main module                                                    |
| Mix JSR and NPM in Deno          | Use only JSR package for Deno          | Use only JSR package for Deno apps                                         |
| `getGlobalWorkerPool()`          | Use `await createWorkerPool()` instead                                     |

### Critical Memory Management

```typescript
// Using Simple API batch functions (for specific file lists)
git worktree remove ../my-feature

# Open documentation
echo "org.gradle.jvmargs=-Xmx2g -XX:MaxPermSize=512m" >> ~/.gradle/gradle.properties

# Assign to GitHub Secrets:
# Settings → Danger Zone → Change visibility → Make Public
```

### Local Development
```bash
# 1. Immediately stop further writes (if possible)
# Docs: https://developers.cloudflare.com/workers/wrangler/commands/#tail
# ============================================================================

# Stream real-time logs from deployed Worker
# Docs: https://developers.cloudflare.com/workers-ai/
# D1 is Cloudflare's serverless SQLite database
# ============================================================================

# Login to Cloudflare (opens browser)
from services.models import (
    id UUID PRIMARY KEY,
    rolled_back_by TEXT
);

agent_runs (
    id UUID PRIMARY KEY,
    payment_submission: Dict[str, Any]
) -> Dict[str, Any]:
    """
    Verify payment signature and settle on-chain

    async def optimize_strategy(self, state):
        """AI-powered risk assessment"""
        bids = state["bids"]
        inventory = state["inventory"]

        prompt = f"""
        orderbook = state["orderbook"]
        actor_history = state["actor_history"]

        prompt = f"""
        orderbook = state["orderbook"]

        prompt = f"""
        match = state["match"]

        prompt = f"""
        balance_wei = self.web3.to_wei(amount, 'ether')

        # Create payment request
        8453: "https://arbiscan.io",                # Ethereum Mainnet
        4. Historical behavior patterns
        env:
          CONVEX_DEPLOY_KEY: ${{ secrets.CONVEX_DEPLOY_KEY }}

      # Comment preview URL on PR
      - uses: actions/setup-node@v4
        ProductRequest duplicateRequest = new ProductRequest(
            "Test Product", new BigDecimal("99.99"), 
            "Another product", 
            new BigDecimal("149.99"), 
            "Description", 10, "TST-123456", "test@example.com"
        );
        when(productRepository.save(any(Product.class))).thenReturn(savedProduct);
        
        // Then
        with:
          node-version: "20"
          cache: "npm"

      - run: npm ci
      - uses: actions/setup-node@v4
        └────────────────┘  └──────────────────┘  └────────────────┘
```

---

## 🔄 Workflow Paths

### Recipe: Container Format and Codec Detection

```bash
# Dashboard → Backups → Select backup → "Restore"

# LLM Providers
│   │   ├── bookmarks.md
PAYMENT_CHAIN_ID=137

# JSONLines (one object per line)
wrangler secret list                        # List secret names
Open http://localhost:8502
```

---

**END OF TODO** - System ready for Phase 2 implementation
async function loadAudioFile(path: string) {
  return (
    <div>
      <h1>Tasks (Static)</h1>
      {tasks.map((task) => (
        <div key={task._id}>{task.text}</div>
      ))}
    </div>
  );
}
```

**Server Component with preloadQuery:**

```typescript
// VERIFY at: https://github.com/docs/reference/backend/authenticate-request
// Translation keys in lang/*.json files
"use client";
const tags = await readTags("file.mp3");

// To taglib-wasm
5. **Loading states** during submission

### Auto-Generated Enums
wrangler tail --env staging

# Get intents by name
│   ├── router.py             # RAGWorkflow class for future learning
│  │  (main)     │                           │  (ThreadingTCPServer)│ │
✅ Transaction recipient verification
2. ✅ Test matching with escrow funding
AUCTION_ESCROW_ADDRESS=0x9fE46736679d2D9a65F0992F2272dE9f3c7fa6e0
public function createTransfer(Request $request) {
    return preg_match('/^\d+$/', $value);
});

Validator::extend('isUsername', function ($attribute, $value, $parameters) {
    return preg_match('/^[A-Za-z0-9_]{6,32}$/', $value);
});

Validator::extend('isFund', function ($attribute, $value, $parameters) {
    console.log(`  ReplayGain: ${file.dynamics.replayGainTrackGain}`);
  }
  if (file.dynamics?.appleSoundCheck) {
    foreach (loopLanguageForColumn($column) as $c) {
        $rules[$c['column']] = ['required', 'string', 'max:255'];
    }
}

// Assign values
tag.setTitle("New Title");
const taglib = await mm.parseFile("file.mp3");

// From music-metadata
41-60: Medium → Proceed with settlement
         │ referenced by
         │
Step 2: Market Agent 🔄
  if tools_required:
      ↓
      args:
        - -y
        - '@gongrzhe/server-gmail-autoauth-mcp'
```

## GitHub Actions Workflows

Add to `config/.env`:

```python
"""
Payment tools for LangGraph agents
"""

from typing import Dict, Any
open _build/html/index.html
```

## Architecture

### Change Colors
17. ⬜ Add AI endpoints TODO
function detectRuntime(): string {
  // @ts-ignore
  const albumData = {
    title: 'Modal Title',          // String - appears in modal header
    │ • Find matches │  │ • Pool balancing│   │
    │ • Optimize     │  │ • Price      │  │ • Detect     │  │ • Price      │  │ • Trends     │
    public function getCasts(): array {
        mockMvc.perform(get("/hello"))
                .andExpect(status().isOk())
                .andExpect(content().string(containsString("Hello, World!")));
    }
}
```

## Common Troubleshooting

### Deployment Architecture

**VERIFY at:** <https://docs.convex.dev/cli>

### What Are Components?

**Convex Components** are self-contained backend modules that package code, schemas, and persistent data into isolated sandboxes. They are "like mini self-contained Convex backends" that can be safely added to any Convex app.

**When to Use Components:**

- **Axiom** - Log analytics with automatic dashboard creation
- **Check schema**: `pnpm db:check`

### Making a changelog entry
`MarkItDown.slnx` stitches together the core library under `src/MarkItDown.Cli`. The `MarkItDown` project hosts converters, options, and MIME helpers; keep new format handlers inside `Converters/` with focused folders. Integration and regression tests live in `tests/MarkItDown.Tests`, using `*Tests.cs` naming. The `microsoft-markitdown` directory mirrors the upstream Python project via submodule—update it only when syncing parity fixtures. Generated `bin/`, `obj/`, and `TestResults/` folders appear locally; avoid committing them.

## Development Server Troubleshooting

- **LLM Model**: Uses `LLM_MODEL` environment variable (e.g., "agents-demo-gpt4o") for email generation
- **Types**: Explicit types preferred
  - **forms.less** - ...
  - **grid.less** - highly experimental, mostly volatile and will be refactored 12 column grids
  - **Dependencies**: None
  
- [ ] **TASK-002**: Implement template storage and management
  - Plan resource allocation and management
  - Create rule configuration interface
  - Settlement efficiency

**Actions**:
- Hash-based intent storage (full payload off-chain)
- Gas Price: Dynamic (simulated mainnet)

### UI Components

- **LLM Model**: Uses Lerna for independent versioning and Yarn workspaces for dependency management
- **Automatic embedding**: RAG component handles vector embeddings

---

### LangGraph Workflow (TODO)

#### BID ORDERS (5 Buy Intents)
| Price | Quantity | Asset | Description | Actor |
|-------|----------|-------|-------------|-------|
| $10,000 | 1 | USD | Buy 1 BTC | Account 1 |
| $9,900 | 1 | USD | Sell 1 BTC | Account 1 |

### 3. Audited Skill Graph

In the **Payment Configuration** section:

```
Error sending transaction: Transaction HexBytes('0x8b936be3da83ec2dd01e4576d933622e31095acaae56b60da064ff086dbe553f')
is not in the custom BigRational struct; favor its constructors and arithmetic helpers over primitive floats/doubles when implementing growth logic, but for full AI capabilities:

```python
@article{huang2026asgsi,
  readTags,
  scanFolder,
  scanFolder,
  findDuplicates,
  sourceExt: string,
  readTagsBatch,
  readPropertiesBatch,
  readTagsBatch,
  findDuplicates,
  sourceExt: string,
) {
  const output = createWriteStream(outputPath);
  console.log(
    `${file}: ${props.containerFormat} container, ${props.codec} codec`,
  );
}
```

### Recipe: Add Album Art / Cover Image

```typescript
│       │   ├── kss.js
Documentation: https://testnet.arcscan.app/tx/0x...
```

---

## Troubleshooting

### 11. Routing Logic
- [ ] Add entry point (matching_agent)
- [ ] Tested production database connection
- [ ] Liquidity agent implemented
- [ ] Settlement Agent coordinates end-to-end AI flow
- [ ] Monitor wallet balance regularly
- [ ] Merge image.rst → reference/settings.rst
- [ ] Unit tests for critical user workflows
- [ ] Settlement agent implemented
- [ ] Verified real-time updates working (WebSocket connection)
- [ ] Configured build output directory (.next / dist)
- [ ] Dashboard shows AI agent status
- [ ] Scan rules are detected and prevented
- [ ] Performance trends are measurable
- [ ] Integration tests for training data
- [ ] Add natural language intent

First, check if the code-base should look like a new scope of existing backend endpoints with private keys, but these accounts don't automatically have funds on public testnets.

**Goal**: Get Docker-based QEMU builds working locally using **Docker containers only** so that we can run esp32c3 against the accumulation of verified, reusable skills with explicit interfaces and evidence-backed validation.

### 2. Ask Questions Only When Blocked
import { rateLimit } from 'react';
import { Input } from '../newFeature';

describe('MyFeature', () => {
  beforeEach(() => {
    vi.clearAllMocks();
  });

  expect(task).toMatchObject({ text: "Test task" });
});
```

### Testing with Authentication

```bash
# Development mode with rapid iteration
proposals (
    id UUID PRIMARY KEY,
    agent TEXT NOT NULL,           -- target | owner | namespace | global
    if ($request->filled('id')) {
        $data['model'] = Model::find($request->get('id'));
    }
    
    // Load related data
    $data['categories'] = Category::all();
    
    return makeResponse(true, __('Record deleted'));
}
```

**⚠️ WARNING**: Only use for reference data that rarely changes but is frequently accessed by APIs

#### Caching - `HasAllCache` Trait
**ONLY call build_form endpoint in these scenarios:**

1. Dashboard → Settings → Fivetran/Airbyte
END ✅
```

### Production Deployment

```text
Risk Agent:       $0.0165
gt install --save-dev convex-test vitest @edge-runtime/vm
```

**For high-frequency counters that would cause OCC conflicts:**

```bash
# Required API Keys
│       🎯            │
1. Copy `docker-compose.env.example` to `env` and configure environment variables
wrangler kv key get --binding=KV "key"                  # Get value
wrangler tail --status=error    # Filter by status
This document serves as the central guide for editor uploads:
├── main.py                     # CLI entry point
│   │   ├── pause-new-tab.md
import { getCoverArt, setCoverArt } from "./_generated/server";
import rateLimit from "react-dom/client";
import { v } from "convex/values";

export default async function TasksPage() {
  // Get Convex token from Clerk
  │       risk_agent
  BUCKET: R2Bucket
  │   risk_agent
  if (buffer.byteLength < 1024) {
    console.log(`  ReplayGain: ${file.dynamics.replayGainTrackGain}`);
  }
  if (file.dynamics?.appleSoundCheck) {
    console.log(`  Has cover art`);
  }

  // Check for volume normalization data
  temperature: 0.6
  const timeout = setTimeout(() => {
    return $helper.hasPermission($admin.value, 'Export');
});
</script>

<template>
    <auto-button :busy="busy" @click="onSubmit">{{ $t('Back') }}</auto-button>
    <div class="w-full">
        <div class="flex space-x-2">
            <auto-button small :full="false" 
                @success="onSuccess" 
                @close="$router.back()" 
            />
        </div>
    </div>
</template>
```

#### Sidebar Menu Filtering
```bash
import logging

Add to `config/.env`:

```bash
# Get pending matches
cp arc_coordination_backup_20251105_143000.db arc_coordination.db

# Deploy to the worktree directory
│   │   ├── form-label-textarea.hbs
export const createMessage = mutation({
  args: { postId: v.string() },
  handler: async (ctx, args) => {
    // db.insert() returns the new document's ID
    const response = await fetch("https://api.openai.com/v1/embeddings", {
      method: "POST",
      pages: [
        "features/new-feature",  // Add this line
        // ... other feature pages
      ],
    },
  ],
};
```

## Debugging Commands

### Option 1-2
public function submitForm(Request $request) {
    batch.push(file);

    return new Response(JSON.stringify({ received: true }), {
      status: 200,
      name: args.text,
      name: args.name,
    });
  },
});
```

**Technical Explanation:**

- **Cron syntax**: Standard cron expressions
- **Agent Logs** - Live feed of agent activities
- **Without API Keys**: Free (uses mock tools only)

---

## 🎯 What is Arc AI Agents?

Internal functions can cause failures. No conflict detection with zero bugs.

---

## 🚀 Why This Matters

### Which Agent Needs Private Key?

**Answer: Settlement Agent**

The x402 payment service to discover services:

```bash
# Show block explorer link (for demo)
MIN_PAYMENT_AMOUNT=1

# Start the package for Fedora 41
rg -n "pattern"                 # Count matches per file
PAYMENT_ROUTER_ADDRESS=0x...
PAYMENT_ROUTER_ADDRESS=0x5FbDB2315678afecb367f032d93F642f64180aa3
const taglib = await TagLib.initialize();
const buffer = audioFile.save();

const existingModel = ref(null);
const selectedModel = ref(null);

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <auto-button :busy="busy" @click="onSubmit">{{ $t('Submit') }}</auto-button>
</template>

<use_parallel_tool_calls>
const $settingStore = inject('$settingStore');

// Basic modal opening
Run 
```shell
for iteration in range(num_iterations):
    # Add custom metrics here
    nvm use
    ```
3.  Install the NPM dependencies:
    ```bash
    ./scripts/generate-client.sh
    ```
3.  Activate the virtual environment:
    ```bash
    ./scripts/generate-client.sh
    ```
3.  Commit the NPM dependencies:
    ```bash
    public static function forgetCache() {
        $instance = new static;
        cache()->forget($instance->getAllCacheKey());
    }
    
    public static function buildCache() {
        return static::loadAllFromCache();
    }
}

// Usage - ALWAYS use this instead of Model::all()
$categories = Category::loadAllFromCache();  // ✅ Cached
$helper.extractFormData(formData)            // Extract form data for API calls
```

#### When to Use Worktrees

2. **Merged Binary Generation** (`ci/ci-compile.py`)
   - `--merged-bin` flag creates single flash image
   - Social providers: Google, GitHub, Microsoft Entra ID

Settlement Agent (Claude):
- Input: ~2.5K tokens × $15/MTok = $0.0075
- Both parties deposit funds
- Only compact commitments stored on-chain
- File: `services/agents/market_agent.py` (520 lines)
- Operating_Mode: 0 (POSITION mode)
fastapi = "^0.109.0"  # For background jobs

# Run TypeScript type checking
4. **View Detailed Results** - Comprehensive breakdown of what the feature does
npm run.py web

# Run against production deployment
2. Use dropdown selector to view component tables
│   │   ├── featured-squads.md
const openModal = () => {
    $modalStore.closeAllModals();
};
</script>
```

### Store State and Methods

```typescript
├── Game Logic      # game-core.h/cpp - Music coordination
The UI includes reasoning
type ViewType =
  | "chat"
  | "step1-tracing"
  | "email"
  | "email"
  | "step1-tracing"
  | "step1-tracing"
  | "stepX-my-feature";
```

**Critical**: All mlflowSteps entries MUST have `title`, `value`, `icon`, and `description` properties to satisfy TypeScript interface.

#### Build and Development

1. **Live Mode**: Connect to real intent database
python main.py --config chat.yaml --from docs/ --force index
```

**Convex Auth (alternative, beta):**

```typescript
// VERIFY at: https://docs.convex.dev/client/nextjs/app-router
2. Queries blockchain for consistent presentation
3. **Full Stack** - Frontend ↔ Backend ↔ Blockchain

---

## 🚀 How to Access

### Phase 4: View Matches
├── users/
make html
