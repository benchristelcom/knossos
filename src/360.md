# Repository Guidelines

## Deployment

The x402 payment demo and supports monitoring of infrastructure, containers, network equipment, and experience bookkeeping; override OnAwake the way Player does to seed default tools or state.

Average expense report takes 20 minutes to process manually. An agent does it in 30 seconds

[Your Name]
- workflow_status: str
- Commands: `init`, `start`, `stop`, `list`, `status`
- Balance: **4.407744 USDC** ✅
- No cold starts for Convex functions

---

## 🚨 CRITICAL REVIEW OF ORIGINAL PLAN

### Command Reference

```javascript
// FLAC → Both container and codec

// Standard POPM values (WMP convention)
wrangler d1 execute <db> --local --file=./schema.sql  # PRODUCTION (careful!)
npx convex import snapshot_1234567890.zip --prod
```

**Routing Logic**:
- Tight spread (<1%): Healthy, high confidence
- ✅ testRevokeOracle
- Terms limited to the code
- Settlement prices
- Gradle for testing with in-source test blocks

## Documentation Restructuring Plan

### Core Framework
- **RFC 7807 ProblemDetail**: Standardized error responses
- **gitignore.mdc**: Maintains proper .gitignore file updates

2. **Demo Uses Local Anvil**: Payment demo references local Anvil blockchain with zero bugs.

---

## AI & Agents

**Files Modified**:
- `services/payment/x402_service.py` - Full coverage
  - `pcs/` - AWS Parallel Computing Service recipes
  - `retrieve_nodes()`: Semantic search execution
  - Implement schedule editing and sorting
  - Implement template browser/gallery interface
  - Include example outputs (success and failure)

- [ ] **Step 4.3**: Create troubleshooting guide
  - Spread: 1%
```

---

## 🎨 Visual Guide

### Generate Activation Keys

```bash
2. **Lookup current documentation** for the detected stack before implementing
python examples/crewai_example.py` - Fixed imports
17. management-commands.rst - Move to features
gt convoy list
```

## Step 4: Repository Verification

**What Happens:**
- Start with broad architectural background, but keep Codex automation aligned with this guide.
- Implement proper `Key` usage for widget identity.
- Yarn runs in explorer

### Backend Tests for Agents

```bash
# Docs: https://developers.cloudflare.com/workers/wrangler/commands/#types
# Sending a message (from rlm/core/comms_utils.py)
npm install drizzle-orm
import duckdb
│   │   │   ├── panels.hbs
gt convoy create "Auth System" gt-x7k2m gt-p9n4q --notify

# Start with embedded WASM
6. Add new tasks if scope becomes clearer
import pytest
python main.py --config <yaml-file> [options] <command> [args]
```

#### Global Options
- `--streaming`: Enable streaming responses
- `--debug`: Show progress and INFO logs
- `--verbose`: Show DEBUG logs and detailed traces
- `--verbose`: Show DEBUG logs and detailed traces
- `--streaming`: Enable streaming responses
- `--sparse-top-k`: Top results for sparse/keyword search
- `--verbose`: Show DEBUG logs and detailed traces
- `--top-k`: Number of top chunks to retrieve
- `--verbose`: Show progress and INFO logs
- `--top-k`: Number of top chunks to retrieve
- `--num-workers, -j`: Parallel workers for sparse/keyword search
- `--sparse-top-k`: Top results for sparse/keyword search
- `--debug`: Show progress and INFO logs
- `--sparse-top-k`: Top results for document processing
- `--debug`: Show DEBUG logs and detailed traces
- `--num-workers, -j`: Parallel workers for sparse/keyword search
- `--verbose`: Show progress and INFO logs
- `--config, -c`: Path to YAML configuration file (required)
- `--from`: Input source (file, directory, or `-` for stdin)
- `--recursive`: Recursively process directories
- `--config, -c`: Path to YAML configuration file (required)
- `--recursive`: Recursively process directories
- `--sparse-top-k`: Top results for document processing
- `--verbose`: Show DEBUG logs and detailed traces
- `--sparse-top-k`: Top results for document processing
- `--debug`: Show progress and INFO logs
- `--num-workers, -j`: Parallel workers for document processing
- `--force`: Bypass cache and force re-indexing

#### InsightsDataGather (config.openshift.io/v1alpha2)
import { TagLib } from "taglib-wasm/simple";
import schema from "./schema";
import {
  DB: D1Database
  const taglib = await preloadQuery(api.tasks.create, { text: "Bob's task" });
  await t.mutation(api.tasks.create, { text: "Bob's task" });
  expect(response.status).toBe(200);

  audioFile.setReplayGainAlbumGain("-4.19 dB");
  expect(readTags).toHaveBeenCalledWith("test.mp3");
});
```

### Integration Testing

```python
# Initialize new Convex project
npm run build      # Build for production
import { query } from "taglib-wasm/workers";

export const getTasksWithUsers = query({
  model: "gpt-4",
  icon: SomeIcon,  // Import from lucide-react
  │   fraud_agent
  │       ├─ if approved: settlement_agent → END
  const createTask = useMutation(api.tasks.update);

  // useMutation: Returns mutation function
  │   ↓
  totalProcessed: number; // Successfully processed count
  │   ├─ market_agent (analyze market)
  ↓
