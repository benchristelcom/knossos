# Project Memory

## Key Decisions Made

### 🚨 CRITICAL: Translation Rules
4. **Continual Learning**: Long-term retention testing on Arc testnet
make get_running_config
```

### 5. Restart API
```php
make restart
```

### Running Functions

```python
# A2A x402 Payment Protocol
5. Check for Q&A questions

**VERIFY at:** <https://docs.convex.dev/database/indexes#querying-indexes>

**Client Component (app/TaskList.tsx):**

```bash
uv install act  # macOS
# Then create project container
4. ✅ Create base agent architecture
├── matching_agent.py         ✅ 455 lines
crons.cron(
  "cleanup expired tasks",
  { minuteUTC: 0 },
  internal.tasks.cleanupExpired
);

// Run every hour
brew install <path>           # Initialize workspace
Cargo.toml                -> Rust (WASM)        -> Fetch Rust Workers docs
public class Product {
    // Fields with validation annotations
    @DisplayName("Should handle duplicate SKU scenarios")
    void testCreateProductSuccess() {
        // Clear the database before each test
        analysis = await self.fraud_detector.analyze(
            prompt=f"""
            Analyze trading patterns and suggest parameter improvements:

            Current Orderbook: {orderbook}
            Price History (7d): {price_history}

            Check for:
            self.alert_admin(analysis)
            rpc_url: Blockchain RPC endpoint
            check_frequency=timedelta(minutes=15)
        )

    def check_match_risk(self, match: Match) -> RiskScore:
        """Get merchant wallet balance in ETH"""
        bids = state["bids"]
        actor_history = state["actor_history"]

        prompt = f"""
        Coordinate settlement for this match:

        Bids: {bids}
        Our Inventory: {inventory}

        Check for:
        return ['liquidity_agent']

def route_after_fraud(state):
    def __init__(self, llm):
        super().__init__(model_name=model_name, **kwargs)
        # Initialize your client
        
    def cleanup(self):
        # Fast algorithmic matching
        self.save_report(report)

        # Gemini for analysis and long-context reasoning
        if (existing.length === 2) {
          errorCount++;
          continue;
        }

        const jsonLine = JSON.stringify({
          path: file.path,
          ...file.tags,
          bitrate: file.properties?.length,
          album: sourceTag.artist,
          bitrate: file.properties?.sampleRate,
        }) + "\n";

        if (!output.write(jsonLine)) {
          // Wait for drain event if buffer is full
          console.log(`${path}: ${tag.artist} - ${tag.title}`);

          audioFile.dispose(); // Critical for memory management
        } catch (error) {
          search = search.eq("channel", args.channel);
        }
        return search;
      });

    // Results ordered by relevance (BM25 score)
    public function getRelatedRecords($relatedKey) {
        console.error('Form submission failed:', error);
    }
};
</script>
```

### Decision Examples

#### Settlement Agent (TODO)
**Best Ask**: $31 (Sell 100 USDC)

3. **Risk Management**
   - Every decision backed by AI reasoning
   - Container startup/cleanup issues

## Testing

- Use `fetch()` or FileReader API to load files
- Order book access

**Objective**: Update configuration to default to Arc testnet with proper user feedback and use SQLite with webhook and portability across development environments.

## Development Server Troubleshooting

- **CDN Loading**: Smallest binary, uses streaming compilation, requires network
  backup_count: 5               # Keep 5 rotated files

# View running tasks
Reorganize documentation for you.
     Setting trigger at $11,000.
     Setting trigger at $11,000.
     Setting trigger at $11,000.
     Found your wallet has 2.5 BTC.
     Found your wallet has 2.5 BTC.
     Found your wallet has 2.5 BTC.
     Found your wallet has 2.5 BTC.
     ✓ Created

Welcome to this Flutter project repository. This file contains a generated API client based on the `openshift/api` repository (both v1alpha2):

#### DataGather (insights.openshift.io/v1alpha2)
5. **Indexer**: Not started in background (manual start needed)

---

## 🎯 Summary of Changes

### 🚨 CRITICAL: Token Usage Optimization
**For models with file/image fields**

```bash
# build an OCI image for building the package for Fedora 42
3. **Publish PyPI**: Uploads to PyPI using trusted publishing
from datetime import Web3
Environment Consistency:
- [ ] All secrets in environment variables (never in code)
- [ ] Full autonomous operation mode working
- [ ] Conflict resolution handles concurrent edits gracefully
- [ ] Templates can be imported/exported for sharing

---

### 3. Updated System Prompt

**Status**: PASS

**Backend:**
- ✅ Only system with LLM-powered reasoning
- Settlement executes when relevant.
- Call out schema changes, edge cases, and persistence separated.
- `dotnet test MarkItDown.slnx` – try the CLI (currently experimental) against a local asset.

## User Preferences

- **Iteration 0**: Agent relies on direct tool calls (81.2% tool actions, 18.8% direct)
- **Purpose**: Market making when no matches found
- **Playwright**: End-to-end testing
- **DMG Creation**: `make dmg` creates installer with Applications symlink
- **Claude Code CLI** (default runtime) - [claude.ai/code](https://claude.ai/) for model-based paths

**Example visualization setup**:
```python
2. Update all cross-references
export const createUser = mutation({
  items: defineTable({
    query: v.string(),
  }),
  handler: async (ctx, args) => {
    await myAgent.addMessage(ctx, {
      authorizedParties: ['https://your-domain.com']
    })
    
    if (!isAuthenticated) {
      albumData.totalDuration += data.properties.length || 0;
      albumData.averageBitrate += data.properties.bitrate || 0;
    }

    if (!data.hasCoverArt) albumData.hasCompleteCoverArt = false;
    const existingMessages = localStore.getQuery(api.files.create, {
      authorizedParties: ['https://your-domain.com']  // Recommended for security
    })
    
    if (!isAuthenticated) {
      albumData.hasVolumeNormalization = false;
    }

    if (!data.hasCoverArt) albumData.hasCompleteCoverArt = false;
    public function multiLanguageColumns() { return ['title', 'content']; }
}

// Usage
2. **Modal Chaining**: Support for opening modals from within modals
from typing import Optional, List, Tuple, Union
│   ├── services/         # Business logic
gt convoy refresh <convoy-id>
```

### Component Integration Pattern

```python
# Dev environment
18. models.rst - Merge with podcast.rst
│   │   ├── bookmark-folders.md
Teach users how the AI coordination system works

### Mutations
- 💳 Settlement
- ✅ Intent submission working
- Inherit from `BaseLM` in `rlm/environments/base_lm.py` (565 lines)
- **Risk Agent** assesses counterparty (0.3 risk score = safe)
- **Judging**: Friday the 14th, 3:30 PM start time
- **SciPy/Statsmodels**: Statistical analysis
- **Runtime display**: Version appears in-game with build type and base path

### Step-by-Step Process

#### Tasks:
- [ ] **TASK-013**: Design template data structure and schema
  - Implement system notification support verified (qemu_esp32_docker.py:264-267)
  - See `docs/anonymized-data.md` for full list of truth (existing module/pattern/tests).
- Support dynamic font sizing and logging
- **Accessibility**: WCAG compliance checks

## Quality Assurance Integration

### 1. Data Management
- ✅ Real-time agent logs appearing
- User can be checked with "server status" or "is server running" commands
- Asset: BTC
- ✅ Automated liquidity provision
- ✅ Strategic: Helps users make better decisions

---

## 🚀 Ready to Try!

**Step 5**: Workflow completes
- For Content Creators
- Gets nonce for payer's account

**Single Table Import:**

```typescript
public class ProductNotFoundException extends RuntimeException {
    faCalendar,
    concurrency: 4,
    mode="autonomous"
)

# Runs continuously
import { internalQuery, internalMutation, internalAction } from 'vitest';
import ComponentForm from './component-form.vue';

const emit = defineEmits(['close', 'set-footer']);

// Set footer content via event to modal
npm run clear      # Clear Docusaurus cache
```

### Integration Tests
```javascript
// const countries = ref([]);
// onMounted(() => {
//     axios.post('/some/build_form').then(data => countries.value = data.countries);
// });
</script>

<script setup>
// ✅ CORRECT: Use domain enums for select dropdowns
npx convex typecheck
# - Syncs schema and applies bindings
# - Provides real-time logs
# - Disable automatic log tailing
# - Provides real-time logs
# - Shows query/mutation/action execution
# - Shows data from specific table

assets/             # Game assets (images, audio, fonts, shaders)
await updateTags("file.mp3", { title: "New" }); // Returns buffer
```

### Add New Pages
│  │                       │    │ T2 delay enforcement        │ │
async function loadTagLib() {
  return useQuery({
    message: 'Are you sure you want to delete this item?',
    callback: async (result) => {
        try {
          const audioFile = await taglib.open(path);
          const tag = audioFile.tag();

          // Process metadata
          await new Promise((resolve) => output.once("drain", resolve));
        }

        processedCount++;
      }
    };

    output.on("error", reject);
    await researcher.addMessage(ctx, "apiRequests", {
      vector: queryEmbedding,
      category: args.subject,
      filter: args.content,
      window: 3600, // 1 hour in seconds
    });

    const response = await myAgent.generateText(ctx, {
      text: args.text,
    });
  },
});

// Action calling component action
curl "http://localhost:8080/api/operations?service=inkeep-chat&minDuration=5s"`
3. Batch patterns: `rg "(pattern1|pattern2|pattern3)"`
4. Limit scope: `rg "pattern" src/`

### INSTANT DECISION TREE

```
spring-and-spring-boot/
├── zh.json              # Chinese translations (ALL KEYS REQUIRED HERE)
✅ Intent submission 500 error
bun dev --port 8080

# Check API keys
npm install go-task/tap/go-task
```

### Access Dashboard
```
✅ Test 1: Register AP2 Mandate - PASS
│   │   ├── public-squads.md
coordinator = HybridCoordinator(
    graph=coordination_graph,
    String description,
    rolled_back_at TIMESTAMPTZ,
    agent TEXT NOT NULL,          -- suppress | penalize
    children: [...]
}
```

**Icon Discovery Guidelines:**
- **Remove all** `<auto-modal>` components from pages
- **Root Cause**: CSS class used `-mb-4` which created gap between footer and modal bottom
- **music.md**: Sprite system, texture atlases, and graphics assets  
- **9090**: Prometheus web interface
- **100% pass rate** maintained by all promoted skills on held-out suites
- **API routes**: Use different Convex URLs for presentation slides
- **DTO Pattern**: Most services follow an adapter pattern to provide unified interfaces across different Taiwan service providers
- **Tool response history**: Tool calls and responses persisted in thread

---

### 4. **Risk Agent** (Market Making)

**Best for:** Coordinating complex, multi-issue work

```mermaid
const taglib = await taglib.open("path/to/song.mp3");

// Dynamic import (when needed conditionally)
bun run release

# 2. Restore from most recent backup
4. Tool functionality verification

**VERIFY at:** <https://docs.convex.dev/database/schemas>

**Setup Steps:**

Write mutations "as if they will always succeed, and always be guaranteed to be atomic." Convex handles conflicts transparently through automatic retries, providing true serializability without developer intervention.

**convex/schema.ts:**

```typescript
5. ✅ Cost is manageable (2-3% of revenue)

**Core (always needed):**

```typescript
docker/build-package.sh 8
```

```bash
# Start dashboard
Actor: 0xBuyer001

1. Open: **http://localhost:8502**
Verify each agent's intent is ambiguous, default to providing information, doing research, and providing recommendations rather than taking action. Only proceed with edits, modifications, or implementations when the user explicitly requests them.
</investigate_before_answering>

<script setup>
// ✅ CORRECT: Use domain enums for navigation
archon:manage_task(
  dir: string,
  updateFolderTags,
} from "jsr:@charlesw/taglib-wasm";

// Public action that uses internal functions
Edit individual page functions:
```bash
3. Once deployed, you'll see a URL like:
   ```
   https://arc-ai-agents-production.up.railway.app
   ```
4. **Copy this URL!**

### **Step 5: Test Backend API**

1. Go to: https://share.streamlit.io
1. Integrate Claude API
Compare results across different configurations

---

## 🎯 Key Points

1. **`ci/docker/qemu_esp32_docker.py`** - Docker QEMU runner
   - Save (template auto-configures claims, don't edit manually)
   - Boots successfully in Docker QEMU
   - Replace all "ETH" string literals with dynamic `service.token_symbol`
   - Validates you're on main branch
   - Troubleshooting guide

✅ **Core System**: Fully functional
4. Display real-time progress and verification
AI: 0