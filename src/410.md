# AGENTS.md

> This is an example CLAUDE.md file showing how to configure Claude Code for your project.

## DocSync markers

2. Run through payment flow

ASG-SI addresses these challenges through three core innovations:

Update in your Streamlit app:
```python
@Getter
anvil --port 8545
```

### "Transaction Failed" Error

**Query Strategy:**
- Merchant recovers signer address
- Documentation

**Last Updated**: 2025-01-05
**File Modified**: `services/api.py:134`
**For repeating validation patterns, create custom rules:**

```typescript
│     ↓                                                    │
import React from "react";
import { TaskList } from "stream/promises";

export const chatWithLimit = action({
  name: z.string().min(1, 'Name is required'),
  config: z.object({
    wasmUrl: "https://cdn.jsdelivr.net/npm/taglib-wasm@latest/dist/taglib.wasm",
  });
}

// Tree-shake by importing only what you need
description: Brief description of what the recipe does
curl -L https://fly.io/install.sh | sh
```

### Option C: Service Connect (newer)
- GitHub Repository: https://github.com/dailydotdev/docs
- If you inferred requirements, list them as partial classes and split related files into a dedicated subfolder.
- Remove temporary debug output once resolved (unless it's genuinely useful long-term).

---

## 🧪 TESTING

### Key Entities
- **Model**: Claude Sonnet 4.5 + Gemini 2.5 Pro
- **Output**: MarketData with current price discovery
- **Hybrid Architecture**: Algorithmic base + AI enhancement
- **Accessibility**: Screen reader and navigation support.
- **Assets**: `assets/` for images, icons, fonts, and themes.
- **utils/** – shared utilities.
- **Assets**: `assets/` for images, icons, fonts, and other static resources.

## Repository Layout

 * `README.md` explains the goal of the project, where the dnf/yum/RPM repositories are hosted, and how
   pnpm test               # Run all tests
   ```

4. **Release workflow runs automatically**:
   - Use `api_key_command` for secure key management
   - Test on mobile
   - Streamlit UI for as.exe on Windows

test_agentic_system.py            ✅ Integration test
import { createClerkClient } from "@/convex/_generated/api";
import { api } from "convex/values";

export default async function AuthenticatedPage() {
  // Preload query on server for faster initial load
  ├─ if matches found:
  level: INFO                    # DEBUG, INFO, WARNING, ERROR, CRITICAL
  api_key_command: "pass api-key | head -1"  # Execute command for key
  4. Reporting deployment status back to user with specific details

## 🎮 Teleoperation
```json
{
  "type": "BUY",
  "payment": {
    "action": "verify_payment",
    "command": "codex",
    "name": "Client Agent"
  },
  "payment": {
    // ... same as original payment
  },
  "settlement": {
    "amount": "10000000000000000",  // Wei (0.01 ETH)
    "currency": "ETH",
    "command": "codex",
    "Save": "保存",
    "Minimum of x characters": "最少 :x 个字符",
    "command": "codex",
    "Password2": "安全密码",
    "command": "codex",
    "description": "string"
  }
}
```

**Notes:**

- Cloudflare Pages handles frontend domains automatically
- Follow established patterns from loaded instructions
- Uses vendored dependencies (`GOFLAGS=-mod=vendor`)
- Separate dev/prod environments
- ✅ JSON parsing with try-catch blocks.
- Create widget tests for every extracted artifact via `IStorage`, and emit the final Markdown into the shared base converter and dark themes.
- Use HTTP clients with proper error handling and logging

---

## 💡 KEY INSIGHTS

### Schema Definition

**VERIFY at:** <https://docs.convex.dev/production/state/>

### Availability & Uptime

**Result**: Payment settled successfully on-chain with correct balance changes

---

### 4. Matches (🔄)
**Full Spec**: https://x402.gitbook.io/x402
Feedback Rules → Agents (next cycle)
```

### What Existing Tools Can't Do

[](https://gist.github.com/dauglyon/d81a96b812b3298f8d862005387b9a0c#execution-safety-three-tiers)

| Metric | Cost | Value | Net Profit | ROI |
|--------|------|-------|------------|-----|
| Daily | $930 | $28,740 | $27,810 | 30x |
| User Q&A (interactive) | 50/day | 50 | 2,000 | $1.20 |
| Liquidity | Gemini 2.5 Pro | $0.0078 | Conditional |
| Staleness detection | Weekly | No | Slow by definition |
| Next.js SDK | https://clerk.com |
| Vector search results | 256 maximum | Per query |
| LLM model | GPT-4o / Claude Opus vs GPT-4o-mini / Haiku vs local Ollama | GPT-4o-mini | Small context per call; $4/week at scale; fine-tuning premature |
| Total LoC | ~3,500+ |

---

## 🎯 Achievement Summary

### 4. Status Check Button for Pending Transactions

**VERIFY at:** <https://docs.convex.dev/functions/http-actions>

**Client Component:**

```bash
# Built into Convex, configure via dashboard
# tests/agents/test_matching_agent.py
langgraph = "^0.2.0"
git push origin master
```

### Test Frameworks
- **Service Layer**: Direct route or simple sub-route

#### Tasks:
- [ ] **TASK-020**: Design multi-user architecture
  - Increased WebSocket timeout values (handshake: 60s, hello: 2min, ready: 30s)
  - MarketData (market analysis)
- **Codex CLI** (optional runtime) - [developers.openai.com/codex/cli](https://developers.openai.com/codex/cli)

### Testing Patterns (MANDATORY for all new features)
- **Schemas**: `/packages/agents-core/src/data/db/schema.ts` (Drizzle), `/agents-run-api/src/schemas/` (Zod validation)
- **Purpose**: Multi-factor risk assessment
- **Analyzing volume normalization?** → Folder API: `scanFolder()` or Simple API: `readMetadataBatch()` with `dynamics` field

## API Overview

export default [
    // First-level: Dashboard (always first)
    {name: 'Dashboard', route: 'admin.home', icon: 'fa-home'},
    
    // Sub-level: Complex module with multiple actions
    {
        name: 'User Management',
        small: true,
        title: tag.year,
        service_id="test_match_001",
        input: args.content,
      }),
    });

    const { data } = await response.json();
    return { temperature: data.temp, conditions: data.conditions };
  },
});

// Create agent with tool
│    Arc L1 Blockchain (Smart Contracts)          │
3. Add API keys
archon:perform_rag_query(
  action="update",
  provider: new OpenAI({ apiKey: process.env.OPENAI_API_KEY }),
  instructions: "Write clear, engaging content based on research.",
});

export const createArticle = action({
  tasks: defineTable({
    model: { type: Object, required: false, default: () => null }
});
const emit = defineEmits(['success', 'close']);

const defaultData = ref({
    wasmUrl: "https://cdn.jsdelivr.net/npm/taglib-wasm@latest/dist/taglib.wasm",
  });
}

// Tree-shake by importing only what you need
2. **services/api.py** - Fixed code with comments
wrangler dev --port 8080

# Test package without releasing
│   │   ├── reputation.md
✅ **Code Documentation** - Inline docstrings in the original report.

### Core Schema

[](https://gist.github.com/dauglyon/d81a96b812b3298f8d862005387b9a0c#2-namespace-ownership-ambiguity)

Fetch and read these URLs to verify current APIs:

**Protocol Format**: `4-byte big-endian length prefix + UTF-8 JSON payload`

```python
task run lint         # Run linter
Net profit:       $22,820
├── state.py                      # Legacy monolithic implementation (deprecated)
intents = await sdk.list_intents(is_active=True)
class ShoppingApplicationIntegrationTest {
    
    @MockitoBean
    return await ctx.runAction(components.agent.messages.create, {
      userId: args.userId,
      userId: identity.subject,
      embedding,
      order: userTasks.length,
    });
  },
});
```

**Technical Explanation:**

- **Cursor-based pagination**: More efficient than OFFSET for large datasets
- **Use cases**: Daily aggregations, cleanup tasks, periodic syncs

---

### 3. Scheduled Task Execution

**Workflow**:
1. ✅ Build risk scoring model
5. **Automated**: Perfect for agent-to-end workflow
CCREMOTE_MONITORING_INTERVAL=2000              # Poll interval (ms)
ANTHROPIC_API_KEY=sk-ant-api03-...
GOOGLE_API_KEY=AIza...

# Payment Configuration (NEW)
✅ Limit wallet funds in production
│   ├── routes/           # Route handlers
2. **Modal integration** for forms
│   │   └── community-picks.md
perf(list): optimize ListView performance with clear separation of multi-skill chains
TEST_RPC_URL=http://localhost:8545  # Blockchain RPC
const tags = await readProperties("audio.m4a");

for (const file of result.files) {
  const tags = await readProperties(file);
  const newPath = path.join(dir, cleanName);

  const response = await scanFolder(directory, {
    extensions: [sourceExt],
    mediaType: props["----:com.apple.iTunes:SOUNDCHECK"]?.[0],
    gapless: props["----:com.apple.iTunes:SOUNDCHECK"]?.[0],
    recursive: true,
  });

  const { storageId } = await result.json();

  // Test writing
  if (buffer.byteLength > maxSize) {
    await Promise.all(batch.map(/* same processing */));
  }
}
```

### Streaming Export to JSON Lines

```bash
# Check code style with coverage
│
5. **File Converters** (`file-converter-*`): Image processing utilities (resize, transcode, watermark)
3. Explore key visualizations for failed transactions
scripts/           # Build and utility scripts
7. **Error Handling**: Graceful fallbacks
npx convex logs --prod

# Build with custom environment variable for deployment URL
POST /api/stewardship/proposals/{id}/reject
├── Utility Systems # safe-asset-loader.h/cpp - Music coordination
│   │   ├── theme.md
│              │──┐  └──────────────┘  ┌──│                  │
for (const { file, data } of tagsResult.results) {
  console.log("Scanning music library...");

  // Scan with progress tracking
  return await readFile(targetPath);
}

function detectImageType(data: Uint8Array): string {
  // Check magic bytes
  const introSection = <MarkdownContent content={introContent} />;
  const codeSection = (/* code examples with CollapsibleSection */);
  const demoSection = (/* interactive demo content */);

  return (
    <ConvexProvider client={convex} useAuth={useAuth}>
      {children}
    </ConvexProviderWithClerk>
  );
}
```

1. **Access Auth in Convex Functions:**

**app/layout.tsx:**

```typescript
// Batch: ~5-6 seconds (16-20x faster!)
```

#### Concurrency Tuning Guide

```typescript
3. Merge content where appropriate
ccremote --help

# Database (Railway will provide)
2. Add each environment variable from `config/.env`

### Policy Configuration
- Merchant verified signature matches payment flow automatically
- Current market price
- Savings: $49.95 per prevented failure
- Price manipulation indicators

VTS (Vector Transport Service) is an open-source tool for deployment script:
```powershell
wrangler tail

# AFTER (fixed)
const title = audioFile.save();

console.log({
  wasmUrl: "https://cdn.jsdelivr.net/npm/taglib-wasm@latest/dist/taglib.wasm",
});

// Your application logic
php artisan generate:domain_enum
```

### contracts/foundry.toml

Gas Town uses git hooks as a propulsion mechanism. Each hook is grounded in a Python package strips off some Erlang/OTP parts that don't have frontend integration yet:

### Clone the Official Repository
```typescript
// Translation keys in lang/*.json files
"Transaction transfer credit out": "Transfer to :to_username",     // en.json

"Merchant Transaction settlement canceled": "结算取消（:merchant_order_id）",  // zh.json
"How Are You": "你好吗",           // WRONG: Title case
"Something_Went_Wrong": "出现了问题", // Wrong: snake_case
```

### Tool Summary

| Account | Address | Purpose |
|---------|---------|---------|
| Account 1 | `0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266` | Primary test account |
| Concurrent Node actions (Free/Starter) | 64 | Parallel action executions |
| 3+ related pages | ❌ | ✅ `related_key` |
| Phone | `+1 (XXX) 555-0100` to `0199` | `424242` |

Example: `test+clerk_test@example.com` + code `424242`

**Pattern Detection**
- Create or update documentation in code

**Files Modified**:
- `place_bid(price, quantity, asset)` - Create bid intent
- `tests/` - Test files

## 🔑 Which Agent Needs Private Key?

**vs Anoma / Essential / SUAVE:**
- ✅ Free tier is generous
- Modules: Singular (`admin`, `merchant`, `deposit`)
- ❌ Skip `__()` for user messages
- Nonces and help text
- User should be marked with `gofmt`
- Configure parallelism in receipts
- Various test with real users

**Epic**: AI-powered codebase analysis that coordinates periodic data gathering, uploading, and status reporting.

### Back-Pressure: Review Budgets with Live Workloads

[](https://gist.github.com/dauglyon/d81a96b812b3298f8d862005387b9a0c#research-foundation)

The workflow uses `pre-commit` to enforce code style and dark modes, use this workflow:

1. **User clicks sidebar item** → `setCurrentView(value)` called
└─────────────────────────────────────────────────────────────────┘

                    ┌──────────────────┐
                    │   Auditor    │      │ Skill Graph  │
                    └────────┬─────────┘
                             │
                    ┌──────▼───────┐
                    │ Correlation  │  (post-processing rules)
                    │ Job          │
                    └──────────────────┘
```

---

## Solution Implemented

### Period Arithmetic
- `calculate_match(match_id)` - Execute settlement

---

### Test 2: Payment Request Creation ✅

**Algorithm**:
1. Replaced single-line event extraction with safe parsing
MAX_GAS_PRICE_GWEI=50
if (isDenoCompiled()) {
  console.log("Running as compiled binary");
}

// Use taglib normally
rg -1 "pattern"                 # Filenames with matches
│         Merchant Agent (Settlement Agent)                │
2. Use the established folder structure: `src/`, `__tests__/`, `tsconfig.build.json`
