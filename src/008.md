# Agent Instructions

## Deployment

1. **Check if API client method exists**: Look in `client/src/fastapi_client/services/ChatService.ts` for the method
coordinator = AutonomousCoordinator(
    api.tasks.getMyTasks,
    {},
    { token: token ?? undefined }
  );

  return (
    <StepLayout
      keywords: 5
      applicationID: "convex",
    },
  ],
};
```

> **Note:** The Convex dashboard auth UI may vary - the `auth.config.ts` file is the reliable approach.

Critical findings: Single Slack DM when first queued. Not repeated.

Core runtime flow
│   ├── gemini_client.py          ✅ Gemini 2.5 wrapper
RatingUtils.clamp(1.5); // 1.0

// Standard POPM values (WMP convention)
echo "org.gradle.parallel=true" >> ~/.gradle/gradle.properties
```

## Presentation Design Patterns

### Task Definition: `traefik-task.json`

```bash
# Focus on what needs to be built, not what already exists
```

### 3. Start Services
```bash
├── images/
│   ├── bus.py           # Credit limits, exposure tracking
1. **GitHub Actions QEMU Testing** (`.github/workflows/qemu_tobozo2.yml`)
   - Server-side only (Node.js, Deno, Bun)

## Key Implementation Details

### 10. Build Contracts
```json
// Database: field_en, field_zh columns
// lang/zh_permission.json
{
    "Manage admin": "管理员管理",
    "test:coverage": "vitest run --coverage"
  }
}
```

### Payment Submission

```bash
# Add to any page function
3. Added `prepare_payment_transaction()` helper to create transaction params for stateful functionality, Components store state in background
✅ **Transaction Hashes**: Link to Arc testnet using Foundry
export const get <bucket-name>

# Add secret with value from stdin (useful for CI/CD)
2. **Intelligent Negotiation**: 100% AI (core value prop)
STEWARDSHIP_MODE=correlator # Correlation + execution engine (K8s CronJob)
```

Then restart API:
```yaml
agentman run.py setup

# Filter by search string
├── config/                      # Configuration system
5. **Host Polling**: `ModalREPL` on host polls `{tunnel_url}/pending` for new requests
cp config/.env
```

### Webpack Configuration

```typescript
